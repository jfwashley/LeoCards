---
phase: 12-pause-cards-in-active-deck-review-let-users-temporarily-excl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/0002_*.sql
  - drizzle/meta/0002_snapshot.json
  - drizzle/meta/_journal.json
autonomous: false
requirements: [P12-01]
user_setup: []

must_haves:
  truths:
    - "cards table has a nullable pausedAt timestamp column in src/db/schema.ts"
    - "A new drizzle migration file exists under drizzle/ adding ONLY the pausedAt column"
    - "The migration has been APPLIED to the live Neon database (npm run db:migrate)"
    - "All pre-existing rows have pausedAt = NULL (active)"
    - "Existing tests (Phases 1-11) still green; tsc clean"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "pausedAt: timestamp(\"pausedAt\") added to cards table between lastStudiedAt and createdAt"
      contains: "pausedAt: timestamp(\"pausedAt\")"
    - path: "drizzle/0002_*.sql"
      provides: "ALTER TABLE cards ADD COLUMN pausedAt timestamp"
      contains: "ADD COLUMN \"pausedAt\" timestamp"
    - path: "drizzle/meta/_journal.json"
      provides: "Journal entry referencing the new 0002 migration"
      contains: "0002"
  key_links:
    - from: "src/db/schema.ts cards table"
      to: "drizzle/0002_*.sql"
      via: "drizzle-kit generate output"
      pattern: "ALTER TABLE \"cards\" ADD COLUMN \"pausedAt\""
    - from: "drizzle/0002_*.sql"
      to: "Neon Postgres `cards` table"
      via: "npm run db:migrate (drizzle-kit migrate, reads _journal.json)"
      pattern: "Migration applied"
---

<objective>
Wave 1: add the nullable `pausedAt` timestamp column to the `cards` table — both
in the Drizzle schema and as a generated SQL migration applied to the live Neon
database. This is the foundation every subsequent plan depends on; no code that
reads or writes `pausedAt` can land until this is shipped.

Purpose: Establishes the single source of truth for pause state per D-02
(CONTEXT.md → "State representation in schema"): NULL = active, non-NULL =
paused since that instant. No backfill needed — existing rows are active by
definition.

Output: schema.ts edit + generated `drizzle/0002_*.sql` + `meta/0002_snapshot.json` +
updated `_journal.json` + the migration APPLIED to the live database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pause-cards-in-active-deck-review-let-users-temporarily-excl/12-CONTEXT.md
@.planning/phases/12-pause-cards-in-active-deck-review-let-users-temporarily-excl/12-RESEARCH.md
@AGENTS.md
@src/db/schema.ts
@drizzle/0001_ambitious_dark_beast.sql

<interfaces>
<!-- Existing cards table shape (verified — src/db/schema.ts lines 94-108).
     The new column slots in between lastStudiedAt and createdAt so it groups
     with the SRS time fields (cooldownUntil, lastStudiedAt, pausedAt). -->

export const cards = pgTable("cards", {
  id: text("id").primaryKey().$type<CardId>(),
  deckId: text("deckId").notNull().references(() => decks.id, { onDelete: "cascade" }),
  front: text("front").notNull(),
  back: text("back").notNull(),
  source: text("source").notNull(),
  recallCount: integer("recallCount").notNull().default(0),
  masteryRound: integer("masteryRound").notNull().default(0),
  cooldownUntil: timestamp("cooldownUntil"),
  lastStudiedAt: timestamp("lastStudiedAt"),
  // pausedAt: timestamp("pausedAt"),  ← NEW: insert here
  createdAt: timestamp("createdAt").notNull().defaultNow(),
  updatedAt: timestamp("updatedAt").notNull().defaultNow(),
});

<!-- Precedent — drizzle/0001_ambitious_dark_beast.sql line 2 added cooldownUntil
     with the exact same nullable-timestamp shape. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pausedAt column to schema.ts and generate the Drizzle migration</name>
  <read_first>
    - src/db/schema.ts (full file — confirm line numbers; the cards table is lines 94–108)
    - drizzle/0001_ambitious_dark_beast.sql (precedent: ADD COLUMN cooldownUntil timestamp)
    - drizzle/meta/_journal.json (version 7, postgresql, breakpoints true — drizzle-kit appends a new entry on generate)
    - 12-RESEARCH.md § "Drizzle migration workflow (verified against repo)" (the exact 5-step workflow + verbatim schema snippet)
    - AGENTS.md (heed deprecation notices; do NOT use db:push for this phase — the precedent is generate → migrate)
  </read_first>
  <files>src/db/schema.ts</files>
  <action>
    Step 1 — Edit `src/db/schema.ts`. Inside the `cards = pgTable("cards", { ... })`
    object, between `lastStudiedAt: timestamp("lastStudiedAt"),` and
    `createdAt: timestamp("createdAt").notNull().defaultNow(),` insert exactly
    one line:

        pausedAt: timestamp("pausedAt"), // null = active, non-null = paused at this instant

    Do NOT add a default. Do NOT add notNull. Do NOT add an index (12-RESEARCH.md
    "Index strategy — recommend DEFER" — zero indexes exist on cards today;
    introducing the first one out of scope for Phase 12). Do NOT touch any
    other table or any other column.

    Step 2 — Run `npm run db:generate`. This invokes drizzle-kit and is
    expected to produce:
      - `drizzle/0002_<adjective>_<name>.sql` containing ONLY
        `ALTER TABLE "cards" ADD COLUMN "pausedAt" timestamp;`
        (with statement-breakpoint comment per the existing 0001 file style)
      - `drizzle/meta/0002_snapshot.json` (new)
      - an updated `drizzle/meta/_journal.json` with the new entry appended

    Step 3 — Inspect the generated SQL. The ONLY DDL statement permitted is
    the ADD COLUMN line above. If drizzle-kit generates anything else (e.g. an
    unrelated diff because schema.ts has drifted from the snapshot), STOP and
    surface the diff to the user before proceeding to Task 2 — do not silently
    commit unrelated migrations.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c 'pausedAt: timestamp("pausedAt")' src/db/schema.ts && ls drizzle/0002_*.sql && grep -c 'ADD COLUMN "pausedAt" timestamp' drizzle/0002_*.sql</automated>
  </verify>
  <done>
    `grep -c 'pausedAt: timestamp("pausedAt")' src/db/schema.ts` == 1; a single
    new `drizzle/0002_*.sql` file exists; `grep -c 'ADD COLUMN "pausedAt" timestamp' drizzle/0002_*.sql` == 1;
    `drizzle/meta/0002_snapshot.json` exists; `_journal.json` has been updated;
    `npx tsc --noEmit` passes; no other migration files were created or modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply the migration to the live Neon database</name>
  <what-built>
    Task 1 produced schema.ts + a new drizzle/0002_*.sql migration file but has
    NOT yet touched the live Neon database. Schema-only code changes type-check
    without the migration applied, but every subsequent plan (12-02, 12-03)
    reads or writes `pausedAt` and will FAIL at runtime against an un-migrated
    DB. This checkpoint exists because the user must confirm they want to apply
    the DDL change to production-shared Neon before downstream plans proceed.
  </what-built>
  <how-to-verify>
    1. Inspect the generated SQL file:
         `cat drizzle/0002_*.sql`
       Confirm it contains exactly one DDL line:
         `ALTER TABLE "cards" ADD COLUMN "pausedAt" timestamp;`
       (statement-breakpoint comment is fine; no other DDL.)
    2. Apply the migration:
         `npm run db:migrate`
       Expected output: drizzle-kit reports the new migration applied and the
       journal advances. No errors, no rollback.
    3. Sanity-check the live schema picked it up (optional but recommended) by
       running a `\d cards` via the Neon console OR a trivial select:
         `SELECT "pausedAt" FROM cards LIMIT 1;`
       Should return rows (or empty) with NULL `pausedAt`, no "column does not
       exist" error.
    4. Confirm existing app still works: `npm run build` and `npm test` both
       green (existing query paths unchanged because no code yet reads the new
       column).
  </how-to-verify>
  <resume-signal>Type "applied" once `npm run db:migrate` ran successfully and the new column is present in Neon. Type "issue: <details>" if anything went sideways (e.g. drift detected, migration failed, unrelated DDL appeared).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drizzle migration → live Neon DB | DDL change adds a nullable column; no privileged data crosses this boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | DDL applied to shared Neon DB | mitigate | Migration file is reviewed before apply (Task 2 step 1); the file contains a single ADD COLUMN — no DROP, no data-touching statements. Generated by drizzle-kit, not hand-written. |
| T-12-02 | Denial of Service | ADD COLUMN on cards table | accept | `cards` table is small per user (dozens to low hundreds of rows project-wide is well under Neon's ALTER TABLE constraints for nullable-without-default columns; Postgres metadata-only operation, no rewrite). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO new packages (12-RESEARCH.md § "Package Legitimacy Audit"). No slopcheck required. |
</threat_model>

<verification>
- `grep -c 'pausedAt: timestamp("pausedAt")' src/db/schema.ts` == 1.
- A single new `drizzle/0002_*.sql` file present containing only the ADD COLUMN
  statement; `drizzle/meta/0002_snapshot.json` present; `_journal.json` updated.
- `npm run db:migrate` ran successfully (BLOCKING checkpoint).
- `npx tsc --noEmit` passes.
- `npm test` — full prior suite (Phases 1–11) still green.
</verification>

<success_criteria>
- The `cards` table in the live Neon DB has a nullable `pausedAt` timestamp column.
- All existing rows have `pausedAt = NULL` (no backfill needed).
- The Drizzle schema, the migration SQL, the snapshot, and `_journal.json` are
  all in sync and committed together as one atomic change.
- No regression in the Phases 1–11 test suite.
</success_criteria>

<output>
After completion, create
`.planning/phases/12-pause-cards-in-active-deck-review-let-users-temporarily-excl/12-01-SUMMARY.md`.
Record explicitly: the exact generated migration filename
(`drizzle/0002_<adjective>_<name>.sql`), confirmation the migration was applied
to the live DB (date + by whom), and the exact line number in schema.ts where
`pausedAt` lives — Plans 12-02/12-03 will reference both.
</output>
</content>
</invoke>