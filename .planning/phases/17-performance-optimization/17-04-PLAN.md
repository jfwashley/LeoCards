---
phase: 17-performance-optimization
plan: 04
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - src/app/(protected)/study/page.tsx
  - src/app/(protected)/deck/new-card/page.tsx
  - src/app/(protected)/deck/browse/page.tsx
  - src/components/study-session.tsx
  - src/components/card-list.tsx
  - src/components/daybreak/ac-progress.tsx
  - src/components/welcome/habitat-teaser.tsx
  - src/app/globals.css
autonomous: false

requirements: [PERF-03]

must_haves:
  truths:
    - "D-06: study/new-card/browse page trees are audited for the same oversized-client-boundary pattern as the dashboard and converted client→RSC where behaviour is identical; the /study route ALWAYS gets ?deck={id} on any prefetch/link wiring (it unconditionally redirects to /dashboard without it — the only route lacking a graceful default)"
    - "D-05: Motion entrances/fades are swapped to CSS ONLY where visually identical — study-session mount fade, card-list accordion, ac-progress indeterminate bar, habitat-teaser glow pulse; study-card.tsx drag physics and level-up-overlay.tsx confetti are load-bearing and are NOT touched (confirmed and skipped); each CSS swap preserves its existing reduced-motion branch"
    - "D-03: any client component not needed for initial paint (e.g. CardEditDialog on edit-click, ImageUploadFlow on the upload path) is lazy-loaded via next/dynamic with loading: () => <DaybreakShimmer/> — the single placeholder style from 17-01; CLS 0 holds via reserved space"
    - "Each of study/new-card/browse real-content roots gains data-perf-ready='true' for the Wave-5 D-15 signal; the ?topic= validation pattern (WR-01) is preserved for browse"
    - "PERF-03 per-route gates: warm-prod mobile re-measured against the Phase 16 baseline for /study (TBT 712→≤200, Perf 82→≥90), /deck/new-card (TBT 891→≤200, Perf 79→≥90 — worst route), /deck/browse (TBT 608→≤200, Perf 84→≥90); LCP/CLS still pass; any route that provably cannot reach its gate within D-01..03 fidelity raises the D-04 checkpoint with measured evidence"
    - "D-10: correctness gates per risky wave — full e2e after this wave, and qa:run because this wave touches study/SRS/data paths and moves client→RSC boundaries (fresh dev server; qa-01/qa-03 need a STUDY_COOLDOWN_MINUTES=1 boot)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "New CSS @keyframes for the swapped Motion animations, each with a prefers-reduced-motion override (hab-fall convention)"
      contains: "prefers-reduced-motion"
    - path: "src/components/card-list.tsx"
      provides: "Accordion via CSS (or lightweight class-toggle) preserving the reduced-motion branch; CardEditDialog lazy-loaded with DaybreakShimmer fallback"
      contains: "DaybreakShimmer"
    - path: "src/app/(protected)/study/page.tsx"
      provides: "Audited/converted study route with data-perf-ready marker"
      contains: "data-perf-ready"
  key_links:
    - from: "src/components/card-list.tsx"
      to: "src/components/daybreak/shimmer.tsx"
      via: "next/dynamic loading fallback"
      pattern: "DaybreakShimmer|dynamic\\("
    - from: "src/components/study-session.tsx"
      to: "src/app/globals.css"
      via: "CSS keyframe replaces the Motion mount fade, reduced-motion preserved"
      pattern: "@keyframes|animation"
---

<objective>
Apply the D-06 client→RSC lever and D-05 Motion→CSS hygiene to the remaining three gate-failing routes — study, new-card (the worst route at 891ms TBT), and browse — then re-measure each against the Phase 16 baseline. Where a route's page tree wraps static markup in an oversized client boundary (the dashboard pattern), convert it to RSC. Where Motion animates a simple, non-load-bearing entrance/fade (study-session mount, card-list accordion, ac-progress bar, habitat-teaser glow), swap to a CSS keyframe following the proven hab-fall convention. Where a client component is not needed for initial paint (CardEditDialog, ImageUploadFlow), lazy-load it behind the single DaybreakShimmer placeholder.

Purpose: These three routes inherit the same shared floor as the dashboard but have their own per-route client boundaries and Motion usages contributing to the 608–891ms mobile TBT. RSC conversion moves hydration off the main thread (the primary TBT lever); CSS swaps remove Motion's JS weight where it buys nothing visual. new-card is the worst route (1111 KB, 891ms TBT) so it has the most headroom to recover. Load-bearing Motion (study-card drag physics, level-up confetti) is explicitly OUT per D-05 — confirm and skip, do not touch.

Output: RSC-converted (where applicable) study/new-card/browse pages with data-perf-ready markers; four CSS keyframe swaps in globals.css each with a reduced-motion override; lazy-loaded CardEditDialog/ImageUploadFlow behind DaybreakShimmer; route-scoped re-measurements for the three routes. autonomous:false — carries the D-04 gate-vs-fidelity checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-performance-optimization/17-CONTEXT.md
@.planning/phases/17-performance-optimization/17-RESEARCH.md
@.planning/phases/17-performance-optimization/17-PATTERNS.md
@.planning/phases/16-performance-baseline-measure/baseline/study-baseline.md
@.planning/phases/16-performance-baseline-measure/baseline/deck-new-card-baseline.md
@.planning/phases/16-performance-baseline-measure/baseline/deck-browse-baseline.md
@AGENTS.md

<interfaces>
<!-- Contracts consumed from prior plans. -->

DaybreakShimmer (from 17-01, src/components/daybreak/shimmer.tsx):
  - the ONE D-03 placeholder; use as `dynamic(() => import("@/components/card-edit-dialog"), { loading: () => <DaybreakShimmer .../> })`

CSS keyframe convention (from 17-01 shimmer + existing hab-fall, src/app/globals.css):
  - named @keyframes in globals.css + paired @media (prefers-reduced-motion: reduce) override; per-instance timing via inline style.animation; class applied via className

data-perf-ready marker (from 17-01 e2e/perf-markers.ts):
  - add data-perf-ready="true" to each of study/new-card/browse real-content roots (the element present only once real data is painted)

/study route gotcha:
  - /study unconditionally redirects to /dashboard when ?deck= is absent — any link/prefetch to /study MUST include ?deck={id}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + convert study/new-card/browse client boundaries to RSC; lazy-load non-critical client components behind DaybreakShimmer; add data-perf-ready markers</name>
  <files>src/app/(protected)/study/page.tsx, src/app/(protected)/deck/new-card/page.tsx, src/app/(protected)/deck/browse/page.tsx, src/components/card-list.tsx</files>
  <read_first>
    - src/app/(protected)/study/page.tsx (lines 20-22 the unconditional ?deck= redirect gotcha; the auth+data-assembly shell)
    - src/app/(protected)/deck/new-card/page.tsx (the shell; new-card-mode-toggle.tsx imports ImageUploadFlow eagerly at line 8 — a lazy-load candidate since it is only needed on the upload path)
    - src/app/(protected)/deck/browse/page.tsx (lines 32-37 the ?topic= WR-01 validation against CATEGORIES that MUST be preserved)
    - src/components/card-list.tsx (line 8 imports CardEditDialog eagerly — only needed on edit-click; a next/dynamic lazy-load candidate)
    - src/components/new-card-mode-toggle.tsx (line 8 ImageUploadFlow eager import — lazy-load candidate)
    - src/components/habitat-scene.tsx (lines 74-77 the ONLY existing next/dynamic usage — the pattern to follow, though it does not use loading:; the D-03 shimmer work is the first real loading: usage)
    - .planning/phases/17-performance-optimization/17-RESEARCH.md (Pattern 3 next/dynamic + shimmer host; the per-route audit recommendation; the study-route ?deck= caveat)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the four route sections; the study-route redirect gotcha; the shimmer next/dynamic host candidates)
    - e2e/perf-markers.ts (the data-perf-ready convention from 17-01)
  </read_first>
  <action>
    For each of study/new-card/browse: read the page tree and identify oversized client boundaries (a "use client" component wrapping mostly-static markup for one or two interactive pieces — the dashboard/DeckView pattern). Convert to RSC where behaviour is identical: server-render the static shell, keep only genuinely-interactive descendants as client leaves. Follow the habitat-page/scene split precedent (already applied to the dashboard in 17-03). If a route's tree has NO oversized client boundary (it may already be lean), record that finding and move on — do not manufacture a conversion.
    Lazy-load client components not needed for the initial paint via next/dynamic with `loading: () => <DaybreakShimmer .../>` (the single 17-01 placeholder): CardEditDialog (imported eagerly by card-list.tsx line 8, only needed on edit-click) and ImageUploadFlow (imported eagerly by new-card-mode-toggle.tsx line 8, only needed on the upload-mode path). Size each shimmer to reserve the space the real component occupies so CLS stays 0.
    PRESERVE the browse ?topic= validation (WR-01): the CATEGORIES allowlist check at browse/page.tsx lines 32-37 must remain — an unrecognized/stale topic falls back to the tiles landing. Do not drop or weaken it during any RSC conversion.
    For /study prefetch/link wiring: any link to /study MUST carry ?deck={id} (it unconditionally redirects to /dashboard without it). This matters for the Wave-5 nav gate but confirm here that no conversion breaks the existing ?deck handling.
    Add `data-perf-ready="true"` to each of the three routes' real-content roots (study-card front / card-list rows / browse tiles — the element present only once real data is painted, not a skeleton). This is the Wave-5 D-15 signal.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (all three routes' RSC conversions + lazy-loads build; the module-boundary checker fails the build on any illegal server→client import, so green build = boundary-correct)
    - `npx tsc --noEmit` exits 0
    - `grep -n "dynamic(" src/components/card-list.tsx src/components/new-card-mode-toggle.tsx` shows next/dynamic used AND `grep -n "DaybreakShimmer" src/components/card-list.tsx src/components/new-card-mode-toggle.tsx` matches (the shimmer is the loading fallback) — for whichever lazy-loads were applied
    - `grep -n "CATEGORIES" src/app/(protected)/deck/browse/page.tsx` still matches (WR-01 ?topic= validation preserved)
    - `grep -rln "data-perf-ready" src/app/(protected)/study src/app/(protected)/deck/new-card src/app/(protected)/deck/browse src/components` shows the marker present for all three routes' content roots
    - `npx biome ci` on all touched files reports no NEW errors
  </acceptance_criteria>
  <done>The three routes' oversized client boundaries are RSC-converted where applicable, non-critical client components lazy-load behind the single shimmer, WR-01/?deck gotchas are preserved, and each route carries the content-visible marker.</done>
</task>

<task type="auto">
  <name>Task 2: D-05 Motion→CSS swaps (study-session fade, card-list accordion, ac-progress bar, habitat-teaser glow); confirm-and-skip the load-bearing Motion usages</name>
  <files>src/components/study-session.tsx, src/components/card-list.tsx, src/components/daybreak/ac-progress.tsx, src/components/welcome/habitat-teaser.tsx, src/app/globals.css</files>
  <read_first>
    - src/components/study-session.tsx (lines 351-356 the mount fade motion.div — a one-shot entrance, strong CSS-swap candidate)
    - src/components/card-list.tsx (lines 292-303 the AnimatePresence accordion with height/opacity + the CRITICAL overflow:hidden + the reduced ? 0 : 0.22 branch that MUST carry over)
    - src/components/daybreak/ac-progress.tsx (lines 125-136 the indeterminate bar translateX loop — repeating, classic CSS keyframe candidate)
    - src/components/welcome/habitat-teaser.tsx (lines 126-137 the ambient glow opacity 0→1→0 loop + the reduced early-return gate at lines 113/125 that MUST carry over)
    - src/components/habitat-celebration.tsx (lines 70-84 + globals.css lines 110-127 — the PROVEN CSS-only reference: named keyframe + prefers-reduced-motion override + inline style timing)
    - src/components/study-card.tsx (lines 3, 38-45 — LOAD-BEARING drag physics via useMotionValue/useTransform; D-05 KEEP; confirm-and-skip, NO conversion)
    - src/components/level-up-overlay.tsx (lines 3, 53-76 — LOAD-BEARING confetti, D-05's explicit exception; confirm-and-skip, NO conversion)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the CSS-only keyframe convention shared-pattern; the four swap sections + the two KEEP sections with exact line refs)
  </read_first>
  <action>
    Swap these four Motion usages to CSS keyframes, each following the hab-fall convention (named @keyframes in globals.css + paired prefers-reduced-motion override; per-instance timing via inline style where needed):
    (a) study-session.tsx lines 351-356: the mount fade (opacity 0→1, y 16→0 over 0.4s ease-out) → a `fade-up` keyframe + `animation: fade-up 0.4s ease-out`. This is a one-shot mount transition — the simplest case.
    (b) card-list.tsx lines 292-303: the accordion (height 0→auto, opacity 0→1, with exit). This is the involved case — AnimatePresence handles BOTH mount and unmount. Use a CSS approach that handles open/close without a JS-driven unmount delay: either the `grid-template-rows: 0fr → 1fr` trick or a lightweight class-toggle with transitionend cleanup. PRESERVE the `reduced ? 0 : 0.22` reduced-motion branch (instant when reduced) and keep overflow:hidden. Budget accordingly — this is more than a one-liner.
    (c) ac-progress.tsx lines 125-136: the indeterminate bar (translateX -100%→100% infinite loop) → a `slide` keyframe + `animation: slide 1.2s ease-in-out infinite`.
    (d) habitat-teaser.tsx lines 126-137: the glow pulse (opacity 0→1→0 over 4s infinite) → an opacity keyframe loop; the existing `reduced` early-return (lines 113/125) MUST carry over unchanged to the CSS version.
    For EACH swap: if the component no longer uses `motion/react` at all after the swap, remove the now-unused import (a real client-bundle reduction). If it still uses Motion elsewhere, leave the import.
    CONFIRM-AND-SKIP (do NOT touch): study-card.tsx (drag physics — load-bearing) and level-up-overlay.tsx (confetti — D-05's explicit exception). Record in the SUMMARY that both were confirmed load-bearing and intentionally left on Motion.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 after all four swaps
    - `npm test` (full vitest) passes — the swapped components' existing tests still pass (bw-atoms.test.tsx covers ac-progress; if it flakes on parallel-load 5s-timeout, re-run that single file in isolation before treating it as a regression per the known-flaky list)
    - For each of study-session.tsx, card-list.tsx, ac-progress.tsx, habitat-teaser.tsx: `grep -c "prefers-reduced-motion\|reduced" <file> globals.css` confirms a reduced-motion path still exists for that animation (no swap dropped its reduced-motion branch)
    - `grep -n "useMotionValue\|useTransform" src/components/study-card.tsx` STILL matches (study-card drag physics untouched) and `grep -n "motion" src/components/level-up-overlay.tsx` STILL matches (confetti untouched) — the load-bearing usages were confirmed and skipped
    - New `@keyframes` for the swapped animations exist in globals.css, each with a prefers-reduced-motion override (grep the keyframe names + a nearby reduced-motion block)
    - `npx biome ci` on all touched files reports no NEW errors (note: biome's no-`!` rule — do not introduce non-null assertions in the swaps)
  </acceptance_criteria>
  <done>Four non-load-bearing Motion animations are CSS keyframes with reduced-motion preserved; the two load-bearing Motion usages are confirmed and untouched; unused Motion imports removed where a component fully dropped Motion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: /study, /deck/new-card, /deck/browse warm-prod re-measurement vs Phase 16 baseline + D-04 gate-vs-fidelity</name>
  <action>PAUSE for human verification. Run the /study, /deck/new-card, /deck/browse route-scoped warm-prod re-measurements (see how-to-verify), present each route mobile medians vs its Phase 16 baseline, confirm the CSS swaps are visually identical, and either confirm each gate is met or raise per-route D-04 decisions with measured evidence. Do not self-approve.</action>
  <files>.planning/phases/17-performance-optimization/measurements/ (route-scoped reports for the 3 routes; no source files)</files>
  <done>All three route gates met (TBT ≤200, Perf ≥90) or per-route D-04 decisions recorded; CSS swaps visually identical with reduced-motion intact; immutable baseline shows zero git diff.</done>
  <what-built>
    The study/new-card/browse RSC conversions + lazy-loads + the four D-05 Motion→CSS swaps, plus route-scoped warm-prod re-measurements of /study, /deck/new-card, and /deck/browse against the Phase 16 baseline. Carries the D-04 gate-vs-fidelity trigger (new-card is the worst route — most likely to surface a gate-vs-fidelity conflict).
  </what-built>
  <how-to-verify>
    1. The executor runs route-scoped warm-prod measurements for the three routes using the route-filtered harness (mobile-first, DATABASE_URL exported, ~7 min/route, self-cleans — run via run_in_background). Can batch: ROUTE_FILTER=/study,/deck/new-card,/deck/browse.
    2. Compare each route's mobile medians against its Phase 16 baseline:
       - /study: baseline TBT 712ms, Perf 82 → gate TBT ≤200, Perf ≥90
       - /deck/new-card: baseline TBT 891ms, Perf 79 (WORST) → gate TBT ≤200, Perf ≥90
       - /deck/browse: baseline TBT 608ms, Perf 84 → gate TBT ≤200, Perf ≥90
       - all three: LCP ≤2500, CLS ≤0.1 (already passing — confirm still passing)
       - PASS: approve, note headroom per route (D-12: gates pass = done, no gold-plating).
       - D-04 CONFLICT: if a route provably cannot reach TBT ≤200 / Perf ≥90 within D-01..03 fidelity, the executor presents measured evidence + a SPECIFIC proposed visible change for that route; Josh approves/vetoes per case.
    3. Confirm the four CSS-swapped animations still look identical to the Motion originals (study-session mount fade, card-list accordion open/close, ac-progress bar, habitat-teaser glow) — D-05's bar is "indistinguishable". Test with reduced-motion on to confirm each still respects it.
    4. Confirm `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` is EMPTY.
  </how-to-verify>
  <resume-signal>Type "approved" if all three route gates are met and the CSS swaps are visually identical; or approve/veto specific D-04 proposed changes per route; or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → client leaf | RSC conversions on the three routes move client boundaries; server-only/QA-gated data must not leak into client props |
| browse ?topic= param → render | untrusted ?topic= query param crosses into the render path; must stay validated against CATEGORIES (WR-01) |
| harness → prod Neon DB | route-scoped measurements provision + self-clean *test.local users via DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Tampering | browse ?topic= query param | mitigate | The CATEGORIES allowlist validation (WR-01, browse/page.tsx lines 32-37) is preserved through any RSC conversion; acceptance criterion greps to confirm CATEGORIES check survives — an unvalidated ?topic= must not reach the render as a nonsense state |
| T-17-04-02 | Information Disclosure | server→client boundary on 3 routes | mitigate | Next build-time module-boundary enforcement fails the build on illegal server-only imports crossing to the client; green `npm run build` is the proof; any QA-gated field stays server-side per the dashboard precedent |
| T-17-04-03 | Tampering | route-scoped measurement OUT_DIR | mitigate | uses the Plan-01 redirected default; checkpoint step 4 requires zero git diff under the immutable baseline/ |
| T-17-04-SC | Tampering | npm installs | accept | No new packages (subtractive/conversion/CSS-swap work only); no legitimacy checkpoint needed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after conversions + swaps.
- `npm run build` exits 0 (server/client boundary correctness for all three routes).
- `npm test` (full vitest) passes; known flakes isolated before being treated as regressions.
- Route-scoped /study, /deck/new-card, /deck/browse warm-prod measurements written to the Phase-17 measurements dir; compared against baselines at the D-04 checkpoint.
- `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` EMPTY.
- Scoped `npx biome ci <touched files>` shows no new errors.
- WAVE GATE (after this wave): full `npx tsc --noEmit` + full `npm test` + full `npm run test:e2e` (per-project web then mobile, fresh dev server) + `npm run qa:run` (this wave touches study/SRS paths and moves client→RSC boundaries — D-10 requires qa:run; fresh dev server; qa-01/qa-03 need STUDY_COOLDOWN_MINUTES=1 server boot per the Phase 15 regime). Re-run full tsc AGAIN if any e2e spec is touched during gate-fixing (biome's no-`!` rule can push a `box?.y` into an argument position → tsc error).
</verification>

<success_criteria>
study/new-card/browse oversized client boundaries are RSC-converted where applicable; non-critical client components lazy-load behind the single shimmer; four non-load-bearing Motion animations are CSS with reduced-motion preserved; the two load-bearing Motion usages are confirmed and untouched; each route's warm-prod mobile gate is met (TBT ≤200, Perf ≥90) OR a D-04 checkpoint was raised with measured evidence; WR-01/?deck gotchas preserved; the immutable baseline is untouched.
</success_criteria>

<output>
Create `.planning/phases/17-performance-optimization/17-04-SUMMARY.md` when done.
</output>
