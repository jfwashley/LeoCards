---
phase: 17-performance-optimization
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - next.config.ts
  - package.json
  - src/components/habitat-widget.tsx
  - src/components/habitat-3d-widget-image.tsx
autonomous: true
requirements: [PERF-03]

must_haves:
  truths:
    - "D-05: dead weight in the client graph is removed outright — three/@types/three move from dependencies to devDependencies (a package.json hygiene fix; RESEARCH Pitfall 2 confirms three is ALREADY absent from the prod client bundle, so this is 0 KB delta by design, NOT a bundle-KB lever); confirmed-dead components habitat-widget.tsx + habitat-3d-widget-image.tsx are deleted only after zero call sites are re-confirmed"
    - "D-07: next.config stable options land freely with before/after numbers; anything the shipped Next 16 docs (node_modules/next/dist/docs) mark experimental REQUIRES a checkpoint with evidence first and does NOT land in this plan — lucide-react is already on Next's default optimizePackageImports list (zero config, zero experimental exposure)"
    - "D-08: shared infrastructure (root layout, auth client) is audited for the ~9 shared chunks all four routes inherit; wins here compound across every route — but NO change lands unless it is stable and behavior-identical, guarded by the /habitat spot-check and existing auth-screen e2e"
    - "A fresh production build after this plan's changes still shows zero three matches in .next/static/chunks and the shared-chunk floor is measured so Wave 3's per-route work has a fresh shared-floor reference"
    - "D-11: one /habitat regression spot-check (cheap mobile n=3 run) after the shared-bundle refactors land — /habitat stays OUT of the key-route gate set (Phase 16 D-03 exclusion stands); this is the revisit-if-refactors-touch-habitat clause firing"
  artifacts:
    - path: "package.json"
      provides: "three + @types/three relocated to devDependencies"
      contains: "devDependencies"
    - path: "next.config.ts"
      provides: "Only stable, docs-verified config options (or intentionally unchanged if none apply without a checkpoint)"
      exports: []
  key_links:
    - from: "package.json"
      to: "build-time-only three usage"
      via: "devDependencies classification (three never ships to client)"
      pattern: "\"three\""
    - from: "next.config.ts"
      to: "node_modules/next/dist/docs"
      via: "docs-verified stable-only option selection (no experimental keys)"
      pattern: "NextConfig"
---

<objective>
Bank the shared-infrastructure hygiene wins that compound across all four routes, and clear confirmed dead weight — WITHOUT touching any experimental Next 16 surface. This plan does the D-05 dependency hygiene (relocate `three`/`@types/three` to devDependencies; delete the two confirmed-dead habitat-widget files), the D-07 next.config work (stable, docs-verified options only — experimental keys are a hard stop requiring a checkpoint), and the D-08 shared-chunk audit of root layout + auth client. It ends by producing a fresh production build and a route-scoped measurement of the shared floor so Wave 3's per-route conversions start from a known-fresh reference.

Purpose: RESEARCH identifies a 514.72 KB shared floor present on every authenticated route before any route-specific code loads — cutting it is the single highest-leverage move (D-08). But RESEARCH also corrects two premises: `three` is ALREADY absent from the client bundle (so its "removal" is package.json hygiene, 0 KB delta by design — do NOT promise a bundle win), and every Cache Components / experimental flag requires a D-07 checkpoint. This plan lands only what is unambiguously safe and stable.

Output: package.json with three relocated; two dead files deleted; next.config.ts with only stable docs-verified options (or unchanged with a documented reason); a fresh `npm run build` proving three still absent; a route-scoped measurement written to the Phase-17 measurements dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-performance-optimization/17-CONTEXT.md
@.planning/phases/17-performance-optimization/17-RESEARCH.md
@.planning/phases/17-performance-optimization/17-PATTERNS.md
@.planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-05 dependency hygiene — relocate three/@types/three to devDependencies + delete confirmed-dead habitat-widget files</name>
  <files>package.json, src/components/habitat-widget.tsx, src/components/habitat-3d-widget-image.tsx</files>
  <read_first>
    - package.json (dependencies block has "three": "^0.160.1" and "@types/three": "^0.160.0"; devDependencies block is the target section — per pattern map these are at ~lines 36, 52 in dependencies)
    - src/components/habitat-widget.tsx (DELETE candidate — read to confirm what it imports so you know what a broken import would look like post-delete)
    - src/components/habitat-3d-widget-image.tsx (DELETE candidate — only reachable via habitat-widget.tsx; its line-15 comment falsely claims preload replaces priority, per RESEARCH — do not propagate that into any surviving code)
    - .planning/phases/17-performance-optimization/17-RESEARCH.md (Pitfall 2 = three is already absent so 0 KB delta by design; Pitfall 6 = confirm zero call sites via multi-pattern grep + tsc before deleting)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the package.json section + the two DELETE-candidate sections with the exact grep-then-tsc confirmation protocol)
  </read_first>
  <action>
    First, RE-CONFIRM the two habitat-widget files are truly dead before deleting: run a multi-pattern grep across src/ for the export name `HabitatWidget`, the filename `habitat-widget`, the filename `habitat-3d-widget-image`, and any barrel re-exports. The only expected match is a comment-only cross-reference in habitat-medallion.tsx (`// ...mirrors habitat-widget.tsx...`) which is NOT an import. If any real import/import() site appears, STOP and do not delete — instead report it. If confirmed dead, delete both src/components/habitat-widget.tsx and src/components/habitat-3d-widget-image.tsx.
    Then relocate in package.json: move `"three": "^0.160.1"` and `"@types/three": "^0.160.0"` from the dependencies block into the devDependencies block. Exact alphabetical placement in devDependencies is low-stakes (that block is not strictly sorted). Change nothing else in package.json. This is a classification fix per D-05/PROJECT.md ("Three.js runs only in the build-time render pipeline") — it is expected to produce 0 KB client-bundle delta, and that is correct, not a bug (RESEARCH Pitfall 2).
    Run `npx tsc --noEmit` immediately after the deletions — a broken import from a supposedly-dead file surfaces as a type error instantly (the definitive dead-code confirmation per Pitfall 6).
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 AFTER deleting both files (proves nothing imported them — the definitive dead-code test per Pitfall 6)
    - `grep -rn "habitat-widget\|habitat-3d-widget-image\|HabitatWidget" src/` returns ONLY the comment-only reference in habitat-medallion.tsx (no import/import() sites)
    - In package.json, `"three"` and `"@types/three"` appear under devDependencies and NOT under dependencies (verify both blocks)
    - `npm test` (full vitest) passes — no test depended on the deleted components. If cooldown-config.test.ts or bw-atoms.test.tsx flake (parallel-load 5s-timeout flakes per the known-flaky list), re-run that single file in isolation before treating it as a regression
    - `npx biome ci package.json` reports no NEW errors (package.json IS in biome scope only because the path contains a slash — include it explicitly)
    - Files src/components/habitat-widget.tsx and src/components/habitat-3d-widget-image.tsx no longer exist
  </acceptance_criteria>
  <done>three is classified build-time-only; the two confirmed-dead widget files are gone; type-check and unit suite prove nothing broke.</done>
</task>

<task type="auto">
  <name>Task 2: D-07 next.config stable-only options + D-08 shared-infra audit (root layout, auth client)</name>
  <files>next.config.ts, src/app/layout.tsx, src/lib/auth-client.ts</files>
  <read_first>
    - next.config.ts (currently a bare NextConfig object with zero keys — no repo precedent; must be docs-driven)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/optimizePackageImports.md (confirm version: experimental frontmatter AND confirm lucide-react is already on the default-optimized list — so NO explicit config is needed for it)
    - node_modules/next/dist/docs/01-app/02-guides/package-bundling.md (serverExternalPackages + the stable bundle levers; check which are stable vs experimental)
    - src/app/layout.tsx (the root layout — the shared-chunk root per pattern map; audit for any obvious client-boundary or heavy import that all routes inherit)
    - src/lib/auth-client.ts (the single client-side auth singleton — the other D-08 shared touchpoint)
    - .planning/phases/17-performance-optimization/17-RESEARCH.md (Pitfall 3 = experimental-flag over-adoption; Standard Stack "Alternatives Considered" = optimizePackageImports for NEW packages needs a checkpoint but manual named-imports do not; the anti-pattern list forbidding cacheComponents/use cache/unstable_instant without D-07)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (next.config.ts section = docs-driven not analog-driven; the shared-patterns auth-session-gate section that must stay unchanged)
  </read_first>
  <action>
    D-07 next.config: consult node_modules/next/dist/docs directly (per AGENTS.md — this Next 16 differs from training data). ONLY add options whose docs page does NOT carry `version: experimental` or `version: draft` frontmatter. Concretely: confirm lucide-react is already covered by Next's DEFAULT optimizePackageImports list — if so, add NO config for it (it is already tree-shaken; touching the experimental optimizePackageImports config surface would itself need a checkpoint). Consider `serverExternalPackages` only if a specific server-only package is being pulled into a bundle incorrectly AND its docs page is stable. If, after reading the docs, there is NO stable option that safely reduces the shared floor without a checkpoint, leave next.config.ts UNCHANGED and record that decision in the SUMMARY (an empty-but-verified config is a valid outcome — do not invent config to look productive). If ANY option you want is marked experimental (cacheComponents, experimental.*, unstable_instant, etc.), STOP and raise a D-07 checkpoint with the measured before/after evidence and the specific flag proposed — do NOT land it.
    D-08 shared-infra audit: read src/app/layout.tsx and src/lib/auth-client.ts. Identify whether either forces an avoidable client boundary or imports something heavy that every route inherits. If a stable, behavior-identical reduction exists (e.g. an unnecessary "use client" on a layout-level wrapper, or a heavy import that can be dynamically loaded), note it — but for THIS plan only LAND changes that are unambiguously stable and behavior-preserving. Anything requiring an experimental API is a checkpoint. The auth session-gate pattern (auth.api.getSession → redirect) must remain byte-for-byte in behavior; the /habitat spot-check (Task 3) and existing auth-screen e2e are the guards.
    Whatever lands (config option, layout tweak, or nothing), the reasoning MUST be recorded in the SUMMARY with the docs citation for why it is stable.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "experimental\|cacheComponents\|unstable_instant\|use cache" next.config.ts` returns NOTHING (zero experimental surface landed without a checkpoint — hard D-07 gate)
    - `npm run build` completes successfully (exit 0) with the current next.config.ts
    - `npx tsc --noEmit` exits 0
    - If next.config.ts was changed, the SUMMARY cites the specific node_modules/next/dist/docs page confirming the option is stable (non-experimental). If unchanged, the SUMMARY states that no stable option applied without a checkpoint (a valid outcome)
    - The auth session-gate logic in src/app/layout.tsx (auth.api.getSession → redirect("/login")) is unchanged in behavior (grep confirms the getSession + redirect calls still present if layout was touched)
    - `npx biome ci next.config.ts src/app/layout.tsx src/lib/auth-client.ts` reports no NEW errors on any file actually touched
  </acceptance_criteria>
  <done>next.config carries only docs-verified stable options (or is intentionally unchanged); the shared-infra audit is complete with any stable win landed and the auth boundary preserved; no experimental flag slipped in without a checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Fresh prod build + shared-floor + /habitat regression spot-check measurement (D-11)</name>
  <files>.planning/phases/17-performance-optimization/measurements/ (harness output; no source files edited)</files>
  <read_first>
    - scripts/measure-cwv.mjs (now route-filtered + OUT_DIR-redirected by Plan 17-01 — this task USES that capability; note it needs DATABASE_URL exported and self-cleans *test.local users)
    - .planning/phases/17-performance-optimization/17-RESEARCH.md (Code Examples "Verifying the true shared-chunk floor" + "Confirming three absence from a fresh production build"; D-11 spot-check rows in Validation Architecture — /habitat n=3 mobile)
    - .planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md (the immutable cross-route reference; /habitat is NOT in it — /habitat is CWV-green from Phase 13.1 and only gets a regression spot-check here, never a gate)
    - .planning/phases/17-performance-optimization/17-CONTEXT.md (D-11 = one /habitat spot-check after shared-bundle refactors land; execution_constraints #1 immutable-baseline rule + #2 measured discipline)
  </read_first>
  <action>
    Run `npm run build` (fresh Turbopack build) and confirm via `grep -rl "three" .next/static/chunks/*.js` that the count is 0 (three still absent — proves the D-05 relocation changed nothing about the client graph, as intended). Then read the shared floor from .next/diagnostics/route-bundle-stats.json (the `/` route's firstLoadUncompressedJsBytes + firstLoadChunkPaths.length) and record it in the SUMMARY as the post-Wave-2 shared-floor reference for Wave 3.
    Run the /habitat regression spot-check per D-11: this is a cheap mobile n=3 run against warm prod using the route-filtered harness. Invoke with the /habitat opt-in the route filter now supports (ROUTE_FILTER=/habitat) and the Phase-17 measurements OUT_DIR default. This run needs DATABASE_URL exported and self-cleans its *test.local user. This is a LONG run (~7 min for one route × presets) — flag it for run_in_background execution. Confirm /habitat still passes its CWV gates (it was Perf 96 mobile per PROJECT.md; a regression here would mean the shared-infra changes hurt it). /habitat stays OUT of the key-route gate set — this is purely the "revisit if refactors touch shared bundle" clause firing.
    Write the spot-check result to .planning/phases/17-performance-optimization/measurements/ (the harness does this automatically via the redirected OUT_DIR). Do NOT write anything under .planning/phases/16-performance-baseline-measure/baseline/.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npm run build && test $(grep -rl "three" .next/static/chunks/*.js 2>/dev/null | wc -l) -eq 0 && echo "THREE_ABSENT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `grep -rl "three" .next/static/chunks/*.js | wc -l` equals 0 (three confirmed absent from the fresh prod bundle — the D-05 relocation is behavior-neutral, per Pitfall 2)
    - The shared floor is read from .next/diagnostics/route-bundle-stats.json and recorded in the SUMMARY (the `/` route firstLoadUncompressedJsBytes + chunk count)
    - The /habitat spot-check ran via ROUTE_FILTER=/habitat (n=3 mobile) and its report was written UNDER .planning/phases/17-performance-optimization/measurements/ (NOT under baseline/); /habitat still meets its CWV "Good" gates (mobile Perf ≥90 — was 96) or a regression is flagged with the measured evidence
    - `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` returns EMPTY (immutable baseline untouched)
    - The long harness run was executed via run_in_background (>10 min commands must not block the foreground per execution_constraints)
  </acceptance_criteria>
  <done>A fresh prod build confirms three is still absent; the post-Wave-2 shared floor is recorded; /habitat is confirmed non-regressed by a cheap spot-check written to the Phase-17 measurements dir; the immutable baseline is provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → prod Neon DB | Task 3's /habitat spot-check provisions a *test.local user on the prod-shared Neon DB via DATABASE_URL, then self-cleans |
| build config → client bundle | next.config.ts changes affect what ships to every client; a wrong experimental flag could change caching/data-freshness behavior |
| package classification → build graph | moving three to devDependencies must not alter what actually ships (it already does not ship) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | next.config.ts experimental flags | mitigate | Hard D-07 gate: grep asserts zero experimental/cacheComponents/unstable_instant keys landed; any experimental option requires a checkpoint with evidence BEFORE landing — it does not ship in this plan |
| T-17-02-02 | Information Disclosure | D-08 layout/auth-client audit | mitigate | Auth session-gate (getSession → redirect) preserved byte-for-byte in behavior; RESEARCH Security Domain notes RSC/shared-infra changes stay BELOW the already-verified auth boundary and Next's build-time module-boundary checks catch accidental server→client leaks as build errors |
| T-17-02-03 | Tampering | measure-cwv.mjs OUT_DIR (Task 3) | mitigate | Uses the Plan-01 redirected OUT_DIR default; acceptance criterion requires git status show zero diff under the immutable baseline/ |
| T-17-02-04 | Denial of Service | /habitat spot-check credentials | accept | Single-user product; the *test.local user self-cleans via cleanup-test-users.mjs; no load/stress surface (out of scope per REQUIREMENTS) |
| T-17-02-SC | Tampering | npm installs | accept | No new packages installed (three is RELOCATED between existing manifest sections, not installed; nothing added). No legitimacy checkpoint needed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after deletions and any config change.
- `npm run build` exits 0; `grep -rl "three" .next/static/chunks/*.js | wc -l` equals 0.
- `npm test` (full vitest) passes; known flakes isolated before being treated as regressions.
- `grep` confirms zero experimental keys in next.config.ts.
- `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` is EMPTY.
- Scoped `npx biome ci <touched files>` shows no new errors.
- WAVE GATE (run after this wave, before Wave 3): full `npx tsc --noEmit` + full `npm test` + full `npm run test:e2e` (per-project: `--project=web` then `--project=mobile`, against a fresh dev server on :3000). Shared-infra changes (D-08) can affect auth screens — the existing auth-screen e2e coverage is the guard.
</verification>

<success_criteria>
three is build-time-only (devDependencies) with a fresh build proving zero client-bundle presence; two confirmed-dead files deleted; next.config carries only docs-verified stable options (or is intentionally unchanged); the D-08 shared-infra audit is complete with the auth boundary preserved; /habitat is confirmed non-regressed; the immutable baseline is untouched. No experimental Next 16 surface landed without a checkpoint.
</success_criteria>

<output>
Create `.planning/phases/17-performance-optimization/17-02-SUMMARY.md` when done.
</output>
