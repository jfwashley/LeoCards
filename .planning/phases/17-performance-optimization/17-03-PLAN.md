---
phase: 17-performance-optimization
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - src/components/habitat-hero.tsx
  - src/components/deck-view.tsx
  - src/app/(protected)/dashboard/page.tsx
  - src/components/countdown-timer.tsx
autonomous: false

requirements: [PERF-03]

must_haves:
  truths:
    - "D-06: client→RSC conversion is the primary TBT lever — HabitatHero (zero hooks, zero handlers today) becomes an RSC by deleting its 'use client' directive with ZERO behavior change; DeckView's static shell (layout wrapper, action buttons, HabitatHero render, StatusText non-countdown branches) is server-rendered in dashboard/page.tsx, keeping ONLY CountdownTimer + the deck-switch handler as small client leaves"
    - "D-02: the dashboard habitat hero renders poster-first for instant LCP — RESOLVED against the actual Daybreak design/baseline: dashboard LCP already passes at 1816ms with the existing static medallion, so D-02 is confirmed satisfied by the existing static-image approach unless the design mocks require a NEW ambient clip (verify before adding any new media — RESEARCH Open Question 1 / Assumption A1; do not gold-plate per D-12)"
    - "QA-mode server-side gating survives the split: cooldownUntil stays gated behind readQaAuth() server-side in dashboard/page.tsx and is NEVER threaded unconditionally into a client prop by the new RSC shell"
    - "The deck-view pre-split baseline test (from 17-01) stays green with NO assertion changes, proving the split is behavior-preserving; the dashboard real-content root gains data-perf-ready='true' for the Wave-5 D-15 signal"
    - "PERF-03 dashboard gate: warm-prod mobile /dashboard re-measured against the Phase 16 baseline (TBT 518ms→ ≤200ms, Perf 86→ ≥90, LCP/CLS still pass) with the route-scoped harness; if unreachable within D-01..03 fidelity, the D-04 checkpoint is raised with measured evidence"
    - "D-10: correctness gates per risky wave — full e2e after this wave, and qa:run because this wave moves a client→RSC boundary on the dashboard (needs a fresh dev server per the Phase 15/16 pattern)"
  artifacts:
    - path: "src/components/habitat-hero.tsx"
      provides: "RSC habitat hero (no 'use client' directive)"
      min_lines: 100
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Server-rendered dashboard shell importing only the CountdownTimer/deck-switch client leaves; preserves readQaAuth gating"
      contains: "readQaAuth"
    - path: "src/components/countdown-timer.tsx"
      provides: "Extracted CountdownTimer client leaf (the only genuinely reactive dashboard piece — useState/useEffect/router.refresh)"
      contains: "router.refresh"
  key_links:
    - from: "src/app/(protected)/dashboard/page.tsx"
      to: "src/components/countdown-timer.tsx"
      via: "imports only the interactive client leaf, renders static shell as RSC"
      pattern: "countdown-timer|CountdownTimer"
    - from: "src/app/(protected)/dashboard/page.tsx"
      to: "readQaAuth"
      via: "server-side QA gate preserved (cooldownUntil never unconditionally client-exposed)"
      pattern: "readQaAuth"
    - from: "src/components/habitat-hero.tsx"
      to: "RSC render"
      via: "'use client' directive removed"
      pattern: "^(?!.*use client)"
---

<objective>
Convert the dashboard's oversized client boundary to server-rendered markup — the primary TBT lever for the route that inherits the heaviest bundle. RESEARCH identifies DeckView as the single largest per-route client boundary: a "use client" wrapper around the ENTIRE dashboard body (header, HabitatHero, action buttons, CardList) for the sake of one countdown timer and a deck-switch handler. This plan extracts the genuinely-interactive pieces (CountdownTimer, deck-switch) into small client leaves and lets dashboard/page.tsx render the rest as RSC, converts the zero-interactivity HabitatHero to RSC outright, resolves the D-02 poster-first question against the real design, and re-measures the dashboard against the Phase 16 baseline.

Purpose: This is the D-06 primary TBT lever applied to the worst-case route. Moving hydration JS off the main thread is how mobile TBT (518ms on dashboard) drops toward ≤200ms. HabitatHero has zero hooks/handlers today — removing its "use client" is a free RSC win. The DeckView split mirrors the existing habitat page/scene RSC-shell precedent exactly. D-02 must be resolved, not assumed: the baseline shows dashboard LCP already passes, so poster-first may already be satisfied — verify before adding new media (D-12: no gold-plating).

Output: RSC habitat-hero; an extracted CountdownTimer client leaf; dashboard/page.tsx rendering the static shell server-side while preserving readQaAuth gating; a data-perf-ready marker on the dashboard content root; a route-scoped dashboard measurement. This plan is autonomous:false — it carries the D-04 gate-vs-fidelity human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-performance-optimization/17-CONTEXT.md
@.planning/phases/17-performance-optimization/17-RESEARCH.md
@.planning/phases/17-performance-optimization/17-PATTERNS.md
@.planning/phases/16-performance-baseline-measure/baseline/dashboard-baseline.md
@src/components/deck-view.tsx
@src/app/(protected)/dashboard/page.tsx
@AGENTS.md

<interfaces>
<!-- Contracts this plan produces/consumes. -->

CountdownTimer (extracted to src/components/countdown-timer.tsx — the ONLY genuinely reactive dashboard piece):
  - keeps "use client"; owns useState/useEffect/useRouter; calls router.refresh() when the cooldown reaches 0 (the existing deck-view.tsx lines 47-114 logic, moved verbatim)
  - StatusText's countdown branch consumes it; non-countdown branches can render as static server markup

deck-switch client leaf:
  - the <AppHeader onDeckChange={...}> wiring needs a client boundary for router.push(`/dashboard?deck=${id}`); keep AppHeader as-is if it is already a client component, otherwise wrap only the deck-switcher control

data-perf-ready marker (from 17-01 e2e/perf-markers.ts convention):
  - add data-perf-ready="true" to the dashboard's real-content root (the element present only once real due-count/card data is painted — e.g. the main content wrapper after data resolves), so Wave 5's waitForPerfReady polls it as the D-15 signal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert HabitatHero to RSC + extract CountdownTimer client leaf</name>
  <files>src/components/habitat-hero.tsx, src/components/countdown-timer.tsx, src/components/deck-view.tsx</files>
  <read_first>
    - src/components/habitat-hero.tsx (line 1 "use client" — the ONLY change needed; the full 144-line file has ZERO hooks/handlers per RESEARCH+pattern map — a <Link> + ChevronRight SVG + conditional subtitle only)
    - src/components/habitat-medallion.tsx (HabitatHero's one child import — confirm it is also hooks-free / RSC-safe before finalizing the conversion, per pattern map)
    - src/components/deck-view.tsx (lines 47-114 CountdownTimer — the client-reactive block to extract verbatim; lines 117+ StatusText which consumes it)
    - src/components/daybreak/bw-medallion.tsx (lines 447-471 — the target RSC-safe shape: props-driven, hooks-free, "Pure (no hooks) — RSC-safe" sibling comment)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (habitat-hero.tsx section = delete line 1 only; deck-view.tsx section = the exact interactive pieces to keep client)
  </read_first>
  <action>
    (a) HabitatHero → RSC: delete the `"use client";` directive on line 1 of src/components/habitat-hero.tsx. Make NO other change — every remaining line (props destructure, ChevronRight inline SVG, <Link href>, <HabitatMedallion>, conditional subtitleContent) is already Server-Component-compatible (zero hooks, zero event handlers, no window/document access). First confirm HabitatMedallion is itself hooks-free (it is, per bw-medallion.tsx's sibling "RSC-safe" comment) so the conversion holds transitively. This exactly meets D-06's "identical behaviour" bar.
    (b) Extract CountdownTimer into a new file src/components/countdown-timer.tsx as a "use client" leaf, moving the deck-view.tsx lines 47-114 logic VERBATIM (useState initial-countdown, the useEffect 60s tick, router.refresh() on cooldown expiry, formatCountdown usage). Export it as a named export `CountdownTimer` with the same props it has today. Update deck-view.tsx (and, in Task 2, dashboard/page.tsx) to import CountdownTimer from the new file instead of defining it inline. Do NOT change its behavior — this is a pure move so the interactive leaf can be imported by a server component without dragging the whole DeckView client boundary along.
    Keep the existing deck-view unit test green (the mock-children baseline from 17-01) — CountdownTimer extraction should not change DeckView's rendered output.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npx vitest run src/components/deck-view.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 src/components/habitat-hero.tsx` is NOT `"use client";` (the directive is removed — HabitatHero is now RSC)
    - `grep -c "use client" src/components/countdown-timer.tsx` is ≥1 (the extracted leaf keeps the client directive) and `grep -n "router.refresh" src/components/countdown-timer.tsx` matches (the reactive logic moved intact)
    - `npx tsc --noEmit` exits 0 (the RSC conversion + extraction type-check; a component that illegally uses a hook without "use client" would fail the build here)
    - `npx vitest run src/components/deck-view.test.tsx` passes with the 17-01 baseline assertions UNCHANGED (behavior preserved)
    - `npx biome ci src/components/habitat-hero.tsx src/components/countdown-timer.tsx src/components/deck-view.tsx` reports no NEW errors on touched files
  </acceptance_criteria>
  <done>HabitatHero is a Server Component; CountdownTimer is a standalone client leaf importable by RSC; DeckView's rendered behavior is unchanged and still passes its baseline test.</done>
</task>

<task type="auto">
  <name>Task 2: Server-render the dashboard shell in page.tsx (D-06 split) + resolve D-02 poster-first + add data-perf-ready marker</name>
  <files>src/app/(protected)/dashboard/page.tsx, src/components/deck-view.tsx</files>
  <read_first>
    - src/app/(protected)/dashboard/page.tsx (the already-async RSC — lines 25-32 auth+redirect, lines 40-41/107-112 the readQaAuth cooldownUntil gate that MUST be preserved, line 116 the current <DeckView> render)
    - src/components/deck-view.tsx (lines 284-330 the DeckView body: the static <div min-h-screen> layout, <main> wrapper, HabitatHero render, action-button block, StatusText — everything except CountdownTimer + handleDeckChange is static and can move server-side)
    - src/app/(protected)/habitat/page.tsx + src/components/habitat-scene.tsx (the repo's ONLY existing "RSC shell hands data to a client component owning only the interactive slice" precedent — the split to mirror)
    - LeoCards/design/ (the Daybreak designer-handoff docs — check the dashboard design to RESOLVE D-02: does the dashboard hero call for a NEW ambient clip, or is the existing static medallion the intended poster-first state? per RESEARCH Open Question 1)
    - .planning/phases/16-performance-baseline-measure/baseline/dashboard-baseline.md (dashboard mobile LCP already 1816ms ≤2500 — evidence D-02 may already be satisfied by the static image; do not add media without design confirmation)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the dashboard/page.tsx QA-gating pattern to preserve; the deck-view split-target section; the e2e content-visible marker convention)
    - e2e/perf-markers.ts (the PERF_READY_ATTR = "data-perf-ready" convention from 17-01 to apply here)
  </read_first>
  <action>
    D-06 dashboard split: move DeckView's static shell into dashboard/page.tsx as server-rendered markup. The outer <div className="min-h-screen ...">, the <main> wrapper, the HabitatHero render (now RSC from Task 1), the action-button block, and StatusText's non-countdown branches become server markup rendered directly in page.tsx. Import ONLY the client leaves: CountdownTimer (from Task 1's new file) where the countdown branch renders, and the deck-switch client boundary for the <AppHeader onDeckChange> wiring (keep AppHeader as its own client component if it already is one — Assumption A3 flags AppHeader was not fully read; if AppHeader needs its own client state, leave it as a client leaf rather than forcing the whole shell client). The goal: the dashboard page ships RSC markup for everything static, hydrating only CountdownTimer + the deck-switcher control. DeckView the component may become a thin server wrapper or be inlined into page.tsx — either is fine as long as the rendered output matches today.
    PRESERVE the QA gate EXACTLY: cooldownUntil must remain gated behind `qaMode = await readQaAuth()` server-side (lines 40-41, 107-112) — never thread cooldownUntil into a client prop unconditionally. The new server shell keeps this gate in place; verify no client leaf receives cooldownUntil unless qaMode is true.
    RESOLVE D-02: read the LeoCards/design/ dashboard handoff. If the design shows the existing static medallion as the hero (no ambient video on the dashboard), then D-02 is SATISFIED by the current static-image approach (LCP already passes) — record this resolution in the SUMMARY and add NO new media (D-12 no gold-plating). ONLY if the design explicitly calls for a NEW ambient clip on the dashboard hero do you mirror habitat-video.tsx's poster-first pattern (Image priority bottom layer + video top layer + reducedMotion early-return) — and if so, that is a fidelity change; note it. Default expectation per RESEARCH Assumption A1: no new media needed.
    Add `data-perf-ready="true"` to the dashboard's real-content root — the element that is present only once the real due-count/card data has rendered (not a skeleton). This is the D-15 signal Wave 5's waitForPerfReady polls.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npm run build 2>&1 | tail -15 && npx vitest run src/components/deck-view.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "readQaAuth" src/app/(protected)/dashboard/page.tsx` still matches AND the cooldownUntil assignment is still guarded by qaMode (the QA server-side gate is preserved — no unconditional client exposure)
    - `grep -rn "data-perf-ready" src/app/(protected)/dashboard/page.tsx src/components/deck-view.tsx` matches on the dashboard real-content root
    - `npm run build` exits 0 (the RSC split builds — Next's module-boundary checker would fail the build if a server component illegally imported client-only code, so a green build is the boundary-correctness proof)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/components/deck-view.test.tsx` passes with UNCHANGED assertions from 17-01 (the split is behavior-preserving)
    - D-02 resolution is recorded in the SUMMARY citing the LeoCards/design/ dashboard handoff: either "satisfied by existing static medallion, no new media (LCP already 1816ms)" OR "design requires new ambient clip — mirrored habitat-video.tsx poster-first pattern" (not silently skipped)
    - `npx biome ci src/app/(protected)/dashboard/page.tsx src/components/deck-view.tsx` reports no NEW errors on touched files
  </acceptance_criteria>
  <done>The dashboard ships RSC markup for everything static, hydrating only the countdown + deck-switch leaves; the QA gate is preserved; D-02 is resolved against the real design; the content-visible marker is in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: /dashboard warm-prod re-measurement vs Phase 16 baseline + D-04 gate-vs-fidelity</name>
  <action>PAUSE for human verification. Run the /dashboard route-scoped warm-prod re-measurement (see how-to-verify), present the mobile medians vs the Phase 16 baseline, and either confirm the gate is met or raise the D-04 gate-vs-fidelity decision with measured evidence. Do not self-approve.</action>
  <files>.planning/phases/17-performance-optimization/measurements/ (route-scoped /dashboard report; no source files)</files>
  <done>Dashboard gate met (TBT ≤200, Perf ≥90) or a D-04 decision recorded; immutable baseline shows zero git diff.</done>
  <what-built>
    The dashboard client→RSC split (DeckView static shell now server-rendered, HabitatHero converted to RSC, CountdownTimer extracted as a client leaf), plus the route-scoped warm-prod re-measurement of /dashboard against the Phase 16 baseline. This checkpoint exists per the Phase 16-03 precedent (executor stops for human-verify on measured perf rather than self-approving) AND carries the D-04 gate-vs-fidelity trigger.
  </what-built>
  <how-to-verify>
    1. The executor will run a route-scoped warm-prod measurement of /dashboard using the route-filtered harness (mobile-first, needs DATABASE_URL exported, ~7 min, self-cleans *test.local — run via run_in_background). Command shape: PHASE_OUT_DIR default (Phase-17 measurements dir), ROUTE_FILTER=/dashboard, `DATABASE_URL=... node scripts/measure-cwv.mjs`.
    2. Compare the /dashboard mobile medians against the Phase 16 baseline (baseline was: mobile TBT 518ms, Perf 86, LCP 1816ms, CLS 0):
       - PASS case: TBT ≤200ms, Perf ≥90, LCP ≤2500ms, CLS ≤0.1 — approve, the dashboard gate is met; note the headroom.
       - D-04 CONFLICT case: if /dashboard provably cannot reach TBT ≤200 / Perf ≥90 within D-01..03 fidelity (mobile-first progressive loading, poster-first, one shimmer style, settled state matches Daybreak), the executor will present the measured evidence and a SPECIFIC proposed visible change (e.g. "defer X below the fold, which briefly shows a shimmer where a static block is today"). Josh approves or vetoes that specific change per case — never a silent fidelity sacrifice, never a silent gate miss.
    3. Visually confirm (against LeoCards/design/ dashboard handoff) the settled dashboard still matches today's Daybreak exactly — the D-01 rule is fidelity applies to the destination, not the journey.
    4. Confirm `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` is EMPTY (immutable baseline untouched by the re-measurement).
  </how-to-verify>
  <resume-signal>Type "approved" if the dashboard gate is met and fidelity holds; or approve/veto the specific D-04 proposed visible change; or describe the issue to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → client leaf | the RSC split moves the client boundary; server-only data (cooldownUntil when QA-authed) must not cross into client props unconditionally |
| harness → prod Neon DB | the route-scoped /dashboard measurement provisions + self-cleans a *test.local user via DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Information Disclosure | dashboard/page.tsx QA-gate after RSC split | mitigate | cooldownUntil stays behind readQaAuth() server-side; RESEARCH Security Domain: any NEW server component from a D-06 conversion must preserve the existing server-side gating pattern, never pass QA fields unconditionally to a client prop; acceptance criterion greps to confirm the qaMode guard survives |
| T-17-03-02 | Information Disclosure | server→client module boundary | mitigate | Next's build-time module-boundary enforcement fails the build if a server component leaks a server-only import into the client graph — a green `npm run build` is the boundary-correctness proof (build-time-caught, not silent-runtime) |
| T-17-03-03 | Tampering | route-scoped measurement OUT_DIR | mitigate | uses the Plan-01 redirected default (Phase-17 measurements dir); checkpoint step 4 requires git status show zero diff under the immutable baseline/ |
| T-17-03-SC | Tampering | npm installs | accept | No new packages (subtractive/conversion work only); no legitimacy checkpoint needed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after conversion + split.
- `npm run build` exits 0 (proves server/client boundary correctness).
- `npx vitest run src/components/deck-view.test.tsx` passes with unchanged baseline assertions.
- Route-scoped /dashboard warm-prod measurement written to the Phase-17 measurements dir; compared against baseline at the D-04 checkpoint.
- `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` EMPTY.
- Scoped `npx biome ci <touched files>` shows no new errors.
- WAVE GATE (after this wave): full `npx tsc --noEmit` + full `npm test` + full `npm run test:e2e` (per-project web then mobile, fresh dev server) + `npm run qa:run` (this wave moves a client→RSC boundary on the dashboard which touches data-display paths — D-10 requires qa:run; needs a fresh dev server, and qa-01/qa-03 need a STUDY_COOLDOWN_MINUTES=1 server boot per the Phase 15 regime).
</verification>

<success_criteria>
The dashboard renders RSC markup for everything static, hydrating only the countdown + deck-switch client leaves; HabitatHero is a Server Component; the QA server-side gate is preserved; D-02 is resolved against the real design (no new media unless the design demands it); the /dashboard warm-prod mobile gate is met (TBT ≤200, Perf ≥90) OR a D-04 checkpoint was raised with measured evidence; the immutable baseline is untouched; the deck-view baseline test still passes unchanged.
</success_criteria>

<output>
Create `.planning/phases/17-performance-optimization/17-03-SUMMARY.md` when done.
</output>
