---
phase: 17-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/measure-cwv.mjs
  - scripts/measure-cwv-lib.mjs
  - scripts/__tests__/measure-cwv-lib.test.ts
  - src/components/daybreak/shimmer.tsx
  - src/components/daybreak/__tests__/shimmer.test.tsx
  - src/app/globals.css
  - src/components/deck-view.test.tsx
autonomous: true
requirements: [PERF-03, PERF-04]

must_haves:
  truths:
    - "D-09: measure-cwv.mjs accepts a route filter (mobile-first, ~7 min/route re-measurement) so each optimization batch re-measures only affected route(s); /habitat is an addable opt-in beyond the 4 key routes, not just a subset filter"
    - "IMMUTABLE BASELINE HOLDS: measure-cwv.mjs OUT_DIR defaults to a NEW Phase-17-owned directory (.planning/phases/17-performance-optimization/measurements/); running the route-filtered harness produces ZERO git diff under .planning/phases/16-performance-baseline-measure/baseline/"
    - "D-03: one reusable Daybreak-toned shimmer component (cream/amber rounded blocks) exists as the single placeholder style; space is always reserved so CLS 0 holds; a prefers-reduced-motion override disables its pulse"
    - "A per-route content-visible marker convention and a prod-vs-dev-build detection signal exist as Wave-0 scaffolds so downstream PERF-04 and task_d326ebac work can assert against them"
    - "deck-view.tsx has a pre-split baseline test asserting current rendered behavior BEFORE any D-06 split, so behavior-preservation is provable, not assumed"
  artifacts:
    - path: "scripts/measure-cwv.mjs"
      provides: "Route-filtered warm-prod Lighthouse harness with parameterized OUT_DIR"
      contains: "ROUTE_FILTER"
    - path: "scripts/measure-cwv-lib.mjs"
      provides: "Pure route-resolution + out-dir-resolution logic, vitest-covered"
      exports: ["resolveRoutes", "resolveOutDir"]
    - path: "src/components/daybreak/shimmer.tsx"
      provides: "Single reusable D-03 shimmer placeholder atom"
      contains: "DaybreakShimmer"
    - path: "src/app/globals.css"
      provides: "@keyframes shimmer-pulse + prefers-reduced-motion override, following the hab-fall convention"
      contains: "shimmer-pulse"
  key_links:
    - from: "scripts/measure-cwv.mjs"
      to: ".planning/phases/17-performance-optimization/measurements/"
      via: "OUT_DIR default (never the Phase 16 baseline path)"
      pattern: "17-performance-optimization.*measurements"
    - from: "scripts/measure-cwv.mjs"
      to: "scripts/measure-cwv-lib.mjs"
      via: "import of resolveRoutes/resolveOutDir"
      pattern: "resolveRoutes|resolveOutDir"
---

<objective>
Land the Wave-0 tooling and scaffolds every downstream Phase 17 wave depends on, WITHOUT any optimization or behavior change yet. Three things must exist before a single route is optimized: (1) the D-09 route-filtered measurement harness with its output redirected away from the immutable Phase 16 baseline, (2) the three genuinely-new patterns the pattern map confirmed have no analog — the D-03 shimmer atom, a per-route content-visible marker convention, and a prod-vs-dev-build detection signal, and (3) a pre-split baseline test for deck-view.tsx so its later D-06 RSC split is provably behavior-preserving.

Purpose: These are the contracts and guardrails. Without the route-filter + OUT_DIR redirect, any mid-phase re-measurement risks silently overwriting the immutable baseline (RESEARCH Pitfall 1). Without the shimmer/marker/prod-detect scaffolds, later waves would each reinvent them. Without the deck-view baseline test, the dashboard RSC split cannot prove it changed nothing observable.

Output: an extended measure-cwv.mjs + lib + tests; a new DaybreakShimmer atom + its keyframe + test; a documented content-visible marker convention and a prod-build detection env-gate (both scaffolded as reusable helpers/comments the downstream specs import); a pre-split deck-view baseline test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-performance-optimization/17-CONTEXT.md
@.planning/phases/17-performance-optimization/17-RESEARCH.md
@.planning/phases/17-performance-optimization/17-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Contracts downstream waves consume from this plan. Executor should honor these names exactly. -->

measure-cwv-lib.mjs exports (NEW — extract route + out-dir logic here for vitest coverage):
  resolveRoutes(filterArg: string|null): string[]
    - filterArg null → the 4 key routes ["/dashboard","/study","/deck/new-card","/deck/browse"]
    - filterArg "/dashboard,/study" → the intersection (comma-separated multi-select)
    - filterArg containing "/habitat" → UNION: /habitat is addable even though it is NOT one of the 4 key routes (D-11 spot-check need)
  resolveOutDir(rootDir: string, phaseOutDir: string|null): string
    - phaseOutDir null → <root>/.planning/phases/17-performance-optimization/measurements  (NEVER the Phase 16 baseline path)
    - phaseOutDir set → <root>/<phaseOutDir>

DaybreakShimmer (NEW — src/components/daybreak/shimmer.tsx), the ONE D-03 placeholder style:
  export function DaybreakShimmer(props: { width?: number|string; height?: number|string; radius?: number; className?: string }): JSX.Element
    - cream/amber rounded block; reserves its own box (width/height) so CLS stays 0
    - pulses via CSS @keyframes shimmer-pulse (added to globals.css); disabled under prefers-reduced-motion

Content-visible marker convention (NEW — documented + scaffolded for Wave 4 e2e):
  Each key route exposes ONE data attribute on its real-content root that appears only when real (non-skeleton) data is painted:
    data-perf-ready="true"
  Downstream PERF-04 gate polls for [data-perf-ready="true"] as the D-15 "real content visible" signal.

Prod-build detection signal (NEW — for D-14 + task_d326ebac):
  Env gate PERF_PROD_BUILD === "1" marks a run as executing against a local `next build && next start` server (never `next dev`). Wave 4 wraps prod-only assertions in this gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route filter + OUT_DIR redirect to measure-cwv.mjs (D-09) with pure logic in the lib + vitest coverage</name>
  <files>scripts/measure-cwv.mjs, scripts/measure-cwv-lib.mjs, scripts/__tests__/measure-cwv-lib.test.ts</files>
  <read_first>
    - scripts/measure-cwv.mjs (the whole harness — but especially line 319 `const ROUTES`, lines 572-578 `const OUT_DIR`, and the writeJsonAtomic/writeTextAtomic helpers at lines 598-668 that consume OUT_DIR)
    - scripts/measure-cwv-lib.mjs (the existing pure-lib half — median/extractMetrics/getBundleKb/classifyBottleneck/renderRouteReport; add the new exports here alongside them)
    - scripts/__tests__/measure-cwv-lib.test.ts (the existing vitest file + its fixture-read convention at lines 1-31; extend it, do NOT create a parallel test file)
    - .planning/phases/17-performance-optimization/17-RESEARCH.md (Pitfall 1 + the Code Examples "Route-scoped measurement" sketch + Open Question 3 which locks comma-separated multi-route + /habitat-addable)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the measure-cwv.mjs section: exact edit points + the Node-ESM/JSDoc/atomic-write conventions to mirror)
  </read_first>
  <action>
    Extract two pure functions into scripts/measure-cwv-lib.mjs and consume them from measure-cwv.mjs:
    (a) resolveRoutes(filterArg) — the canonical 4 key routes are "/dashboard","/study","/deck/new-card","/deck/browse". When filterArg is null return all 4. When set (read from env ROUTE_FILTER, comma-separated), return the intersection with the 4 — EXCEPT "/habitat" must be treated as an addable union member (it is not one of the 4 key routes but D-11 needs it as an opt-in spot-check target). So `ROUTE_FILTER=/habitat` yields ["/habitat"], `ROUTE_FILTER=/dashboard,/habitat` yields ["/dashboard","/habitat"].
    (b) resolveOutDir(rootDir, phaseOutDir) — when phaseOutDir (read from env PHASE_OUT_DIR) is null, default to path.join(rootDir, ".planning","phases","17-performance-optimization","measurements"). NEVER default to the Phase 16 baseline path. When PHASE_OUT_DIR is set, honor it.
    In measure-cwv.mjs: replace the hardcoded `const ROUTES` (line 319) with `const ROUTES = resolveRoutes(process.env.ROUTE_FILTER ?? null)` and replace the hardcoded `const OUT_DIR` block (lines 572-578) with `const OUT_DIR = resolveOutDir(ROOT, process.env.PHASE_OUT_DIR ?? null)`. Ensure OUT_DIR is created (mkdir recursive) before the first write, since the Phase-17 measurements dir will not exist yet. Reuse the existing writeJsonAtomic/writeTextAtomic helpers unchanged. Follow the existing JSDoc @param/@returns convention on both new lib functions.
    Add a package.json note is NOT required (measure:cwv script already exists at line 25 and invokes the script; ROUTE_FILTER/PHASE_OUT_DIR are env-driven so no new npm script is needed) — do not touch package.json in this task.
    Add sibling describe blocks to scripts/__tests__/measure-cwv-lib.test.ts covering: resolveRoutes(null) returns the 4 keys; comma-select intersection; /habitat union addable; resolveOutDir(null) returns the phase-17 measurements path and NEVER contains "16-performance-baseline-measure/baseline"; resolveOutDir with an explicit dir honors it.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && node --check scripts/measure-cwv.mjs && node --check scripts/measure-cwv-lib.mjs && npx vitest run scripts/__tests__/measure-cwv-lib.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/measure-cwv.mjs` and `node --check scripts/measure-cwv-lib.mjs` both exit 0 (.mjs is outside tsconfig — validate with node --check + scoped biome, NEVER tsc)
    - `npx vitest run scripts/__tests__/measure-cwv-lib.test.ts` passes, including a test asserting resolveOutDir(root, null) does NOT contain the substring "16-performance-baseline-measure/baseline"
    - `grep -n "resolveRoutes\|resolveOutDir" scripts/measure-cwv.mjs` shows both are imported/called, and `grep -n "const ROUTES = \[" scripts/measure-cwv.mjs` returns NO hardcoded-array match (it now calls resolveRoutes)
    - resolveRoutes("/habitat") returns exactly ["/habitat"] (union-addable, proven by a vitest case) — a plain subset filter would return [] which is wrong
    - `npx biome ci scripts/measure-cwv.mjs scripts/measure-cwv-lib.mjs scripts/__tests__/measure-cwv-lib.test.ts` reports no NEW errors on these touched files (repo is never globally biome-clean; scope to touched files only)
  </acceptance_criteria>
  <done>The harness re-measures any subset of routes (plus /habitat as an opt-in) and writes to a Phase-17 directory by default; the immutable baseline path is unreachable via the default code path; pure logic is unit-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Create the D-03 DaybreakShimmer atom + its shimmer-pulse keyframe + test</name>
  <files>src/components/daybreak/shimmer.tsx, src/components/daybreak/__tests__/shimmer.test.tsx, src/app/globals.css</files>
  <read_first>
    - src/components/daybreak/pill.tsx (the simplest atom: props+className merge convention to mirror)
    - src/components/daybreak/bw-medallion.tsx (size-prop + cream/amber palette-const convention; lines 447-471)
    - src/app/globals.css (the @keyframes hab-fall convention at lines 111-127 + the prefers-reduced-motion override block — mirror this exact shape for shimmer-pulse)
    - src/components/daybreak/__tests__ (existing daybreak test convention to place the new test alongside)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the shimmer.tsx section + "No Analog Found" table — this is a genuinely new pattern; base API on pill.tsx + bw-medallion.tsx)
  </read_first>
  <action>
    Create src/components/daybreak/shimmer.tsx exporting `DaybreakShimmer` with props { width?: number|string; height?: number|string; radius?: number; className?: string }. It renders ONE cream/amber rounded block using the established Daybreak palette (the #FFF1DC / #F0E3CF / #FFE7BC family already used across bw-medallion.tsx and ac-progress.tsx — pick a cream base with a subtly darker amber sweep). The block MUST reserve its own box via the width/height props (default to sensible full-width height like width:"100%", height:16, radius:8) so that whatever it stands in for keeps CLS at 0. It must be RSC-safe (no hooks, no "use client") since it is only ever a static placeholder. Merge className via the `.filter(Boolean).join(" ")` convention from pill.tsx. Include `aria-hidden="true"` (it is decorative, mirrors bw-medallion.tsx).
    Add a new `@keyframes shimmer-pulse` to src/app/globals.css following the EXACT hab-fall convention: a named keyframe (opacity or background-position pulse) plus a paired `@media (prefers-reduced-motion: reduce)` block that sets `animation: none` on the shimmer class. The component applies the animation via a className that the keyframe targets.
    Create src/components/daybreak/__tests__/shimmer.test.tsx (jsdom) asserting: the component renders, honors an explicit width/height (the reserved box is present in the DOM), merges a passed className, and carries aria-hidden.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx vitest run src/components/daybreak/__tests__/shimmer.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/daybreak/__tests__/shimmer.test.tsx` passes with a test asserting the reserved box (explicit width/height style) is in the rendered DOM (CLS-0 space reservation is provable)
    - `grep -n "shimmer-pulse" src/app/globals.css` matches AND a `prefers-reduced-motion` block referencing the shimmer class exists in globals.css
    - `grep -n "\"use client\"" src/components/daybreak/shimmer.tsx` returns NOTHING (shimmer is RSC-safe — no client directive)
    - `grep -n "aria-hidden" src/components/daybreak/shimmer.tsx` matches
    - `npx tsc --noEmit` exits 0 (component is well-typed)
    - `npx biome ci src/components/daybreak/shimmer.tsx src/components/daybreak/__tests__/shimmer.test.tsx src/app/globals.css` reports no NEW errors on touched files
  </acceptance_criteria>
  <done>The single D-03 placeholder style exists as a reusable, RSC-safe, space-reserving, reduced-motion-respecting atom that downstream lazy-load boundaries can drop in as `loading: () => <DaybreakShimmer .../>`.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-split deck-view baseline test + document the content-visible marker and prod-build-detection conventions</name>
  <files>src/components/deck-view.test.tsx, e2e/perf-markers.ts</files>
  <read_first>
    - src/components/deck-view.test.tsx (the EXISTING test scaffold — extend it; its mock-children convention for AppHeader/HabitatHero/CardList is at lines 1-50 per the pattern map)
    - src/components/deck-view.tsx (lines 47-114 CountdownTimer, 117+ StatusText, 284-330 DeckView body, line 411 data-testid="add-a-card" — the current rendered behavior to lock)
    - e2e/13-perf.spec.ts (lines 137-175 the poll-for-marker idiom the content-visible signal will reuse; lines 215/257-270 the throttling-conditional + soft-assert style)
    - .planning/phases/17-performance-optimization/17-RESEARCH.md (Open Questions 2 + Wave 0 Gaps + Pitfall 5 for the prod-vs-dev detection rationale)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the e2e/13-perf.spec.ts section + the "No Analog Found" rows for both the content-visible marker and the prod-build detection mechanism)
  </read_first>
  <action>
    (a) Extend src/components/deck-view.test.tsx with a baseline block that asserts the CURRENT observable behavior of DeckView before any RSC split lands in Wave 3: the header renders, the HabitatHero mock is present, the CardList mock is present, and the "add-a-card" affordance (data-testid="add-a-card") is present. Use the exact vi.mock child-component convention already in the file. This is the behavior-preservation reference — Wave 3's split must keep this test green with no changes to its assertions.
    (b) Document the content-visible marker convention as an executable scaffold, not just prose: add a short exported helper module e2e/perf-markers.ts exporting `PERF_READY_ATTR = "data-perf-ready"` and a `waitForPerfReady(page, timeoutMs)` helper that polls for `[data-perf-ready="true"]` using the same poll-until-marker idiom as e2e/13-perf.spec.ts lines 142-151. Wave 3 route conversions will add `data-perf-ready="true"` to each key route's real-content root; Wave 4 imports waitForPerfReady as the D-15 signal. Do NOT yet add the attribute to any route in this plan — only the helper + constant.
    (c) Document the prod-build detection signal as a scaffold: in e2e/perf-markers.ts also export `IS_PROD_BUILD = process.env.PERF_PROD_BUILD === "1"`. Wave 4 wraps the D-14 nav gate + the task_d326ebac INP assertion in this flag so they only run against a `next build && next start` server. Add a JSDoc comment on the export citing Pitfall 5 (port 3000 is shared by next dev and next start, so an explicit env signal is required).
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx vitest run src/components/deck-view.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/deck-view.test.tsx` passes, including the new baseline assertions (header + habitat-hero mock + card-list mock + add-a-card affordance all present)
    - `grep -n "data-perf-ready\|PERF_READY_ATTR\|waitForPerfReady\|IS_PROD_BUILD" e2e/perf-markers.ts` matches all four identifiers
    - `grep -n "PERF_PROD_BUILD" e2e/perf-markers.ts` matches and a nearby comment references the shared-port-3000 rationale
    - `npx tsc --noEmit` exits 0 (the new e2e helper module type-checks; run tsc since this touches .ts test-infra, per the "run full tsc after any wave editing e2e" constraint)
    - `npx biome ci src/components/deck-view.test.tsx e2e/perf-markers.ts` reports no NEW errors on touched files
  </acceptance_criteria>
  <done>DeckView's current behavior is locked by a test that Wave 3 must preserve; the content-visible marker and prod-build-detection conventions exist as importable scaffolds so Wave 3/Wave 4 wire them without reinventing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → prod Neon DB | measure-cwv.mjs provisions *test.local users on the prod-shared Neon DB using DATABASE_URL; the OUT_DIR change affects where measurement artifacts are written |
| test tooling → filesystem | the route filter / OUT_DIR redirect controls which directory the harness writes to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | measure-cwv.mjs OUT_DIR | mitigate | resolveOutDir defaults to a NEW Phase-17 measurements dir and NEVER to the immutable Phase 16 baseline; a vitest case asserts the default path excludes "16-performance-baseline-measure/baseline"; acceptance criterion requires git status show zero diff under baseline/ |
| T-17-01-02 | Information Disclosure | DaybreakShimmer / deck-view test | accept | Pure presentational atom + jsdom test — no data, no secrets, no DB access; nothing to disclose |
| T-17-01-SC | Tampering | npm installs | accept | This plan installs NO new packages (RESEARCH Package Legitimacy Audit: phase is subtractive; Node built-ins + already-installed vitest/playwright only). No install task → no legitimacy checkpoint needed this plan |
</threat_model>

<verification>
- `node --check` passes on both .mjs scripts (they are outside tsconfig).
- `npx vitest run` passes for the three touched/added test files.
- `npx tsc --noEmit` exits 0 (Tasks 2-3 touch .tsx/.ts).
- CRITICAL: `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` returns EMPTY — the immutable baseline is untouched.
- Scoped `npx biome ci <touched files>` shows no new errors (repo is never globally biome-clean).
</verification>

<success_criteria>
The route-filtered harness writes to a Phase-17 directory by default (immutable baseline unreachable); the D-03 shimmer atom + keyframe exist and are tested; the content-visible marker and prod-build-detection conventions exist as importable scaffolds; the deck-view pre-split baseline test locks current behavior. No optimization or route behavior has changed yet — this is pure tooling/scaffolding.
</success_criteria>

<output>
Create `.planning/phases/17-performance-optimization/17-01-SUMMARY.md` when done.
</output>
