---
phase: 17-performance-optimization
plan: 05
type: execute
wave: 5
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - e2e/13-perf.spec.ts
  - src/components/study-session.tsx
  - src/components/card-list.tsx
autonomous: false

requirements: [PERF-03, PERF-04]

must_haves:
  truths:
    - "PERF-04 / D-13: e2e/13-perf.spec.ts is extended to cover 6 hub-and-spoke navigations (dashboard↔study, dashboard↔new-card, dashboard↔browse) as in-app link taps; Browser Back is NOT separately gated (D-13); links to /study always carry ?deck={id}"
    - "D-14: the 6-nav gate runs against a LOCAL PROD BUILD (next build && next start), never the dev server — gated by the PERF_PROD_BUILD env signal (from 17-01) so it is structurally impossible to accidentally measure next dev; the file's existing INP assertion is gated to prod-build-only runs (folded task_d326ebac), clearing the v4.0 backlog item"
    - "D-15: PASS = the destination's REAL content (the data-perf-ready='true' marker from 17-03/17-04) visibly rendered ≤100ms median n≥5 per pair, prefetch-warm; skeletons/loading.tsx do NOT count; measured via waitForPerfReady polling, NOT the existing LCP/CLS/INP capture"
    - "D-16 / D-17: link prefetch is tuned so the RSC payload is local before the tap for the 6 gated links; mutations (study-session completion via /api/study/complete, card add) explicitly invalidate via router.refresh() so dashboard due-counts + habitat state are correct on landing — no always-refetch, no visibly self-correcting numbers (generalizes the existing deck-view/countdown-timer router.refresh() pattern, NOT cacheComponents/revalidateTag which need a D-07 checkpoint)"
    - "The afterAll stale-path write (13-3d-habitat/perf-results.json, best-effort guarded per commit 88c16ad) SURVIVES the edits; final phase-end proof: full 4-route×2-preset measure-cwv run (official after-record vs Phase 16 baseline) + qa:run (criterion-4) + /habitat inclusion, all green"
    - "D-10: qa:run runs once at phase end as the official criterion-4 proof (the core-journey harness still passes after all perf refactors), against a fresh dev server"
  artifacts:
    - path: "e2e/13-perf.spec.ts"
      provides: "6 hub-and-spoke nav-timing tests + content-visible (data-perf-ready) signal + prod-build-gated INP; afterAll stale-path guard preserved"
      contains: "data-perf-ready"
    - path: "src/components/study-session.tsx"
      provides: "Study-completion mutation explicitly invalidates via router.refresh so dashboard due-counts/habitat are fresh on landing (D-17)"
      contains: "router.refresh"
  key_links:
    - from: "e2e/13-perf.spec.ts"
      to: "e2e/perf-markers.ts"
      via: "imports waitForPerfReady + IS_PROD_BUILD from the 17-01 scaffold"
      pattern: "waitForPerfReady|IS_PROD_BUILD|perf-markers"
    - from: "src/components/study-session.tsx"
      to: "dashboard due-count / habitat state"
      via: "router.refresh() on mutation so landing shows fresh state (D-17)"
      pattern: "router\\.refresh"
---

<objective>
Deliver the PERF-04 instant-nav gate and the D-17 targeted invalidation, then run the official phase-end proof. Extend e2e/13-perf.spec.ts (which today covers only /dashboard + /habitat, 2 of the needed pairs, with no content-visible signal) to instrument all 6 D-13 hub-and-spoke navigations against a LOCAL PROD BUILD, measuring the destination's real content visible ≤100ms via the data-perf-ready marker (not skeletons). Fold in the task_d326ebac INP-prod-build-only fix while in this exact file. Tune prefetch (D-16) so the RSC payload is warm before the tap, and wire mutation-triggered router.refresh() (D-17) so dashboard due-counts and habitat state are correct on landing after study completion / card add. Finish with the official full 4-route×2-preset measure-cwv run (the after-record vs the Phase 16 baseline), qa:run (criterion-4 proof), and /habitat inclusion.

Purpose: PERF-04 is a distinct concern from PERF-03 — RESEARCH is explicit that the existing spec's INP/LCP/CLS capture cannot serve as the "instant nav" bar; a NEW content-visible signal is required (D-15). D-14 mandates a prod build (the 13-perf lesson: never gate on dev). D-17 prevents stale due-counts/habitat on landing without an always-refetch, using the non-experimental router.refresh() already in the codebase (cacheComponents/revalidateTag would need a D-07 checkpoint). This plan closes with the immutable-baseline-diffed final run that certifies the phase.

Output: an extended e2e/13-perf.spec.ts (6 nav pairs + content-visible signal + prod-gated INP, afterAll guard preserved); D-17 router.refresh() invalidation at the mutation callsites; prefetch tuning; and the executed final full run + qa:run + habitat spot-check. autonomous:false — carries the final measured-run human-verify checkpoint (Phase 16-03 precedent + D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-performance-optimization/17-CONTEXT.md
@.planning/phases/17-performance-optimization/17-RESEARCH.md
@.planning/phases/17-performance-optimization/17-PATTERNS.md
@.planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md
@e2e/13-perf.spec.ts
@AGENTS.md

<interfaces>
<!-- Contracts consumed from prior plans. -->

From e2e/perf-markers.ts (17-01):
  PERF_READY_ATTR = "data-perf-ready"
  waitForPerfReady(page, timeoutMs): polls for [data-perf-ready="true"] — the D-15 "real content visible" probe
  IS_PROD_BUILD = process.env.PERF_PROD_BUILD === "1" — gates the D-14 nav pass + the task_d326ebac INP assertion to prod-build-only

From 17-03 / 17-04: each key route's real-content root carries data-perf-ready="true" (dashboard, study, new-card, browse).

Existing e2e/13-perf.spec.ts idioms to reuse:
  - measureVitals() (lines 36-178) — the per-route capture; the poll-for-marker loop (lines 142-151) is the shape waitForPerfReady mirrors
  - per-profile context setup (lines 185-224) — browser.newContext + signUpWithDeck from ./helpers
  - expect.soft() gate style (lines 257-270) — one failing pair does not hide others
  - afterAll writes 13-3d-habitat/perf-results.json best-effort guarded (lines 240-254) — MUST survive edits (commit 88c16ad)

D-17 mutation callsites (non-experimental router.refresh() pattern from deck-view.tsx / countdown-timer.tsx):
  - study-session.tsx: completion posts /api/study/complete then router.push (lines ~218, ~386) — ensure the landed dashboard/habitat reflects fresh state
  - card-add commit callsites (distributed across card-list / review-list / the new-card flow) — ensure a card add invalidates the dashboard/browse view so counts are fresh on landing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend e2e/13-perf.spec.ts — 6 hub-and-spoke nav pairs + content-visible (data-perf-ready) signal + prod-build-gated INP (task_d326ebac); preserve the afterAll stale-path guard</name>
  <files>e2e/13-perf.spec.ts</files>
  <read_first>
    - e2e/13-perf.spec.ts (WHOLE file — especially measureVitals lines 36-178, the poll-for-marker idiom 142-151, per-profile setup 185-224, the expect.soft gate 257-270, and the afterAll guard 240-254 that must survive; the INP assertion is at lines 266-268)
    - e2e/perf-markers.ts (waitForPerfReady + IS_PROD_BUILD + PERF_READY_ATTR from 17-01 — import these)
    - e2e/helpers.ts (signUpWithDeck at line 152 — reuse for context setup)
    - playwright.config.ts (baseURL http://localhost:3000, webServer undefined — the SAME port for next dev and next start; the reason the PERF_PROD_BUILD signal is required)
    - .planning/phases/17-performance-optimization/17-RESEARCH.md (Pitfalls 4+5: INP≠TBT, and prod-vs-dev server confusion; D-13/14/15 requirements; the content-visible signal design in Open Question 2)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the e2e/13-perf.spec.ts section: the exact idioms to extend, the content-visible poll pattern, the prod-build INP-gating conditional-block style)
  </read_first>
  <action>
    Add a nav-timing test group covering the 6 D-13 hub-and-spoke navigations (dashboard→study, study→dashboard, dashboard→new-card, new-card→dashboard, dashboard→browse, browse→dashboard) as IN-APP LINK TAPS (page.click on the in-app nav link, not page.goto). Browser Back is NOT gated (D-13). Every link/tap targeting /study MUST include ?deck={id} (it redirects to /dashboard without one). For each pair: warm the prefetch first (the destination link should be prefetched so the RSC payload is local before the tap — see Task 2), then time from the tap to when waitForPerfReady resolves (the destination's data-perf-ready='true' marker appears). Record contentVisibleMs. Run n≥5 per pair and take the median (mirror the existing median approach; the existing measureVitals discards run-1 as a cold hit — apply the same warm-up discipline).
    Gate PASS/FAIL with expect.soft(median_contentVisibleMs).toBeLessThanOrEqual(100) per pair (D-15; soft so one failing pair does not mask the others). Skeletons/loading.tsx do NOT count — waitForPerfReady only resolves on the real-content marker.
    D-14 prod-build gate: wrap the entire nav-timing group (and its assertions) so it ONLY runs when IS_PROD_BUILD is true (PERF_PROD_BUILD=1) — this is what makes it structurally impossible to measure next dev. If PERF_PROD_BUILD is not set, the nav group is skipped (test.skip) with a clear message that it requires a prod build.
    task_d326ebac (fold-in while in this file): wrap the EXISTING INP assertion (lines 266-268, `expect.soft(r.inp,...)`) in the same IS_PROD_BUILD guard so INP is only asserted against a confirmed prod build (dev-server INP is HMR-noise, not a real metric — this is the v4.0 backlog item). Mirror the existing `if (isMobile) {...}` conditional-block style.
    PRESERVE the afterAll stale-path write guard (lines 240-254): the try/catch that writes 13-3d-habitat/perf-results.json best-effort and never fails the suite on ENOENT (commit 88c16ad) must remain intact — do not remove or unguard it.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npx playwright test e2e/13-perf.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (full tsc — this task edits an e2e spec; biome's no-`!` rule can push a `box?.y` into an argument position causing a tsc error, so run full tsc, not just biome)
    - `npx playwright test e2e/13-perf.spec.ts --list` enumerates the 6 new nav-pair tests (or shows them as skipped when PERF_PROD_BUILD is unset — the list still shows them)
    - `grep -n "waitForPerfReady\|IS_PROD_BUILD\|PERF_READY_ATTR" e2e/13-perf.spec.ts` matches (the 17-01 scaffolds are wired in)
    - `grep -n "?deck=" e2e/13-perf.spec.ts` matches (study links carry the deck param)
    - The INP assertion (previously at lines 266-268) is now inside an IS_PROD_BUILD guard (grep shows the INP expect wrapped by the prod-build condition) — task_d326ebac done
    - The afterAll try/catch writing 13-3d-habitat/perf-results.json is STILL present and STILL guarded (grep confirms the try/catch + console.warn on failure survive)
    - `toBeLessThanOrEqual(100)` appears for the contentVisibleMs assertion (the D-15 bar)
    - `npx biome ci e2e/13-perf.spec.ts` reports no NEW errors
  </acceptance_criteria>
  <done>The spec measures 6 hub-and-spoke navs against a prod build via the content-visible marker with a ≤100ms bar; INP is prod-build-gated (task_d326ebac cleared); the afterAll stale-path guard survives.</done>
</task>

<task type="auto">
  <name>Task 2: D-16 prefetch tuning for the 6 gated links + D-17 mutation-triggered router.refresh() invalidation</name>
  <files>src/components/study-session.tsx, src/components/card-list.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/02-guides/prefetching.md (the STABLE prefetch model — automatic/manual/hover-triggered, client cache TTL, static-vs-dynamic prefetch table; D-16 wants the RSC payload local before the tap)
    - src/components/deck-view.tsx + src/components/countdown-timer.tsx (the EXISTING router.refresh() pattern to generalize — the only non-experimental invalidation mechanism in the codebase)
    - src/components/study-session.tsx (lines ~218 router.push('/habitat?celebrate=...') after /api/study/complete at line ~258; line ~386 router.push('/dashboard?deck=...') — the study-completion mutation→navigation callsites where dashboard due-counts/habitat state must be fresh on landing)
    - src/components/card-list.tsx (a card-add/edit commit surface — verify whether a card mutation needs an invalidation so the dashboard/browse view is fresh on landing)
    - .planning/phases/17-performance-optimization/17-RESEARCH.md (D-16 prefetch guidance; D-17 non-experimental invalidation = router.refresh(); the anti-pattern forbidding cacheComponents/revalidateTag without a D-07 checkpoint; the router.refresh() code example already in the codebase)
    - .planning/phases/17-performance-optimization/17-PATTERNS.md (the router.refresh() targeted-invalidation shared-pattern section)
  </read_first>
  <action>
    D-16 prefetch: for the 6 gated hub-and-spoke links (the in-app nav links between dashboard and study/new-card/browse), ensure Next prefetches the destination RSC payload so it is local before the tap. Consult prefetching.md for the STABLE mechanism (Link default prefetch vs explicit prefetch prop vs hover-triggered — pick per what the docs prescribe for these authenticated, mostly-static routes; this is a discretion-area call but must use a stable API, not unstable_instant which needs a D-07 checkpoint). If the links already prefetch by default, confirm and record that; only add explicit prefetch tuning if the default is insufficient. Remember /study links need ?deck={id}.
    D-17 invalidation: generalize the existing deck-view/countdown-timer router.refresh() pattern to the mutation callsites so landing shows fresh state WITHOUT an always-refetch:
    (a) study-session.tsx: after /api/study/complete succeeds and before/with the navigation (lines ~218/~258/~386), ensure the dashboard due-counts and habitat state are invalidated so the landed page reflects the completed session (not a stale cached count). Use router.refresh() (the non-experimental mechanism) — do NOT reach for cacheComponents/revalidateTag (D-07 checkpoint required).
    (b) card-add commit: verify whether adding a card leaves a stale dashboard/browse count on landing; if so, invalidate via router.refresh() at the commit callsite. If the existing flow already navigates in a way that produces fresh data (e.g. a full navigation that re-runs the server component), confirm and record that no extra invalidation is needed (D-12: no gold-plating; do not add refreshes that are not needed).
    The bar is D-17: cached destinations serve instantly, but mutations explicitly invalidate so numbers are correct on landing — no always-refetch, no visibly self-correcting numbers.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npm test && npm run build 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0
    - `grep -n "router.refresh" src/components/study-session.tsx` matches (study-completion invalidation wired) OR the SUMMARY records that the existing navigation already produces fresh dashboard/habitat state and cites why no extra refresh is needed
    - `grep -rn "cacheComponents\|revalidateTag\|use cache\|unstable_instant" src/components/study-session.tsx src/components/card-list.tsx` returns NOTHING (D-17 used the non-experimental router.refresh(), not an experimental API — no D-07 checkpoint bypassed)
    - `npm test` (full vitest) passes — study-session and card-list existing tests still green (isolate known flakes before treating as regressions)
    - Prefetch decision recorded in SUMMARY citing prefetching.md: either "Link default prefetch already warms these 6 links" or the specific stable tuning applied; /study links confirmed to carry ?deck={id}
    - `npx biome ci src/components/study-session.tsx src/components/card-list.tsx` reports no NEW errors
  </acceptance_criteria>
  <done>The 6 gated links prefetch their destination RSC payload via a stable API; study-completion (and card-add if needed) invalidate via router.refresh() so landing shows fresh counts/habitat with no always-refetch; no experimental API was used.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Official phase-end proof — final full measure-cwv run + PERF-04 prod-build nav gate + qa:run + /habitat</name>
  <action>PAUSE for human verification. Run the official phase-end proof (see how-to-verify): the full 4-route×2-preset measure-cwv after-record vs the Phase 16 baseline, the PERF-04 6-nav gate against a local prod build, qa:run for criterion-4, and the /habitat inclusion — then present results and confirm certification or raise the final D-04 decision. All >10-min runs via run_in_background. Do not self-approve.</action>
  <files>.planning/phases/17-performance-optimization/measurements/ (final full-run after-record; no source files)</files>
  <done>All four PERF-03 route gates pass (or D-04 decisions made); PERF-04 6-nav prod-build gate passes; qa:run green (criterion-4); /habitat non-regressed; immutable baseline shows zero git diff across the whole phase.</done>
  <what-built>
    The complete Phase 17 optimization: shared-infra hygiene (Plan 02), dashboard RSC split (Plan 03), the three-route RSC/Motion work (Plan 04), and the PERF-04 instant-nav gate + D-17 invalidation (this plan, Tasks 1-2). This checkpoint runs the OFFICIAL phase-end proof — the immutable-baseline-diffed final measurements plus the correctness gates — and is where the phase is certified. This is the autonomous:false measured-run checkpoint (Phase 16-03 precedent) plus the final D-04 gate-vs-fidelity trigger.
  </what-built>
  <how-to-verify>
    The executor runs the following LONG operations (all >10 min — run via run_in_background) and presents the results:
    1. OFFICIAL FINAL RUN (PERF-03 after-record): full 4-route × 2-preset measure-cwv run (~28 min, sequential, DATABASE_URL exported, self-cleans *test.local), written to the Phase-17 measurements dir (PHASE_OUT_DIR default — NEVER overwriting baseline/). Diff each route's mobile medians against the Phase 16 baseline:
       - /dashboard 518→ , /study 712→ , /deck/new-card 891→ , /deck/browse 608→  (TBT, all must be ≤200)
       - Perf: 86/82/79/84 → all must be ≥90; LCP ≤2500, CLS ≤0.1 (confirm still passing)
       - Every route that passes = its optimization landed with a measured before/after (PERF-03 satisfied). Any route that provably cannot reach its gate within D-01..03 fidelity → D-04 checkpoint with the measured evidence + specific proposed visible change; Josh approves/vetoes.
    2. /habitat inclusion in the final run (D-11): confirm /habitat still passes (regression guard); it stays OUT of the key-route gate set but is included in the final full run.
    3. PERF-04 gate (criterion-3): run the extended e2e/13-perf.spec.ts against a LOCAL PROD BUILD — `next build && next start` then `PERF_PROD_BUILD=1 npx playwright test e2e/13-perf.spec.ts` (per-project web then mobile, never combined). Confirm all 6 hub-and-spoke pairs render real content ≤100ms median. Verify it ran against the prod build (not dev) via the PERF_PROD_BUILD gate.
    4. CRITERION-4 proof: `npm run qa:run` against a fresh dev server (qa-01/qa-03 need a STUDY_COOLDOWN_MINUTES=1 server boot per the Phase 15 regime) — the core-journey harness still passes after all refactors (no learning-pipeline regressions).
    5. D-17 spot-check: manually complete a study session and confirm the dashboard due-count + habitat state are correct on landing (not stale, not visibly self-correcting); add a card and confirm the count is fresh on landing.
    6. Confirm `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` is EMPTY (immutable baseline untouched across the ENTIRE phase).
  </how-to-verify>
  <resume-signal>Type "approved" when all four PERF-03 route gates pass (or D-04 decisions are made), the PERF-04 6-nav gate passes against the prod build, qa:run is green, and D-17 landing-freshness is confirmed; or describe the gap to close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → prod Neon DB | the final full run + /habitat provision + self-clean *test.local users via DATABASE_URL on the prod-shared DB |
| prod-build gate → e2e server | the PERF_PROD_BUILD signal must correctly distinguish next start from next dev (both on port 3000) or the gate measures the wrong server |
| mutation → cache freshness | D-17 invalidation affects data correctness on landing after study completion / card add — under-invalidation shows stale counts, over-invalidation defeats the instant-nav goal |
| e2e afterAll → filesystem | the afterAll writes to an archived 13-3d-habitat path; a broken write must never fail the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | Tampering | PERF_PROD_BUILD prod-vs-dev signal | mitigate | The nav gate + INP assertion run ONLY when PERF_PROD_BUILD=1 (test.skip otherwise), making it structurally impossible to certify against next dev (Pitfall 5); the checkpoint step 3 explicitly confirms the prod build was the target |
| T-17-05-02 | Spoofing/Correctness | D-17 stale-vs-fresh data on landing | mitigate | router.refresh() explicitly invalidates after study-completion/card-add so due-counts + habitat state are correct on landing; checkpoint step 5 manually verifies no stale/self-correcting numbers; no always-refetch (defeats instant-nav) and no experimental cache API (would need D-07) |
| T-17-05-03 | Tampering | final-run OUT_DIR | mitigate | PHASE_OUT_DIR default writes to the Phase-17 measurements dir; checkpoint step 6 requires zero git diff under the immutable baseline/ across the whole phase |
| T-17-05-04 | Denial of Service | e2e afterAll stale-path write | mitigate | The best-effort try/catch (commit 88c16ad) writing 13-3d-habitat/perf-results.json is preserved so an ENOENT on the archived path never fails the perf suite |
| T-17-05-SC | Tampering | npm installs | accept | No new packages (test extension + invalidation wiring only); no legitimacy checkpoint needed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (full tsc — this plan edits an e2e spec, per the "run full tsc after any e2e edit" constraint).
- `npx playwright test e2e/13-perf.spec.ts --list` enumerates the 6 nav pairs.
- `npm test` + `npm run build` green after the D-17 wiring.
- FINAL PROOF (at the checkpoint): full 4-route×2-preset measure-cwv (official after-record) + PERF-04 6-nav gate against a local prod build + `npm run qa:run` (criterion-4) + /habitat inclusion — all green, diffed against the immutable Phase 16 baseline.
- `git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/` EMPTY across the entire phase.
- Scoped `npx biome ci <touched files>` shows no new errors.
- All >10-min runs executed via run_in_background.
</verification>

<success_criteria>
PERF-04 is met: 6 hub-and-spoke navs render real content ≤100ms median against a local prod build, gated so dev-server runs cannot certify it; task_d326ebac is cleared (INP prod-build-only). D-16 prefetch warms the 6 links via a stable API; D-17 mutations invalidate via router.refresh() so landing shows fresh counts/habitat with no always-refetch and no experimental API. The final full 4-route×2-preset run is the official PERF-03 after-record vs the Phase 16 baseline (all four route gates pass or D-04 decisions made); qa:run proves criterion-4; /habitat is non-regressed; the afterAll guard survives; the immutable baseline is untouched across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/17-performance-optimization/17-05-SUMMARY.md` when done.
</output>
