---
phase: 16-performance-baseline-measure
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - scripts/measure-cwv.mjs
  - package.json
autonomous: true
requirements: [PERF-01, PERF-02]
user_setup:
  - service: neon
    why: "Direct-DB provisioning of the *test.local user/deck/cards and cleanup (same env as Phase 15)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon dashboard -> project -> connection string (the prod-shared DB used by Phase 15)"

must_haves:
  truths:
    - "The harness measures the REAL authenticated routes, not the /login shell: it signs up a *test.local user on prod, injects the __Secure-better-auth.session_token cookie into a puppeteer-core page, and fails loud if any Lighthouse run lands on /login (D-01)"
    - "Before measuring, the harness provisions a *test.local user with a deck + cards so /study, /deck/browse, /deck/new-card render realistic non-empty state (D-02)"
    - "The ROUTES constant is exactly ['/dashboard','/study','/deck/new-card','/deck/browse'] — /habitat is excluded (D-03)"
    - "CWV numbers come strictly from warm Vercel prod (leocards.vercel.app); the local .next build is read ONLY for bundle composition — no local-prod CWV (D-05)"
    - "The run matrix is n=6 per route x preset (1 warm-up discarded + median of runs 2-6), across mobile + desktop presets, executed sequentially (D-06)"
    - "The harness never logs the session token/password; the provisioned user is *@test.local and a finally block always invokes cleanup-test-users.mjs with %@test.local (Phase-15 self-clean pattern, T-16-SC / QAJ-06 lineage)"
    - "Every scripted POST to /api/auth/sign-up/email sends Origin: https://leocards.vercel.app so better-auth originCheckMiddleware does not 403 (RESEARCH Pitfall 1)"
  artifacts:
    - path: "scripts/measure-cwv.mjs"
      provides: "The PERF-01 harness: inlined auth+provision, puppeteer-core launch + cookie injection, sequential Lighthouse run loop (mobile+desktop presets), redirect guard, wires scripts/measure-cwv-lib.mjs for medians/bundle/classification/report writing, finally-cleanup"
      min_lines: 180
    - path: "package.json"
      provides: "measure:cwv + measure:cleanup npm scripts"
      contains: "measure:cwv"
  key_links:
    - from: "scripts/measure-cwv.mjs"
      to: "scripts/measure-cwv-lib.mjs"
      via: "import { median, computeMedians, extractMetrics, getBundleKb, classifyBottleneck, renderRouteReport, renderSummary }"
      pattern: "from ['\"]\\./measure-cwv-lib\\.mjs['\"]"
    - from: "scripts/measure-cwv.mjs signUp"
      to: "prod /api/auth/sign-up/email"
      via: "fetch with Origin header"
      pattern: "Origin"
    - from: "scripts/measure-cwv.mjs"
      to: "puppeteer-core page + Lighthouse navigation"
      via: "lighthouse.navigation(page, url, {config, flags})"
      pattern: "navigation\\("
    - from: "scripts/measure-cwv.mjs finally"
      to: "scripts/cleanup-test-users.mjs"
      via: "spawnSync with %@test.local + CLEANUP_DB_URL"
      pattern: "cleanup-test-users\\.mjs"
---

<objective>
Build `scripts/measure-cwv.mjs` — the codified, repeatable warm-prod CWV harness that replaces the ad-hoc shell commands in 13-PERF-REAL.md (PERF-01). It authenticates a realistic `*test.local` user on prod, drives Lighthouse (Node API, via a puppeteer-core page) across the 4 key routes x 2 presets at n=6, and uses the Plan-01 pure lib to compute medians, parse bundle composition, classify the bottleneck, and render the per-route + summary reports (PERF-02). It self-cleans in a `finally`.

This plan produces the harness CODE only — it is syntax-checkable (`node --check`) and its pure wiring is covered by Plan 01's tests, but it is NOT executed against prod here. The actual immutable-baseline RUN is Plan 03 (Wave 3), so that a green Wave-2 gate never depends on live-prod availability.

Purpose: One command, repeatable, diffable — the measurement instrument Phase 17 will re-run before/after every optimization.
Output: `scripts/measure-cwv.mjs` + `measure:cwv` / `measure:cleanup` npm scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-performance-baseline-measure/16-RESEARCH.md
@.planning/phases/16-performance-baseline-measure/16-PATTERNS.md

<interfaces>
<!-- Contracts this plan CONSUMES from Plan 01 (scripts/measure-cwv-lib.mjs) — import, do not reimplement: -->
//   median, computeMedians, extractMetrics, getBundleKb, classifyBottleneck, renderRouteReport, renderSummary
//   Metric shape: { lcp, tbt, cls, fcp, ttfb, score, bootupTime }
//   getBundleKb(stats, route) -> { kb, chunks, chunkPaths }
//   classifyBottleneck(metrics, bundleKb) -> { class: 'bundle'|'RSC waterfall'|'hydration', score }

<!-- Analog helpers this plan INLINES from scripts/qa-lib.mjs (do NOT import qa-lib — its module-level -->
<!-- DEBUG_CHEAT_SECRET guard process.exit(1)s at load; RESEARCH Pitfall 5). Copy + adapt: -->
// extractSessionCookie(res)  — qa-lib.mjs lines 89-101 (regex better-auth\.session_token=([^;]+))
// signUp(baseUrl,email,pw)   — qa-lib.mjs lines 127-137 BUT ADD `Origin: baseUrl` header (prod CSRF, RESEARCH Pitfall 1)
// mintTestEmail()            — qa-lib.mjs lines 186-188, but MUST end @test.local (use a cwv+ local-part)
// provision(baseUrl,opts)    — qa-lib.mjs lines 210-252 (signUp -> getUserId -> Drizzle insert deck+cards)
// getUserId(baseUrl,token)   — qa-lib.mjs lines 168-176

<!-- Cleanup contract (scripts/cleanup-test-users.mjs — VERIFIED): reads process.env.CLEANUP_DB_URL -->
<!-- (NOT DATABASE_URL), guards pattern must end @(leocards-)?test.local, takes pattern as argv[2]. -->
<!-- So the spawn env MUST set CLEANUP_DB_URL = CLEANUP_DB_URL ?? DATABASE_URL and pass '%@test.local'. -->

<!-- Drizzle schema (VERIFIED src/db/schema.ts): -->
// export const decks = pgTable("decks", { id, userId, language, name, ... })  // language + name NOT NULL
// export const cards = pgTable("cards", { id, deckId, front, back, source, ... })  // front/back/source NOT NULL
// import { cards, decks } from "../src/db/schema.ts"  (works on Node 25.8.1 per qa-lib.mjs)

<!-- Bundle stats (VERIFIED .next/diagnostics/route-bundle-stats.json): array of -->
//   { route, firstLoadUncompressedJsBytes, firstLoadChunkPaths[] } — all 4 target routes present.

<!-- Lighthouse Node API (VERIFIED node_modules/lighthouse/core/index.js): -->
//   import lighthouse from 'lighthouse/core/index.js'; await lighthouse.navigation(page, url, {config, flags})
//   result.lhr.audits['largest-contentful-paint'|'total-blocking-time'|'cumulative-layout-shift'|
//                     'first-contentful-paint'|'server-response-time'|'bootup-time'].numericValue
//   result.lhr.categories.performance.score  (0-1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, env guard, inlined auth + provision, puppeteer launch + cookie injection</name>
  <files>scripts/measure-cwv.mjs</files>
  <read_first>
    - scripts/qa-lib.mjs (INLINE source: extractSessionCookie 89-101, signUp 127-137, mintTestEmail 186-188, getUserId 168-176, provision 210-252, Drizzle client setup 23-57) — copy the bodies, adapting per the notes below
    - .planning/phases/16-performance-baseline-measure/16-PATTERNS.md §"Shebang + module header", §"Imports block", §"Env guard", §"Inlined auth helpers", §"Puppeteer-core browser launch + cookie injection" (lines 31-190 — the exact adaptation excerpts)
    - .planning/phases/16-performance-baseline-measure/16-RESEARCH.md §"Pattern 1" (auth + cookie), §"Common Pitfalls" 1/2/4/5 (Origin header, __Secure- prefix, cookie re-inject, qa-lib exit guard)
    - src/db/schema.ts lines 83-101 (decks/cards column contracts — front/back/source/language/name NOT NULL)
    - scripts/cleanup-test-users.mjs (the cleanup contract this file will call in Task 3 — note it reads CLEANUP_DB_URL, not DATABASE_URL)
  </read_first>
  <action>
    Create scripts/measure-cwv.mjs with: (1) shebang `#!/usr/bin/env node` + the header/USAGE/SECURITY block (PATTERNS lines 33-66) documenting required env DATABASE_URL, optional PROD_URL (default `https://leocards.vercel.app`), CHROME_PATH (default the Playwright chromium-1208 path), CLEANUP_DB_URL. (2) Imports: `spawnSync` from node:child_process; `readFile, mkdir, writeFile, rename` from node:fs/promises; `path`; `fileURLToPath`; `lighthouse from 'lighthouse/core/index.js'`; `puppeteer from 'puppeteer-core'`; `neon`, `drizzle`; `cards, decks from '../src/db/schema.ts'`; and the 7 named exports from `./measure-cwv-lib.mjs`. (3) `__filename/__dirname/ROOT` trio + `const PROD_URL = process.env.PROD_URL ?? 'https://leocards.vercel.app'` + `const CHROME_PATH = process.env.CHROME_PATH ?? 'C:\\Users\\jfwas\\AppData\\Local\\ms-playwright\\chromium-1208\\chrome-win64\\chrome.exe'`. (4) Env guard: FATAL exit if `DATABASE_URL` unset; include the comment that DEBUG_CHEAT_SECRET is intentionally NOT checked and qa-lib is NOT imported (PATTERNS lines 101-107). (5) Drizzle client `const db = drizzle({ client: neon(process.env.DATABASE_URL) })`. (6) INLINE `extractSessionCookie` (verbatim from qa-lib). (7) INLINE `signUp` adding `Origin: baseUrl` to headers (RESEARCH Pitfall 1) and name 'CWV Tester'. (8) INLINE `mintTestEmail` returning `cwv+${Date.now()}+${rand}@test.local` (MUST be @test.local). (9) INLINE `getUserId` + `provision` (deck: language 'fr', name; cards from opts.cards with source 'manual'). (10) `async function launchBrowser()` -> `puppeteer.launch({ executablePath: CHROME_PATH, headless: true, args: ['--no-sandbox','--disable-dev-shm-usage'] })` with a helpful error if launch fails (bad CHROME_PATH — RESEARCH Pitfall/failure-mode 4). (11) `async function injectCookie(page, token)` -> `page.setCookie({ name: '__Secure-better-auth.session_token', value: token, domain: 'leocards.vercel.app', path: '/', secure: true, httpOnly: true, sameSite: 'Lax' })` (PATTERNS lines 177-187). Log only the email (never the token/password), using a `[measure-cwv]` prefix. Do NOT add the Lighthouse loop or report writing yet — Tasks 2 and 3.
  </action>
  <verify>
    <automated>node --check scripts/measure-cwv.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/measure-cwv.mjs` exits 0
    - File imports the lib: `grep -c "from ['\"]./measure-cwv-lib.mjs['\"]" scripts/measure-cwv.mjs` >= 1
    - File does NOT import qa-lib: `grep -c "qa-lib" scripts/measure-cwv.mjs` returns 0
    - signUp sends Origin: `grep -c "Origin" scripts/measure-cwv.mjs` >= 1
    - mintTestEmail targets test.local: `grep -c "@test.local" scripts/measure-cwv.mjs` >= 1
    - Cookie name is the prod __Secure- form: `grep -c "__Secure-better-auth.session_token" scripts/measure-cwv.mjs` >= 1
    - DATABASE_URL guard present: `grep -c "DATABASE_URL" scripts/measure-cwv.mjs` >= 1
    - No token logging: `grep -c "console.log(.*sessionToken\|console.log(.*token\|console.log(.*password" scripts/measure-cwv.mjs` returns 0
  </acceptance_criteria>
  <done>node --check passes; lib imported (qa-lib NOT); Origin header + @test.local + __Secure- cookie + DATABASE_URL guard present; token/password never logged.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse preset configs + sequential n=6 run loop + redirect guard + median wiring</name>
  <files>scripts/measure-cwv.mjs</files>
  <read_first>
    - .planning/phases/16-performance-baseline-measure/16-RESEARCH.md §"Code Examples" (mobileConfig lines 534-549, desktop preset lines 271-282), §"Pattern 1" step 4-5, §"Common Pitfalls" 3 (discard cold run 1) + 4 (re-inject cookie each run)
    - .planning/phases/16-performance-baseline-measure/16-PATTERNS.md §"Lighthouse run loop structure" (lines 194-235: the ROUTES/PRESETS/N_RUNS constants + sequential double-for loop cadence mirroring qa-run.mjs)
    - scripts/measure-cwv-lib.mjs (extractMetrics + computeMedians + median — import and call, do not reimplement)
    - scripts/qa-run.mjs lines 197-231 (the sequential for-loop cadence + [prefix] progress logging to mirror)
  </read_first>
  <action>
    Add to scripts/measure-cwv.mjs: (1) `const ROUTES = ['/dashboard','/study','/deck/new-card','/deck/browse']` (exactly these four — D-03, /habitat excluded) and `const PRESETS = ['mobile','desktop']` and `const N_RUNS = 6` (D-06: discard run 0, median of runs 1-5). (2) `const mobileConfig` and `const desktopConfig` objects per RESEARCH: mobile = `{ extends: 'lighthouse:default', settings: { formFactor:'mobile', throttling:{ rttMs:150, throughputKbps:1638, requestLatencyMs:562, downloadThroughputKbps:1474, uploadThroughputKbps:675, cpuSlowdownMultiplier:4 }, screenEmulation:{ mobile:true, width:412, height:823, deviceScaleFactor:1.75, disabled:false }, onlyCategories:['performance'], disableFullPageScreenshot:true } }`; desktop = `{ extends:'lighthouse:default', settings:{ formFactor:'desktop', throttling:{ cpuSlowdownMultiplier:1 }, screenEmulation:{ mobile:false, width:1350, height:940, deviceScaleFactor:1 }, onlyCategories:['performance'] } }`. (3) `async function measureRoutexPreset(browser, route, preset, token)` that opens a fresh page, loops i=0..N_RUNS-1: `await injectCookie(page, token)` BEFORE each run (Pitfall 4), calls `await lighthouse.navigation(page, `${PROD_URL}${route}`, { config: preset==='mobile'?mobileConfig:desktopConfig, flags:{ logLevel:'silent' } })`, then a REDIRECT GUARD: read `result.lhr.finalDisplayedUrl` (or `finalUrl`) and if it includes `/login` (or does not include the requested route path) THROW `[measure-cwv] auth FAILED — ${route} run ${i} landed on /login; session cookie not honored` (Pitfall 2 — the silent-garbage-baseline failure mode). Push `extractMetrics(result.lhr)` to a `runs` array; close the page. Return `{ route, preset, runs, warmRuns: runs.slice(1), medians: computeMedians(runs.slice(1)) }`. (4) A driving double-for loop over ROUTES x PRESETS calling `measureRoutexPreset` SEQUENTIALLY (never parallel — RESEARCH: self-contention skews TBT), logging `[measure-cwv] --- ${route} x ${preset} ---` progress. Collect results into a Map keyed `${route}::${preset}`. Do NOT write report files yet (Task 3). Keep the token from provision in scope for injection.
  </action>
  <verify>
    <automated>node --check scripts/measure-cwv.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/measure-cwv.mjs` exits 0
    - ROUTES is exactly the 4 targets and excludes habitat: `grep -c "'/dashboard'" scripts/measure-cwv.mjs` >= 1 AND `grep -c "/habitat" scripts/measure-cwv.mjs` returns 0
    - n=6 run count present: `grep -c "N_RUNS = 6\|N_RUNS=6" scripts/measure-cwv.mjs` >= 1
    - Both presets configured: file contains `formFactor` with both `'mobile'` and `'desktop'` (`grep -c "formFactor" scripts/measure-cwv.mjs` >= 2)
    - Cookie re-injected inside the run loop: `injectCookie` appears in the per-run loop body (`grep -c "injectCookie" scripts/measure-cwv.mjs` >= 2 — the helper def in Task 1 + the loop call)
    - Redirect guard present: `grep -c "/login" scripts/measure-cwv.mjs` >= 1 AND references `finalDisplayedUrl` or `finalUrl`
    - Uses imported computeMedians (not a local reimpl): `grep -c "computeMedians(" scripts/measure-cwv.mjs` >= 1
    - Lighthouse invoked via navigation: `grep -c "navigation(" scripts/measure-cwv.mjs` >= 1
  </acceptance_criteria>
  <done>node --check passes; exactly 4 routes (no /habitat); n=6; mobile+desktop configs; per-run cookie re-inject; redirect guard that throws on /login; medians via imported computeMedians; sequential loop.</done>
</task>

<task type="auto">
  <name>Task 3: Report output (raw JSON + markdown per route + summary), finally-cleanup, npm scripts</name>
  <files>scripts/measure-cwv.mjs, package.json</files>
  <read_first>
    - .planning/phases/16-performance-baseline-measure/16-PATTERNS.md §"Bundle composition parse" (lines 277-295 — read the LOCAL route-bundle-stats.json, D-05), §"Report file I/O" (lines 319-337 — mkdir + atomic .tmp/rename write to the baseline/ dir), §"Cleanup in finally block" (lines 343-364 — the exact CLEANUP_DB_URL + %@test.local spawnSync), §"Summary console output" (lines 371-387), §"package.json scripts block" (lines 494-510)
    - scripts/measure-cwv-lib.mjs (getBundleKb, classifyBottleneck, renderRouteReport, renderSummary — import and call)
    - scripts/qa-run.mjs lines 232-312 (finally cleanup + results-summary + exit-code pattern to mirror exactly)
    - .planning/phases/16-performance-baseline-measure/16-RESEARCH.md §"Recommended Project Structure" (lines 169-189 — the baseline/ output layout + file naming) and §"Pitfall 6" (build-freshness note for the USAGE comment)
    - package.json (scripts block — add entries next to qa:run / qa:cleanup, same node scripts/ convention)
  </read_first>
  <action>
    Complete scripts/measure-cwv.mjs and update package.json. In measure-cwv.mjs: (1) `async function readBundleStats()` reading `path.join(ROOT,'.next','diagnostics','route-bundle-stats.json')` and JSON.parse (D-05: LOCAL build for bundle ONLY — this is the sole `.next/` read; NO prod call for bundle). (2) `const OUT_DIR = path.join(ROOT,'.planning','phases','16-performance-baseline-measure','baseline')` + `await mkdir(OUT_DIR, { recursive: true })`. (3) A `slugify(route)` mapping `/dashboard`->`dashboard`, `/study`->`study`, `/deck/new-card`->`deck-new-card`, `/deck/browse`->`deck-browse`. (4) After the measurement loop: for each route, write `${slug}-${preset}-runs.json` atomically (write `.tmp` then `rename`, PATTERNS lines 333-336) containing `{ route, preset, runs, warmRuns, medians }`; compute `const bundle = getBundleKb(stats, route)`; compute `const bottleneck = classifyBottleneck(medians_mobile, bundle.kb)` (use the MOBILE medians as the classification basis — mobile is the constrained profile per D-06/13.1); write `${slug}-baseline.md` from `renderRouteReport({ route, dateIso, mobile, desktop, bundle, bottleneck })`. (5) Write `16-BASELINE-SUMMARY.md` in OUT_DIR from `renderSummary(rows)`. (6) Wrap the ENTIRE setup+measure+report body in `try { ... } finally { ... }`; the finally MUST: `await browser.close()` (guard for undefined if launch failed), then spawnSync cleanup EXACTLY per PATTERNS lines 350-363 — `const cleanupEnv = { ...process.env, CLEANUP_DB_URL: process.env.CLEANUP_DB_URL ?? process.env.DATABASE_URL }` and `spawnSync(process.execPath, [path.join(ROOT,'scripts','cleanup-test-users.mjs'), '%@test.local'], { stdio:'inherit', env: cleanupEnv })`, logging FAILED if non-zero. (7) A results summary table + `process.exit(0/1)` mirroring qa-run.mjs lines 272-311. In package.json: add `"measure:cwv": "node scripts/measure-cwv.mjs"` and `"measure:cleanup": "node scripts/cleanup-test-users.mjs %@test.local"` to the scripts block (do not disturb existing entries). Note in the file header USAGE that `npm run build` must run first so bundle stats match the deployed commit (Pitfall 6).
  </action>
  <verify>
    <automated>node --check scripts/measure-cwv.mjs && node -e "const p=require('./package.json'); if(!p.scripts['measure:cwv']||!p.scripts['measure:cleanup']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/measure-cwv.mjs` exits 0
    - Bundle read is the sole local .next read, atomic-write present: `grep -c "route-bundle-stats.json" scripts/measure-cwv.mjs` >= 1 AND `grep -c ".tmp" scripts/measure-cwv.mjs` >= 1 AND `grep -c "rename(" scripts/measure-cwv.mjs` >= 1
    - Output dir is the baseline/ path: `grep -c "16-performance-baseline-measure.*baseline\|baseline" scripts/measure-cwv.mjs` >= 1 AND `mkdir` present
    - Both artifact types written per route: file contains `-runs.json` and `-baseline.md` literals
    - Summary written: `grep -c "16-BASELINE-SUMMARY.md" scripts/measure-cwv.mjs` >= 1
    - Cleanup in finally with correct env + pattern: `grep -c "cleanup-test-users.mjs" scripts/measure-cwv.mjs` >= 1 AND `grep -c "CLEANUP_DB_URL" scripts/measure-cwv.mjs` >= 1 AND `grep -c "%@test.local" scripts/measure-cwv.mjs` >= 1 AND `grep -c "finally" scripts/measure-cwv.mjs` >= 1
    - Renderers wired from lib: `grep -c "renderRouteReport(\|renderSummary(\|getBundleKb(\|classifyBottleneck(" scripts/measure-cwv.mjs` >= 4
    - npm scripts present: `node -e "const p=require('./package.json'); process.exit(p.scripts['measure:cwv']&&p.scripts['measure:cleanup']?0:1)"` exits 0
    - package.json still valid JSON and existing qa scripts intact: `node -e "const p=require('./package.json'); process.exit(p.scripts['qa:run']?0:1)"` exits 0
  </acceptance_criteria>
  <done>node --check passes; per-route raw JSON + markdown + summary written to baseline/ via atomic writes; bundle read from local .next only; finally-cleanup with CLEANUP_DB_URL + %@test.local; renderers/classifier wired from the lib; measure:cwv + measure:cleanup npm scripts added without breaking existing ones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> prod /api/auth/sign-up/email | Untrusted script crosses into prod auth; better-auth validates Origin (CSRF) |
| harness -> Neon prod-shared DB (provision) | INSERT-only writes of a *test.local user/deck/cards on the shared DB |
| harness -> Vercel prod (Lighthouse navigation) | Reads pages using an injected session cookie; the token is a secret in transit |
| harness -> local .next/ (bundle stats) | Reads a build artifact; no untrusted input |
| harness -> cleanup-test-users.mjs (destructive) | DELETE gated to *test.local domain by the cleanup script's own guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Spoofing / Repudiation | sign-up POST to prod | mitigate | Send `Origin: https://leocards.vercel.app` on every auth POST (RESEARCH Pitfall 1); without it better-auth returns 403 — fail-fast, no silent bad state |
| T-16-04 | Information disclosure | session token / password in logs | mitigate | Never `console.log` token/password/cookie value (qa-lib discipline); only the *test.local email is logged. Enforced by Task-1 acceptance criterion (no token-logging grep) |
| T-16-05 | Tampering | residual *test.local user in prod DB | mitigate | `finally` always spawns cleanup-test-users.mjs `%@test.local` with `CLEANUP_DB_URL`; cleanup script re-guards the domain; runs even on measurement failure |
| T-16-06 | Elevation of privilege | direct-DB provisioning | mitigate | provision() only INSERTs *test.local rows (never UPDATE/DELETE arbitrary rows); mirrors the Phase-15 provisioning contract |
| T-16-07 | Tampering (silent) | auth silently fails -> baseline measures /login shell | mitigate | Redirect guard in Task 2 THROWS if any Lighthouse run's finalUrl lands on /login (Pitfall 2/4) — a garbage-but-green baseline is prevented |
| T-16-SC | Tampering | npm installs (supply chain) | mitigate | RESEARCH §Package Legitimacy Audit: ZERO new packages. All imports (`lighthouse@13.3.0`, `puppeteer-core@24.43.1`, `@neondatabase/serverless`, `drizzle-orm`, node built-ins) are already installed + audited. No install step, no [ASSUMED]/[SUS] packages, no legitimacy checkpoint required. |
</threat_model>

<verification>
- `node --check scripts/measure-cwv.mjs` exits 0 (the .mjs is outside tsconfig; this is its syntax gate — tsc does not cover it).
- `node -e` package.json guard confirms `measure:cwv` + `measure:cleanup` added and existing scripts intact.
- Full `npx vitest run` remains green (this plan adds no test but must not break collection; Plan-01 lib tests still pass since the lib is unchanged).
- `tsc --noEmit` remains green (no TS files changed).
- `npx biome ci scripts/measure-cwv.mjs package.json` — scoped to touched files only.
- NOT run here: the harness against prod. That is Plan 03 (Wave 3) so this wave's gate is offline-deterministic.
</verification>

<success_criteria>
- `scripts/measure-cwv.mjs` exists, passes `node --check`, imports the Plan-01 lib (does NOT import qa-lib), and inlines auth+provision with the prod `Origin` header + `@test.local` email + `__Secure-` cookie.
- The run matrix is exactly 4 routes x 2 presets x n=6 (discard run 1), sequential, with a redirect guard that fails loud on `/login`.
- Reports (raw JSON + markdown per route + summary) are written to `.planning/phases/16-performance-baseline-measure/baseline/` via atomic writes, using the Plan-01 renderers + classifier; bundle composition read from the LOCAL `.next` build only.
- `finally` self-cleans `*@test.local` via cleanup-test-users.mjs with `CLEANUP_DB_URL`; `measure:cwv` + `measure:cleanup` npm scripts added without breaking existing ones.
- Zero new npm packages introduced.
</success_criteria>

<output>
Create `.planning/phases/16-performance-baseline-measure/16-02-SUMMARY.md` when done.
</output>
