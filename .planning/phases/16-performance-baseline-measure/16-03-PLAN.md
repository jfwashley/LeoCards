---
phase: 16-performance-baseline-measure
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - .planning/phases/16-performance-baseline-measure/baseline/dashboard-mobile-runs.json
  - .planning/phases/16-performance-baseline-measure/baseline/dashboard-desktop-runs.json
  - .planning/phases/16-performance-baseline-measure/baseline/dashboard-baseline.md
  - .planning/phases/16-performance-baseline-measure/baseline/study-mobile-runs.json
  - .planning/phases/16-performance-baseline-measure/baseline/study-desktop-runs.json
  - .planning/phases/16-performance-baseline-measure/baseline/study-baseline.md
  - .planning/phases/16-performance-baseline-measure/baseline/deck-new-card-mobile-runs.json
  - .planning/phases/16-performance-baseline-measure/baseline/deck-new-card-desktop-runs.json
  - .planning/phases/16-performance-baseline-measure/baseline/deck-new-card-baseline.md
  - .planning/phases/16-performance-baseline-measure/baseline/deck-browse-mobile-runs.json
  - .planning/phases/16-performance-baseline-measure/baseline/deck-browse-desktop-runs.json
  - .planning/phases/16-performance-baseline-measure/baseline/deck-browse-baseline.md
  - .planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md
autonomous: false
requirements: [PERF-01, PERF-02]
user_setup:
  - service: neon
    why: "Provision + clean the *test.local user against the prod-shared DB during the real run"
    env_vars:
      - name: DATABASE_URL
        source: "Neon dashboard -> connection string (same prod-shared DB as Phase 15)"

must_haves:
  truths:
    - "Running one npm script (npm run measure:cwv) produces warm-prod Lighthouse medians at n>=5 for mobile + desktop across all four key routes — the codified replacement for 13-PERF-REAL.md's ad-hoc shell commands (PERF-01, D-06)"
    - "Each of the four routes has a committed markdown baseline report with median CWV numbers + bundle composition + a ranked bottleneck classification naming its single top Phase-17 target (PERF-02, D-04, D-07)"
    - "Each route also has committed raw JSON run data for exact machine diffing in Phase 17 (D-04)"
    - "CWV medians were captured from warm prod (leocards.vercel.app) with run 1 discarded as the cold-Vercel warm-up; bundle composition came from a fresh local next build only (D-05, D-06)"
    - "Exactly the four roadmap routes are baselined; /habitat is NOT re-baselined (D-03)"
    - "The real run authenticated a *test.local user with a provisioned deck + cards (realistic non-empty state) and self-cleaned — cleanup-test-users.mjs left zero *test.local residue in prod (D-01, D-02)"
    - "No optimization change lands in this phase — the committed baseline is the immutable before-reference every Phase-17 change diffs against (ROADMAP SC4)"
  artifacts:
    - path: ".planning/phases/16-performance-baseline-measure/baseline/dashboard-baseline.md"
      provides: "Committed /dashboard baseline report (medians + bundle + bottleneck)"
      contains: "Bottleneck"
    - path: ".planning/phases/16-performance-baseline-measure/baseline/study-baseline.md"
      provides: "Committed /study baseline report"
      contains: "Bottleneck"
    - path: ".planning/phases/16-performance-baseline-measure/baseline/deck-new-card-baseline.md"
      provides: "Committed /deck/new-card baseline report"
      contains: "Bottleneck"
    - path: ".planning/phases/16-performance-baseline-measure/baseline/deck-browse-baseline.md"
      provides: "Committed /deck/browse baseline report"
      contains: "Bottleneck"
    - path: ".planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md"
      provides: "Cross-route summary table (the immutable before-reference index)"
      contains: "dashboard"
  key_links:
    - from: "npm run measure:cwv"
      to: "warm prod leocards.vercel.app"
      via: "Lighthouse navigation over authenticated puppeteer-core page"
      pattern: "leocards.vercel.app"
    - from: "baseline reports"
      to: "local .next build bundle stats"
      via: "getBundleKb over route-bundle-stats.json from a fresh next build"
      pattern: "First-load JS"
---

<objective>
Execute the Plan-02 harness for real and produce the IMMUTABLE Phase-16 baseline: run a fresh `npm run build` (so bundle stats match the deployed commit), run `npm run measure:cwv` against warm prod, and commit the resulting 4 per-route markdown reports + 8 raw-JSON run files + cross-route summary. This is PERF-02's actual deliverable — the written per-route baselines — and the completion of PERF-01 (proving the codified harness runs end-to-end against warm prod).

A human-verify checkpoint gates the commit: because these numbers become the immutable before-reference for every Phase-17 change, a person confirms the medians are plausible (auth honored, no `/login` shells, no all-cold outliers) before they are locked in.

Purpose: Turn the instrument into truth. Once committed, Phase 17 diffs against these files and never edits them.
Output: `.planning/phases/16-performance-baseline-measure/baseline/` populated + committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-performance-baseline-measure/16-RESEARCH.md
@.planning/phases/16-performance-baseline-measure/16-CONTEXT.md

<interfaces>
<!-- This plan RUNS the artifacts from Plans 01+02. No new code. -->
<!-- Harness entrypoint: `npm run measure:cwv` -> node scripts/measure-cwv.mjs (Plan 02). -->
<!-- Required env at run time: DATABASE_URL (Neon prod-shared). Optional: PROD_URL (default -->
<!--   https://leocards.vercel.app), CHROME_PATH (default Playwright chromium-1208), CLEANUP_DB_URL. -->
<!-- Bundle prerequisite: `npm run build` first (Turbopack -> .next/diagnostics/route-bundle-stats.json). -->
<!-- Output layout (Plan 02 writes these into baseline/): -->
//   {slug}-mobile-runs.json, {slug}-desktop-runs.json, {slug}-baseline.md  for slug in
//     dashboard | study | deck-new-card | deck-browse
//   16-BASELINE-SUMMARY.md
<!-- Self-clean: harness finally-block spawns cleanup-test-users.mjs %@test.local (Plan 02). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh build + execute the warm-prod harness, produce baseline artifacts</name>
  <files>.planning/phases/16-performance-baseline-measure/baseline/</files>
  <read_first>
    - scripts/measure-cwv.mjs (the harness being run — confirm its required env + the USAGE header; created in Plan 02)
    - .planning/phases/16-performance-baseline-measure/16-RESEARCH.md sections "Common Pitfalls" 3 (cold run 1) + 6 (build freshness) + "Environment Availability" (DATABASE_URL / CHROME_PATH / prod reachability, ~28 min runtime for 48 LH runs)
    - .planning/phases/16-performance-baseline-measure/16-PATTERNS.md section "report artifact" (the expected markdown shape, so the executor can eyeball the output for sanity)
    - .planning/milestones/v2.1-phases/13-3d-habitat/13-PERF-REAL.md (the cold-outlier annotation + median discipline this run reproduces)
  </read_first>
  <action>
    Run the harness end-to-end and verify the artifacts landed. Steps: (1) confirm DATABASE_URL is exported (the harness FATALs without it) — if absent, STOP and surface it as a blocker rather than fabricating data. (2) Run `npm run build` first so the local route-bundle-stats.json reflects the current commit (Pitfall 6); a Turbopack build is expected. (3) Run `npm run measure:cwv`. Expect roughly 28 minutes: 4 routes x 2 presets x 6 runs equals 48 sequential Lighthouse navigations, plus provisioning at the start and cleanup at the end. Do NOT parallelize or shortcut the run count. (4) If the harness throws the redirect guard (a run landed on /login), treat it as a real auth failure (check DATABASE_URL points at the prod-shared DB, the prod deployment is reachable, and the __Secure- cookie name is correct) — do not weaken the guard to force a pass. (5) On success, confirm the baseline directory contains all 13 files: for each slug in dashboard, study, deck-new-card, deck-browse the two -runs.json plus one -baseline.md, plus 16-BASELINE-SUMMARY.md. (6) Confirm the harness self-cleaned by running `npm run measure:cleanup` and verifying it reports 0 matched test.local users remaining. Under NO circumstances hand-edit the numbers in the reports — if a run is anomalous, re-run the whole harness; the values must be genuine warm-prod medians (D-05/D-06). This task performs a live prod measurement and an INSERT then DELETE of a test.local user on the prod-shared DB; it is inherently side-effectful (not a checkpoint) because the run itself is fully automatable.
  </action>
  <verify>
    <automated>test -s .planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md &amp;&amp; test -s .planning/phases/16-performance-baseline-measure/baseline/dashboard-baseline.md &amp;&amp; test -s .planning/phases/16-performance-baseline-measure/baseline/dashboard-mobile-runs.json &amp;&amp; test -s .planning/phases/16-performance-baseline-measure/baseline/study-baseline.md &amp;&amp; test -s .planning/phases/16-performance-baseline-measure/baseline/deck-new-card-baseline.md &amp;&amp; test -s .planning/phases/16-performance-baseline-measure/baseline/deck-browse-baseline.md</automated>
    <note>Also assert each {slug}-mobile-runs.json parses with runs.length &gt;= 6 and a numeric medians.lcp (run a one-off node check). Do not create a helper file unless convenient.</note>
  </verify>
  <acceptance_criteria>
    - The baseline directory contains all 13 files (4 slugs x {mobile-runs.json, desktop-runs.json, baseline.md} + 16-BASELINE-SUMMARY.md), each non-empty
    - Each {slug}-mobile-runs.json parses as JSON with a `runs` array of length >= 6 and a numeric `medians.lcp`
    - Each {slug}-baseline.md contains a "Medians" table, a bundle "First-load JS" figure, and a "Bottleneck" classification naming a Phase-17 target
    - 16-BASELINE-SUMMARY.md references all four routes (dashboard, study, deck-new-card, deck-browse)
    - No /login string appears as a measured finalUrl in any runs.json (auth was honored — the harness would have thrown otherwise)
    - `npm run measure:cleanup` reports 0 matched test.local users (zero prod residue)
    - Report numbers are machine-generated (the executor did not hand-edit any median value)
  </acceptance_criteria>
  <done>Fresh build ran; measure:cwv completed n=6 x 4 routes x 2 presets against warm prod; all 13 baseline files present and non-empty with valid runs+medians; auth honored (no /login shells); zero test.local residue after cleanup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sanity-check the immutable baseline before it is locked</name>
  <files>.planning/phases/16-performance-baseline-measure/baseline/</files>
  <action>PAUSE for human verification — this is a checkpoint, not automated work. Present the what-built and how-to-verify steps below to the user and wait for the resume-signal before proceeding to Task 3 (commit). Do NOT auto-approve: these medians become the immutable Phase-17 before-reference, so a human must confirm plausibility (auth honored, no /login shells, no cold-only outliers) first. workflow.auto_advance does not apply to this checkpoint.</action>
  <what-built>
    The Plan-02 harness was run against warm prod and wrote 13 baseline artifacts to
    `.planning/phases/16-performance-baseline-measure/baseline/`: four `{slug}-baseline.md`
    reports (medians + bundle composition + bottleneck classification), eight
    `{slug}-{preset}-runs.json` raw-run files, and `16-BASELINE-SUMMARY.md`. These numbers
    become the IMMUTABLE before-reference for every Phase-17 optimization, so they get one
    human sanity-pass before commit.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md` and skim the cross-route table.
    2. For each of the four routes, confirm the mobile median LCP is a plausible warm-prod number (roughly 1000-4000 ms — NOT ~600 ms, which would indicate a `/login` shell was measured instead of the real page).
    3. Open one `{slug}-mobile-runs.json` and confirm run 1 (the discarded cold run) is present in `runs` but the reported `medians` are over runs 2-6 (`warmRuns`), and that no run's finalUrl contains `/login`.
    4. Confirm each `{slug}-baseline.md` names ONE bottleneck class (bundle vs RSC waterfall vs hydration) and a specific Phase-17 target — i.e. the classification is actionable, not blank.
    5. Confirm the summary covers exactly the four routes and `/habitat` is absent (D-03).
    6. If anything looks like a cold-start artifact or auth failure, reply with what looks off and re-run `npm run measure:cwv` rather than approving — do NOT hand-edit numbers.
  </how-to-verify>
  <verify>
    <human-check>Human confirms the four routes' warm-prod medians are plausible (no /login shells, no cold-only outliers), each report names one actionable bottleneck + Phase-17 target, and /habitat is absent — then replies to resume.</human-check>
  </verify>
  <resume-signal>Reply "approved" to lock the baseline and commit, or describe what looks wrong.</resume-signal>
  <done>A human has confirmed the baseline medians are plausible and approved locking them; or has flagged an anomaly and requested a re-run.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the immutable baseline</name>
  <files>.planning/phases/16-performance-baseline-measure/baseline/</files>
  <read_first>
    - .planning/phases/16-performance-baseline-measure/16-CONTEXT.md section specifics (the baseline is an IMMUTABLE before-reference — commit it as the reference, never re-edit)
  </read_first>
  <action>
    After the human approves in Task 2, git-add the entire baseline directory and commit with message `docs(16): warm-prod CWV baseline for the four key routes (PERF-01/PERF-02)`. Verify the .gitignore does NOT exclude .planning/phases/**/baseline/ (the qa-manifest gitignore entries are scoped to scripts/qa-manifest-*.json and /.next/ — the baseline path is not ignored, so the artifacts WILL be tracked). Confirm `git status` shows the baseline files staged/committed and that no stray .tmp files were left behind by the atomic-write step. Do not commit any .next/ build output (it is gitignored) or any test.local residue.
  </action>
  <verify>
    <automated>git status --porcelain .planning/phases/16-performance-baseline-measure/baseline/</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` shows the baseline commit with a message referencing PERF-01/PERF-02
    - `git ls-files .planning/phases/16-performance-baseline-measure/baseline/` lists all 13 baseline files (they are tracked, not gitignored)
    - No .tmp files remain in the baseline directory
    - .next/ was not committed (`git ls-files .next` returns nothing)
  </acceptance_criteria>
  <done>Baseline directory committed with a PERF-01/PERF-02 message; all 13 files tracked; no .tmp leftovers; no .next/ or test-user residue committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner -> Neon prod-shared DB | The real run INSERTs a *test.local user/deck/cards, then DELETEs them via cleanup |
| runner -> Vercel prod | Live Lighthouse navigations using an injected session cookie (secret in transit) |
| committed artifacts -> git history | The baseline JSON/markdown becomes a permanent, human-readable record — must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information disclosure | committed baseline artifacts | mitigate | Reports contain only metric numbers + bundle sizes + route names; the harness never writes the session token/password/email-secret into artifacts (Plan-02 logging discipline). Task 2 human review + Task 3 git status catch any stray sensitive file before commit |
| T-16-09 | Tampering | residual *test.local user after the real run | mitigate | Harness finally-block self-cleans; Task 1 additionally runs `npm run measure:cleanup` and asserts 0 matched users — zero prod residue confirmed before commit |
| T-16-10 | Repudiation / integrity | hand-edited or cold-contaminated baseline | mitigate | Task 1 forbids hand-editing numbers; the human checkpoint (Task 2) verifies warm-run plausibility (no /login shells, medians over warm runs) before the immutable reference is locked |
| T-16-SC | Tampering | npm installs (supply chain) | mitigate | RESEARCH section Package Legitimacy Audit: ZERO new packages this phase. `npm run build` and `npm run measure:cwv` use only already-installed, already-audited tooling. No install step, no [ASSUMED]/[SUS] packages, no legitimacy checkpoint required. |
</threat_model>

<verification>
- Task 1 automated check confirms all 13 baseline files exist, are non-empty, and carry valid runs (n>=6) + numeric medians.
- Human checkpoint (Task 2) confirms warm-prod plausibility (no /login shells, no cold-only medians, actionable bottleneck per route, /habitat absent) before commit.
- Task 3 confirms the commit landed with all 13 files tracked, no .tmp leftovers, no .next/ or test-user residue.
- `npm run measure:cleanup` reports zero test.local users remaining (D-01/D-02 self-clean).
- No code files change in this plan, so `tsc --noEmit` and `npx vitest run` remain green from Waves 1-2 (re-run as the standard wave gate to confirm no regression).
</verification>

<success_criteria>
- `npm run measure:cwv` ran end-to-end against warm prod and produced all 13 baseline artifacts (4 markdown reports + 8 raw-JSON + summary), each with genuine warm-prod medians (run 1 discarded) and bundle composition from a fresh local build.
- Each route report names a single ranked bottleneck and its Phase-17 target; exactly the four roadmap routes are covered (no /habitat).
- The real run self-cleaned — zero *test.local residue in prod.
- A human confirmed the baseline is plausible, and it is committed as the immutable Phase-17 before-reference.
- No optimization changed; no new npm packages.
</success_criteria>

<output>
Create `.planning/phases/16-performance-baseline-measure/16-03-SUMMARY.md` when done.
</output>
