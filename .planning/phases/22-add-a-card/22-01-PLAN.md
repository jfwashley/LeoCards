---
phase: 22-add-a-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/daybreak/lang-chip.tsx
  - src/components/daybreak/ac-btn.tsx
  - src/components/daybreak/ac-seg.tsx
  - src/components/daybreak/ac-banner.tsx
  - src/components/daybreak/ac-progress.tsx
  - src/components/daybreak/ac-review-row.tsx
  - src/components/daybreak/ac-pair-row.tsx
  - src/components/daybreak/__tests__/ac-atoms.test.tsx
  - src/components/deck-switcher.tsx
requirements: [ADC-01, ADC-02, ADC-03]
nyquist_compliant: true
autonomous: true

must_haves:
  truths:
    - "All six new Add-a-Card atoms (ACSeg, ACBtn, ACProgress, ACBanner, ACReviewRow, ACPairRow) compose the existing Daybreak primitives + inline d1 tokens, render behavior-free presentational surfaces only, and ship with a unit test — they add no network calls, inputs, or routes — L-02"
    - "ACProgress shows Leo (LionFace in a sunrise disc) not a tiger and no emoji; ACBanner/ACReviewRow use CSS glyphs (✓/!/pencil/close) not emoji — L-01"
    - "ACSeg renders each segment as a real <button type='button'> with the segment label as its accessible name (so getByRole('button',{name:/Type a word/}) and {name:/From an image/} match), and ACBtn renders a real <button> in every kind (primary/disabled/ghost/ghost-danger) so getByRole('button',{name:'Save card'}) matches — L-06"
    - "ACPairRow renders the TARGET (ES) field on top and the NATIVE (EN) field beneath — the deliberate D-01 'lead with the source field' orientation for image-extracted pairs; this atom must NOT be built native-on-top — D-01"
    - "The LangChip text chip (EN/ES, no flag emoji) is extracted to a shared src/components/daybreak/lang-chip.tsx named export and deck-switcher.tsx imports it from there with its existing trigger/popover/create behavior unchanged — D-02, L-01"
  artifacts:
    - path: "src/components/daybreak/ac-seg.tsx"
      provides: "Segmented toggle atom — two <button> segments, amber pill track, active white segment"
      contains: "type=\"button\""
    - path: "src/components/daybreak/ac-btn.tsx"
      provides: "Multi-variant Daybreak button (<button> in all kinds)"
      contains: "kind"
    - path: "src/components/daybreak/ac-progress.tsx"
      provides: "Calm long-wait atom — LionFace sunrise disc + motion/react indeterminate amber bar + title/sub"
      contains: "LionFace"
    - path: "src/components/daybreak/ac-banner.tsx"
      provides: "ok/error banner atom with CSS circle glyph"
      contains: "kind"
    - path: "src/components/daybreak/ac-review-row.tsx"
      provides: "keep/exclude word row atom — checkbox + word + edit + remove buttons"
      contains: "onToggle"
    - path: "src/components/daybreak/ac-pair-row.tsx"
      provides: "translation pair atom — ES (target) field on top, EN (native) beneath (D-01)"
      contains: "targetLabel"
    - path: "src/components/daybreak/lang-chip.tsx"
      provides: "Shared LangChip text chip (named export), reused by deck-switcher + Add-a-Card context line"
      contains: "export"
    - path: "src/components/daybreak/__tests__/ac-atoms.test.tsx"
      provides: "Unit test — ACSeg/ACBtn render as <button>, ACPairRow target-on-top order, ACBanner kinds"
  key_links:
    - from: "src/components/daybreak/ac-progress.tsx"
      to: "src/components/daybreak/lion-face.tsx"
      via: "LionFace in the sunrise disc"
      pattern: "LionFace"
    - from: "src/components/daybreak/ac-progress.tsx"
      to: "motion/react"
      via: "indeterminate amber bar animate loop"
      pattern: "motion/react"
    - from: "src/components/deck-switcher.tsx"
      to: "src/components/daybreak/lang-chip.tsx"
      via: "import { LangChip } (extracted, behavior unchanged)"
      pattern: "daybreak/lang-chip"
---

<objective>
Build the six new Daybreak atoms every other Phase 22 plan composes, and extract the shared `LangChip` so the type-a-word context line and the deck-switcher use one chip definition. No behavior, no network, no routes — pure presentational atoms following the established `src/components/daybreak/*` inline-token convention.

Atoms created: `ACSeg` (segmented toggle), `ACBtn` (multi-variant button), `ACProgress` (calm long-wait), `ACBanner` (ok/error), `ACReviewRow` (keep/exclude row), `ACPairRow` (translation pair — D-01 target-on-top), plus `lang-chip.tsx` (extracted shared chip).

Purpose: Plan 02 (type-a-word) needs `ACSeg`/`ACBtn`/`ACBanner`/`LangChip`; Plan 03 (image pick/confirm/extract) needs `ACProgress`/`ACBtn`/`ACBanner`/`LangChip`; Plan 04 (review/translate/result) needs `ACReviewRow`/`ACPairRow`/`ACProgress`/`ACBtn`/`ACBanner`. Building and unit-testing the atoms first means the three consumer plans integrate against settled contracts (ADC-01, ADC-02, ADC-03).
Output: six new `daybreak/*` atoms + a shared `lang-chip.tsx` + one unit test, with `deck-switcher.tsx` re-pointed at the extracted chip (no behavior change).

This is a PRESENTATION-ONLY re-skin (L-02). No backend, schema, route, or pipeline change. The atoms hold no state beyond what their props drive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-add-a-card/22-CONTEXT.md
@.planning/phases/22-add-a-card/22-RESEARCH.md
@.planning/phases/22-add-a-card/22-PATTERNS.md
@.planning/phases/22-add-a-card/22-VALIDATION.md

# Project conventions — Next.js 16 has breaking changes; read node_modules/next/dist/docs/ before asserting any Next.js API
@AGENTS.md

# Design contract (this phase runs --skip-ui; the JSX mock IS the spec — recreate faithfully EXCEPT the D-01/D-06 notes)
@design/handoff-daybreak/daybreak-addcard.jsx

<interfaces>
<!-- Atom contracts the three consumer plans (02/03/04) depend on. Extracted from PATTERNS.md + the design contract. -->

Existing Daybreak primitives to compose (VERIFIED present):
  src/components/daybreak/lion-face.tsx  → <LionFace size mane="#E8973B" face="#FFD9A6" muzzle="#FFF1DC" ink="#4A331C" /> (aria-hidden set)
  src/components/daybreak/t-btn.tsx       → file-structure analog for ACBtn (import * as React; <button>)
  src/components/daybreak/t-field.tsx     → field shape analog for ACPairRow (label/input/error stack)
  src/components/daybreak/card.tsx        → inline-style border/shadow analog for ACBanner

d1 tokens (VERIFIED hifi-daybreak.jsx) — use these literal hex values inline:
  ink #4A331C · muted #9C8467 · primary #F28A1F · link #C96F12 · green #3E9B5F · red #DE5F4A
  pillBg #FFF1DC · pillText #B4762A · fieldBg #FFFBF4 · fieldBorder 1.5px solid #EDDFC9
  fieldRadius 12 · btnRadius 14 · cardRadius 22 · font-display var(--font-display) (Baloo 2 700)

Atom prop shapes the consumers will call against (from PATTERNS.md):
  ACSeg({ mode:"type"|"image", onChange:(m)=>void })
  ACBtn(React.ButtonHTMLAttributes & { kind?:"primary"|"disabled"|"ghost"|"ghost-danger", icon?:ReactNode })
  ACProgress({ title:string, sub:string, searching?:boolean })
  ACBanner({ kind:"ok"|"error", children:ReactNode })
  ACReviewRow({ word, excluded, last?, onToggle, onEdit, onRemove })
  ACPairRow({ targetLabel, nativeLabel, target, native, failed?, last?, onEditTarget, onEditNative })  // ES top, EN below — D-01

LangChip to extract (VERIFIED deck-switcher.tsx lines 14-37, currently local, NOT exported):
  function LangChip({ code, size=24 }): 23-line inline-style span (bg #FFF1DC, border 1px #F0E3CF, radius 6, color #B4762A)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ACSeg + ACBtn + ACBanner + extract LangChip (with unit test)</name>
  <files>src/components/daybreak/ac-seg.tsx, src/components/daybreak/ac-btn.tsx, src/components/daybreak/ac-banner.tsx, src/components/daybreak/lang-chip.tsx, src/components/deck-switcher.tsx, src/components/daybreak/__tests__/ac-atoms.test.tsx</files>
  <behavior>
    - ACSeg renders exactly two <button type="button"> segments; their accessible names are "Type a word" and "From an image"; the active segment (mode prop) has aria-pressed=true.
    - Clicking a segment calls onChange with that segment's mode.
    - ACBtn renders a <button> for kind primary/ghost/ghost-danger; kind="disabled" renders a <button> with the disabled attribute set; the button's accessible name is its children text (e.g. "Save card").
    - ACBanner kind="ok" renders a "✓" circle glyph + children; kind="error" renders a "!" circle glyph + children.
    - The extracted LangChip renders its `code` text (e.g. "EN") inside a chip; deck-switcher still renders its trigger pill and create flow unchanged after importing LangChip from the shared file.
  </behavior>
  <read_first>
    - src/components/daybreak/t-btn.tsx (the atom file-structure convention to copy: `import * as React`, named export, real <button>, inline tokens; --db-btn-shadow value)
    - src/components/daybreak/card.tsx (inline border/shadow token convention for ACBanner)
    - src/components/deck-switcher.tsx lines 14-37 (the local LangChip to extract verbatim) and its trigger/import region (to re-point at the shared file)
    - src/components/__tests__/deck-switcher.test.tsx (the `// @vitest-environment jsdom` docblock + afterEach(cleanup) + @testing-library/react convention to copy for the new atom test)
    - design/handoff-daybreak/daybreak-addcard.jsx — ACSeg (lines 59-71), ACBtn (lines 173-179), ACBanner (lines 133-141)
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"ac-seg.tsx", §"ac-btn.tsx", §"ac-banner.tsx", §"LangChip Sub-Component" (token maps + Pitfall 1/2 button rule)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"Common Pitfalls" Pitfall 1 + Pitfall 2 (segments and buttons MUST be <button>, never <div>)
  </read_first>
  <action>
    Create `src/components/daybreak/lang-chip.tsx`: move the `LangChip({ code, size=24 })` function out of `deck-switcher.tsx` verbatim (same inline styles — bg #FFF1DC, border 1px solid #F0E3CF, radius 6, color #B4762A, fontWeight 700, width size+7, height size, fontSize size*0.46) and `export function LangChip`. Then in `deck-switcher.tsx` delete the local `LangChip` definition and add `import { LangChip } from "@/components/daybreak/lang-chip"` — change NOTHING else in deck-switcher (trigger pill, popover, createDeck, `data-testid="deck-picker-trigger"` all stay; this is a pure extraction so the header deck picker behaves identically — L-02).
    Create `src/components/daybreak/ac-seg.tsx`: implement `ACSeg({ mode, onChange })` per PATTERNS.md — an outer track div (display flex, gap 5, padding 5, background #F4E7D2, borderRadius 14) mapping `['type','image']` to two `<button type="button">` segments. Each segment: flex 1, height 44, borderRadius 11, white background + boxShadow `0 2px 8px rgba(160,110,40,0.14)` when active (mode===m) else transparent/no shadow, color #4A331C active / #9C8467 inactive, fontWeight 700, fontSize 14.5, `onClick={() => onChange(m)}`, `aria-pressed={mode===m}`, and the visible label text "Type a word" / "From an image" (a small CSS/SVG glyph before the label is fine — no emoji, L-01). CRITICAL (Pitfall 1): segments are `<button>`, not `<div>`, so the label is the accessible name.
    Create `src/components/daybreak/ac-btn.tsx`: implement `ACBtn({ kind="primary", icon, children, ...props })` extending `React.ButtonHTMLAttributes<HTMLButtonElement>`, always rendering `<button>` (Pitfall 2). Token map: primary = bg #F28A1F, color #FFF, boxShadow `0 10px 22px rgba(242,138,31,0.30)`, radius 14; disabled = bg #F4E7D2, color #B49B78 AND set the `disabled` attribute (not purely visual); ghost = bg #FFFFFF, border 1.5px solid #EDDFC9, color #4A331C; ghost-danger = bg #FFFFFF, border 1.5px solid #DE5F4A, color #DE5F4A. Default height 54 (allow override via incoming style/className for the 46px cancel + 42px secondary cases). Spread `{...props}` so consumers attach onClick/type/aria.
    Create `src/components/daybreak/ac-banner.tsx`: implement `ACBanner({ kind, children })` per PATTERNS.md — flex row, padding 11px 14px, radius 12; ok = bg #EAF5EC border 1.5px #C5E4CD, error = bg #FCEBE6 border 1.5px #F2C9BF; leading 22px circle glyph (bg #3E9B5F "✓" for ok, bg #DE5F4A "!" for error, white text) + children text (14px, 600, #4A331C). No emoji (L-01).
    Then WRITE `src/components/daybreak/__tests__/ac-atoms.test.tsx` with the `// @vitest-environment jsdom` docblock + `afterEach(cleanup)`. Tests: (1) ACSeg renders two buttons whose accessible names are "Type a word" and "From an image", and clicking the "From an image" button calls onChange("image"); (2) ACBtn kind="primary" renders a button with accessible name "Save card"; ACBtn kind="disabled" renders a button with the `disabled` property true; (3) ACBanner kind="ok" contains "✓" and its children text. (Add the ACPairRow/ACReviewRow/ACProgress assertions in Task 2 — keep them in this same file.)
  </action>
  <verify>
    <automated>npx vitest run src/components/daybreak/__tests__/ac-atoms.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/daybreak/lang-chip.tsx` has a named `export function LangChip` — verifiable: `grep -c "export function LangChip" src/components/daybreak/lang-chip.tsx` returns 1.
    - `deck-switcher.tsx` imports LangChip from the shared file and no longer defines it locally — `grep -c "daybreak/lang-chip" src/components/deck-switcher.tsx` returns ≥1 AND `grep -c "function LangChip" src/components/deck-switcher.tsx` returns 0. `data-testid="deck-picker-trigger"` and `createDeck` are still present (`grep -c "deck-picker-trigger" src/components/deck-switcher.tsx` ≥1, `grep -c "createDeck" src/components/deck-switcher.tsx` ≥2).
    - `grep -c "type=\"button\"" src/components/daybreak/ac-seg.tsx` returns ≥2 (both segments are buttons — Pitfall 1).
    - `ac-btn.tsx` renders a `<button>` (no `<div role`): `grep -c "<button" src/components/daybreak/ac-btn.tsx` ≥1; `grep -c "<div role=\"button\"" src/components/daybreak/ac-btn.tsx` returns 0.
    - No emoji in the new atoms — `grep -rnP "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]" src/components/daybreak/ac-seg.tsx src/components/daybreak/ac-btn.tsx src/components/daybreak/ac-banner.tsx` returns no matches (the ✓/! are ASCII-range circle glyphs, not emoji).
    - `npx vitest run src/components/daybreak/__tests__/ac-atoms.test.tsx` passes the ACSeg/ACBtn/ACBanner tests.
  </acceptance_criteria>
  <done>ACSeg/ACBtn/ACBanner exist as real-button Daybreak atoms; LangChip is shared and deck-switcher re-points at it with behavior unchanged; the atom unit test is green for these three.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ACProgress + ACReviewRow + ACPairRow (D-01 target-on-top) with unit test</name>
  <files>src/components/daybreak/ac-progress.tsx, src/components/daybreak/ac-review-row.tsx, src/components/daybreak/ac-pair-row.tsx, src/components/daybreak/__tests__/ac-atoms.test.tsx</files>
  <behavior>
    - ACProgress renders a LionFace inside a sunrise disc + an animated indeterminate bar + the title and sub text; searching=true adds a magnifier overlay element.
    - ACReviewRow renders a checkbox reflecting !excluded, the word text (line-through + muted when excluded), and two <button> controls wired to onEdit and onRemove; toggling the checkbox calls onToggle.
    - ACPairRow renders the TARGET (ES) input ABOVE the NATIVE (EN) input (D-01); editing the top input calls onEditTarget, editing the bottom calls onEditNative; failed=true gives the EN field a red border + the "Translation unavailable — enter manually." helper.
  </behavior>
  <read_first>
    - src/components/daybreak/lion-face.tsx (LionFace prop shape for the disc)
    - src/components/daybreak/auth-card.tsx (the sunrise-disc gradient + amber-dot pattern reused in ACProgress)
    - src/components/daybreak/t-field.tsx (the label/input/error field shape ACPairRow composes twice)
    - src/components/daybreak/card.tsx + src/components/daybreak/pill.tsx (inline-style row conventions for ACReviewRow)
    - src/components/study-session.tsx lines 1-8 (the `import { motion } from "motion/react"` convention for the indeterminate bar)
    - design/handoff-daybreak/daybreak-addcard.jsx — ACProgress (lines 210-224), ACReviewRow (lines 228-237), ACPairRow (lines 240-254)
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"ac-progress.tsx", §"ac-review-row.tsx", §"ac-pair-row.tsx" (token maps, the D-01 orientation note, ACProgress copy strings)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"Anti-Patterns to Avoid" (ACPairRow orientation is intentional — do not correct)
  </read_first>
  <action>
    Create `src/components/daybreak/ac-progress.tsx`: implement `ACProgress({ title, sub, searching })` per PATTERNS.md. Centered column (gap 18, padding 28). A 116x116 sunrise disc (borderRadius 50%, background `linear-gradient(180deg, #FFE7BC, #FFFDF8)`, border 1px #F0E3CF) holding `<LionFace size={62} mane="#E8973B" face="#FFD9A6" muzzle="#FFF1DC" ink="#4A331C" />` plus an always-present 14px amber dot (#FFC95C) at right 16 / top 14, plus a magnifier glyph overlay rendered only when `searching` (CSS/SVG, no emoji — L-01). Title span uses `fontFamily: "var(--font-display)"`, fontSize 23, 700, #4A331C. The indeterminate bar: a 74%-wide, 12px-tall track (#F1E6D2, overflow hidden) containing a `motion.div` with `animate={{ x: ['-100%','100%'] }}` `transition={{ repeat: Infinity, duration: 1.2, ease: 'easeInOut' }}` styled width 42%, radius 7, background #F28A1F. Sub span: 14.5px, #9C8467, lineHeight 1.5, maxWidth 280. (No Cancel button inside this atom — the consumer renders Cancel beside it.)
    Create `src/components/daybreak/ac-review-row.tsx`: implement `ACReviewRow({ word, excluded, last, onToggle, onEdit, onRemove })` per PATTERNS.md. A flex row (gap 13, padding 13px 2px, borderBottom `1px solid #F4ECDD` unless `last`). Leading checkbox control (26x26, radius 8; on=!excluded → bg #F28A1F white "✓", off → border 2px #D8C7AC transparent) — make the checkbox a clickable `<button type="button" onClick={onToggle}>` (so toggling works and is reachable). Word span: flex 1, fontSize 17.5, 600, color #9C8467 + line-through when excluded else #4A331C. Edit `<button type="button" onClick={onEdit}>` (34x34, radius 10, border 1.5px #EDDFC9, bg #FFFBF4, a pencil glyph) and remove `<button type="button" onClick={onRemove}>` (34x34, radius 10, no border, a close glyph #9C8467). No emoji (CSS/SVG glyphs only — L-01).
    Create `src/components/daybreak/ac-pair-row.tsx`: implement `ACPairRow({ targetLabel, nativeLabel, target, native, failed, last, onEditTarget, onEditNative })` per PATTERNS.md. CRITICAL — D-01 orientation: the FIRST (top) field is the TARGET (ES) field (bold, value=target, onChange→onEditTarget), the SECOND (bottom) field is the NATIVE (EN) field (value=native, onChange→onEditNative). A container (padding 14px 0, borderBottom `1px solid #F4ECDD` unless `last`, flex column gap 9). Each field row: a 24px label span (`targetLabel`/`nativeLabel`, 11px 700 #9C8467) + an `<input>` (minHeight 46, radius 11, padding 0 13px, bg #FFFBF4, fontSize 16.5; target input fontWeight 700; native input border 1.5px #DE5F4A + color #DE5F4A when `failed` else border 1.5px #EDDFC9 + color #4A331C). When `failed`, render below: `<span>Translation unavailable — enter manually.</span>` (12.5px 600 #DE5F4A, marginLeft 35). Do NOT flip to native-on-top — this orientation is the deliberate D-01 exception and the UI auditor must not "correct" it.
    Then EXTEND `src/components/daybreak/__tests__/ac-atoms.test.tsx` (same file from Task 1): add (4) ACReviewRow renders the word and, when excluded=true, the word element has line-through styling and the checkbox reflects off; clicking the checkbox button calls onToggle; (5) ACPairRow renders the target value in the TOP input and the native value in the BOTTOM input — assert DOM order: query the two inputs and confirm the first has the target value and the second has the native value (this is the D-01 regression guard); (6) ACPairRow with failed=true renders the "Translation unavailable — enter manually." helper. (ACProgress is presentational/animated — a render-without-crash + "title text present" assertion is sufficient.)
  </action>
  <verify>
    <automated>npx vitest run src/components/daybreak/__tests__/ac-atoms.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/daybreak/ac-progress.tsx` imports LionFace and motion/react — `grep -c "LionFace" src/components/daybreak/ac-progress.tsx` ≥1 AND `grep -c "motion/react" src/components/daybreak/ac-progress.tsx` ≥1.
    - `ac-pair-row.tsx` exists and the test asserting target-on-top DOM order passes (the D-01 orientation guard).
    - `ac-pair-row.tsx` contains the exact helper string "Translation unavailable — enter manually." — `grep -c "Translation unavailable — enter manually." src/components/daybreak/ac-pair-row.tsx` returns 1.
    - `ac-review-row.tsx` checkbox/edit/remove are real buttons — `grep -c "type=\"button\"" src/components/daybreak/ac-review-row.tsx` returns ≥3.
    - No emoji in the three atoms — `grep -rnP "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]" src/components/daybreak/ac-progress.tsx src/components/daybreak/ac-review-row.tsx src/components/daybreak/ac-pair-row.tsx` returns no matches.
    - `npx vitest run src/components/daybreak/__tests__/ac-atoms.test.tsx` passes all six suites (Task 1 + Task 2).
  </acceptance_criteria>
  <done>ACProgress (Leo sunrise disc + motion bar), ACReviewRow (button controls), and ACPairRow (D-01 target-on-top) exist and are unit-tested; the orientation regression guard is green.</done>
</task>

<task type="auto">
  <name>Task 3: Atom gate — typecheck + atom unit test + scoped biome</name>
  <files>src/components/daybreak/ac-seg.tsx, src/components/daybreak/ac-btn.tsx, src/components/daybreak/ac-progress.tsx, src/components/daybreak/ac-banner.tsx, src/components/daybreak/ac-review-row.tsx, src/components/daybreak/ac-pair-row.tsx, src/components/daybreak/lang-chip.tsx, src/components/deck-switcher.tsx, src/components/daybreak/__tests__/ac-atoms.test.tsx</files>
  <read_first>
    - .planning/phases/22-add-a-card/22-VALIDATION.md §"Test Infrastructure" (scope biome to touched files; never `biome ci .`)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"Validation Architecture" (vitest config, jsdom env)
  </read_first>
  <action>
    Run the foundation gate for the atoms. This is a re-skin: extracting LangChip must not regress the deck-switcher unit test. Run `npx tsc --noEmit` (whole project), then `npm test` (full vitest run — the new atom test + the existing `deck-switcher.test.tsx` must both stay green), then scope biome to ONLY the nine touched files. Do NOT run `biome ci .` (the repo has ~429 pre-existing errors across untouched files; git hooks are disabled). Fix only NEW errors this plan introduced; never touch pre-existing repo errors in untouched files.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npx biome ci src/components/daybreak/ac-seg.tsx src/components/daybreak/ac-btn.tsx src/components/daybreak/ac-progress.tsx src/components/daybreak/ac-banner.tsx src/components/daybreak/ac-review-row.tsx src/components/daybreak/ac-pair-row.tsx src/components/daybreak/lang-chip.tsx src/components/deck-switcher.tsx "src/components/daybreak/__tests__/ac-atoms.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test` exits 0 — all unit tests green, including the existing `src/components/__tests__/deck-switcher.test.tsx` (proves the LangChip extraction preserved deck-switcher behavior).
    - `npx biome ci` over exactly the nine touched files exits 0 (scoped, not repo-wide).
  </acceptance_criteria>
  <done>Typecheck clean, full unit suite green (including deck-switcher), scoped biome clean — the atoms are ready for the three consumer plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds presentational atoms + a chip extraction only. No network calls, no inputs leaving the client, no auth/route/schema change. All Add-a-Card pipelines (`/api/translate`, `/api/extract`, `saveImageCards`, `createDeck`) are untouched and unreferenced by these atoms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-T | Tampering | New `daybreak/*` atoms | accept (no surface) | Pure presentational components; no data flow, no fetch, no input persisted. They render props the consumer passes. No new attack surface. |
| T-22-01-I | Information Disclosure | LangChip extraction from deck-switcher | accept (preserved) | Code is moved verbatim, not changed; the deck-switcher's `createDeck` call and ownership scoping are untouched (proven by the retained deck-switcher unit test). No new data exposure. |
| T-22-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this plan (`motion/react`, `react` already present — see RESEARCH "Package Legitimacy Audit": N/A, no new packages). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages; legitimacy gate N/A. |

No HIGH-severity threats. Surface-only atom plan — no new network calls, inputs, auth, routes, or schema. All pipelines preserved unchanged.
</threat_model>

<verification>
- `npx tsc --noEmit` clean (whole project).
- `npx vitest run src/components/daybreak/__tests__/ac-atoms.test.tsx` green (ACSeg/ACBtn/ACBanner as real buttons; ACPairRow D-01 target-on-top order; ACReviewRow toggle; ACBanner kinds).
- `npm test` green (full unit suite; deck-switcher test proves the LangChip extraction was behavior-preserving).
- `npx biome ci` clean over the touched files only.
- Manual (deferred to phase UAT): visual fidelity of each atom vs `design/handoff-daybreak/daybreak-addcard.jsx` recorded in `22-HUMAN-UAT.md`.
</verification>

<success_criteria>
- Six new Daybreak atoms exist (`ACSeg`, `ACBtn`, `ACProgress`, `ACBanner`, `ACReviewRow`, `ACPairRow`), composing the existing primitives + d1 tokens, all behavior-free (L-02), Leo-not-tiger / no-emoji (L-01).
- ACSeg segments + ACBtn are real `<button>` elements so downstream e2e role+name locators resolve (L-06 prerequisite).
- ACPairRow is built target-on-top per D-01 and the orientation is locked by a unit test.
- LangChip is a shared named export reused by deck-switcher (behavior unchanged) and ready for the Add-a-Card context line (D-02 chip).
- Typecheck, full unit suite, and scoped biome pass.
</success_criteria>

<output>
Create `.planning/phases/22-add-a-card/22-01-SUMMARY.md` when done.
</output>
