---
phase: 22-add-a-card
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/components/image-drop-zone.tsx
  - src/components/daybreak/ac-stepper.tsx
  - src/components/daybreak/ac-deck-select.tsx
  - src/components/image-upload-flow.tsx
  - src/components/__tests__/image-upload-flow-cancel.test.tsx
  - e2e/11-phase9-image-upload.spec.ts
requirements: [ADC-01, ADC-03]
nyquist_compliant: true
autonomous: true

must_haves:
  truths:
    - "The from-an-image Pick surface is the Daybreak ACDrop drop zone (browse/paste, dashed border, amber drag-over, file-error inline); its file-type/size copy uses 5 MB (the real enforced limit) and JPG · PNG · WebP — NOT the mock's '10 MB' placeholder — D-06"
    - "From Confirm onward the toggle is replaced by the 5-dot stepper (Image · Extract · Review · Translate · Add); Back = previous step, Re-pick (on Confirm) = back to the dropzone, Cancel = safe exit to /dashboard discarding the in-progress image flow — nothing commits before the later 'Add N cards' step — D-04, D-05"
    - "The Confirm step's deck selector is a full-width 'Add words to' ACDeckSelect field row (LangChip + deck name + chevron + 'Defaults to your active deck · change it or create a new one') that reuses the Phase 21 DeckSwitcher popover internals including inline '+ New deck' create — only the trigger presentation changes from a compact pill to a full-width row; it carries data-testid='confirm-deck-select' (NOT 'deck-picker-trigger') — D-02, ADC-01"
    - "The Extracting and Translating waits are restyled to the calm Daybreak ACProgress (Leo in a sunrise disc + indeterminate amber bar + honest 'up to 30 seconds' copy), and Cancel on Extracting returns to Confirm with the picked image + chosen deck preserved (existing D-16 reducer preservation) plus a one-line cancelled.current ref guard so a late-resolving extraction result is ignored — modeled exactly on review-list.tsx; the 35s AbortController is NOT wired to Cancel and NO EXTRACT_CANCEL action is added — D-03"
    - "All image-flow behavior is preserved untouched — the pick/deck reducer, file/deck preservation, the /api/extract call + 35s AbortController, the no-words/extract-error work-preserving retry — only the rendered surface changes plus the single D-03 guard line; Leo not tiger, no emoji — L-02, L-01"
    - "The e2e/11 spec locators broken by the restyle (toggle label 'From an image', drop-zone copy, the removed 'Next: choose deck' / 'Choose different image' buttons, the max-h-64 inline-style break, 'Add words to' label, Re-pick) are retargeted to role+accessible-name / data-testid preserving intent, and a new test proves Cancel-on-Extracting returns to Confirm with the image preserved — L-06, D-03"
  artifacts:
    - path: "src/components/image-drop-zone.tsx"
      provides: "ACDrop Daybreak drop zone — dashed border, amber drag-over, 5 MB + JPG·PNG·WebP copy, data-testid file-error"
      contains: "5MB"
    - path: "src/components/daybreak/ac-stepper.tsx"
      provides: "5-dot stepper atom (Image · Extract · Review · Translate · Add)"
      contains: "Image"
    - path: "src/components/daybreak/ac-deck-select.tsx"
      provides: "Full-width 'Add words to' deck field wrapping the Phase 21 DeckSwitcher popover (D-02)"
      contains: "confirm-deck-select"
    - path: "src/components/image-upload-flow.tsx"
      provides: "Daybreak Pick/Confirm/Extracting/no-words/error surface + D-03 cancelled guard, behavior preserved"
      contains: "cancelled"
    - path: "src/components/__tests__/image-upload-flow-cancel.test.tsx"
      provides: "Unit test for the D-03 cancelled.current late-result guard"
      contains: "cancelled"
    - path: "e2e/11-phase9-image-upload.spec.ts"
      provides: "Retargeted image-upload spec + Cancel-on-Extracting-preserves-image test"
      contains: "From an image"
  key_links:
    - from: "src/components/daybreak/ac-deck-select.tsx"
      to: "src/components/deck-switcher.tsx"
      via: "reused DeckSwitcher popover + createDeck inline create"
      pattern: "deck-switcher"
    - from: "src/components/image-upload-flow.tsx"
      to: "src/components/daybreak/ac-progress.tsx"
      via: "ACProgress for Extracting (searching) + restyled Cancel"
      pattern: "ac-progress"
    - from: "src/components/image-upload-flow.tsx"
      to: "/api/extract"
      via: "handleExtract fetch + 35s AbortController (preserved) + D-03 cancelled guard"
      pattern: "/api/extract"
---

<objective>
Re-skin the from-an-image Pick → Confirm → Extracting portion of the stepper to Daybreak (ADC-01 deck field + ADC-03 first half). Restyle `image-drop-zone.tsx` to the `ACDrop` visual (D-06 5 MB copy), build the 5-dot `ACStepper` (D-05) and the full-width `ACDeckSelect` deck field that reuses the Phase 21 `DeckSwitcher` popover + inline create (D-02), restyle the Extracting/no-words/error surfaces in `image-upload-flow.tsx` onto `ACProgress`, and add the single D-03 `cancelled.current` late-result guard with a restyled Cancel that returns to Confirm preserving the image+deck. Then retarget the e2e/11 spec and add the Cancel-preserves-image test.

Purpose: ADC-01 (working in-flow deck create via the reused picker) and ADC-03 (Pick + Confirm + Extracting with calm long-wait + cancelable extraction). The Review/Translate/Result steps are Plan 04.
Output: Daybreak `image-drop-zone.tsx`, `ac-stepper.tsx`, `ac-deck-select.tsx`, restyled+guarded `image-upload-flow.tsx`, a D-03 guard unit test, and retargeted `e2e/11-phase9-image-upload.spec.ts` + a Cancel-preserves-image test.

PRESENTATION-ONLY + ONE GUARD LINE: the `pick`/`deck` reducer, the file/deck (`file`/`previewUrl`/`selectedDeckId`) preservation, the `/api/extract` call + the 35s `AbortController`, and the retry paths are reused verbatim. The ONLY sanctioned behavior change in this plan is the D-03 `cancelled.current` guard (modeled on `review-list.tsx`). Do NOT wire the AbortController to Cancel; do NOT add an `EXTRACT_CANCEL` action (L-02, D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-add-a-card/22-CONTEXT.md
@.planning/phases/22-add-a-card/22-RESEARCH.md
@.planning/phases/22-add-a-card/22-PATTERNS.md
@.planning/phases/22-add-a-card/22-VALIDATION.md

# Project conventions — Next.js 16 has breaking changes; read node_modules/next/dist/docs/ before asserting any Next.js API
@AGENTS.md

# Design contract (this phase runs --skip-ui; the JSX mock IS the spec — recreate faithfully EXCEPT the D-06 '10 MB' placeholder)
@design/handoff-daybreak/daybreak-addcard.jsx
@design/handoff-daybreak/daybreak-addcard-boards.jsx

# Atoms this plan consumes (created in Plan 01)
@src/components/daybreak/ac-progress.tsx
@src/components/daybreak/ac-btn.tsx
@src/components/daybreak/ac-banner.tsx
@src/components/daybreak/lang-chip.tsx

# The Phase 21 deck picker this plan reuses for D-02 (already imported by image-upload-flow.tsx)
@src/components/deck-switcher.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from RESEARCH/PATTERNS (VERIFIED line refs) + Plan 01 atoms + the design contract. -->

image-upload-flow.tsx behavior to PRESERVE (VERIFIED line refs):
  reducer step:"pick"|"deck" (NOT a 6-step machine — the 5 dots are DISPLAY only);
  "pick" → ADVANCE_STEP → "deck"; BACK_TO_PICK returns to "deck" step with step:"pick"; CLEAR_FILE → "pick" file:null;
  EXTRACT_RETRY (line 102-108) does NOT touch file/previewUrl/selectedDeckId — the "D-16 preservation" comment (line 101);
  handleExtract() (lines 194-271): POST /api/extract; const controller=new AbortController(); setTimeout(()=>controller.abort(),35_000) (lines 225-226) — KEEP exactly;
  `cancelled` ref does NOT yet exist here — this plan adds it (D-03).

D-03 cancelled guard — exact pattern (modeled on review-list.tsx lines 451, 490-492, 513):
  const cancelled = useRef(false);                       // declare alongside dropZoneRef
  function handleExtract(){ cancelled.current=false; dispatch({type:"EXTRACT_START"}); /*…*/ }   // reset before each attempt (Pitfall 3)
  // after fetch resolves, before each dispatch:
  if (cancelled.current) return;                         // guard EXTRACT_SUCCESS / EXTRACT_NO_WORDS / EXTRACT_ERROR
  function handleCancelExtraction(){ cancelled.current=true; dispatch({type:"BACK_TO_PICK"}); }  // NO EXTRACT_CANCEL, NO AbortController wiring

DeckSwitcher to REUSE unchanged (VERIFIED props):
  interface DeckSwitcherProps { decks: DeckOption[]; activeDeckId: string|null; onDeckChange:(id)=>void; nativeLang: string }
  data-testid="deck-picker-trigger" stays on the HEADER instance; the new full-width trigger gets data-testid="confirm-deck-select".

Plan 01 atoms: ACProgress({title,sub,searching}); ACBtn({kind,icon,...}); ACBanner({kind,children}); LangChip({code,size?}).

image-drop-zone.tsx copy to change (VERIFIED): "Drop an image here" → "Upload a Photo" (idle) / "Drop to upload" (dragOver);
  "or click to browse, or paste a screenshot (Ctrl+V)" → "or browse your files · paste a screenshot". 5 MB + JPG·PNG·WebP copy stays from image-validation.ts (D-06 — unchanged, already "under 5MB").

ACStepper stages (daybreak-addcard.jsx AC_STAGES): ["Image","Extract","Review","Translate","Add"] — 5 dots (D-05).
ACDrop design values: daybreak-addcard.jsx lines 182-197. ACDeckSelect: lines 257-268. ACThumb: data-testid="image-preview".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ACDrop drop-zone restyle (D-06) + ACStepper + ACDeckSelect reusing DeckSwitcher (D-02, D-05)</name>
  <files>src/components/image-drop-zone.tsx, src/components/daybreak/ac-stepper.tsx, src/components/daybreak/ac-deck-select.tsx</files>
  <read_first>
    - src/components/image-drop-zone.tsx (self — preserve the click/drag/paste handlers + the `validateImageFile` call + the error dispatch; only copy + visual change)
    - src/lib/image-validation.ts (the 5 MB + JPG/PNG/WebP error copy — D-06; do NOT change it, it is pipeline not surface)
    - src/components/deck-switcher.tsx (the Phase 21 popover to reuse — props, the PopoverTrigger, createDeck inline create; decide the cleanest way to drive it from a full-width trigger)
    - src/components/ui/popover.tsx (the wrapper DeckSwitcher uses — if ACDeckSelect needs to host the popover with a custom trigger)
    - design/handoff-daybreak/daybreak-addcard.jsx lines 182-197 (ACDrop), 100-117 (ACStepper/ACFlowTop + AC_STAGES), 257-268 (ACDeckSelect)
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"image-drop-zone.tsx" (ACDrop copy + visual), §"image-upload-flow.tsx" → "D-02 ACDeckSelect wrapper pattern" (the confirm-deck-select testid + the DeckSwitcher reuse approaches)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"Pitfall 7" (the confirm-deck-select vs deck-picker-trigger non-conflict) + §"deck-switcher.tsx" (the trigger-replacement approaches)
  </read_first>
  <action>
    Restyle `image-drop-zone.tsx` to the `ACDrop` visual — keep the existing click-to-browse, drag-over, and paste (Ctrl+V) handlers and the `validateImageFile()` call + error dispatch UNCHANGED. Visual: dashed border 2.5px #E4D2B2 idle / #F28A1F dragOver / #DE5F4A error; bg #FFFDF9 idle / #FFF1D9 dragOver; an ACUpload glyph (amber upload arrow/tray, no emoji — L-01); "Upload a Photo" heading (var(--font-display) 20px 700) when idle and "Drop to upload" when dragOver; an "or browse your files · paste a screenshot" link line (#C96F12 700 for the browse affordance); a "JPG · PNG · WebP" pill (bg #FFF1DC, muted, radius 999). D-06: any file-type/size copy shown here is the 5 MB / JPG·PNG·WebP copy from `image-validation.ts` — do NOT introduce "10 MB". Add `data-testid="file-error"` to the file-error display element (L-06).
    Create `src/components/daybreak/ac-stepper.tsx`: a presentational `ACStepper({ current })` rendering the 5 dots/labels Image · Extract · Review · Translate · Add (D-05) per AC_STAGES, with the active/completed dots amber and upcoming muted. (Pick is pre-stepper and shows NO dots; Result lives under the "Add" dot — do not add a 6th dot.) Keep it pure/presentational; the consumer passes which stage is active.
    Create `src/components/daybreak/ac-deck-select.tsx`: the full-width "Add words to" deck field (D-02). It accepts the SAME data as DeckSwitcher (`decks`, `activeDeckId`, `onDeckChange`, `nativeLang`) and renders a full-width field-style trigger row: a "Add words to" label (13px 700 #4A331C), a 52px row containing `<LangChip code={activeDeck.language.toUpperCase()} />` + the full deck name + a down chevron, and a helper "Defaults to your active deck · change it or create a new one" (12.5px muted). It MUST reuse the Phase 21 `DeckSwitcher` popover internals — including the inline "+ New deck" create flow — rather than reimplement them: the cleanest approach (planner's discretion per RESEARCH) is to host the existing popover content with this full-width row as the trigger (either by passing a custom trigger to DeckSwitcher or wrapping the same Popover content). The trigger row carries `data-testid="confirm-deck-select"` (NOT "deck-picker-trigger" — that belongs to the header instance; no conflict per RESEARCH Pitfall 7). createDeck must work in-flow (do not stub it). Do NOT modify deck-switcher.tsx's own behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "image-drop-zone|ac-stepper|ac-deck-select" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Upload a Photo" src/components/image-drop-zone.tsx` ≥1 AND `grep -c "Drop an image here" src/components/image-drop-zone.tsx` returns 0 (copy changed).
    - `grep -c "10 MB" src/components/image-drop-zone.tsx` returns 0 AND `grep -c "10MB" src/components/image-drop-zone.tsx` returns 0 (D-06 — no 10 MB placeholder); `image-drop-zone.tsx` still calls `validateImageFile` (`grep -c "validateImageFile" src/components/image-drop-zone.tsx` ≥1).
    - `grep -c "data-testid=\"file-error\"" src/components/image-drop-zone.tsx` =1.
    - `ac-stepper.tsx` lists the five stages — `grep -c "Translate" src/components/daybreak/ac-stepper.tsx` ≥1 AND `grep -c "Extract" src/components/daybreak/ac-stepper.tsx` ≥1 (Image · Extract · Review · Translate · Add).
    - `ac-deck-select.tsx` reuses DeckSwitcher's popover/create and carries the scoped testid — `grep -c "confirm-deck-select" src/components/daybreak/ac-deck-select.tsx` =1 AND (`grep -c "deck-switcher" src/components/daybreak/ac-deck-select.tsx` ≥1 OR `grep -c "createDeck" src/components/daybreak/ac-deck-select.tsx` ≥1) — the inline create path is wired, not stubbed.
    - `npx tsc --noEmit` reports no error in the three touched files.
  </acceptance_criteria>
  <done>The drop zone is the Daybreak ACDrop with 5 MB / JPG·PNG·WebP copy + a file-error testid; the 5-dot ACStepper and the full-width ACDeckSelect (reusing the Phase 21 DeckSwitcher popover + inline create, scoped testid) exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle image-upload-flow Pick/Confirm/Extracting + D-03 cancelled guard (ADC-03, D-02, D-03, D-04, L-02)</name>
  <files>src/components/image-upload-flow.tsx, src/components/__tests__/image-upload-flow-cancel.test.tsx</files>
  <behavior>
    - Picking a valid file shows the Confirm surface (ACThumb thumbnail + ACDeckSelect "Add words to" + an "Extract words" primary button); Re-pick returns to the dropzone; Cancel exits to /dashboard.
    - Clicking "Extract words" shows the calm ACProgress Extracting wait; clicking its Cancel returns to the Confirm surface with the picked image AND chosen deck still present (no re-pick needed).
    - A late-resolving extraction result that arrives AFTER Cancel does NOT advance the flow (the cancelled.current guard swallows it); a fresh extraction after Cancel still works (the guard is reset before each attempt).
  </behavior>
  <read_first>
    - src/components/image-upload-flow.tsx (self — the reducer lines 50-111, the D-16 comment line 101, handleExtract lines 194-271, the AbortController lines 225-226 — preserve ALL; only the rendered surface + the D-03 guard change)
    - src/components/review-list.tsx lines 451, 490-492, 513 (the canonical cancelled.current pattern to copy for D-03)
    - src/components/review-list.test.ts lines 1-60 (the Wave-0 reducer-test scaffold convention: vi.hoisted mocks, vi.mock for next/headers + @/lib/deck-actions + next/cache — the model for the cancel unit test)
    - src/components/daybreak/ac-progress.tsx + ac-btn.tsx + ac-banner.tsx + ac-deck-select.tsx + ac-stepper.tsx (the atoms to render per step)
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md (image-upload-flow is a "use client" component; the /api/extract route is unchanged — confirm no v16 client/route API drift before editing)
    - design/handoff-daybreak/daybreak-addcard.jsx + daybreak-addcard-boards.jsx (ACPick, ACConfirm, ACExtracting, ACNoWords boards) + ACFlowTop (Back/Re-pick/Cancel controls)
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"image-upload-flow.tsx" (the exact 5-step D-03 guard insertion + the surface transformation map) and §"Reducer-Preserved Restyle Pattern"
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"D-03 Cancelled Guard — Exact Pattern" + §"image-upload-flow.tsx" (the structural Pick→Confirm flow change + Pitfall 3 reset + Pitfall 6 "Next: choose deck" removal)
  </read_first>
  <action>
    Restyle ONLY the rendered surface of `image-upload-flow.tsx` per the PATTERNS.md transformation map, and add the D-03 guard. Do NOT restructure the reducer, change action types, or alter the `/api/extract` call or the 35s `AbortController`.
    Surface: render the `ACStepper` (from Task 1) for the Confirm-onward steps with the right stage active (Confirm → "Image" dot; Extracting → "Extract" dot) and the ACFlowTop controls — Back (previous step), Re-pick on Confirm (→ dropzone, dispatch CLEAR_FILE / BACK_TO_PICK as the existing reducer expects), and Cancel = safe exit to /dashboard (discards the in-progress flow, saves nothing) (D-04). The Pick surface shows the restyled `ImageDropZone` and NO stepper dots (pre-stepper). Per RESEARCH Pitfall 6, the current "Next: choose deck" intermediate button is removed — picking a valid file goes directly to the Confirm surface (ACThumb + ACDeckSelect + "Extract words"); map the existing `ADVANCE_STEP` so file-pick lands on Confirm. The Confirm thumbnail (`ACThumb`) carries `data-testid="image-preview"` (L-06). The deck field on Confirm is the Task-1 `ACDeckSelect` (D-02), wired to the existing deck-selection dispatch (`SELECT_DECK`/whatever the reducer uses) and `decks`/`defaultDeckId`/`nativeLang` props. The Extracting wait is `<ACProgress title="Reading your image…" sub="This can take up to 30 seconds. Hang tight — your lion is sniffing out the words." searching={true} />` with a Cancel `ACBtn kind="ghost"` beside it calling `handleCancelExtraction()`. No-words → a LionFace sunrise-disc empty state ("No words found.") with a work-preserving Try-again `ACBtn`; extract-error → `ACBanner kind="error"` + a Try-again `ACBtn` (both preserve file+deck for retry). Leo not tiger, no emoji (L-01).
    D-03 guard (the ONLY behavior change — exact 5-step pattern from RESEARCH/PATTERNS, modeled on review-list.tsx): (1) declare `const cancelled = useRef(false)` alongside `dropZoneRef`; (2) at the top of `handleExtract()` set `cancelled.current = false` before `dispatch({type:"EXTRACT_START"})` (Pitfall 3 — reset before each attempt); (3) after the `if (res.ok)` branch resolves, add `if (cancelled.current) return;` before the `EXTRACT_NO_WORDS` dispatch AND before the `EXTRACT_SUCCESS` dispatch; (4) in the error branch add `if (cancelled.current) return;` before the `EXTRACT_ERROR` dispatch; (5) add `function handleCancelExtraction(){ cancelled.current = true; dispatch({type:"BACK_TO_PICK"}); }` (returns to Confirm with file/deck preserved via the existing D-16 reducer behavior). Do NOT wire the 35s `AbortController` to Cancel; do NOT add an `EXTRACT_CANCEL` action (D-03).
    Then WRITE `src/components/__tests__/image-upload-flow-cancel.test.tsx` (model the vi.hoisted + vi.mock scaffold from review-list.test.ts; `// @vitest-environment jsdom` + afterEach(cleanup) if it renders the component, or a pure logic test if you can exercise the guard via the exported handler). Tests: (1) when `cancelled.current` is true, a resolved extraction does NOT dispatch EXTRACT_SUCCESS (the guard returns early) — assert no advance to the review surface / no `extractWords` set; (2) after Cancel then a fresh handleExtract(), the reset makes a subsequent success advance normally (Pitfall 3 regression). If exercising the private guard through the component is impractical, extract the guard decision into a tiny pure helper (e.g. `shouldIgnoreResult(cancelledRef)`) co-located in the file and unit-test that — keeping the component behavior intact.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/image-upload-flow-cancel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useRef(false)" src/components/image-upload-flow.tsx` ≥1 AND `grep -c "cancelled.current" src/components/image-upload-flow.tsx` ≥3 (declare + reset + ≥1 guard).
    - The forbidden behavior changes are absent — `grep -c "EXTRACT_CANCEL" src/components/image-upload-flow.tsx` returns 0; the AbortController is preserved and NOT wired to Cancel — `grep -c "AbortController" src/components/image-upload-flow.tsx` ≥1 AND `grep -c "controller.abort" src/components/image-upload-flow.tsx` ≥1 (still the 35s timeout only).
    - `handleCancelExtraction` dispatches BACK_TO_PICK — `grep -c "handleCancelExtraction" src/components/image-upload-flow.tsx` ≥1 AND `grep -c "BACK_TO_PICK" src/components/image-upload-flow.tsx` ≥1.
    - The /api/extract call is preserved — `grep -c "/api/extract" src/components/image-upload-flow.tsx` ≥1.
    - The Confirm thumbnail + deck field present — `grep -c "image-preview" src/components/image-upload-flow.tsx` ≥1 AND `grep -c "ACDeckSelect" src/components/image-upload-flow.tsx` ≥1; the Extracting wait uses ACProgress — `grep -c "ac-progress" src/components/image-upload-flow.tsx` ≥1.
    - The old intermediate button is gone — `grep -c "Next: choose deck" src/components/image-upload-flow.tsx` returns 0 (Pitfall 6).
    - `npx vitest run src/components/__tests__/image-upload-flow-cancel.test.tsx` passes both guard tests.
  </acceptance_criteria>
  <done>image-upload-flow's Pick/Confirm/Extracting/no-words/error surfaces are Daybreak (ACThumb + ACDeckSelect + ACProgress, Re-pick/Cancel nav), the D-03 cancelled guard ignores late results and resets per attempt, the reducer + /api/extract + 35s AbortController are untouched, and the guard is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget e2e/11 image-upload spec + Cancel-preserves-image test (L-06, D-03)</name>
  <files>e2e/11-phase9-image-upload.spec.ts</files>
  <read_first>
    - e2e/11-phase9-image-upload.spec.ts (lines 21, 22, 27/31/73, 33, 44, 53, 64, 66, 68/87/100, 72, 88, 90, 97 — every at-risk locator)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"e2e Selector Audit" → "e2e/11-phase9-image-upload.spec.ts" table (the exact per-line retargets) + "Structural flow change" note + §"Validation Architecture" (per-project Playwright batches; webServer undefined; :3000 dev server; generic {name:"Next"} collides with the Next.js devtools button — use specific locators)
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"e2e Selector Retargeting (L-06)" (the retarget map + data-testid additions)
    - .planning/phases/22-add-a-card/22-VALIDATION.md §"Wave 0 Requirements" (the e2e/11 retargets + the Cancel-on-Extracting new test)
  </read_first>
  <action>
    Retarget every locator the restyle breaks in `e2e/11-phase9-image-upload.spec.ts`, PRESERVING each assertion's behavioral intent (never re-add old copy, never weaken/rewrite an assertion — only the locator/string changes to match the new Daybreak surface). Apply the RESEARCH table:
    - line 22 `getByRole("button",{name:"From image"})` → `getByRole("button",{name:"From an image"})` (D-07; line 21 "Type a word" stays).
    - lines 27/31/73 `getByText("Drop an image here")` → `getByText("Upload a Photo")`.
    - line 33 `getByText("or click to browse, or paste a screenshot (Ctrl+V)")` → `getByText(/browse your files/)` (partial, robust to wording).
    - lines 44/53 (file-type / size error copy) → keep the regex assertions (`/JPG, PNG, or WebP only/i`, `/please pick one under 5MB/i`) since `image-validation.ts` copy is unchanged (D-06), but prefer scoping to `getByTestId("file-error")` for robustness.
    - line 64 `toHaveClass(/max-h-64/)` → REMOVE the class assertion (ACThumb uses inline styles); replace with `getByTestId("image-preview")` visible (structural, not class).
    - line 66 `getByRole("button",{name:"Choose different image"})` → `getByRole("button",{name:"Change image"})` (or "Remove"/"Re-pick" per the actual Confirm controls implemented — match what Task 2 rendered).
    - lines 68/87/100 `getByRole("button",{name:"Next: choose deck"})` → the button is REMOVED (Pitfall 6); rewrite those steps as assertions about the Confirm surface appearing (e.g. `getByTestId("image-preview")` + `getByTestId("confirm-deck-select")` visible) and `getByRole("button",{name:"Extract words"})` for the extract gate (line 90 "Extract words" stays).
    - line 72 `getByRole("button",{name:"Remove selected image"})` → `getByRole("button",{name:"Remove"})` (or the aria-label Task 2 set).
    - line 88 `getByText("Add words to:")` → `getByText("Add words to")` (no colon).
    - line 97 `getByRole("button",{name:"Back"})` → `getByRole("button",{name:"Re-pick"})` (the Confirm flow-top control).
    Then ADD a new test (D-03 Cancel-preserves-image): pick a valid fixture image → reach Confirm (assert `image-preview` visible) → click "Extract words" → on the Extracting wait click Cancel → assert the flow is back on Confirm with `getByTestId("image-preview")` STILL visible and `getByTestId("confirm-deck-select")` still showing the chosen deck (no re-pick required). This is the D-03 behavior guard. (If a real Extract round-trip needs API keys, the test can intercept/stub the /api/extract response or simply assert the Cancel→Confirm transition preserves the preview without waiting for a real extraction — the guard's observable effect is the preserved image, not the extraction result.)
    After edits, grep the WHOLE `e2e/` dir for each changed string (`"Drop an image here"`, `"Next: choose deck"`, `"Choose different image"`, `"Add words to:"`, `max-h-64`) and fix stragglers; watch for Playwright strict-mode multi-match (scope with `confirm-deck-select` / `image-preview` / `add-card-title` testids if a phrase renders twice — e.g. the Confirm deck row vs the header deck picker).
  </action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "From image" e2e/11-phase9-image-upload.spec.ts` returns 0 and `grep -c "From an image" e2e/11-phase9-image-upload.spec.ts` ≥1 (D-07).
    - `grep -c "Next: choose deck" e2e/11-phase9-image-upload.spec.ts` returns 0 (removed-button steps rewritten); `grep -c "max-h-64" e2e/11-phase9-image-upload.spec.ts` returns 0 (class assertion removed).
    - `grep -c "Drop an image here" e2e/11-phase9-image-upload.spec.ts` returns 0; `grep -c "Upload a Photo" e2e/11-phase9-image-upload.spec.ts` ≥1.
    - A Cancel-preserves-image test exists — `grep -ci "cancel" e2e/11-phase9-image-upload.spec.ts` ≥1 AND the new test references `image-preview` after a Cancel (`grep -c "image-preview" e2e/11-phase9-image-upload.spec.ts` ≥1).
    - `grep -rc "Next: choose deck" e2e/` and `grep -rc "Drop an image here" e2e/` both return 0 across the whole dir (stragglers fixed).
    - No assertion intent was changed (reviewer-checkable diff: only locators/strings differ; the new Cancel test adds coverage, it does not weaken existing checks).
    - NOTE for the wave gate: the full Playwright run happens at the Wave 2 boundary (per VALIDATION sampling) after a fresh `npm run dev` restart — `npx playwright test --project=web e2e/11-phase9-image-upload.spec.ts e2e/04-manual-card-entry.spec.ts e2e/09-language-breakdown.spec.ts` then the `--project=mobile` batch. These retargets + the Cancel test are validated there.
  </acceptance_criteria>
  <done>e2e/11 is fully retargeted to role+name / data-testid with intent preserved (toggle label, drop-zone copy, removed buttons, the max-h-64 break, deck label, Re-pick), a D-03 Cancel-preserves-image test is added, and no stale literals remain anywhere in e2e/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/extract | Existing auth-gated, rate-limited (10/min), magic-byte-validated Claude-vision endpoint (5 MB client / 7 MB server cap). Called from the preserved `handleExtract` + 35s AbortController. Unchanged this plan. |
| client → createDeck server action | In-flow deck create via the reused DeckSwitcher popover; ownership/validation enforced server-side. Unchanged this plan. |
| client (file input) → browser memory | Picked image validated client-side by `validateImageFile` (type + 5 MB) before any upload; copy/limit unchanged (D-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-T | Tampering | The D-03 cancelled.current guard in image-upload-flow | accept (client-only) | The guard is a client-side late-result ignore (one ref). It neither adds nor removes any server-side validation — the `/api/extract` magic-byte + rate-limit + 5 MB/7 MB checks are untouched. It cannot bypass a server control; worst case it discards a result the client already paid for. No new attack surface. |
| T-22-03-I | Information Disclosure | image-drop-zone + ACThumb restyle | accept (preserved) | Only copy + visual change; `validateImageFile` (type/size) is unchanged. No image leaves the client without passing the existing client validation and the unchanged server magic-byte/size checks. |
| T-22-03-E | Elevation of Privilege | ACDeckSelect reuse of DeckSwitcher createDeck | accept (preserved) | The full-width trigger drives the SAME `createDeck` action with the same language-code argument as the header picker; server-side ownership/validation is unchanged. Only the trigger presentation changed (D-02). |
| T-22-03-S | Spoofing | Cancel → /dashboard exit | accept | Client navigation to an already-protected route; no auth decision moves client-side. |
| T-22-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (`motion/react`, `@base-ui/react` already present — RESEARCH "Package Legitimacy Audit": N/A, no new packages). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages; legitimacy gate N/A. |

No HIGH-severity threats. Surface-only re-skin + a single client-side late-result guard (D-03) that touches no server-side validation. No new network calls, inputs, auth, routes, or schema; `/api/extract`, the 35s AbortController, `createDeck`, and `validateImageFile` are all preserved unchanged.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/image-upload-flow-cancel.test.tsx` green (D-03 guard ignores late results + resets per attempt).
- `npx tsc --noEmit` clean (whole project) for the touched files.
- Wave-2 boundary: fresh `npm run dev` restart, then `npx playwright test --project=web e2e/11-phase9-image-upload.spec.ts e2e/04-manual-card-entry.spec.ts e2e/09-language-breakdown.spec.ts` then the `--project=mobile` batch — green (image-upload flow + the Cancel-preserves-image test).
- `npx biome ci` clean over the touched files only (scoped, never `biome ci .`).
- e2e retarget greps clean (no stale `Next: choose deck` / `Drop an image here` / `max-h-64`).
- Manual (phase UAT): the Pick/Confirm/Extracting/no-words/error states + the in-flow "+ New deck" create match `daybreak-addcard-boards.jsx` — recorded in `22-HUMAN-UAT.md`.
</verification>

<success_criteria>
- ADC-01/ADC-03 (first half): the from-an-image Pick → Confirm → Extracting surfaces are Daybreak; the Confirm deck field is the full-width ACDeckSelect reusing the Phase 21 DeckSwitcher popover + working inline "+ New deck" create (D-02).
- D-04 nav (Pick = pre-stepper dropzone; Confirm onward = 5-dot stepper with Back/Re-pick/Cancel; Cancel exits, nothing commits) and D-05 (Image · Extract · Review · Translate · Add) implemented.
- D-03: Extracting/Translating restyled to calm ACProgress; Cancel returns to Confirm preserving image+deck via the one-line cancelled.current guard (no AbortController wiring, no EXTRACT_CANCEL). D-06: 5 MB / JPG·PNG·WebP copy.
- L-02 behavior preserved (reducer, /api/extract, 35s AbortController, retries); L-01 Leo-not-tiger/no-emoji; L-06 e2e/11 retargeted with intent preserved + Cancel-preserves-image test.
</success_criteria>

<output>
Create `.planning/phases/22-add-a-card/22-03-SUMMARY.md` when done.
</output>
