---
phase: 22-add-a-card
plan: 04
type: execute
wave: 3
depends_on: ["22-01", "22-03"]
files_modified:
  - src/components/review-list.tsx
  - e2e/11-phase9-image-upload.spec.ts
requirements: [ADC-03]
nyquist_compliant: true
autonomous: true

must_haves:
  truths:
    - "The Review step is restyled to Daybreak — ACReviewRow per kept word with select-all/none, 'Already in your deck · skipped' struck-through muted chips for duplicates, the 'keep at least one' guard, and a 'Translate N words' primary — preserving the dedupe, keep/exclude, and select-all/none behavior — ADC-03, L-02"
    - "The Translating wait uses the calm Daybreak ACProgress ('Translating N words…' / 'Almost there. You'll be able to check and fix each one.') with the existing in-flight translate-cancel preserved (its cancelled.current guard is the source model and stays untouched) — ADC-03, L-02"
    - "The Check-translations step uses ACPairRow with the TARGET (ES) field ON TOP and the NATIVE (EN) field BENEATH — the deliberate D-01 'lead with the source field' orientation, flipping the current native-on-top ReviewTranslationRow; the UI auditor must NOT correct it back to native-on-top — D-01"
    - "The commit action reads 'Add N cards' and nothing commits before it; the per-row translate-fail manual entry and the batched per-row-tolerant saveImageCards commit (added/skipped/failed counts) are preserved untouched — ADC-03, L-02"
    - "The Result states are Daybreak and live under the 'Add' stepper dot (no 6th step): success = LionFace (not tiger, no emoji) + 'N cards added!' + 'Go to my deck'; partial = a card with Added (green ✓) / Already learned (muted –) / Couldn't add (red !) counts; all-failed = an error banner + Try again / Back to deck — D-05, L-01"
    - "The e2e/11 spec assertions for the review/translate/result copy that this restyle changes ('Next: translate' → 'Translate N words', 'Check translations', the result counts) are retargeted to role+accessible-name / data-testid preserving each assertion's intent, never re-adding old copy — L-06"
  artifacts:
    - path: "src/components/review-list.tsx"
      provides: "Daybreak Review/Translate/Check/Result surface — ACReviewRow, ACProgress, ACPairRow (D-01 target-on-top), ACResult; behavior preserved"
      contains: "Add"
    - path: "e2e/11-phase9-image-upload.spec.ts"
      provides: "Review/translate/result locators retargeted (shared spec; Plan 03 owns the pick/confirm half)"
  key_links:
    - from: "src/components/review-list.tsx"
      to: "src/components/daybreak/ac-pair-row.tsx"
      via: "ACPairRow (D-01 target-on-top) for Check translations"
      pattern: "ac-pair-row"
    - from: "src/components/review-list.tsx"
      to: "src/lib/deck-actions.ts"
      via: "saveImageCards batched per-row-tolerant commit (preserved)"
      pattern: "saveImageCards"
    - from: "src/components/review-list.tsx"
      to: "/api/translate"
      via: "runTranslationFanOut + cancelled.current cancel (preserved)"
      pattern: "/api/translate"
---

<objective>
Re-skin the Review → Translate → Check → Result tail of the from-an-image stepper to Daybreak (ADC-03 second half). Restyle `review-list.tsx`'s rendered surface onto the Plan 01 atoms — `ACReviewRow` (keep/exclude + select-all/none + dedupe chips), `ACProgress` (Translating wait), `ACPairRow` (Check translations, flipped to target-on-top per D-01), the "Add N cards" commit, and the `ACResultSuccess`/`ACResultPartial`/all-failed Result states (under the "Add" dot, D-05) — while preserving every behavior. Then retarget the review/translate/result locators in the shared e2e/11 spec.

Purpose: ADC-03 (editable keep/exclude review list, editable translation pairs, result outcomes; nothing saves until "Add N cards"). This completes the image stepper begun in Plan 03.
Output: Daybreak-restyled `review-list.tsx` + the review/translate/result locator retargets in `e2e/11-phase9-image-upload.spec.ts`.

PRESENTATION-ONLY: the `reviewListReducer` (lines 100-438), all `dispatch` calls, `handleCancel`, `handleNext`, `handleCommit`, `commitReviewRows`, `runTranslationFanOut`, `getSameLanguageDeckBackWords`, and the existing `cancelled.current` translate-cancel guard (lines 451, 490-492, 513) are reused VERBATIM — only the JSX returned per step changes (L-02). This plan changes NO reducer logic; the existing `review-list.test.ts` reducer tests MUST stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-add-a-card/22-CONTEXT.md
@.planning/phases/22-add-a-card/22-RESEARCH.md
@.planning/phases/22-add-a-card/22-PATTERNS.md
@.planning/phases/22-add-a-card/22-VALIDATION.md

# Project conventions — Next.js 16 has breaking changes; read node_modules/next/dist/docs/ before asserting any Next.js API
@AGENTS.md

# Design contract (this phase runs --skip-ui; the JSX mock IS the spec — recreate faithfully; ACPairRow is target-on-top per D-01)
@design/handoff-daybreak/daybreak-addcard.jsx
@design/handoff-daybreak/daybreak-addcard-boards.jsx

# Atoms this plan consumes (created in Plan 01)
@src/components/daybreak/ac-review-row.tsx
@src/components/daybreak/ac-pair-row.tsx
@src/components/daybreak/ac-progress.tsx
@src/components/daybreak/ac-btn.tsx
@src/components/daybreak/ac-banner.tsx

# The stepper atom (created in Plan 03) so the Result lives under the "Add" dot
@src/components/daybreak/ac-stepper.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from RESEARCH/PATTERNS (VERIFIED line refs) + Plan 01 atoms + the design contract. -->

review-list.tsx behavior to PRESERVE (VERIFIED line refs):
  reducerListReducer lines 100-438; step machine "loading-dedupe" → "step-a" → "translating" → "step-b" → "committing" → "success";
  cancelled.current declared line 451, set in handleCancel() lines 490-492, guard line 513 — DO NOT modify (this is the source model for Plan 03's D-03 guard);
  runTranslationFanOut → POST /api/translate; commitReviewRows → saveImageCards(deckId,[{front,back}]) per-row try/catch (addedCount/failedCount/skippedCount);
  AlreadyLearnedRow (line 366) renders line-through muted duplicates; handleGoToDeck() → /dashboard?deck=${deckId} — preserve.
  CURRENT ReviewTranslationRow renders native field TOP / target field BOTTOM — D-01 requires the FLIP to target(ES) top / native(EN) bottom.

Copy changes (VERIFIED): "Next: translate" button → "Translate N words" (count + right chevron); "step-b" "Check translations" <p> → Baloo 2 display heading; the success <CheckCircle2> + plain text → LionFace disc + "N cards added!".

Plan 01 atoms:
  ACReviewRow({ word, excluded, last?, onToggle, onEdit, onRemove })
  ACPairRow({ targetLabel, nativeLabel, target, native, failed?, last?, onEditTarget, onEditNative })  // ES top, EN below — D-01
  ACProgress({ title, sub, searching? })  ·  ACBtn({ kind, icon, ... })  ·  ACBanner({ kind, children })

Result mapping (daybreak-addcard-boards.jsx ACResultSuccess / ACResultPartial): success when addedCount>0 && failedCount===0; partial when failedCount>0 && addedCount>0; all-failed when addedCount===0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle review-list Review + Translating + Check (D-01 target-on-top) (ADC-03, D-01, L-02)</name>
  <files>src/components/review-list.tsx</files>
  <behavior>
    - The Review step renders one ACReviewRow per kept word with a working select-all / select-none, struck-through muted chips for already-in-deck duplicates, and a "Translate N words" primary that is gated by "keep at least one word".
    - The Translating step shows the calm ACProgress wait and its Cancel still cancels the in-flight fan-out (existing cancelled.current guard unchanged).
    - The Check-translations step renders ACPairRow with the ES (target) field ABOVE the EN (native) field; per-row translate failures show the red EN field + "Translation unavailable — enter manually."; editing either field updates via the existing dispatch actions.
  </behavior>
  <read_first>
    - src/components/review-list.tsx (self — preserve the reducer + all handlers; the step renders are the only edits; the current ReviewTranslationRow native-on-top region is the D-01 flip target)
    - src/components/review-list.test.ts (the existing reducer-test scaffold + the exported-symbol contract — MUST stay green; do not change the reducer/exports it asserts)
    - src/components/daybreak/ac-review-row.tsx + ac-pair-row.tsx + ac-progress.tsx + ac-btn.tsx (Plan 01 atoms; ACPairRow is already target-on-top)
    - design/handoff-daybreak/daybreak-addcard.jsx — ACReviewRow (228-237), ACPairRow (240-254, target-on-top), ACProgress (210-224); daybreak-addcard-boards.jsx — ACReview, ACTranslating, ACCheck boards
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"review-list.tsx" (the step-by-step surface transformation table + the explicit field-orientation flip + the "Next: translate" → "Translate N words" copy change)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"review-list.tsx" (the verified line refs + the D-01 flip note) + §"Anti-Patterns to Avoid" (ACPairRow orientation is intentional)
  </read_first>
  <action>
    Restyle ONLY the rendered surface of `review-list.tsx` for the `loading-dedupe`, `step-a` (Review), `translating`, and `step-b` (Check) states per the PATTERNS.md transformation table. Do NOT touch the reducer, the dispatch actions, `runTranslationFanOut`, `commitReviewRows`, `getSameLanguageDeckBackWords`, or the existing `cancelled.current` translate-cancel guard.
    `loading-dedupe`: replace the bare `<Loader2>` with a minimal `ACProgress`/spinner (presentational).
    `step-a` (Review): render one `<ACReviewRow word={...} excluded={...} onToggle={...} onEdit={...} onRemove={...} />` per kept word, wiring `onToggle`/`onEdit`/`onRemove` to the EXISTING `TOGGLE_WORD`/`EDIT_WORD`/`REMOVE_WORD` dispatch actions (do not rename them). Replace the shadcn "Select all"/"Select none" `<Button variant="ghost">` with link-style controls wired to the existing select-all / `SELECT_NONE` actions. Keep the dedupe duplicates as struck-through muted "Already in your deck · skipped" chips (the existing `AlreadyLearnedRow`/`duplicates` source — restyle, don't re-source). Replace the "Next: translate" button with `<ACBtn kind="primary">Translate N words</ACBtn>` (count = kept words, right chevron icon), keeping the existing onClick (`handleNext`/`TRANSLATE_START`) and the "Keep at least one word to continue." guard.
    `translating`: replace the `<Loader2> + "Translating N words…" + <Button variant="ghost">Cancel</Button>` with `<ACProgress title={\`Translating ${n} words…\`} sub="Almost there. You'll be able to check and fix each one." searching={false} />` + a Cancel `ACBtn kind="ghost"` wired to the EXISTING `handleCancel` (which sets `cancelled.current=true` then `onCancel()` — unchanged).
    `step-b` (Check translations): render the heading as a Baloo 2 display heading ("Check translations"), then one `<ACPairRow targetLabel="ES" nativeLabel="EN" target={row.word} native={row.nativeText} failed={row.translateFailed} onEditTarget={...} onEditNative={...} />` per row. CRITICAL — D-01 flip: the ES (target = `row.word`) field is ON TOP, the EN (native = `row.nativeText`) field is BENEATH (the current `ReviewTranslationRow` has native on top — flip it). Wire `onEditTarget`/`onEditNative` to the EXISTING `EDIT_TARGET`/`EDIT_NATIVE` dispatch actions (do not rename). Per-row translate failures render the ACPairRow `failed` state (red EN field + "Translation unavailable — enter manually."). The commit button is built in Task 2.
    (The Result states are Task 2.) Leo not tiger, no emoji anywhere (L-01).
  </action>
  <verify>
    <automated>npx vitest run src/components/review-list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The reducer/handlers are preserved — `grep -c "reviewListReducer" src/components/review-list.tsx` ≥1, `grep -c "runTranslationFanOut" src/components/review-list.tsx` ≥1, `grep -c "commitReviewRows" src/components/review-list.tsx` ≥1, `grep -c "cancelled.current" src/components/review-list.tsx` ≥1 (the translate-cancel guard is untouched).
    - The atoms are wired — `grep -c "ac-review-row" src/components/review-list.tsx` ≥1 AND `grep -c "ac-pair-row" src/components/review-list.tsx` ≥1 AND `grep -c "ac-progress" src/components/review-list.tsx` ≥1.
    - The copy change is done — `grep -c "Translate" src/components/review-list.tsx` ≥1 (the "Translate N words" primary) AND `grep -c "Next: translate" src/components/review-list.tsx` returns 0 (old copy gone).
    - The D-01 flip is in place — ACPairRow is called with `target={` set from `row.word` (the ES/target value) on top: `grep -c "ACPairRow" src/components/review-list.tsx` ≥1 (the orientation itself is locked by Plan 01's atom unit test; this plan must pass `row.word` as `target` and `row.nativeText` as `native`).
    - `npx vitest run src/components/review-list.test.ts` stays green (reducer behavior unchanged by the restyle).
  </acceptance_criteria>
  <done>The Review/Translating/Check surfaces are Daybreak (ACReviewRow + dedupe chips + select-all/none, ACProgress translating with preserved cancel, ACPairRow target-on-top per D-01), all reducer behavior and the translate-cancel guard preserved, and the reducer tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle the 'Add N cards' commit + Result states (success/partial/all-failed) (ADC-03, D-05, L-01, L-02)</name>
  <files>src/components/review-list.tsx</files>
  <behavior>
    - The Check step's commit button reads "Add N cards" where N is the count that will commit; nothing commits before it is pressed.
    - On commit success with no failures, the Result shows a LionFace + "N cards added!" + "Go to my deck".
    - On partial success, the Result shows Added / Already learned / Couldn't add counts; on all-failed, it shows an error banner + Try again / Back to deck.
  </behavior>
  <read_first>
    - src/components/review-list.tsx (self — the `step-b` commit button + the `committing`/`success` renders; preserve `handleCommit`/`commitReviewRows`/`handleGoToDeck` and the addedCount/failedCount/skippedCount tallies)
    - src/components/daybreak/ac-btn.tsx + ac-banner.tsx + ac-progress.tsx + src/components/daybreak/lion-face.tsx (atoms for the commit button + Result states + the LionFace success disc) + src/components/daybreak/ac-stepper.tsx (so Result renders under the "Add" dot)
    - design/handoff-daybreak/daybreak-addcard-boards.jsx — ACResultSuccess, ACResultPartial (incl. the all-failed variant) boards
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"review-list.tsx" (the "Add N cards" + the three Result-state rows in the transformation table)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"State of the Art" (CheckCircle2 → LionFace; the result mapping) + the §"specifics" Result copy
  </read_first>
  <action>
    Restyle the commit button and the Result states; do NOT change `handleCommit`, `commitReviewRows`, `saveImageCards`, `handleGoToDeck`, or the count tallies.
    Commit button (end of `step-b`): replace the `<Button variant="default">Add {n} cards</Button>` with `<ACBtn kind="primary">Add {n} cards</ACBtn>` (N = the count that will commit — keep the existing computation), keeping the existing onClick → `handleCommit`/commit dispatch. Nothing commits before this (D-04/ADC-03 — the commit is the only write).
    `committing`: render a disabled `ACBtn` with a spinner, or an inline `ACProgress` (presentational; behavior unchanged).
    `success` render — map the existing counts to the three Daybreak Result states (under the "Add" stepper dot, D-05 — render `ACStepper current="Add"` above):
      • success (addedCount>0 && failedCount===0): `LionFace` (size ~72) in a sunrise disc + a Baloo 2 "N cards added!" heading + a "Go to my deck" `ACBtn` wired to the existing `handleGoToDeck` (→ /dashboard?deck=…). No tiger, no emoji (L-01).
      • partial (failedCount>0 && addedCount>0): a `Card`-surface block with three rows — Added (green ✓, addedCount) / Already learned (muted –, skippedCount) / Couldn't add (red !, failedCount) — + "Go to my deck" and (if useful) a retry control, per the ACResultPartial board.
      • all-failed (addedCount===0): `<ACBanner kind="error">Couldn't add cards — please try again.</ACBanner>` + a "Try again" `ACBtn` + a "Back to deck" `ACBtn` (ghost). Wire Try-again / Back-to-deck to the existing dispatch/handlers (do not invent new write paths).
    Replace the current `<CheckCircle2>` lucide usage with the LionFace disc per RESEARCH "State of the Art".
  </action>
  <verify>
    <automated>npx vitest run src/components/review-list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The commit + result behavior is preserved — `grep -c "handleCommit" src/components/review-list.tsx` ≥1, `grep -c "saveImageCards" src/components/review-list.tsx` ≥1, `grep -c "handleGoToDeck" src/components/review-list.tsx` ≥1, and the counts are still used — `grep -c "addedCount" src/components/review-list.tsx` ≥1 AND `grep -c "failedCount" src/components/review-list.tsx` ≥1.
    - The commit label is "Add N cards" (templated) — `grep -c "Add " src/components/review-list.tsx` ≥1 with the count interpolation present (e.g. `Add {` / `Add ${`); the success heading uses "cards added" — `grep -c "cards added" src/components/review-list.tsx` ≥1.
    - The all-failed banner copy is present — `grep -c "Couldn't add cards — please try again." src/components/review-list.tsx` ≥1.
    - LionFace replaces the lucide check in the success state — `grep -c "LionFace" src/components/review-list.tsx` ≥1 AND `grep -c "CheckCircle2" src/components/review-list.tsx` returns 0; the Result renders under the Add dot — `grep -c "ac-stepper" src/components/review-list.tsx` ≥1.
    - No emoji — `grep -rnP "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]" src/components/review-list.tsx` returns no matches (✓/–/! are ASCII glyphs).
    - `npx vitest run src/components/review-list.test.ts` stays green.
  </acceptance_criteria>
  <done>The commit reads "Add N cards" (nothing commits before it) and the three Daybreak Result states (success LionFace + count / partial counts / all-failed banner) render under the Add dot, with all commit/navigation behavior and the count tallies preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget e2e/11 review/translate/result locators + scoped gate (L-06)</name>
  <files>e2e/11-phase9-image-upload.spec.ts</files>
  <read_first>
    - e2e/11-phase9-image-upload.spec.ts (any assertions reaching the review/translate/result steps — "Next: translate", "Check translations", result counts/copy; note Plan 03 already retargeted the pick/confirm half of this same file)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"e2e Selector Audit" (the review/translate/result copy changes) + §"Validation Architecture" (per-project Playwright batches; webServer undefined; :3000; the {name:"Next"} devtools-collision note — use specific locators)
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"e2e Selector Retargeting (L-06)"
    - .planning/phases/22-add-a-card/22-VALIDATION.md §"Sampling Rate" (the per-project full-suite run)
  </read_first>
  <action>
    Retarget any review/translate/result-step locators this restyle breaks in `e2e/11-phase9-image-upload.spec.ts`, PRESERVING each assertion's intent (only the locator/string changes; never re-add old copy, never weaken an assertion). Specifically: any `"Next: translate"` literal → `getByRole("button", { name: /Translate \d+ words?/ })` (matches the new "Translate N words" label, count-agnostic); any `"Check translations"` text assertion → keep as a heading/`getByText` match (copy preserved, only its element/role changed to a display heading — use `getByText(/Check translations/)` or `getByRole("heading", { name: /Check translations/ })`); any result-count copy that changed (e.g. the success "cards added" string or the all-failed "Couldn't add cards" string) → match the new strings or scope via a result `data-testid` if one was added in Task 2. Use role+accessible-name for the "Translate N words", "Add N cards", and "Go to my deck" buttons (they are real `ACBtn` buttons), and `{exact:true}`/specific locators where a generic name could collide with the Next.js devtools button.
    Because the full image happy-path (extract→translate→commit) needs live API keys (per VALIDATION "Manual-Only Verifications"), do NOT add a new deep integration test here — retarget the existing assertions only, and rely on the reducer unit tests (`review-list.test.ts`) for the keep/exclude/translate/commit logic. After edits, grep the WHOLE `e2e/` dir for `"Next: translate"` and fix any straggler; watch for strict-mode multi-match (scope with a testid if "Translate"/"Add" render in more than one place).
  </action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Next: translate" e2e/11-phase9-image-upload.spec.ts` returns 0 (retargeted).
    - `grep -rc "Next: translate" e2e/` returns 0 across the whole dir (no stragglers).
    - The "Translate N words" assertion uses a role+name locator — `grep -c "Translate" e2e/11-phase9-image-upload.spec.ts` ≥1 within a `getByRole("button"` (count-agnostic regex acceptable).
    - No assertion intent was changed (reviewer-checkable diff: only locators/strings differ from the prior revision; no expectation rewritten).
    - NOTE for the phase gate: the full per-project Playwright suites (`--project=web` then `--project=mobile`) run before `/gsd:verify-work` after a fresh `npm run dev` restart; the review/translate/result copy retargets are validated there. `npx biome ci src/components/review-list.tsx e2e/11-phase9-image-upload.spec.ts` (scoped, never `biome ci .`) must be clean for the touched files.
  </acceptance_criteria>
  <done>The e2e/11 review/translate/result locators are retargeted to role+name / data-testid with intent preserved, no stale "Next: translate" literal remains anywhere, and the touched files pass scoped biome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/translate | Existing auth-gated DeepL endpoint; called from the preserved `runTranslationFanOut`. Unchanged this plan. |
| client → saveImageCards server action | The batched per-row-tolerant card-create commit; ownership/validation enforced server-side. Unchanged this plan — the only write, gated behind "Add N cards". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-T | Tampering | review-list surface restyle (Review/Translate/Check/Result) | accept (preserved) | Only the JSX returned per step changes; the reducer, `runTranslationFanOut`, `commitReviewRows`/`saveImageCards`, the count tallies, and the existing `cancelled.current` translate-cancel guard are untouched. Same payloads cross the same boundaries. No new attack surface. |
| T-22-04-I | Information Disclosure | ACPairRow editable pairs (D-01) | accept (preserved) | The fields edit the same in-memory rows via the existing `EDIT_TARGET`/`EDIT_NATIVE` dispatch; only the visual order (ES on top) changed. No new data leaves the client; the commit still validates server-side. |
| T-22-04-E | Elevation of Privilege | "Add N cards" commit | accept (preserved) | The commit calls the SAME `saveImageCards` action with the same per-row `{front,back}` payloads; server-side ownership/validation is unchanged. Nothing commits before the explicit action (no auto-write). |
| T-22-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (`motion/react` already present — RESEARCH "Package Legitimacy Audit": N/A, no new packages). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages; legitimacy gate N/A. |

No HIGH-severity threats. Surface-only re-skin of the review/translate/result tail. No new network calls, inputs, auth, routes, or schema; `/api/translate`, `saveImageCards`, the fan-out, and the translate-cancel guard are all preserved unchanged.
</threat_model>

<verification>
- `npx vitest run src/components/review-list.test.ts` green (reducer behavior unchanged by the restyle).
- `npx tsc --noEmit` clean (whole project) for the touched files.
- Phase gate (before `/gsd:verify-work`): fresh `npm run dev` restart, then `npx playwright test --project=web` then `npx playwright test --project=mobile` green (full suites; the review/translate/result retargets validated alongside Plan 02/03's specs).
- `npx biome ci` clean over the two touched files only (scoped, never `biome ci .`).
- e2e retarget greps clean (no stale `Next: translate`).
- Manual (phase UAT, needs live DeepL + Claude-vision keys): the full image→extract→translate→"Add N cards"→Result happy path with real counts, and the Review/Check/Result states vs `daybreak-addcard-boards.jsx` — recorded in `22-HUMAN-UAT.md`.
</verification>

<success_criteria>
- ADC-03 (second half): Review (ACReviewRow + select-all/none + dedupe chips + keep-at-least-one), Translating (ACProgress + preserved cancel), Check (ACPairRow target-on-top per D-01 + per-row fail), the "Add N cards" commit (nothing commits before it), and the three Result states are all Daybreak.
- D-01: the Check pairs are ES-on-top / EN-beneath (flipped from native-on-top); the orientation is the deliberate exception and is locked by Plan 01's atom test.
- D-05: the Result lives under the "Add" stepper dot (no 6th step). L-01: LionFace not tiger, no emoji.
- L-02 behavior preserved (reducer, /api/translate fan-out, saveImageCards, counts, translate-cancel guard); L-06 e2e/11 review/translate/result locators retargeted with intent preserved.
</success_criteria>

<output>
Create `.planning/phases/22-add-a-card/22-04-SUMMARY.md` when done.
</output>
