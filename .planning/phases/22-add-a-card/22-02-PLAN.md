---
phase: 22-add-a-card
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/components/new-card-mode-toggle.tsx
  - src/components/daybreak/ac-context.tsx
  - src/components/daybreak/ac-top.tsx
  - src/components/translation-form.tsx
  - src/app/(protected)/deck/new-card/page.tsx
  - e2e/04-manual-card-entry.spec.ts
requirements: [ADC-01, ADC-02]
nyquist_compliant: true
autonomous: true

must_haves:
  truths:
    - "The Add-a-Card destination shows the Daybreak segmented toggle ('Type a word | From an image'), a persistent context line (LangChip EN → LangChip ES · 'saves to your Spanish deck'), and a top-left '‹ My deck' escape link to /dashboard — the toggle is the pre-stepper nav for the Pick surface — ADC-01, D-04"
    - "The mode toggle's image label reads 'From an image' (replacing the current 'From image'), driving the L-06 e2e selector retarget — D-07"
    - "Type-a-word is restyled to Daybreak native-first linked fields (ACField order EN then ES) with an auto-translate shimmer/'Translating…' pending state, soft translate-fail ('Translation unavailable — enter manually.'), Save locked until both filled, and a 'Card saved — add another.' ACBanner above the fields that clears the form for the next card — ADC-02, D-01"
    - "All of translation-form's behavior is preserved untouched — the bidirectional debounced /api/translate auto-translate, activeField stale-result guard, and saveCard — only the JSX surface (shadcn Input/Label/Button → ACField/ACBtn/ACBanner) changes — L-02"
    - "The context line uses the shared LangChip text chips (EN/ES), not flag emoji, and shows Leo-not-tiger conventions throughout — L-01"
    - "The e2e specs that targeted the old toggle label and the type-a-word copy are retargeted to role+accessible-name / data-testid preserving each assertion's intent, never re-adding old copy; a new test guards the 'From an image' label after toggling — L-06, D-07"
  artifacts:
    - path: "src/components/new-card-mode-toggle.tsx"
      provides: "Daybreak Add-a-Card shell — ACTop + ACContext + ACSeg toggle + child mount"
      contains: "From an image"
    - path: "src/components/daybreak/ac-context.tsx"
      provides: "Context line atom — LangChip EN → LangChip ES · saves to your <deck>"
      contains: "LangChip"
    - path: "src/components/daybreak/ac-top.tsx"
      provides: "Top bar atom — '‹ My deck' link + 'Add a Card' display title (data-testid add-card-title)"
      contains: "add-card-title"
    - path: "src/components/translation-form.tsx"
      provides: "Daybreak native-first linked fields + shimmer + ACBanner saved state, behavior preserved"
      contains: "Card saved — add another."
    - path: "src/app/(protected)/deck/new-card/page.tsx"
      provides: "Daybreak full-bleed shell wrapping NewCardModeToggle (RSC, no behavior change)"
    - path: "e2e/04-manual-card-entry.spec.ts"
      provides: "Retargeted manual-entry spec + 'From an image' label regression test"
      contains: "From an image"
  key_links:
    - from: "src/components/new-card-mode-toggle.tsx"
      to: "src/components/daybreak/ac-seg.tsx"
      via: "ACSeg toggle (mode/onChange)"
      pattern: "ac-seg"
    - from: "src/components/daybreak/ac-top.tsx"
      to: "/dashboard"
      via: "'‹ My deck' Link href=/dashboard"
      pattern: "/dashboard"
    - from: "src/components/translation-form.tsx"
      to: "/api/translate"
      via: "debounced bidirectional translateFrom (preserved)"
      pattern: "/api/translate"
    - from: "src/components/translation-form.tsx"
      to: "src/components/daybreak/ac-banner.tsx"
      via: "ACBanner kind=ok 'Card saved — add another.'"
      pattern: "ac-banner"
---

<objective>
Re-skin the type-a-word half of Add-a-Card to Daybreak (ADC-01 chrome + ADC-02 flow). Replace the two shadcn buttons in `new-card-mode-toggle.tsx` with the `ACSeg` segmented toggle (D-07 label "From an image"), add the persistent `ACContext` line + the `ACTop` "‹ My deck" escape, restyle `translation-form.tsx` onto `ACField`/`ACBtn`/`ACBanner` (native-first per D-01) while preserving every behavior, and wrap the page entry in the Daybreak shell. Then retarget the manual-entry e2e spec and add the D-07 label regression test.

Purpose: ADC-01 (single destination + toggle + context line + escape) and ADC-02 (type-a-word all states). The toggle is the pre-stepper Pick-surface navigation (D-04); the from-an-image stepper is Plans 03/04.
Output: Daybreak toggle/context/top, restyled translation-form, Daybreak page shell, retargeted `e2e/04-manual-card-entry.spec.ts` + a "From an image" regression test.

PRESENTATION-ONLY: the translation-form reducer, the debounced bidirectional `translateFrom` (`/api/translate`), the `activeField.current` stale-result guard, and `saveCard` are reused verbatim — only the rendered surface changes (L-02). No new routes; the Pick→stepper transition stays an in-page conditional render on the single `/deck/new-card` route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-add-a-card/22-CONTEXT.md
@.planning/phases/22-add-a-card/22-RESEARCH.md
@.planning/phases/22-add-a-card/22-PATTERNS.md
@.planning/phases/22-add-a-card/22-VALIDATION.md

# Project conventions — Next.js 16 has breaking changes; read node_modules/next/dist/docs/ before asserting any Next.js API
@AGENTS.md

# Design contract (this phase runs --skip-ui; the JSX mock IS the spec)
@design/handoff-daybreak/daybreak-addcard.jsx
@design/handoff-daybreak/daybreak-addcard-boards.jsx

# Atoms this plan consumes (created in Plan 01)
@src/components/daybreak/ac-seg.tsx
@src/components/daybreak/ac-btn.tsx
@src/components/daybreak/ac-banner.tsx
@src/components/daybreak/lang-chip.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + Plan 01 atoms + the design contract. -->

Plan 01 atoms (already built):
  ACSeg({ mode:"type"|"image", onChange })  — two <button> segments, labels "Type a word"/"From an image"
  ACBtn(ButtonHTMLAttributes & { kind?, icon? })  — real <button>, kind primary/disabled/ghost/ghost-danger
  ACBanner({ kind:"ok"|"error", children })
  LangChip({ code, size? })  — shared text chip

NewCardModeToggle current contract to PRESERVE (src/components/new-card-mode-toggle.tsx, VERIFIED):
  "use client"; props { decks, activeDeckId, nativeLang, nativeLangLabel, targetLangLabel, targetLang };
  const [mode,setMode] = useState<"type"|"image">("type");  // KEEP
  renders <TranslationForm .../> or <ImageUploadFlow .../> by mode — KEEP both child mounts + all props

translation-form.tsx behavior to PRESERVE (VERIFIED line refs from RESEARCH/PATTERNS):
  reducer FormAction/FormState; translateFrom(text,direction) debounced 500ms via use-debounce → POST /api/translate;
  activeField.current stale-guard; Save disabled when !nativeText.trim() || !targetText.trim() || isSaving; saveCard.
  Current copy to change: "Card saved." → "Card saved — add another."; "Translation unavailable. Enter manually." → "Translation unavailable — enter manually."; keep "Save card" label.
  Current back link <Link href="/dashboard">…<ArrowLeft/>Back to my deck</Link> + <h1>Add a Card</h1> → REMOVE here (moves to ACTop in the toggle wrapper).

page.tsx current shell to replace (VERIFIED lines 46-57):
  <div className="min-h-screen bg-background"><main className="px-8 py-8 max-w-4xl mx-auto w-full"><NewCardModeToggle .../></main></div>
  — async RSC; searchParams: Promise<{deck?}>; 0-deck → redirect("/dashboard"); all props passed. KEEP all behavior; only swap the wrapper classes to the Daybreak centered phone-width column.

ACTop / ACContext design values (daybreak-addcard.jsx): ACTop lines 82-93 ("‹ My deck" Link → /dashboard; "Add a Card" var(--font-display) 20px 700; data-testid="add-card-title" on the title). ACContext lines 47-56 (LangChip EN → LangChip ES, → arrow #9C8467, "saves to your <bold deck>").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Daybreak Add-a-Card shell — ACTop + ACContext + ACSeg toggle (ADC-01, D-04, D-07, L-01)</name>
  <files>src/components/new-card-mode-toggle.tsx, src/components/daybreak/ac-context.tsx, src/components/daybreak/ac-top.tsx, src/app/(protected)/deck/new-card/page.tsx</files>
  <read_first>
    - src/components/new-card-mode-toggle.tsx (self — preserve "use client", props, `useState<"type"|"image">`, both child mounts + their props; only the surrounding JSX changes)
    - src/app/(protected)/deck/new-card/page.tsx (self — preserve the RSC auth/getUserDecks/0-deck redirect/searchParams; only the outer wrapper classes change)
    - src/app/(protected)/dashboard/page.tsx (the Phase 21 Daybreak full-bleed shell pattern to mirror)
    - src/components/daybreak/lang-chip.tsx (shared chip for the context line) and src/components/daybreak/ac-seg.tsx (the toggle)
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md (confirm the v16 server/client boundary: page.tsx stays a server component; the toggle is the "use client" island — do not rely on training-data Next.js knowledge)
    - design/handoff-daybreak/daybreak-addcard.jsx lines 47-56 (ACContext), 82-93 (ACTop), 78-100 (ACTypeScreen layout order)
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"new-card-mode-toggle.tsx" (before/after, ACTop/ACContext prop shapes, LangChip reuse) and §"page.tsx" (shell wrap)
  </read_first>
  <action>
    Create `src/components/daybreak/ac-top.tsx`: a presentational `ACTop` — a flex row with a left `<Link href="/dashboard">` reading "‹ My deck" (link color #C96F12, no emoji) and a centered/left "Add a Card" title (`<span data-testid="add-card-title">` styled `fontFamily: var(--font-display)`, fontSize 20, fontWeight 700, color #4A331C). Keep it tiny and self-contained. (`data-testid="add-card-title"` is required so e2e/04 + e2e/09 `getByText("Add a Card")` stay locatable — L-06.)
    Create `src/components/daybreak/ac-context.tsx`: a presentational `ACContext({ nativeLang, targetLang, targetDeckName })` rendering `<LangChip code={nativeLang} />` + a muted "→" (#9C8467) + `<LangChip code={targetLang} />` + " · saves to your " + a bold `{targetDeckName}` span. Use the shared `LangChip` from `daybreak/lang-chip`. No flag emoji (L-01). The chip codes are the short codes (e.g. "EN"/"ES") — derive from the label props (uppercase first two letters is acceptable, or pass explicit codes; planner's discretion to keep it unambiguous).
    Rewrite the RENDER of `NewCardModeToggle`: keep `"use client"`, the props, and `const [mode,setMode]=useState<"type"|"image">("type")`. Replace the `<div className="flex gap-2 mb-6">` two-`<Button>` block with: `<ACTop />`, then `<ACContext nativeLang={...} targetLang={...} targetDeckName={targetLangLabel + " deck"} />` (so it reads "saves to your Spanish deck"), then `<ACSeg mode={mode} onChange={setMode} />`. Keep the existing `{mode === "type" ? <TranslationForm .../> : <ImageUploadFlow .../>}` conditional and ALL of its props unchanged. Delete the `import { Button } from "@/components/ui/button"` line. The image label change from "From image" → "From an image" lives inside `ACSeg` (Plan 01) — confirm the rendered toggle shows "From an image" (D-07).
    In `page.tsx`: replace ONLY the outer wrapper — swap `<div className="min-h-screen bg-background"><main className="px-8 py-8 max-w-4xl mx-auto w-full">` for the Daybreak centered phone-width column (mirror dashboard/page.tsx: `min-h-screen bg-background flex flex-col items-center` outer + `w-full max-w-lg px-5 py-6` main; exact max-width/padding is discretion to match the mock's phone shell). Do NOT change the RSC logic, the `Promise.all`, the 0-deck `redirect("/dashboard")`, `searchParams`, `activeDeck` selection, or any `<NewCardModeToggle>` prop.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "new-card-mode-toggle|ac-context|ac-top|new-card/page" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "From an image" src/components/new-card-mode-toggle.tsx` returns 0 only if the label lives in ACSeg — instead verify the toggle wires ACSeg: `grep -c "ACSeg" src/components/new-card-mode-toggle.tsx` ≥1, and the rendered label is asserted by the e2e test in Task 3. `grep -c "From image" src/components/new-card-mode-toggle.tsx` returns 0 (old label gone — D-07).
    - `grep -c "ui/button" src/components/new-card-mode-toggle.tsx` returns 0 (shadcn Button removed); `grep -c "ACTop" src/components/new-card-mode-toggle.tsx` ≥1 and `grep -c "ACContext" src/components/new-card-mode-toggle.tsx` ≥1.
    - `new-card-mode-toggle.tsx` still mounts both children — `grep -c "TranslationForm" src/components/new-card-mode-toggle.tsx` ≥1 AND `grep -c "ImageUploadFlow" src/components/new-card-mode-toggle.tsx` ≥1 — with `useState<"type" | "image">` preserved.
    - `ac-top.tsx` has `data-testid="add-card-title"` and a `Link` to `/dashboard` — `grep -c "add-card-title" src/components/daybreak/ac-top.tsx` =1, `grep -c "/dashboard" src/components/daybreak/ac-top.tsx` ≥1.
    - `ac-context.tsx` imports the shared LangChip — `grep -c "daybreak/lang-chip" src/components/daybreak/ac-context.tsx` ≥1.
    - `page.tsx` still redirects 0-deck to /dashboard and passes all props — `grep -c "redirect(\"/dashboard\")" "src/app/(protected)/deck/new-card/page.tsx"` ≥1 AND `grep -c "NewCardModeToggle" "src/app/(protected)/deck/new-card/page.tsx"` ≥1; the Next.js server/client boundary matches the v16 docs read above (page.tsx has no "use client").
    - `npx tsc --noEmit` reports no error in the four touched files.
  </acceptance_criteria>
  <done>The Add-a-Card destination renders ACTop ("‹ My deck") + ACContext (EN→ES · saves to your Spanish deck) + the ACSeg toggle ("Type a word | From an image"), inside the Daybreak page shell; both child flows still mount; D-07 label changed; no behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle translation-form to Daybreak native-first fields + shimmer + ACBanner (ADC-02, D-01, L-02)</name>
  <files>src/components/translation-form.tsx</files>
  <behavior>
    - The Save control is disabled while either field is empty (or while saving) and enabled when both are filled.
    - On a successful save the form shows a "Card saved — add another." banner ABOVE the fields and clears both inputs for the next card.
    - A translate failure shows "Translation unavailable — enter manually." on the receiving field (soft fail; manual entry still allowed).
    - The bidirectional debounced auto-translate still fires (typing one side fills the other) — behavior unchanged from the current reducer.
  </behavior>
  <read_first>
    - src/components/translation-form.tsx (self — preserve the reducer, translateFrom debounce, activeField.current guard, saveCard; the before→after element map is the edit list)
    - src/components/daybreak/t-field.tsx (the field shape ACField-style fields follow) and src/components/daybreak/ac-btn.tsx + src/components/daybreak/ac-banner.tsx (Plan 01 atoms to render)
    - src/components/study-session.tsx lines 1-8 (motion/react import convention for the shimmer pending field)
    - design/handoff-daybreak/daybreak-addcard.jsx lines 144-159 (ACField pending shimmer), 162-170 (ACLinkBadge swap between fields), and daybreak-addcard-boards.jsx ACTypeEmpty/Translating/Errors/Saved
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"translation-form.tsx" (the full before→after transformation map + copy changes + the banner-moves-above-fields note + ACLinkBadge)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"translation-form.tsx" (exact current copy strings + the ACBanner-above-fields layout change)
  </read_first>
  <action>
    Restyle ONLY the JSX surface of `translation-form.tsx` — do NOT touch the reducer, `translateFrom`, the 500ms `use-debounce`, `activeField.current`, or `saveCard`. Apply the PATTERNS.md transformation map:
    Remove the in-form `<Link href="/dashboard">…Back to my deck</Link>` and `<h1>Add a Card</h1>` (they now live in `ACTop` in the toggle wrapper). Keep the form NATIVE-FIRST (D-01): English field first (label `nativeLangLabel`), Spanish field beneath (label `targetLangLabel`) — this is the deliberate type-mode orientation and must NOT be flipped to target-on-top.
    Replace each `<Label>`+`<Input>` pair with an ACField-style field (compose `t-field.tsx` shape or inline the field with the d1 tokens): idle border 1.5px #EDDFC9; value-present border 1.5px #F28A1F; error border 1.5px #DE5F4A. When that field is the auto-translate receiver and a translation is pending, render the pending treatment: amber wash bg #FFF8EC + a small spinner + "Translating…" indicator + a shimmer bar (use `motion/react` animate or Tailwind `animate-pulse` — discretion). Pass the translate error to the receiving field as its error: render "Translation unavailable — enter manually." (em dash; replacing the current "Translation unavailable. Enter manually.").
    Between the two fields render an `ACLinkBadge`-style swap control (a 34px disc, bg #FFF1DC, border 1.5px #F0E3CF, a bidirectional-arrows glyph) — clicking it swaps the two field values and re-triggers translate via the EXISTING dispatch actions (do not add new reducer actions; wire it to the same SET_NATIVE/SET_TARGET the reducer already has). If wiring swap cleanly to existing actions is not possible without a reducer change, render the badge as a non-interactive divider (swap is a mock nicety, not an ADC requirement) — but prefer the working swap if the existing actions support it.
    Replace the Save `<Button className="w-full h-11">` with `<ACBtn kind={canSave ? "primary" : "disabled"} onClick={handleSave}>Save card</ACBtn>` (keep the "Save card" label so the e2e role+name locator survives — L-06; ACBtn is a real <button> from Plan 01). Keep the existing disabled predicate (`!nativeText.trim() || !targetText.trim() || isSaving`).
    Move the success message ABOVE the fields as `<ACBanner kind="ok">Card saved — add another.</ACBanner>` (replacing the current `<p>Card saved.</p>` below the button) and render save errors as `<ACBanner kind="error">{state.saveError}</ACBanner>`. The form clear on success is already in the reducer — preserve it; the banner + cleared fields are the "add another" rhythm.
    Keep the "Save unlocks once both sides are filled." helper as a muted caption (same copy).
  </action>
  <verify>
    <automated>npx vitest run --root . 2>&1 | grep -iE "translation|fail|pass" | head; npx tsc --noEmit 2>&1 | grep -E "translation-form" || echo "tsc clean for translation-form"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Card saved — add another." src/components/translation-form.tsx` returns 1 (new banner copy) AND `grep -c "Card saved\." src/components/translation-form.tsx` returns 0 (old copy gone).
    - `grep -c "Translation unavailable — enter manually." src/components/translation-form.tsx` returns ≥1 (em-dash copy).
    - The save logic is preserved — `grep -c "saveCard" src/components/translation-form.tsx` ≥1 AND `grep -c "/api/translate" src/components/translation-form.tsx` ≥1 AND `grep -c "use-debounce" src/components/translation-form.tsx` ≥1.
    - shadcn primitives removed from the form body — `grep -c "@/components/ui/input" src/components/translation-form.tsx` returns 0 and `grep -c "@/components/ui/button" src/components/translation-form.tsx` returns 0; the Save control is an `ACBtn` (`grep -c "ac-btn" src/components/translation-form.tsx` ≥1).
    - The Save button label "Save card" is still present (e2e survives) — `grep -c "Save card" src/components/translation-form.tsx` ≥1.
    - `npx tsc --noEmit` reports no error in translation-form.tsx; the validated e2e for this behavior runs in Task 3 (no per-file unit test exists for translation-form — its behavior is covered by the e2e in Task 3, which is the Nyquist sample for this surface).
  </acceptance_criteria>
  <done>type-a-word is Daybreak: native-first ACFields with shimmer pending + soft translate-fail, a real-button "Save card" gated until both filled, and a "Card saved — add another." banner above the fields that clears for the next card; all translate/save behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget e2e/04 manual-entry spec + add 'From an image' label regression test (L-06, D-07)</name>
  <files>e2e/04-manual-card-entry.spec.ts</files>
  <read_first>
    - e2e/04-manual-card-entry.spec.ts (lines 13, 17/49/60, 35, 51 — the at-risk locators)
    - .planning/phases/22-add-a-card/22-RESEARCH.md §"e2e Selector Audit" → "e2e/04-manual-card-entry.spec.ts" table (the exact retargets) and §"Validation Architecture" (playwright per-project batches; webServer undefined; dev server on :3000)
    - .planning/phases/22-add-a-card/22-PATTERNS.md §"e2e Selector Retargeting (L-06)" (the retarget map + data-testid additions)
    - .planning/phases/22-add-a-card/22-VALIDATION.md §"Wave 0 Requirements" (the D-07 label regression test + the "Save card" / "Card saved" / "Add a Card" retargets)
  </read_first>
  <action>
    Retarget the L-06 at-risk locators in `e2e/04-manual-card-entry.spec.ts`, PRESERVING each assertion's intent (never re-add old copy, never weaken an assertion — only change the locator/string to match the new Daybreak surface):
    - Line 13 `page.getByText("Add a Card")` → `page.getByTestId("add-card-title")` (the title is now a Baloo 2 display span with that testid — added in Task 1).
    - Lines 17/49/60 `page.getByRole("button", { name: "Save card" })` → keep as-is (ACBtn is a real <button> and the label "Save card" is preserved — confirm it still resolves; no change needed beyond verifying).
    - Line 51 `page.getByText("Card saved")` → `page.getByText(/Card saved/)` (regex tolerates the new "Card saved — add another." copy).
    - Line 35 `page.getByText("Translation unavailable")` → keep as-is (partial match survives the em-dash change).
    Then ADD a new test (D-07 regression guard): navigate to the Add-a-Card page (reuse the spec's existing auth/login helper + nav), assert `page.getByRole("button", { name: "Type a word" })` is visible, click `page.getByRole("button", { name: "From an image" })`, and assert it is visible / pressed — proving the D-07 label is "From an image" and the toggle switches. (Use role+accessible-name so it depends on the ACSeg <button> labels, not literal page text.)
    After edits, grep the WHOLE `e2e/` dir for any remaining `getByText("Add a Card")` and `getByText("Card saved")` exact-string locators and fix stragglers; watch for strict-mode multi-match (scope with the `add-card-title` testid if "Add a Card" renders twice anywhere).
  </action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getByText(\"Add a Card\")" e2e/04-manual-card-entry.spec.ts` returns 0 (retargeted to the testid).
    - `grep -c "From an image" e2e/04-manual-card-entry.spec.ts` ≥1 (the new D-07 regression test exists) and it uses `getByRole("button"` for the toggle (`grep -c "getByRole(\"button\", { name: \"From an image\" })" e2e/04-manual-card-entry.spec.ts` ≥1).
    - `grep -rc "getByText(\"Add a Card\")" e2e/` returns 0 across the whole dir (stragglers fixed) — except where intentionally scoped via testid.
    - The Save-button and "Translation unavailable" assertions are unchanged in intent (reviewer-checkable diff: only locator expressions / the new test differ; no expectation rewritten).
    - NOTE for the wave gate: the full Playwright run happens at the Wave 2 boundary (per VALIDATION sampling) after a fresh `npm run dev` restart — `npx playwright test --project=web e2e/04-manual-card-entry.spec.ts` (then `--project=mobile`). These retargets + the new test are validated there.
  </acceptance_criteria>
  <done>e2e/04 is retargeted to data-testid / role+name with intent preserved, a "From an image" (D-07) regression test is added, and no stale "Add a Card"/"Card saved" exact-string locators remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/translate | Existing auth-gated DeepL endpoint; called from the preserved debounced `translateFrom`. Unchanged this plan (no new params, no new call). |
| client → saveCard server action | Existing card-create action; ownership/validation enforced server-side. Unchanged this plan. |
| client → /deck/new-card RSC | `(protected)/layout.tsx` + `auth.api.getSession` already gate this route; the page shell swap changes only wrapper classes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-T | Tampering | translation-form surface restyle | accept (preserved) | Only JSX changes; the reducer, `/api/translate` call shape, and `saveCard` are untouched. No new input crosses a boundary; the same fields post the same payload. No new attack surface. |
| T-22-02-I | Information Disclosure | page.tsx shell swap | accept (preserved) | Only the outer wrapper `className`s change; the RSC auth gate, `getUserDecks`/`getUserNativeLanguage` ownership scoping, and the 0-deck redirect are unchanged. |
| T-22-02-S | Spoofing | "‹ My deck" Link + toggle | accept | Client-side navigation to an already-protected route; no auth decision moves to the client. |
| T-22-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (`motion/react` already present — RESEARCH "Package Legitimacy Audit": N/A). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages; legitimacy gate N/A. |

No HIGH-severity threats — presentation-only re-skin of the type-a-word surface + page shell. No new network calls, inputs, auth, routes, or schema; `/api/translate` + `saveCard` preserved unchanged.
</threat_model>

<verification>
- `npx tsc --noEmit` clean (whole project) for the touched files.
- Wave-2 boundary: fresh `npm run dev` restart, then `npx playwright test --project=web e2e/04-manual-card-entry.spec.ts` then `--project=mobile e2e/04-manual-card-entry.spec.ts` green (manual-entry flow + the new "From an image" regression test).
- e2e retarget greps clean (no stale `getByText("Add a Card")` / exact `getByText("Card saved")`).
- Manual (phase UAT): the toggle/context/type-a-word states match `daybreak-addcard-boards.jsx` (ACTypeEmpty/Translating/Errors/Saved) — recorded in `22-HUMAN-UAT.md`.
</verification>

<success_criteria>
- ADC-01: single destination with the ACSeg toggle ("Type a word | From an image"), the persistent ACContext line (EN → ES · saves to your Spanish deck), and a working "‹ My deck" escape — inside the Daybreak page shell.
- ADC-02: type-a-word is Daybreak across states (empty/translating/translate-fail/save-fail/saved) with native-first fields (D-01), Save locked until both filled, and the "Card saved — add another." banner + form clear.
- D-07 label changed to "From an image" and guarded by a new e2e test; L-06 retargets done with intent preserved.
- All translate/save behavior preserved (L-02); page.tsx behavior unchanged (RSC, redirect, props).
</success_criteria>

<output>
Create `.planning/phases/22-add-a-card/22-02-SUMMARY.md` when done.
</output>
