---
phase: 24-habitat
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - src/components/habitat-scene.tsx
  - src/components/habitat-video.tsx
  - src/app/(protected)/habitat/page.tsx
  - src/components/study-session.tsx
  - e2e/07-habitat-display.spec.ts
autonomous: true
requirements: [HAB-01, HAB-04, HAB-05]
must_haves:
  truths:
    - "D-01 the page stays a contained 16/9 card — habitat-scene.tsx keeps WRAPPER_STYLE exactly (aspectRatio '16/9', maxHeight 'min(70vh, 400px)', position relative) so the VS4 invariant test stays green; the kept HabitatVideo clip renders inside it, re-skinned overlays around it (NOT full-screen, NOT a flat-geometric rebuild)"
    - "D-02 presentation-only: the data layer (habitat-engine.ts, habitat-queries.ts, /api/habitat, computeHabitatState) is untouched; habitat-scene.tsx consumes level/mood/quality/isDecaying/nextLevelThreshold from HabitatState and the existing decayFilter on the video is left as-is"
    - "D-05 the mood colour tint is a CSS sibling layer (position:absolute; inset:0; pointer-events:none; zIndex 1) above the video and below the chrome, driven by moodTint (golden-hour radial at L9) — composited WITH, never replacing, the video's decayFilter; pointer-events:none so chrome stays clickable (Pitfall 6)"
    - "D-11 the existing tuned decayFilter saturate(q)·brightness(0.6+0.4q) (floor q=0.10) stays on the video unchanged, beneath the new mood tint"
    - "D-06 the kept video clips and the dev-only 3D capture pipeline (src/lib/habitat-3d/*, habitat-3d-canvas.tsx, the ?capture=video dev mount) are left untouched — not deleted, not re-rendered; the Daybreak look comes from the CSS tint + chrome over the existing clips, never from re-baking them"
    - "D-03 three motion tiers: desktop autoplays the looping clip; mobile autoplays then freezes to the still via video.pause() after ~10s and pauses offscreen via IntersectionObserver (D-04, resumes on re-entry); reduced-motion shows the still poster only plus a 'Motion paused' label (data-testid habitat-motion-paused)"
    - "D-07 the level-up celebration auto-settles after ~2.5s on its own (setTimeout 2500 in habitat-scene.tsx) — no tap-to-continue, no tap-to-skip"
    - "D-09 the trigger is repaired: habitat-scene.tsx consumes celebratingLevel (not the dead _celebratingLevel) via a lazy useState initializer that fires on mount; study-session.tsx handleLevelUpDismiss routes to /habitat?celebrate=${leveledUp} so a real level-up surfaces the celebration"
    - "D-13 offline state is a Daybreak cached banner ('You're offline — showing last known state', scene still shown, data-testid habitat-offline-banner) and error is the friendly 'We couldn't load your habitat.' + Try again; the legacy MoodIndicator is removed and replaced by HTop"
    - "D-14 exactly the 8 mock state boards render (new-user L1, mid L5, lush L9, level-up celebration, decaying, offline, error, reduced-motion) — no separate sleeping/napping or night-cycle state on this page"
    - "HAB-04 perf: no motion/react and no zod are introduced on the /habitat route; tint is CLS=0 (absolute, pointer-events:none in the already-sized wrapper); the celebration + tint are SSR-safe (WR-01 — emit deterministic markup, swap after mount, like habitat-video.tsx) so reduced-motion does not flash motion"
    - "T-24-03-CELEBRATE security: page.tsx validates ?celebrate is an integer in [1,9] before passing celebratingLevel down (Number.isInteger && >=1 && <=9), so a crafted ?celebrate=999 yields null and never renders a broken level (STRIDE Tampering — same hardening as Phase 23 WR-01 ?topic=)"
  artifacts:
    - path: "src/components/habitat-scene.tsx"
      provides: "Re-skinned scene: HTop + tint + HProgCard/HDecayCard + Motion-paused + celebration + offline/error reskin + celebratingLevel repair"
      min_lines: 180
    - path: "src/components/habitat-video.tsx"
      provides: "Mobile freeze via videoRef + IntersectionObserver (preserves loop/muted/data-testid)"
      contains: "IntersectionObserver"
    - path: "src/app/(protected)/habitat/page.tsx"
      provides: "?celebrate integer-[1,9] validation"
      contains: "Number.isInteger"
    - path: "src/components/study-session.tsx"
      provides: "handleLevelUpDismiss → /habitat?celebrate=N"
      contains: "/habitat?celebrate="
    - path: "e2e/07-habitat-display.spec.ts"
      provides: "Retargeted to habitat-level-badge / habitat-mood-chip testids"
      contains: "habitat-level-badge"
  key_links:
    - from: "src/app/(protected)/habitat/page.tsx"
      to: "src/components/habitat-scene.tsx"
      via: "celebratingLevel prop (validated)"
      pattern: "celebratingLevel"
    - from: "src/components/study-session.tsx"
      to: "/habitat?celebrate=N"
      via: "router.push in handleLevelUpDismiss"
      pattern: "/habitat\\?celebrate="
    - from: "src/components/habitat-scene.tsx"
      to: "src/components/daybreak/h-top.tsx"
      via: "import { HTop }"
      pattern: "HTop"
---

<objective>
Wire the Wave-1 helpers and Wave-2 atoms into the live habitat surface: re-skin habitat-scene.tsx (HTop chrome, mood tint, bottom progress/decay cards, "Motion paused" label, Daybreak offline/error, repaired celebration trigger), add the mobile-freeze tier to habitat-video.tsx, harden the ?celebrate param in page.tsx, repair the study→/habitat handoff, and retarget the e2e selectors the re-skin breaks.

This is the integration plan — it owns every file that two waves of foundation feed into. It changes ONLY the JSX render tree + the three-line wiring repairs; the engine, /api/habitat, decayFilter, WRAPPER_STYLE, offline-cache/type-guard, and the video's loop/muted/data-testid attributes are preserved verbatim (D-01/D-02/D-11). The existing level-up-overlay.tsx (motion/react) is NOT imported here — the CSS-only HabitatCelebration from Plan 02 is used instead.

Purpose: deliver the 8 Daybreak state boards + repaired level-up + 3-tier motion on a single contained card.
Output: 4 source edits + 1 e2e retarget; full unit + habitat e2e green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-habitat/24-PATTERNS.md
@.planning/phases/24-habitat/24-RESEARCH.md
@.planning/phases/24-habitat/24-VALIDATION.md

@src/components/habitat-scene.tsx
@src/components/habitat-video.tsx
@src/app/(protected)/habitat/page.tsx

<interfaces>
<!-- Wave-1/2 contracts this plan consumes (already shipped): -->
From src/components/daybreak/h-top.tsx:        export function HTop({ mood, level }: { mood: TigerMood; level: number }): JSX.Element
From src/components/daybreak/h-prog-card.tsx:   export function HProgCard({ level, effectiveCardCount, nextLevelThreshold }): JSX.Element
From src/components/daybreak/h-decay-card.tsx:  export function HDecayCard(): JSX.Element
From src/components/daybreak/h-back.tsx:        export function HBack(): JSX.Element   // for the error-state header
From src/components/habitat-celebration.tsx:    export function HabitatCelebration({ celebratingLevel, onSettle }): JSX.Element
From src/lib/habitat-tint.ts:                   export function moodTint(mood, isDecaying, level): string
From src/hooks/use-prefers-reduced-motion.ts:   export function usePrefersReducedMotion(): boolean

<!-- Existing seams to repair (verified line refs): -->
habitat-scene.tsx line 113: `celebratingLevel: _celebratingLevel = null` → consume it (D-09)
habitat-scene.tsx line 94: MoodIndicator() → remove, replace with HTop
habitat-scene.tsx lines 51-55: WRAPPER_STYLE → keep VERBATIM (VS4 test pins 16/9 + min(70vh,400px))
habitat-video.tsx: <video> tag carries data-testid="habitat-video", loop, muted, playsInline — preserve; add ref={videoRef}
study-session.tsx lines 214-220: handleLevelUpDismiss gates leveledUp===10 → /dashboard; repair to /habitat?celebrate=${leveledUp}, drop deckId from deps
page.tsx line 22: const celebratingLevel = Number(params.celebrate) ... → add integer-[1,9] guard

<!-- e2e retargets in 07-habitat-display.spec.ts: -->
line 29: getByText("Level")  → getByTestId("habitat-level-badge")
lines 39, 59: getByText(/Excited|Happy|Neutral|Sad/) → getByTestId("habitat-mood-chip")
line 26: page.goto(href!) → page.goto(href ?? "")   (biome noNonNullAssertion — fix while editing this file)
KEEP unchanged: line 10 getByText(/Level 1/) + line 14 getByText(/\d+ of \d+ cards/) (dashboard widget) + line 51 [aria-label="Loading habitat"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: page.tsx ?celebrate=[1,9] validation + study-session →/habitat handoff (T-24-03-CELEBRATE)</name>
  <files>src/app/(protected)/habitat/page.tsx, src/components/study-session.tsx</files>
  <behavior>
    - page.tsx: ?celebrate=5 → celebratingLevel 5; ?celebrate=999 / 0 / "abc" / "" / absent → null (Number.isInteger && >=1 && <=9).
    - study-session.tsx: handleLevelUpDismiss with a non-null leveledUp routes to /habitat?celebrate=${leveledUp}; "Back to deck" still → /dashboard.
  </behavior>
  <read_first>
    - .planning/phases/24-habitat/24-RESEARCH.md § Security Domain (the exact integer-[1,9] guard for ?celebrate) and § Pattern 3 (study-session handleLevelUpDismiss repair, lines 306-313)
    - .planning/phases/24-habitat/24-PATTERNS.md § page.tsx (validation snippet) and § study-session.tsx (one-line routing change)
    - src/app/(protected)/habitat/page.tsx (the server shell — line 22 Number(params.celebrate) is the seam)
    - src/components/study-session.tsx (handleLevelUpDismiss lines 214-220; "Back to deck" line ~386 stays /dashboard)
  </read_first>
  <action>
    In src/app/(protected)/habitat/page.tsx, replace the raw `Number(params.celebrate)` assignment with a validated one: parse `rawCelebrate = params.celebrate ? Number(params.celebrate) : null`, then `celebratingLevel = (rawCelebrate !== null && Number.isInteger(rawCelebrate) && rawCelebrate >= 1 && rawCelebrate <= 9) ? rawCelebrate : null`. Leave auth/getHabitatFacts/computeHabitatState/readHabitatOverride/the HabitatScene render unchanged (D-02). In src/components/study-session.tsx, change handleLevelUpDismiss so that when `leveledUp !== null` it `router.push(\`/habitat?celebrate=${leveledUp}\`)` (replacing the leveledUp===10 → /dashboard?...&celebrate=10 branch), and remove deckId from the useCallback deps since it is no longer referenced inside. Do NOT change the "Back to deck" button (~line 386) — it stays /dashboard?deck=${deckId}. Use template-literal interpolation; no `!`.
  </action>
  <acceptance_criteria>
    - page.tsx contains `Number.isInteger(` and a `>= 1` and `<= 9` bound on the celebrate value (grep all three); it no longer assigns celebratingLevel directly from an unguarded Number().
    - study-session.tsx contains `/habitat?celebrate=` (grep) and handleLevelUpDismiss's deps array no longer lists deckId (manual review — confirm `}, [showLevelUp, router]` not `..., deckId]`).
    - `npx vitest run src/components/__tests__/habitat-scene-video.test.ts` — the celebratingLevel-consumed assertion still requires Task 2; this task is verified by tsc + the e2e celebration spec at the wave gate.
    - `npx tsc --noEmit` passes; `npx biome ci "src/app/(protected)/habitat/page.tsx" src/components/study-session.tsx` reports no errors (no `!`).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx biome ci "src/app/(protected)/habitat/page.tsx" src/components/study-session.tsx</automated>
  </verify>
  <done>?celebrate hardened to integer [1,9] in page.tsx; study-session handleLevelUpDismiss routes to /habitat?celebrate=N with deckId dropped from deps; "Back to deck" unchanged; tsc + scoped biome clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: habitat-scene.tsx re-skin + celebration trigger repair + bottom cards + tint + offline/error</name>
  <files>src/components/habitat-scene.tsx</files>
  <behavior>
    - habitat-scene-video test: habitat-scene.tsx consumes `celebratingLevel` (not `_celebratingLevel`) and contains `setTimeout(... 2500` (D-07); VS4 16/9 wrapper invariant still passes.
    - habitat-celebration test (cross-file part): habitat-scene.tsx's 2500ms timer present.
  </behavior>
  <read_first>
    - .planning/phases/24-habitat/24-PATTERNS.md § habitat-scene.tsx (PRIMARY EDIT SURFACE — exact imports, the lazy-useState celebration repair, the moodTint layer markup with pointer-events:none/zIndex, the offline-banner reskin, the error-state reskin with HBack, the "Motion paused" label, the bottom-card switch, and the explicit "Remove MoodIndicator/MOOD_LABELS/MOOD_DOT_CLASSES" note)
    - .planning/phases/24-habitat/24-RESEARCH.md § Pattern 3 (celebration mount-trigger + 2.5s settle) and § Pitfall 4/6 (SSR-safe tint, pointer-events:none) and § Code Examples (HabitatScene wiring)
    - design/handoff-daybreak/daybreak-habitat.jsx (HabScreen line 86-94 "Motion paused", HabDecay 113-125, HabOffline 128-136, HabError 145-158, HabCelebrate 162-186 — the 8 boards D-14 must match)
    - src/components/habitat-scene.tsx (the file itself: WRAPPER_STYLE 51-55 KEEP; _celebratingLevel 113; MoodIndicator 90-101 REMOVE; offline 253; error 195; showLevelUp 121/260 dead → replace; CACHE_KEY/isValidHabitatState/retry KEEP verbatim)
    - src/components/__tests__/habitat-scene-video.test.ts (VS4 pins the 16/9 wrapper strings — must remain)
  </read_first>
  <action>
    Re-skin src/components/habitat-scene.tsx (keep "use client"). Add imports: HTop, HProgCard, HDecayCard, HBack (from src/components/daybreak/*), HabitatCelebration, usePrefersReducedMotion, moodTint (from src/lib/habitat-tint). REPAIR the celebration trigger (D-09): change the prop from `_celebratingLevel` to `celebratingLevel`, add `const [showCelebration, setShowCelebration] = useState(() => celebratingLevel != null && celebratingLevel > 0)` and a mount-only `useEffect` that `setTimeout(() => setShowCelebration(false), 2500)` and clears it on cleanup (D-07). Keep WRAPPER_STYLE verbatim (VS4). Inside the wrapper div, AFTER <HabitatVideo> and BEFORE the chrome, add the mood tint layer: an aria-hidden div, position:absolute; inset:0; pointer-events:none; zIndex:1, background = the L9 golden-hour radial when level>=9 && !isDecaying else moodTint(state.mood, state.isDecaying, state.level) (D-05; composited over the untouched decayFilter, D-11). Replace MoodIndicator with `<HTop mood={state.mood} level={state.level} />` and DELETE the MoodIndicator function + MOOD_LABELS + MOOD_DOT_CLASSES. Re-skin the offline banner (data-testid="habitat-offline-banner", "You're offline — showing last known state", scene still shown) and the error state (friendly "We couldn't load your habitat." + a Button onClick={retry} "Try again", with an HBack in the error header) per the handoff. Add the "Motion paused" label (data-testid="habitat-motion-paused") rendered when `reducedMotion` (HabScreen line 94). Replace the dead showLevelUp text overlay with the bottom-card switch: `state.isDecaying ? <HDecayCard /> : <HProgCard level effectiveCardCount nextLevelThreshold />` (D-10), and render `{showCelebration && celebratingLevel !== null && <HabitatCelebration celebratingLevel={celebratingLevel} onSettle={() => setShowCelebration(false)} />}`. Keep CACHE_KEY/isValidHabitatState/retry/captureMode verbatim. Do NOT import level-up-overlay.tsx or motion/react (Pitfall 1). Use `?.`/guards, never `!`. The result must render exactly the 8 D-14 boards and no night/sleep state.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/habitat-scene-video.test.ts src/components/__tests__/habitat-celebration.test.ts` PASSES green (VS4 16/9 wrapper intact; celebratingLevel consumed; setTimeout 2500 present).
    - habitat-scene.tsx contains `celebratingLevel` and NOT `_celebratingLevel` (grep: `grep -n "_celebratingLevel" src/components/habitat-scene.tsx` is empty); contains `setTimeout(` with `2500`; contains `pointerEvents: "none"` on the tint layer; contains data-testids habitat-offline-banner and habitat-motion-paused.
    - habitat-scene.tsx imports neither `motion/react` nor `level-up-overlay` (`grep -n "motion/react\|level-up-overlay" src/components/habitat-scene.tsx` is empty); the MoodIndicator function is removed (`grep -n "function MoodIndicator" src/components/habitat-scene.tsx` is empty).
    - WRAPPER_STYLE still matches `aspectRatio: "16/9"` and `maxHeight: "min(70vh, 400px)"` (the VS4 test enforces this — must stay green).
    - `npx tsc --noEmit` passes; `npx biome ci src/components/habitat-scene.tsx` reports no errors (no `!`).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/habitat-scene-video.test.ts src/components/__tests__/habitat-celebration.test.ts && npx biome ci src/components/habitat-scene.tsx</automated>
  </verify>
  <done>Scene re-skinned: HTop chrome, CSS mood tint (pointer-events:none over untouched decayFilter), HProgCard/HDecayCard switch, Motion-paused label, Daybreak offline/error, repaired celebratingLevel trigger with 2.5s auto-settle; MoodIndicator removed; no motion/react; VS4 + celebration tests green; tsc + scoped biome clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: habitat-video.tsx mobile-freeze tier + e2e/07 retarget</name>
  <files>src/components/habitat-video.tsx, e2e/07-habitat-display.spec.ts</files>
  <behavior>
    - habitat-video test (extended in Plan 01): habitat-video.tsx contains the mobile-freeze logic — IntersectionObserver, window.innerWidth < 768, video.pause(), a setTimeout freeze, and video.play().catch — and is gated to skip on desktop and under reducedMotion; the <video> still carries loop + muted + data-testid="habitat-video".
    - e2e/07: the habitat page asserts on getByTestId("habitat-level-badge") and getByTestId("habitat-mood-chip").
  </behavior>
  <read_first>
    - .planning/phases/24-habitat/24-PATTERNS.md § habitat-video.tsx (MINIMAL EDIT — the exact videoRef + IntersectionObserver useEffect, ref wiring, and the "all existing exports unchanged" note) and § e2e/07-habitat-display.spec.ts (the retarget table)
    - .planning/phases/24-habitat/24-RESEARCH.md § Pattern 2 (SSR-safe mobile freeze, pause-is-the-freeze rationale, 10s timer) and § Pitfall 3 (play() Promise .catch) and § e2e Impact Audit
    - src/components/habitat-video.tsx (the file: existing reducedMotion const, the <video> tag with data-testid/loop/muted/playsInline, and the exported pure helpers clampLevel/clipBasename/posterSrc/webmSrc/mp4Src/decayFilter that tests import)
    - e2e/07-habitat-display.spec.ts (lines 26/29/39/59 are the retarget sites; lines 10/14/51 stay)
    - e2e/13-habitat-3d.spec.ts + e2e/13-habitat-states.spec.ts (DO NOT edit — they assert v.loop===true, v.muted===true, and the clip source regex; this task must not break those: the freeze pauses via JS but keeps the loop/muted attributes)
  </read_first>
  <action>
    In src/components/habitat-video.tsx, add `const videoRef = useRef<HTMLVideoElement>(null)` and a `useEffect([reducedMotion])` implementing the mobile-freeze tier (D-03/D-04): return early if `!videoRef.current || reducedMotion`; compute `isMobile = typeof window !== "undefined" && window.innerWidth < 768` and return early if not mobile (desktop keeps looping); otherwise set up a `scheduleFreeze` that `setTimeout(() => video.pause(), 10_000)` (the ~2-loop tuning knob) and an IntersectionObserver(threshold 0.1) that on isIntersecting calls `video.play().catch(() => {})` then re-schedules the freeze, and on leaving clears the timer and `video.pause()`; observe the video; cleanup disconnects the observer and clears the timer. Wire `ref={videoRef}` onto the existing <video> element WITHOUT changing its data-testid="habitat-video", loop, muted, or playsInline attributes, and leave the reduced-motion still-poster branch and all exported helpers untouched. Use `?.`, never `!`. Then retarget e2e/07-habitat-display.spec.ts: line 29 `getByText("Level")` → `getByTestId("habitat-level-badge")`; lines 39 and 59 `getByText(/Excited|Happy|Neutral|Sad/)` → `getByTestId("habitat-mood-chip")`; line 26 `page.goto(href!)` → `page.goto(href ?? "")` (biome noNonNullAssertion). Keep line 10 (Level 1 dashboard widget), line 14 (N of M cards), and line 51 ([aria-label="Loading habitat"]) unchanged — never re-add stale "Level"/mood copy to satisfy a string, and do not introduce a strict-mode multi-match (the testids are unique).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/habitat-video.test.ts` PASSES green (mobile-freeze logic detected; helpers + loop/muted intact).
    - habitat-video.tsx contains `IntersectionObserver`, `window.innerWidth < 768`, `.pause()`, `.play().catch(`, and `ref={videoRef}` (grep all); it still contains `data-testid="habitat-video"`, `loop`, and `muted` on the video (grep).
    - e2e/07-habitat-display.spec.ts contains `getByTestId("habitat-level-badge")` and `getByTestId("habitat-mood-chip")` and no longer contains `getByText("Level")` or `href!` (grep: `grep -n "getByText(\"Level\")\|href!" e2e/07-habitat-display.spec.ts` is empty); lines for /Level 1/ and "Loading habitat" remain.
    - habitat-video.tsx contains no `!` non-null assertion and no `import.*motion/react`; `npx tsc --noEmit` passes.
    - `npx biome ci src/components/habitat-video.tsx e2e/07-habitat-display.spec.ts` reports no errors.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/habitat-video.test.ts && npx tsc --noEmit && npx biome ci src/components/habitat-video.tsx e2e/07-habitat-display.spec.ts</automated>
  </verify>
  <done>Mobile-freeze tier added to habitat-video.tsx (IntersectionObserver + 10s pause, desktop/reduced-motion skipped, play().catch), loop/muted/data-testid preserved; e2e/07 retargeted to the new testids + href!→href??"" fixed; 13-* specs untouched and still valid; habitat-video unit green; tsc + scoped biome clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → page.tsx server shell | `?celebrate=N` query param is untrusted user input crossing into the RSC render |
| study-session client → /habitat URL | the celebrate value the study flow constructs is bounded by the engine (leveledUp ≤ 9) but is re-validated server-side at page.tsx |
| HabitatState (server) → scene client | trusted server-computed mood/level/quality/isDecaying; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-CELEBRATE | Tampering | `?celebrate=N` in src/app/(protected)/habitat/page.tsx | mitigate | Validate integer in [1,9] (`Number.isInteger(rawCelebrate) && rawCelebrate >= 1 && rawCelebrate <= 9`) BEFORE passing celebratingLevel to HabitatScene; a crafted `?celebrate=999`/`0`/`abc` resolves to null and renders no broken level (ASVS V5; same pattern as Phase 23 WR-01 `?topic=`) |
| T-24-03-OPENREDIRECT | Tampering | study-session router.push(`/habitat?celebrate=${leveledUp}`) | mitigate | leveledUp originates from the trusted study-complete engine response (≤ 9), interpolated into a fixed internal path; the destination still re-validates at page.tsx — no open-redirect, no external host |
| T-24-03-PLAY | Denial of Service | habitat-video.tsx video.play() Promise | mitigate | Always `.catch(() => {})` on play() (Pitfall 3) so a mobile autoplay-policy rejection cannot throw an unhandled rejection / trip an error boundary |
| T-24-03-CLS | Tampering (UX integrity) | mood tint layer in habitat-scene.tsx | mitigate | Tint is position:absolute; inset:0; pointer-events:none; zIndex:1 inside the already-sized WRAPPER_STYLE — CLS=0, and pointer-events:none keeps the chrome clickable (Pitfall 6); the 13-perf.spec CLS≤0.1 gate must stay green |
| T-24-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH § Package Legitimacy Audit) — nothing to verify |

Auth/session/access-control are unchanged — the /habitat route remains behind (protected)/layout.tsx; no new endpoint, no new data fetch (D-02). V4 access-control is N-A for this presentation-only re-skin.
</threat_model>

<verification>
- Full unit suite green: `npx vitest run` (all habitat specs: names, tint, prog-card, celebration, video, scene-video).
- Habitat e2e green per-project (fresh harness-managed dev server): `npx playwright test e2e/07-habitat-display.spec.ts e2e/13-habitat-3d.spec.ts e2e/13-habitat-states.spec.ts e2e/24-habitat-celebration.spec.ts` (run --project=web then --project=mobile).
- 13-perf.spec.ts is NOT edited but must remain green at the wave gate (CLS ≤ 0.1 on /habitat desktop+mobile — the tint is CLS=0; the mobile freeze only reduces work). 13-habitat-3d / 13-habitat-states assert loop/muted/clip-src and must stay green (freeze preserves those attributes).
- `npx tsc --noEmit` passes AFTER the e2e wave (a biome `?.` rewrite can push a `box?.y`-style value into a number arg → tsc error; run full tsc last).
- `npx biome ci src/components/habitat-scene.tsx src/components/habitat-video.tsx "src/app/(protected)/habitat/page.tsx" src/components/study-session.tsx e2e/07-habitat-display.spec.ts` reports no errors (scoped to touched files — never the whole repo; the repo has ~429 pre-existing biome errors and is never clean).
- No file on the /habitat route imports motion/react or zod; no `!` non-null assertions introduced.
</verification>

<success_criteria>
- The /habitat page renders the 8 Daybreak state boards on a contained 16/9 card: HTop chrome, CSS mood tint over the kept clip, HProgCard/HDecayCard, "Motion paused" under reduced-motion, repaired level-up celebration (auto-settles ~2.5s), Daybreak offline + error.
- 3-tier motion works: desktop loops, mobile freezes ~10s + pauses offscreen, reduced-motion is the still poster.
- ?celebrate is hardened to [1,9]; study→/habitat handoff surfaces a real level-up; e2e/07 retargeted to stable testids; 13-* + 13-perf stay green.
- Perf/SSR invariants held: no motion/react or zod, CLS=0 tint, WR-01 SSR-safe overlays, no `!`.
</success_criteria>

<output>
Create `.planning/phases/24-habitat/24-03-SUMMARY.md` when done
</output>
