---
phase: 24-habitat
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/components/daybreak/h-back.tsx
  - src/components/daybreak/h-mood-chip.tsx
  - src/components/daybreak/h-level-badge.tsx
  - src/components/daybreak/h-top.tsx
  - src/components/daybreak/h-prog-card.tsx
  - src/components/daybreak/h-decay-card.tsx
  - src/components/habitat-celebration.tsx
autonomous: true
requirements: [HAB-02, HAB-03, HAB-05]
must_haves:
  truths:
    - "D-13 the floating chrome is re-skinned to the Daybreak mock exactly via new atoms: HBack (top-left circular frosted button → /dashboard), HMoodChip (frosted pill, coloured dot + label, 4 moods), HLevelBadge (round LVL pill, gold #F2B33A at L9 via level>=9 not ===9), composed by HTop; offline/error/progress copy still sourced from H_NAME/H_NEXT"
    - "D-03 the bottom progress card HProgCard shows 'Level N · {H_NAME}', an amber progress-bar fill at pct%, and 'Next at L{at}: {what}' from H_NEXT for levels 1–8"
    - "D-12 at L9 (nextLevelThreshold === null) HProgCard hides the percentage/bar and reads 'Course 1 complete — you grew the whole world.' — never the mock's dead level>=10 branch; HLevelBadge renders gold at L9"
    - "D-10 HDecayCard renders the encouraging 'Leo misses you' / 'A quick session brings the world back to life.' copy + a 'Study now' primary that routes into the study flow (→ /dashboard, since HabitatState carries no deckId — RESEARCH Pattern 4b)"
    - "D-08 the celebration content is falling confetti + a big 'Level N' display + the what-appeared reveal sourced from H_NEXT (e.g. 'An elephant moved in!'); at L9 it shows the 'Course 1 complete' line instead (no H_NEXT[9])"
    - "D-14 only the 4 engine moods (Excited/Happy/Neutral/Sad) are rendered in HMoodChip — no sleeping/napping/night state board is added; HDecayCard is the decaying board, not a night-cycle"
    - "Perf is preserved: habitat-celebration.tsx is a NEW CSS-only component using the @keyframes hab-fall class — it does NOT import motion/react (which the existing level-up-overlay.tsx uses and which was stripped from /habitat in Phase 13.1); confetti is gated on !reducedMotion (WR-01 SSR-safe, swap-after-mount like habitat-video.tsx)"
  artifacts:
    - path: "src/components/daybreak/h-mood-chip.tsx"
      provides: "HMoodChip atom, 4-mood frosted pill"
      contains: "habitat-mood-chip"
    - path: "src/components/daybreak/h-level-badge.tsx"
      provides: "HLevelBadge atom, gold at L9"
      contains: "habitat-level-badge"
    - path: "src/components/daybreak/h-prog-card.tsx"
      provides: "HProgCard — pct bar + H_NEXT unlock + L9 Course-1-complete"
      contains: "habitat-prog-card"
    - path: "src/components/daybreak/h-decay-card.tsx"
      provides: "HDecayCard — Leo misses you + Study now"
      contains: "habitat-decay-card"
    - path: "src/components/habitat-celebration.tsx"
      provides: "CSS-only celebration overlay (confetti + Level N + reveal)"
      contains: "habitat-celebration"
  key_links:
    - from: "src/components/daybreak/h-prog-card.tsx"
      to: "src/lib/habitat-names.ts"
      via: "import { H_NAME, H_NEXT }"
      pattern: "H_NEXT"
    - from: "src/components/habitat-celebration.tsx"
      to: "src/hooks/use-prefers-reduced-motion.ts"
      via: "usePrefersReducedMotion gate on confetti"
      pattern: "usePrefersReducedMotion"
---

<objective>
Build the Daybreak presentational atoms the scene will compose in Wave 3: the chrome (HBack/HMoodChip/HLevelBadge/HTop), the two bottom cards (HProgCard, HDecayCard), and the CSS-only celebration overlay (HabitatCelebration). All consume the Wave-1 constants/state shape; none touch the scene wiring or domain logic (D-02).

These are pure props-to-markup atoms in the established Daybreak style: inline `style={{}}` objects (never Tailwind classes), no "use client" except where a hook is used (only habitat-celebration.tsx needs it for usePrefersReducedMotion). Every atom carries the `data-testid` the e2e retargets and unit greps expect. The celebration is a NEW file (RESEARCH Pitfall 1) — it must NOT reuse level-up-overlay.tsx, which imports motion/react (verified line 3) and is forbidden on the /habitat route.

Purpose: ship the visual contract before the integration plan, so Wave 3 is pure wiring.
Output: 6 Daybreak atoms + 1 celebration component, all unit/grep-green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-habitat/24-PATTERNS.md
@.planning/phases/24-habitat/24-RESEARCH.md

@src/lib/habitat-names.ts
@src/components/daybreak/ac-top.tsx
@src/components/daybreak/lang-chip.tsx

<interfaces>
<!-- Wave-1 contracts this plan consumes (already shipped by 24-01): -->
From src/lib/habitat-names.ts:
  export const H_NAME: Record<number, string>;                       // keys 1-9
  export const H_NEXT: Record<number, { at: number; what: string }>; // keys 1-8 ONLY (guard L9)
From src/lib/habitat-engine.ts (DO NOT edit — D-02):
  export type TigerMood = "excited" | "happy" | "neutral" | "sad";
From src/hooks/use-prefers-reduced-motion.ts:
  export function usePrefersReducedMotion(): boolean;  // defaults false on SSR (WR-01)

<!-- data-testids these atoms MUST emit (consumed by e2e + unit grep in Plans 01/03): -->
  HBack → habitat-back-btn
  HMoodChip → habitat-mood-chip
  HLevelBadge → habitat-level-badge
  HProgCard → habitat-prog-card
  HDecayCard → habitat-decay-card
  HabitatCelebration → habitat-celebration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chrome atoms — HBack, HMoodChip, HLevelBadge, HTop</name>
  <files>src/components/daybreak/h-back.tsx, src/components/daybreak/h-mood-chip.tsx, src/components/daybreak/h-level-badge.tsx, src/components/daybreak/h-top.tsx</files>
  <read_first>
    - .planning/phases/24-habitat/24-PATTERNS.md § h-back.tsx, § h-mood-chip.tsx, § h-level-badge.tsx, § h-top.tsx (each gives the exact inline-style markup + testid + the MOOD_CONFIG table)
    - design/handoff-daybreak/daybreak-habitat.jsx lines 16-19 (HBack), 21-29 (HMoodChip + MOOD lines 11-13), 30-37 (HLevelBadge), 39-47 (HTop) — pixel/behavioural source of truth
    - src/components/daybreak/ac-top.tsx (three-slot flex-row analog + next/link usage, RSC-safe, inline styles)
    - src/components/daybreak/lang-chip.tsx (frosted chip atom convention)
    - src/components/habitat-medallion.tsx (existing Daybreak level-badge styling for visual consistency)
  </read_first>
  <action>
    Create four atoms in src/components/daybreak/ (inline styles only, no Tailwind class, no "use client" — all RSC-safe). HBack: a next/link href="/dashboard", data-testid="habitat-back-btn", aria-label="Back to Dashboard", 40px circular rgba(255,255,255,0.86) frosted button with backdropFilter blur(4px), a CSS-border chevron (no emoji/SVG). HMoodChip({ mood }): import type TigerMood from "@/lib/habitat-engine"; a MOOD_CONFIG record mapping the 4 moods to {label, color} (excited #F2B33A / happy #3E9B5F / neutral #B7A98F / sad #7C93B0); render a 36px frosted pill, data-testid="habitat-mood-chip", a coloured dot + the label; use `cfg?.color ?? "#B7A98F"` and `cfg?.label ?? mood` (no `!`). HLevelBadge({ level }): a 46px round pill, data-testid="habitat-level-badge", background gold #F2B33A when level >= 9 else #F28A1F (D-12: >=9 not ===9), "LVL" micro-label over the number in var(--font-display). HTop({ mood, level }): a relative z-index:3 flex row, space-between, rendering HBack | HMoodChip | HLevelBadge. Match the handoff dimensions/colours exactly.
  </action>
  <acceptance_criteria>
    - All four files exist; `grep -l "habitat-back-btn" src/components/daybreak/h-back.tsx`, `grep -l "habitat-mood-chip" src/components/daybreak/h-mood-chip.tsx`, `grep -l "habitat-level-badge" src/components/daybreak/h-level-badge.tsx` each match.
    - HLevelBadge gates gold on `level >= 9` (grep: `grep -n "level >= 9\|level>=9\|isMax" src/components/daybreak/h-level-badge.tsx` shows the >= form, not `=== 9`).
    - None of the four contain `import.*motion/react` or a `!` non-null assertion (`grep -rn "motion/react" src/components/daybreak/h-back.tsx src/components/daybreak/h-mood-chip.tsx src/components/daybreak/h-level-badge.tsx src/components/daybreak/h-top.tsx` is empty; biome enforces no-`!`).
    - h-back.tsx/h-mood-chip.tsx/h-level-badge.tsx/h-top.tsx have NO "use client" line (`grep -L "use client" ...` lists all four — they are RSC-safe).
    - `npx tsc --noEmit` passes; `npx biome ci src/components/daybreak/h-back.tsx src/components/daybreak/h-mood-chip.tsx src/components/daybreak/h-level-badge.tsx src/components/daybreak/h-top.tsx` reports no errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx biome ci src/components/daybreak/h-back.tsx src/components/daybreak/h-mood-chip.tsx src/components/daybreak/h-level-badge.tsx src/components/daybreak/h-top.tsx</automated>
  </verify>
  <done>HBack/HMoodChip/HLevelBadge/HTop created with correct testids and gold-at-L9 logic; RSC-safe, inline-style, no motion/react, no `!`; tsc + scoped biome clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bottom cards — HProgCard (GREEN prog-card test) + HDecayCard</name>
  <files>src/components/daybreak/h-prog-card.tsx, src/components/daybreak/h-decay-card.tsx</files>
  <behavior>
    - habitat-prog-card test (from Plan 01): h-prog-card.tsx references effectiveCardCount / nextLevelThreshold with Math.round + Math.min (pct capped 100), uses nextLevelThreshold === null as the max signal, and contains the "Course 1 complete" copy on that branch.
    - For levels 1–8 a percentage + amber bar + "Next at L{at}: {what}" render; at L9 they do not.
  </behavior>
  <read_first>
    - .planning/phases/24-habitat/24-PATTERNS.md § h-prog-card.tsx (full markup incl. pct derivation + L9 branch + generic icon disc) and § h-decay-card.tsx (Leo-misses-you card + Study-now → /dashboard)
    - .planning/phases/24-habitat/24-RESEARCH.md § Pattern 4 (pct = Math.min(100, Math.round(effectiveCardCount/nextLevelThreshold*100))) and § Pattern 4b ("Study now" → /dashboard rationale)
    - design/handoff-daybreak/daybreak-habitat.jsx lines 50-73 (HProgCard) and HabDecay lines 113-125
    - src/components/habitat-hero.tsx (Phase 21 progress-math analog for the same threshold)
    - src/components/daybreak/ac-banner.tsx (status-card pattern analog for HDecayCard)
    - src/lib/habitat-names.ts (H_NAME / H_NEXT — already shipped by 24-01)
  </read_first>
  <action>
    Create src/components/daybreak/h-prog-card.tsx (no "use client", import { H_NAME, H_NEXT }): props { level, effectiveCardCount, nextLevelThreshold: number | null }; compute `const isMax = nextLevelThreshold === null; const pct = isMax ? 100 : Math.min(100, Math.round((effectiveCardCount / nextLevelThreshold) * 100));` and `const nx = level < 9 ? H_NEXT[level] : null` (Pitfall 5 guard — never index H_NEXT[9]). data-testid="habitat-prog-card", a frosted rounded card: title "Level {level} · {H_NAME[level]}", and when nx: a "{pct}% to L{level+1}" label + an amber-gradient bar at width pct% + a generic icon disc with "Next at L{nx.at}: {nx.what}"; when !nx (L9): the "✨ Course 1 complete — you grew the whole world." line (D-12). Create src/components/daybreak/h-decay-card.tsx (no "use client", import next/link): data-testid="habitat-decay-card", a sad-Leo disc (CSS dot, no emoji), "Leo misses you" (display font) + "A quick session brings the world back to life." + a 48px amber "Study now" Link href="/dashboard" (D-10 / Pattern 4b — HabitatState has no deckId). Inline styles only; use `?.`/guards, never `!`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/habitat-prog-card.test.ts` PASSES green.
    - h-prog-card.tsx contains `nextLevelThreshold === null` and the literal "Course 1 complete" (grep both); it never bare-indexes `H_NEXT[level]` without the `level < 9` guard (grep: `grep -n "H_NEXT\[" src/components/daybreak/h-prog-card.tsx` and confirm the guard precedes it).
    - h-decay-card.tsx contains `href="/dashboard"` and the strings "Leo misses you" and "Study now" (grep all three).
    - Neither card has "use client" or `import.*motion/react`; `npx tsc --noEmit` passes.
    - `npx biome ci src/components/daybreak/h-prog-card.tsx src/components/daybreak/h-decay-card.tsx` reports no errors.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/habitat-prog-card.test.ts && npx biome ci src/components/daybreak/h-prog-card.tsx src/components/daybreak/h-decay-card.tsx</automated>
  </verify>
  <done>HProgCard GREEN against its spec (pct + L9 Course-1-complete + guarded H_NEXT); HDecayCard renders Leo-misses-you + Study-now→/dashboard; both RSC-safe inline-style, no motion/react, no `!`; tsc + scoped biome clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CSS-only HabitatCelebration overlay (GREEN celebration test)</name>
  <files>src/components/habitat-celebration.tsx</files>
  <behavior>
    - habitat-celebration test (from Plan 01): imports do NOT match motion/react or framer-motion; confetti divs use the hab-confetti class and are gated on !reducedMotion; data-testid="habitat-celebration" present.
    - Reveal copy comes from H_NEXT for levels 1–8; L9 shows "Course 1 complete"; reduced-motion path renders no falling confetti (D-08 static fallback).
  </behavior>
  <read_first>
    - .planning/phases/24-habitat/24-PATTERNS.md § habitat-celebration.tsx (full CSS-only markup, CONFETTI_COLORS/COUNT, hab-confetti class, what-appeared reveal, L9 branch, SSR-safety note)
    - .planning/phases/24-habitat/24-RESEARCH.md § Pattern 3 (trigger semantics — celebration shows on mount, parent owns the 2.5s timer) and § Pitfall 1 + Pitfall 4 (no motion/react; WR-01 SSR flash acceptable, swap-after-mount)
    - design/handoff-daybreak/daybreak-habitat.jsx HabCelebrate lines 162-186 (confetti + Level N + reveal)
    - src/components/level-up-overlay.tsx (STRUCTURE reference ONLY — line 3 imports motion/react; do NOT copy any motion.div — it is forbidden on /habitat)
    - src/hooks/use-prefers-reduced-motion.ts (the SSR-safe gate to reuse)
    - src/app/globals.css (the @keyframes hab-fall + .hab-confetti reduced-motion override added in 24-01)
  </read_first>
  <action>
    Create src/components/habitat-celebration.tsx with "use client" (it uses usePrefersReducedMotion). Props { celebratingLevel: number; onSettle: () => void } — the auto-settle timer is owned by the parent scene (Plan 03), this component just renders. Import { H_NAME, H_NEXT } and usePrefersReducedMotion. Define CONFETTI_COLORS (the 5-colour palette matching level-up-overlay.tsx lines 11-12 for cross-flow consistency) and CONFETTI_COUNT = 26. Render an absolute inset-0 z-20 overlay, data-testid="habitat-celebration", pointer-events:none: a "Level up!" eyebrow + a 62px "Level {celebratingLevel}" + the level name; when !reducedMotion, a confetti layer of 26 absolutely-positioned divs each className="hab-confetti" with `animation: hab-fall 2.5s ease-in {delay}s forwards` (no inline keyframe — the keyframe lives in globals.css from 24-01); the what-appeared reveal uses `const unlockEntry = celebratingLevel < 9 ? H_NEXT[celebratingLevel] : null` (guard — never H_NEXT[9]); when unlockEntry, "{Capitalized what} moved in!"; when !unlockEntry (L9), "Course 1 complete — you grew the whole world." Use CONFETTI_COLORS[i % CONFETTI_COLORS.length] (always in-bounds), `?.`/guards, never `!`. Confetti is gated on !reducedMotion so reduced-motion renders the static fallback (D-08).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/habitat-celebration.test.ts` PASSES green (the parts targeting habitat-celebration.tsx; the setTimeout-2500 assertion targets habitat-scene.tsx and lands GREEN in Plan 03).
    - habitat-celebration.tsx imports contain neither `motion/react` nor `framer-motion` (`grep -n "motion/react\|framer-motion" src/components/habitat-celebration.tsx` is empty).
    - File contains `data-testid="habitat-celebration"`, the `hab-confetti` class, a `reducedMotion` gate, and the `celebratingLevel < 9` H_NEXT guard (grep all four).
    - `npx tsc --noEmit` passes; `npx biome ci src/components/habitat-celebration.tsx` reports no errors (no `!`).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/habitat-celebration.test.ts && npx biome ci src/components/habitat-celebration.tsx</automated>
  </verify>
  <done>habitat-celebration.tsx created CSS-only (hab-confetti + globals hab-fall), confetti gated on !reducedMotion, reveal from H_NEXT with L9 guard; no motion/react, no `!`; celebration-source assertions GREEN; tsc + scoped biome clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HabitatState (server-computed) → atoms | Atoms render trusted server-derived props (mood/level/counts); no untrusted input enters here |
| Link href targets | HBack → /dashboard, HDecayCard Study-now → /dashboard — static internal routes, not user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-LEVEL | Tampering | HLevelBadge / HProgCard / HabitatCelebration rendering an out-of-range level | mitigate | Atoms guard H_NEXT access with `level < 9` (Pitfall 5) and gold/`isMax` with `>= 9`; the upstream `?celebrate` integer-[1,9] validation that feeds celebratingLevel is enforced in page.tsx by Plan 03 (T-24-03-CELEBRATE) — these atoms are defensive but not the validation boundary |
| T-24-02-OPENREDIRECT | Tampering | HBack / HDecayCard Link hrefs | accept | hrefs are hard-coded literal internal paths ("/dashboard") — no user-controlled URL, no open-redirect surface |
| T-24-02-DOS | Denial of Service | 26 CSS confetti divs | accept | Fixed count (26), CSS-only animation, gated off under reduced-motion; negligible render cost, no runtime loop (RESEARCH Don't-Hand-Roll) |
| T-24-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH § Package Legitimacy Audit) — nothing to verify |

Auth/session/access-control unchanged (route already protected) — N-A here. The single real input-validation threat (`?celebrate=N`) is mitigated in Plan 03.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/habitat-prog-card.test.ts src/components/__tests__/habitat-celebration.test.ts src/components/__tests__/habitat-tint.test.ts src/lib/__tests__/habitat-names.test.ts` — all green except any cross-file scene assertions that resolve in Plan 03.
- `npx tsc --noEmit` passes.
- `npx biome ci src/components/daybreak/h-back.tsx src/components/daybreak/h-mood-chip.tsx src/components/daybreak/h-level-badge.tsx src/components/daybreak/h-top.tsx src/components/daybreak/h-prog-card.tsx src/components/daybreak/h-decay-card.tsx src/components/habitat-celebration.tsx` reports no errors (scoped — never the whole repo).
- No file in this plan imports motion/react or uses a `!` non-null assertion.
</verification>

<success_criteria>
- 6 Daybreak atoms + HabitatCelebration exist with the correct data-testids, gold-at-L9, guarded H_NEXT, and Daybreak inline styling matching the handoff.
- HProgCard and HabitatCelebration source assertions are unit-green; HDecayCard routes Study-now → /dashboard.
- Perf invariant held: the celebration is CSS-only (hab-confetti + globals hab-fall), no motion/react anywhere in the plan.
</success_criteria>

<output>
Create `.planning/phases/24-habitat/24-02-SUMMARY.md` when done
</output>
