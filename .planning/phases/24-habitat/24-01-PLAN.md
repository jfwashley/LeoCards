---
phase: 24-habitat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/habitat-names.ts
  - src/lib/habitat-tint.ts
  - src/app/globals.css
  - src/lib/__tests__/habitat-names.test.ts
  - src/components/__tests__/habitat-tint.test.ts
  - src/components/__tests__/habitat-prog-card.test.ts
  - src/components/__tests__/habitat-celebration.test.ts
  - e2e/24-habitat-celebration.spec.ts
  - src/components/__tests__/habitat-video.test.ts
  - src/components/__tests__/habitat-scene-video.test.ts
autonomous: true
requirements: [HAB-01, HAB-02, HAB-03, HAB-04]
must_haves:
  truths:
    - "D-05 mood-driven CSS tint mapping exists as a pure exported moodTint helper: excited→warm amber, happy→green, neutral→beige, sad→cool blue-grey, isDecaying→grey wash, level≥9→golden-hour rgba (sourced from the handoff MOOD/SKY palettes), composited over (not replacing) the existing decayFilter"
    - "D-12 L9 is the canonical cap: H_NEXT has keys 1–8 only (H_NEXT[9] is undefined), and the progress-card pct logic treats nextLevelThreshold === null as max → 'Course 1 complete', never the dead level>=10 branch"
    - "D-14 the tint helper and mood data cover exactly the 4 engine moods (excited/happy/neutral/sad) — no sleeping/napping/night-cycle tint or fifth state is introduced on this page"
    - "H_NAME returns the correct level name for levels 1–9 (Bare mound … Golden hour) and H_NEXT returns the named next unlock for levels 1–8 with .at = level+1"
    - "Nyquist Wave 0: the 5 new test files exist and the 3 existing habitat test files are extended, every one runnable headless via npx vitest run / npx playwright with no watch-mode flag, so every downstream task has an automated verify"
    - "globals.css defines @keyframes hab-fall and a prefers-reduced-motion override that disables .hab-confetti animation — CSS-only, no motion/react"
  artifacts:
    - path: "src/lib/habitat-names.ts"
      provides: "H_NAME and H_NEXT constants (pure data module, RSC-safe, no imports)"
      contains: "H_NEXT"
    - path: "src/lib/habitat-tint.ts"
      provides: "moodTint(mood, isDecaying, level) pure helper returning an rgba/gradient string"
      exports: ["moodTint"]
    - path: "src/app/globals.css"
      provides: "@keyframes hab-fall + reduced-motion .hab-confetti override"
      contains: "hab-fall"
    - path: "e2e/24-habitat-celebration.spec.ts"
      provides: "e2e scaffold: ?celebrate=5 shows then settles the celebration overlay"
      contains: "habitat-celebration"
  key_links:
    - from: "src/lib/habitat-tint.ts"
      to: "design/handoff-daybreak/daybreak-habitat-scene.jsx MOOD/SKY palettes"
      via: "tint hex values derived from the handoff"
      pattern: "moodTint"
---

<objective>
Lay the Wave-0 foundation for the Habitat Daybreak re-skin: the pure data + helper modules that every downstream atom consumes, the CSS confetti keyframe, and the full Nyquist test scaffold (5 new files + 3 extended) so Waves 2–3 each ship with an automated verify.

This plan adds ZERO consumer wiring and touches NO habitat domain logic (D-02) — it only creates source-of-truth constants (`H_NAME`/`H_NEXT`), the `moodTint` helper (D-05), the `hab-fall` keyframe, and the test files. It deliberately does not import `getLayersForLevel` from `habitat-ui-utils.ts` (the dead 3D-era 10-level table — RESEARCH Pitfall 7); level names/unlocks come only from the handoff's `H_NAME`/`H_NEXT`.

Purpose: downstream plans need stable contracts (constants, tint mapping) and red tests before implementation.
Output: 2 source modules, 1 globals.css addition, 5 new + 3 extended test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-habitat/24-VALIDATION.md
@.planning/phases/24-habitat/24-PATTERNS.md
@.planning/phases/24-habitat/24-RESEARCH.md

<interfaces>
<!-- The engine state shape downstream consumers read. Extracted from src/lib/habitat-engine.ts — do NOT edit this file (D-02). -->
From src/lib/habitat-engine.ts:
  export type TigerMood = "excited" | "happy" | "neutral" | "sad";
  export interface HabitatState {
    level: number;
    mood: TigerMood;
    quality: number;
    effectiveCardCount: number;   // Math.floor(quality * learnedCardCount)
    isDecaying: boolean;
    nextLevelThreshold: number | null;   // null === max level (L9 cap, D-12)
    // ...other fields unchanged
  }

<!-- The constants this plan CREATES (contract for Waves 2-3): -->
export const H_NAME: Record<number, string>   // keys 1-9
export const H_NEXT: Record<number, { at: number; what: string }>  // keys 1-8 ONLY
export function moodTint(mood: TigerMood, isDecaying: boolean, level: number): string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — 5 new test files + extend 3 existing</name>
  <files>src/lib/__tests__/habitat-names.test.ts, src/components/__tests__/habitat-tint.test.ts, src/components/__tests__/habitat-prog-card.test.ts, src/components/__tests__/habitat-celebration.test.ts, e2e/24-habitat-celebration.spec.ts, src/components/__tests__/habitat-video.test.ts, src/components/__tests__/habitat-scene-video.test.ts</files>
  <read_first>
    - .planning/phases/24-habitat/24-VALIDATION.md (Wave 0 Requirements + per-requirement command map — the authority for which files and commands)
    - .planning/phases/24-habitat/24-RESEARCH.md § Validation Architecture (Phase Requirements → Test Map)
    - .planning/phases/24-habitat/24-PATTERNS.md § Wave 0 Test Files (exact analog + import patterns per file)
    - src/components/__tests__/habitat-video.test.ts (node-env Vitest analog: pure-helper import + readFileSync source-grep; the file this task EXTENDS for mobile-freeze logic)
    - src/components/__tests__/habitat-scene-video.test.ts (source-grep invariants analog incl. existing VS4 wrapper test; the file this task EXTENDS for celebratingLevel)
    - e2e/07-habitat-display.spec.ts (Playwright + signUpWithDeck helper analog for the new 24-habitat-celebration.spec.ts)
  </read_first>
  <action>
    Create the 5 Wave-0 test files and extend the 3 existing ones, all node-environment Vitest (no jsdom, never render components — @testing-library/react is NOT installed) or Playwright. New files:
    (1) src/lib/__tests__/habitat-names.test.ts — assert H_NAME has entries 1–9, H_NEXT has entries 1–8 and H_NEXT[9] is undefined (Pitfall 5 guard), and each H_NEXT[level].at === level+1.
    (2) src/components/__tests__/habitat-tint.test.ts — import moodTint from ../../lib/habitat-tint; assert isDecaying→grey wash string, level≥9 (not decaying)→a golden-hour rgba distinct from any mood tint, and each of the 4 moods→its own distinct rgba.
    (3) src/components/__tests__/habitat-prog-card.test.ts — source-grep src/components/daybreak/h-prog-card.tsx for the pct formula identifiers (effectiveCardCount, nextLevelThreshold, Math.round, Math.min) and assert the file references nextLevelThreshold === null and the "Course 1 complete" copy for the L9 branch.
    (4) src/components/__tests__/habitat-celebration.test.ts — source-grep src/components/habitat-celebration.tsx: imports do NOT match motion/react or framer-motion; confetti is gated on reducedMotion and uses the hab-confetti class; data-testid="habitat-celebration" present; and source-grep src/components/habitat-scene.tsx for setTimeout(... 2500 (the D-07 auto-settle timer).
    (5) e2e/24-habitat-celebration.spec.ts — beforeEach signUpWithDeck(page, "French"); goto /habitat?celebrate=5; assert getByTestId("habitat-celebration") visible within 10s; then waitForTimeout(3200) and assert it is not visible (D-07 ~2.5s settle).
    Extend: habitat-video.test.ts with assertions that the mobile-freeze logic exists (source-grep for IntersectionObserver, window.innerWidth < 768, video.pause, setTimeout freeze, and the .catch on play); habitat-scene-video.test.ts with a source-grep that habitat-scene.tsx consumes celebratingLevel (NOT the dead _celebratingLevel prefix) — keep the existing VS4 16/9 wrapper test untouched.
    Use ?. optional chaining never ! (biome noNonNullAssertion). No .only / no watch flags. Tests reference files that do not yet exist (Task 2 / Waves 2–3) and are expected RED now — that is the intent.
  </action>
  <acceptance_criteria>
    - Files exist at all 5 new paths and the 3 existing test files contain the new assertions (grep: `grep -l "habitat-celebration" e2e/24-habitat-celebration.spec.ts`).
    - `npx vitest run src/lib/__tests__/habitat-names.test.ts src/components/__tests__/habitat-tint.test.ts src/components/__tests__/habitat-prog-card.test.ts src/components/__tests__/habitat-celebration.test.ts` runs (fails RED on missing source modules — acceptable this task; it must EXECUTE, not error on syntax).
    - No test file contains `.only(`, `--watch`, or a non-null `!` assertion (grep returns nothing for `grep -rn "\.only(" src/lib/__tests__/habitat-names.test.ts src/components/__tests__/habitat-tint.test.ts`).
    - `npx biome ci src/lib/__tests__/habitat-names.test.ts src/components/__tests__/habitat-tint.test.ts src/components/__tests__/habitat-prog-card.test.ts src/components/__tests__/habitat-celebration.test.ts e2e/24-habitat-celebration.spec.ts` reports no errors on the new files.
  </acceptance_criteria>
  <verify>
    <automated>npx biome ci src/lib/__tests__/habitat-names.test.ts src/components/__tests__/habitat-tint.test.ts src/components/__tests__/habitat-prog-card.test.ts src/components/__tests__/habitat-celebration.test.ts e2e/24-habitat-celebration.spec.ts</automated>
  </verify>
  <done>All 5 new test files created, 3 existing extended; the 4 new unit specs execute under vitest (RED on missing source); biome clean on new files; no watch flags, no `!`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constants module + moodTint helper + hab-fall keyframe (GREEN the data tests)</name>
  <files>src/lib/habitat-names.ts, src/lib/habitat-tint.ts, src/app/globals.css</files>
  <behavior>
    - habitat-names test HN1: H_NAME[1..9] are the handoff names; HN2: H_NEXT[9] === undefined; HN3: H_NEXT[level].at === level+1 for 1–8.
    - habitat-tint test: moodTint("excited",false,5), ("happy",..), ("neutral",..), ("sad",..) each return a distinct rgba; moodTint(any,true,5) returns the grey wash; moodTint("happy",false,9) returns the golden-hour rgba distinct from the happy mood tint.
  </behavior>
  <read_first>
    - .planning/phases/24-habitat/24-PATTERNS.md § src/lib/habitat-names.ts (verbatim constants) AND § habitat-scene.tsx moodTint helper (the exact rgba mapping to extract into habitat-tint.ts)
    - .planning/phases/24-habitat/24-RESEARCH.md § Pattern 1 (CSS Mood Tint — MOOD/SKY values, golden-hour radial, opacity 0.18–0.28) and § Code Examples (H_NAME/H_NEXT)
    - design/handoff-daybreak/daybreak-habitat.jsx lines 4-10 (H_NAME/H_NEXT source of truth) and lines 11-13 (MOOD palette)
    - design/handoff-daybreak/daybreak-habitat-scene.jsx (SKY/PAL palettes, golden-hour overlay, decay colour — REFERENCE only, D-01: not rebuilding the scene)
    - src/components/daybreak/lang-chip.tsx (Daybreak module convention: inline data, no "use client" on pure modules)
  </read_first>
  <action>
    Create src/lib/habitat-names.ts as a pure data module (no imports, no "use client"): export const H_NAME (Record number→string, keys 1–9) and H_NEXT (Record number→{at,what}, keys 1–8 ONLY) verbatim from PATTERNS.md. Add a header comment that H_NEXT has no L9 entry and callers must guard with nextLevelThreshold === null (D-12). Create src/lib/habitat-tint.ts exporting moodTint(mood: TigerMood, isDecaying: boolean, level: number): string per the PATTERNS mapping: isDecaying → "rgba(120,120,130,0.10)"; level >= 9 → the golden-hour rgba; else the 4-mood map (excited rgba 242,179,58,0.12 / happy 62,155,95,0.10 / neutral 183,169,143,0.10 / sad 124,147,176,0.12); use `map[mood] ?? "rgba(0,0,0,0)"` (no `!`); import type { TigerMood } from "@/lib/habitat-engine". Do NOT add the night/sleep state (D-14) — only the 4 moods. Add to src/app/globals.css the @keyframes hab-fall (translateY -10vh→110vh, rotate 0→720deg, opacity 1→0) and a `@media (prefers-reduced-motion: reduce) { .hab-confetti { animation: none; } }` override. CSS-only — no motion/react anywhere.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/habitat-names.test.ts src/components/__tests__/habitat-tint.test.ts src/components/__tests__/habitat-prog-card.test.ts` — habitat-names and habitat-tint specs PASS green (habitat-prog-card may still be red until Wave 2).
    - `grep -n "hab-fall" src/app/globals.css` returns the @keyframes line, and `grep -n "prefers-reduced-motion" src/app/globals.css` shows the .hab-confetti override.
    - habitat-names.ts and habitat-tint.ts contain no `import.*motion/react`, no `getLayersForLevel`, and no `!` non-null assertion (`grep -rn "getLayersForLevel\|motion/react" src/lib/habitat-names.ts src/lib/habitat-tint.ts` is empty).
    - `npx tsc --noEmit` passes (no type errors introduced).
    - `npx biome ci src/lib/habitat-names.ts src/lib/habitat-tint.ts` reports no errors.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/__tests__/habitat-names.test.ts src/components/__tests__/habitat-tint.test.ts && npx biome ci src/lib/habitat-names.ts src/lib/habitat-tint.ts</automated>
  </verify>
  <done>habitat-names.ts + habitat-tint.ts created and GREEN against their specs; hab-fall keyframe + reduced-motion override added to globals.css; tsc + scoped biome clean; no motion/react, no getLayersForLevel, no `!`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author → constants | H_NAME/H_NEXT are static authored strings, never user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-XSS | Tampering/Injection | H_NAME / H_NEXT strings | accept | Constants are static literals authored from the handoff, rendered as text nodes (no dangerouslySetInnerHTML downstream); no user input path reaches them — not applicable |
| T-24-01-CSS | Tampering | @keyframes hab-fall in globals.css | accept | Pure CSS animation, no JS, no external/user-controlled values; cannot carry a payload |
| T-24-01-SC | Tampering | npm/pip/cargo installs | accept | RESEARCH § Package Legitimacy Audit: no new packages installed in this phase; nothing to verify |

Auth/access-control/session surfaces are unchanged in this plan (the /habitat route is already protected by (protected)/layout.tsx) — out of scope / N-A here. The real input-validation threat (untrusted ?celebrate=N) is owned by Plan 03 (T-24-03-CELEBRATE), where page.tsx is edited.
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/habitat-names.test.ts src/components/__tests__/habitat-tint.test.ts` is green.
- `npx tsc --noEmit` passes.
- `npx biome ci src/lib/habitat-names.ts src/lib/habitat-tint.ts src/lib/__tests__/habitat-names.test.ts src/components/__tests__/habitat-tint.test.ts e2e/24-habitat-celebration.spec.ts` reports no errors (scoped to touched files — never the whole repo).
- globals.css contains `@keyframes hab-fall` and the reduced-motion `.hab-confetti` override.
</verification>

<success_criteria>
- H_NAME (1–9) and H_NEXT (1–8, none at 9) exported as a pure RSC-safe module; moodTint covers the 4 moods + decay wash + golden-hour and is unit-green.
- All 5 Wave-0 test files exist and the 3 existing files are extended; every downstream task has an automated verify command.
- No motion/react, no zod, no getLayersForLevel, no `!` introduced; hab-fall keyframe is CSS-only.
</success_criteria>

<output>
Create `.planning/phases/24-habitat/24-01-SUMMARY.md` when done
</output>
