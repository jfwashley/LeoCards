---
phase: 25-my-account
plan: 05
type: execute
wave: 4
depends_on: [25-04]
autonomous: true
requirements: [ACC-01, ACC-04, ACC-05, ACC-06]
files_modified:
  - src/components/account-nav-button.tsx
  - src/components/app-header.tsx
  - src/components/logout-button.tsx
  - e2e/01-auth-signup-login.spec.ts
  - e2e/10-mobile-responsive.spec.ts
  - e2e/25-my-account.spec.ts
  - e2e/helpers.ts

must_haves:
  truths:
    - "D-01: the dashboard header's bare logout glyph is REPLACED by an AccountNavButton (person-silhouette head-and-shoulders line glyph in ink #4A331C, built from divs like LogoutGlyph not <svg>, inside the same 36px bordered-button visual — 1.5px #EDDFC9 border, radius 10, white fill — wrapped in a 44px transparent hit area) that navigates to /account; logout has moved inside the section; entry is dashboard header ONLY (study/habitat/deck chrome untouched)"
    - "ACC-01: /account is reachable from the dashboard header glyph (data-testid account-nav-btn, aria-label 'My Account', RSC-safe next/link)"
    - "Removing the header Sign-out control is fully absorbed: e2e/01-auth-signup-login.spec.ts (lines 48/70/99) and e2e/10-mobile-responsive.spec.ts (line 46) are retargeted in this plan's files_modified — never re-adding the old header copy, never leaving a stale literal; each assertion's behavioral intent is preserved (navigate to /account then click the section Sign out; assert the nav glyph visible on mobile)"
    - "logout-button.tsx is deleted only AFTER app-header stops importing it and a tsc --noEmit call-site check confirms zero remaining references"
    - "e2e/25-my-account.spec.ts covers the real pipeline: reach /account from the dashboard glyph; details render; change-password wrong-current inline error + happy path (re-login with the new password); logout lands /login; delete-account confirm → /login → old creds fail; plus the email-change PENDING-BANNER state (token round-trip is unit/integration-covered, no live inbox — the seam is noted explicitly)"
    - "Every interactive control the new spec touches clears the 44px touch-target floor (account-nav-btn 36px visual/44px hit, account-back 40px/44px), asserted with an explicit null-guard on boundingBox (if box===null throw) — never `box?.height` in an expect ARGUMENT position (Phase 23 tsc lesson)"
  artifacts:
    - path: "src/components/account-nav-button.tsx"
      provides: "header account nav glyph → /account"
      exports: ["AccountNavButton"]
    - path: "e2e/25-my-account.spec.ts"
      provides: "full account-flow e2e"
    - path: "src/components/app-header.tsx"
      provides: "header with AccountNavButton in place of LogoutButton"
  key_links:
    - from: "src/components/app-header.tsx"
      to: "AccountNavButton"
      via: "right-cluster import + render"
      pattern: "AccountNavButton"
    - from: "e2e/01-auth-signup-login.spec.ts"
      to: "account-nav-btn → account-logout-btn"
      via: "retargeted sign-out flow"
      pattern: "account-nav-btn"
    - from: "e2e/25-my-account.spec.ts"
      to: "verification token seam"
      via: "e2e/helpers.ts pending-token read"
      pattern: "getPendingEmailChangeToken|verify-email"
---

<objective>
Land the header glyph swap (D-01) and all e2e coverage LAST — after /account is fully live (25-04) — so removing the header "Sign out" control never leaves the app or its specs broken. This plan builds the AccountNavButton, swaps it into app-header (removing LogoutButton), deletes the now-dead logout-button.tsx, retargets the exactly-two impacted existing specs, and adds the new full-flow e2e/25 spec + the test-only pending-token seam.

Ordering rationale (Phases 20-23 L-06 lesson): the moment app-header drops the "Sign out" button, e2e/01:48,70,99 + e2e/10:46 break — they can only be retargeted to the new /account → account-logout-btn flow once /account exists. Hence this is Wave 4, owning both retarget files in files_modified.

Purpose: make /account reachable from the dashboard and prove the whole phase against the real pipeline.
Output: account-nav-button.tsx, app-header swap, logout-button deletion, 2 e2e retargets, e2e/25 + helper seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-my-account/25-UI-SPEC.md
@.planning/phases/25-my-account/25-PATTERNS.md
@.planning/phases/25-my-account/25-RESEARCH.md

<interfaces>
AccountNavButton: RSC-safe (no "use client"), a bare next/link href="/account", data-testid="account-nav-btn", aria-label="My Account", rendering AccountGlyph inside the 36px LogoutButton frame within a 44px transparent hit wrapper.

Exact e2e retargets (RESEARCH Pitfall 13 + UI-SPEC retarget audit — grep-confirmed the ONLY hits):
  e2e/01-auth-signup-login.spec.ts lines 48, 70, 99:
    OLD: await page.getByRole("button", { name: "Sign out" }).click();  await page.waitForURL(/\/login/, ...)
    NEW: await page.getByTestId("account-nav-btn").click(); await page.waitForURL(/\/account/, ...);
         await page.getByRole("button", { name: "Sign out" }).click(); await page.waitForURL(/\/login/, ...)
  e2e/10-mobile-responsive.spec.ts line 46:
    OLD: await expect(page.getByRole("button", { name: "Sign out" })).toBeVisible();
    NEW: await expect(page.getByTestId("account-nav-btn")).toBeVisible();

Email-change e2e seam (RESEARCH Validation Architecture): no live inbox — after triggering requestEmailChange via the real UI, read the pending verification row's token directly (a new e2e/helpers.ts helper using the Drizzle db client, or a DEBUG_CHEAT_SECRET-gated read endpoint mirroring /api/debug/state), then page.goto("/api/account/verify-email?token=..."). If direct DB/endpoint access is impractical in-scope, the request→click→success round-trip stays unit-covered (route.test.ts) + this spec asserts only the PENDING-BANNER state; document the seam in the SUMMARY.
</interfaces>

Analog sources (excerpts in 25-PATTERNS.md):
@src/components/app-header.tsx
@src/components/logout-button.tsx
@src/components/daybreak/h-back.tsx
@e2e/helpers.ts
@e2e/01-auth-signup-login.spec.ts
@e2e/10-mobile-responsive.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: account-nav-button.tsx (person-silhouette glyph → /account)</name>
  <files>src/components/account-nav-button.tsx</files>
  <read_first>
    - 25-UI-SPEC.md §Header glyph swap (exact AccountGlyph geometry: canvas 16×16, head 6.5×6.5 circle top-center, shoulders 14×7.5 radius 8px 8px 0 0 bottom-center, ink #4A331C; 36px inner frame identical to LogoutButton; 44px transparent outer hit; aria-label "My Account"; data-testid account-nav-btn)
    - 25-PATTERNS.md → "account-nav-button.tsx" (h-back RSC-safe Link structure + logout-button glyph div-technique + the exact 36px frame values to preserve)
    - src/components/logout-button.tsx (LogoutGlyph div/absolute-position technique + the 36px frame style object to match), src/components/daybreak/h-back.tsx (bare next/link, no "use client")
  </read_first>
  <action>
    Create `src/components/account-nav-button.tsx` — RSC-safe (NO `"use client"`), a bare `next/link` `href="/account"`, `data-testid="account-nav-btn"`, `aria-label="My Account"`. Structure it as a 44×44 transparent flex-centered outer Link wrapping the 36×36 visual frame whose style object matches LogoutButton's EXACTLY (width/height 36, borderRadius 10, border "1.5px solid #EDDFC9", background "#FFFFFF", inline-flex centered, boxSizing border-box, flex none). Inside, render an inline `AccountGlyph` built with the same div/absolute-position/border-radius technique as LogoutGlyph (NOT an `<svg>`): a 16×16 relative canvas, a 6.5×6.5 circle (borderRadius 50%) absolutely centered at top, and a 14×7.5 shoulders shape (borderRadius "8px 8px 0 0") absolutely centered at bottom, both `background: "#4A331C"`, the canvas `aria-hidden`. The glyph is ink-line on white — never amber-filled.
  </action>
  <acceptance_criteria>
    - No `"use client"` in the file (RSC-safe Link); exports `AccountNavButton`.
    - The 36px inner frame style matches LogoutButton's frame numbers exactly; a 44px transparent outer hit wrapper is present.
    - `grep -n "account-nav-btn\|My Account\|/account" src/components/account-nav-button.tsx` all present; no `<svg` tag (div-glyph technique).
    - `npx tsc --noEmit` clean; `npx biome ci src/components/account-nav-button.tsx` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx biome ci src/components/account-nav-button.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>AccountNavButton renders the person-silhouette glyph in the 36px frame / 44px hit, links to /account, RSC-safe; tsc + biome clean.</done>
</task>

<task type="auto">
  <name>Task 2: app-header swap + delete logout-button + retarget e2e/01 & e2e/10</name>
  <files>src/components/app-header.tsx, src/components/logout-button.tsx, e2e/01-auth-signup-login.spec.ts, e2e/10-mobile-responsive.spec.ts</files>
  <read_first>
    - 25-UI-SPEC.md §Header glyph swap (swap note + the "verify 44px hit doesn't crowd DeckSwitcher — reduce cluster gap to ~6-7 if needed") and the data-testid Contract e2e Retarget Audit table
    - 25-RESEARCH.md Pitfall 13 (exact file/line blast radius + the fix for each)
    - 25-PATTERNS.md → "app-header.tsx" (one-line swap in the right cluster) + the e2e/01 and e2e/10 retarget sections (exact before/after)
    - src/components/app-header.tsx (line 5 import + line 54 <LogoutButton/> in the gap-9 right cluster), e2e/01-auth-signup-login.spec.ts (lines 48/70/99), e2e/10-mobile-responsive.spec.ts (line 46)
  </read_first>
  <action>
    In `src/components/app-header.tsx`: change the import on line 5 from `LogoutButton` (`@/components/logout-button`) to `AccountNavButton` (`@/components/account-nav-button`), and replace `<LogoutButton />` in the right cluster (line 54) with `<AccountNavButton />`. If the 44px hit area visually crowds `DeckSwitcher` in the `gap: 9` cluster, reduce the cluster gap to ~6-7 (UI-SPEC note). Then DELETE `src/components/logout-button.tsx` — but ONLY after confirming via `npx tsc --noEmit` that nothing else imports it (its signOut logic was already lifted into account-logout-section in 25-03; grep for `logout-button` and `LogoutButton` across src/ to confirm zero remaining references before deleting).

    Retarget the exactly-two impacted specs, preserving each assertion's behavioral intent, never re-adding the old header copy, never leaving a stale "Sign out" header literal:
    - `e2e/01-auth-signup-login.spec.ts` lines 48, 70, 99: before each `getByRole("button",{name:"Sign out"}).click()` insert a navigation hop — `await page.getByTestId("account-nav-btn").click(); await page.waitForURL(/\/account/, { timeout: 10_000 });` — then the existing Sign out click (now targeting account-logout-btn, same accessible name) + the existing waitForURL(/\/login/). The accessible-name string stays "Sign out".
    - `e2e/10-mobile-responsive.spec.ts` line 46: replace `expect(page.getByRole("button",{name:"Sign out"})).toBeVisible()` with `expect(page.getByTestId("account-nav-btn")).toBeVisible()` (the test verifies mobile header chrome doesn't overflow — the nav glyph is the header control now).
  </action>
  <acceptance_criteria>
    - `src/components/app-header.tsx` imports + renders `AccountNavButton`; no reference to LogoutButton remains.
    - `src/components/logout-button.tsx` no longer exists; `grep -rn "logout-button\|LogoutButton" src/` returns zero hits.
    - `grep -n "Sign out" e2e/01-auth-signup-login.spec.ts` still targets the section button AFTER an `account-nav-btn` navigation on lines 48/70/99; `grep -n "account-nav-btn" e2e/10-mobile-responsive.spec.ts` present on the mobile-header assertion; no stale header "Sign out" visibility assertion remains in e2e/10.
    - `npx tsc --noEmit` clean; `npx biome ci src/components/app-header.tsx e2e/01-auth-signup-login.spec.ts e2e/10-mobile-responsive.spec.ts` clean.
    - `npx playwright test e2e/01-auth-signup-login.spec.ts e2e/10-mobile-responsive.spec.ts` green (restart a fresh dev server first — never-live-run harness lesson).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test e2e/01-auth-signup-login.spec.ts e2e/10-mobile-responsive.spec.ts</automated>
  </verify>
  <done>Header renders AccountNavButton, logout-button.tsx deleted with zero dangling refs, both impacted specs retargeted green.</done>
</task>

<task type="auto">
  <name>Task 3: e2e/25-my-account.spec.ts + pending-token seam in helpers</name>
  <files>e2e/25-my-account.spec.ts, e2e/helpers.ts</files>
  <read_first>
    - 25-UI-SPEC.md data-testid Contract (all account testids) + Copywriting Contract (exact strings to assert)
    - 25-RESEARCH.md Validation Architecture (email-send seam — DB token read or debug endpoint; unit-covered fallback) + Pitfall 13
    - 25-PATTERNS.md → "e2e/25-my-account.spec.ts" (signUpWithDeck/testEmail reuse, clickAndWaitForNetworkSettle/waitForCompilation anti-flake idioms, the new DB-read seam)
    - e2e/helpers.ts (testEmail, signUpWithDeck, waitForCompilation, clickAndWaitForNetworkSettle), e2e/10-mobile-responsive.spec.ts lines 108-113 (boundingBox 44px assertion pattern — use `if (box === null) throw` narrowing, NOT box?.height in an expect argument)
  </read_first>
  <action>
    Add a test-only seam to `e2e/helpers.ts`: a `getPendingEmailChangeToken(userId)` (or email-keyed) helper that reads the pending `verification` row's token directly via the same Drizzle `db` client the app uses (identifier `change-email:{userId}`, JSON.parse the value for `token`). If direct DB access from the Playwright runner proves impractical, instead add a `DEBUG_CHEAT_SECRET`-gated read mirroring `/api/debug/state` (404 when the secret is unset — never customer-reachable). Reuse the existing `*test.local` throwaway-user convention.

    Create `e2e/25-my-account.spec.ts` using `signUpWithDeck(page)` + `testEmail()`. Cover the real pipeline (data-testid-first selectors; wrap fire-and-forget mutations with clickAndWaitForNetworkSettle/waitForCompilation):
    1. Reach: from the dashboard, click `account-nav-btn` → waitForURL(/\/account/); assert the details card renders name/email/"Member since"/"I speak".
    2. Touch targets: assert `account-nav-btn` (on dashboard) and `account-back-btn` (on /account) each have boundingBox height AND width ≥ 44 — narrow with `const box = await el.boundingBox(); if (box === null) throw new Error("no box");` then `expect(box.height).toBeGreaterThanOrEqual(44)` (NEVER `box?.height` as an expect argument — Phase 23 tsc lesson).
    3. Change password — wrong current: expand account-password-row, fill a wrong current + valid new/confirm, submit, assert inline "That password isn't right." under the current field. Then happy path: fill the correct current + a new password, submit, assert "Password updated"; then Sign out and sign back in with the NEW password to prove it applied.
    4. Email pending-banner: Edit details, change the email to a second unique testEmail, Save; assert the account-email-pending-banner shows "Verification sent to {newEmail}" and the Email row still shows the OLD address. (Token round-trip: EITHER read the token via the helper seam and page.goto the verify-email URL then assert account-verify-result-banner success; OR, if the seam is out-of-scope, stop at the pending-banner assertion and note the round-trip is unit-covered by route.test.ts — document in the SUMMARY.)
    5. Logout: click account-logout-btn → waitForURL(/\/login/).
    6. Delete: (fresh throwaway user) reach /account, click account-delete-row → account-delete-confirm-btn → waitForURL(/\/login/); then attempt to sign in with the deleted creds and assert it fails (cannot sign in — D-14).
  </action>
  <acceptance_criteria>
    - e2e/25-my-account.spec.ts exists and covers reach, details render, change-password wrong+happy (incl. re-login with new password), pending-banner, logout→/login, delete→/login→sign-in-fails.
    - Touch-target assertions use an explicit `if (box === null) throw` narrow, not `box?.height`/`box?.width` in an expect argument.
    - The email-token seam is either exercised (helper/endpoint) or explicitly deferred-to-unit in the spec comment + SUMMARY; no live inbox is required for the suite to pass.
    - `npx tsc --noEmit` clean; `npx biome ci e2e/25-my-account.spec.ts e2e/helpers.ts` clean; `npx playwright test e2e/25-my-account.spec.ts` green against a freshly-restarted dev server.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test e2e/25-my-account.spec.ts</automated>
  </verify>
  <done>Full account-flow e2e green (real pipeline, throwaway *test.local users), touch targets ≥44 with safe null-guards, email round-trip either seam-covered or unit-deferred+documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard header → /account | the nav glyph is a pure navigation Link (no user input) |
| e2e pending-token seam → verification table | test-only read of the token store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-A | Tampering (regression) | header sign-out removal | mitigate | Owning plan retargets the exactly-two grep-confirmed specs (e2e/01:48,70,99 + e2e/10:46) in files_modified; behavioral intent preserved; zero stale literals |
| T-25-05-B | Info Disclosure | e2e pending-token seam | mitigate | Seam is a test-only Drizzle read OR a DEBUG_CHEAT_SECRET-gated endpoint that 404s when the secret is unset (never customer-reachable, mirrors /api/debug/state) |
| T-25-05-C | Tampering | account-nav glyph | accept | Pure RSC next/link, no state, no user input — negligible surface |
| T-25-05-SC | Tampering | dependency supply chain | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit N/A) |
</threat_model>

<verification>
Phase gate (this is the last wave — full phase certification):
- Full `npx tsc --noEmit` clean (run the FULL tsc AFTER the e2e wave — biome's no-`!` rule can push `box?.y`-style expressions into a toBeGreaterThan arg → tsc error; use `if (x === null) throw` narrowing).
- Full `npx vitest run` green (~2079 tests; known flakes cooldown-config.test.ts / bw-atoms.test.tsx isolated before treating a timeout as a regression).
- `npx biome ci` scoped to this plan's touched files only.
- `npx playwright test e2e/25-my-account.spec.ts e2e/01-auth-signup-login.spec.ts e2e/10-mobile-responsive.spec.ts` green against a FRESH dev server (restart before the run).
- Zero new dependencies; src/db/schema.ts untouched; no repo-wide biome.
</verification>

<success_criteria>
- /account reachable from the dashboard glyph (ACC-01); logout works from the section (ACC-04); delete removes the account and blocks re-sign-in (ACC-05); Daybreak header + mobile intact (ACC-06); D-01 delivered.
- logout-button.tsx removed with zero dangling references.
- e2e/01 + e2e/10 retargeted; e2e/25 full-flow spec green; email round-trip covered (seam or unit-deferred).
</success_criteria>

<output>
Create `.planning/phases/25-my-account/25-05-SUMMARY.md` when done.
</output>
