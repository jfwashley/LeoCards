---
phase: 15-core-journey-qa-harness
plan: 05
type: execute
wave: 4
depends_on: [15-03, 15-04]
files_modified:
  - scripts/qa-run.mjs
  - package.json
autonomous: true
requirements: [QAJ-01, QAJ-02, QAJ-03, QAJ-04, QAJ-05, QAJ-06]
must_haves:
  truths:
    - "D-01: the orchestrator scripts/qa-run.mjs is a standalone Node script (scripts/*.mjs convention) that runs all five journey scripts in sequence and reports per-journey PASS/FAIL — NOT Playwright, NOT Vitest"
    - "D-04: qa-run.mjs targets the local dev server and, after the journeys, invokes scripts/cleanup-test-users.mjs with the *test.local pattern so every self-provisioned QA user is removed (self-clean)"
    - "QAJ-06: a full qa-run leaves zero residue in prod data — cleanup runs with arg %@test.local and the run reports the deleted-user count"
    - "QAJ-01..QAJ-05: qa-run.mjs sequences qa-01-learn-card, qa-02-mastery, qa-03-resume, qa-04-habitat, qa-05-decay and exits non-zero if any journey fails"
    - "npm scripts expose the harness (e.g. qa:run, qa:cleanup) so it is run-on-demand without memorizing node invocations"
  artifacts:
    - path: "scripts/qa-run.mjs"
      provides: "sequential journey orchestrator + cleanup invocation + pass/fail report"
      contains: "cleanup-test-users"
    - path: "package.json"
      provides: "npm scripts wiring the harness (qa:run, qa:cleanup)"
      contains: "qa:run"
  key_links:
    - from: "scripts/qa-run.mjs"
      to: "scripts/qa-01..qa-05"
      via: "spawnSync node <script> sequentially, collect exit codes"
      pattern: "qa-0[1-5]"
    - from: "scripts/qa-run.mjs"
      to: "scripts/cleanup-test-users.mjs"
      via: "spawnSync with %@test.local pattern in a finally"
      pattern: "cleanup-test-users"
    - from: "package.json"
      to: "scripts/qa-run.mjs"
      via: "scripts.qa:run"
      pattern: "qa-run\\.mjs"
---

<objective>
Implement the harness orchestrator `scripts/qa-run.mjs` that runs all five journey scripts in sequence, collects per-journey pass/fail, invokes `scripts/cleanup-test-users.mjs` with the `*test.local` pattern to self-clean (QAJ-06), and exits non-zero if any journey or the cleanup fails. Wire npm scripts so the harness is run-on-demand. This closes the phase: one command exercises the entire core learning journey against the real pipeline and leaves no residue.

Purpose: A single entry point (D-01) that proves the whole journey correct and self-cleans (D-04 / QAJ-06).
Output: `scripts/qa-run.mjs` + `package.json` npm-script entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Repo reality (encode into wave gate):
- Worktrees OFF, sequential on `main`. Waves = dependency ordering + gates.
- Biome scoped: `npx biome ci scripts/qa-run.mjs package.json` — NEVER `biome ci .`. (Biome may not lint package.json; if it errors on JSON, scope to `scripts/qa-run.mjs` only and format package.json with the repo's existing JSON tooling or leave its formatting untouched.)
- Per-wave gate = full `tsc --noEmit` + full `npx vitest run`.
- Live full-run is an operator/orchestrator step. Note the cooldown caveat: qa-01 and qa-03's real-wait mode need `STUDY_COOLDOWN_MINUTES=1`, but qa-02/qa-04 want zero-cooldown — so a single `qa:run` cannot satisfy both cooldown regimes in ONE server boot. Resolve this in qa-run.mjs design (see Task 1): either (a) qa-run defaults to the time-shift fast path for qa-03 (no STUDY_COOLDOWN_MINUTES needed) and documents that qa-01's live cooldown assertion is the one journey requiring a dedicated `STUDY_COOLDOWN_MINUTES=1` boot, or (b) qa-run accepts a flag selecting the cooldown regime. Document the chosen contract in the script header and the SUMMARY.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-core-journey-qa-harness/15-CONTEXT.md
@.planning/phases/15-core-journey-qa-harness/15-RESEARCH.md
@.planning/phases/15-core-journey-qa-harness/15-PATTERNS.md
@.planning/phases/15-core-journey-qa-harness/15-03-SUMMARY.md
@.planning/phases/15-core-journey-qa-harness/15-04-SUMMARY.md

<interfaces>
<!-- Orchestrator inputs. -->

Journey scripts (each: exit 0 + "[QAJ-NN] PASS" stdout on success; exit 1 + "[QAJ-NN] FAIL: <msg>" on failure):
  scripts/qa-01-learn-card.mjs   (QAJ-01 — needs STUDY_COOLDOWN_MINUTES=1 for its live cooldown assertion)
  scripts/qa-02-mastery.mjs      (QAJ-02 — zero-cooldown)
  scripts/qa-03-resume.mjs       (QAJ-03 — two-phase: provision/persist then resume; supports time-shift fast path)
  scripts/qa-04-habitat.mjs      (QAJ-04 — zero-cooldown)
  scripts/qa-05-decay.mjs        (QAJ-05 — time-shift, zero-cooldown fine)

Cleanup (QAJ-06): scripts/cleanup-test-users.mjs — default pattern %@leocards-test.local; MUST be invoked with arg "%@test.local" to reap harness users. Reads CLEANUP_DB_URL (alias DATABASE_URL). Exits 0 even when nothing matches; prints the deleted count.

Orchestrator analog (scripts/render-habitat-posters.mjs lines 101-118): shebang, `import { spawnSync } from "node:child_process"`, fileURLToPath/import.meta.url → ROOT = path.resolve(__dirname, ".."), a `fail(msg)` helper that console.error + process.exit(1).

package.json scripts: this repo already has `test`, `dev`, etc. Add `qa:run` → `node scripts/qa-run.mjs` and `qa:cleanup` → `node scripts/cleanup-test-users.mjs %@test.local` (confirm exact existing script style before editing — match quoting/indentation).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| qa-run.mjs → child processes | spawns journey + cleanup scripts inheriting env (DATABASE_URL, DEBUG_CHEAT_SECRET) |
| qa-run.mjs → cleanup | deletes users matching a pattern — must be the *test.local pattern only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Tampering | cleanup deletes wrong users | mitigate | qa-run ALWAYS passes the literal `%@test.local` pattern; the cleanup script itself re-guards with /@(leocards-)?test\.local$/ and refuses any non-test domain |
| T-15-17 | Denial of Service | a hung journey blocks the run | mitigate | spawnSync per journey is sequential and bounded; on non-zero exit qa-run records the failure and proceeds to cleanup in a finally (no orphaned test users) |
| T-15-18 | Information Disclosure | child stdout leaks secrets | mitigate | journeys already never log secrets (15-02/03/04); qa-run forwards stdout but adds none of its own secret logging |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH); only package.json SCRIPT entries are added — no dependency change |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: scripts/qa-run.mjs (sequential orchestrator + self-clean)</name>
  <files>scripts/qa-run.mjs</files>
  <read_first>
    - scripts/render-habitat-posters.mjs (spawnSync orchestrator skeleton + ROOT resolution + fail() helper)
    - scripts/cleanup-test-users.mjs (the cleanup contract: arg %@test.local, CLEANUP_DB_URL/DATABASE_URL, exit codes)
    - .planning/phases/15-core-journey-qa-harness/15-03-SUMMARY.md + 15-04-SUMMARY.md (any journey CLI flags / two-phase resume contract for qa-03; cooldown regimes)
    - .planning/phases/15-core-journey-qa-harness/15-PATTERNS.md § "scripts/qa-run.mjs"
  </read_first>
  <action>
    Create `scripts/qa-run.mjs`. Header documents the cooldown-regime contract chosen in execution_context (option a or b) and required env (DATABASE_URL, DEBUG_CHEAT_SECRET; optional QA_BASE_URL). Guard required env at startup with process.exit(1). Resolve ROOT via fileURLToPath/import.meta.url. Define the journey list in order: qa-01, qa-02, qa-03 (provision phase then resume phase — drive qa-03's two phases per its contract from 15-04-SUMMARY, defaulting to the time-shift fast path so no STUDY_COOLDOWN_MINUTES is needed), qa-04, qa-05. For each, `spawnSync(process.execPath, [path.join(ROOT, "scripts", "<name>")], { stdio: "inherit", env: process.env })`; record exit status; continue running the rest even if one fails (collect all results) UNLESS a journey's failure makes the next meaningless. After the journeys, in a `finally`, ALWAYS run cleanup: `spawnSync(process.execPath, [path.join(ROOT, "scripts", "cleanup-test-users.mjs"), "%@test.local"], { stdio: "inherit", env: process.env })`. Print a summary table (journey → PASS/FAIL + the cleanup deleted-count). Exit 0 only if every journey passed AND cleanup succeeded; otherwise exit 1. Do not log secrets.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && node --check scripts/qa-run.mjs && echo "qa-run parses"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/qa-run.mjs` exits 0
    - file references all five journeys `qa-01`..`qa-05` and `cleanup-test-users.mjs` with the `%@test.local` arg
    - cleanup is invoked in a `finally` (grep finds `finally` and `%@test.local`)
    - the run exits non-zero when any journey fails (grep shows an exit-code aggregation, not an unconditional exit 0)
    - (operator/orchestrator live run, server up) `node scripts/qa-run.mjs` runs all journeys + cleanup and exits 0 on a clean pass
  </acceptance_criteria>
  <done>qa-run.mjs sequences all five journeys, self-cleans via cleanup-test-users.mjs with the *test.local pattern, reports per-journey results, and exits non-zero on any failure.</done>
</task>

<task type="auto">
  <name>Task 2: Wire npm scripts (qa:run, qa:cleanup)</name>
  <files>package.json</files>
  <read_first>
    - package.json (the existing scripts block — match its quoting/indentation/ordering style; do NOT reformat unrelated entries or touch dependencies)
  </read_first>
  <action>
    Add two entries to the `scripts` block: `"qa:run": "node scripts/qa-run.mjs"` and `"qa:cleanup": "node scripts/cleanup-test-users.mjs %@test.local"`. Preserve the existing JSON style; change nothing else (no dependency edits, no reformat of other scripts).
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && node -e "const p=require('./package.json'); if(!p.scripts['qa:run']||!p.scripts['qa:cleanup']) process.exit(1); console.log('qa scripts present');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `qa:run` and `qa:cleanup` in the scripts block
    - the node -e check exits 0 (both scripts present and package.json still parses as valid JSON)
    - `qa:cleanup` invokes cleanup with the `%@test.local` arg (grep finds it)
    - no change to the `dependencies`/`devDependencies` blocks (git diff touches only the scripts block)
  </acceptance_criteria>
  <done>`npm run qa:run` and `npm run qa:cleanup` are wired; package.json remains valid JSON with no dependency changes.</done>
</task>

</tasks>

<verification>
- `node --check scripts/qa-run.mjs` passes; package.json parses and has both scripts.
- WAVE GATE: full `npx tsc --noEmit` + full `npx vitest run`.
- Biome scoped: `npx biome ci scripts/qa-run.mjs` (and package.json if Biome handles JSON; else leave package.json formatting untouched).
- (Operator/orchestrator) live `npm run qa:run` with the dev server up: all five journeys PASS and cleanup reports the users removed; `npm run qa:cleanup` then reports zero residual.
</verification>

<success_criteria>
- One command (`npm run qa:run`) exercises QAJ-01..QAJ-05 against the real pipeline (D-01) and self-cleans (D-04 / QAJ-06).
- The run exits non-zero if any journey fails — it is a real gate, not a smoke print.
- No new dependencies; only npm-script entries added.
</success_criteria>

<output>
Create `.planning/phases/15-core-journey-qa-harness/15-05-SUMMARY.md` when done. Record: the chosen cooldown-regime contract (how qa-01's live cooldown assertion is handled vs the zero-cooldown journeys), the full-run live result if executed, and the post-run residual-user count from cleanup (proving QAJ-06).
</output>
