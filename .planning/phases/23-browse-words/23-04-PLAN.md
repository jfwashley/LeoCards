---
phase: 23-browse-words
plan: 04
type: execute
wave: 3
depends_on: [23-02, 23-03]
files_modified:
  - e2e/helpers.ts
  - e2e/03-word-list-browser.spec.ts
  - e2e/09-language-breakdown.spec.ts
  - e2e/10-mobile-responsive.spec.ts
autonomous: true
requirements: [BRW-01, BRW-02, BRW-03, BRW-04]
must_haves:
  truths:
    - "The addWordsFromBrowser e2e helper navigates the two-screen IA (lands on tiles, opens a topic, then adds words) so every spec that seeds a deck from Browse still works"
    - "e2e/03 exercises the full re-skinned flow: tiles landing then topic tile then level filter then optimistic add/remove, using structural (role/testid) selectors not literal copy or CSS classes"
    - "e2e/09 and e2e/10 no longer reference removed copy ('Back to my deck', category-pill buttons) and use the new structural selectors"
    - "The whole Browse e2e batch passes against a freshly restarted dev server"
  artifacts:
    - path: "e2e/helpers.ts"
      provides: "addWordsFromBrowser updated for the two-screen IA (tile click + waitForURL(/topic=/) before adding)"
      contains: "topic="
    - path: "e2e/03-word-list-browser.spec.ts"
      provides: "Retargeted Browse spec — tiles/topic/level structural selectors, two-step navigation"
      contains: "topic-tile-"
    - path: "e2e/09-language-breakdown.spec.ts"
      provides: "Retargeted breakdown spec — browse-words-title + Add-a-card back-path, no 'Back to my deck'"
      contains: "browse-words-title"
    - path: "e2e/10-mobile-responsive.spec.ts"
      provides: "Retargeted mobile spec — topic-tile selector, >=44px toggle assertion preserved"
      contains: "topic-tile-"
  key_links:
    - from: "e2e/helpers.ts addWordsFromBrowser"
      to: "Browse two-screen IA (Plan 23-03)"
      via: "topic-tile click + waitForURL(/topic=/) before [aria-label*='Add']"
      pattern: "topic-tile-"
    - from: "e2e/03-word-list-browser.spec.ts"
      to: "Browse tiles + list testids (Plan 23-03)"
      via: "getByTestId(topic-tile) / getByTestId(browse-words-title) / data-testid=word-row"
      pattern: "word-row"
---

<objective>
Retarget every at-risk Playwright locator broken by the Browse re-skin (L-06) and adopt the two-screen navigation in the e2e suite. The most important fix is the shared `addWordsFromBrowser` helper, which currently expects word rows on the Browse landing — after the two-screen IA the landing shows tiles, so the helper must click into a topic before adding words (this single seam breaks specs 02/09/10 via the helper). Then retarget `e2e/03` (the Browse spec), `e2e/09`, and `e2e/10` off literal copy and CSS-class selectors onto the new role/testid structural selectors.

Purpose: BRW-01/02/03/04 behavioral verification, and L-06 (the project's known landmine) — stale literal locators must be retargeted with the re-skin, never re-adding old copy. This plan makes the Browse e2e batch green again and proves the re-skinned flow end-to-end.

Output: `e2e/helpers.ts` (helper fix), `e2e/03-word-list-browser.spec.ts`, `e2e/09-language-breakdown.spec.ts`, `e2e/10-mobile-responsive.spec.ts` (retargets).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-browse-words/23-CONTEXT.md
@.planning/phases/23-browse-words/23-RESEARCH.md
@.planning/phases/23-browse-words/23-PATTERNS.md

<interfaces>
<!-- New structural selectors introduced by Plans 23-02 / 23-03 (the retarget targets) -->
data-testid="browse-words-title"   // centered "Browse Words" title on the tiles landing (BrowseTiles)
data-testid="topic-tile-{slug}"    // slug = category.toLowerCase().replace(/[^a-z]/g, "-") -> topic-tile-animals, topic-tile-food-drink
data-testid="word-row"             // each Row-A word row (BWWordRow) on the list screen
data-testid="browse-words-link"    // D-03 "Browse words" link on the Add-a-Card header (ACTop) — distinct from card-list's browse-words-empty
data-testid="browse-words-empty"   // PRESERVED — the empty-deck card-list "Browse words" link (unchanged)
LEVEL tiles: role button, names "All" / "A1" / "A2" / "B1"   // list screen only (BrowseList)
Landing back-link: role link, name /Add a card/i -> /deck/new-card?deck=… (D-04)
List back-link:    role link, name /Topics/i -> /deck/browse?deck=… (D-02)
Empty state: text "No words at this level." + role button name "Show all levels"
Toggle aria-labels: /Add … to deck/ and /Remove … from deck/   // PRESERVED (role-based, survive re-skin)

<!-- Helper + specs as they are TODAY (what to change). Sources read at plan time. -->
e2e/helpers.ts addWordsFromBrowser (lines 160-182): clicks data-testid="browse-words-empty" -> waitForURL(/deck/browse) -> waitForSelector('[aria-label*="Add"]') -> clicks N add buttons -> goto /dashboard.
  BREAKS: after the re-skin the landing shows tiles, so '[aria-label*="Add"]' never appears until a topic is opened.
e2e/03 (5 tests): getByText("Browse Words"), getByRole(button "Animals"/"Food"/"All"/"A1"), .locator(".border-b.border-border.py-2"). All land on /deck/browse then expect pills/rows there.
e2e/09 "browse words and add card links work from empty deck" (lines 17-32): getByText("Browse Words") + getByRole(link "Back to my deck") -> /dashboard.
e2e/10 "word list browser works on mobile" (lines 88-106): getByRole(button "Animals"); keeps the >=44px boundingBox assertion on '[aria-label*="Add"]'.
Helper signatures preserved: signUpWithDeck(page, "French"|"Spanish"), waitForCompilation(page). Playwright config: timeout 180s, workers 1, Chromium + Pixel 7.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the addWordsFromBrowser helper for the two-screen IA</name>
  <read_first>
    - e2e/helpers.ts — the full file; addWordsFromBrowser is lines 160-182. Preserve every other export (testEmail, waitForCompilation, signUpFreshUser, signIn, completeWelcomeFlow, signUpWithDeck) verbatim — other specs import them.
    - .planning/phases/23-browse-words/23-RESEARCH.md — §e2e audit ("Files where 'Browse words' link locator SURVIVES") and Pattern 2 (topic slug encoding).
    - src/components/word-list-browser.tsx (Plan 23-03) — confirm the landing renders topic-tile links and the list renders word rows + [aria-label*="Add"] toggles.
  </read_first>
  <action>
    Modify ONLY the `addWordsFromBrowser` function in `e2e/helpers.ts`. After it clicks `data-testid="browse-words-empty"`, `waitForURL(/\/deck\/browse/)`, and `waitForCompilation`, insert a topic drill-in step BEFORE waiting for add buttons: `await page.getByTestId("topic-tile-food-drink").click();` (Food & Drink is densely populated for the French pair), then `await page.waitForURL(/topic=/);` and `await waitForCompilation(page);`. Then keep the existing `waitForSelector('[aria-label*="Add"]')`, the add-N loop, and the `goto("/dashboard")` exactly as-is.

    Keep the function signature and ALL other helper exports unchanged. This single fix unblocks specs 02 / 09 / 10 that seed a deck via this helper.
  </action>
  <verify>
    <automated>npx biome check e2e/helpers.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: addWordsFromBrowser clicks a `topic-tile-` testid and `waitForURL(/topic=/)` BEFORE `waitForSelector('[aria-label*="Add"]')` (grep for topic-tile- and topic= in helpers.ts).
    - Source assertion: all other exports (testEmail, waitForCompilation, signUpFreshUser, signIn, completeWelcomeFlow, signUpWithDeck) are present and unchanged.
    - CLI: `npx biome check e2e/helpers.ts` clean.
    - Behavior (proven in Task 4's batch run): specs that call addWordsFromBrowser (e2e/09, e2e/10) successfully seed a deck.
  </acceptance_criteria>
  <done>addWordsFromBrowser drills into a topic before adding, so deck-seeding via Browse works under the two-screen IA; no other helper changed.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget e2e/03-word-list-browser.spec.ts to the two-screen IA + structural selectors</name>
  <read_first>
    - e2e/03-word-list-browser.spec.ts — all 5 tests (75 lines). Surviving role-based locators to KEEP: getByRole("link", { name: "Browse words" }).first() (Browse entry) and [aria-label*="Add"] / [aria-label*="Remove"] (toggle).
    - .planning/phases/23-browse-words/23-RESEARCH.md — §e2e audit table (every line number + exact retarget) AND "Strict-mode multi-match watch".
    - .planning/phases/23-browse-words/23-PATTERNS.md — section "e2e/03-word-list-browser.spec.ts" with the worked retarget for each test.
    - src/components/word-list-browser.tsx (Plan 23-03) — confirm the testids/roles actually emitted (browse-words-title, topic-tile-{slug}, word-row, LEVEL buttons, toggle aria-labels).
  </read_first>
  <action>
    Retarget all 5 tests in `e2e/03-word-list-browser.spec.ts`. Preserve the signUpWithDeck(page, "French") beforeEach and the surviving getByRole("link", { name: "Browse words" }).first() entry.

    Test 1 ("browse words page loads…"): replace getByText("Browse Words") with getByTestId("browse-words-title"). Replace category-pill assertions with tile assertions: expect(page.getByTestId("topic-tile-animals")).toBeVisible() and expect(page.getByTestId("topic-tile-food-drink")).toBeVisible(). Then drill in: await page.getByTestId("topic-tile-animals").click(); await page.waitForURL(/topic=/); then expect(page.getByRole("button", { name: "All" })).toBeVisible() and expect(page.getByRole("button", { name: "A1" })).toBeVisible().

    Test 2 ("can add a word…") + Test 3 ("can remove a word…"): after navigating to Browse, insert await page.getByTestId("topic-tile-food-drink").click(); await page.waitForURL(/topic=/); BEFORE addButton.waitFor. Keep the [aria-label*="Add"] / [aria-label*="Remove"] .first() assertions (they prove the optimistic toggle flip).

    Test 4 ("category filter changes visible words"): replace getByRole("button", { name: "Food" }).click() with await page.getByTestId("topic-tile-food-drink").click(); await page.waitForURL(/topic=Food/);. Replace .locator(".border-b.border-border.py-2") with page.locator('[data-testid="word-row"]'). Assert wordRows.first() visible.

    Test 5 ("difficulty filter works"): after navigating to Browse, drill into a topic (getByTestId("topic-tile-food-drink").click() + waitForURL(/topic=/)) BEFORE clicking the level. Keep getByRole("button", { name: "A1" }).click() (safe on the list screen). Replace .locator(".border-b.border-border.py-2") with page.locator('[data-testid="word-row"]').

    Add a 6th test for BRW-04 (D-09 empty state + reset): drill into a topic, click a level that yields no words for that topic for the French pair, assert page.getByText("No words at this level.") is visible, click page.getByRole("button", { name: "Show all levels" }), and assert a word-row reappears (or the "All" tile is active). If a guaranteed-empty topic+level combo cannot be asserted deterministically, choose one you verified empty by reading the French wordlist JSON (src/data/wordlists/en-fr.json) and document it in a code comment.

    Do NOT re-add any removed literal (getByText("Browse Words"), category-pill button names, the CSS class). Strict-mode watch: "All"/"A1" exist only on the list screen, so the drill-in must precede them.
  </action>
  <verify>
    <automated>npx biome check e2e/03-word-list-browser.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: e2e/03 contains NO getByText("Browse Words"), NO category-pill getByRole("button", { name: "Animals" | "Food" }), and NO .border-b.border-border.py-2 (grep confirms all three are gone).
    - Source assertion: e2e/03 uses getByTestId("browse-words-title"), getByTestId("topic-tile-animals"), getByTestId("topic-tile-food-drink"), data-testid="word-row", and at least one waitForURL(/topic=/) drill-in.
    - Source assertion: a 6th test asserts getByText("No words at this level.") and clicks getByRole("button", { name: "Show all levels" }) (BRW-04).
    - Source assertion: the surviving [aria-label*="Add"] / [aria-label*="Remove"] toggle assertions and the getByRole("link", { name: "Browse words" }).first() entry remain.
    - CLI: `npx biome check e2e/03-word-list-browser.spec.ts` clean.
    - Behavior: proven green in Task 4's batch run.
  </acceptance_criteria>
  <done>e2e/03 drives tiles -> topic -> level -> optimistic add/remove and the empty-state reset entirely via structural selectors; no removed copy or CSS-class locators remain.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget e2e/09 and e2e/10 off removed copy and category-pill selectors</name>
  <read_first>
    - e2e/09-language-breakdown.spec.ts — full file (45 lines). At-risk test: "browse words and add card links work from empty deck" (lines 17-32): getByText("Browse Words") (24) + getByRole("link", { name: "Back to my deck" }) (26). The other two tests use addWordsFromBrowser (fixed in Task 1) — do not change them.
    - e2e/10-mobile-responsive.spec.ts — full file. At-risk test: "word list browser works on mobile" (lines 88-106): getByRole("button", { name: "Animals" }) (94). Other tests use addWordsFromBrowser (fixed in Task 1) — do not change them. PRESERVE the >=44px boundingBox assertion on [aria-label*="Add"] (BRW-03 touch-target proof).
    - .planning/phases/23-browse-words/23-RESEARCH.md — §e2e audit rows for 09:24, 09:26, 10:94 + the "SURVIVES" table.
    - .planning/phases/23-browse-words/23-PATTERNS.md — section "e2e/09-language-breakdown.spec.ts" with the exact before/after.
  </read_first>
  <action>
    e2e/09 — in the "browse words and add card links work from empty deck" test ONLY: replace getByText("Browse Words") with getByTestId("browse-words-title"). The "Back to my deck" link no longer exists (D-04); replace the back-navigation with the new path: click page.getByRole("link", { name: /Add a card/i }) and waitForURL(/\/deck\/new-card/), then from new-card click page.getByRole("link", { name: /My deck/i }) (the ACTop "‹ My deck" escape) and waitForURL(/\/dashboard/). Keep the subsequent getByRole("link", { name: "Add a card" }) -> new-card -> getByTestId("add-card-title") assertion intact. Leave the other two e2e/09 tests unchanged.

    e2e/10 — in the "word list browser works on mobile" test ONLY: replace getByRole("button", { name: "Animals" }) with getByTestId("topic-tile-animals") for the landing assertion. Because the toggle now lives on the list screen, add a drill-in before the boundingBox assertion: await page.getByTestId("topic-tile-animals").click(); await page.waitForURL(/topic=/); await waitForCompilation(page); then keep the existing [aria-label*="Add"] .first() boundingBox >= 44 assertion and the scrollWidth <= MOBILE.width + 1 check. Leave all other e2e/10 tests unchanged. (Add the waitForCompilation import if not already imported in that file.)

    Do NOT re-add "Back to my deck", getByText("Browse Words"), or any category-pill button name. Strict-mode watch on "Add a card" (link) and "My deck" (link) — they resolve uniquely on their respective screens.
  </action>
  <verify>
    <automated>npx biome check e2e/09-language-breakdown.spec.ts e2e/10-mobile-responsive.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: e2e/09 no longer contains getByText("Browse Words") or "Back to my deck"; it uses getByTestId("browse-words-title") and navigates back via the Add-a-card -> My deck path.
    - Source assertion: e2e/10 no longer contains getByRole("button", { name: "Animals" }); it uses getByTestId("topic-tile-animals") and retains the boundingBox >= 44 toggle assertion after a waitForURL(/topic=/) drill-in.
    - Source assertion: the unrelated tests in 09 and 10 (those using addWordsFromBrowser, the dashboard-header test, the study test, the card-management test) are unchanged.
    - CLI: `npx biome check` clean on both files.
    - Behavior: proven green in Task 4's batch run.
  </acceptance_criteria>
  <done>e2e/09 and e2e/10 use the new structural selectors and navigation; no removed copy remains; touch-target and breakdown intents preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Run the Browse e2e batch against a fresh dev server and confirm green</name>
  <read_first>
    - playwright.config.ts — confirm the dev-server / baseURL setup (timeout 180s, workers 1, Chromium + Pixel 7). Determine whether the config auto-starts the dev server (webServer) or expects an already-running server.
    - .planning/phases/23-browse-words/23-VALIDATION.md — §Sampling Rate: "restart a fresh dev server before the e2e batch — project lesson."
  </read_first>
  <action>
    Ensure a clean dev server backs the e2e run (project lesson: a stale HMR dev server causes flaky Browse e2e). If playwright.config.ts defines a webServer that boots its own server, rely on it (kill any stray dev server on the same port first). Otherwise start a fresh dev server, wait until ready, then run the batch.

    Run the affected specs: `npx playwright test e2e/02-first-visit-deck-creation.spec.ts e2e/03-word-list-browser.spec.ts e2e/09-language-breakdown.spec.ts e2e/10-mobile-responsive.spec.ts` (e2e/02 is helper-dependent — Task 1 changed its seeding path, so include it). Investigate and fix any failure that traces to a selector THIS plan changed. Do NOT modify component behavior (owned by 23-02/23-03); if a failure reveals a genuine component gap, record it as a blocker in the SUMMARY rather than patching the spec to pass vacuously.

    Then run the full unit suite once to confirm no regression: `npx vitest run`.
  </action>
  <verify>
    <automated>npx playwright test e2e/02-first-visit-deck-creation.spec.ts e2e/03-word-list-browser.spec.ts e2e/09-language-breakdown.spec.ts e2e/10-mobile-responsive.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the four specs (02, 03, 09, 10) pass against a freshly (re)started dev server — no failures, no strict-mode multi-match errors.
    - Behavior: `npx vitest run` is fully green (all phase rendered-component tests + the pre-existing suite).
    - CLI: the Playwright run exits 0; any flake is re-run once and confirmed stable.
    - Manual fallback (only if a deterministic empty-state combo is impossible in e2e): the BRW-04 empty state + reset is verified via the bw-atoms rendered-component test (Plan 23-03 Task 3) and noted as covered there.
  </acceptance_criteria>
  <done>The Browse e2e batch + the full unit suite are green against a fresh dev server; the re-skinned two-screen flow, optimistic add/remove, level filtering, and empty-state reset are verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes only e2e test specs + a test helper. No production trust boundary is touched; no untrusted input, network, DB, auth, or filesystem write path is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-COV | Repudiation / test integrity | e2e specs assert behavior they no longer truly exercise | mitigate | Retargets preserve the BEHAVIORAL intent of each test (add/remove toggle, level filter, touch target, navigation) — only the locators change. The Task 4 batch run proves the assertions still fire against the real re-skinned UI; selectors are never loosened to pass vacuously. L-06: no removed copy is re-added. |
| T-23-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH.md §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx biome check` clean on all four touched files.
- `npx playwright test` green for e2e/02, e2e/03, e2e/09, e2e/10 against a fresh dev server.
- `npx vitest run` fully green (no regression).
- Grep confirms no remaining "Back to my deck", getByText("Browse Words"), category-pill getByRole("button", { name: "Animals"|"Food" }), or .border-b.border-border.py-2 in the e2e directory.
</verification>

<success_criteria>
- The shared addWordsFromBrowser helper navigates the two-screen IA; deck-seeding specs work again.
- e2e/03 verifies BRW-01/02/03/04 (tiles, topic nav, level filter, optimistic add/remove, empty-state reset) with structural selectors.
- e2e/09 and e2e/10 retargeted off removed copy; touch-target + breakdown intents preserved.
- The Browse e2e batch + full unit suite are green against a fresh dev server (L-06 fully closed for this phase).
</success_criteria>

<output>
Create `.planning/phases/23-browse-words/23-04-SUMMARY.md` when done.
</output>
