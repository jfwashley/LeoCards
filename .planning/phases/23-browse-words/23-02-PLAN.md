---
phase: 23-browse-words
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/daybreak/ac-top.tsx
  - src/components/new-card-mode-toggle.tsx
  - src/components/daybreak/__tests__/ac-atoms.test.tsx
autonomous: true
requirements: [BRW-01]
must_haves:
  truths:
    - "On the Add-a-Card landing in type-a-word mode, a 'Browse words ›' link appears top-right and points to /deck/browse?deck={activeDeckId}"
    - "The 'Browse words ›' link is absent when ACTop is rendered without a browsePath (image stepper context)"
    - "The existing '‹ My deck' left escape link and the 'Add a Card' centered title (data-testid=add-card-title) are unchanged"
  artifacts:
    - path: "src/components/daybreak/ac-top.tsx"
      provides: "ACTop with optional browsePath prop → conditional top-right 'Browse words ›' link (D-03)"
      exports: ["ACTop"]
      contains: "browsePath"
    - path: "src/components/new-card-mode-toggle.tsx"
      provides: "Threads browsePath to ACTop only in type mode, computed from activeDeckId"
      contains: "browse?deck="
  key_links:
    - from: "src/components/new-card-mode-toggle.tsx"
      to: "src/components/daybreak/ac-top.tsx browsePath"
      via: "mode === 'type' ? `/deck/browse?deck=${activeDeckId}` : undefined"
      pattern: "browsePath"
    - from: "src/components/daybreak/ac-top.tsx"
      to: "/deck/browse"
      via: "next/link Link href={browsePath}"
      pattern: "data-testid=\"browse-words-link\""
---

<objective>
Add the D-03 "Browse words ›" entry link to the Add-a-Card landing header: a right-side link on `ACTop` that mirrors the existing left "‹ My deck" escape, shown only on the type-a-word landing (not during the image stepper), targeting `/deck/browse?deck={activeDeckId}`. Add rendered-component test coverage for both the present and absent cases.

Purpose: Browse was removed from the populated dashboard action line in Phase 21 (L-05), so active users currently cannot reach Browse. D-03 routes the entry through Add-a-Card, making Add-a-Card and Browse the two co-located "build your deck" paths. This is the primary entry point for BRW-01's landing.

Output: `ac-top.tsx` (optional `browsePath` prop + conditional link), `new-card-mode-toggle.tsx` (threads `browsePath` in type mode), `ac-atoms.test.tsx` (new D-03 cases appended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-browse-words/23-CONTEXT.md
@.planning/phases/23-browse-words/23-PATTERNS.md

<interfaces>
<!-- Current ACTop (no props). Source: src/components/daybreak/ac-top.tsx -->
export function ACTop()  // renders: left "‹ My deck" Link → /dashboard ; centered span data-testid="add-card-title" "Add a Card" ; right spacer <span style={{ flex: "none", width: 60 }} />
Left link style (mirror for the right link): { fontSize: 14, fontWeight: 600, color: "#C96F12", textDecoration: "none", display: "inline-flex", alignItems: "center", flex: "none" }
`import Link from "next/link";` is already present.

<!-- Current NewCardModeToggle. Source: src/components/new-card-mode-toggle.tsx -->
props include `activeDeckId: string`. Local state: `const [mode, setMode] = useState<"type" | "image">("type")`. It renders `<ACTop />` unconditionally at the top (line 33), then `<ACContext .../>`, `<ACSeg mode onChange />`, then the type/image branch. ImageUploadFlow owns its own ACStepper header for steps ≥ Confirm; the image "pick" step shows this same ACTop (verified — PATTERNS.md Assumption Resolution A2).

<!-- Existing test pattern. Source: src/components/daybreak/__tests__/ac-atoms.test.tsx -->
Uses screen.getByTestId / screen.queryByTestId, render(<ACTop .../>), afterEach(cleanup).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional browsePath prop + conditional 'Browse words ›' link to ACTop (D-03)</name>
  <read_first>
    - src/components/daybreak/ac-top.tsx — the full current component (47 lines). Preserve the left link and the title span verbatim.
    - .planning/phases/23-browse-words/23-PATTERNS.md — section "src/components/daybreak/ac-top.tsx" for the exact prop shape and replacement of the right spacer.
    - .planning/phases/23-browse-words/23-RESEARCH.md — Pattern 3 (D-03 link on ACTop) for the rationale and the testid.
  </read_first>
  <action>
    Modify `src/components/daybreak/ac-top.tsx`. Add an interface `ACTopProps` with one optional field: `browsePath?: string` (JSDoc: "When provided, renders 'Browse words ›' at top-right — landing/Pick only; omitted during the image stepper."). Change the signature to `export function ACTop({ browsePath }: ACTopProps)`.

    Replace ONLY the current right spacer (`<span style={{ flex: "none", width: 60 }} />`) with a conditional: when `browsePath` is truthy, render a `next/link` `<Link href={browsePath} data-testid="browse-words-link">` whose label is the text `Browse words ›` (use the `›` glyph `&#8250;` or literal "›" — a right single angle quote, mirroring the left link's `&#8249;`). Style it to mirror the left link: `{ fontSize: 14, fontWeight: 600, color: "#C96F12", textDecoration: "none", display: "inline-flex", alignItems: "center", flex: "none" }`. When `browsePath` is falsy, render the original spacer `<span style={{ flex: "none", width: 60 }} />` unchanged (keeps the title visually centered during the stepper).

    Do NOT change the left "‹ My deck" Link or the `data-testid="add-card-title"` title span — e2e/04 and e2e/09 depend on the title testid. Keep the outer flex container (`justifyContent: "space-between"`) as-is.

    Update the file header comment to note the optional right-side "Browse words ›" link (D-03).
  </action>
  <verify>
    <automated>npx biome check src/components/daybreak/ac-top.tsx && npx tsc --noEmit 2>&1 | (grep "ac-top" || echo "no ac-top type errors")</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ACTop` accepts `browsePath?: string` and renders a `next/link` `Link` with `data-testid="browse-words-link"` and `href={browsePath}` only when `browsePath` is truthy (grep for `browse-words-link` and `browsePath`).
    - Source assertion: the link label contains the text `Browse words` and a trailing right-angle glyph; the right link color is `#C96F12` (matches the left link).
    - Source assertion: the `data-testid="add-card-title"` span and the left `&#8249; My deck` link are still present and unchanged.
    - CLI: `npx biome check src/components/daybreak/ac-top.tsx` clean; `npx tsc --noEmit` shows no `ac-top` type error.
  </acceptance_criteria>
  <done>ACTop renders the right-side Browse-words link when given browsePath, falls back to the centering spacer otherwise, and leaves the left link + title untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Thread browsePath from NewCardModeToggle to ACTop in type mode only (D-03)</name>
  <read_first>
    - src/components/new-card-mode-toggle.tsx — the full current component. `activeDeckId` is already a prop; `mode` state already exists; `<ACTop />` is rendered at line 33.
    - src/components/daybreak/ac-top.tsx — the updated ACTop signature from Task 1 (`browsePath?: string`).
    - .planning/phases/23-browse-words/23-PATTERNS.md — section "src/components/new-card-mode-toggle.tsx" (Assumption A2 verified: pass browsePath only when mode === "type").
  </read_first>
  <action>
    Modify `src/components/new-card-mode-toggle.tsx`. Change the `<ACTop />` render (line 33) to `<ACTop browsePath={mode === "type" ? \`/deck/browse?deck=${activeDeckId}\` : undefined} />`.

    Rationale to honor (D-03 + A2): the image "pick" step also shows this ACTop, but "Browse words" is meaningful only from the type-a-word landing; passing `browsePath` only when `mode === "type"` is the conservative, correct interpretation and needs no inspection of ImageUploadFlow's internal step state. Make no other changes — all other props, the ACContext, ACSeg, and the type/image branch render logic are preserved verbatim. Do NOT add `?topic=` (Browse opens at the tiles landing).
  </action>
  <verify>
    <automated>npx biome check src/components/new-card-mode-toggle.tsx && npx tsc --noEmit 2>&1 | (grep "new-card-mode-toggle" || echo "no new-card-mode-toggle type errors")</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the `<ACTop ... />` render passes `browsePath={mode === "type" ? \`/deck/browse?deck=${activeDeckId}\` : undefined}` (grep for `browse?deck=` and `mode === "type"`).
    - Source assertion: no other lines of `new-card-mode-toggle.tsx` changed (ACContext, ACSeg, TranslationForm, ImageUploadFlow props all intact).
    - CLI: `npx biome check src/components/new-card-mode-toggle.tsx` clean; `npx tsc --noEmit` shows no `new-card-mode-toggle` type error.
  </acceptance_criteria>
  <done>In type mode the Add-a-Card header shows "Browse words ›" → /deck/browse?deck={activeDeckId}; in image mode the prop is undefined and the link is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add D-03 ACTop browsePath rendered-component test cases</name>
  <read_first>
    - src/components/daybreak/__tests__/ac-atoms.test.tsx — the existing file. Append a new `describe` block; do not modify existing blocks. Note the `// @vitest-environment jsdom` docblock + `afterEach(cleanup)` are already present.
    - src/components/daybreak/ac-top.tsx — the updated ACTop (Task 1): `browsePath?: string`, `data-testid="browse-words-link"`.
    - .planning/phases/23-browse-words/23-PATTERNS.md — the exact ACTop test cases to add (present + absent).
  </read_first>
  <action>
    Append to `src/components/daybreak/__tests__/ac-atoms.test.tsx`: add `import { ACTop } from "@/components/daybreak/ac-top";` to the import block (keep biome import ordering), then a new `describe("ACTop — D-03 Browse words entry link", ...)` with two `it`s: (1) `render(<ACTop browsePath="/deck/browse?deck=abc" />)` then assert `screen.getByTestId("browse-words-link")` is truthy AND its `getAttribute("href")` equals `"/deck/browse?deck=abc"`; (2) `render(<ACTop />)` then assert `screen.queryByTestId("browse-words-link")` is `null`. Optionally add a third `it` asserting the title `screen.getByTestId("add-card-title")` is still present when `browsePath` is provided (regression guard that the left/title structure survived).

    Do NOT use watch mode.
  </action>
  <verify>
    <automated>npx vitest run src/components/daybreak/__tests__/ac-atoms.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npx vitest run src/components/daybreak/__tests__/ac-atoms.test.tsx` exits 0; the new D-03 block passes AND all pre-existing ACSeg/ACBtn/ACBanner/ACReviewRow/ACPairRow/ACProgress tests still pass (no regression).
    - Source assertion: the file contains `describe("ACTop — D-03 Browse words entry link"` with both a present-link and an absent-link assertion (grep for `browse-words-link` in the test file).
    - CLI: `npx biome check src/components/daybreak/__tests__/ac-atoms.test.tsx` clean.
  </acceptance_criteria>
  <done>ac-atoms.test.tsx covers ACTop showing the link with the correct href when browsePath is set and hiding it when omitted; full ac-atoms suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → in-app navigation | The new `<Link>` target is built from `activeDeckId`, which is server-provided to `NewCardModeToggle` (the user's own active deck). No untrusted input is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-IDOR | Elevation/Access | `browsePath` deck id in the link | accept | `activeDeckId` is the server-resolved active deck for the authenticated user; the link only navigates to `/deck/browse?deck=…`, whose server page (preserved) re-resolves the deck against the session and redirects if it is not the user's. No new authorization surface. |
| T-23-02-OPENREDIR | Tampering | `Link href={browsePath}` | accept | `browsePath` is constructed internally as a fixed `/deck/browse?deck=${activeDeckId}` template, never from user input — no open-redirect vector. |
| T-23-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH.md §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx vitest run src/components/daybreak/__tests__/ac-atoms.test.tsx` green (new + existing cases).
- `npx biome check` clean on all three touched files.
- `npx tsc --noEmit` shows no new type errors in touched files.
- Note: the e2e proof that the link survives `.first()` disambiguation against the empty-deck card-list link is owned by Plan 23-04 (it touches the e2e specs).
</verification>

<success_criteria>
- D-03 "Browse words ›" link appears top-right on the Add-a-Card type-a-word landing, points to `/deck/browse?deck={activeDeckId}`, and is absent when `browsePath` is omitted.
- The existing "‹ My deck" escape and "Add a Card" title are unchanged.
- Rendered-component tests cover both the present and absent cases; the full ac-atoms suite is green.
</success_criteria>

<output>
Create `.planning/phases/23-browse-words/23-02-SUMMARY.md` when done.
</output>
