---
phase: 23-browse-words
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/daybreak/bw-medallion.tsx
  - src/components/daybreak/__tests__/bw-atoms.test.tsx
autonomous: true
requirements: [BRW-01]
must_haves:
  truths:
    - "A BWMedallion renders a geometric amber topic icon for every one of the 14 CATEGORIES without crashing"
    - "The medallion is RSC-safe (pure, no hooks, aria-hidden) — usable from a server-rendered tile and a client list alike"
    - "A rendered-component test file exists and passes, covering the medallion for all 14 categories"
  artifacts:
    - path: "src/components/daybreak/bw-medallion.tsx"
      provides: "BWMedallion atom + 14 CSS-art TopicIcon glyphs keyed by category name (D-08)"
      exports: ["BWMedallion"]
      contains: "Greetings"
    - path: "src/components/daybreak/__tests__/bw-atoms.test.tsx"
      provides: "Wave 0 rendered-component test scaffold (jsdom) — BWMedallion coverage; extended by Plan 23-03"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "src/components/daybreak/bw-medallion.tsx"
      to: "src/data/wordlists/schema.ts CATEGORIES"
      via: "icon map keyed by the 14 canonical category names"
      pattern: "Days & Months"
---

<objective>
Create the `BWMedallion` Daybreak atom: a rounded amber-tinted medallion containing one of 14 geometric, single-weight amber topic icons (D-08), recreated faithfully from the mock's `TOPIC_ICON` map as RSC-safe CSS-drawn placeholders. Also create the Wave 0 rendered-component test file that the rest of the phase's Nyquist coverage builds on.

Purpose: BRW-01's topic-tiles landing needs a per-category icon-on-medallion. This atom is consumed by the topic tiles AND the list-screen mini header (Plan 23-03). Building it first unblocks the core Browse re-skin and gives Plan 23-03 a tested dependency to import.

Output: `src/components/daybreak/bw-medallion.tsx` (new atom) + `src/components/daybreak/__tests__/bw-atoms.test.tsx` (new test scaffold covering the medallion; Plan 23-03 extends the same file with Browse-screen-atom cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-browse-words/23-CONTEXT.md
@.planning/phases/23-browse-words/23-PATTERNS.md

<interfaces>
<!-- The 14 canonical category names this atom must key on (display order). Source: src/data/wordlists/schema.ts -->
CATEGORIES = ["Greetings","Numbers","Colors","Days & Months","Food & Drink","Family","Body","Animals","Clothing","Home","Weather","Shopping","Travel","Work"]

<!-- Analog: the existing RSC-safe CSS-art component to copy structurally. Source: src/components/daybreak/lion-face.tsx -->
LionFace: pure function component (no hooks). Root is `<div style={{ width, height, position: "relative", flex: "none" }} aria-hidden="true">` with absolutely-positioned child divs. No imports beyond React types.

<!-- Existing test file pattern. Source: src/components/daybreak/__tests__/ac-atoms.test.tsx line 1-12 -->
First line MUST be the docblock: // @vitest-environment jsdom
Imports: { cleanup, fireEvent, render, screen } from "@testing-library/react"; { afterEach, describe, expect, it, vi } from "vitest";
afterEach(() => cleanup());
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the BWMedallion atom with all 14 CSS-art topic icons (D-08)</name>
  <read_first>
    - design/handoff-daybreak/daybreak-browse.jsx — THE VISUAL CONTRACT. Lines 6-28 are the `TOPIC_ICON` map (14 glyphs) + `STK`, `TG`, `line`, `ring` helpers; lines 52-59 are `BWMedallion`. Recreate these 1:1.
    - src/components/daybreak/lion-face.tsx — the analog RSC-safe CSS-art pattern: position:relative root, absolutely-positioned children, aria-hidden, no hooks, no non-React imports.
    - src/data/wordlists/schema.ts — the canonical `CATEGORIES` (14 names, exact strings incl. "Days & Months" and "Food & Drink").
    - .planning/phases/23-browse-words/23-PATTERNS.md — section "src/components/daybreak/bw-medallion.tsx" for the exact translation rules (ring/line helpers, token literals, the STk2 typo note).
  </read_first>
  <action>
    Create `src/components/daybreak/bw-medallion.tsx` as a pure RSC-safe module (NO "use client", no hooks), exporting `BWMedallion`.

    Translate the mock's helpers to typed TS inline-style helpers: define `const STK = 2.2;` and `const ICON_COLOR = "#F28A1F";` (the mock's `bt.primary`); `r(s: React.CSSProperties): React.CSSProperties` returning `{ position: "absolute", border: \`${STK}px solid ${ICON_COLOR}\`, boxSizing: "border-box", ...s }` (the mock's `ring`); `l(s: React.CSSProperties): React.CSSProperties` returning `{ position: "absolute", background: ICON_COLOR, borderRadius: 2, ...s }` (the mock's `line`).

    Build an internal `TopicIcon({ name }: { name: string })` that renders a `<div style={{ position: "relative", width: 27, height: 26, flex: "none" }}>` (the mock's `TG`) containing the per-category glyph. Port ALL 14 entries from the mock's `TOPIC_ICON` map exactly (Greetings, Numbers, Colors, Days & Months, Food & Drink, Family, Body, Animals, Clothing, Home, Weather, Shopping, Travel, Work) using `r()`/`l()` and literal inline-style divs for the bespoke shapes (Clothing's `clipPath`, Travel's teardrop `borderRadius`+`rotate`, Home/Greetings border-triangles, etc.). Substitutions when porting: mock `bt.primary` → `ICON_COLOR`; mock `bt.surface` → `"#FFFFFF"` (Travel inner circle); mock `bt.fontDisplay` → `"var(--font-display)"` (Numbers "123" span); the `STk2()` call in "Days & Months" is a mock typo for `STK` — inline `STK` (= 2.2). Implement the icon lookup as a `Record<string, React.ReactNode>` or a `switch` keyed by `name`; if `name` is unknown, render nothing inside the medallion (no crash — defensive for an unexpected category).

    Export `BWMedallion({ name, size = 52 }: { name: string; size?: number })`: a `<div style={{ width: size, height: size, borderRadius: 16, background: "#FFF1DC", display: "flex", alignItems: "center", justifyContent: "center", flex: "none" }} aria-hidden="true">` wrapping `<TopicIcon name={name} />`. (`#FFF1DC` is the mock's `tint`/`bt.pillBg`.) Put `aria-hidden="true"` on the medallion container (mirrors LionFace) — the tile/header that consumes it supplies the accessible name.

    File header comment (mirror lion-face.tsx style): note it is ported 1:1 from `design/handoff-daybreak/daybreak-browse.jsx` `TOPIC_ICON`, that it is a CSS placeholder per PROJECT.md (swappable later, keep the palette), and that it is pure/RSC-safe.

    Do NOT import any icon library (lucide-react etc.) — these are CSS-drawn divs only (L-01: no emoji, no flag glyphs).
  </action>
  <verify>
    <automated>npx biome check src/components/daybreak/bw-medallion.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | (grep "bw-medallion" || echo "no bw-medallion type errors")</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/components/daybreak/bw-medallion.tsx` exports `BWMedallion` and contains no `"use client"` directive and no React hook calls (no `useState`/`useEffect`/`useRef`/`useMemo`).
    - Source assertion: the file contains a glyph branch for each of the 14 exact category strings — verify with `grep -F` for "Greetings", "Days & Months", "Food & Drink", "Numbers", "Work" (all present).
    - Source assertion: the file imports no icon library and contains no emoji or flag characters (no `lucide-react` import).
    - Source assertion: `BWMedallion` container style sets `background` to `"#FFF1DC"`, `borderRadius: 16`, and `aria-hidden="true"`.
    - CLI: `npx biome check src/components/daybreak/bw-medallion.tsx` reports no errors.
    - CLI: `npx tsc --noEmit` produces no type error mentioning `bw-medallion`.
  </acceptance_criteria>
  <done>BWMedallion renders an amber medallion + correct geometric glyph for all 14 categories; pure, RSC-safe, biome-clean, type-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the Wave 0 rendered-component test scaffold for BWMedallion</name>
  <read_first>
    - src/components/daybreak/__tests__/ac-atoms.test.tsx — the EXACT test pattern: `// @vitest-environment jsdom` first line, `@testing-library/react` + vitest imports, `afterEach(() => cleanup())`, `describe`/`it` blocks.
    - src/components/daybreak/bw-medallion.tsx — the component under test (created in Task 1): props `{ name, size? }`, `aria-hidden` container.
    - src/data/wordlists/schema.ts — `CATEGORIES` to iterate in the "all 14" test.
    - .planning/phases/23-browse-words/23-VALIDATION.md — the Nyquist contract this file partially satisfies (BRW-01 rendered-component row).
  </read_first>
  <action>
    Create `src/components/daybreak/__tests__/bw-atoms.test.tsx`. First line MUST be `// @vitest-environment jsdom`. Import `{ cleanup, render }` from `@testing-library/react` and `{ afterEach, describe, expect, it }` from `vitest`, plus `BWMedallion` from `@/components/daybreak/bw-medallion` and `CATEGORIES` from `@/data/wordlists/schema`. Add `afterEach(() => cleanup())`.

    Write a `describe("BWMedallion — topic icon container", ...)` with: (1) an `it` that iterates `CATEGORIES` and calls `render(<BWMedallion name={cat} />)` inside the loop, asserting `render` does not throw and the container has at least one child element for each (e.g. `expect(container.firstChild).toBeTruthy()`); use `cleanup()` between iterations or render into separate containers to avoid bleed. (2) an `it` that renders `<BWMedallion name="Food & Drink" size={26} />` and asserts the rendered medallion root element's inline style `borderRadius` is `"16px"` (jsdom normalizes `16` → `"16px"`) and that it carries `aria-hidden="true"` (query via `container.querySelector('[aria-hidden="true"]')`). (3) an `it` that renders `<BWMedallion name="not-a-real-category" />` and asserts it does NOT throw (defensive unknown-name path).

    Add a top-of-file comment noting this file is the phase's shared Browse rendered-component test scaffold and that Plan 23-03 extends it with `BWWordRow`/`BWLevels`/`BrowseEmpty` cases — do not duplicate the file.

    Do NOT use watch mode. Tests must run under `npx vitest run`.
  </action>
  <verify>
    <automated>npx vitest run src/components/daybreak/__tests__/bw-atoms.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npx vitest run src/components/daybreak/__tests__/bw-atoms.test.tsx` exits 0 with all BWMedallion tests passing.
    - Source assertion: first line of the file is exactly `// @vitest-environment jsdom`.
    - Source assertion: the file contains an iteration over `CATEGORIES` rendering `BWMedallion` for each (grep for `CATEGORIES` and `BWMedallion`).
    - Behavior: the "all 14 categories" test renders 14 times with no thrown error; the unknown-name test renders without throwing.
    - CLI: `npx biome check src/components/daybreak/__tests__/bw-atoms.test.tsx` reports no errors.
  </acceptance_criteria>
  <done>bw-atoms.test.tsx exists, runs green under `vitest run`, and covers all 14 medallions + the unknown-name guard. Scaffold ready for Plan 23-03 to extend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a pure presentational CSS-art atom + a test file. No untrusted input crosses any boundary; no network, DB, auth, or filesystem access is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-XSS | Tampering/Info-disclosure | `BWMedallion` `name` prop rendered into the icon lookup | accept | `name` is used only as a `Record`/`switch` key to select a static glyph; it is never interpolated into HTML, `dangerouslySetInnerHTML`, or a style sink. React escapes any text. Unknown names render nothing. No exposure. |
| T-23-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan (RESEARCH.md §Package Legitimacy Audit: not applicable — zero new deps). |
</threat_model>

<verification>
- `npx vitest run src/components/daybreak/__tests__/bw-atoms.test.tsx` green.
- `npx biome check src/components/daybreak/bw-medallion.tsx src/components/daybreak/__tests__/bw-atoms.test.tsx` clean.
- `npx tsc --noEmit` shows no new type errors in the touched files.
</verification>

<success_criteria>
- BWMedallion renders a correct geometric amber glyph for all 14 CATEGORIES, RSC-safe and biome/type clean.
- The Wave 0 rendered-component test scaffold exists and passes, ready for Plan 23-03 to extend.
</success_criteria>

<output>
Create `.planning/phases/23-browse-words/23-01-SUMMARY.md` when done.
</output>
