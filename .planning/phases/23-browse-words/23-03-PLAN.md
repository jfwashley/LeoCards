---
phase: 23-browse-words
plan: 03
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/app/(protected)/deck/browse/page.tsx
  - src/components/word-list-browser.tsx
  - src/components/daybreak/__tests__/bw-atoms.test.tsx
autonomous: true
requirements: [BRW-01, BRW-02, BRW-03, BRW-04]
must_haves:
  truths:
    - "Visiting /deck/browse?deck={id} (no topic) renders the Daybreak tiles landing: '‹ Add a card' back-link, 'Browse Words' title, context line, 'Pick a topic', and a 14-tile grid each showing a medallion + real per-pair word count"
    - "Clicking a topic tile navigates to /deck/browse?deck={id}&topic={Category} and renders that topic's Daybreak word list: '‹ Topics' back-link, mini-medallion topic header, context line, a LEVEL tile row (All/A1/A2/B1), and Row-A word rows"
    - "Each word row shows native bold on top, target-language code + target beneath, a CEFR chip, and a 38px circular toggle (outlined + to add / filled amber check in deck); in-deck rows have a warm tint"
    - "Add/remove is optimistic and instant: the toggle flips immediately and shows an amber spinner while saving; on failure it reverts and shows 'Failed. Try again.' inline in reserved space (no layout shift, scroll preserved), auto-clearing after 3s"
    - "A topic+level combination with no words shows LionFace + 'No words at this level.' + contextual subtext + a 'Show all levels' button that resets the CEFR filter to All on the same topic"
  artifacts:
    - path: "src/app/(protected)/deck/browse/page.tsx"
      provides: "Server entry reading ?topic= alongside ?deck=, computing per-category counts, branching to tiles vs list (D-01/D-07)"
      contains: "topic"
    - path: "src/components/word-list-browser.tsx"
      provides: "BrowseTiles (landing) + BrowseList (word list) Daybreak screens; optimistic add/remove machine preserved verbatim"
      exports: ["BrowseTiles", "BrowseList"]
      contains: "word-row"
    - path: "src/components/daybreak/__tests__/bw-atoms.test.tsx"
      provides: "Extended rendered-component coverage: BWWordRow states (in-deck tint, reserved error line) + empty-state reset (BRW-03/04)"
      contains: "word-row"
  key_links:
    - from: "src/app/(protected)/deck/browse/page.tsx"
      to: "src/components/word-list-browser.tsx BrowseTiles/BrowseList"
      via: "requestedTopic ? BrowseList : BrowseTiles"
      pattern: "requestedTopic"
    - from: "src/components/word-list-browser.tsx"
      to: "src/components/daybreak/bw-medallion.tsx"
      via: "BWMedallion in tiles + list header"
      pattern: "BWMedallion"
    - from: "src/components/word-list-browser.tsx"
      to: "src/lib/deck-actions.ts addWordToCard/removeWordFromDeck"
      via: "preserved optimistic handleAdd/handleRemove via useTransition"
      pattern: "addWordToCard"
    - from: "BrowseTiles topic tile"
      to: "/deck/browse?deck=&topic="
      via: "next/link with encodeURIComponent(category)"
      pattern: "encodeURIComponent"
---

<objective>
Re-skin the Browse Words experience to Daybreak and adopt the locked two-screen IA: a topic-tiles landing then a per-topic word list with a CEFR level-tile row. The single `/deck/browse` route gains a `?topic=` query param read server-side (no param = tiles, `?topic={Category}` = list), with per-category counts computed from real word-list data. The word-list optimistic add/remove state machine is PRESERVED VERBATIM; only the markup and the navigation structure change.

Purpose: BRW-01/02/03/04. This is the heart of the phase — the visual + IA re-skin that turns today's single-screen pill-row browser into the Daybreak tiles-to-list drill-down with Row-A word rows and scroll-stable optimistic toggles.

Output: `page.tsx` (?topic= + counts + branch), `word-list-browser.tsx` (BrowseTiles + BrowseList screens, preserve state logic), `bw-atoms.test.tsx` (extended with word-row + empty-state cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-browse-words/23-CONTEXT.md
@.planning/phases/23-browse-words/23-RESEARCH.md
@.planning/phases/23-browse-words/23-PATTERNS.md

<interfaces>
<!-- AGENTS.md MANDATE: before editing page.tsx / routing, read node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md — searchParams is a Promise in Next.js 16 (this page already awaits it). -->

<!-- Data layer — PRESERVE, do not modify. Source: src/lib/wordlist.ts, src/data/wordlists/schema.ts -->
getWordList(sourceLang, targetLang): Promise<WordList>   // WordList = { sourceLang, targetLang, words: WordEntry[] }
filterWords(words, { category?: Category; cefr?: CefrLevel }): WordEntry[]   // synchronous
WordEntry = { id: string; category: string; cefr: "A1"|"A2"|"B1"; native: string; target: string }
CATEGORIES = ["Greetings","Numbers","Colors","Days & Months","Food & Drink","Family","Body","Animals","Clothing","Home","Weather","Shopping","Travel","Work"]

<!-- Server actions — PRESERVE, do not modify. Source: src/lib/deck-actions.ts -->
addWordToCard(deckId, wordId, front=native, back=target)   // inserts source="wordlist" card; revalidatePath("/dashboard")
removeWordFromDeck(deckId, front, back)                     // deletes matching source="wordlist" card
getDeckCardWords(deckId): Set<"native::target">            // in-deck detection (source="wordlist" only — DO NOT "fix")

<!-- Atoms to compose. Sources in src/components/daybreak/ -->
BWMedallion({ name, size? })          // Plan 23-01 — amber medallion + topic glyph
LangChip({ code, size? })             // existing — text chip "EN"/"FR"/"ES"
LionFace({ size?, mane?, face?, ... })// existing — empty-state mascot (L-01)
TBtn                                  // existing primary button — usable for "Show all levels"

<!-- Current component being rewritten. Source: src/components/word-list-browser.tsx -->
Current export: WordListBrowser({ words, existingWords, deckId, nativeLangLabel, targetLangLabel }).
Optimistic machine lives at lines 43-147 (deckWords/loadingWords/errorWords Sets+Map, useTransition, handleAdd/handleRemove, 3s auto-clear) — COPY VERBATIM into BrowseList.
wordKey(word) = `${word.native}::${word.target}` (line 21) — preserve.
The current error-display anti-pattern is lines 307-312 (position:absolute; -bottom-4) — REPLACE with reserved inline space (D-06).

<!-- Daybreak token literals (mock bt = window.d1Theme). Source: 23-RESEARCH.md "Code Examples" + 23-PATTERNS.md "Shared Patterns" -->
AMBER #F28A1F · INK #4A331C · MUTED #9C8467 · LINK #C96F12 · TINT #FFF1DC · CARD_BG #FFFFFF
CARD_BORDER 1px solid #EDDFC9 · FIELD_BORDER 1px solid #EDDFC9 · FONT_DISPLAY var(--font-display)
in-deck row: bg #FFF7E9 + border 1px solid #F4E3C4 + radius 14 · CEFR chip: #B4762A on #FFF1DC, radius 6, pad "2px 7px" · error text #DE5F4A
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ?topic= reading + per-category counts + tiles/list branch to the browse server page (D-01/D-07)</name>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md — AGENTS.md mandate: confirm the Next.js 16 searchParams Promise + await searchParams contract before editing this page.
    - src/app/(protected)/deck/browse/page.tsx — the full current page (69 lines). Preserve auth, the decks.length===0 redirect, the !activeDeck redirect, the getWordList/getDeckCardWords Promise.all, and the LANGUAGE_LABELS map.
    - .planning/phases/23-browse-words/23-PATTERNS.md — section "src/app/(protected)/deck/browse/page.tsx" for the exact imports, the categoryCounts computation, and the branching return.
    - src/lib/wordlist.ts + src/data/wordlists/schema.ts — filterWords + CATEGORIES signatures.
  </read_first>
  <action>
    Modify `src/app/(protected)/deck/browse/page.tsx`. Extend the props interface to `searchParams: Promise<{ deck?: string; topic?: string }>` and read `const requestedTopic = params.topic;` alongside the existing `requestedDeckId`. Add `import { CATEGORIES } from "@/data/wordlists/schema";` and add `filterWords` to the existing `@/lib/wordlist` import. Replace the `WordListBrowser` import with `import { BrowseList, BrowseTiles } from "@/components/word-list-browser";` (these exist after Task 2 — same plan).

    After `getWordList` resolves, compute server-side (synchronous, no extra I/O): `const categoryCounts: Record<string, number> = Object.fromEntries(CATEGORIES.map((cat) => [cat, filterWords(wordList.words, { category: cat }).length]));`.

    Replace the single `<WordListBrowser ...>` return with a branch inside the SAME outer shell (`<div className="min-h-screen bg-background"><main className="px-8 py-8 max-w-4xl mx-auto w-full"> ... </main></div>`): when `requestedTopic` is truthy render `<BrowseList words={wordList.words} topic={requestedTopic} existingWords={existingWords} deckId={activeDeck.id} nativeLang={nativeLang} targetLang={activeDeck.language} nativeLangLabel={nativeLangLabel} targetLangLabel={targetLangLabel} />`; otherwise render `<BrowseTiles categories={CATEGORIES} categoryCounts={categoryCounts} deckId={activeDeck.id} nativeLangLabel={nativeLangLabel} targetLangLabel={targetLangLabel} />`.

    Security (V5, threat T-23-03-TOPIC): do NOT add an error/whitelist path for `requestedTopic` — pass it through to `BrowseList`, where `filterWords` compares `w.category === topic` against static in-memory data. An unknown/forged topic yields an empty list (rendered as the D-09 empty state), never an error leak or crash — the intended safe fallback.

    Preserve everything else verbatim (auth, both redirects, label derivation). Do NOT change the data layer.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx biome check "src/app/(protected)/deck/browse/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the props interface reads `searchParams: Promise<{ deck?: string; topic?: string }>` and the body reads `params.topic`.
    - Source assertion: `categoryCounts` is computed via `CATEGORIES.map(... filterWords(wordList.words, { category: cat }).length ...)`.
    - Source assertion: the return branches on `requestedTopic` rendering `BrowseList` (with `topic={requestedTopic}`) vs `BrowseTiles` (with `categoryCounts`); the outer `min-h-screen bg-background` shell + `max-w-4xl` main are preserved.
    - Source assertion: auth guard, `decks.length === 0` redirect, `!activeDeck` redirect, and the `getWordList`/`getDeckCardWords` Promise.all are unchanged.
    - CLI: `npx tsc --noEmit` passes (requires Task 2's BrowseTiles/BrowseList exports — do Task 2 with or before this verify) and `npx biome check` on the page is clean.
  </acceptance_criteria>
  <done>The page reads ?topic=, computes real per-category counts, and renders tiles (no topic) or the topic word list (topic present) inside the preserved shell; auth + redirects + data layer untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite word-list-browser.tsx into Daybreak BrowseTiles + BrowseList, preserving the optimistic machine (D-02/D-04/D-05/D-06/D-09, BRW-01/02/03/04)</name>
  <read_first>
    - src/components/word-list-browser.tsx — the full current file (316 lines). The optimistic state machine (lines 43-147), wordKey (line 21), and the React.memo row (lines 233-315) MUST be preserved in logic; only markup changes. Note the error anti-pattern at lines 307-312 to replace.
    - design/handoff-daybreak/daybreak-browse.jsx — THE VISUAL CONTRACT. Atoms: BWLandingTop (61-69, "‹ My deck" — OVERRIDE to "‹ Add a card" per D-04), BWChevL (70-72), BWListTop (73-84, "‹ Topics"), BWContext (86-95, EN→ES placeholders — generalize per D-05), BWLevels (98-112), BWTopicTile (115-125), BWTopicGrid (126-132), BWLvlTag (135-137), BWWordRow (138-156, Row A), BWList (157-163).
    - design/handoff-daybreak/daybreak-browse-boards.jsx — the 4 board states incl. BrowseEmpty (58-74): exact empty-state copy + layout (LionFace in #F3E3C6 92px disc, "No words at this level.", subtext, "Show all levels").
    - .planning/phases/23-browse-words/23-PATTERNS.md — section "src/components/word-list-browser.tsx": props interfaces, the verbatim-preserve note, the BWWordRow anatomy + reserved error line, the back-link/top-bar replacements, the encodeURIComponent tile link.
    - .planning/phases/23-browse-words/23-RESEARCH.md — Pattern 2 (encodeURIComponent), Pattern 5 (reserved inline error space), Pitfalls 3/6/7 (no useEffect sync, fix the absolute error, keep React.memo + useCallback).
    - src/components/daybreak/lang-chip.tsx, src/components/daybreak/lion-face.tsx, src/components/daybreak/bw-medallion.tsx (Plan 23-01), src/components/daybreak/t-btn.tsx, src/components/daybreak/ac-context.tsx — atoms + the context-line analog to compose.
  </read_first>
  <action>
    Rewrite `src/components/word-list-browser.tsx` (keep "use client"). Replace the single `WordListBrowser` export with TWO named exports — `BrowseTiles` and `BrowseList` — plus module-local sub-components (import `BWMedallion` from Plan 23-01; build `BWContext`, the LEVEL tile row, the topic tile, `BWWordRow`, `BrowseEmpty`, and a left-chevron helper inline). Remove now-unused imports (lucide-react icons, ui/button Button, cn); add `LangChip`, `LionFace`, `TBtn`, `BWMedallion`.

    PRESERVE VERBATIM (copy into BrowseList): `wordKey`; the deckWords/loadingWords/errorWords state with the `useState(() => new Set(existingWords))` lazy initializer (do NOT add a useEffect to sync from props — Pitfall 3); `useTransition`; handleAdd/handleRemove with optimistic flip, revert-on-catch, the `"Failed. Try again."` message, and the 3s setTimeout auto-clear; the `useCallback([deckId])` memoization; the `filteredWords` useMemo (adapt category to the `topic` prop). Keep the CEFR filter as in-page `useState<"All"|CefrLevel>("All")` — NOT a URL param (D-01).

    BrowseTiles (props `{ categories: readonly string[]; categoryCounts: Record<string,number>; deckId: string; nativeLangLabel: string; targetLangLabel: string }`):
    - Top bar (BWLandingTop, D-04 OVERRIDE): left next/link `<Link href={`/deck/new-card?deck=${deckId}`}>` with a left-chevron + the text `Add a card` (NOT "My deck" — deliberate mock override; the UI auditor must not "correct" this); centered title span `data-testid="browse-words-title"` reading `Browse Words` in var(--font-display) (20/700/#4A331C); a balancing right spacer.
    - BWContext (D-05): `LangChip code={toChipCode(nativeLangLabel)}` arrow `LangChip code={toChipCode(targetLangLabel)}` then muted text `tap a word to add it to your ` + bold `{targetLangLabel} deck`. Derive chip codes with `label.trim().slice(0,2).toUpperCase()` (same helper as ACContext).
    - A `Pick a topic` display heading.
    - 3-column grid (gridTemplateColumns "1fr 1fr 1fr", gap ~11) mapping `categories`: each tile is a next/link `<Link href={`/deck/browse?deck=${deckId}&topic=${encodeURIComponent(category)}`} data-testid={`topic-tile-${category.toLowerCase().replace(/[^a-z]/g, "-")}`}>` styled as the white rounded card (radius 18, bg #FFFFFF, border 1px solid #EDDFC9, soft shadow, padding "15px 12px 13px", centered column) containing `<BWMedallion name={category} />`, the category name (13.5/700/#4A331C), and `{categoryCounts[category] ?? 0} words` (11.5/#9C8467). Render all 14 tiles even at count 0 (D-07). The tile Link is the accessible element (medallion is aria-hidden).

    BrowseList (props `{ words, existingWords, deckId, topic, nativeLang, targetLang, nativeLangLabel, targetLangLabel }`):
    - Top bar (BWListTop, D-02): left next/link `<Link href={`/deck/browse?deck=${deckId}`}>` with left-chevron + text `Topics`; center = `<BWMedallion name={topic} size={26} />` + topic name (var(--font-display), 18/700/#4A331C); right spacer.
    - BWContext (same as tiles).
    - BWLevels row: a `LEVEL` label (12.5/700/#9C8467, slight letter-spacing) above a flex row of 4 equal tiles `["All","A1","A2","B1"]` rendered as `<button type="button">` (role-locatable for e2e). Each tile height 46, radius 13, var(--font-display) 16/700; active (=== current filter) bg #F28A1F, color #FFF, amber shadow, no border; inactive bg #FFFFFF, color #4A331C, border 1px solid #EDDFC9. onClick sets the CEFR filter state.
    - Word list: map `filteredWords` to BWWordRow (the preserved React.memo row). If `filteredWords.length === 0`, render `BrowseEmpty` instead of the list.

    BWWordRow (preserve React.memo + the onAdd/onRemove/word/inDeck/loading/error prop shape; replace markup): container `<div data-testid="word-row">` with display:flex, alignItems:center, gap:12, padding "13px 12px", marginBottom 7, borderRadius 14, minHeight 44 (BRW-03 ≥44px target) and the warm tint when inDeck (bg #FFF7E9, border 1px solid #F4E3C4) else transparent bg + transparent 1px border. Left column (flex:1, minWidth:0): `word.native` bold (17.5/700/#4A331C) on top; beneath, a row with the 2-letter target-language code (derived from `targetLangLabel`, 10/700/#9C8467) + `word.target` muted (14.5/#9C8467). Reserved error line (D-06, Pitfall 6) ALWAYS rendered as the last child of the left column: a `<div data-role="error-line" style={{ fontSize:12, color:"#DE5F4A", minHeight:16, marginTop:2 }}>{error ?? ""}</div>` — NO position:absolute, NO -bottom-4. Then the CEFR chip (11/700, color #B4762A, bg #FFF1DC, radius 6, pad "2px 7px") showing `word.cefr`. Then the 38px circular toggle as a `<button type="button">` carrying the preserved aria-label (`Add {word.native} to deck` when not in deck calling onAdd; `Remove {word.native} from deck` when in deck calling onRemove): outlined amber + when not in deck (CSS-drawn plus = two 2.4px amber bars per mock line 152); filled amber circle with a white check (&#10003;) when in deck; while loading, show a small amber spinner inside the toggle (the optimistic flip already applied the in-deck visual) — keep the button present and you MAY disable it during load. Do NOT add the mock's `last` prop (D-01 — rows use margin, not dividers).

    BrowseEmpty (D-09 — props `{ topic: string; level: string; onShowAll: () => void }`): centered column — `LionFace size={54}` inside a 92px #F3E3C6 disc; `No words at this level.` in var(--font-display) (20/700/#4A331C); subtext `There are no {level} words in {topic} yet. Try another level or topic.` (14.5/#9C8467, max-width ~250, line-height 1.5); a primary button (TBtn or an inline primary matching the mock: height 48, amber bg, white, display font, amber shadow) reading `Show all levels` whose onClick calls `onShowAll`. Wire `onShowAll` in BrowseList to reset the CEFR filter to `"All"` (stays on the current topic). Button is role-locatable by name "Show all levels".

    Left-chevron helper (no icon library): a `<span aria-hidden="true">` with width:8, height:8, borderLeft "2.2px solid #C96F12", borderBottom "2.2px solid #C96F12", transform rotate(45deg), display inline-block, flex none (mock BWChevL / PATTERNS.md "Left chevron glyph").

    Discretion: a tiles-to-list transition animation is optional (motion/react is available) — keep minimal or omit; natural page scroll is fine (do NOT recreate the mock's fixed-height bottom mask-fade — it is a phone-shell artifact). NO text search box on Browse (search belongs to the dashboard accordion).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx biome check src/components/word-list-browser.tsx "src/app/(protected)/deck/browse/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `word-list-browser.tsx` exports `BrowseTiles` and `BrowseList` (named) and no longer exports `WordListBrowser`.
    - Source assertion: the optimistic machine is preserved — `addWordToCard`, `removeWordFromDeck`, `useTransition`, `useState(() => new Set(existingWords))`, the `"Failed. Try again."` string, and a `setTimeout(..., 3000)` are all present; there is NO `useEffect` that sets `deckWords` from props.
    - Source assertion: each word row container has `data-testid="word-row"`; the reserved error line has `data-role="error-line"` and renders `{error ?? ""}` (NOT position:absolute, NOT -bottom-4 — grep confirms `-bottom-4` is absent from the file).
    - Source assertion: the landing back-link text is `Add a card` (D-04) and there is NO occurrence of the string `My deck` and NO occurrence of `Back to my deck` in this file; the list back-link text is `Topics`; the landing title span carries `data-testid="browse-words-title"`.
    - Source assertion: topic tiles are next/link `<Link>` (not `<button>`) with `encodeURIComponent(category)` in the href and `data-testid` matching `topic-tile-`; LEVEL tiles are `<button>`; the in-deck row background literal `#FFF7E9` and the toggle aria-labels `Add ... to deck` / `Remove ... from deck` are present.
    - Source assertion: lucide-react, `@/components/ui/button`, and `@/lib/utils` (cn) are no longer imported; `BWMedallion`, `LangChip`, `LionFace` are imported.
    - CLI: `npx tsc --noEmit` passes and `npx biome check` is clean on both the component and the page.
  </acceptance_criteria>
  <done>Browse renders as the Daybreak tiles landing and per-topic list with Row-A rows, scroll-stable optimistic toggles, and the D-09 empty state; the optimistic machine and in-deck detection are unchanged; D-04 landing back-link is "‹ Add a card".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend bw-atoms.test.tsx with BWWordRow + empty-state rendered-component cases (BRW-03/04)</name>
  <read_first>
    - src/components/daybreak/__tests__/bw-atoms.test.tsx — the Wave 0 scaffold from Plan 23-01 (BWMedallion cases). Append new describe blocks; keep the `// @vitest-environment jsdom` docblock and `afterEach(cleanup)`.
    - src/components/word-list-browser.tsx — the rewritten file (Task 2). To test BWWordRow and BrowseEmpty in isolation, EXPORT them (named) from word-list-browser.tsx if they are not already exported (add `export` to the two sub-components so the test can import them) — do this as part of Task 2 if not yet done; verify here.
    - src/components/daybreak/__tests__/ac-atoms.test.tsx — the assertion idioms (jsdom normalizes hex → rgb() in element.style: use `toMatch(/rgb\(/)` or compare to the rgb() form, NOT the literal hex; querySelector for data-role; getByRole button by name).
    - .planning/phases/23-browse-words/23-VALIDATION.md — the BRW-03 MANDATORY rendered-component requirement (optimistic/inline UI is rendered-component territory, not reducer-only) and BRW-04 empty-state reset row.
    - .planning/phases/23-browse-words/23-PATTERNS.md — the example BWWordRow + BrowseEmpty test cases.
  </read_first>
  <action>
    Append to `src/components/daybreak/__tests__/bw-atoms.test.tsx`. Import the now-exported `BWWordRow` and `BrowseEmpty` from `@/components/word-list-browser` (and `WordEntry` type from the schema for a mock word). Define a `mockWord: WordEntry` (e.g. `{ id: "food-001", category: "Food & Drink", cefr: "A1", native: "water", target: "agua" }`).

    Add `describe("BWWordRow — Row A states", ...)` with:
    - in-deck warm tint: `render(<BWWordRow word={mockWord} inDeck loading={false} error={undefined} onAdd={() => {}} onRemove={() => {}} targetLangLabel="Spanish" />)` (pass whatever props the row actually declares — match Task 2's signature), then `const row = screen.getByTestId("word-row")` and assert `row.style.background` matches the in-deck tint. jsdom normalizes `#FFF7E9` to `rgb(255, 247, 233)` — assert via `expect(row.style.background).toBe("rgb(255, 247, 233)")` (or `toMatch(/rgb\(255,\s*247,\s*233\)/)`).
    - not-in-deck: render with `inDeck={false}` and assert the toggle button has accessible name matching `/Add water to deck/`.
    - in-deck remove affordance: render with `inDeck` and assert a button with accessible name matching `/Remove water to deck|Remove water from deck/` exists (match Task 2's exact aria-label).
    - reserved error line present when no error: render with `error={undefined}`; query `screen.getByTestId("word-row").querySelector('[data-role="error-line"]')` is truthy and its `textContent` is `""` (proves the line reserves space even with no error — scroll-stability guard).
    - error rendered when present: render with `error="Failed. Try again."`; the `[data-role="error-line"]` textContent equals `"Failed. Try again."`.

    Add `describe("BrowseEmpty — D-09 empty state", ...)` with:
    - `render(<BrowseEmpty topic="Body" level="B1" onShowAll={() => {}} />)` then assert `screen.getByText("No words at this level.")` is truthy and the subtext containing `B1` and `Body` is present.
    - reset handler: `const onShowAll = vi.fn(); render(<BrowseEmpty topic="Body" level="B1" onShowAll={onShowAll} />); fireEvent.click(screen.getByRole("button", { name: "Show all levels" })); expect(onShowAll).toHaveBeenCalled();`.

    Do NOT use watch mode. If BWWordRow/BrowseEmpty are easier to test through a thin wrapper, prefer exporting and testing them directly (the Phase 19/22 pattern).
  </action>
  <verify>
    <automated>npx vitest run src/components/daybreak/__tests__/bw-atoms.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npx vitest run src/components/daybreak/__tests__/bw-atoms.test.tsx` exits 0 with the new BWWordRow + BrowseEmpty cases passing alongside the Plan 23-01 BWMedallion cases.
    - Source assertion: the test asserts the in-deck row background equals the rgb() form of #FFF7E9 (rendered-component, not reducer-only — BRW-03 mandate).
    - Source assertion: the test asserts the reserved error line ([data-role="error-line"]) is present and empty when error is undefined, AND shows the message when error is set.
    - Source assertion: the test clicks "Show all levels" and asserts the onShowAll callback fires (BRW-04 reset).
    - CLI: `npx biome check src/components/daybreak/__tests__/bw-atoms.test.tsx` clean.
  </acceptance_criteria>
  <done>bw-atoms.test.tsx now proves the Row-A in-deck tint, the scroll-stable reserved error line (empty + populated), and the empty-state "Show all levels" reset — the BRW-03/04 rendered-component coverage the project lesson requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server page (searchParams) | `?topic=` and `?deck=` are untrusted URL inputs crossing into the server component. |
| client → server actions | `addWordToCard` / `removeWordFromDeck` receive deckId + word fields from the client (unchanged path). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-TOPIC | Tampering / Info-disclosure | `?topic=` param in page.tsx → filterWords | mitigate | Treat `topic` as untrusted: it is used ONLY to select from the fixed CATEGORIES via `filterWords` (in-memory `w.category === topic` over static JSON). No DB query, no injection sink, no reflected raw output. An unknown/forged topic yields an empty list → the D-09 empty state renders. No error leak, no crash. (V5 Input Validation.) |
| T-23-03-XSS | Tampering | topic name rendered into the list header / empty-state subtext | mitigate | React JSX escapes the interpolated `topic` string automatically; no `dangerouslySetInnerHTML`. The topic also drives `BWMedallion` only as a lookup key. |
| T-23-03-AC | Elevation / Access control | `addWordToCard` / `removeWordFromDeck` / `getDeckCardWords` deck-ownership | mitigate (preserve) | These server actions and queries are scoped to `deckId`, resolved server-side from the authenticated session in page.tsx; their existing auth + deck-ownership checks are PRESERVED unchanged (L-02). No new write path or data exposure — counts + curated word lists are user-agnostic. Do NOT weaken or "fix" the source="wordlist"-only matching. |
| T-23-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH.md §Package Legitimacy Audit: not applicable — zero new deps). |
</threat_model>

<verification>
- `npx tsc --noEmit` passes across the repo with the new exports + page branch.
- `npx biome check` clean on `word-list-browser.tsx`, the browse `page.tsx`, and `bw-atoms.test.tsx`.
- `npx vitest run src/components/daybreak/__tests__/bw-atoms.test.tsx` green (medallion + word-row + empty-state).
- `npx vitest run src/lib` green (data-layer unchanged — sanity).
- Note: e2e proof of the two-screen navigation, optimistic add/remove, and level filtering is owned by Plan 23-04 (it retargets the specs). Do NOT run the Browse e2e here — it still carries pre-reskin selectors until 23-04 lands.
</verification>

<success_criteria>
- BRW-01: tiles landing renders 14 medallion tiles with real per-pair counts.
- BRW-02: per-topic list shows "‹ Topics" back-link, mini-medallion topic header, the LEVEL tile row, and the context line; `?topic=` routing works (server-rendered, deep-linkable, browser-back returns list→tiles).
- BRW-03: Row-A rows (native-on-top + target marker, CEFR chip, circular toggle), in-deck warm tint, optimistic add/remove with the amber spinner and the scroll-stable reserved-space error.
- BRW-04: empty result shows LionFace + "No words at this level." + "Show all levels" reset to All.
- The optimistic machine + in-deck detection are byte-for-byte behaviorally preserved; D-04 landing back-link is "‹ Add a card".
</success_criteria>

<output>
Create `.planning/phases/23-browse-words/23-03-SUMMARY.md` when done.
</output>
