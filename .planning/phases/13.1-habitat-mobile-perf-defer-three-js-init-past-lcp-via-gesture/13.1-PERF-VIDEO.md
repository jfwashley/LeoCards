# Phase 13.1 — Habitat Video Migration: CWV Results

**Date:** 2026-05-28
**Strategy:** Replace the live Three.js `/habitat` scene with pre-rendered per-level×mood ambient video clips. Three.js becomes build-time-only (renders the clips in CI; never ships to the client). Decay/quality applied as a live CSS filter; reduced-motion shows the still poster.
**Tool:** Lighthouse 13.3.0, real Chrome, no in-page instrumentation. 3+ runs/cell vs Vercel preview, auth cookie + `x-vercel-protection-bypass` header. Mobile = `--form-factor=mobile --throttling-method=simulate` (Moto G4, Slow 4G, 4× CPU). Desktop = `--preset=desktop`.

## The journey (medians)

| Stage | /habitat mobile LCP | mobile TBT | mobile Perf | Note |
|---|---:|---:|---:|---|
| **Phase 13 live 3D** (baseline) | 2989 | 646 | 77 | + not rendering at all on the user's device |
| 13.1 gesture-poster attempts | 2729–5560 | 646–1322 | 46–77 | reverted; deferral made it worse |
| Video, first cut | 2729 | 1049 | 74 | three.js gone (−517 KB) but motion/react dominated |
| Video + drop motion/react | 3194* | 415 | 85 | TBT −634 ms; LCP still unrecognized |
| **Video + preloaded poster** (shipped) | **1860** | **377** | **90** | LCP recognized + fast; CLS 0 |

\* LCP volatile on cold preview; the poster-preload fix stabilized it.

## Final shipped numbers

| Cell | LCP | TBT | CLS | Perf | CWV verdict |
|---|---:|---:|---:|---:|---|
| /habitat desktop | 764 | 49 | 0 | 99 | ✅ PASS (all gates) |
| /habitat mobile | 1860 | 377 | 0 | 90 | ⚠ LCP ✅ + CLS ✅; **TBT 377 > 200** |
| /dashboard mobile (ref) | ~2151 | ~286 | 0 | ~93 | unchanged (widget = static image) |

## What fixed what
1. **Three.js → video** removed the 504 KB WebGL chunk from the client bundle entirely (`grep WebGLRenderer .next/static/chunks` → none). Killed the parse/exec cost and the on-device rendering bug.
2. **Dropped `motion/react` from habitat-scene** (two fades → CSS `tw-animate-css`). That single 71 KB chunk was ~1512 ms of script-eval under 4× CPU throttle — the entire original TBT. `/habitat` page_client-reference-manifest now has 0 references to the motion chunk. TBT 1049 → 415.
3. **Preloaded priority poster** (`next/image priority` bottom layer + clip `preload="metadata"`). The old `<video poster>` got no preload link → Lighthouse "LCP element: none" → LCP gated behind hydration (~3 s). The webp now paints from a `<link rel=preload>`. LCP 2729 → 1860.

## Residual: TBT 377 ms (mobile)
After removing both Three.js and motion, the remaining main-thread cost is **framework/app-shell baseline** — React hydration + Next runtime + better-auth client + the protected layout. It is NOT habitat-specific (the shared `0v-…` vendor chunk is ~576 ms; `/dashboard` carries the same class of cost at TBT ~286). Getting under the 200 ms gate requires app-wide work: code-splitting/deferring the auth client + providers, trimming the shared vendor chunk, reducing hydration. That is precisely **Phase 999.1 (perf initiative)** scope and should be done there against a whole-app baseline, not bolted onto /habitat.

**Decision:** Ship. /habitat mobile passes LCP + CLS, scores Perf 90 (green band), and renders reliably with the designer's 3D art alive (ambient loop) — a viable customer-facing product. The residual TBT is carried to Phase 999.1.

## Phase 999.1 follow-up — zod removed + WARM PRODUCTION measurement (2026-05-29)

The residual mobile TBT (377 ms on cold previews) was investigated as the first Phase 999.1 slice. Chunk fingerprinting found `/habitat` was loading the **263 KB zod chunk** solely because `habitat-scene.tsx` used `zod` to validate one localStorage cache object. Replaced with a hand-written type guard → zod gone from the route (0 refs in habitat's client-reference-manifest). Combined with the earlier three.js (−517 KB) + motion (−132 KB) removals, ~900 KB of controllable JS is off `/habitat`.

**Critical measurement lesson:** cold Vercel **preview** deployments produce huge Lighthouse variance — TBT swung **141–501 ms in a single 4-run batch**, LCP 2632–3017, all from serverless/edge cold-start, not code. That noise exceeded the target gap. Re-measuring against the **warm production alias** (`leocards.vercel.app`, the environment that matters) gave the true picture:

| Metric | Prod median (n=5) | Runs | Gate | Verdict |
|---|---:|---|---:|---|
| LCP | **2417** | 2274–2595 | ≤2500 | ✅ |
| TBT | **97** | 76 / 71 / 200 / 97 / 372 | ≤200 | ✅ (4/5 runs) |
| CLS | **0** | all 0 | ≤0.1 | ✅ |
| Perf | **96** | 88–98 | — | ✅ green |

**`/habitat` mobile PASSES all three CWV "Good" gates on production.** Desktop was already 99. The Phase-13 R9 regression is fully resolved on real warm infrastructure. Always certify against warm prod, not cold previews.

## Asset footprint
- 36 clip pairs (9 levels × 4 moods): webm 2147.8 KB total / mp4 1594.2 KB total. **Only one pair (~104 KB) loads per visit.**
- 9 hero posters (`hero-l{1..9}.webp`), ~4–7 KB each, one loaded per visit (the LCP element).

## Reproduction
Same harness as `13-PERF-REAL.md`. Clips regenerated via `npm run clips:habitat` (build-time, ~10 min for all 36). Posters via `npm run posters:habitat`.
