---
phase: 20-study-screen
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/components/level-up-overlay.tsx
  - src/components/study-session.tsx
  - src/components/__tests__/level-up-overlay.test.tsx
autonomous: true
requirements: [STU-02]
must_haves:
  truths:
    - "D-04: the end screen replaces the 🐯 tiger emoji with the Daybreak LionFace, restyles the three stats (cards studied · % correct · learned) to Baloo 2 display numerals, keeps the 'learned' numeral as the amber hero number (text-primary), and uses the Daybreak TBtn for 'Back to deck' — no mini-habitat teaser is added"
    - "D-05: the level-up overlay shows a static Soft-Clay Leo from public/habitat/widget-l{level}.webp (clamped to ≤9, never the ~1.3MB clips/ or a live render) with Daybreak-recolored confetti (amber #F28A1F, green #3E9B5F, red #DE5F4A, gold #F2B33A, ink #4A331C), keeping the 'Your habitat grew!' beat and tap-to-dismiss"
    - "D-06: the level-up confetti is gated behind prefers-reduced-motion via usePrefersReducedMotion — under reduced motion no falling particles render, while the static Soft-Clay Leo + level summary still render; the 3D card flip and swipe tilt keep their current behavior this phase (their reduced-motion variants are deferred)"
    - "A Wave 0 component test (level-up-overlay.test.tsx, jsdom) asserts D-06: confetti particles are ABSENT when usePrefersReducedMotion returns true and PRESENT when it returns false, while the Soft-Clay Leo img and the level summary render in both cases"
    - "The study-session.tsx PRESERVE zone (lines 1-294: types, reducer, computeStats, props, showLevelUp/handleLevelUpDismiss incl. the dead ===10 branch, the 300ms swipe-enable effect, commitIdRef, gradedRef, and the commit() POST /api/study/complete effect) is untouched — only the render JSX (lines ~297-483) and the level-up overlay presentation change"
    - "The existing study e2e (e2e/06-study-session.spec.ts, e2e/study-progression.spec.ts, e2e/14-qa-parity.spec.ts) stays green — chrome, committing/error, and end-screen behavior are unchanged, only presentation differs"
  artifacts:
    - path: "src/components/level-up-overlay.tsx"
      provides: "Daybreak level-up: static Soft-Clay Leo (widget-l{N}.webp, L9-clamped), recolored confetti, prefers-reduced-motion confetti gate, cream overlay"
      contains: "usePrefersReducedMotion"
    - path: "src/components/study-session.tsx"
      provides: "Daybreak session chrome (X-circle quit, centered label, quit-confirm), committing/error restyle, end screen with LionFace + Baloo 2 numerals + TBtn; PRESERVE zone lines 1-294 untouched"
      contains: "LionFace"
    - path: "src/components/__tests__/level-up-overlay.test.tsx"
      provides: "Wave 0 D-06 component test: confetti absent under reduced motion, Leo + summary always present"
      contains: "usePrefersReducedMotion"
  key_links:
    - from: "src/components/level-up-overlay.tsx"
      to: "public/habitat/widget-l{N}.webp"
      via: "img src with Math.min(level, 9) clamp"
      pattern: "widget-l"
    - from: "src/components/level-up-overlay.tsx"
      to: "src/hooks/use-prefers-reduced-motion.ts"
      via: "import + gate confetti with {!reduced && ...}"
      pattern: "usePrefersReducedMotion"
    - from: "src/components/study-session.tsx"
      to: "src/components/daybreak/lion-face.tsx"
      via: "import { LionFace } — end screen replaces 🐯"
      pattern: "daybreak/lion-face"
    - from: "src/components/study-session.tsx"
      to: "src/components/daybreak/t-btn.tsx"
      via: "import { TBtn } — Back to deck / Retry"
      pattern: "daybreak/t-btn"
---

<objective>
Reskin the study-session chrome, the committing/error states, the end screen, and the level-up celebration overlay to the Daybreak design system, and add the one net-new test the phase needs (the D-06 reduced-motion confetti gate). The end screen swaps the 🐯 emoji for the Daybreak `LionFace`, restyles the three stats to Baloo 2 display numerals (keeping "learned" as the amber hero number), and uses `TBtn` for "Back to deck" (D-04). The level-up overlay brings in a static Soft-Clay Leo (`widget-l{level}.webp`, L9-clamped) with Daybreak-recolored confetti that is gated behind `prefers-reduced-motion` (D-05/D-06). All session logic — the reducer, the requeue, `commitIdRef` idempotency, and the `POST /api/study/complete` effect — is preserved byte-for-byte (PRESERVE zone, lines 1-294).

Purpose: STU-02 — the session-result/end screen and the level-up hand-off are the emotional payoff of the study loop. They must match the Daybreak visual language and brand (Leo the lion), and the most celebratory moment in the app now gets Leo imagery and an accessibility-safe confetti gate.
Output: Reskinned `level-up-overlay.tsx` and the render-only zone of `study-session.tsx`, plus a new `level-up-overlay.test.tsx` Wave 0 component test.

NOTE (Wave 2, depends on 20-01): This plan imports `LionFace`/`TBtn` and consumes the `StudyCard`/`CardStack` components reskinned in 20-01 by their UNCHANGED signatures. The full-suite e2e gate in Task 4 runs against both this plan's chrome/end-screen reskin AND 20-01's card reskin.

NOTE (deferred logic — DO NOT TOUCH, and do NOT "opportunistically fix"): `study-session.tsx` `handleLevelUpDismiss` checks `leveledUp === 10` while the habitat caps at L9 — this is a DEAD branch flagged by CONTEXT for a separate, non-Daybreak logic ticket. Leave the condition exactly as-is. SIMILARLY, the level-up overlay currently uses `level === 10` for the max-level copy ("A bird arrived in your habitat!"). The research/pattern docs floated changing this to `level >= 9` — DECLINE that change. Phase 20 is purely presentational; preserve all conditions verbatim (CONTEXT Deferred + scope fence). The asset src clamp `Math.min(level, 9)` is NOT a logic change — it only prevents a 404 on a nonexistent `widget-l10.webp` and is required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-study-screen/20-CONTEXT.md
@.planning/phases/20-study-screen/20-RESEARCH.md
@.planning/phases/20-study-screen/20-PATTERNS.md

<interfaces>
<!-- Daybreak primitives (Phase 19) — import and compose, do not reimplement. -->
LionFace (src/components/daybreak/lion-face.tsx): <LionFace size={80} /> — default colors are correct for study context; no override needed. Replaces the 🐯 emoji.
TBtn (src/components/daybreak/t-btn.tsx): <TBtn onClick={...}>Back to deck</TBtn>; for non-full-width use a width/maxWidth override style, e.g. <TBtn style={{ maxWidth: 280 }} onClick={...}>…</TBtn>
usePrefersReducedMotion (src/hooks/use-prefers-reduced-motion.ts): const reduced = usePrefersReducedMotion(); — SSR-safe (useState(false) default), "use client". Returns true when (prefers-reduced-motion: reduce).

<!-- Daybreak chrome / end-screen tokens (HiFiStudy + d1 theme). -->
Top bar X-circle: width 40, height 40, borderRadius 999, border "1.5px solid #EDDFC9", background "#FFFFFF", color "#4A331C", fontSize 15; centered label "Study session" 14px/600 muted, letterSpacing 0.3; 40px spacer right.
End screen: LionFace size 80; headline font-display 20px bold; stat numerals font-display 32px bold (Baloo 2); "learned" numeral text-primary (amber) — PRESERVED; "Back to deck" → TBtn.
Confetti palette (Daybreak): ["#F28A1F", "#3E9B5F", "#DE5F4A", "#F2B33A", "#4A331C"]
Soft-Clay Leo: <img src={`/habitat/widget-l${Math.min(level, 9)}.webp`} alt="Leo" width={160} height={160} style={{ borderRadius: 16, objectFit: "cover" }} /> — clamp ≤9; widget-l{1..9}.webp all confirmed present.
Overlay bg: cream — style={{ background: "rgba(255, 246, 233, 0.92)" }} (drop backdrop-blur-sm).
</interfaces>

<preserve_zone>
<!-- PRESERVE / do-not-touch in study-session.tsx (from 20-PATTERNS.md). Edit ONLY lines ~297-483 (the four `if (state.phase === ...)` render blocks + return JSX). Any failure in e2e/06-study-session.spec.ts / e2e/study-progression.spec.ts / e2e/14-qa-parity.spec.ts means this zone was modified. -->
- Types + reducer (lines 14-154) — FLIP_CARD/ENABLE_SWIPE/SWIPE_GRADE/requeue/COMMIT_DONE/COMMIT_ERROR/RETRY_COMMIT
- computeStats (lines 160-175) — feeds the end screen
- StudySessionProps interface (lines 181-185) — consumed by study/page.tsx; signature must not change
- showLevelUp state + handleLevelUpDismiss incl. the dead `=== 10` branch (lines 198-219) — preserve as-is; do NOT fix the dead branch
- 300ms swipe-enable useEffect (lines 225-232)
- commitIdRef + lazy-init (lines 238-241) — idempotency key (WR-04)
- gradedRef + phase-guard (lines 244-248)
- commit() async effect → POST /api/study/complete (lines 250-294)
- Safe edit zone: lines 297-483 ONLY (boundary is immediately before `// Render: committing` at line ~297).
<!-- In level-up-overlay.tsx (82 lines), preserve the confetti motion mechanics (CONFETTI_COUNT, CONFETTI_ROTATE_DIRS, the y/opacity/rotate animate + 2.5s easeIn transition) and the onDismiss tap behavior; preserve the `level === 10` copy condition (do NOT change to `level >= 9`). -->
</preserve_zone>

<test_infra>
<!-- Verified from repo. -->
vitest 4.1.1 — `npm test` (= `vitest run`). vitest.config.ts default `environment: "node"`, setupFiles ["./src/test-setup.ts"], "@" alias → ./src. jsdom ^29 and @testing-library/react ^16.3.2 ARE installed (Phase 19 Plan 01 Wave 0). @testing-library/user-event is NOT installed — the confetti test needs no user interaction (render-only), so do not depend on it. For a component-render test, add the per-file docblock `// @vitest-environment jsdom` (established pattern: src/app/(auth)/__tests__/signup-payload.test.tsx, src/components/daybreak/__tests__/{t-btn,t-field}.test.tsx). The reduced-motion HOOK is mocked with `vi.mock("@/hooks/use-prefers-reduced-motion", ...)` returning a controllable value — mirror the vi.mock conventions in src/components/__tests__/qa-state-badge.test.ts (pure) and the daybreak primitive tests (render). After each render, call cleanup() (afterEach) per the Phase 19 RTL isolation note.
</test_infra>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — D-06 reduced-motion confetti component test (jsdom)</name>
  <files>src/components/__tests__/level-up-overlay.test.tsx</files>
  <read_first>
    - src/components/level-up-overlay.tsx — the CURRENT file (82 lines): the `LevelUpOverlayProps { level, onDismiss }`, the always-rendered confetti `<div className="absolute inset-0 overflow-hidden pointer-events-none">` wrapping the 36 `motion.div` particles (lines 42-65), and the content block (lines 67-79). This test is written against the POST-reskin structure from Task 2, so coordinate the testid/markers with Task 2 (the confetti container gets a stable hook for the assertion; the Soft-Clay Leo is an `<img>`).
    - src/components/daybreak/__tests__/t-btn.test.tsx — the established jsdom render-test pattern: the `// @vitest-environment jsdom` docblock, `render(...)` from @testing-library/react, `screen.getBy*`, and `afterEach(cleanup)`
    - src/components/__tests__/qa-state-badge.test.ts — the `vi.mock(...)` + `describe/it/expect` conventions used in this repo (how modules are stubbed)
    - src/hooks/use-prefers-reduced-motion.ts — the hook signature `usePrefersReducedMotion(): boolean` so the mock returns the right shape
    - .planning/phases/20-study-screen/20-VALIDATION.md — the Wave 0 Requirements section (the confetti gate is the only net-new test gap) and the Per-Task Verification Map intent
    - .planning/phases/20-study-screen/20-RESEARCH.md — OQ-4 (the reduced-motion gate pattern) and "Validation Architecture → Wave 0 Gaps"
    - node_modules/next/dist/docs/01-app — only if any Next-specific import (e.g. next/image) appears in the overlay; a plain `<img>` is used per the interfaces block, so this is likely N/A
  </read_first>
  <behavior>
    - level-up-overlay.test.tsx (jsdom env): with `vi.mock("@/hooks/use-prefers-reduced-motion", () => ({ usePrefersReducedMotion: vi.fn() }))`, render `<LevelUpOverlay level={5} onDismiss={() => {}} />` twice:
      - Case A (reduced motion ON): the mocked hook returns `true`. Assert the confetti container/particles are ABSENT (e.g. `screen.queryByTestId("confetti-layer")` is null, OR `container.querySelectorAll('[data-confetti-particle]').length === 0`), AND the Soft-Clay Leo img IS present (`screen.getByAltText("Leo")` / an `img[src*="widget-l"]`), AND the level summary text "Your habitat grew!" (or the level numeral) IS present.
      - Case B (reduced motion OFF): the mocked hook returns `false`. Assert the confetti container/particles ARE present (`queryByTestId("confetti-layer")` not null, OR particle count > 0), AND the Soft-Clay Leo img + summary are still present.
    - The asset src reflects the clamp: rendering with `level={12}` yields an img whose src contains `widget-l9` (asserts `Math.min(level, 9)`), and `level={5}` yields `widget-l5`.
  </behavior>
  <action>
    Create `src/components/__tests__/level-up-overlay.test.tsx` as a jsdom-env Vitest component test (first line `// @vitest-environment jsdom`). Import `render`, `screen`, `cleanup` from `@testing-library/react` and `afterEach`, `describe`, `it`, `expect`, `vi`, `beforeEach` from `vitest`. `vi.mock("@/hooks/use-prefers-reduced-motion", () => ({ usePrefersReducedMotion: vi.fn() }))`, then in the tests import the mocked fn and set its return value per case (`vi.mocked(usePrefersReducedMotion).mockReturnValue(true|false)`). Add `afterEach(cleanup)`. Write the three assertions from the behavior block: (A) reduced-on → confetti absent, Leo+summary present; (B) reduced-off → confetti present, Leo+summary present; (C) the `widget-l9` clamp for `level={12}` and `widget-l5` for `level={5}`. Use the stable markers that Task 2 will add: a `data-testid="confetti-layer"` on the confetti container `<div>` and a `data-confetti-particle` attribute on each particle (Task 2 adds these); query Leo via `getByAltText("Leo")`. Do NOT rely on `@testing-library/user-event` (not installed) — no interaction is needed. This test is expected to FAIL until Task 2 lands the gate + markers (RED), then pass (GREEN).
  </action>
  <verify>
    <automated>npx vitest run "src/components/__tests__/level-up-overlay.test.tsx" --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/__tests__/level-up-overlay.test.tsx` exists with the `// @vitest-environment jsdom` docblock and `afterEach(cleanup)`
    - It mocks `@/hooks/use-prefers-reduced-motion` and drives `usePrefersReducedMotion` to `true`/`false` per case
    - Case A (reduced ON) asserts confetti ABSENT while the Soft-Clay Leo img (`getByAltText("Leo")`) and the level summary render
    - Case B (reduced OFF) asserts confetti PRESENT while Leo + summary render
    - Case C asserts the asset clamp: `level={12}` → img src contains `widget-l9`; `level={5}` → `widget-l5`
    - After Task 2 lands, `npx vitest run "src/components/__tests__/level-up-overlay.test.tsx"` exits 0 (green). (Written in Wave 0 / Task 1, it may legitimately be RED until Task 2 adds the gate + markers — that is the expected TDD order.)
  </acceptance_criteria>
  <done>A jsdom component test locks the D-06 contract: confetti particles do not render under prefers-reduced-motion while the static Soft-Clay Leo + level summary still render, and the asset src is L9-clamped.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin the level-up overlay — Soft-Clay Leo + Daybreak confetti + reduced-motion gate (D-05/D-06)</name>
  <files>src/components/level-up-overlay.tsx</files>
  <read_first>
    - src/components/level-up-overlay.tsx — the FULL current file (82 lines): `"use client"` + `import { motion } from "motion/react"` (line 3); `CONFETTI_COLORS = ["#F97316","#3B82F6","#10B981","#F59E0B","#EF4444"]` (line 9) and `CONFETTI_COUNT = 36` (line 10); `CONFETTI_ROTATE_DIRS` (lines 14-16); the outer `<motion.div ... bg-background/90 backdrop-blur-sm>` (line 34); the always-rendered confetti `<div>` + 36 particles (lines 42-65); and the content `<div>` with "Habitat Level", the `text-[28px]` level numeral, the `level === 10 ? ... : "Your habitat grew!"` copy (lines 72-75), and "Tap anywhere to continue"
    - .planning/phases/20-study-screen/20-PATTERNS.md — `src/components/level-up-overlay.tsx` Pattern Assignment: items 1-5 (import the hook, recolor confetti, add the reduced-motion gate, add the Soft-Clay Leo img, change the overlay bg) with exact snippets; plus "Shared Patterns → Reduced-Motion Gate"
    - .planning/phases/20-study-screen/20-RESEARCH.md — OQ-1 (widget-l{N}.webp at ~160×160, L9 clamp; clips/ excluded), OQ-4 (the gate pattern), Pitfall 4 (Math.min(level,9) — widget-l10.webp does not exist)
    - .planning/phases/20-study-screen/20-CONTEXT.md — D-05 (Soft-Clay Leo only, NOT the full scene/clip; "Your habitat grew!" beat + tap-to-dismiss) and D-06 (gate confetti only; flip/swipe tilt unchanged) and the Deferred note (the `level === 10` dead branch is OUT OF SCOPE — do not "fix" it to `level >= 9`)
    - src/hooks/use-prefers-reduced-motion.ts — the hook to import (already `"use client"`, SSR-safe)
    - design/handoff-daybreak/hifi-daybreak.jsx — the `d1` theme (token hexes for the confetti palette) and design/handoff-daybreak/README.md (the "accent gold #F2B33A" / celebratory amber note)
    - node_modules/next/dist/docs/01-app — a plain `<img>` with a dynamic `src` is used (not next/image) to avoid a Next.js 16 dynamic-src constraint; confirm there is no lint rule forbidding `<img>` here, else use the project's image convention. The component is already `"use client"`.
  </read_first>
  <action>
    Reskin `level-up-overlay.tsx` to Daybreak. (1) Import the gate: add `import { usePrefersReducedMotion } from "@/hooks/use-prefers-reduced-motion";` after the motion import. (2) Recolor confetti: replace `CONFETTI_COLORS` with the Daybreak palette `["#F28A1F", "#3E9B5F", "#DE5F4A", "#F2B33A", "#4A331C"]` (keep `CONFETTI_COUNT = 36` and `CONFETTI_ROTATE_DIRS` exactly). (3) Inside the component, before the return, add `const reduced = usePrefersReducedMotion();` and `const assetLevel = Math.min(level, 9);`. (4) Gate the confetti: wrap the confetti container `<div>` in `{!reduced && ( ... )}` so particles render ONLY when motion is allowed (D-06). Add `data-testid="confetti-layer"` to that container `<div>` and `data-confetti-particle` to each particle `motion.div` (these are the stable hooks Task 1 asserts on); preserve the particle motion mechanics (the `initial`/`animate` y/opacity/rotate and the `transition={{ duration: 2.5, delay: (index % 8) * 0.07, ease: "easeIn" }}`) unchanged. (5) Add the Soft-Clay Leo (D-05): inside the content `<div>` (which ALWAYS renders, even under reduced motion), add an `<img src={`/habitat/widget-l${assetLevel}.webp`} alt="Leo" width={160} height={160} style={{ borderRadius: 16, objectFit: "cover" }} />` above the "Habitat Level" label. Restyle the level numeral to Baloo 2 (`className="font-display text-[62px] font-bold text-primary leading-none"`). KEEP the `level === 10 ? "A bird arrived in your habitat!" : "Your habitat grew!"` copy condition EXACTLY as-is (do NOT change to `level >= 9` — scope fence) and keep "Tap anywhere to continue". (6) Overlay bg: change the outer `motion.div` to a cream overlay — remove `bg-background/90 backdrop-blur-sm` from the className and set `style={{ background: "rgba(255, 246, 233, 0.92)" }}` (keep the existing `onClick={onDismiss}` and the initial/animate/exit opacity fade). Do NOT change `LevelUpOverlayProps`. Add a one-line code comment at the `level === 10` copy line noting it is intentionally untouched per CONTEXT Deferred (presentation-only; logic preserved).
  </action>
  <verify>
    <automated>npx vitest run "src/components/__tests__/level-up-overlay.test.tsx" --reporter=verbose && npx tsc --noEmit && echo TSC-OK && grep -c "usePrefersReducedMotion" src/components/level-up-overlay.tsx && grep -c "widget-l" src/components/level-up-overlay.tsx && grep -c "Math.min(level, 9)" src/components/level-up-overlay.tsx && grep -c "#F28A1F" src/components/level-up-overlay.tsx && grep -c "data-testid=\"confetti-layer\"" src/components/level-up-overlay.tsx && grep -c "level === 10" src/components/level-up-overlay.tsx && (grep -v '^#' src/components/level-up-overlay.tsx | grep -c "level >= 9")</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/level-up-overlay.tsx` imports `usePrefersReducedMotion` (grep ≥ 1) and gates the confetti container with `{!reduced && ...}`
    - The confetti container has `data-testid="confetti-layer"` and each particle has `data-confetti-particle`; the Wave 0 test `npx vitest run "src/components/__tests__/level-up-overlay.test.tsx"` now exits 0 (green)
    - `CONFETTI_COLORS` is the Daybreak palette: `#F28A1F` present (grep ≥ 1); the old `#F97316`/`#3B82F6` values are gone
    - The Soft-Clay Leo renders from `widget-l` (grep ≥ 1) with the L9 clamp `Math.min(level, 9)` (grep ≥ 1) and `alt="Leo"`; it is inside the always-rendered content block (present under reduced motion)
    - The dead-branch copy condition is preserved: `grep -c "level === 10"` ≥ 1 AND `grep -v '^#' | grep -c "level >= 9"` returns 0 (the floated `level >= 9` tweak was correctly DECLINED per scope fence)
    - The overlay bg is the Daybreak cream `rgba(255, 246, 233, 0.92)` and `backdrop-blur-sm` is removed
    - `npx tsc --noEmit` exits 0 (prints `TSC-OK`)
  </acceptance_criteria>
  <done>The level-up overlay shows a static Soft-Clay Leo (widget-l{level}.webp, L9-clamped) over a cream overlay with Daybreak-recolored confetti that is suppressed under prefers-reduced-motion while Leo + the summary still render; the dead `level === 10` branch is left untouched and the Wave 0 test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin study-session chrome + committing/error + end screen to Daybreak (D-04 + Discretion), PRESERVE zone untouched</name>
  <files>src/components/study-session.tsx</files>
  <read_first>
    - src/components/study-session.tsx — read the FULL file (483 lines) to locate the boundary, but EDIT ONLY lines ~297-483. Read closely: the import block (line 9 `import { Button } from "@/components/ui/button"` — to be replaced by LionFace + TBtn imports); the PRESERVE zone lines 1-294 (reducer, computeStats, props, showLevelUp/handleLevelUpDismiss, the 300ms effect, commitIdRef, gradedRef, the commit() effect) — DO NOT TOUCH; then the four render blocks — committing (lines ~300-306), error (lines ~312-324, currently a shadcn `<Button variant="default">`), end (lines ~330-388, the 🐯 emoji + the three stats + the shadcn Button), and studying (the top bar lines ~413-449 + the quit-confirm popover + the hint line ~475-479 + the `<CardStack>`/`<StudyCard>` mounts + the `<LevelUpOverlay>` mount)
    - .planning/phases/20-study-screen/20-PATTERNS.md — `src/components/study-session.tsx` Pattern Assignment: the imports to ADD/REMOVE, and the committing / error / end / top-bar / quit-confirm / hint-line snippets with line refs, plus "Shared Patterns → TBtn / LionFace / Daybreak Display Type"
    - .planning/phases/20-study-screen/20-RESEARCH.md — OQ-5 (the end-screen mapping table: 🐯→LionFace, Baloo 2 numerals, learned stays amber, Button→TBtn), Pitfall 1 (treat lines 1-294 read-only), Pitfall 7 (replace ALL `<Button variant="default">` in the render blocks with TBtn), and the "HiFiStudy Design Spec — Verified Tokens" top bar + hint spec
    - .planning/phases/20-study-screen/20-CONTEXT.md — D-04 (LionFace, Baloo 2 numerals, learned = amber hero number, NO mini-habitat teaser) and the Claude's Discretion block (top bar / quit-confirm / "Saving your progress…" + retry — Daybreak restyle, optional light Leo touch) and the Deferred note (do NOT touch the `=== 10` dead branch in handleLevelUpDismiss)
    - src/components/daybreak/lion-face.tsx and src/components/daybreak/t-btn.tsx — the LionFace `size` prop + TBtn props/API
    - design/handoff-daybreak/hifi-shared.jsx — `HiFiStudy()` top bar (X-circle 40×40 #EDDFC9 border, centered "Study session" label, 40px spacer) and the hint pill spec (#9C8467 text, #FFFFFF bg, 1.5px #F0E3CF border, 9px 18px, radius 999)
    - node_modules/next/dist/docs/01-app — `"use client"` component + `router.push` (next/navigation) usage in Next.js 16; skim before editing (AGENTS.md — breaking changes vs training data)
  </read_first>
  <action>
    Edit ONLY the render zone (lines ~297-483) of `study-session.tsx`. Mark the read-only boundary with a comment immediately above `// Render: committing` (e.g. `// ⬇ Safe edit zone — render only. Lines above are PRESERVE (reducer/commit/idempotency).`). Imports: REMOVE `import { Button } from "@/components/ui/button"` and ADD `import { LionFace } from "@/components/daybreak/lion-face";` and `import { TBtn } from "@/components/daybreak/t-btn";` (the LevelUpOverlay import already exists). (1) committing block (Discretion): Daybreak cream bg, center a `<LionFace size={48} />` above "Saving your progress…" (optional light Leo touch). (2) error block (Pitfall 7): replace the shadcn `<Button variant="default" onClick={() => dispatch({ type: "RETRY_COMMIT" })}>` with `<TBtn style={{ maxWidth: 280 }} onClick={() => dispatch({ type: "RETRY_COMMIT" })}>Retry saving session</TBtn>`; optional `<LionFace size={56} />` above the message; keep `state.message`. (3) end block (D-04): replace the `🐯` `<span>` with `<LionFace size={80} />`; keep the headline copy "Great work, keep it up!" but style it `className="font-display text-[20px] font-bold text-foreground"`; restyle each stat numeral to `className="font-display text-[32px] font-bold text-foreground"` and the labels stay `text-sm text-muted-foreground`; the "learned" numeral KEEPS `text-primary` (amber hero number — D-04); replace the shadcn Button with `<TBtn style={{ maxWidth: 280 }} onClick={() => router.push(`/dashboard?deck=${deckId}`)}>Back to deck</TBtn>` (keep the exact router.push target). Do NOT add a mini-habitat teaser (D-04). KEEP the `<AnimatePresence>`-wrapped `<LevelUpOverlay level={showLevelUp} onDismiss={handleLevelUpDismiss} />` mount and the `showLevelUp !== null` guard exactly. (4) studying top bar (Discretion): replace it with the Daybreak chrome — an X-circle quit `<button aria-label="Quit study session" onClick={() => dispatch({ type: "TOGGLE_QUIT_CONFIRM" })}>` styled `width:40,height:40,borderRadius:999,border:"1.5px solid #EDDFC9",background:"#FFFFFF",color:"#4A331C",fontSize:15` showing `✕`, a centered `<span className="text-[14px] font-semibold text-muted-foreground" style={{ letterSpacing: 0.3 }}>Study session</span>`, and a `<span style={{ width: 40 }} aria-hidden="true" />` spacer. PRESERVE the `dispatch({ type: "TOGGLE_QUIT_CONFIRM" })` / `QUIT_SESSION` wiring exactly. (5) quit-confirm popover (Discretion): restyle to the Daybreak card surface (radius 18, #FFFFFF, 1px #F0E3CF border, soft shadow); the secondary "Keep studying" is a plain styled `<button>` (border #EDDFC9), the primary "Save and quit" is a small `<TBtn style={{ width: "auto", height: 36, padding: "0 16px", fontSize: 14, borderRadius: 10 }} onClick={() => dispatch({ type: "QUIT_SESSION" })}>`. (6) hint line below the card (D-02 HiFiStudy): restyle the `showSwipeHint` text to the Daybreak hint pill — `style={{ color: "#9C8467", background: "#FFFFFF", border: "1.5px solid #F0E3CF", padding: "9px 18px", borderRadius: 999 }}` with `→` in `#3E9B5F` and `←` in `#DE5F4A` ("Swipe → if you got it · ← still learning"). Keep the `<CardStack remainingCount={...} />` and `<StudyCard ... />` mounts with their existing props (20-01 reskinned them; signatures unchanged). DO NOT edit anything above line ~297 — the reducer, commit effect, commitIdRef, handleLevelUpDismiss (incl. the dead `=== 10` branch), and StudySessionProps stay byte-for-byte.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo TSC-OK && grep -c "daybreak/lion-face" src/components/study-session.tsx && grep -c "daybreak/t-btn" src/components/study-session.tsx && grep -c "LionFace size={80}" src/components/study-session.tsx && grep -c "font-display text-\[32px\]" src/components/study-session.tsx && grep -c "router.push(\`/dashboard?deck=\${deckId}\`)" src/components/study-session.tsx && grep -c "leveledUp === 10" src/components/study-session.tsx && (grep -v '^#' src/components/study-session.tsx | grep -c "🐯") && (grep -v '^#' src/components/study-session.tsx | grep -c "@/components/ui/button")</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/study-session.tsx` imports `LionFace` from `@/components/daybreak/lion-face` and `TBtn` from `@/components/daybreak/t-btn`; the shadcn Button import is gone: `grep -v '^#' | grep -c "@/components/ui/button"` returns 0
    - The end screen uses `<LionFace size={80} />` (grep ≥ 1) and the 🐯 emoji is gone: `grep -v '^#' | grep -c "🐯"` returns 0
    - Stat numerals use `font-display text-[32px]` (Baloo 2, grep ≥ 1); the "learned" numeral keeps `text-primary` (amber hero number, D-04)
    - "Back to deck" uses TBtn and the router target is unchanged: `grep -c 'router.push(\`/dashboard?deck=\${deckId}\`)'` ≥ 1
    - The PRESERVE zone is intact: `grep -c "leveledUp === 10"` ≥ 1 (the dead branch in handleLevelUpDismiss was NOT touched/"fixed")
    - The top bar shows the Daybreak X-circle ("Quit study session" aria-label) + centered "Study session" label + 40px spacer; the quit-confirm dispatches `QUIT_SESSION` unchanged
    - No mini-habitat teaser was added to the end screen (D-04)
    - `npx tsc --noEmit` exits 0 (prints `TSC-OK`)
  </acceptance_criteria>
  <done>The session chrome (X-circle quit, centered label, Daybreak quit-confirm), the committing/error states, and the end screen (LionFace not 🐯, Baloo 2 numerals, amber "learned" hero number, TBtn "Back to deck") are Daybreak-styled; the reducer/commit/idempotency PRESERVE zone and the dead-branch logic are untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Behavior-preservation gate — full study unit + e2e suite green after the Phase 20 reskin</name>
  <files>src/components/study-session.tsx, src/components/level-up-overlay.tsx</files>
  <read_first>
    - e2e/06-study-session.spec.ts — the study-session coverage (study card shows + "Study session" label + quit button + prompt + "Tap to reveal"; tap flips; keyboard arrows grade; quit session saves + retry-on-error). Confirm its selectors still resolve after the chrome reskin — in particular the quit control now has aria-label "Quit study session" (the X-circle); if the spec targets the OLD "Quit session" button text/label, note the mismatch in the SUMMARY rather than silently rewriting the assertion (the chrome label treatment is Claude's Discretion, but the spec is the behavior contract — prefer keeping an accessible name the spec can find)
    - e2e/study-progression.spec.ts — the full study loop → commit → end-screen progression incl. the level-up path; confirm it stays green (it may assert /Great work/ which is preserved copy; verify no selector targets the removed 🐯 emoji)
    - e2e/14-qa-parity.spec.ts — the QA-badge parity test (badge absent from the customer DOM) — must stay green (this plan does not touch the badge mount, and 20-01 preserved it)
    - .planning/phases/20-study-screen/20-VALIDATION.md — the e2e gotcha (KILL + restart `npm run dev` on :3000 before any e2e gate; `webServer: undefined`, `baseURL: localhost:3000`; prefer specific/`{exact:true}` locators — generic "Next" collides with the Next.js dev-tools button) and the Behavior-Preservation Tests table
    - playwright.config.ts — confirm `baseURL` + test dir + the absent managed `webServer`
  </read_first>
  <action>
    Verification-only task (no new product code). Prove the Phase 20 reskin (20-01 card/stack + this plan's chrome/end/level-up) preserved all behavior. FIRST, per the VALIDATION.md gotcha, kill any running dev server and start a fresh `npm run dev` on :3000 (long-running dev servers degrade and block e2e). Run `npm test` (the full vitest suite — includes the new `level-up-overlay.test.tsx` and the preserved `qa-state-badge.test.ts`). Then run the three behavior-preservation specs: `e2e/06-study-session.spec.ts`, `e2e/study-progression.spec.ts`, `e2e/14-qa-parity.spec.ts`. All must pass. If a spec is RED because a selector targeted removed styling/emoji or the old chrome label, the FIX is to restore an accessible name / preserved copy in the component (the chrome must remain operable and discoverable) — do NOT edit the spec to chase the reskin, and do NOT touch the PRESERVE zone. If a genuinely legitimate copy/selector update is unavoidable, surface it explicitly in the SUMMARY with the before/after and the reason. Confirm `tsc --noEmit` (typecheck) and `biome ci .` (lint) are clean as the final phase-readiness check.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && echo TSC-OK && npx playwright test e2e/06-study-session.spec.ts e2e/study-progression.spec.ts e2e/14-qa-parity.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (vitest run) exits 0 — the full unit suite is green, including the new `src/components/__tests__/level-up-overlay.test.tsx` (D-06 gate) and the preserved `src/components/__tests__/qa-state-badge.test.ts`
    - `npx playwright test e2e/06-study-session.spec.ts e2e/study-progression.spec.ts e2e/14-qa-parity.spec.ts` passes — the study card flow, the full study→commit→end progression, the quit/retry path, and the QA-badge parity all still work after the chrome + end-screen + level-up reskin
    - `npx tsc --noEmit` exits 0 (prints `TSC-OK`) and (recorded in SUMMARY) `biome ci .` is clean
    - No e2e spec was edited to chase the reskin; any unavoidable selector/copy change is documented in the SUMMARY with before/after + rationale; the PRESERVE zone (lines 1-294) was not touched
    - The dev server was restarted fresh before the e2e run (VALIDATION.md gotcha) — noted in the SUMMARY
  </acceptance_criteria>
  <done>The full study unit + e2e suite is green against the complete Phase 20 reskin, proving the Daybreak chrome/end-screen/level-up changes preserved the study engine, the reducer/commit/idempotency, and the QA-badge mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-only reskin of two client components + one new test. No new inputs, data flows, or auth surface. The level-up overlay's only new external resource is `public/habitat/widget-l{N}.webp` — static, same-origin assets, not user-supplied URLs (the level index is clamped to a fixed 1-9 range, so no path is attacker-controlled). `study-session.tsx`'s commit path (`POST /api/study/complete`) and the server entry `study/page.tsx` (auth + SEC-02 deck ownership) are untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-PRESERVE | Tampering | study-session.tsx reducer / commit() / commitIdRef (idempotency) | mitigate | The render-only edit boundary (lines 297-483) is enforced; the idempotent grade-commit (`POST /api/study/complete` with the stable `commitIdRef` UUID, WR-04) and the reducer are untouched. Task 4's behavior-preservation e2e (`e2e/06-study-session.spec.ts`, `e2e/study-progression.spec.ts`) gates that quit/commit/retry still work; a regression turns a spec red. |
| T-20-02-PATH | Tampering (path injection) | level-up-overlay.tsx widget-l{N}.webp img src | mitigate | The asset index is `Math.min(level, 9)` over a numeric prop — it cannot be steered outside the fixed `widget-l1..9.webp` set, so the `src` is not attacker-controllable and cannot 404/traverse. The Wave 0 test asserts the clamp. |
| T-20-02-QA | Information Disclosure | QaStateBadge parity | mitigate | This plan does not touch the badge mount (20-01 preserved it); Task 4 re-runs `e2e/14-qa-parity.spec.ts` to confirm the badge stays absent from the customer DOM after the chrome/end-screen reskin. |
| T-20-02-AC | Access Control | Deck-ownership (SEC-02) + auth in study/page.tsx | accept | Out of scope and untouched — the preserved server-side ownership/auth checks in `study/page.tsx` (read-only this phase) remain intact. No new HIGH/MEDIUM threat is introduced by this presentation-only phase. |
</threat_model>

<verification>
- `npx vitest run "src/components/__tests__/level-up-overlay.test.tsx"` — green (D-06 confetti gate + L9 clamp locked)
- `npx tsc --noEmit` — exits 0; no type errors in `level-up-overlay.tsx` or `study-session.tsx`
- `grep -c "usePrefersReducedMotion" level-up-overlay.tsx` ≥ 1; `grep -c "widget-l" level-up-overlay.tsx` ≥ 1; `grep -c "#F28A1F" level-up-overlay.tsx` ≥ 1
- `grep -c "level === 10" level-up-overlay.tsx` ≥ 1 and `grep -v '^#' | grep -c "level >= 9"` = 0 (dead-branch copy preserved; floated tweak declined)
- `grep -c "daybreak/lion-face" study-session.tsx` ≥ 1; `grep -v '^#' | grep -c "🐯"` = 0; `grep -c "leveledUp === 10" study-session.tsx` ≥ 1 (PRESERVE zone intact)
- `npm test` — green (full unit suite incl. new level-up test + qa-state-badge)
- `npx playwright test e2e/06-study-session.spec.ts e2e/study-progression.spec.ts e2e/14-qa-parity.spec.ts` — green (behavior preserved; fresh dev server on :3000 first)
- Manual/visual (deferred to phase gate, --skip-ui): end screen (LionFace not 🐯, Baloo 2 numerals, amber "learned", amber "Back to deck"), level-up Soft-Clay Leo + recolored confetti + tap-to-dismiss, Daybreak chrome — all vs `hifi-shared.jsx` HiFiStudy + the Daybreak references
</verification>

<success_criteria>
- The end screen is Daybreak: LionFace replaces 🐯, the three stats use Baloo 2 display numerals, "learned" stays the amber hero number, and "Back to deck" is a Daybreak TBtn — no mini-habitat teaser (STU-02, D-04)
- The level-up overlay shows a static Soft-Clay Leo (widget-l{level}.webp, L9-clamped) with Daybreak-recolored confetti, keeping the "Your habitat grew!" beat + tap-to-dismiss (STU-02, D-05)
- The level-up confetti is gated behind prefers-reduced-motion (no particles under reduced motion; Leo + summary still show), locked by a green Wave 0 component test; the flip/swipe tilt are unchanged (STU-02, D-06)
- The session chrome + committing/error states are Daybreak-restyled, and the reducer/commit/idempotency PRESERVE zone (lines 1-294) and the dead-branch logic are untouched — proven by a green full study unit + e2e suite
</success_criteria>

<output>
Create `.planning/phases/20-study-screen/20-02-SUMMARY.md` when done. Record: the exact confetti palette used, confirmation that the Soft-Clay Leo is the static widget-l{N}.webp (L9-clamped, not a clip/live render), that the D-06 confetti gate is locked by a green component test, that the `level === 10` / `leveledUp === 10` dead branches were intentionally left untouched (CONTEXT Deferred — not "fixed" to `level >= 9`), that the PRESERVE zone (lines 1-294) was not modified, that the dev server was restarted fresh before the e2e gate, and the full study suite (unit + the three e2e specs) is green. Note any e2e selector/copy adjustment with before/after + rationale if one was unavoidable.
</output>
