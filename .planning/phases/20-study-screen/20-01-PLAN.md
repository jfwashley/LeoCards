---
phase: 20-study-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/study-card.tsx
  - src/components/card-stack.tsx
autonomous: true
requirements: [STU-01]
must_haves:
  truths:
    - "D-01: the ghost-peek stack (card-stack.tsx) adopts the Daybreak card surface (radius 22, #FFFFFF, 1px #F0E3CF border) while keeping its existing count-aware geometry — up to 3 layers that thin out as the session nears its end — and is the ONLY remaining-cards cue on the screen"
    - "D-02: the flashcard (study-card.tsx) uses the Daybreak card surface (radius 22, #FFFFFF, 1px #F0E3CF border, shadow 0 12px 30px rgba(160,110,40,0.16)) and progressive swipe color feedback uses green #3E9B5F (swipe-right/knew it) and red #DE5F4A (swipe-left/still learning), replacing the generic green-100/red-100 overlays"
    - "D-02: the fixed interaction model is preserved byte-for-byte — tap/Enter/Space to flip, 3D Y-axis flip (~300ms), 300ms swipe-enable guard, swipe-right=knew it / swipe-left=still learning, keyboard arrows as the non-touch path (handleKeyDown + handleDragEnd threshold velocity>500||offset>80 unchanged)"
    - "D-03: no new progress indicator is added — the count-aware ghost-peek stack stays the sole 'cards remaining' cue (no '4 of 12' counter, no progress bar)"
    - "The QaStateBadge mount condition is preserved exactly ({qaCardData && <QaStateBadge .../>}, renders only when QA-authed) and is not clipped by the new 22px card radius"
    - "The existing study e2e suite (e2e/06-study-session.spec.ts, e2e/study-progression.spec.ts, e2e/14-qa-parity.spec.ts) stays green — behavior is unchanged, only presentation differs"
  artifacts:
    - path: "src/components/study-card.tsx"
      provides: "Daybreak-skinned flashcard: card surface, ALL-CAPS prompt, 42px Baloo 2 word, amber Tap-to-reveal pill, green/red swipe overlays; flip/swipe/keyboard logic preserved"
      contains: "rgba(62,155,95"
    - path: "src/components/card-stack.tsx"
      provides: "Count-aware ghost-peek stack reskinned to the Daybreak card surface; geometry (translateY/scale/opacity) preserved"
      contains: "#F0E3CF"
  key_links:
    - from: "src/components/study-card.tsx"
      to: "swipe feedback overlay"
      via: "useTransform on motion x value with Daybreak green/red rgba"
      pattern: "rgba\\(62,155,95"
    - from: "src/components/study-card.tsx"
      to: "QaStateBadge"
      via: "qaCardData-gated mount (preserved)"
      pattern: "qaCardData && <QaStateBadge"
    - from: "src/components/card-stack.tsx"
      to: "Daybreak card surface tokens"
      via: "inline style on the layer divs"
      pattern: "borderRadius: 22"
---

<objective>
Reskin the study flashcard (`study-card.tsx`) and the count-aware ghost-peek stack (`card-stack.tsx`) to the Daybreak design system. This is a presentation-only change: the Daybreak card surface, the ALL-CAPS "WHAT'S THE TRANSLATION?" prompt, the 42px Baloo 2 word, the amber "Tap to reveal" pill, the green/red swipe color feedback, and the Daybreak hint cues replace the current shadcn `bg-card`/`rounded-xl` styling — while the flip, swipe, keyboard, 300ms swipe-enable guard, and the QaStateBadge mount are preserved byte-for-byte (D-02). The ghost-peek stack adopts the Daybreak card surface but keeps its count-aware geometry as the sole "cards remaining" cue (D-01, D-03).

Purpose: STU-01 — the study card is the most-used screen in the app and the visual centerpiece of the study flow. It must match the Daybreak hi-fi (`hifi-shared.jsx` `HiFiStudy()`) while the SRS interaction model stays exactly as shipped.
Output: Reskinned `src/components/study-card.tsx` and `src/components/card-stack.tsx`.

NOTE (cross-plan sequencing): This plan is Wave 1 and touches only `study-card.tsx` + `card-stack.tsx`. The session chrome, end screen, and level-up overlay are reskinned in 20-02 (Wave 2), which imports `StudyCard`/`CardStack` by their UNCHANGED public signatures. Do NOT change the props of either component — `StudyCard` and `CardStack` must keep the exact prop shapes `study-session.tsx` already passes (verified read-only zone). This plan does not touch `study-session.tsx`.

NOTE (deferred logic — do not touch): The reduced-motion variants for the 3D flip (crossfade) and the swipe tilt (calm/instant) are explicitly DEFERRED to a later accessibility pass (CONTEXT Deferred). This phase keeps the flip and swipe tilt at their current behavior; do NOT gate them behind `prefers-reduced-motion` here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-study-screen/20-CONTEXT.md
@.planning/phases/20-study-screen/20-RESEARCH.md
@.planning/phases/20-study-screen/20-PATTERNS.md

<interfaces>
<!-- Daybreak surface tokens (the single source of truth). Sourced from auth-card.tsx inner card div (lines 184-194) + card.tsx (lines 14-20), confirmed identical. -->
Daybreak card surface (apply via inline style on a face/layer div):
  borderRadius: 22, background: "#FFFFFF", border: "1px solid #F0E3CF",
  boxShadow: "0 12px 30px rgba(160, 110, 40, 0.16)", boxSizing: "border-box"

<!-- HiFiStudy() verified tokens (design/handoff-daybreak/hifi-shared.jsx). -->
Prompt label: 12.5px, fontWeight 700, letterSpacing 2.2, muted color (#9C8467), ALL-CAPS — "WHAT'S THE TRANSLATION?"
Word: font-display (Baloo 2), 42px, fontWeight 700, ink color (#4A331C)
"Tap to reveal" pill: bottom 22px, 13px, fontWeight 600, color #B4762A, background #FFF1DC, padding 8px 16px, borderRadius 999
Swipe feedback: green #3E9B5F (right/knew it), red #DE5F4A (left/still learning)

<!-- Daybreak swipe rgba ramps (replace ONLY the color values in the two useTransform overlays). -->
bgColorRight: x [0,40,200] → ["rgba(0,0,0,0)", "rgba(62,155,95,0.6)", "rgba(62,155,95,0.8)"]
bgColorLeft:  x [-200,-40,0] → ["rgba(222,95,74,0.8)", "rgba(222,95,74,0.6)", "rgba(0,0,0,0)"]
</interfaces>

<preserve_zone>
<!-- PRESERVE / do-not-touch in study-card.tsx (from 20-PATTERNS.md). Any failure in e2e/06-study-session.spec.ts, e2e/study-progression.spec.ts, or e2e/14-qa-parity.spec.ts means this zone was modified. -->
- handleKeyDown (lines 57-70) — Enter/Space flip, Arrow grade — BEHAVIORAL, do not touch
- handleDragEnd threshold (lines 72-81) — velocity > 500 || offset > 80 — BEHAVIORAL, do not touch
- 3D flip mechanics (transformStyle: preserve-3d, backfaceVisibility, rotateY animate + 300ms duration) — keep exactly; only the surface CLASSNAME→inline-style and the content inside the faces change
- QaStateBadge mount + qaCardData build (lines 27-37, 111) — {qaCardData && <QaStateBadge data={qaCardData} />} — renders only when QA-authed; do not change the condition
- The motion x value and the useTransform tilt — only the swipe-overlay COLOR rgba values change (not the input ranges, not the x value, not the tilt)
- The StudyCard and CardStack PROP SHAPES — consumed by study-session.tsx (read-only this phase); signatures must not change
</preserve_zone>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin the ghost-peek stack (card-stack.tsx) to the Daybreak card surface (D-01/D-03)</name>
  <files>src/components/card-stack.tsx</files>
  <read_first>
    - src/components/card-stack.tsx — the full current file (30 lines): the `CardStackProps { remainingCount }` interface, `visibleLayers = Math.min(3, remainingCount)`, the `if (visibleLayers === 0) return null`, the `opacities = [0.6, 0.35, 0.15]` array, and the layer `<div>` with `className="absolute inset-0 rounded-xl bg-card border border-border"` + the `transform: translateY((i+1)*8)px scale(1-(i+1)*0.03)` / `zIndex: -(i+1)` / `opacity` style (line 20) — the count-aware geometry that MUST be preserved
    - .planning/phases/20-study-screen/20-PATTERNS.md — `src/components/card-stack.tsx` Pattern Assignment: the "What changes: replace only the className on the layer divs" section and the full post-reskin component (lines 226-260), plus the Daybreak card-surface token block under "Shared Patterns → Daybreak Card Surface"
    - src/components/daybreak/auth-card.tsx — the `GhostPeek` atom (the Daybreak peeking-edge look) and the inner card div surface tokens (lines ~184-194: background #FFFFFF, border 1px solid #F0E3CF, borderRadius 22) — the token source
    - src/components/daybreak/card.tsx — the standalone Daybreak `Card` surface (same tokens, confirmation)
    - design/handoff-daybreak/hifi-shared.jsx — `HiFiStudy()` ghost-peek reference (study spec uses 68%/82% conceptual widths vs auth's 62%/76% — Pitfall 2; geometry here stays translateY/scale, so this is a sizing reference only)
    - node_modules/next/dist/docs/01-app — this is a `"use client"` component in Next.js 16; skim the relevant client-component guidance before editing (this is NOT the Next.js you know — AGENTS.md)
  </read_first>
  <action>
    Replace ONLY the visual surface of each ghost layer div in `card-stack.tsx`. Remove the `rounded-xl bg-card border border-border` classes from the layer `<div>` (keep `className="absolute inset-0"`), and add the Daybreak card-surface tokens to the inline `style` object alongside the existing geometry: add `borderRadius: 22`, `background: "#FFFFFF"`, `border: "1px solid #F0E3CF"`, and `boxSizing: "border-box"`. PRESERVE exactly the count-aware geometry and structure: `visibleLayers = Math.min(3, remainingCount)`, the `if (visibleLayers === 0) return null` guard, the `opacities = [0.6, 0.35, 0.15]` array, and the per-layer `transform: translateY(${(i + 1) * 8}px) scale(${1 - (i + 1) * 0.03})` + `zIndex: -(i + 1)` + `opacity: opacities[i]`. Keep the `// biome-ignore lint/suspicious/noArrayIndexKey` comment on the `key={i}` (static positional layers). Do NOT change `CardStackProps` or the component's exported signature. Do NOT add any count text, progress bar, or "N of M" indicator (D-03 — the stack is the sole remaining-cards cue). The component stays `"use client"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo TSC-OK && grep -c "borderRadius: 22" src/components/card-stack.tsx && grep -c "#F0E3CF" src/components/card-stack.tsx && grep -c "Math.min(3, remainingCount)" src/components/card-stack.tsx && (grep -v '^#' src/components/card-stack.tsx | grep -c "rounded-xl\|bg-card\|border-border")</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/card-stack.tsx` layer divs use the Daybreak surface inline tokens: `borderRadius: 22` (grep ≥ 1), `background: "#FFFFFF"`, and `border: "1px solid #F0E3CF"` (grep `#F0E3CF` ≥ 1)
    - The shadcn surface classes are gone from the layer divs: `grep -v '^#' | grep -c "rounded-xl\|bg-card\|border-border"` returns 0
    - The count-aware geometry is preserved verbatim: `Math.min(3, remainingCount)` present, `opacities` array `[0.6, 0.35, 0.15]` present, `translateY(${(i + 1) * 8}px) scale(${1 - (i + 1) * 0.03})` present
    - No "N of M" / progress text or bar is added (D-03) — the file renders only the ghost layers, no text nodes
    - `CardStackProps` is unchanged (still just `{ remainingCount: number }`)
    - `npx tsc --noEmit` exits 0 (prints `TSC-OK`) — gating on the exit code, not on a grep of tsc output
  </acceptance_criteria>
  <done>The ghost-peek stack renders Daybreak white card edges with the amber #F0E3CF border at radius 22, stays count-aware (1→2→3 layers thinning toward session end), adds no progress indicator, and the component signature is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin the flashcard (study-card.tsx) — Daybreak surface, swipe colors, prompt/word/pill (D-02), preserving flip/swipe/keyboard + QA badge</name>
  <files>src/components/study-card.tsx</files>
  <read_first>
    - src/components/study-card.tsx — the FULL current file (179 lines). Specifically: the `import { motion, useMotionValue, useTransform } from "motion/react"` (line 3); the `qaCardData` build + props (lines 27-37); the two swipe-overlay `useTransform` ramps `bgColorRight`/`bgColorLeft` (lines 40-49) whose rgba COLORS change; `handleKeyDown` (lines 57-70) and `handleDragEnd` (lines 72-81) which are PRESERVE-zone; the two swipe-overlay `<motion.div ... rounded-xl>` (lines 101-108); the `QaStateBadge` mount `{qaCardData && ...}` (line 111); the 3D flip wrapper + the two face `<motion.div>` with `bg-card border border-border rounded-xl` + `backfaceVisibility`/`rotateY` (lines 115-177); and the current front/back content (prompt, word, "Tap to reveal", swipe hint)
    - .planning/phases/20-study-screen/20-PATTERNS.md — `src/components/study-card.tsx` Pattern Assignment: items 1-6 (swipe overlay colors, overlay border-radius, card face surfaces, front-face content, back-face content + arrow cues, QaStateBadge breathing room) with the exact replacement snippets and line refs
    - .planning/phases/20-study-screen/20-RESEARCH.md — OQ-3 (exact swipe rgba swap), "HiFiStudy Design Spec — Verified Tokens" (card area: prompt 12.5px/700/2.2 muted, word Baloo 2 42px ink, Tap-to-reveal pill #B4762A on #FFF1DC), and Pitfalls 5 (QA badge corner breathing at radius 22) + 6 (overlay border-radius must move rounded-xl → rounded-[22px])
    - src/components/daybreak/auth-card.tsx — the inner card div surface tokens (lines ~184-194) — the canonical inline-style surface to copy onto both faces
    - design/handoff-daybreak/hifi-shared.jsx — `HiFiStudy()` card area (the front-face prompt/word/pill and the back-face arrow cues `←`/`→` in red/green) — the visual contract
    - src/components/qa-state-badge.tsx — read ONLY to confirm the badge self-positions in a corner (so the radius-22 surface needs no overflow-hidden change); do NOT modify this file
    - node_modules/next/dist/docs/01-app — `"use client"` + motion/react usage in Next.js 16; skim before editing (AGENTS.md — breaking changes vs training data)
  </read_first>
  <action>
    Reskin the flashcard surface and content to Daybreak while preserving ALL interaction logic. (1) Swipe colors (D-02): in the two `useTransform` overlays, replace ONLY the rgba color values — `bgColorRight` uses `["rgba(0,0,0,0)", "rgba(62,155,95,0.6)", "rgba(62,155,95,0.8)"]` (Daybreak green #3E9B5F) and `bgColorLeft` uses `["rgba(222,95,74,0.8)", "rgba(222,95,74,0.6)", "rgba(0,0,0,0)"]` (Daybreak red #DE5F4A). Do NOT change the input ranges `[0,40,200]`/`[-200,-40,0]`, the `x` motion value, or the tilt useTransform. (2) Overlay radius (Pitfall 6): change the two swipe-overlay divs' `rounded-xl` to `rounded-[22px]` so they match the card corners. (3) Card faces (D-02): on BOTH face `<motion.div>` elements, remove `bg-card border border-border rounded-xl` from the className and add the Daybreak surface via inline `style`: `borderRadius: 22, background: "#FFFFFF", border: "1px solid #F0E3CF", boxShadow: "0 12px 30px rgba(160, 110, 40, 0.16)"` — KEEPING the existing `backfaceVisibility: "hidden"` / `WebkitBackfaceVisibility: "hidden"` / the back face's `rotateY: -180` and the `animate={{ rotateY: ... }}` + `transition={{ duration: 0.3, ease: [0.4,0,0.2,1] }}` exactly. (4) Front-face content (HiFiStudy): style the prompt as `className="text-[12.5px] font-bold tracking-[2.2px] text-muted-foreground text-center uppercase"`, the word as `className="font-display text-[42px] font-bold text-foreground text-center leading-[1.15]"`, and the "Tap to reveal" pill (rendered only when `!flipped`) as `className="absolute bottom-[22px] text-[13px] font-semibold rounded-full px-4 py-2"` with inline `style={{ color: "#B4762A", background: "#FFF1DC" }}`. (5) Back-face content: keep the translation word in the same `font-display text-[42px]` style; render the swipe arrow cue (only when `swipeReady`) as a centered 13px line with `←` in `#DE5F4A` and `→` in `#3E9B5F` (e.g. "← still learning · got it →"). (6) QA badge (Pitfall 5): do NOT change the `{qaCardData && <QaStateBadge data={qaCardData} />}` mount (line 111) — only verify the outer `motion.div` wrapper has no `overflow-hidden` that would clip the badge at the new 22px radius (it does not; the badge self-positions). PRESERVE byte-for-byte: `handleKeyDown`, `handleDragEnd` (velocity>500 || offset>80), the 300ms-guarded `swipeReady` consumption, the 3D flip preserve-3d wrapper, and the `StudyCard` prop signature. No new imports are needed (motion/react already imported).
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo TSC-OK && grep -c "rgba(62,155,95" src/components/study-card.tsx && grep -c "rgba(222,95,74" src/components/study-card.tsx && grep -c "borderRadius: 22" src/components/study-card.tsx && grep -c "rounded-\[22px\]" src/components/study-card.tsx && grep -c "qaCardData && <QaStateBadge" src/components/study-card.tsx && grep -c "handleDragEnd" src/components/study-card.tsx && (grep -v '^#' src/components/study-card.tsx | grep -c "bg-card\|border-border")</automated>
  </verify>
  <acceptance_criteria>
    - Swipe feedback uses the Daybreak palette: `rgba(62,155,95` (green #3E9B5F) ≥ 1 AND `rgba(222,95,74` (red #DE5F4A) ≥ 1; the old `rgba(220,252,231` / `rgba(254,226,226` values are gone
    - Both card faces use the Daybreak surface inline tokens: `borderRadius: 22` (grep ≥ 1, expect 2) and `boxShadow: "0 12px 30px rgba(160, 110, 40, 0.16)"` present; the shadcn surface classes are gone: `grep -v '^#' | grep -c "bg-card\|border-border"` returns 0
    - The swipe-overlay divs use `rounded-[22px]` (grep ≥ 1) to match the card corners (Pitfall 6)
    - The front face renders the ALL-CAPS prompt (`uppercase` + `tracking-[2.2px]`) and the word in `font-display text-[42px]`; the "Tap to reveal" pill uses `color: "#B4762A"` / `background: "#FFF1DC"`
    - The QaStateBadge mount condition is unchanged: `grep -c "qaCardData && <QaStateBadge"` ≥ 1
    - The interaction logic is preserved: `handleDragEnd` present (grep ≥ 1); `handleKeyDown` present; the `velocity > 500` / `offset > 80` threshold and the `rotateY` flip transition are untouched
    - `npx tsc --noEmit` exits 0 (prints `TSC-OK`)
  </acceptance_criteria>
  <done>The flashcard renders the Daybreak card surface (radius 22, #FFFFFF, #F0E3CF border, soft shadow) with the ALL-CAPS prompt, 42px Baloo 2 word, amber Tap-to-reveal pill, and green/red swipe feedback; the flip, swipe threshold, keyboard path, 300ms swipe guard, and QaStateBadge mount are all preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Behavior-preservation gate — existing study e2e stays green after the reskin</name>
  <files>src/components/study-card.tsx, src/components/card-stack.tsx</files>
  <read_first>
    - e2e/06-study-session.spec.ts — the existing study-session coverage (study card shows, tap flips, keyboard arrows grade, quit session saves/retries) — the selectors/strings it asserts against; confirm none of them target the removed shadcn classes or the old swipe-overlay rgba (they assert behavior + copy, not styling)
    - e2e/study-progression.spec.ts — the full study loop + commit + end-screen progression; confirm it still passes after the card reskin (no behavior change)
    - e2e/14-qa-parity.spec.ts — the QA-badge parity test (badge absent from the customer DOM); confirm the preserved `{qaCardData && ...}` mount keeps this green
    - .planning/phases/20-study-screen/20-VALIDATION.md — the e2e gotcha (kill + restart `npm run dev` on :3000 before any e2e gate; `webServer: undefined` + `baseURL: localhost:3000`; prefer specific/`{exact:true}` locators — generic "Next" collides with the Next.js dev-tools button) and the Behavior-Preservation Tests table
    - playwright.config.ts — confirm `baseURL` + test dir + the lack of a managed `webServer` (a fresh dev server must already be running)
  </read_first>
  <action>
    This is a verification-only task — it adds NO new code; it proves the Tasks 1-2 reskin did not break behavior (it is a presentation-only phase). Per the VALIDATION.md gotcha, FIRST kill any running dev server and start a fresh one: `npm run dev` on port 3000 (long-running dev servers degrade and block e2e). Then run the three behavior-preservation specs and confirm they pass against the reskinned card + stack: `e2e/06-study-session.spec.ts` (reducer/state transitions, tap-flip, keyboard arrows, quit/retry), `e2e/study-progression.spec.ts` (full loop → commit → end screen), and `e2e/14-qa-parity.spec.ts` (QaStateBadge absent from the customer DOM — proves the preserved mount condition). If any spec is RED, the regression is in Tasks 1-2 (the PRESERVE zone was modified) — fix the reskin, do NOT edit the specs to make them pass (these specs are the contract). If a spec asserts copy that the reskin legitimately changed (it should not — Task 2 keeps the prompt/hint copy semantics), surface it in the SUMMARY rather than silently rewriting the assertion. Also run `npm test` (vitest) to confirm the existing unit suite (incl. `qa-state-badge.test.ts`) is unaffected.
  </action>
  <verify>
    <automated>npm test && npx playwright test e2e/06-study-session.spec.ts e2e/study-progression.spec.ts e2e/14-qa-parity.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (vitest run) exits 0 — the existing unit suite, including `src/components/__tests__/qa-state-badge.test.ts`, is green
    - `npx playwright test e2e/06-study-session.spec.ts e2e/study-progression.spec.ts e2e/14-qa-parity.spec.ts` passes — tap-flip, keyboard-arrow grading, quit/retry, the full study→commit→end loop, and the QA-badge parity all still work after the reskin
    - No e2e spec file was edited to make it pass (the specs are the behavior contract; any red signals a Task 1-2 regression to fix in the components)
    - The dev server was restarted fresh before the e2e run (VALIDATION.md gotcha) — note this in the SUMMARY
  </acceptance_criteria>
  <done>The existing study unit + e2e suites are green against the reskinned study-card.tsx and card-stack.tsx, proving the Daybreak reskin preserved the study engine, the interaction model, and the QA-badge mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a presentation-only reskin of two client components. It introduces no new inputs, no new data flows, and no new auth surface. The only data crossing into these components are existing props (`card`, `flipped`, `swipeReady`, `remainingCount`, `qaMode`-derived `qaCardData`) passed by `study-session.tsx`, themselves originating from the preserved server entry `study/page.tsx`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-PRESERVE | Tampering | study-card.tsx interaction logic (flip/swipe/keyboard/300ms guard) | mitigate | The grade/requeue path is unchanged — only styling and card content change. The behavior-preservation e2e (Task 3: `e2e/06-study-session.spec.ts`, `e2e/study-progression.spec.ts`) gates that the reducer-driven flow still works; any deviation turns a spec red. |
| T-20-01-QA | Information Disclosure | QaStateBadge mount in study-card.tsx | mitigate | The `{qaCardData && <QaStateBadge .../>}` condition (renders ONLY when QA-authed) is preserved verbatim; `e2e/14-qa-parity.spec.ts` asserts the badge is absent from the customer (non-QA) DOM, so a regression that leaks QA internals to customers turns the parity spec red. |
| T-20-01-AC | Access Control | Deck-ownership (SEC-02) + auth in study/page.tsx | accept | Out of scope and untouched — the server-side ownership check and auth in `study/page.tsx` (read-only this phase) remain intact; this plan never touches the RSC entry or any query/route. No new HIGH/MEDIUM threat is introduced. |
</threat_model>

<verification>
- `npx tsc --noEmit` — exits 0; no type errors in `study-card.tsx` or `card-stack.tsx`
- `grep -c "rgba(62,155,95" study-card.tsx` ≥ 1 and `grep -c "rgba(222,95,74" study-card.tsx` ≥ 1 (Daybreak swipe palette)
- `grep -c "borderRadius: 22" card-stack.tsx` ≥ 1 and `grep -c "#F0E3CF" card-stack.tsx` ≥ 1 (Daybreak ghost-peek surface)
- `grep -c "qaCardData && <QaStateBadge" study-card.tsx` ≥ 1 (QA badge mount preserved)
- `npm test` — green (existing unit suite incl. qa-state-badge)
- `npx playwright test e2e/06-study-session.spec.ts e2e/study-progression.spec.ts e2e/14-qa-parity.spec.ts` — green (behavior preserved; fresh dev server on :3000 first)
- Manual/visual (deferred to phase gate, --skip-ui): the study card + ghost-peek stack match `hifi-shared.jsx` HiFiStudy — Daybreak card surface, ALL-CAPS prompt, amber "Tap to reveal" pill, green/red swipe feedback, count-aware stack
</verification>

<success_criteria>
- The study card is Daybreak-skinned: card surface (radius 22, #FFFFFF, #F0E3CF border, soft shadow), ALL-CAPS "WHAT'S THE TRANSLATION?" prompt, 42px Baloo 2 word, amber "Tap to reveal" pill, green #3E9B5F / red #DE5F4A swipe feedback (STU-01, D-02)
- The ghost-peek stack adopts the Daybreak card surface and stays count-aware (1→3 layers) as the sole remaining-cards cue — no new progress indicator (STU-01, D-01, D-03)
- The interaction model (tap/Enter/Space flip, 3D flip ~300ms, 300ms swipe-enable guard, swipe-right=knew/left=still-learning, keyboard arrows) and the QaStateBadge mount are preserved byte-for-byte (D-02), proven by green behavior-preservation e2e
</success_criteria>

<output>
Create `.planning/phases/20-study-screen/20-01-SUMMARY.md` when done. Record: confirmation that `StudyCard`/`CardStack` prop signatures are unchanged (so 20-02 can import them as-is), that the swipe palette is now Daybreak green/red, that the QaStateBadge mount + interaction logic were preserved, that the dev server was restarted fresh before the e2e gate, and that the three behavior-preservation specs are green.
</output>
