---
phase: 27-performance-batch-2
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/study/complete/route.ts
  - src/app/api/study/complete/route.test.ts
autonomous: true
requirements: [PERF-21]
must_haves:
  truths:
    - "The ownership check, card load, and factsBefore reads run in one Promise.all, not a 3-step waterfall"
    - "factsAfter is derived in JS from factsBefore + computed card updates, not a second getHabitatFacts call"
    - "The commit's observable behavior (mastery updates, habitat facts) is unchanged"
  artifacts:
    - path: "src/app/api/study/complete/route.ts"
      provides: "Promise.all read consolidation + derived factsAfter"
      contains: "Promise.all"
  key_links:
    - from: "src/app/api/study/complete/route.ts"
      to: "getHabitatFacts"
      via: "called once (factsBefore only), factsAfter derived"
      pattern: "getHabitatFacts"
---

<objective>
Ship item 17 (PERF-21): trim the `/api/study/complete` READ path. The ownership check → card load → `getHabitatFacts` waterfall can be `Promise.all`'d (factsBefore depends only on `session.user.id`, known before any query), and `factsAfter` (a second full `getHabitatFacts`, 2 more queries) is derivable from `factsBefore` + the computed card updates. Both reviewers flagged the double habitat fetch. The WRITE side is already the shipped 26-02 `db.batch()`; this is the read-side counterpart.

Purpose: Eliminate 2 of the route's ~5-6 DB round trips per study commit with zero behavior change.
Output: Promise.all'd reads + derived factsAfter + count-assertion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-RESEARCH.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promise.all the read waterfall + derive factsAfter</name>
  <read_first>
    - src/app/api/study/complete/route.ts (ownership check lines 135-147; card load 150-160; factsBefore line 179; the db.batch() write lines 228-291/282-295 — unchanged; factsAfter second getHabitatFacts at line 298)
    - src/lib/habitat-queries.ts (getHabitatFacts internals lines 28-44 — confirm it returns only userId/lastActivityAt/learnedCardCount, no hidden fields, so factsAfter is fully derivable)
    - src/app/api/study/complete/route.test.ts (existing test to extend)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Pattern 6 — the exact Promise.all + derivation shape)
  </read_first>
  <behavior>
    - Test 1: The route issues ONE fewer getHabitatFacts call than before (getHabitatFacts called once, not twice) — count assertion.
    - Test 2: The derived factsAfter equals what a live re-fetch would return for a fixture where some cards cross the masteryRound>=3 "learned" threshold (learnedCardCount = factsBefore.learnedCardCount + crossedToLearned; lastActivityAt = now).
    - Test 3: A card that does NOT cross the threshold does not increment learnedCardCount.
  </behavior>
  <action>
    Collapse the ownership check, card load, and `factsBefore` into one `Promise.all([...])` — factsBefore (getHabitatFacts on session.user.id) has ZERO dependency on the ownership/card results, so it parallelizes safely. Preserve the early-return ownership gate: after the Promise.all resolves, still return 403 if the owned-deck row is absent (do the guard check AFTER the parallel fetch, on ownedDeckRows). Keep the `db.batch()` WRITE side (lines 228-291) byte-unchanged — item 17 is read-side only.
    Replace the second `getHabitatFacts` (`factsAfter`, line 298) with a JS derivation: `crossedToLearned = cardUpdates.filter(u => (cardMap.get(u.cardId)?.masteryRound ?? 0) < 3 && u.newRound >= 3).length`; then `factsAfter = { userId: factsBefore.userId, lastActivityAt: now, learnedCardCount: factsBefore.learnedCardCount + crossedToLearned }`. First confirm (read) getHabitatFacts returns no fields beyond these three — if it does, keep the re-fetch and document the deviation. Extend route.test.ts with the count + derivation-equality assertions (never a timing gate, D-09).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/study/complete/route.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getHabitatFacts" src/app/api/study/complete/route.ts` shows exactly ONE invocation (factsBefore); factsAfter is derived, not fetched
    - `grep -n "Promise.all" src/app/api/study/complete/route.ts` shows the reads consolidated
    - The db.batch() write block is unchanged (diff shows no edit to lines 228-291)
    - Test proves getHabitatFacts called once + factsAfter-equals-derived for a threshold-crossing fixture
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Reads Promise.all'd, factsAfter derived, write batch untouched; count + derivation test-proven; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| study client → /api/study/complete | Ownership gate must remain before any write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09-01 | Elevation of Privilege | ownership gate ordering | mitigate | Parallelizing the reads must NOT skip the 403 ownership guard — the guard runs on ownedDeckRows AFTER the Promise.all, before the db.batch() write (ASVS V4, unchanged access-control predicate) |
| T-27-09-02 | Tampering | derived factsAfter correctness | mitigate | factsAfter is derived deterministically from in-memory factsBefore + cardUpdates; test asserts equality with a live-fetch equivalent (no silent drift) |
| T-27-09-SC | Tampering | npm installs | n/a | No package installs this phase |
</threat_model>

<verification>
- `npx vitest run src/app/api/study/complete/route.test.ts` passes; `npx tsc --noEmit` clean
- After the wave: full `npm test` green; `npm run qa:run` (study/SRS path — items 8/12/17, D-10) all journeys pass; `e2e/06-study-session.spec.ts` + `study-progression.spec.ts` pass against a restarted prod-parity server
- Manual/informal (prod build only): a study commit shows 2 fewer DB round trips in logs
</verification>

<success_criteria>
- PERF-21 shipped: read waterfall Promise.all'd, factsAfter derived (2 fewer round trips), write batch and ownership gate intact
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-09-SUMMARY.md` when done
</output>
