---
phase: 27-performance-batch-2
plan: 04
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/app/(protected)/dashboard/page.tsx
  - src/lib/deck-queries.ts
  - src/lib/study-queries.ts
  - src/lib/__tests__/dashboard-data.test.ts
autonomous: true
requirements: [PERF-16]
must_haves:
  truths:
    - "The dashboard fetches a deck's cards with ONE query, not getDeckCards + getStudyCards"
    - "The study subset is derived in JS, not stitched with an O(n²) find"
    - "Native language is read from session.user.nativeLanguage, not a separate getUserNativeLanguage query"
    - "The unread createdAt field is not serialized into the client payload"
  artifacts:
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "consolidated single-query data pass"
      contains: "session.user.nativeLanguage"
    - path: "src/lib/__tests__/dashboard-data.test.ts"
      provides: "query-call-count + study-subset-derivation assertion"
      contains: "toHaveBeenCalledTimes"
  key_links:
    - from: "src/app/(protected)/dashboard/page.tsx"
      to: "src/lib/study-queries.ts"
      via: "single card query + JS-derived study subset"
      pattern: "getDeckCards|getStudyCards"
---

<objective>
Ship item 12 (PERF-16): consolidate the dashboard's data pass. Three stacked wins — (1) it fetches the same deck's cards twice (`getDeckCards` all-columns + `getStudyCards` subset) then stitches them with an O(n²) find; (2) `getUserNativeLanguage` is a separate query for a field better-auth already returns on `session.user`; (3) `createdAt` is serialized per row but never read by the client. One query, derive the study subset in JS, drop the dead field.

Purpose: This cost multiplies with item 9 — every pause-toggle refresh re-pays the dashboard pass.
Output: One card query, JS-derived study subset, session-field native language, dropped createdAt; new query-layer test (Wave 0 gap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-RESEARCH.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md

<interfaces>
Depends on 27-01's `src/lib/auth-session.ts` (`getSession()`).
Session-type gotcha (25-04): `session.user.nativeLanguage` types `string | null | undefined` despite its `defaultValue: "en"` config — normalize with `?? "en"` exactly as `account/page.tsx:39` does.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write dashboard query-layer consolidation test (Wave 0)</name>
  <read_first>
    - src/app/(protected)/dashboard/page.tsx (lines 199-209 the existing Promise.all; 235-238 the getDeckCards+getStudyCards double fetch; 271-285 cardRows reading c.createdAt — verify actual createdAt usage before dropping)
    - src/lib/deck-queries.ts (getDeckCards, getUserNativeLanguage:68) and src/lib/study-queries.ts (getStudyCards)
    - src/components/card-list.tsx (does CardList/CardRow read createdAt? confirm genuinely unread before dropping)
    - .planning/phases/26-performance-batch/... 26-02 batchCalls===1 assertion style (the count-assertion proof convention to mirror)
  </read_first>
  <action>
    Create `src/lib/__tests__/dashboard-data.test.ts`. Assert the consolidation contract via count/shape assertions (never timing, D-09):
    - The dashboard data pass issues ONE card-fetching query for a deck, not two (mock getDeckCards/getStudyCards or the underlying db call; assert the study subset is derived from the single result).
    - The study subset derived in JS equals the set getStudyCards would have returned (fixture-based equality) — proving no behavior change.
    - Native language resolves from a session field, not a getUserNativeLanguage call (assert getUserNativeLanguage is NOT invoked in the consolidated path).
    If dashboard/page.tsx's shaping is inline, extract a pure helper (e.g. `deriveStudySubset(cards)` in study-queries.ts) and test that plus the query-count. This is RED until Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/dashboard-data.test.ts 2>&1 | grep -qE "pass|PASS|fail|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts a single card query (call-count) and JS-derived study-subset equality with a fixture
    - Test asserts getUserNativeLanguage is not called in the consolidated path
    - No timing/wall-clock assertions
  </acceptance_criteria>
  <done>Failing query-layer consolidation test exists with count + derivation assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate the dashboard data pass</name>
  <read_first>
    - src/app/(protected)/dashboard/page.tsx (full data-fetch region ~199-290)
    - src/lib/deck-queries.ts, src/lib/study-queries.ts (getDeckCards/getStudyCards/getUserNativeLanguage implementations)
    - src/lib/auth-session.ts (27-01 getSession)
    - src/components/card-list.tsx (confirm createdAt is unread by the client consumer)
  </read_first>
  <action>
    Replace the `getDeckCards` + `getStudyCards` double fetch (lines ~235-238) with ONE card query (keep it in study-queries.ts / deck-queries.ts as the query layer), then derive the study subset in JS from that single result — no O(n²) `find` stitch. If the two existing functions return different column sets, make the single query return the superset needed and slice/derive the study subset via a pure helper.
    Drop `getUserNativeLanguage` from the dashboard: read `const nativeLanguage = session.user.nativeLanguage ?? "en"` (the 25-04 `?? "en"` normalization; do NOT leave it typed as string|null|undefined). Remove the now-unused `getUserNativeLanguage` import from dashboard/page.tsx only (leave the exported function in deck-queries.ts — new-card/other pages may still use it).
    Drop `createdAt` from the row payload serialized to the client ONLY after confirming (Task 1 read) that CardList/CardRow never reads it — if it IS read, keep it and note the deviation in the summary.
    Swap `auth.api.getSession` (line 199) to `getSession()` from `@/lib/auth-session`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/dashboard-data.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Task-1 test passes (GREEN): one card query, JS-derived study subset
    - `grep -n "getStudyCards\|getUserNativeLanguage" src/app/(protected)/dashboard/page.tsx` shows neither is imported/called (or documents why createdAt/a call was retained)
    - dashboard/page.tsx reads `session.user.nativeLanguage ?? "en"` and uses `getSession()` from `@/lib/auth-session`
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Dashboard issues one card query, derives study subset in JS, uses the session native-language field, drops createdAt (if confirmed unread); test green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC query layer → DB | Consolidated queries must retain the same ownership WHERE guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Elevation of Privilege | consolidated card query | mitigate | Preserve the existing `WHERE userId/deckId = ...` ownership guards when collapsing two queries into one; verify at review the same access-control predicate remains (ASVS V4, RESEARCH Security Domain) |
| T-27-04-02 | Information Disclosure | createdAt in payload | mitigate | Dropping the unread field reduces the serialized surface; no widening |
| T-27-04-SC | Tampering | npm installs | n/a | No package installs this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/dashboard-data.test.ts` passes; `npx tsc --noEmit` clean
- After the wave: full `npm test` green; `npm run qa:run` (dashboard/session path — items 8/12, 17 D-10) all journeys pass
- Manual/informal (prod build only): dashboard render shows one card query in logs, not two
</verification>

<success_criteria>
- PERF-16 shipped: single card query + JS-derived study subset, session-field native language, createdAt dropped (or retention documented), ownership guards intact
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-04-SUMMARY.md` when done
</output>
