---
phase: 27-performance-batch-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
autonomous: false
requirements: [PERF-18]
must_haves:
  truths:
    - "cards, decks, recall_events, session each declare a secondary index in schema.ts"
    - "The four indexes exist on the hosted Neon DB after db:push"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "index() declarations on 4 tables"
      contains: "cards_deckId_idx"
  key_links:
    - from: "src/db/schema.ts"
      to: "Neon DB"
      via: "npm run db:push"
      pattern: "index\\("
---

<objective>
Ship item 14 (PERF-18): add the four missing secondary indexes — `cards(deckId)`, `decks(userId)`, `recall_events(cardId)`, `session(userId)` — to the Drizzle schema and push them to the hosted Neon DB. Cheap insurance before the tables grow (card-delete cascades currently scan the append-only recall_events table).

Purpose: The schema has zero non-PK indexes today. This is the one item that becomes a query cliff rather than a constant cost.
Output: Four `index()` declarations + a gated `db:push` against Neon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-RESEARCH.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add four index() declarations to schema.ts</name>
  <read_first>
    - src/db/schema.ts (the `cards`, `decks`, `recall_events`, `session` pgTable definitions — currently the plain 2-arg `pgTable(name, columns)` form; and `milestones_seen` lines 127-143 which shows the existing 3-arg array-callback `unique(...)` convention to mirror)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Code Examples: verified `index()` syntax for drizzle-orm 0.45.1)
    - node_modules/drizzle-orm/pg-core/indexes.d.ts (installed 0.45.1 index() builder API)
  </read_first>
  <action>
    Add `index` to the existing `drizzle-orm/pg-core` import. Convert `cards`, `decks`, `recall_events`, `session` from the 2-arg `pgTable(name, columns)` form to the 3-arg form with an array-callback third argument, exactly matching `milestones_seen`'s existing shape:
    - `cards`: `(table) => [ index("cards_deckId_idx").on(table.deckId) ]`
    - `decks`: `(table) => [ index("decks_userId_idx").on(table.userId) ]`
    - `recall_events`: `(table) => [ index("recall_events_cardId_idx").on(table.cardId) ]`
    - `session`: `(table) => [ index("session_userId_idx").on(table.userId) ]`
    Leave all existing columns byte-unchanged. `session` is better-auth's table but is declared as a plain `pgTable` in THIS project's schema (the drizzle-adapter reads the app's own schema) — adding an index here is safe with no better-auth-side config. Column names: verify the actual FK column identifiers in each table before writing (deckId/userId/cardId are the expected names per the review; confirm against the file).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c '_idx")' src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -o '[a-z_]*_idx' src/db/schema.ts | sort -u` lists exactly: cards_deckId_idx, decks_userId_idx, recall_events_cardId_idx, session_userId_idx
    - `index` is imported from `drizzle-orm/pg-core`
    - `npx tsc --noEmit` is clean (index() builder types satisfied)
    - No column definitions changed (diff shows only the added third-arg callbacks + import)
  </acceptance_criteria>
  <done>Four indexes declared in schema.ts matching the milestones_seen 3-arg convention; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push indexes to Neon with Josh's authorization (D-08)</name>
  <action>PAUSE for human authorization (D-08). Do NOT run any hosted-DB write until Josh explicitly authorizes it. Then export DATABASE_URL via grep from .env.local (never source it) and run npm run db:push, confirming the four indexes are created against the correct Neon instance. See how-to-verify for exact steps.</action>
  <files>src/db/schema.ts (no source edit here — this is the hosted db:push of Task 1's indexes)</files>
  <read_first>
    - src/db/schema.ts (the four new indexes from Task 1)
    - drizzle.config.ts (confirms the bare `process.env.DATABASE_URL` read — drizzle-kit does NOT auto-load .env.local)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Pitfall 5: DATABASE_URL export requirement)
  </read_first>
  <what-built>Task 1 declared four secondary indexes in schema.ts. This step applies them to the hosted Neon production DB via `npm run db:push` — a hosted-DB write that requires Josh's explicit authorization (D-08). Additive index creation is instant at current row counts.</what-built>
  <how-to-verify>
    1. Confirm Josh has authorized this hosted-DB write (D-08 gate). Do NOT proceed without it.
    2. Extract DATABASE_URL WITHOUT sourcing .env.local (it contains a runnable curl block that EXECUTES on source): `export DATABASE_URL=$(grep -m1 '^DATABASE_URL=' .env.local | cut -d= -f2- | tr -d '"')`
    3. Run `npm run db:push` in that same shell. On Windows/Git-Bash, do not let MSYS rewrite any path-like values.
    4. Confirm the output reports the four new indexes being created (NOT "no changes detected" — that signals a wrong/empty DB connection, Pitfall 5).
    5. Optional confirmation: query `information_schema` (or Drizzle Studio) for `cards_deckId_idx`, `decks_userId_idx`, `recall_events_cardId_idx`, `session_userId_idx`.
  </how-to-verify>
  <acceptance_criteria>
    - `npm run db:push` output shows the four indexes created against the correct Neon instance
    - No "no changes detected" false-positive (would mean wrong DB target)
  </acceptance_criteria>
  <verify><human-check>Josh confirms db:push output shows the four indexes created on Neon; no "no changes detected".</human-check></verify>
  <done>The four indexes exist on the hosted Neon DB (db:push output or information_schema confirms); no data/column change; push authorized by Josh.</done>
  <resume-signal>Type "pushed" once db:push reports the four indexes created, or describe any error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell → hosted Neon DB | `db:push` mutates production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | Neon schema push | mitigate | D-08 human-authorization gate before any hosted write; additive index-only (no column/data change), instant at current row counts |
| T-27-02-02 | Information Disclosure | .env.local DATABASE_URL | mitigate | Extract via grep, never `source` (.env.local contains a runnable curl+API-key block that executes on source) |
| T-27-02-03 | Denial of Service | wrong-DB push | mitigate | Verify db:push does NOT report "no changes detected" (Pitfall 5 — signals wrong/empty DB target) |
| T-27-02-SC | Tampering | npm installs | n/a | No package installs this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (types come from config, not the live DB — this passes even before the push, hence the BLOCKING push task is mandatory to avoid a false-positive verified state)
- Post-push: the four indexes exist on Neon (db:push output or information_schema query)
</verification>

<success_criteria>
- PERF-18 shipped: four secondary indexes declared and live on Neon
- No data or column change; push gated on Josh's authorization
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-02-SUMMARY.md` when done
</output>
