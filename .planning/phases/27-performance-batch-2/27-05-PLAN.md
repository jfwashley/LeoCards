---
phase: 27-performance-batch-2
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/card-list.tsx
  - src/components/card-list.test.tsx
autonomous: true
requirements: [PERF-13, PERF-20]
must_haves:
  truths:
    - "The pause/resume icon flips before the POST resolves (optimistic), and rolls back on error"
    - "Rapid repeated toggles coalesce trailing router.refresh() calls"
    - "A search keystroke does not re-render every card row"
    - "Card rows mount only after the accordion open tween completes"
  artifacts:
    - path: "src/components/card-list.tsx"
      provides: "optimistic pause + React.memo CardRow + useDeferredValue search"
      contains: "React.memo"
    - path: "src/components/card-list.test.tsx"
      provides: "optimistic-flip, rollback, and render-count assertions"
      contains: "memo"
  key_links:
    - from: "src/components/card-list.tsx (CardList)"
      to: "extracted CardRow"
      via: "React.memo row receiving primitives + stable callbacks"
      pattern: "React.memo"
---

<objective>
Ship items 9 + 16 (PERF-13, PERF-20), both in `card-list.tsx`: (9) make the pause/resume toggle optimistic — flip the icon immediately, roll back on error, coalesce trailing refreshes; (16) extract a `React.memo` row and defer the search filter with `useDeferredValue` so keystrokes don't re-render every row, and defer row mounting until the accordion tween completes.

Purpose: Tap feedback goes from a server round trip to instant; search stops re-rendering up to ~300 rows per keystroke.
Output: Optimistic toggle + memoized rows + deferred search in card-list.tsx, with behavior/render-count tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-RESEARCH.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optimistic pause/resume toggle (item 9)</name>
  <read_first>
    - src/components/card-list.tsx (togglePause lines ~230-255; the Pitfall-2 comment lines 172-177 and 239 explaining WHY router.refresh() exists; pendingCardIds Set lines ~181-184)
    - src/components/word-list-browser.tsx (BrowseList optimistic-Set state machine lines 565-649 — the handleAdd optimistic-flip/rollback/auto-clearing-error pattern to copy)
    - src/components/card-list.test.tsx (existing test structure to extend)
  </read_first>
  <behavior>
    - Test 1: Clicking pause flips the icon to the paused state synchronously (before the mocked POST resolves).
    - Test 2: When the POST rejects, the icon rolls back to its prior state and a transient error is surfaced (mirroring word-list-browser's auto-clearing error map).
    - Test 3: router.refresh() is still called on success (the existing revalidation correctness guarantee is preserved).
  </behavior>
  <action>
    Rework `togglePause` to flip the card's paused state OPTIMISTICALLY (immediately, before the fetch) using the optimistic-Set pattern from `word-list-browser.tsx`'s `BrowseList` (lines 565-649): set the optimistic state, issue the POST inside the existing `startTransition`, and on failure roll the optimistic flip back (mirroring the `catch` at card-list.tsx:245-246 but adding a VISIBLE rollback + transient error, not only `console.error`). Keep calling `router.refresh()` on SUCCESS — the Pitfall-2 comment documents that `revalidatePath` alone does not update the open tab, so this correctness guarantee must remain. Coalesce trailing refreshes: extend the existing `pendingCardIds` Set / `startTransition` to de-dupe rapid successive toggles so N quick taps do not fire N separate refreshes (Claude's-discretion rollback/coalescing mechanics per CONTEXT — copy the existing optimistic-Set shape). Do NOT touch any card-swipe drag physics (17 D-05 carve-out) — this is the deck-management list, not the study swipe card; keep the boundary clean.
  </action>
  <verify>
    <automated>npx vitest run src/components/card-list.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test proves the icon flips before the POST resolves (optimistic)
    - Test proves rollback-on-error restores prior state
    - `grep -n "router.refresh" src/components/card-list.tsx` still present in the success path
    - No change to card-swipe drag code
  </acceptance_criteria>
  <done>Pause toggle is optimistic with rollback + coalesced refresh; behavior test-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: React.memo CardRow + useDeferredValue search + deferred row mount (item 16)</name>
  <read_first>
    - src/components/card-list.tsx (inlined row markup in the .map() at lines ~481-591; the existing `React.memo` wrap of CardList at line 146; query state line ~153; filtered useMemo lines ~257-266; accordion height:0→auto tween lines ~292-303)
    - src/components/word-list-browser.tsx (BWWordRow React.memo extraction lines 128-146, 295 — the exact "row receives primitives + stable callbacks, not the whole array" shape to copy)
    - src/components/card-list.test.tsx (to extend with a render-count assertion)
  </read_first>
  <behavior>
    - Test 1: Typing in the search box does not re-render rows whose props are unchanged (render-count/memo assertion via a render spy on the extracted row).
    - Test 2: The filtered list responds to the DEFERRED query value (useDeferredValue) — filtering still produces correct results.
  </behavior>
  <action>
    Extract the per-card row markup (currently inlined in the `.map()` at ~481-591) into its own `React.memo`-wrapped component (`CardRow` or similar), mirroring `BWWordRow`'s extraction exactly: the row receives primitive props + STABLE callbacks (wrap the per-row handlers in `useCallback` so memo actually holds), never the whole array. Wrap the existing `query` state in `useDeferredValue` and use the deferred value inside the `filtered` `useMemo` dependency array (React's documented API; no existing in-repo analog needed for this single hook). Defer mounting rows until the `height:0→auto` accordion tween (lines ~292-303) completes — reuse card-list's existing `panelMounted`/animationend accordion pattern (the same technique 25-02 used in change-password-card.tsx) so the mounted subtree is not relayed out every animation frame. Keep the outer `CardList` `React.memo` wrap intact.
  </action>
  <verify>
    <automated>npx vitest run src/components/card-list.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "React.memo" src/components/card-list.tsx` >= 2 (outer CardList + extracted CardRow)
    - `grep -n "useDeferredValue" src/components/card-list.tsx` present, applied to the search query
    - Render-count test proves unchanged rows do not re-render on a keystroke
    - `npx tsc --noEmit` clean (no biome noNonNullAssertion `!` — guard undefined explicitly per project gotchas)
  </acceptance_criteria>
  <done>Rows are memo-extracted, search deferred, mount gated on the accordion tween; render-count test-proven; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client optimistic state → server | Optimistic UI must not diverge permanently from server truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering | optimistic pause state | mitigate | Rollback-on-error restores server-truth state; router.refresh() on success re-syncs from the server (no permanent client/server divergence) |
| T-27-05-SC | Tampering | npm installs | n/a | No package installs this phase (React.memo/useDeferredValue are React built-ins) |
</threat_model>

<verification>
- `npx vitest run src/components/card-list.test.tsx` passes; `npx tsc --noEmit` clean
- After the wave: full `npm test` green; `e2e/12-pause-cards.spec.ts` passes against a restarted prod-parity server (grep for LOCAL helpers)
- Manual/informal (prod build only): pause icon flips instantly; search stays responsive on a large deck
</verification>

<success_criteria>
- PERF-13 shipped: optimistic pause with rollback + coalesced refresh
- PERF-20 shipped: memoized rows + deferred search + tween-gated mount; drag physics untouched
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-05-SUMMARY.md` when done
</output>
