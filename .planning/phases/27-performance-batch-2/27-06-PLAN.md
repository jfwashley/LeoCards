---
phase: 27-performance-batch-2
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/translation-form.tsx
  - src/app/api/translate/route.ts
  - src/lib/translation-cache.ts
  - src/components/translation-form.test.tsx
  - src/app/api/translate/__tests__/route.test.ts
autonomous: true
requirements: [PERF-19, PERF-23]
must_haves:
  truths:
    - "A slow earlier translation response never overwrites a newer one for the same direction (AbortController)"
    - "AbortError is a silent no-op, never a user-visible 'Translation unavailable' error"
    - "The existing activeField field-switch guard still works (composed, not replaced)"
    - "A repeated identical translation request hits the in-memory LRU, not DeepL, on the 2nd+ call"
  artifacts:
    - path: "src/lib/translation-cache.ts"
      provides: "bounded TTL LRU factory keyed sourceLang:targetLang:text"
      contains: "createTranslationCache"
    - path: "src/components/translation-form.tsx"
      provides: "AbortController per debounced fire"
      contains: "AbortController"
    - path: "src/app/api/translate/__tests__/route.test.ts"
      provides: "DeepL call-count assertion on repeated identical request"
      contains: "translateText"
  key_links:
    - from: "src/app/api/translate/route.ts"
      to: "src/lib/translation-cache.ts"
      via: "cache check before the DeepL try/catch, set after"
      pattern: "createTranslationCache|translationCache"
    - from: "src/components/translation-form.tsx"
      to: "fetch(/api/translate)"
      via: "signal: controller.signal"
      pattern: "controller.signal"
---

<objective>
Ship items 15 + 19 (PERF-19, PERF-23), both on the translate path. (15) Fix the translation-form stale-response race: it debounces but never aborts, so typing "cha"→"chat" lets a slow first response overwrite the newer translation — an `AbortController` per debounced fire fixes both the wrong-result race and wasted DeepL quota (a correctness bug). (19) Add an in-memory LRU cache in front of DeepL (same single-instance assumptions as the existing rate limiter) plus client-side dedupe within a fan-out, slotting into the shipped PERF-09 `texts[]` handler.

Purpose: Correctness fix + protect the 500k-char/month DeepL free tier from re-translating common vocabulary for every user.
Output: AbortController race fix, LRU cache factory + wiring, client fan-out dedupe; new translation-form test (Wave 0 gap) + extended route test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-RESEARCH.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AbortController stale-response race fix (item 15)</name>
  <read_first>
    - src/components/translation-form.tsx (translateFrom lines ~238-296; the existing `activeField.current === direction` field-switch guard; the debounce via use-debounce; the TranslationResponseSchema parse at lines ~10-12/260; error handling that surfaces "Translation unavailable")
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Pitfall 4 — the AbortController must COMPOSE with, not replace, the existing activeField guard; the two are different races)
  </read_first>
  <behavior>
    - Test 1: Fire two translations for the SAME direction with reordered response timing (slow first, fast second); only the later-fired (fast) result lands in state.
    - Test 2: An aborted request (AbortError) produces NO user-visible "Translation unavailable" error — it is a silent no-op.
    - Test 3: The existing field-switch guard still discards a response whose direction no longer matches activeField.
  </behavior>
  <action>
    Store one `AbortController` ref (single ref is sufficient — only one field is realistically typed into at a time). At the START of each new `translateFrom` invocation, call `controller.abort()` on the previous controller, create a fresh one, and pass `{ signal: controller.signal }` to the `fetch`. Treat a caught `AbortError` (`err.name === "AbortError"`) as a silent no-op — do NOT set the "Translation unavailable" error state for it (Pitfall 4). Keep the existing `activeField.current === direction` guard intact — it protects a DIFFERENT race (field switch) and must remain (compose, do not replace). Keep the `use-debounce` hook as-is (item 15 does not replace debouncing).
  </action>
  <verify>
    <automated>npx vitest run src/components/translation-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - New `src/components/translation-form.test.tsx` exists and proves the slow-first/fast-second same-direction case lands only the newer result
    - `grep -c "AbortController\|controller.signal\|AbortError" src/components/translation-form.tsx` >= 3
    - The `activeField` guard still present in translateFrom
    - AbortError path sets no error state
  </acceptance_criteria>
  <done>AbortController composes with the existing guard; stale-response race test-proven; AbortError is silent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translation LRU cache + client fan-out dedupe (item 19)</name>
  <read_first>
    - src/app/api/translate/route.ts (texts[] array branch lines ~96-112 and singular text branch lines ~114-126, both calling client.translateText inside try/catch → 502; auth+rate-limit gate lines 40-58)
    - src/lib/rate-limit.ts (full file — the Map-based, module-scope singleton, periodic-cleanup convention to mirror for the LRU factory)
    - src/app/api/translate/__tests__/route.test.ts (existing test to extend, added in 26-01)
    - the client fan-out `runTranslationFanOut` (from 26-01; grep `runTranslationFanOut` to locate it — client-side dedupe within a fan-out lands here)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Code Examples: hand-rolled LRU shape; Pattern/Don't-Hand-Roll: do NOT add lru-cache dependency)
  </read_first>
  <behavior>
    - Test 1: Two identical translate requests (same text/sourceLang/targetLang) call the mocked DeepL `translateText` exactly ONCE — the 2nd is served from cache.
    - Test 2: A request with a different key still calls DeepL (cache miss).
    - Test 3: The 502-on-DeepL-failure error-response shape is unchanged for a cache miss.
  </behavior>
  <action>
    Create `src/lib/translation-cache.ts` exporting `createTranslationCache({ maxSize, ttlMs })` returning `{ get, set }`, mirroring `rate-limit.ts`'s Map-based, module-scope, single-instance convention (insertion-order Map = cheap LRU via delete+re-set on hit). Key = `${sourceLang}:${targetLang}:${text}`. Pick a bounded `maxSize` and `ttlMs` (Claude's discretion, CONTEXT — size it comparably to the rate limiter's assumptions; e.g. a few thousand entries, ~1h TTL). Instantiate a module-scope singleton in route.ts.
    In BOTH the `texts[]` and singular `text` branches: check the cache BEFORE the `client.translateText` try/catch (short-circuit a hit with zero DeepL call), and `set` after a successful translation. Preserve the exact catch→502 response shape on miss+failure. The cache key is built from already-`RequestSchema`-validated inputs (bounded length, enum langs) — no new unvalidated surface (Security V5).
    Add client-side dedupe within the fan-out (`runTranslationFanOut`): collapse duplicate words in a single batch so the same text is not requested twice concurrently. Extend `src/app/api/translate/__tests__/route.test.ts` with the DeepL call-count assertion (mock the deepl client; 2nd identical request → translateText called once). Count assertion, never timing (D-09).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/translate/__tests__/route.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/translation-cache.ts` exports `createTranslationCache` with get/set keyed `sourceLang:targetLang:text`
    - route.ts checks the cache before the DeepL call in both the texts[] and singular branches
    - Route test proves a repeated identical request calls DeepL `translateText` exactly once
    - `grep -rc "lru-cache" package.json` shows NO new lru-cache dependency added
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>LRU cache short-circuits repeated translations; client fan-out dedupes; DeepL call-count test-proven; no new deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/translate | Request body validated by RequestSchema; feeds the LRU key |
| LRU cache → cross-user reads | Cache is shared across users on a single instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Tampering | translation LRU cache poisoning | mitigate | Cache key built only from already-RequestSchema-validated text/sourceLang/targetLang (bounded length, enum langs); no attacker-controllable unvalidated key surface (ASVS V5) |
| T-27-06-02 | Information Disclosure | cross-user cache reads | accept | Translation output is deterministic public dictionary content (source text → target text), identical for all users; no per-user data in the value — sharing is safe |
| T-27-06-03 | Denial of Service | DeepL quota amplification | mitigate | Existing 30/min per-user rate limiter (shared deployment assumptions) bounds request volume regardless of cache hit rate |
| T-27-06-SC | Tampering | npm installs | n/a | No package installs — LRU hand-rolled per rate-limit.ts convention (no lru-cache dep) |
</threat_model>

<verification>
- `npx vitest run src/components/translation-form.test.tsx src/app/api/translate/__tests__/route.test.ts` passes; `npx tsc --noEmit` clean
- After the wave: full `npm test` green; `e2e/04-manual-card-entry.spec.ts` (translation form) passes against a restarted prod-parity server
- Manual/informal: rapid typing never surfaces a stale translation; repeated words don't re-hit DeepL
</verification>

<success_criteria>
- PERF-19 shipped: AbortController fixes the stale-response race; AbortError silent; existing guard preserved
- PERF-23 shipped: in-memory LRU + client fan-out dedupe cut repeated DeepL calls; no new dependency
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-06-SUMMARY.md` when done
</output>
