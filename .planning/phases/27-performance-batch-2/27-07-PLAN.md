---
phase: 27-performance-batch-2
plan: 07
type: execute
wave: 2
depends_on: [27-06]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/app/(auth)/reset-password/page.tsx
  - src/components/welcome/welcome-step-choose.tsx
  - src/components/review-list.tsx
  - src/components/translation-form.tsx
  - src/components/account-details-card.tsx
  - src/components/change-password-card.tsx
autonomous: true
requirements: [PERF-14]
must_haves:
  truths:
    - "All 9 client-side zod importers import from zod/mini, not full zod"
    - "Validation semantics and error messages are unchanged"
    - "zodResolver call sites are byte-unchanged (compatible with zod/mini)"
  artifacts:
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "zod/mini schema (canonical form-page conversion)"
      contains: "zod/mini"
  key_links:
    - from: "the 9 client importers"
      to: "zod/mini"
      via: "import * as z from \"zod/mini\""
      pattern: "from \"zod/mini\""
---

<objective>
Ship item 10 (PERF-14): swap the 9 client-side zod importers from full zod v4 to the tree-shakeable `zod/mini` subpath. These hydrate full zod for trivial validation on the first paint of `/signup`, `/login`, etc.; `zod/mini` is the same package's tree-shakeable build (~44KB+ savings, directional). Server-only zod importers are explicitly OUT of scope (zero client-bundle benefit).

Purpose: Trim first-load JS and parse cost on every auth/onboarding entry point.
Output: All 9 client importers on zod/mini; zodResolver unchanged; zero stragglers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-RESEARCH.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md

<interfaces>
Depends on 27-06: `translation-form.tsx` receives the AbortController fix there FIRST, then its zod→zod/mini conversion here (shared file — ordering matters; 27-06 is Wave 1, this is Wave 2).
zod/mini API delta (RESEARCH Pattern 4, verified against installed zod 4.3.6):
- `import { z } from "zod"` → `import * as z from "zod/mini"`
- `z.string().min(1, "msg")` → `z.string().check(z.minLength(1, "msg"))`
- `z.string().email("msg")` → `z.email("msg")` (top-level schema in v4/mini)
- `z.object({...}).refine(...)` cross-field checks: zod/mini supports `.refine()` on object schemas the same way — verify change-password-card's confirmNewPassword===newPassword check
`zodResolver` needs ZERO changes — its `"_zod" in schema` detection dispatches to zod/v4/core generically (verified node_modules/@hookform/resolvers/zod/dist/zod.js).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the 4 auth pages + welcome-step-choose to zod/mini</name>
  <read_first>
    - src/app/(auth)/signup/page.tsx (canonical client-zod convention lines 1-34 — the reference shape every form page mirrors)
    - src/app/(auth)/login/page.tsx, forgot-password/page.tsx, reset-password/page.tsx (mirror signup)
    - src/components/welcome/welcome-step-choose.tsx
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Pattern 4, Pitfall 3)
  </read_first>
  <action>
    In each of signup, login, forgot-password, reset-password pages and welcome-step-choose.tsx: change `import { z } from "zod"` to `import * as z from "zod/mini"`, and convert method-chaining validators to zod/mini's functional `.check()` composition per the interfaces block (`.min(n,msg)`→`.check(z.minLength(n,msg))`, `.email(msg)`→top-level `z.email(msg)`, etc.). Keep every `zodResolver(schema)` call and `z.infer<typeof schema>` type byte-unchanged (zod/mini shares the `_zod` core). Preserve all error-message strings exactly (users see them). Do NOT change any server-only importer.
  </action>
  <verify>
    <automated>npx vitest run src/app/\(auth\) src/components/welcome && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rL 'from "zod/mini"' src/app/(auth)/login/page.tsx src/app/(auth)/signup/page.tsx src/app/(auth)/forgot-password/page.tsx src/app/(auth)/reset-password/page.tsx src/components/welcome/welcome-step-choose.tsx` returns nothing (all 5 import zod/mini)
    - `grep -rn 'from "zod"' <the 5 files>` returns zero (no full-zod straggler)
    - Existing form-validation tests for these files pass; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>5 form components on zod/mini with identical validation + messages; tsc + existing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert review-list, translation-form, account-details-card, change-password-card + straggler sweep</name>
  <read_first>
    - src/components/review-list.tsx (client zod importer)
    - src/components/translation-form.tsx (post-27-06; uses a bare `z.object` RESPONSE-schema parse, NOT zodResolver — simpler conversion, lines ~10-12)
    - src/components/account-details-card.tsx (zodResolver + z.object, lines 3,7,19-22,80-83)
    - src/components/change-password-card.tsx (zodResolver; verify the confirmNewPassword===newPassword cross-field check converts correctly — Pitfall 3)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Pattern 4, Pitfall 3)
  </read_first>
  <action>
    Convert review-list.tsx, translation-form.tsx, account-details-card.tsx, change-password-card.tsx to `import * as z from "zod/mini"` with `.check()` composition per the interfaces block. For translation-form.tsx: `z.object({ translation: z.string().min(1) })` → `z.object({ translation: z.string().check(z.minLength(1)) })` (no zodResolver dependency here — pure response-schema parse). For change-password-card.tsx: re-verify the cross-field `.refine()` (confirmNewPassword===newPassword) works on the zod/mini object schema (Pitfall 3 — mixing `"zod"` and `"zod/mini"` imports in one schema produces a type mismatch, not a runtime crash). Keep zodResolver calls unchanged. After all 9 files are converted, grep every target for `from "zod"` (not `"zod/mini"`) to confirm ZERO stragglers (Pitfall 3 — a single missed import silently keeps full zod and inflates the bundle).
  </action>
  <verify>
    <automated>npx vitest run src/components/review-list.test.tsx src/components/translation-form.test.tsx src/components/account-details-card.test.tsx src/components/change-password-card.test.tsx 2>/dev/null; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 9 target files (the 5 from Task 1 + these 4) import `zod/mini`; `grep -rln 'from "zod"' <all 9>` returns nothing (zero stragglers, exact `"zod"` not `"zod/mini"`)
    - change-password-card's cross-field confirm check still validates (test green)
    - translation-form still parses the translate response (its 27-06 AbortController logic untouched)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>All 9 client importers on zod/mini; zero full-zod stragglers; cross-field checks intact; tsc + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form input → zod validation | Validation strictness must be identical after the swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07-01 | Tampering | client-side input validation | mitigate | zod/mini shares the same `_zod` core and validation semantics as full zod — item 10 is a bundle-size change only, NOT a strictness change (ASVS V5); server-side validation (unchanged) remains the authoritative gate |
| T-27-07-SC | Tampering | npm installs | n/a | No package installs — zod/mini is a subpath of the already-installed zod |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; existing form-validation tests for all 9 files pass
- After the wave: full `npm test` green; relevant e2e (auth signup/login) passes against a restarted prod-parity server
- Manual/informal (prod build only, D-10): `npm run build` first-load JS for /signup, /login shrinks (directional ~44KB per RESEARCH A2 — verify via build chunk diff, do not trust the source diff alone, Pitfall 3)
</verification>

<success_criteria>
- PERF-14 shipped: all 9 client zod importers on zod/mini; identical validation + messages; zodResolver unchanged; zero stragglers
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-07-SUMMARY.md` when done
</output>
