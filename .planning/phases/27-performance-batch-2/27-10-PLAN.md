---
phase: 27-performance-batch-2
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/extract/route.ts
  - src/app/api/extract/__tests__/extract-eval.test.ts
autonomous: false
requirements: [PERF-17]
must_haves:
  truths:
    - "Image extraction runs on claude-haiku-4-5 and passes the existing eval expectations"
    - "A manual real-photo side-by-side confirms extraction quality did not measurably drop, else revert to claude-sonnet-4-6 (D-05)"
    - "Streaming ships in this phase ONLY if the measured median exceeds ~4s (D-06)"
  artifacts:
    - path: "src/app/api/extract/route.ts"
      provides: "Haiku model id (+ conditional streaming)"
      contains: "claude-haiku-4-5"
  key_links:
    - from: "src/app/api/extract/route.ts"
      to: "@ai-sdk/anthropic"
      via: "anthropic(\"claude-haiku-4-5\")"
      pattern: "claude-haiku-4-5"
---

<objective>
Ship item 13 (PERF-17): speed up the extraction spinner. Trial `claude-haiku-4-5` (typically 2-3x faster, ~1/3 cost) for word-listing OCR behind the existing eval expectations plus a manual real-photo side-by-side (D-05). If the measured median end-to-end extraction is still >~4s, stream `partialOutputStream` so review rows populate progressively — threshold-gated, lands in this SAME phase without another ask (D-06). Revert to `claude-sonnet-4-6` if extraction quality measurably drops.

Purpose: The current model blocks until the full ~512-token response; a faster model (or streaming) cuts perceived latency.
Output: Haiku model swap (verified) + a gated quality/latency checkpoint + conditional streaming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-RESEARCH.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the extraction model to claude-haiku-4-5</name>
  <read_first>
    - src/app/api/extract/route.ts (model instantiation line 164 `anthropic("claude-sonnet-4-6")`; the comment at 159 dating the choice to 2026-05-19; error handling lines 208-230 — AbortError→504, NoObjectGeneratedError→empty-words 200 never logging image bytes, generic→502, finally clearTimeout; generateText/Output.object lines 175-200)
    - src/app/api/extract/__tests__/extract-eval.test.ts (existing eval expectations — the schema/shape conformance gate)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Assumption A1 — model id is [ASSUMED] from WebSearch, confirm at execution; Pitfall 6 — evals check shape not per-word accuracy)
  </read_first>
  <action>
    Change line 164 from `anthropic("claude-sonnet-4-6")` to `anthropic("claude-haiku-4-5")` (drop-in string swap for `@ai-sdk/anthropic@3.0.78`; no API-shape change). Update the dating comment at line 159 to note the 2026-07-22 Haiku trial + the D-05 revert condition. Preserve ALL existing error handling (AbortError→504, NoObjectGeneratedError→empty-words 200, generic→502, finally clearTimeout) byte-unchanged — the model swap alone touches none of it. Run the existing eval test suite (necessary-but-not-sufficient shape/schema gate per Pitfall 6). If `ANTHROPIC_API_KEY` is unset locally, the route 503s gracefully and the eval runs against the mocked model path — that still validates the code change.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/extract/__tests__/extract-eval.test.ts && grep -c "claude-haiku-4-5" src/app/api/extract/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claude-haiku-4-5" src/app/api/extract/route.ts` == 1 and `grep -c "claude-sonnet-4-6"` == 0 (unless retained as a documented revert reference)
    - Existing extract-eval tests pass (schema/shape conformance)
    - All error-handling branches (504/200/502/clearTimeout) unchanged
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Model swapped to Haiku; existing eval suite green; error handling intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual quality side-by-side + latency median measurement (D-05/D-06)</name>
  <action>PAUSE for human verification (D-05/D-06). Do the real-photo side-by-side (Haiku vs sonnet) and record the median extraction wall-time on a prod-like build, then record the decision (done / stream / revert). Do not self-approve — this gates whether streaming ships this phase and whether the model reverts. See how-to-verify.</action>
  <files>src/app/api/extract/route.ts (no code edit here — measurement + decision only; Task 3 acts on the decision)</files>
  <read_first>
    - .planning/phases/27-performance-batch-2/27-CONTEXT.md (D-05 quality gate, D-06 4s streaming threshold)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Pitfall 6, Environment Availability — ANTHROPIC_API_KEY may be unset)
  </read_first>
  <what-built>Task 1 swapped extraction to claude-haiku-4-5 and passed the existing (shape-only) eval suite. The Phase 10 offline vision eval reference-set is incomplete carried debt, so it does NOT gate extraction quality — a manual real-photo side-by-side is required before considering item 13 done (D-05), and the median wall-time decides whether streaming ships (D-06).</what-built>
  <how-to-verify>
    1. On a prod-like build (D-10), run extraction on a few REAL photos with both models (Haiku now vs claude-sonnet-4-6 reference). Compare per-word extraction accuracy side-by-side.
    2. Record the MEDIAN end-to-end extraction wall-time on a typical photo (informal stopwatch, prod-like build — D-06/D-10).
    3. Decide:
       - If Haiku quality measurably dropped → REVERT to claude-sonnet-4-6 (Task 3 handles the revert); item 13 = model unchanged, streaming re-evaluated on sonnet timing.
       - If quality is fine AND median > ~4s → streaming SHIPS this phase (Task 3 implements it).
       - If quality is fine AND median <= ~4s → done; streaming skipped (backlog only if the spinner feels long later).
  </how-to-verify>
  <acceptance_criteria>
    - A recorded quality verdict (parity / dropped) from a real-photo side-by-side
    - A recorded median extraction wall-time and the resulting decision (revert / streaming / done)
  </acceptance_criteria>
  <verify><human-check>A recorded quality verdict + median wall-time + decision (done/stream/revert) from a prod-like build.</human-check></verify>
  <done>Quality verdict, median seconds, and the resulting decision (done/stream/revert) are recorded for Task 3 to act on.</done>
  <resume-signal>Report: (a) quality verdict, (b) median seconds, (c) decision — one of "done", "stream", or "revert".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Conditional — implement streaming OR revert, per Task 2's decision</name>
  <read_first>
    - src/app/api/extract/route.ts (generateText/Output.object at 175-200 — becomes streamObject if streaming; the single Response.json return)
    - src/components/review-list.tsx (the extraction client flow that would consume progressive rows — NOTE: also touched by 27-07 zod/mini in Wave 2; if streaming lands here, it runs Wave 1 BEFORE 27-07, so 27-07 rebases onto this change)
    - src/app/api/translate/route.ts (auth/rate-limit/error skeleton for the streaming-branch response shape)
    - node_modules/ai/dist/index.d.ts (streamObject + partialOutputStream — the NON-experimental name; verify partial-object emission cadence against a real Haiku call before locking client consumption — RESEARCH Open Question 2)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Standard Stack streamObject/partialOutputStream; Alternatives; Open Question 2 — do not pre-build speculatively)
  </read_first>
  <action>
    Branch on Task 2's decision (recorded in the checkpoint resume):
    - Decision "done" → NO code change; mark this task complete-as-no-op in the summary (Haiku shipped, under the 4s bar).
    - Decision "revert" → change line 164 back to `anthropic("claude-sonnet-4-6")`, restore the comment, re-run the eval suite; item 13 ships as "trialed Haiku, reverted for quality" and streaming is re-evaluated on sonnet timing (implement streaming only if sonnet median also >~4s).
    - Decision "stream" → convert `generateText({ output: Output.object({...}) })` to `streamObject({...})` + consume `partialOutputStream` (the non-experimental property in `ai@6.0.185`), shape the route's HTTP response as a stream, and wire `review-list.tsx` to populate review rows progressively. Preserve the existing error semantics (504/200/502). This is real UI/wiring work (RESEARCH Open Question 2) — verify the partial-object emission cadence against a real Haiku call first. Extend the eval/route tests to cover the streaming response shape with a count/shape assertion (never timing, D-09).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/extract && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The route reflects Task 2's decision: Haiku retained (done), sonnet restored (revert), or streamObject/partialOutputStream wired (stream)
    - If streaming: `grep -c "partialOutputStream" src/app/api/extract/route.ts` >= 1 and review-list consumes progressive rows; if done/revert: no streaming code present
    - Existing + any new extract tests pass; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Extraction path finalized per the measured decision (Haiku kept, reverted, or streaming shipped); tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded image → extraction model | Image bytes must never be logged; model swap is spend/quality-sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10-01 | Information Disclosure | image bytes in error logs | mitigate | Preserve the existing NoObjectGeneratedError→empty-words-200 branch that never logs image bytes (unchanged by the model swap) |
| T-27-10-02 | Denial of Service | Anthropic spend/quality regression | mitigate | D-05 quality gate (eval shape + manual real-photo side-by-side) + explicit revert path before item 13 is "done"; model id re-verified at execution (Assumption A1) |
| T-27-10-03 | Tampering | streaming partial-JSON parsing | mitigate | Use the AI SDK's `partialOutputStream` (handles partial-JSON/backpressure) — do not hand-roll an SSE/chunked-JSON parser (RESEARCH Don't-Hand-Roll) |
| T-27-10-SC | Tampering | npm installs | n/a | No package installs — `ai`/`@ai-sdk/anthropic` already installed |
</threat_model>

<verification>
- `npx vitest run src/app/api/extract` passes; `npx tsc --noEmit` clean
- Checkpoint recorded: quality verdict + median + decision
- After the wave: full `npm test` green; if streaming landed, e2e image-extraction flow passes against a restarted prod-parity server
- Manual UAT (D-05): real-photo side-by-side confirms quality parity (carried into HUMAN-UAT if the reference-set gap persists)
</verification>

<success_criteria>
- PERF-17 shipped: Haiku trialed behind the eval + manual gate; streaming shipped iff median >~4s; revert path honored if quality dropped (D-05/D-06)
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-10-SUMMARY.md` when done
</output>
