---
phase: 27-performance-batch-2
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/daybreak/h-prog-card.tsx
  - src/components/daybreak/h-back.tsx
  - src/components/daybreak/h-mood-chip.tsx
  - src/components/habitat-scene.tsx
autonomous: true
requirements: [PERF-22]
must_haves:
  truths:
    - "No backdrop-filter blur remains over the playing habitat video in any of the 4 target files"
    - "account-back.tsx (static page, no per-frame cost) is NOT touched"
    - "The overlay chrome still renders (the 92%-opaque backgrounds were already near-solid)"
  artifacts:
    - path: "src/components/daybreak/h-prog-card.tsx"
      provides: "backdropFilter removed"
      contains: "h-prog-card"
  key_links:
    - from: "the 4 habitat overlay components"
      to: "GPU per-frame cost"
      via: "removed backdrop-filter over the looping video"
      pattern: "backdropFilter"
---

<objective>
Ship item 18 (PERF-22), broadened per D-02: drop the `backdrop-filter: blur()` from ALL four over-video habitat overlays — `h-prog-card.tsx:45` (6px, the originally-flagged one), `h-back.tsx:19`, `h-mood-chip.tsx:27`, and `habitat-scene.tsx:308` (all 4px). Each forces the GPU to re-blur its backdrop every video frame for as long as the clip loops; the backgrounds are already ~92%-opaque white, so the blur is nearly invisible. Trivially revertible (D-01).

Purpose: Remove a per-frame GPU cost that runs continuously over the looping habitat video.
Output: Four blur properties removed + CSS-absence assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove backdrop-filter from the 4 over-video overlays</name>
  <read_first>
    - src/components/daybreak/h-prog-card.tsx (line ~45, `backdropFilter: "blur(6px)"`)
    - src/components/daybreak/h-back.tsx (line ~19, `backdropFilter: "blur(4px)"`)
    - src/components/daybreak/h-mood-chip.tsx (line ~27, `backdropFilter: "blur(4px)"`)
    - src/components/habitat-scene.tsx (line ~308, `backdropFilter: "blur(4px)"`)
    - src/components/daybreak/account-back.tsx (DO NOT edit — static page, no per-frame cost; confirm it is excluded)
  </read_first>
  <action>
    Delete the `backdropFilter` (and any paired `WebkitBackdropFilter`) inline-style property from each of the four files. Leave the existing background color / opacity and every other style untouched — the ~92%-opaque white backgrounds already provide the panel contrast, so removing the blur is a near-invisible pixel change. Do NOT touch `account-back.tsx` (D-02 explicitly excludes it). If a file uses a Tailwind `backdrop-blur-*` class instead of an inline style, remove that class instead.
  </action>
  <verify>
    <automated>grep -rn "backdropFilter\|WebkitBackdropFilter\|backdrop-blur" src/components/daybreak/h-prog-card.tsx src/components/daybreak/h-back.tsx src/components/daybreak/h-mood-chip.tsx src/components/habitat-scene.tsx | grep -vc '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "backdropFilter\|backdrop-blur" src/components/daybreak/h-prog-card.tsx src/components/daybreak/h-back.tsx src/components/daybreak/h-mood-chip.tsx src/components/habitat-scene.tsx` returns 0 for each
    - `grep -c "backdropFilter\|backdrop-blur" src/components/daybreak/account-back.tsx` is UNCHANGED (still present — not touched)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>All four over-video blurs removed; account-back.tsx untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Assert blur absence in component tests</name>
  <read_first>
    - src/components/daybreak/account-back.test.tsx (existing daybreak component test structure to mirror)
    - the 4 edited files from Task 1
  </read_first>
  <action>
    Add a lightweight CSS-absence assertion covering the four edited components — either extend an existing daybreak component test or add a focused test that renders each overlay and asserts no `backdrop-filter`/`backdropFilter` style is applied (a source/DOM-style assertion, never a timing gate, D-09). If a rendered-DOM assertion is awkward for these presentational components, a source-level grep-style test (read the file, assert the string is absent) is acceptable as the regression guard. The pixel-level visual confirmation over the playing video is a manual UAT / e2e-visual step, not automatable here.
  </action>
  <verify>
    <automated>npx vitest run src/components/daybreak 2>&1 | grep -qE "pass|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - A committed test asserts the absence of backdrop-filter in the 4 target components
    - `npx vitest run src/components/daybreak` passes
  </acceptance_criteria>
  <done>A regression test guards against blur re-introduction on the 4 overlays.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure client-side CSS property removal; no data, auth, or input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08-01 | (none) | CSS-only change | accept | Removing a decorative blur introduces no new trust boundary or attack surface; trivially revertible (D-01) |
| T-27-08-SC | Tampering | npm installs | n/a | No package installs this phase |
</threat_model>

<verification>
- `grep` confirms zero backdrop-filter in the 4 files, unchanged in account-back.tsx; `npx tsc --noEmit` clean
- After the wave: `e2e/07-habitat-display.spec.ts` + `e2e/13-habitat-states.spec.ts` pass against a restarted prod-parity server
- Manual UAT (D-01, prod build): eyeball the habitat overlays over the playing video — chrome still reads clearly; revert if any panel looks wrong
</verification>

<success_criteria>
- PERF-22 shipped: all four over-video backdrop blurs removed; account-back.tsx untouched; regression-guarded
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-08-SUMMARY.md` when done
</output>
