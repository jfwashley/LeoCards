---
phase: 27-performance-batch-2
plan: 03
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/app/(protected)/deck/browse/page.tsx
  - src/app/(protected)/deck/browse/__tests__/browse-page.test.ts
autonomous: true
requirements: [PERF-15]
must_haves:
  truths:
    - "The topic-detail (BrowseList) branch serializes only the requested topic's word subset, not all ~280 words"
    - "categoryCounts is not computed on the topic-detail branch"
    - "The topic-picker (BrowseTiles) branch still shows correct per-category counts"
  artifacts:
    - path: "src/app/(protected)/deck/browse/page.tsx"
      provides: "server-side topic filtering before RSC serialization"
      contains: "requestedTopic"
    - path: "src/app/(protected)/deck/browse/__tests__/browse-page.test.ts"
      provides: "filtered-subset payload assertion"
      contains: "filter"
  key_links:
    - from: "src/app/(protected)/deck/browse/page.tsx"
      to: "BrowseList"
      via: "filtered word subset prop"
      pattern: "BrowseList"
---

<objective>
Ship item 11 (PERF-15): filter the Browse catalogue server-side. The topic-detail view currently serializes all ~280 words (~35 KB) into the RSC payload then filters client-side to the ~20 rendered; instead pass only the requested topic's subset and skip the `categoryCounts` computation on that branch (it is only consumed by the topic-picker view).

Purpose: Cut the RSC payload for every topic-detail navigation from ~280 words to ~20.
Output: browse/page.tsx serves a filtered subset; new data-shaping test (Wave 0 gap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-performance-batch-2/27-CONTEXT.md
@.planning/phases/27-performance-batch-2/27-RESEARCH.md
@.planning/phases/27-performance-batch-2/27-PATTERNS.md

<interfaces>
Depends on 27-01's `src/lib/auth-session.ts`:
- `getSession(): Promise<Session | null>` — swap browse/page.tsx's `auth.api.getSession` call to this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write browse-page data-shaping test (Wave 0)</name>
  <read_first>
    - src/app/(protected)/deck/browse/page.tsx (lines 24-94: getSession, `?topic=` validation at 34-37, categoryCounts at 63-69, BrowseList/BrowseTiles prop threading at ~79/87-94)
    - src/lib/wordlist.ts (RESEARCH Open Question 1: read getWordList/filterWords to determine whether it is a cheap in-memory lookup — dictates whether the fix is pure prop-slicing or a data-fetch change)
    - src/components/word-list-browser.tsx (BrowseList/BrowseTiles prop contracts)
  </read_first>
  <action>
    Create `src/app/(protected)/deck/browse/__tests__/browse-page.test.ts`. Because the page is an RSC with a getSession dependency, extract or test the pure data-shaping logic: assert that for a given `requestedTopic`, the word set passed toward `BrowseList` has length equal to that topic's filtered count (~20), NOT the full wordlist length (~280); and assert `categoryCounts` is only built for the topic-picker branch. If the shaping logic is inline in the RSC and hard to unit-test directly, extract a small pure helper (e.g. `shapeBrowseData(wordList, requestedTopic)` in the same file or a colocated module) and test that — a length/array-shape assertion, never a timing gate (D-09). This is a RED test until Task 2 lands the filtering.
  </action>
  <verify>
    <automated>npx vitest run src/app/\(protected\)/deck/browse/__tests__/browse-page.test.ts 2>&1 | grep -qE "fail|FAIL|pass|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and asserts the filtered subset length differs from the full wordlist length for a topic-detail request
    - Test asserts categoryCounts is absent/unused on the topic-detail branch
    - Uses a source/length assertion, not a timing or byte-size wall-clock gate
  </acceptance_criteria>
  <done>A failing (RED) data-shaping test exists covering the filtered-subset expectation.</done>
</task>

<task type="auto">
  <name>Task 2: Filter server-side + swap to cached getSession</name>
  <read_first>
    - src/app/(protected)/deck/browse/page.tsx (full — the target of the in-place edit)
    - src/lib/wordlist.ts (getWordList/filterWords internals confirmed in Task 1)
    - src/lib/auth-session.ts (27-01 — the getSession accessor to import)
    - .planning/phases/27-performance-batch-2/27-RESEARCH.md (Pattern 5)
  </read_first>
  <action>
    When `requestedTopic` is set (topic-detail branch): pass only `filterWords(wordList.words, { category: requestedTopic })` to `BrowseList` instead of the full `wordList.words`, and skip computing `categoryCounts` entirely (it is only consumed by `BrowseTiles`, the topic-picker branch). Keep the existing `?topic=` validation against `CATEGORIES` (lines 34-37, the WR-01 fix) byte-unchanged. Keep the CEFR filter client-side (do not move it server-side — item 11 scope is topic filtering only). On the topic-picker branch, `categoryCounts` and the full wordlist behavior stay as-is so tile counts remain correct.
    Swap `const session = await auth.api.getSession({ headers: await headers() })` (line 24) to `const session = await getSession()` and `import { getSession } from "@/lib/auth-session"`; remove now-dead `auth`/`headers` imports only if unused elsewhere in the file. If this file's `getUserNativeLanguage(session.user.id)` call (line 41) can read from `session.user.nativeLanguage ?? "en"` instead, apply that swap too (item 12 win, opportunistic since the file is already open) — otherwise leave it.
  </action>
  <verify>
    <automated>npx vitest run src/app/\(protected\)/deck/browse/__tests__/browse-page.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The Task-1 test now passes (GREEN): topic-detail branch serializes the filtered subset
    - `grep -n "categoryCounts" src/app/(protected)/deck/browse/page.tsx` shows it is not computed on the requestedTopic branch
    - browse/page.tsx imports `getSession` from `@/lib/auth-session`; no `auth.api.getSession` remains in the file
    - `?topic=` CATEGORIES validation unchanged; CEFR filter still client-side
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Topic-detail Browse serves only the requested subset; getSession swapped; test green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RSC `?topic=` param | Already validated against CATEGORIES (WR-01); unchanged this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | `?topic=` URL param | accept | Existing WR-01 allow-list validation against CATEGORIES is preserved byte-unchanged; no new input surface |
| T-27-03-02 | Information Disclosure | filtered word subset | accept | The word catalogue is non-sensitive static content; filtering reduces exposure, does not widen it |
| T-27-03-SC | Tampering | npm installs | n/a | No package installs this phase |
</threat_model>

<verification>
- `npx vitest run` browse test passes; `npx tsc --noEmit` clean
- After the wave: full `npm test` green; `e2e/03-word-list-browser.spec.ts` passes against a restarted prod-parity server (grep this spec for LOCAL helper copies, not just files_modified)
- Manual/informal (prod build only): topic-detail RSC payload is materially smaller than the topic-picker payload
</verification>

<success_criteria>
- PERF-15 shipped: topic-detail Browse serializes only the requested topic's subset; categoryCounts skipped on that branch; tile counts still correct
</success_criteria>

<output>
Create `.planning/phases/27-performance-batch-2/27-03-SUMMARY.md` when done
</output>
