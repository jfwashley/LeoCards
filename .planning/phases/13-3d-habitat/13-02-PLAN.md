---
phase: 13-3d-habitat
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/habitat-3d/clay-world.ts
  - src/lib/habitat-3d/clay-animation.ts
  - src/lib/habitat-3d/clay-ambient.ts
  - src/lib/habitat-3d/clay-characters.ts
  - src/lib/habitat-3d/clay-level.ts
  - src/lib/habitat-3d/__tests__/clay-level.test.ts
  - src/lib/habitat-3d/__tests__/clay-world.test.ts
autonomous: true
requirements:
  - R1   # all 9 levels (via clay-level + clay-world)
  - R2   # ESM port (drops CDN UMD + babel/standalone)
  - R3   # scene composition reads only HabitatState-derived inputs
complexity: L
must_haves:
  truths:
    - "featuresForLevel(N) for N in 1..9 returns the same feature object as the designer code"
    - "buildClayWorld() composes a THREE.Group with sub-groups named for each feature (lake, trees, flowers, butterflies, mushrooms, cave, toys)"
    - "Only the Storybook lion + Elephant character builders are ported (chibi, mascot, bunny, giraffe dropped per RESEARCH A.5 trimming)"
    - "No reference to React, no reference to window/document inside src/lib/habitat-3d/clay-*.ts (pure scene-graph code)"
  artifacts:
    - path: "src/lib/habitat-3d/clay-level.ts"
      provides: "featuresForLevel + LEVEL_CONFIG"
      exports: ["featuresForLevel", "LEVEL_CONFIG", "DEFAULT_FEATURES", "FeatureFlags"]
    - path: "src/lib/habitat-3d/clay-world.ts"
      provides: "buildClayWorld()"
      exports: ["buildClayWorld"]
    - path: "src/lib/habitat-3d/clay-characters.ts"
      provides: "buildLionStorybook + buildElephant"
      exports: ["buildLionStorybook", "buildElephant"]
    - path: "src/lib/habitat-3d/clay-animation.ts"
      provides: "applyLionWalk/Sleep/Extras + animateElephant"
      exports: ["applyLionWalk", "applyLionSleep", "applyLionExtras", "animateElephant", "updateWorld"]
    - path: "src/lib/habitat-3d/clay-ambient.ts"
      provides: "buildStorybookAmbient, sleepBubbles, drinkingFX"
  key_links:
    - from: "src/lib/habitat-3d/clay-world.ts"
      to: "src/lib/habitat-3d/clay-level.ts"
      via: "imports featuresForLevel + LEVEL_CONFIG"
      pattern: "featuresForLevel"
    - from: "src/lib/habitat-3d/clay-world.ts"
      to: "src/lib/habitat-3d/palette.ts"
      via: "imports CLAY + toonGrad"
      pattern: "CLAY"
---

<objective>
Port the meat of `habitat-clay-styles.jsx` — world builder, ambient effects,
the Storybook lion + Elephant character rigs, and the feature-flag-additive
level table — into pure TypeScript modules under `src/lib/habitat-3d/`. No
React, no DOM access; this is the scene-graph foundation Plan 03's React
wrapper will mount.

Drop the three alternative-style components (`ClayChibi`, `ClayStorybookV2`,
`ClayMascot`) and their unused character builders (`buildLionChibi`,
`buildBunny`, `buildLionMascot`, `buildGiraffe`) per RESEARCH section A.5
"Recommendation: port only `buildLionStorybook` and `buildElephant`" —
saves ~250 LOC and keeps the surface tight for Phase 13's actual scope.

Purpose: gives Plan 03 a typed, testable scene-graph API to mount.

Output:
- `clay-world.ts` (port of `habitat-clay-styles.jsx:50-606`)
- `clay-animation.ts` (port of `:608-720`, `:780-971`, `:973-1036`)
- `clay-ambient.ts` (port of `:720-781`, `:1038-1078`, `:1079-1368`)
- `clay-characters.ts` (port of ONLY `buildLionStorybook` `:1832-2026` and `buildElephant` `:2027-2114`)
- `clay-level.ts` (port of `:44-48` + `:2229-2254`)
- Vitest unit tests proving `featuresForLevel(1..9)` matches the SPEC R1 acceptance unlock table
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-3d-habitat/13-SPEC.md
@.planning/phases/13-3d-habitat/13-CONTEXT.md
@.planning/phases/13-3d-habitat/13-RESEARCH.md
@.planning/design/habitat-art-assets.md
@.planning/design/animations/habitat-clay-styles.jsx
@.planning/phases/13-3d-habitat/13-01-SUMMARY.md
@src/lib/habitat-3d/scene-host.ts
@src/lib/habitat-3d/palette.ts
@src/lib/habitat-3d/types.ts
@src/lib/habitat-engine.ts
@AGENTS.md

<interfaces>
<!-- From Plan 01 (types.ts) — already exported. Plan 02 consumes these. -->

```typescript
import type { SceneContext, WorldOpts } from "./types";
import { CLAY, toonGrad } from "./palette";

// Plan 02 NEW exports — Plan 03 will consume.

export interface FeatureFlags {
  lake?: boolean; lilies?: boolean; path?: boolean;
  trees?: boolean; rocks?: boolean;
  flowers?: boolean; grass?: boolean; butterflies?: boolean;
  mushrooms?: boolean; cave?: boolean; toy?: boolean;
  songbirds?: boolean;  // L9
}

export interface LevelConfig {
  showElephant: boolean;  // L5+
  sleepCycle:   boolean;  // L7+
  sky:          "default" | "golden-hour";  // L9 → golden-hour
}

export function featuresForLevel(level: number): FeatureFlags;
export const LEVEL_CONFIG: Record<number, LevelConfig>;

export interface ClayWorld {
  root:           THREE.Group;
  featureGroups:  Record<string, THREE.Object3D>;  // Plan 04 uses for decay opacity
  fog:            THREE.Fog | null;
  // ... see clay-world.ts for full shape
}

export function buildClayWorld(
  ctx: SceneContext,
  features: FeatureFlags,
  opts: WorldOpts,
): ClayWorld;
```
</interfaces>

<key-source-references>
Per RESEARCH section A.5, lines to port:

| Module                  | Source lines in habitat-clay-styles.jsx       |
|-------------------------|-----------------------------------------------|
| clay-level.ts            | :44-48 (DEFAULT_FEATURES), :2229-2254 (featuresForLevel + LEVEL_CONFIG) |
| clay-world.ts           | :50-606 (buildClayWorld + ground/lake/path/trees/rocks/flowers/butterflies/mushrooms/cave/toys/lion path) |
| clay-animation.ts       | :608-720 (sleep cycle), :780-971 (walk + storybook extras), :973-1036 (elephant anim), :569-603 (updateWorld) |
| clay-ambient.ts         | :720-781 (sleep bubbles), :1038-1078 (drinking FX), :1079-1368 (pollen/petals/birds) |
| clay-characters.ts      | :1832-2026 (buildLionStorybook), :2027-2114 (buildElephant) |
| DROPPED (do NOT port)   | :1370-1511 (chibi lion), :1512-1559 (bunny), :1560-1733 (mascot lion), :1734-1831 (giraffe), :2159-2228 (ClayChibi/V2/Mascot wrappers), :2399-2420 (LEVEL_TITLES, HUD_STREAK), :2422-2425 (window.* exports) |
</key-source-references>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port clay-level.ts with full level-1..9 acceptance table</name>
  <files>src/lib/habitat-3d/clay-level.ts, src/lib/habitat-3d/__tests__/clay-level.test.ts</files>
  <behavior>
    For each level 1..9, `featuresForLevel(level)` must return the
    feature-flag set per `.planning/design/habitat-art-assets.md` table:

    - Test L1: `{}` (or all flags false) — "just Leo on the mound"
    - Test L2: `{ lake: true, lilies: true, path: true }`
    - Test L3: L2 ∪ `{ trees: true, rocks: true }` (additive)
    - Test L4: L3 ∪ `{ flowers: true, grass: true, butterflies: true }`
    - Test L5: L4 (visual change is the Elephant via `LEVEL_CONFIG.showElephant`)
    - Test L6: L4 ∪ `{ mushrooms: true }`
    - Test L7: L6 ∪ `{ cave: true }` + `LEVEL_CONFIG[7].sleepCycle === true`
    - Test L8: L7 ∪ `{ toy: true }`
    - Test L9: L8 ∪ `{ songbirds: true }` + `LEVEL_CONFIG[9].sky === "golden-hour"`

    Additionally:
    - Test "additive": `featuresForLevel(N+1)` is a superset of `featuresForLevel(N)` for N in 1..8 (verifies SPEC R1 "single base + feature-flag additive" architecture per D-27).
    - Test "boundary": `featuresForLevel(0)` and `featuresForLevel(10)` clamp to L1 and L9 respectively (defensive; engine clamps via `Math.min(9, level)` already but lib should not crash on out-of-range).
  </behavior>
  <action>
    Port `featuresForLevel` and `LEVEL_CONFIG` from
    `habitat-clay-styles.jsx:2229-2254`. Use the exact same flag names
    (`lake`, `lilies`, `path`, ...) so Plan 03 / 04 don't need to map.
    Add `songbirds` flag for L9 (the designer used `worldOpts.sky` for
    the L9 visual change but also has bird-related ambient — verify
    against the source code; if the designer only used sky variance,
    drop `songbirds` and gate via `LEVEL_CONFIG[9].sky === "golden-hour"` only).

    DO NOT add any features the designer didn't ship — this plan is a
    port, not a redesign. If the designer code lacks a feature the
    art-assets doc claims, raise the conflict in the plan SUMMARY rather
    than inventing one.

    Tests use the table above; the unit tests ARE the SPEC R1 acceptance
    (matches SPEC "Acceptance" R3 line: "TypeScript compile + a focused
    unit test that mounts the new component with HabitatState fixtures at
    each level (1-9) and asserts the right feature flags are set on the
    scene root.").
  </action>
  <verify>
    <automated>npm run test -- src/lib/habitat-3d/__tests__/clay-level.test.ts</automated>
  </verify>
  <done>
    All 11 tests (9 level cases + additive + boundary) pass; SPEC R1
    binding logic locked at the feature-flag layer.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port clay-characters.ts — Storybook lion + elephant ONLY</name>
  <files>src/lib/habitat-3d/clay-characters.ts, src/lib/habitat-3d/__tests__/clay-characters.test.ts</files>
  <behavior>
    - Test 1 (buildLionStorybook returns a rig): returned object has `root`, `headG`, `tailRoot`, `legs[]`, `eyes`, `materials[]` properties — the channels Plan 04 binds mood to.
    - Test 2 (buildElephant returns a rig): returned object has `root`, `trunkSegments[]`, `materials[]`.
    - Test 3 (deterministic geometry): two calls with the same seed produce identical `root.position` and `vertexCount`s.
    - Test 4 (no excluded builders exported): the module does NOT export `buildLionChibi`, `buildBunny`, `buildLionMascot`, or `buildGiraffe` (these were dropped per RESEARCH A.5).
  </behavior>
  <action>
    Port ONLY `buildLionStorybook` (`habitat-clay-styles.jsx:1832-2026`)
    and `buildElephant` (`:2027-2114`). DO NOT port `buildLionChibi`,
    `buildBunny`, `buildLionMascot`, `buildGiraffe` — these belong to the
    dropped alt-style components per RESEARCH A.5 and would only become
    dead code under Phase 13's scope.

    Export the character "rig" shapes Plan 04 needs (`root`, `headG`,
    `tailRoot`, `legs[]`, `materials[]`). Type these in `types.ts`
    extending the Plan 01 contracts.

    Use `_mulberry32(0xC0FFEE)` deterministic RNG already in palette.ts
    for any geometry seed. No `Math.random()` for shape generation
    (RESEARCH section A.3 "No `Math.random()` issues" note — random is OK
    for blink intervals, NOT for geometry).
  </action>
  <verify>
    <automated>npm run test -- src/lib/habitat-3d/__tests__/clay-characters.test.ts && npm run typecheck</automated>
  </verify>
  <done>
    Two character rigs export with stable interfaces; alt-style builders
    confirmed absent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port clay-world.ts + clay-ambient.ts + clay-animation.ts</name>
  <files>src/lib/habitat-3d/clay-world.ts, src/lib/habitat-3d/clay-ambient.ts, src/lib/habitat-3d/clay-animation.ts, src/lib/habitat-3d/__tests__/clay-world.test.ts</files>
  <behavior>
    - Test 1 (buildClayWorld with L1 features): returns `world.root` containing the ground + Leo placeholder; `world.featureGroups.lake === undefined` (or its `.visible === false`).
    - Test 2 (buildClayWorld with L4 features): `world.featureGroups.flowers`, `world.featureGroups.butterflies`, `world.featureGroups.trees` all present (Plan 04 needs these handles for decay opacity).
    - Test 3 (golden-hour sky variant): `buildClayWorld(ctx, ..., { sky: "golden-hour" })` produces a different sky `ShaderMaterial.uniforms.color1.value` than the default — verifies L9 path.
    - Test 4 (updateWorld respects reducedMotion gate): calling `updateWorld(ctx, world, { reducedMotion: true, dt: 0.016, t: 1.0 })` does NOT mutate `butterflies` rotation or `clouds` x-position; without the flag, both change.
    - Test 5 (no React imports in any clay-*.ts): grep `import .* from "react"` in `src/lib/habitat-3d/clay-*.ts` returns no matches.
  </behavior>
  <action>
    Port the world builder, ambient effects, and animation updaters per
    the source-reference table in `<context>`. The tests above lock
    R6 (reducedMotion) at the `updateWorld` layer — Plan 03's React
    wrapper will only need to flip the flag.

    Use a `WebGLRenderer` mock (`new THREE.WebGLRenderer()` with no
    canvas) under Vitest — three.js works headless without a real GL
    context for scene-graph mutation; rendering tests don't apply at
    this layer.

    Cleanup contract: each builder returns a `dispose()` function that
    traverses its sub-tree and calls `geometry.dispose()` +
    `material.dispose()` (matches `habitats-shared.jsx:198-208` pattern
    that Plan 01 already documented). Plan 03's React wrapper composes
    these into the unmount cleanup chain.

    DO NOT port `LEVEL_TITLES` or `HUD_STREAK` constants
    (`habitat-clay-styles.jsx:2399-2420`) — RESEARCH Open Q 3:
    "DON'T port — let the designer's HUD die with the JSX wrapper. v1.0's
    React HUD is already correct and bigger-picture-integrated."

    Hard-code `companionMode = "meadow"` for the level-5 elephant
    placement and `variant = level === 9 ? "golden-hour" : "default"` per
    RESEARCH Open Q 4. Document the choice in clay-world.ts as a
    top-of-file comment.

    **Sanity grep before lock-in (catches designer-source drift):**
    ```bash
    grep -nE "companionMode|variant\s*=" .planning/design/animations/habitat-clay-styles.jsx
    ```
    Expected: only level-5 elephant placement consumes `companionMode`, and
    only level-9's sky variant uses `variant`. If the grep surfaces OTHER
    levels consuming these knobs, STOP — the planner's hard-coding above
    would silently drop those variants. Escalate to Josh + update Plan 02
    Task 3 before proceeding.

    The drinking FX (`:1038-1078`) and sleep bubbles (`:720-781`) are
    triggered by `state` flags Plan 03's wrapper will set — the
    ambient module exposes them as switchable groups.
  </action>
  <verify>
    <automated>npm run test -- src/lib/habitat-3d/__tests__/clay-world.test.ts && npm run typecheck && npm run lint -- src/lib/habitat-3d/</automated>
  </verify>
  <done>
    Five tests green; clay-world.ts, clay-ambient.ts, clay-animation.ts
    all compile and lint clean; no React or DOM imports inside
    `src/lib/habitat-3d/clay-*.ts`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Designer JSX → ported TS | Behavioral drift during port (mistranslation) |
| ported TS → THREE scene graph | None — sandboxed inside the renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | designer JSX port | mitigate | Each module's tests pin the public shape (rig channels, feature groups, sky variant) so a wrong port shows up as a failing test, not a silent visual regression. Specifically, the level-1..9 unit table (Task 1) IS the SPEC R1 acceptance check. |
| T-13-06 | Denial of Service | unbounded particle pool in pollen/petals/birds (`clay-ambient.ts`) | mitigate | Designer code already caps pool size (verified in `habitat-clay-styles.jsx:1079-1368`); port retains the cap. Mobile-quality scaling (Q=0.55) compounds the cap. |
| T-13-07 | Information Disclosure | clay-* modules | accept | No PII; no network; pure scene-graph code. No surface. |
| T-13-08 | Tampering | three npm dep (re-asserted in this plan) | inherited from Plan 01 | Lockfile + pinned version; no new deps introduced in Plan 02. |

</threat_model>

<verification>
- `npm run test -- src/lib/habitat-3d/__tests__/` — all green
- `npm run typecheck` — clean
- `npm run lint -- src/lib/habitat-3d/` — clean
- `grep -rn "from \"react\"" src/lib/habitat-3d/` returns no matches
  (locks the pure-TS contract for the module)
- `grep -rn "window\\.\\|document\\." src/lib/habitat-3d/clay-` returns
  no matches (clay-* modules must be DOM-free; scene-host owns
  `performance.now` and event listeners)
- `grep -rn "buildLionChibi\\|buildBunny\\|buildLionMascot\\|buildGiraffe" src/lib/habitat-3d/`
  returns no matches (dropped builders confirmed absent)
</verification>

<success_criteria>
- [ ] `featuresForLevel(N)` for N=1..9 matches the unlock table in
      `.planning/design/habitat-art-assets.md` (locked by Task 1 unit tests)
- [ ] `featuresForLevel(N+1) ⊇ featuresForLevel(N)` for N=1..8 (additive)
- [ ] `LEVEL_CONFIG[5].showElephant === true`, `LEVEL_CONFIG[7].sleepCycle
      === true`, `LEVEL_CONFIG[9].sky === "golden-hour"`
- [ ] `buildClayWorld()` returns a `ClayWorld` with named `featureGroups`
      that Plan 04 can target for decay
- [ ] `updateWorld()` accepts and respects a `reducedMotion` flag (Plan 03
      passes it in; Plan 04 validates end-to-end)
- [ ] Only `buildLionStorybook` + `buildElephant` exported from
      `clay-characters.ts`
- [ ] `npm run typecheck` + `npm run lint` clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-3d-habitat/13-02-SUMMARY.md`
including:
- Total LOC ported (target ~1500 LOC per RESEARCH A.5 totals)
- LOC dropped from the alt-style components (target ~600+ LOC)
- The exact public exports from each new module (so Plan 03 can wire them)
- Any places where the port deviated from the designer code (with justification)
</output>
