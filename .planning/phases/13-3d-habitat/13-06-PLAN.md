---
phase: 13-3d-habitat
plan: 06
type: execute
wave: 5
depends_on: ["13-04", "13-05"]
files_modified:
  - .planning/phases/13-3d-habitat/13-WIDGET-PERF.md
  - .planning/phases/13-3d-habitat/13-PERF.md
  - package.json
  - package-lock.json
  - src/components/habitat-canvas.tsx               # DELETE
  - src/components/habitat-layers.tsx               # DELETE
  - src/components/habitat-widget-canvas.tsx       # DELETE
  - src/components/tiger-sprite.tsx                 # DELETE (orphan after R10)
  - src/components/sparkle-particles.tsx            # DELETE (orphan after R10)
  - src/components/bird-sprite.tsx                  # DELETE (orphan after R10)
  - public/sprites/habitat.png                      # DELETE
  - public/sprites/habitat.json                     # DELETE
  - public/sprites/tiger.png                        # DELETE
  - public/sprites/tiger.json                       # DELETE
  - e2e/scripts/render-hero-images.spec.ts          # conditional — only if D-28 falls back
  - public/habitat/widget-l{1..9}.webp              # conditional — only if D-28 falls back
autonomous: false   # contains a checkpoint:decision (D-28 live-vs-cached) and a checkpoint:human-verify (final visual)
requirements:
  - R8   # mini-widget D-28 perf gate + decision + implementation
  - R9   # CWV "Good" on dashboard + /habitat (desktop + mobile emulation)
  - R10  # all PixiJS code removed (6 files, not the 3 named in SPEC)
  - R2   # final acceptance — Three.js as code-split chunk + no babel/standalone
complexity: L
must_haves:
  truths:
    - "Lighthouse on /dashboard and /habitat passes CWV Good (LCP ≤ 2.5s, INP ≤ 200ms, CLS ≤ 0.1) on desktop AND mobile emulation"
    - "D-28 live-vs-cached decision is recorded with measured numbers in 13-WIDGET-PERF.md"
    - "If live: widget ships as Plan 05 delivered. If cached: 9 hero images generated + widget swapped to <img>"
    - "All 6 PixiJS files deleted (the 3 named in SPEC + the 3 orphans the researcher flagged)"
    - "grep -rn \"pixi\" src/ package.json returns no matches"
    - "grep -r \"babel/standalone\" src/ public/ returns no matches"
    - "Three.js code-split confirmed via next build output"
    - "npm uninstall pixi.js @pixi/react completed"
  artifacts:
    - path: ".planning/phases/13-3d-habitat/13-WIDGET-PERF.md"
      provides: "D-28 measurement report + live-vs-cached decision"
    - path: ".planning/phases/13-3d-habitat/13-PERF.md"
      provides: "Lighthouse reports for dashboard + /habitat, all 3 CWV metrics × 2 routes × 2 profiles"
  key_links:
    - from: ".planning/phases/13-3d-habitat/13-PERF.md"
      to: "Lighthouse JSON output"
      via: "committed inline or as attached artifact"
      pattern: "LCP|INP|CLS"
---

<objective>
Close out Phase 13 by:

1. Measuring D-28 (mini-widget live 3D perf) — Lighthouse on dashboard
   (desktop + mobile emulation) plus real-device measurement of cold-load
   TTI + sustained FPS during widget auto-orbit. Write findings to
   `13-WIDGET-PERF.md` and lock the decision.

2. If D-28 says "cached" — generate 9 hero images via Playwright
   build-time render (RESEARCH Example 4) and swap
   `habitat-widget.tsx`'s dynamic import to a static `<img>`-based
   variant (`habitat-3d-widget-image.tsx`).

3. Measuring CWV on `/habitat` (SPEC R9) on both desktop and mobile
   emulation; commit Lighthouse output to `13-PERF.md`.

4. Removing ALL v1.0 PixiJS code — 6 files (3 named in SPEC R10 + 3
   orphans the researcher identified — `tiger-sprite.tsx`,
   `sparkle-particles.tsx`, `bird-sprite.tsx` per RESEARCH section F.1):

   ```
   DELETE: habitat-canvas.tsx, habitat-layers.tsx, habitat-widget-canvas.tsx,
           tiger-sprite.tsx, sparkle-particles.tsx, bird-sprite.tsx
   DELETE: public/sprites/habitat.{png,json}, public/sprites/tiger.{png,json}
   UNINSTALL: pixi.js, @pixi/react
   ```

5. Verifying the final acceptance greps:
   - `grep -rn "pixi" src/ package.json` → 0
   - `grep -r "babel/standalone\|unpkg.com" src/ public/` → 0
   - `npm ls three` → 0.160.x
   - `next build` output table shows Three.js in a separate chunk

Purpose: final gate; everything is green or Phase 13 doesn't ship.

Output:
- `13-WIDGET-PERF.md` with measured numbers + decision
- `13-PERF.md` with Lighthouse reports
- Either: widget ships live (no further code) OR widget swapped to <img>
  + 9 hero images committed
- 6 PixiJS files + 4 sprite assets deleted
- `package.json` shows no pixi deps; `package-lock.json` updated
- Phase 13 SUMMARY ready for ship
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-3d-habitat/13-SPEC.md
@.planning/phases/13-3d-habitat/13-RESEARCH.md
@.planning/phases/13-3d-habitat/13-01-SUMMARY.md
@.planning/phases/13-3d-habitat/13-02-SUMMARY.md
@.planning/phases/13-3d-habitat/13-03-SUMMARY.md
@.planning/phases/13-3d-habitat/13-04-SUMMARY.md
@.planning/phases/13-3d-habitat/13-05-SUMMARY.md
@src/components/habitat-canvas.tsx
@src/components/habitat-widget.tsx
@src/components/habitat-scene.tsx
@package.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure D-28 — Lighthouse dashboard + real-device TTI/FPS</name>
  <files>.planning/phases/13-3d-habitat/13-WIDGET-PERF.md</files>
  <action>
    Per RESEARCH section E.1, execute:

    1. Start a production build of the app:
       ```bash
       npm run build && npm run start
       ```

    2. Run Lighthouse on the dashboard route with both profiles
       (consult Next.js 16 docs if the dashboard route changed — per
       AGENTS.md / CLAUDE.md — but the route name should be stable):

       ```bash
       npx lighthouse http://localhost:3000/dashboard --preset=desktop \
         --output=json --output-path=.planning/phases/13-3d-habitat/lighthouse-dashboard-desktop.json \
         --throttling-method=devtools

       npx lighthouse http://localhost:3000/dashboard \
         --output=json --output-path=.planning/phases/13-3d-habitat/lighthouse-dashboard-mobile.json \
         --throttling-method=devtools
       ```

       (`--preset=perf` was researcher's note; in current Lighthouse,
       mobile profile is the default. Use whatever Lighthouse version
       this project's Chrome ships.)

    3. Real-device measurement (CONTEXT Q2): on at least one
       physical mid-tier Android (Pixel 6/7 or Samsung A-series via
       Chrome remote inspection), record:
       - Cold-load TTI (clear cache, navigate to /dashboard)
       - Sustained FPS during widget auto-orbit (DevTools Performance
         tab, record 10 seconds)
       - Any console warnings (especially `THREE.WebGLRenderer: Context
         Lost.` per RESEARCH Pitfall 1)

    4. Write `13-WIDGET-PERF.md` with the structure:

       ```markdown
       # Phase 13 — D-28 Widget Performance Measurement

       **Measured:** 2026-MM-DD
       **Build:** npm run build (production)

       ## Methodology
       - Lighthouse desktop: `lighthouse --preset=desktop`
       - Lighthouse mobile: `lighthouse` (default)
       - Real device: Pixel N, Chrome vNN, throttled to "Slow 4G" in DevTools

       ## Results

       | Metric        | Desktop  | Mobile emu | Real device | CWV Good gate | Pass? |
       |---------------|---------:|----------:|------------:|--------------:|:-----:|
       | LCP           | X.X s    | X.X s     | X.X s       | ≤ 2.5 s       |       |
       | INP           | X ms     | X ms      | X ms        | ≤ 200 ms      |       |
       | CLS           | X.XX     | X.XX      | X.XX        | ≤ 0.1         |       |
       | Cold-load TTI | —        | —         | X.X s       | ≤ 5 s         |       |
       | Sustained FPS | —        | —         | XX          | ≥ 30          |       |
       | Context-lost? | —        | —         | yes/no      | no            |       |

       ## Decision

       [LIVE / CACHED] — rationale below.
       ```

    Do not yet decide live-vs-cached — Task 2 is the decision
    checkpoint.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-3d-habitat/13-WIDGET-PERF.md && grep -E "LCP|INP|CLS" .planning/phases/13-3d-habitat/13-WIDGET-PERF.md</automated>
  </verify>
  <done>
    Measurement report committed; all required numbers populated; no
    decision yet.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: D-28 live-vs-cached decision</name>
  <decision>
    Based on the measurements in `13-WIDGET-PERF.md`, decide whether the
    dashboard widget ships as live 3D (Plan 05's current implementation)
    or falls back to a cached `<img>` per level.
  </decision>
  <context>
    Decision rule from RESEARCH section E.1:
    *"If desktop AND mobile-emulation AND real-device all pass CWV Good
    with the live 3D widget enabled → ship live. Otherwise → ship
    cached."*

    Additional considerations:
    - Context-lost incidents on real device → strong signal toward cached
    - Sustained FPS < 30 on real device → strong signal toward cached
    - Marginal pass on mobile-emulation only → judgment call; lean cached
      if user-base is mobile-heavy
  </context>
  <options>
    <option id="live">
      <name>Ship LIVE 3D widget</name>
      <pros>Mood + decay reactive; no extra build pipeline; freshest data per page load.</pros>
      <cons>Higher GPU cost; ongoing perf risk as the scene grows.</cons>
    </option>
    <option id="cached">
      <name>Ship CACHED hero images per level</name>
      <pros>Zero GPU cost on dashboard; deterministic LCP; images double as social-share artifacts (RESEARCH E.3 note).</pros>
      <cons>Loses mood/decay reactivity on the widget (still on full /habitat page); adds a build-time pipeline that runs whenever scene changes.</cons>
    </option>
  </options>
  <resume-signal>
    Reply "live" or "cached". Decision is recorded in 13-WIDGET-PERF.md
    and drives Task 3.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3 (CONDITIONAL on "cached"): build hero-image pipeline + swap widget to &lt;img&gt;</name>
  <files>e2e/scripts/render-hero-images.spec.ts, public/habitat/widget-l1.webp, ..., public/habitat/widget-l9.webp, src/components/habitat-3d-widget-image.tsx, src/components/habitat-widget.tsx, package.json</files>
  <action>
    SKIP THIS TASK if Task 2 returned "live".

    If "cached":

    1. Create `e2e/scripts/render-hero-images.spec.ts` per RESEARCH
       Example 4 — loops levels 1..9, loads `/habitat?devLevel=N&snapshot=true`,
       waits for `canvas[data-ready=true]`, sets a fixed orbit theta via
       the `__habitatSetTheta` test affordance from Plan 03, screenshots
       the canvas. Save as PNG initially; post-process to WebP via
       `sharp` (add as devDependency if not present — small allowance for
       a build-pipeline-only dep, justified by R8).

       ```bash
       npm install --save-dev sharp
       ```

    2. Add `package.json` script:
       ```json
       "build:hero-images": "playwright test e2e/scripts/render-hero-images.spec.ts && node scripts/png-to-webp.mjs"
       ```

    3. Run the pipeline once; commit `public/habitat/widget-l{1..9}.webp`
       (or .png if WebP conversion is skipped — researcher's note in
       RESEARCH section E.3).

    4. Create `src/components/habitat-3d-widget-image.tsx`:

       ```typescript
       "use client";
       import Image from "next/image";
       import type { HabitatState } from "@/lib/habitat-engine";

       export default function HabitatWidgetImage({ habitatState }: { habitatState: HabitatState }) {
         return (
           <Image
             src={`/habitat/widget-l${habitatState.level}.webp`}
             alt={`Tiger habitat level ${habitatState.level}`}
             width={80} height={80}
             priority
           />
         );
       }
       ```

       Per AGENTS.md, consult `node_modules/next/dist/docs/` for
       `next/image` if the API differs in Next.js 16 — but the basic
       `next/image` usage should be stable.

    5. Edit `src/components/habitat-widget.tsx:11` to dynamic-import
       `habitat-3d-widget-image` INSTEAD of
       `habitat-3d-widget-canvas`. Since `next/image` is SSR-safe, the
       `ssr: false` can be removed for this variant:

       ```typescript
       import HabitatWidgetImage from "@/components/habitat-3d-widget-image";
       // ... <HabitatWidgetImage habitatState={habitatState} />
       ```

    6. Delete `src/components/habitat-3d-widget-canvas.tsx` (Plan 05's
       output — superseded by the image variant).

    If "live" was chosen in Task 2, skip all of the above; Plan 05's
    widget ships as-is.
  </action>
  <verify>
    <automated>if grep -q "cached" .planning/phases/13-3d-habitat/13-WIDGET-PERF.md; then ls public/habitat/widget-l*.webp | wc -l | grep -q "9"; else echo "live — skipping"; fi</automated>
  </verify>
  <done>
    Either: hero images exist + widget points to image variant, OR no
    change because "live" was chosen.
  </done>
</task>

<task type="auto">
  <name>Task 4: Measure CWV on /habitat + dashboard (SPEC R9) — final gate</name>
  <files>.planning/phases/13-3d-habitat/13-PERF.md, .planning/phases/13-3d-habitat/lighthouse-habitat-desktop.json, lighthouse-habitat-mobile.json, lighthouse-dashboard-desktop.json (re-run if cached path took effect), lighthouse-dashboard-mobile.json</files>
  <action>
    Per SPEC R9, run Lighthouse on BOTH routes (`/habitat` and
    `/dashboard`) with both profiles (desktop + mobile emulation),
    against the final ship configuration (live OR cached widget per
    Task 2 decision).

    If Task 2 was "cached", the dashboard Lighthouse from Task 1 is
    stale — re-run it now that the widget is `<img>`-based.

    Commit:
    - `lighthouse-habitat-desktop.json`
    - `lighthouse-habitat-mobile.json`
    - `lighthouse-dashboard-desktop.json`
    - `lighthouse-dashboard-mobile.json`

    Write `13-PERF.md`:

    ```markdown
    # Phase 13 — Core Web Vitals Performance Report

    **Measured:** 2026-MM-DD
    **Build:** npm run build (production)
    **Widget variant:** LIVE / CACHED (per 13-WIDGET-PERF.md)

    ## Acceptance Gate (SPEC R9)

    | Route       | Profile  | LCP    | INP   | CLS    | All pass? |
    |-------------|----------|-------:|------:|-------:|:---------:|
    | /habitat    | desktop  | X.X s  | X ms  | X.XX   |    ✓/✗    |
    | /habitat    | mobile   | X.X s  | X ms  | X.XX   |    ✓/✗    |
    | /dashboard  | desktop  | X.X s  | X ms  | X.XX   |    ✓/✗    |
    | /dashboard  | mobile   | X.X s  | X ms  | X.XX   |    ✓/✗    |

    All four rows must show ✓ for Phase 13 to ship.

    Thresholds: LCP ≤ 2.5s, INP ≤ 200ms, CLS ≤ 0.1
    ```

    If ANY of the four rows fails, this task does NOT proceed to
    delete PixiJS. Instead, escalate: file a gap-closure plan
    (`/gsd-plan-phase 13 --gaps`) identifying the failing route +
    metric. Do NOT silently proceed.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-3d-habitat/13-PERF.md && grep -E "LCP|INP|CLS" .planning/phases/13-3d-habitat/13-PERF.md</automated>
  </verify>
  <done>
    13-PERF.md committed; all 4 rows show ✓ OR a gap-closure plan is
    filed and the rest of this plan halts.
  </done>
</task>

<task type="auto">
  <name>Task 5: Delete all PixiJS files + sprites + uninstall pixi deps</name>
  <files>(see frontmatter file_modified DELETE list)</files>
  <action>
    Per SPEC R10 + RESEARCH section F.1, delete all 6 PixiJS-dependent
    files (NOT just the 3 named in SPEC):

    ```bash
    rm src/components/habitat-canvas.tsx
    rm src/components/habitat-layers.tsx
    rm src/components/habitat-widget-canvas.tsx
    rm src/components/tiger-sprite.tsx
    rm src/components/sparkle-particles.tsx
    rm src/components/bird-sprite.tsx
    rm public/sprites/habitat.png
    rm public/sprites/habitat.json
    rm public/sprites/tiger.png
    rm public/sprites/tiger.json
    rmdir public/sprites 2>/dev/null || true
    ```

    Also delete any orphaned test files for the deleted components:
    ```bash
    rm -f src/components/__tests__/habitat-canvas.test.tsx
    rm -f src/components/__tests__/habitat-layers.test.tsx
    rm -f src/components/__tests__/habitat-widget-canvas.test.tsx
    rm -f src/components/__tests__/tiger-sprite.test.tsx
    rm -f src/components/__tests__/sparkle-particles.test.tsx
    rm -f src/components/__tests__/bird-sprite.test.tsx
    ```

    Uninstall PixiJS:
    ```bash
    npm uninstall pixi.js @pixi/react
    ```

    Final acceptance greps (per SPEC line 121-122):

    ```bash
    grep -rn "pixi" src/ package.json   # MUST return no matches
    grep -r "babel/standalone\|unpkg.com" src/ public/   # MUST return no matches
    npm ls three          # MUST show three@0.160.x
    ```

    All three commands must report 0 matches / correct version. If any
    fails, do not proceed — investigate the leftover reference.

    Per RESEARCH "B.2 Bundle code-split", run `npm run build` and
    inspect output:
    - `.next/static/chunks/` should contain a chunk >100KB whose name
      includes a Three.js hash
    - The chunk should NOT appear under "First Load JS" for the
      dashboard or habitat — it should be lazy-loaded

    Document chunk size in the SUMMARY.
  </action>
  <verify>
    <automated>! grep -rn "pixi" src/ package.json && ! grep -r "babel/standalone\|unpkg.com" src/ public/ && npm ls three && npm run build</automated>
  </verify>
  <done>
    All 6 PixiJS files deleted, 4 sprite files deleted, 2 deps
    uninstalled, all greps return empty, `next build` succeeds with
    Three.js in a separate chunk.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final manual smoke test — Josh signs off the full migration</name>
  <what-built>
    Phase 13 is functionally complete:
    - `/habitat` renders Three.js for all 9 levels
    - Dashboard widget renders Three.js (or cached image per D-28 decision)
    - PixiJS is gone from src/ and package.json
    - Lighthouse passes CWV Good on both routes
    - Mood + decay states verified at level 5 (28 screenshots in Plan 04)
    - Keyboard orbit + reduced-motion work (Plan 03 Playwright spec)
  </what-built>
  <how-to-verify>
    1. `npm run build && npm run start` — open http://localhost:3000.

    2. Sign in as the test user. Confirm:
       - Dashboard shows the 3D widget (or hero image if cached)
       - Click widget → /habitat loads the full 3D scene
       - Level badge + mood label render correctly
       - Drag the canvas → camera orbits
       - Scroll wheel → no zoom (D-26)
       - Press ArrowLeft / ArrowRight with the canvas focused → camera rotates

    3. Mutate `learnedCardCount` directly in Neon to land at effective-card-count
       values {0, 5, 15, 30, 50, 80, 120, 170, 230} in turn (per SPEC R1
       acceptance). Reload `/habitat` between each value. Confirm each level
       shows the correct features per `.planning/design/habitat-art-assets.md`:
       - L1: just Leo on the mound
       - L2: + lake + lilies + path
       - L3: + trees + rocks
       - L4: + flowers + grass + butterflies
       - L5: + elephant
       - L6: + mushrooms
       - L7: + cave (sleep cycle visible during simulated night)
       - L8: + toys
       - L9: + songbirds + golden-hour sky

    4. DevTools "Emulate prefers-reduced-motion: reduce": confirm
       auto-orbit halts on both the full /habitat canvas and the
       widget (if live).

    5. DevTools Performance recording on /habitat: confirm sustained
       frame rate is ≥ 30fps on the dev machine.

    6. Final visual scan: does the 3D scene MATCH the designer's
       intent? Compare to `.planning/design/animations/`'s original
       HTML preview if needed.

    7. If anything is wrong: describe per-issue. Otherwise: reply
       "approved" to ship Phase 13.
  </how-to-verify>
  <resume-signal>
    Reply "approved" or describe issues.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev script (`render-hero-images.spec.ts`) writes to `public/` | Build-time only; assets are static, no user data |
| `next build` output chunk loaded by browser | Three.js chunk is signed by integrity hash via Next.js asset hashing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-25 | Tampering | `npm uninstall pixi.js @pixi/react` could leave transitive deps | mitigate | After uninstall, run `npm dedupe && npm prune`; verify `node_modules/pixi.js/` does not exist. The final grep `grep -rn pixi src/ package.json` covers `package.json`; also grep `package-lock.json` once to confirm. |
| T-13-26 | Information Disclosure | `13-WIDGET-PERF.md` + Lighthouse JSON committed | accept | No PII; pages measured are authenticated routes loaded with a test fixture session — Lighthouse output may include the test user's display name. Scrub before commit if needed. |
| T-13-27 | Denial of Service | Hero-image build pipeline runs Playwright on CI | accept | Run is on-demand (`npm run build:hero-images`), not on every CI build. Cost = 30s × 9 levels = ~5 minutes total. Triggered only when D-28 falls back. |
| T-13-28 | Elevation of Privilege | `?devLevel=N&snapshot=true` URL override (Plan 04 affordance) used by hero-image pipeline | mitigate | Override is dev-only (`process.env.NODE_ENV !== "production"`); hero-image build runs against a dev-build server, not against production. Confirmed in Plan 04 Task 3. |
| T-13-29 | Tampering | Final `three` dep + delete of all PixiJS | mitigate | Lockfile commit captures the post-uninstall state; final grep `grep -rn "pixi" src/ package.json` is a hard pass/fail gate. |
| T-13-30 | Denial of Service | Three.js code-split chunk fails to lazy-load (would inline into main bundle) | mitigate | Acceptance check via `next build` output table; if Three.js appears in "First Load JS" for any page, escalate to a config fix — but the existing `dynamic({ ssr: false })` boundary already splits per Next.js 16 docs (`node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md`). |

</threat_model>

<verification>
**Hard gates (all must pass):**

- [ ] `13-WIDGET-PERF.md` committed with all required metrics + decision
- [ ] `13-PERF.md` committed with 4-row table all ✓
- [ ] `grep -rn "pixi" src/ package.json` returns 0 matches
- [ ] `grep -r "babel/standalone\|unpkg.com" src/ public/` returns 0 matches
- [ ] `npm ls three` shows `three@0.160.x`
- [ ] `npm ls pixi.js` reports "(empty)"
- [ ] All 6 PixiJS .tsx files do not exist on disk
- [ ] All 4 sprite files do not exist on disk
- [ ] `next build` output shows Three.js in a separate chunk (not "First Load JS")
- [ ] `npm run test` — all unit tests green
- [ ] `npm run test:e2e` — all Playwright specs green (Plan 03 + Plan 04 + any others)
- [ ] `npm run typecheck` + `npm run lint` clean
- [ ] Josh final smoke-test approval (Task 6)
</verification>

<success_criteria>
- [ ] D-28 decision recorded with measurement evidence
- [ ] If "cached": 9 hero images exist, widget swapped to <img>, hero-image script committed
- [ ] If "live": Plan 05's widget ships unchanged
- [ ] CWV Good met on dashboard + /habitat × desktop + mobile (4/4 rows)
- [ ] All 6 PixiJS files + 4 sprites + 2 packages removed
- [ ] Final acceptance greps all return 0 matches
- [ ] Three.js code-split chunk confirmed
- [ ] Josh's final approval recorded
- [ ] Phase 13 complete; Visual style "cute 3D illustrated habitats" can move
      from PROJECT.md `Active` → `Validated` (per habitat-art-assets.md item 10)
</success_criteria>

<output>
After completion, create `.planning/phases/13-3d-habitat/13-06-SUMMARY.md`
including:
- D-28 final decision + verbatim measured numbers
- 4-row CWV table snapshot
- Files deleted (sha of git rm)
- Final `npm ls three` output
- Bundle chunk size of the lazy-loaded Three.js chunk
- Josh's approval quote
- Total Phase 13 LOC delta (added/removed)
</output>
