---
phase: 19-daybreak-foundation-onboarding-auth
plan: 05
type: execute
wave: 3
depends_on: [19-01]
files_modified:
  - src/components/card-list.tsx
autonomous: true
requirements: [ONB-06]
must_haves:
  truths:
    - "The empty-deck state shows a Daybreak Leo medallion, 'Your deck is empty', an encouraging line, a 'Browse words' primary, and a '+ Add a card' secondary action"
    - "The no-search-results state shows a Daybreak Leo medallion, 'No words match \"<query>\"', and a 'Clear search' action that clears the query"
    - "Both empty states match the Daybreak ObEmptyDeck / ObNoSearch hi-fi"
  artifacts:
    - path: "src/components/card-list.tsx"
      provides: "Daybreak empty-deck + no-search-results presentation"
      contains: "Your deck is empty"
  key_links:
    - from: "src/components/card-list.tsx"
      to: "src/components/daybreak/lion-face.tsx"
      via: "import LionFace for the empty-state medallions"
      pattern: "lion-face"
    - from: "src/components/card-list.tsx"
      to: "Clear search action"
      via: "onClick resets query state to empty"
      pattern: "setQuery"
---

<objective>
Restyle the two shared empty states that live in `src/components/card-list.tsx` to the Daybreak design system (ONB-06): the empty-deck state (`Your deck is empty` + Browse words / Add a card) and the no-search-results state (`No words match "<query>"` + Clear search), matching the `ObEmptyDeck` and `ObNoSearch` mocks.

Purpose: ONB-06 — the shared empty-state visuals. These render on the dashboard "Your words" surface and are the only ONB-06 deliverable in this phase. The full dashboard redesign (DSH-*) is Phase 21; this plan touches ONLY the two empty-state presentation blocks, not the card table/rows/search-bar layout.
Output: Updated empty-deck and no-search-results blocks in `card-list.tsx`, Daybreak-faithful.

SCOPE GUARD: Do NOT redesign the card list table, mobile card rows, mastery meters, pause/edit actions, or the search input chrome here — those are DSH-04/05 (Phase 21). Restyle only the `cards.length === 0` empty-deck block and the `filtered.length === 0 && query` no-results block. Preserve all existing behavior, props, and the QA badge wiring untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-CONTEXT.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-01-SUMMARY.md

<interfaces>
<!-- Primitives (19-01) available to compose -->
LionFace (src/components/daybreak/lion-face.tsx): <LionFace size={66} mane="#E8973B" face="#FFD9A6" muzzle="#FFF1DC" ink="#4A331C" />

<!-- Existing card-list.tsx state the empty blocks rely on -->
query: string ; setQuery(""): clears search ; cards: CardRow[] ; filtered: CardRow[]
Existing routes/links: "/deck/browse" (Browse words). "Add a card" destination link — match the existing add-card entry used elsewhere in the deck view.

<!-- Daybreak tokens (globals.css) for the empty-state surfaces -->
--db-pill-bg: #fff1dc  (medallion circle bg)  ;  --primary: #f28a1f  ;  --foreground: #4a331c (ink) ; --muted-foreground
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the empty-deck + no-search-results states to Daybreak (ONB-06)</name>
  <files>src/components/card-list.tsx</files>
  <read_first>
    - src/components/card-list.tsx — the full current file: the empty-deck block (`cards.length === 0`, lines 71-86, currently "Your deck is empty" + Browse words link) and the no-results block (`filtered.length === 0 && query`, lines 111-121, currently "No cards match …" + "Try a different word."), plus the `query`/`setQuery` state and the "Clear search" button already present in the search bar (lines 99-108) — reuse the same `setQuery("")` to power the empty-state "Clear search" action
    - design/handoff-daybreak/daybreak-onboarding.jsx — ObEmptyDeck (lines 146-164): Leo in a 110px #FFF1DC medallion, "Your deck is empty", body "Add a few words and Leo's habitat starts to grow.", an 80%-width column with a "Browse words" primary (height 50) and a "+ Add a card" ghost button (height 50); and ObNoSearch (lines 165-181): Leo in a 96px #F3E3C6 medallion, "No words match “{query}”", body "Try a different spelling, or clear the search.", and a ghost "Clear search" button (height 44)
    - design/handoff-daybreak/README.md — §6 Onboarding & Auth (empty states)
    - design/handoff-daybreak/LeoCards Daybreak Onboarding & Auth.html — the ObEmptyDeck / ObNoSearch artboards for visual reference
    - src/components/daybreak/lion-face.tsx — LionFace props
    - src/components/deck-view.tsx — to confirm the canonical "Add a card" link/route used in the deck view (so the empty-state "+ Add a card" points at the same destination as the rest of the app)
    - .planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md — the Daybreak Token Reference + the empty-state design specs
  </read_first>
  <action>
    Edit ONLY the two empty-state branches in `card-list.tsx`. EMPTY-DECK block (`cards.length === 0`): replace the current markup with the Daybreak ObEmptyDeck layout — a centered vertical column with a circular Leo medallion (a ~110px rounded-full `bg-[var(--db-pill-bg)]` circle containing `<LionFace size={66} .../>`), the heading "Your deck is empty" (`font-display text-[22px] font-bold text-foreground`), the body "Add a few words and Leo's habitat starts to grow." (`text-muted-foreground max-w-[240px] text-center`), then an ~80%-width column with two stacked full-width actions: a primary "Browse words" linking to `/deck/browse` (styled as the Daybreak primary — amber, rounded-14, height 50, `shadow-[var(--db-btn-shadow)]`) and a secondary "+ Add a card" ghost button (surface bg, 1.5px #EDDFC9 border, height 50) linking to the deck-view's canonical add-card destination. NO-RESULTS block (`filtered.length === 0 && query`): replace with the Daybreak ObNoSearch layout — a centered column with a ~96px rounded-full `bg-[#F3E3C6]` Leo medallion (`<LionFace size={56} .../>`), the heading `No words match "{query}"` (`font-display text-[20px] font-bold` — keep the curly-quote rendering of the live query value), the body "Try a different spelling, or clear the search." (`text-muted-foreground max-w-[230px]`), and a ghost "Clear search" button (height 44) whose `onClick` calls `setQuery("")` (reuse the existing clear behavior). Do NOT touch the search input, the desktop table, the mobile card rows, the mastery meter, the pause/edit buttons, the QA badge, or the `CardEditDialog`. Keep the component a `React.memo` client component with the same props and exports. Preserve the existing strings the e2e suite asserts where still valid: keep the exact heading "Your deck is empty" and keep a "Browse words" accessible link (the `02-first-visit` spec checks both).
  </action>
  <verify>
    <automated>grep -c "Your deck is empty" src/components/card-list.tsx; grep -c "Browse words" src/components/card-list.tsx; grep -c "lion-face" src/components/card-list.tsx; npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "card-list\.tsx"; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/card-list.tsx` empty-deck block keeps the exact heading "Your deck is empty", renders a `<LionFace>` medallion, a "Browse words" link to `/deck/browse`, and a "+ Add a card" action
    - The no-results block renders a `<LionFace>` medallion, the heading text `No words match "<query>"` interpolating the live query, and a "Clear search" control whose handler calls `setQuery("")`
    - The file still imports/uses the existing card table, mobile rows, QA badge, and `CardEditDialog` unchanged (grep: `CardEditDialog` and `QaStateBadge` still present) — scope was limited to the two empty-state blocks
    - The component remains a `React.memo` export with unchanged `CardListProps`
    - `npx tsc --noEmit` reports no type errors in `card-list.tsx`
  </acceptance_criteria>
  <done>The empty-deck and no-search-results states match the Daybreak ObEmptyDeck/ObNoSearch mocks (Leo medallion, headings, actions); Clear search clears the query; the rest of the card list is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-only restyle of two existing client-side empty-state branches. No new network, auth, or DB surface; the live `query` value is already user input rendered by React. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-XSS | Tampering (XSS via search query) | `No words match "{query}"` rendering | mitigate | The query is interpolated as a normal JSX text child (React auto-escapes); no `dangerouslySetInnerHTML`. The existing code already renders the query this way — parity preserved. |
| T-19-05-PRES | Information Disclosure | card-list empty states (client) | accept | Presentation-only; references no secrets and adds no data fetching. No new attack surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` — no type errors in card-list.tsx
- `grep -c "Your deck is empty" card-list.tsx` ≥ 1 and a "Browse words" link is present (keeps the `02-first-visit` spec assertions valid)
- Manual/visual (deferred to phase gate, --skip-ui): both empty states match daybreak-onboarding.jsx ObEmptyDeck/ObNoSearch and the `LeoCards Daybreak Onboarding & Auth.html` artboard
</verification>

<success_criteria>
- Empty-deck state Daybreak-styled with Leo medallion + Browse words / + Add a card (ONB-06)
- No-search-results state Daybreak-styled with Leo medallion + Clear search that clears the query (ONB-06)
- Card list table/rows/search chrome left untouched (scope held to empty states; DSH-* remains Phase 21)
</success_criteria>

<output>
Create `.planning/phases/19-daybreak-foundation-onboarding-auth/19-05-SUMMARY.md` when done. Record: that only the two empty-state branches were restyled (table/rows untouched), and confirm the "Your deck is empty" + "Browse words" strings were preserved for e2e stability.
</output>
