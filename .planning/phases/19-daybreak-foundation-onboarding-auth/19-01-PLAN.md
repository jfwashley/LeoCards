---
phase: 19-daybreak-foundation-onboarding-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/daybreak/t-field.tsx
  - src/components/daybreak/t-btn.tsx
  - src/components/daybreak/pill.tsx
  - src/components/daybreak/card.tsx
  - src/components/daybreak/__tests__/t-field.test.tsx
  - src/components/daybreak/__tests__/t-btn.test.tsx
  - src/hooks/use-prefers-reduced-motion.ts
  - src/components/habitat-video.tsx
autonomous: true
requirements: [DSY-01, DSY-02, DSY-03]
must_haves:
  truths:
    - "Implements CONTEXT decisions D-01 (dedicated Daybreak primitives TField/TBtn/Pill/Card in src/components/daybreak/) and D-09 (global DSY-03 conventions baked into the primitives: ≥44px touch targets, inline post-submit per-field validation, single full-width primary with spinner)"
    - "TField renders a labeled input, forwards its ref to the <input>, and shows a red border + helper text when an error prop is passed"
    - "TBtn renders an amber full-width button that shows a spinner and is disabled while isPending is true"
    - "Pill and Card primitives render the Daybreak warm-tint chip and white card surface"
    - "usePrefersReducedMotion() returns the user's reduced-motion preference and is importable from a shared hook module"
    - "Daybreak tokens (cream bg, amber primary) and Baloo 2 + Figtree fonts are confirmed live app-wide in globals.css + layout.tsx"
  artifacts:
    - path: "src/components/daybreak/t-field.tsx"
      provides: "Labeled input primitive (forwardRef) reused by every auth screen"
      exports: ["TField"]
    - path: "src/components/daybreak/t-btn.tsx"
      provides: "Primary button primitive with spinner/disabled state"
      exports: ["TBtn"]
    - path: "src/components/daybreak/pill.tsx"
      provides: "Pill/chip primitive"
      exports: ["Pill"]
    - path: "src/components/daybreak/card.tsx"
      provides: "Card surface primitive"
      exports: ["Card"]
    - path: "src/hooks/use-prefers-reduced-motion.ts"
      provides: "SSR-safe reduced-motion media-query hook"
      exports: ["usePrefersReducedMotion"]
    - path: "src/components/daybreak/__tests__/t-field.test.tsx"
      provides: "Unit coverage for TField (label, error class, ref forwarding, disabled)"
    - path: "src/components/daybreak/__tests__/t-btn.test.tsx"
      provides: "Unit coverage for TBtn (spinner when isPending, disabled, renders children)"
  key_links:
    - from: "src/components/habitat-video.tsx"
      to: "src/hooks/use-prefers-reduced-motion.ts"
      via: "import { usePrefersReducedMotion }"
      pattern: "use-prefers-reduced-motion"
    - from: "src/components/daybreak/t-field.tsx"
      to: "react-hook-form register()"
      via: "React.forwardRef ref spread"
      pattern: "forwardRef"
---

<objective>
Stand up the Daybreak shared foundation: the reusable primitives (`TField`, `TBtn`, `Pill`, `Card`) in `src/components/daybreak/`, the SSR-safe `usePrefersReducedMotion()` hook, unit tests for the two interactive primitives, and a verification pass confirming the Daybreak tokens/fonts spike is correctly applied app-wide. This is the contract layer every other Phase 19 plan consumes.

Purpose: DSY-01/02/03 are the design-system foundation. Auth screens (19-02, 19-03), the welcome flow (19-04), and empty states (19-05) all import these primitives, so they MUST exist and be tested first. The reduced-motion hook is extracted now because the welcome teaser (19-04) depends on it.
Output: Four primitive components, one shared hook, two unit-test files, an updated `habitat-video.tsx` import, and a confirmed token/font baseline.

WAVE 0 NOTE (test infra): This plan IS the Wave 0 work for the phase. The two unit-test files it creates (`__tests__/t-field.test.tsx`, `__tests__/t-btn.test.tsx`) are the DSY-02 stubs that 19-VALIDATION.md tracks as Wave 0 requirements. These primitive component tests render via `@testing-library/react`, which needs a DOM environment — the repo `vitest.config.ts` currently sets `environment: "node"`. Task 1 MUST ensure the daybreak primitive tests run under a DOM (jsdom): either add a `// @vitest-environment jsdom` docblock at the top of each `__tests__/*.test.tsx` file, or add a `test.environmentMatchGlobs`/per-file environment so `src/components/**/*.test.tsx` use jsdom — whichever the installed Vitest 4.1.1 supports — without changing the default `node` env that the existing non-DOM unit tests rely on. Confirm `@testing-library/react` + `jsdom` resolve before writing the assertions; if `jsdom` is not installed, that is a genuine Wave 0 install (`npm i -D jsdom @testing-library/react`) and must happen in Task 1 before the tests can go green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-CONTEXT.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-RESEARCH.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md

<interfaces>
<!-- Contracts this plan PRODUCES. Downstream plans (19-02..19-05) import these exact shapes. -->

TField (src/components/daybreak/t-field.tsx) — forwardRef<HTMLInputElement, TFieldProps>
  TFieldProps extends React.InputHTMLAttributes<HTMLInputElement> {
    label: string;        // required visible <label>, htmlFor wired to the input id
    error?: string;       // when set: red border + this string rendered as helper
    hint?: string;        // shown only when error is absent (e.g. "At least 8 characters")
  }
  Usage: <TField label="Email" error={isSubmitted ? errors.email?.message : undefined} {...register("email")} />

TBtn (src/components/daybreak/t-btn.tsx) — function component
  TBtnProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
    isPending?: boolean;  // shows <Loader2 className="animate-spin" /> and disables the button
  }
  Usage: <TBtn type="submit" isPending={isPending}>Sign in</TBtn>

Pill (src/components/daybreak/pill.tsx)
  PillProps { children: React.ReactNode; className?: string }

Card (src/components/daybreak/card.tsx)
  CardProps { children: React.ReactNode; className?: string }

usePrefersReducedMotion (src/hooks/use-prefers-reduced-motion.ts) — () => boolean
  SSR-safe default false; subscribes to "(prefers-reduced-motion: reduce)".
</interfaces>

<token_reference>
<!-- From src/app/globals.css :root (lines 53-96) — already live; primitives consume these. -->
--db-field-bg: #fffbf4   (TField fill)
--border / field border literal: #EDDFC9
--destructive: #de5f4a   (error border + helper text → Tailwind border-destructive / text-destructive)
--primary: #f28a1f       (TBtn bg → bg-primary)
--db-btn-shadow: 0 10px 22px rgba(242, 138, 31, 0.3)
--db-pill-bg: #fff1dc  /  --db-pill-text: #b4762a
--db-card-radius: 22px  /  --db-card-border: #f0e3cf  /  --db-card-shadow: 0 12px 30px rgba(160, 110, 40, 0.16)
</token_reference>

<test_infra>
<!-- Verified from repo. The validation map in 19-VALIDATION.md is the source of truth. -->
Vitest 4.1.1 — `npx vitest run` (quick). Config: vitest.config.ts, default environment "node", setupFiles ["./src/test-setup.ts"], excludes e2e/**, "@" alias → ./src.
The daybreak primitive tests need a DOM env (jsdom) — see WAVE 0 NOTE in <objective>.
Playwright 1.58.2 — `npx playwright test` (e2e; specs in e2e/). Not used by this plan.
</test_infra>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build TField + TBtn primitives with unit tests</name>
  <files>src/components/daybreak/t-field.tsx, src/components/daybreak/t-btn.tsx, src/components/daybreak/__tests__/t-field.test.tsx, src/components/daybreak/__tests__/t-btn.test.tsx</files>
  <read_first>
    - design/handoff-daybreak/hifi-shared.jsx — `TField` (lines 59-73) and `TBtn` (lines 75-88): the exact visual atoms to port (field height 48→use h-12, radius, error red, button height 50, radius 14, spinner)
    - design/handoff-daybreak/README.md — §Design System (token values) and §1 Login (field/button spec)
    - src/app/(auth)/login/page.tsx — the inline `fieldClass()` helper (lines 25-30) and `Button`+`Loader2` block (lines 127-133): the precise Tailwind values being extracted into the primitives
    - .planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md — Pattern Assignments for `t-field.tsx` and `t-btn.tsx` (the canonical TSX to produce, including displayName and react-hook-form usage)
    - src/app/globals.css — confirm `--db-field-bg`, `--db-btn-shadow`, `--destructive`, `--primary` exist before referencing them
    - vitest.config.ts — confirm the test runner config: default `environment: "node"`, `setupFiles: ["./src/test-setup.ts"]`, `exclude` includes `e2e/**`, and the `@` → `./src` alias. The daybreak primitive tests need a DOM, so set a per-file jsdom environment (see WAVE 0 NOTE) rather than flipping the global default.
    - src/components/__tests__/qa-state-badge.test.ts — an existing component-style Vitest test to mirror for import/render conventions (and to confirm whether @testing-library/react + jsdom are already wired)
  </read_first>
  <behavior>
    - TField: renders a `<label>` containing the `label` text whose `htmlFor` matches the input `id`; renders an `<input>` that receives a forwarded ref (verify via a `useRef`/callback ref in the test capturing an HTMLInputElement); applies an error/destructive border class and renders the `error` string when `error` is set; renders `hint` only when `error` is absent; spreads arbitrary input props (e.g. `type`, `placeholder`, `disabled`, `aria-*`).
    - TBtn: renders its `children` as the label when `isPending` is falsy; renders the Loader2 spinner (`svg` with `animate-spin`) and sets `disabled` when `isPending` is true; is also `disabled` when the `disabled` prop is true; spreads `type`, `onClick`, etc.
  </behavior>
  <action>
    First ensure the daybreak primitive tests can run under a DOM: confirm `@testing-library/react` and `jsdom` resolve (mirror src/components/__tests__/qa-state-badge.test.ts to see what's already available); if `jsdom` is absent, install it as a dev dependency. Give each new `__tests__/*.test.tsx` file a jsdom environment without changing the global `node` default (a `// @vitest-environment jsdom` docblock at the top of each test file is the least-invasive route in Vitest 4.1.1). Then create `TField` as `React.forwardRef<HTMLInputElement, TFieldProps>` (props extend `React.InputHTMLAttributes<HTMLInputElement>` plus `label: string`, `error?: string`, `hint?: string`); derive `fieldId` from `id ?? label.toLowerCase().replace(/\s+/g, "-")`; wire `<label htmlFor>` to it; spread `...inputProps` and forward `ref` onto the `<input>`; use the exact Tailwind classes from the login `fieldClass()` extraction (`h-12 rounded-xl border-[1.5px] bg-[var(--db-field-bg)] px-3.5 text-[15px]`, focus ring `focus-visible:ring-2 focus-visible:ring-primary/40`, `border-destructive` when error else `border-[#EDDFC9]`); set `TField.displayName = "TField"`. Mark `"use client"` (it is imported by client form pages and uses forwardRef). Create `TBtn` as a function component (props extend `React.ButtonHTMLAttributes<HTMLButtonElement>` plus `isPending?: boolean`); render `<button disabled={isPending || disabled}>` with classes `h-[50px] w-full rounded-[14px] text-[16px] font-bold bg-primary text-primary-foreground shadow-[var(--db-btn-shadow)]` plus disabled/hover states, merging an optional incoming `className`; render `<Loader2 className="size-5 animate-spin" />` from `lucide-react` when `isPending` else `children`. Write Vitest + @testing-library/react tests asserting the behaviors above (ref capture for TField; spinner presence + `disabled` attribute for TBtn). Do NOT introduce shadcn `Input`/`Button` as dependencies — these primitives are standalone DOM elements.
  </action>
  <verify>
    <automated>npx vitest run src/components/daybreak/__tests__/t-field.test.tsx src/components/daybreak/__tests__/t-btn.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/daybreak/t-field.tsx` exports a component created with `React.forwardRef` and sets `TField.displayName = "TField"` (grep: `forwardRef` and `displayName` both present)
    - `src/components/daybreak/t-btn.tsx` exports `TBtn` and imports `Loader2` from `lucide-react`
    - Each daybreak test file runs under a DOM environment (jsdom docblock present, or an equivalent per-file env) without changing the global `node` default in vitest.config.ts
    - The TField test captures a non-null `HTMLInputElement` through a forwarded ref (ref-forwarding assertion present)
    - The TBtn test asserts the spinner renders AND the button has the `disabled` attribute when `isPending` is true
    - `npx vitest run src/components/daybreak/__tests__/` exits 0 with both test files passing
  </acceptance_criteria>
  <done>TField and TBtn exist as standalone primitives matching the handoff atoms, both unit-tested (ref forwarding + spinner/disabled) under a DOM env, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Build Pill + Card primitives</name>
  <files>src/components/daybreak/pill.tsx, src/components/daybreak/card.tsx</files>
  <read_first>
    - design/handoff-daybreak/hifi-shared.jsx — the card surface used inside `HiFiLogin` (lines 105-107) for radius/border/shadow reference
    - src/components/daybreak/auth-card.tsx — the inner surface `<div>` (lines 184-194): the exact card surface style (radius 22, border #F0E3CF, shadow) to mirror in the standalone `Card`
    - .planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md — Pattern Assignments for `pill.tsx` and `card.tsx` (canonical TSX + token sources)
    - src/app/globals.css — `--db-pill-bg`/`--db-pill-text` (lines 93-94) and `--db-card-*` (lines 88-90)
  </read_first>
  <action>
    Create `Pill` (props `{ children: React.ReactNode; className?: string }`) as an inline-flex rounded-full `<span>` using classes `inline-flex items-center rounded-full px-2.5 py-0.5 text-[12px] font-semibold bg-[var(--db-pill-bg)] text-[var(--db-pill-text)]`, merging an optional `className`. Create `Card` (props `{ children: React.ReactNode; className?: string }`) as a `<div>` matching the auth-card inner surface — borderRadius 22, background `#FFFFFF`, `1px solid #F0E3CF` border, boxShadow `0 12px 30px rgba(160, 110, 40, 0.16)` (mirror the inline-style approach used in `auth-card.tsx` for visual parity, or the equivalent `shadow-[var(--db-card-shadow)]` Tailwind), accepting `className` for layout overrides. Both are pure presentational components (no `"use client"` needed — RSC-safe, no hooks).
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo TSC-OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/daybreak/pill.tsx` exports `Pill` and references `var(--db-pill-bg)` and `var(--db-pill-text)`
    - `src/components/daybreak/card.tsx` exports `Card` and applies a 22px radius + `#F0E3CF` border + the Daybreak card shadow
    - `npx tsc --noEmit` exits 0 and prints `TSC-OK` — the whole project type-checks INCLUDING the new `pill.tsx` and `card.tsx`. This gates on the tsc exit code being 0; a missing or erroring primitive file makes tsc exit non-zero and fails the gate (do NOT use a grep-on-tsc-output pattern that passes when files are absent).
  </acceptance_criteria>
  <done>Pill and Card primitives exist, the project type-checks clean (tsc exit 0), and they visually match the Daybreak chip and card surface tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Extract usePrefersReducedMotion hook + verify token/font baseline</name>
  <files>src/hooks/use-prefers-reduced-motion.ts, src/components/habitat-video.tsx</files>
  <read_first>
    - src/components/habitat-video.tsx — lines 88-110: the existing inline `usePrefersReducedMotion` implementation to extract verbatim, and every local call site to repoint at the new import
    - .planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md — `use-prefers-reduced-motion.ts` Pattern Assignment (exact hook code + "update habitat-video.tsx import" note)
    - .planning/phases/19-daybreak-foundation-onboarding-auth/19-RESEARCH.md — Pattern 6 (hook spec; SSR-safe default false)
    - src/app/globals.css — confirm `:root` Daybreak tokens (cream `--background: #fff6e9`, amber `--primary: #f28a1f`) and `@theme inline` font vars are present (DSY-01 verification, no edit expected)
    - src/app/layout.tsx — confirm Baloo 2 (`--font-display`) + Figtree (`--font-sans`) wired via `next/font/google` and applied on `<html>` (DSY-01 verification, no edit expected)
    - node_modules/next/dist/docs — if any token/font gap is found and an edit becomes necessary, read the relevant Next.js 16 font/metadata guide before editing
  </read_first>
  <action>
    Create `src/hooks/use-prefers-reduced-motion.ts` (`"use client"`) exporting `usePrefersReducedMotion(): boolean` — copy the implementation verbatim from `habitat-video.tsx` (SSR-safe `useState(false)`, guard `typeof window === "undefined" || typeof window.matchMedia !== "function"`, subscribe to `"(prefers-reduced-motion: reduce)"`, `addEventListener?.`/`removeEventListener?.` cleanup). Then edit `habitat-video.tsx` to remove the local hook definition and `import { usePrefersReducedMotion } from "@/hooks/use-prefers-reduced-motion"` instead, leaving all call sites working. Finally, VERIFY (do not rewrite) DSY-01: confirm `globals.css :root` defines the cream `--background` and amber `--primary` and the `@theme inline` font mappings, and that `layout.tsx` loads `Baloo_2` + `Figtree` and applies `figtree.variable`/`baloo2.variable` + `font-sans` on `<html>`. The spike is already in the tree — only make a minimal edit if a token or font wiring is actually missing; otherwise record the baseline as confirmed in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/habitat-scene-video.test.ts --reporter=verbose && grep -rn "use-prefers-reduced-motion" src/components/habitat-video.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/use-prefers-reduced-motion.ts` exports `usePrefersReducedMotion` and matches on `(prefers-reduced-motion: reduce)`
    - `src/components/habitat-video.tsx` imports `usePrefersReducedMotion` from `@/hooks/use-prefers-reduced-motion` and no longer defines the hook inline (grep for a second `function usePrefersReducedMotion` returns nothing)
    - `npx vitest run src/components/__tests__/habitat-scene-video.test.ts` exits 0 (existing habitat-video behavior preserved after the extraction)
    - SUMMARY records DSY-01 baseline confirmed: cream `--background`, amber `--primary`, Baloo 2 + Figtree present in globals.css/layout.tsx
  </acceptance_criteria>
  <done>The reduced-motion hook is a shared module consumed by habitat-video (no regression), and the Daybreak token/font baseline is confirmed live app-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds presentation-only primitives + a media-query hook; no network, auth, or DB surface is introduced or modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-XSS | Tampering (XSS) | `Pill`/`Card`/`TField` children & props | mitigate | Render `children`/props through normal JSX only; no `dangerouslySetInnerHTML` anywhere in the primitives. TField passes user-typed values through React's escaped text rendering. |
| T-19-01-PRES | Information Disclosure | All primitives (client components) | accept | Presentation-only; no secrets, tokens, or env values are referenced. The reduced-motion hook reads only a CSS media query. No new attack surface. |
</threat_model>

<verification>
- `npx vitest run src/components/daybreak/__tests__/` — both primitive test files green (under a DOM env)
- `npx vitest run src/components/__tests__/habitat-scene-video.test.ts` — no regression from the hook extraction
- `npx tsc --noEmit` — exits 0; no type errors anywhere, including the daybreak primitives and the hook
- Token/font baseline confirmed in globals.css + layout.tsx (DSY-01)
</verification>

<success_criteria>
- TField (forwardRef, error/hint), TBtn (isPending spinner/disabled), Pill, Card exist in `src/components/daybreak/` (DSY-02)
- TField + TBtn unit-tested; tests pass (DSY-02, DSY-03 — validation/disabled behavior covered)
- `usePrefersReducedMotion()` extracted to `src/hooks/` and consumed by habitat-video with no regression
- Daybreak tokens + Baloo 2/Figtree confirmed applied app-wide (DSY-01)
</success_criteria>

<output>
Create `.planning/phases/19-daybreak-foundation-onboarding-auth/19-01-SUMMARY.md` when done. Record: the exact exported prop shapes of TField/TBtn/Pill/Card (downstream plans depend on them), how the daybreak tests get their DOM env (jsdom docblock vs config), whether `jsdom` had to be installed, the DSY-01 baseline-confirmation result, and any token/font gap found+fixed.
</output>
</content>
</invoke>
