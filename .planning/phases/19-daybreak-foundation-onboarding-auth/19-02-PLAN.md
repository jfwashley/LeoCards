---
phase: 19-daybreak-foundation-onboarding-auth
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
autonomous: true
requirements: [ONB-01, ONB-02, DSY-03]
must_haves:
  truths:
    - "Login renders inside AuthCard with the sunrise scene and composes TField/TBtn primitives (no inline shadcn Input/Button/Label, no local fieldClass helper)"
    - "Signup renders in the Daybreak AuthCard sunrise shell with Name/Email/Password fields only — no Native language field"
    - "Signup shows per-field validation after submit (red border + helper, not toasts), the 'An account with this email already exists.' error, and a submitting spinner"
    - "A successful signup routes the user to /welcome (not /dashboard)"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Daybreak Login composed from TField/TBtn primitives"
      contains: "TField"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Daybreak Signup, language field removed, redirects to /welcome"
      contains: "/welcome"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/daybreak/t-field.tsx"
      via: "import { TField }"
      pattern: "daybreak/t-field"
    - from: "src/app/(auth)/signup/page.tsx"
      to: "authClient.signUp.email"
      via: "name/email/password only"
      pattern: "signUp\\.email"
    - from: "src/app/(auth)/signup/page.tsx"
      to: "/welcome"
      via: "router.push after success"
      pattern: "router\\.push\\(\"/welcome\"\\)"
---

<objective>
Bring the Login and Signup screens fully onto the Daybreak design system. Refactor the existing Login spike to compose the new `TField`/`TBtn` primitives (D-02), and restyle Signup into the Daybreak `AuthCard` sunrise shell while removing the native-language field (D-04) and redirecting successful signup to `/welcome` (D-05). All better-auth logic, react-hook-form wiring, and error copy are preserved (D-08).

Purpose: ONB-01 (Login) and ONB-02 (Signup) — two of the four auth screens. Login proves the primitive composition pattern; Signup is the first screen to drop the language capture, which is the load-bearing D-04 flow change.
Output: Refactored `login/page.tsx` and `signup/page.tsx`, both Daybreak-faithful and primitive-based.

NOTE (cross-wave sequencing): This plan changes signup's success redirect to `/welcome`, but the `/welcome` route itself and the e2e harness that walks it are delivered in 19-04 (Wave 3). Between this plan landing and 19-04 landing, the `01-auth-signup-login.spec.ts` / `02-first-visit-deck-creation.spec.ts` flows that depend on the old picker will be red — that is expected and is repaired in 19-04, which owns those specs and the helpers. Do NOT edit `e2e/helpers.ts` or those spec files here. The per-task gate is `npx vitest run` (unit), which stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-CONTEXT.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md
@.planning/phases/19-daybreak-foundation-onboarding-auth/19-01-SUMMARY.md

<interfaces>
<!-- Primitives produced by 19-01 — import and compose, do not reimplement. -->
TField: forwardRef input. <TField label="Email" type="email" placeholder="you@example.com" error={isSubmitted ? errors.email?.message : undefined} {...register("email")} />
TBtn: <TBtn type="submit" isPending={isPending}>Create account</TBtn>

<!-- Auth shell already in the tree (19-01 left it unchanged). -->
AuthCard, DaybreakAuthScene (src/components/daybreak/auth-card.tsx):
  <AuthCard scene={<DaybreakAuthScene variant="sunrise" />}>{title + form}</AuthCard>
  variant: "sunrise" | "daylight" | "dusk" (login/signup → "sunrise", per D-07)

<!-- better-auth client (src/lib/auth-client.ts) — preserve calls exactly. -->
authClient.signIn.email({ email, password })
authClient.signUp.email({ email, password, name })   // NO nativeLanguage after D-04
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Login onto TField/TBtn primitives (D-02)</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <read_first>
    - src/app/(auth)/login/page.tsx — the full current file: the form logic (lines 32-64), the inline `fieldClass()` (lines 25-30), the Label/Input/error blocks (lines 72-125), the Button (lines 127-133), and the `LoginPage` shell with `AuthCard`/`Suspense`/Sign-up link (lines 138-164) — all of which must be preserved in behavior while the field/button markup is swapped
    - src/components/daybreak/t-field.tsx — TField prop shape (from 19-01)
    - src/components/daybreak/t-btn.tsx — TBtn prop shape (from 19-01)
    - .planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md — `login/page.tsx` Pattern Assignment ("What changes" list + "Form/Shell structure to preserve")
    - design/handoff-daybreak/hifi-shared.jsx — `HiFiLogin` (lines 92-125): the reference for field order, "Forgot password?" placement, and "Welcome back" title
    - node_modules/next/dist/docs — if touching `useSearchParams`/`Suspense` behavior, confirm the Next.js 16 client-hooks guidance first
  </read_first>
  <action>
    In `login/page.tsx`, replace the shadcn imports (`Button`, `Input`, `Label`) and the `Loader2` import with `import { TField } from "@/components/daybreak/t-field"` and `import { TBtn } from "@/components/daybreak/t-btn"`. Delete the `fieldClass()` helper. Replace the Email `Label`+`Input`+error block with a single `<TField label="Email" type="email" placeholder="you@example.com" error={isSubmitted ? errors.email?.message : undefined} {...register("email")} />`. Replace the Password block with `<TField label="Password" type="password" placeholder="••••••••" error={isSubmitted ? (errors.password?.message ?? authError ?? undefined) : undefined} {...register("password")} />`, keeping the "Forgot password?" `Link` to `/forgot-password` directly beneath it (self-end, `text-[var(--db-link)]`). Replace the submit `Button` with `<TBtn type="submit" isPending={isPending}>Sign in</TBtn>`. PRESERVE exactly: the `loginSchema`, `useForm` + `zodResolver`, `onSubmit` calling `authClient.signIn.email`, the `setAuthError("Incorrect email or password.")` path, the `callbackUrl` read, `router.push(callbackUrl ?? "/dashboard")`, the `LoginPage` outer fragment with `AuthCard scene={<DaybreakAuthScene variant="sunrise" />}`, the "Welcome back" `<h2 className="font-display ...">`, the `Suspense` wrapper around `LoginForm`, and the "Sign up" cross-link below the card. Keep `noValidate` on the form.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "login/page\.tsx" ; test $? -ne 0 && echo "OK" && grep -c "daybreak/t-field\|daybreak/t-btn" "src/app/(auth)/login/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/login/page.tsx` imports `TField` from `@/components/daybreak/t-field` and `TBtn` from `@/components/daybreak/t-btn`
    - The file no longer imports `Button`/`Input`/`Label` from `@/components/ui/*` and no longer defines a `fieldClass` helper (grep for `fieldClass` returns nothing)
    - `authClient.signIn.email(` is still present and `router.push(callbackUrl ?? "/dashboard")` is unchanged (login still goes to dashboard per D-05)
    - `DaybreakAuthScene variant="sunrise"` is present
    - `npx tsc --noEmit` reports no type errors in `login/page.tsx`
  </acceptance_criteria>
  <done>Login composes the Daybreak primitives, sunrise scene + ghost-peek shell preserved, all sign-in logic intact, login still routes to /dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Signup to Daybreak, remove language field, redirect to /welcome (D-04/D-05)</name>
  <files>src/app/(auth)/signup/page.tsx</files>
  <read_first>
    - src/app/(auth)/signup/page.tsx — the full current file: `signupSchema` with `nativeLanguage` (line 21), `defaultValues.nativeLanguage` (lines 37-39), the `authClient.signUp.email` call passing `nativeLanguage` (lines 46-51), `router.push("/dashboard")` (line 61), the native-language `<select>` block (lines 124-140), and the shadcn `Card`+`Label`+`Input`+`Button` markup to replace
    - src/app/(auth)/login/page.tsx — the POST-refactor Login (Task 1 of this plan) as the canonical Daybreak shell + primitive pattern to mirror
    - design/handoff-daybreak/daybreak-auth.jsx — `AuSignup` (lines 67-77): the Daybreak Signup spec — sunrise tint, "Your lion is waiting." tagline, "Create your account" title, Name/Email/Password(+"At least 8 characters" hint) fields, "Create account" button, and the default/errors/taken/submitting states
    - design/handoff-daybreak/README.md — §6 Onboarding & Auth (Signup states)
    - .planning/phases/19-daybreak-foundation-onboarding-auth/19-PATTERNS.md — `signup/page.tsx` Pattern Assignment ("What changes (D-04)" 8-item list + schema/duplicate-email patterns to keep)
    - .planning/phases/19-daybreak-foundation-onboarding-auth/19-RESEARCH.md — Pitfall 1 (signup must redirect to /welcome) and State of the Art (remove nativeLanguage from schema + signUp call)
  </read_first>
  <action>
    Edit `signup/page.tsx`: remove `nativeLanguage` from `signupSchema` (leaving `name`/`email`/`password` min-8) and remove the `defaultValues` object entirely (or the `nativeLanguage` key). Remove `nativeLanguage` from the `authClient.signUp.email({...})` arguments so only `email`, `password`, `name` are passed. Delete the entire native-language `<select>` field block. Change the success path from `router.refresh(); router.push("/dashboard")` to `router.push("/welcome")` (D-05). Replace the shadcn `Card` shell with the Login-style page structure: an outer `<>` fragment wrapping `<AuthCard scene={<DaybreakAuthScene variant="sunrise" />}>` (import both from `@/components/daybreak/auth-card`), a `<h2 className="font-display text-[22px] font-bold text-foreground">Create your account</h2>`, then the form. Replace each `Label`+`Input`+error block with `<TField .../>`: Name (`error={isSubmitted ? errors.name?.message : undefined}`), Email (`error={isSubmitted ? (errors.email?.message ?? (emailError ?? undefined)) : undefined}` — preserving the existing `emailError` "An account with this email already exists." behavior), Password (`type="password"`, pass `hint="At least 8 characters"` shown only when no error, `error={isSubmitted ? errors.password?.message : undefined}`). Replace the submit `Button` with `<TBtn type="submit" isPending={isPending}>Create account</TBtn>`. Keep the "Already have an account? Sign in" link beneath the card (Daybreak link styling). Preserve `noValidate`, the `isPending` start/stop in `onSubmit`, and the duplicate-email `setEmailError` path. Keep the field `<label>` text exactly "Name", "Email", "Password" so existing/updated e2e `getByLabel` selectors resolve.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "signup/page\.tsx" ; test $? -ne 0 && echo "TS-OK" ; grep -c "nativeLanguage" "src/app/(auth)/signup/page.tsx" ; grep -c "router.push(\"/welcome\")" "src/app/(auth)/signup/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/signup/page.tsx` contains zero occurrences of `nativeLanguage` (grep count = 0) — removed from schema, defaults, and the signUp call
    - The file no longer renders a `<select>` and no longer shows a "Native language" label
    - `authClient.signUp.email(` is called with only `email`, `password`, `name`
    - On success the file calls `router.push("/welcome")` (grep count ≥ 1) and does NOT push `/dashboard`
    - The file imports `TField`, `TBtn`, and `AuthCard`/`DaybreakAuthScene`; renders `DaybreakAuthScene variant="sunrise"` and the "Create your account" title
    - The `emailError` "already exists" path is preserved (grep: `already exists`)
    - `npx tsc --noEmit` reports no type errors in `signup/page.tsx`
  </acceptance_criteria>
  <done>Signup is Daybreak-styled with Name/Email/Password only, all states preserved, language capture removed, success routes to /welcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → better-auth API | Email/password/name cross from the untrusted client into `authClient.signIn`/`signUp` (better-auth Route Handler). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-AUTH | Spoofing / Auth | `authClient.signIn.email` / `signUp.email` | mitigate | Preserve better-auth flows verbatim — no change to credential handling, hashing, or session issuance (handled by better-auth + `nextCookies()`). This plan only swaps presentation. |
| T-19-02-ENUM | Information Disclosure | Signup duplicate-email error | accept | Existing copy "An account with this email already exists." is unchanged. Signup intentionally reveals existence (standard for registration); the privacy-safe path is the FORGOT flow (19-03), which is not weakened here. |
| T-19-02-VAL | Tampering (input validation) | Signup/Login zod schemas | mitigate | Keep client-side zod (email format, password min-8); better-auth re-validates server-side. Removing `nativeLanguage` from the schema removes a writable field from the signUp payload — narrowing, not widening, input surface. |
| T-19-02-SECRET | Information Disclosure | Client components | accept | No secrets referenced; client only calls public auth endpoints. No new attack surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` — no type errors in login/page.tsx or signup/page.tsx
- `grep -c nativeLanguage signup/page.tsx` returns 0
- `grep -c 'router.push("/welcome")' signup/page.tsx` returns ≥ 1
- Manual/visual (deferred to phase gate, --skip-ui): both screens render against the daybreak-auth.jsx / HiFiLogin reference and the `LeoCards Daybreak Onboarding & Auth.html` artboard
</verification>

<success_criteria>
- Login composed from TField/TBtn, sunrise AuthCard shell preserved, sign-in logic intact (ONB-01, DSY-03)
- Signup Daybreak-styled, Name/Email/Password only, all four states (default/validation/taken/submitting) present, routes to /welcome (ONB-02, DSY-03, D-04, D-05)
- No native-language capture remains in signup (D-04)
</success_criteria>

<output>
Create `.planning/phases/19-daybreak-foundation-onboarding-auth/19-02-SUMMARY.md` when done. Record: confirmation that signup no longer captures nativeLanguage and now routes to /welcome (so 19-04 can rely on it), and that login still routes to /dashboard.
</output>
