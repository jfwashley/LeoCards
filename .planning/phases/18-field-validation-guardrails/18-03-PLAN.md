---
phase: 18-field-validation-guardrails
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - .planning/phases/18-field-validation-guardrails/baseline/
  - .planning/phases/18-field-validation-guardrails/18-baseline-thresholds.json
  - .planning/phases/18-field-validation-guardrails/18-BASELINE-SUMMARY.md
autonomous: false
requirements: [PERF-06]

must_haves:
  truths:
    - "A fresh 4-route × mobile+desktop warm-prod baseline is measured against the deployed Phase 26/27 code and committed immutably (D-10: fresh official baseline; D-08: mobile is the binding gate basis)"
    - "The baseline numbers reflect the Phase 26/27 optimizations (not the stale pre-26/27 Phase 17 numbers)"
    - "A machine-readable threshold table (default absolute gates + per-route D-11 exception gates + baseline medians) exists for the orchestrator to consume"
  artifacts:
    - path: ".planning/phases/18-field-validation-guardrails/baseline/"
      provides: "Immutable fresh baseline: per-route reports + raw runs JSON (Phase 16 artifact discipline)"
    - path: ".planning/phases/18-field-validation-guardrails/18-baseline-thresholds.json"
      provides: "Threshold table the perf-recert orchestrator reads (default gates, D-11 exceptions, baseline medians for drift)"
      contains: "exceptions"
    - path: ".planning/phases/18-field-validation-guardrails/18-BASELINE-SUMMARY.md"
      provides: "Human-readable immutable baseline summary + D-11 exception rationale"
  key_links:
    - from: "scripts/measure-cwv.mjs"
      to: ".planning/phases/18-field-validation-guardrails/baseline/"
      via: "PHASE_OUT_DIR override"
      pattern: "PHASE_OUT_DIR"
---

<objective>
Run the fresh official Phase 18 baseline (D-10): one full 4-route × mobile+desktop warm-prod run against deployed prod, committed immutably under the Phase 16 artifact discipline. This locks in the Phase 26/27 optimized numbers as the regression baseline AND doubles as proof those optimizations survived deployment. Then mechanically derive the threshold table the re-cert gate uses — absolute gates plus per-route D-11 exception gates for any route still missing an absolute gate.

Purpose: Every future drift-warning comparison and every accepted-miss exception derives from this locked baseline. If prod were stale (Pitfall 1), the baseline would mislabel pre-26/27 numbers as post — so a deployment-freshness re-check gates the run.
Output: immutable `baseline/` artifacts + `18-BASELINE-SUMMARY.md` + `18-baseline-thresholds.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-field-validation-guardrails/18-CONTEXT.md
@.planning/phases/18-field-validation-guardrails/18-RESEARCH.md
@.planning/phases/18-field-validation-guardrails/18-PATTERNS.md
@.planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md
@.planning/phases/18-field-validation-guardrails/18-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deployment-freshness re-check before the baseline (Pitfall 1 / A2)</name>
  <files>(none — verification checkpoint, no files modified)</files>
  <action>PAUSE to re-confirm deployment freshness before the baseline (Pitfall 1). Do NOT run the baseline until the Vercel current-production SHA == git rev-parse origin/main. If stale, Josh redeploys main HEAD first. Follow the how-to-verify steps.</action>
  <what-built>Nothing new — this gate confirms the deployment freshness established in Plan 01 Task 3 still holds (no newer deploy has superseded it, and no stale build is live). The baseline is only trustworthy if it measures current main HEAD (Phase 26/27 + SpeedInsights).</what-built>
  <how-to-verify>
    1. In the Vercel Dashboard → LeoCards → Deployments, read the current Production deployment's commit SHA.
    2. Confirm it equals `git rev-parse origin/main`.
    3. Warning sign (Pitfall 1): if `/deck/new-card` in the baseline below still shows ~338ms TBT / the exact pre-26/27 Phase 17 numbers, prod may be stale — stop and re-deploy before trusting the baseline.
    4. If stale, Josh triggers a fresh prod deploy of main HEAD and waits for it to go live before Task 2.
  </how-to-verify>
  <read_first>
    - .planning/phases/18-field-validation-guardrails/18-RESEARCH.md (§Pitfall 1, §Open Questions 1, §Assumptions A2)
    - .planning/phases/18-field-validation-guardrails/18-01-SUMMARY.md (the confirmed live SHA from Plan 01)
  </read_first>
  <acceptance_criteria>
    - Current production deployment SHA == `git rev-parse origin/main` at the moment the baseline is about to run.
    - Josh confirms prod is current (Phase 26/27 code live); if it was stale, a re-deploy landed first.
  </acceptance_criteria>
  <resume-signal>Type "fresh — SHA matches, run the baseline" or "stale — redeployed, now matches".</resume-signal>
  <verify><human-check>A human completed the checkpoint per the how-to-verify steps and gave the resume signal.</human-check></verify>
  <done>Current production deployment SHA == main HEAD confirmed; the baseline may run against Phase 26/27 code.</done>
</task>

<task type="auto">
  <name>Task 2: Run the fresh baseline + derive the threshold table</name>
  <files>.planning/phases/18-field-validation-guardrails/baseline/, .planning/phases/18-field-validation-guardrails/18-baseline-thresholds.json</files>
  <read_first>
    - scripts/measure-cwv.mjs (header REQUIRED/OPTIONAL env: DATABASE_URL, ROUTE_FILTER, PHASE_OUT_DIR; the 4 routes; n=6 run-0-discard mobile+desktop loop; the 16-BASELINE-SUMMARY.md literal summary filename)
    - scripts/measure-cwv-lib.mjs (deriveExceptionGate from Plan 02 — the D-11 mechanical derivation)
    - .planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md (artifact format to replicate)
    - .planning/phases/17-performance-optimization/17-CONTEXT.md (D-04 accepted-miss rationale for the exception doc)
  </read_first>
  <action>
    Run the harness against deployed prod for all four key routes × both presets:
    `MSYS_NO_PATHCONV=1 DATABASE_URL="<prod url>" PHASE_OUT_DIR=".planning/phases/18-field-validation-guardrails/baseline" node scripts/measure-cwv.mjs`
    (no ROUTE_FILTER = all 4 routes; D-10 requires the full 2-preset run, unlike the re-cert default which is mobile-only per D-08). This writes per-route `<slug>-baseline.md` + `<slug>-mobile-runs.json` + `<slug>-desktop-runs.json` and the inherited `16-BASELINE-SUMMARY.md` cross-route summary into the baseline dir (the literal summary filename is not phase-derived — accept it living in the Phase 18 baseline dir, as Phase 17 already did; the Phase-18 human-readable summary is written separately in Task 3).

    Then write `18-baseline-thresholds.json`: the machine-readable table perf-recert.mjs (Plan 04) consumes. Structure it as `{ driftPct: 15, routes: { "/dashboard": { medians: {...}, gates: {...}, exceptions: {...} }, ... } }` where per route: `medians` = the mobile baseline medians (lcp/tbt/cls/score) for drift comparison; `gates` = the default absolute gates (LCP≤2500, TBT≤200, CLS≤0.1, Perf≥90); and `exceptions` holds a D-11 exception gate ONLY for a metric whose mobile median MISSES its absolute gate at this fresh baseline (candidate `/deck/new-card` TBT), computed via `deriveExceptionGate(freshMedian, 15)` (e.g. median 320 → 368). Routes/metrics that pass get no exception entry.
  </action>
  <acceptance_criteria>
    - `.planning/phases/18-field-validation-guardrails/baseline/` contains 4× `*-baseline.md`, 4× `*-mobile-runs.json`, 4× `*-desktop-runs.json`, and the cross-route summary.
    - Baseline `/deck/new-card` numbers differ from the exact pre-26/27 Phase 17 stale values (freshness sanity check, Pitfall 1) — or Task 1 confirmed & documented why not.
    - `18-baseline-thresholds.json` parses as valid JSON, has `driftPct` (number) and a `routes` object with an entry per key route holding `medians` + `gates`.
    - Every route that misses an absolute gate has a corresponding `exceptions` entry equal to `deriveExceptionGate(freshMedian, 15)`; routes that pass have none.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/jfwas/Projects/LeoCards && node -e "const t=require('./.planning/phases/18-field-validation-guardrails/18-baseline-thresholds.json'); if(typeof t.driftPct!=='number'||!t.routes) process.exit(1); console.log('routes:',Object.keys(t.routes).length)"</automated>
  </verify>
  <done>Immutable fresh baseline artifacts written; threshold table with default gates + D-11 exceptions + baseline medians committed for the orchestrator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the baseline reflects 26/27 + commit immutably</name>
  <files>.planning/phases/18-field-validation-guardrails/18-BASELINE-SUMMARY.md</files>
  <action>Write 18-BASELINE-SUMMARY.md (mirroring 16-BASELINE-SUMMARY.md plus a D-11 exceptions section), then PAUSE for Josh to sanity-check the numbers reflect Phase 26/27 (not stale Phase 17 values) before the baseline is committed immutable. Follow the how-to-verify steps.</action>
  <what-built>The fresh baseline artifacts and threshold table are written. Because every future drift comparison and exception gate derives from these numbers (and a stale-prod baseline would silently corrupt all of them — Pitfall 1), Josh sanity-checks the numbers reflect the Phase 26/27 optimizations before they are committed immutable (the Phase 16 human-verify-then-commit discipline).</what-built>
  <how-to-verify>
    1. Review `18-BASELINE-SUMMARY.md` (write it in this step, mirroring `16-BASELINE-SUMMARY.md`: per-route mobile/desktop Perf, bundle KB, top class, PLUS a D-11 exceptions section recording any accepted-miss route, its fresh median, its derived exception gate, and the Phase-17-D-04 rationale).
    2. Confirm the numbers look like post-26/27 optimized values (e.g. batched writes / consolidated queries / zod-mini bundle diet visible as improvement or non-regression vs the Phase 16 `16-BASELINE-SUMMARY.md`), not stale Phase 17 values.
    3. Approve — then the baseline dir + `18-BASELINE-SUMMARY.md` + `18-baseline-thresholds.json` are committed as immutable, never-re-edited artifacts.
  </how-to-verify>
  <read_first>
    - .planning/phases/16-performance-baseline-measure/baseline/16-BASELINE-SUMMARY.md (summary format to replicate)
    - .planning/phases/18-field-validation-guardrails/baseline/ (the just-written per-route reports)
  </read_first>
  <acceptance_criteria>
    - `18-BASELINE-SUMMARY.md` exists with the cross-route table AND a D-11 exceptions section (or an explicit "no exceptions — all routes pass absolute gates" note).
    - Josh confirms the baseline reflects Phase 26/27 code.
    - Baseline artifacts committed; treated as immutable from here (no later edits).
  </acceptance_criteria>
  <resume-signal>Type "verified — reflects 26/27, commit immutable" or describe the anomaly.</resume-signal>
  <verify><human-check>A human completed the checkpoint per the how-to-verify steps and gave the resume signal.</human-check></verify>
  <done>18-BASELINE-SUMMARY.md written; Josh confirms numbers reflect 26/27; baseline committed immutable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deployed prod → baseline harness | Baseline trust depends on prod running the intended (current) code |
| DATABASE_URL → measure-cwv child | Prod DB credential provisions the `*test.local` measurement user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Spoofing (of the baseline's provenance) | Stale prod deploy measured as if it were 26/27 code | mitigate | Task 1 blocking deployment-freshness gate (SHA == main HEAD) + Task 3 human sanity-check of numbers vs Phase 16 before immutable commit |
| T-18-02b | Information Disclosure | DATABASE_URL passed to the measure-cwv child; session tokens during provisioning | mitigate | Pass DATABASE_URL via env (never argv/logs); measure-cwv.mjs already logs only emails, never tokens/passwords — do not add logging of secrets in this run |
| T-18-06 | Repudiation | Baseline later silently edited, breaking drift comparisons | mitigate | Committed immutable per Phase 16 discipline; never re-edited (D-10) |
</threat_model>

<verification>
- Freshness gate passed (SHA == main HEAD) before the run.
- Baseline artifacts + `18-BASELINE-SUMMARY.md` + `18-baseline-thresholds.json` present and internally consistent; threshold JSON parses.
- Numbers reflect Phase 26/27, human-verified, committed immutable.
</verification>

<success_criteria>
The locked Phase 18 baseline exists and certifies the deployed Phase 26/27 code, and the orchestrator has a machine-readable threshold table (absolute gates + auto-derived D-11 exceptions + baseline medians for drift) to consume.
</success_criteria>

<output>
Create `.planning/phases/18-field-validation-guardrails/18-03-SUMMARY.md` when done.
</output>
