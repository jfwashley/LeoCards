---
phase: 18-field-validation-guardrails
plan: 05
type: execute
wave: 4
depends_on: ["18-04"]
files_modified:
  - AGENTS.md
  - .planning/phases/18-field-validation-guardrails/measurements/
autonomous: false
requirements: [PERF-06]

must_haves:
  truths:
    - "A real end-to-end green run of the re-cert gate passes against current prod + local prod build, with its report committed as evidence"
    - "A real end-to-end red run with an impossible threshold override prints a red FAIL table and exits non-zero, with its report committed as evidence (D-13-2)"
    - "The D-15 cadence convention is documented in AGENTS.md so future sessions know when to run the gate"
  artifacts:
    - path: ".planning/phases/18-field-validation-guardrails/measurements/"
      provides: "Committed green + red re-cert reports as success-criterion-3 evidence"
    - path: "AGENTS.md"
      provides: "Documented perf-recert cadence convention (D-15)"
      contains: "perf:recert"
  key_links:
    - from: "scripts/perf-recert.mjs"
      to: ".planning/phases/18-field-validation-guardrails/measurements/"
      via: "dated report writes on both green and red runs"
      pattern: "recert-"
---

<objective>
Prove the re-cert gate works both ways, and document when to use it. Run the full pipeline green (all gates pass against current prod + local prod build) and red (impossible threshold override → red table + non-zero exit, D-13-2) — no sabotage deploy, just an env override — and commit both dated reports as the success-criterion-3 evidence trail. Then document the D-15 cadence convention in AGENTS.md.

Purpose: "Demonstrated, not assumed" (ROADMAP success criterion 3) — the gate must be shown to fail loudly on a real regression signal and pass on healthy code, and future sessions must know the run-after-perf-relevant-deploys rule.
Output: committed green + red reports; AGENTS.md cadence note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-field-validation-guardrails/18-CONTEXT.md
@.planning/phases/18-field-validation-guardrails/18-RESEARCH.md
@.planning/phases/18-field-validation-guardrails/18-04-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green run — full re-cert passes end-to-end, commit evidence</name>
  <files>.planning/phases/18-field-validation-guardrails/measurements/</files>
  <read_first>
    - scripts/perf-recert.mjs (the orchestrator + its env/USAGE header)
    - .planning/phases/18-field-validation-guardrails/18-04-SUMMARY.md (how the command is invoked; report location)
    - .planning/phases/18-field-validation-guardrails/18-baseline-thresholds.json (the gates the run is evaluated against)
  </read_first>
  <action>
    Run the full re-cert command with real thresholds against current prod + a fresh local prod build:
    `MSYS_NO_PATHCONV=1 DATABASE_URL="<prod url>" node scripts/perf-recert.mjs`
    (mobile default per D-08; ~35-40 min including the prod build + nav gate). Confirm the console prints a per-route PASS table (D-11 exception routes green, drift warnings surfaced without failing), the nav gate passes at 850ms, and the process exits 0. Commit the dated `measurements/recert-<date>.md` + `.json` report as green evidence. If a real hard-fail surfaces (a genuine regression vs the fresh baseline), STOP and report it as a finding — per the phase boundary, this phase documents/triages regressions, it does not optimize.
  </action>
  <acceptance_criteria>
    - The command exits 0 (`echo $?` is 0) on the green run.
    - A dated `measurements/recert-*.md` report exists showing a per-route PASS table + the nav-gate PASS row.
    - Drift warnings (if any) are shown as WARNING, not FAIL (D-09 behavior confirmed on real data).
    - The green report is committed.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/jfwas/Projects/LeoCards && ls .planning/phases/18-field-validation-guardrails/measurements/recert-*.md >/dev/null 2>&1</automated>
    <human-check>Green run exited 0 and the committed report shows all routes PASS + nav-gate PASS.</human-check>
  </verify>
  <done>A real green re-cert run passed end-to-end and its report is committed as evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Red-path demo (D-13-2) + D-15 cadence doc</name>
  <files>.planning/phases/18-field-validation-guardrails/measurements/, AGENTS.md</files>
  <read_first>
    - scripts/perf-recert.mjs (the GATE_* override path + its D-15 header comment)
    - AGENTS.md (current conventions structure — add a perf-recert cadence note in the same style)
    - .planning/phases/18-field-validation-guardrails/18-RESEARCH.md (§Validation Architecture PERF-06 D-13-2)
  </read_first>
  <action>
    Run the D-13-2 red-path demo against real prod with a deliberately impossible threshold override (no sabotage deploy):
    `MSYS_NO_PATHCONV=1 GATE_TBT=10 DATABASE_URL="<prod url>" node scripts/perf-recert.mjs`
    Confirm the pipeline prints a red FAIL table (the impossible TBT gate trips on every route) and exits non-zero, and that the FAILED run STILL writes its dated report marked FAILED (D-07). Commit that red report as the success-criterion-3 evidence.

    Then add a short "Performance re-cert gate" subsection to AGENTS.md documenting the D-15 cadence: run `npm run perf:recert` after deploys touching perf-relevant surfaces (bundle deps, shared layout/providers, route pages, `next.config.ts`) and before any release/milestone; it is local on-demand only (D-14 — no CI this phase); red → revert or fix forward. Cross-reference the script header.
  </action>
  <acceptance_criteria>
    - The red run exits non-zero (`echo $?` is non-zero) and prints a red FAIL table naming the tripped TBT gate.
    - A dated red report exists in `measurements/` marked FAILED (the red record is part of the evidence trail, D-07).
    - `grep -i "perf:recert" AGENTS.md` matches and the note states the run-after-perf-relevant-deploys cadence + local-on-demand-only (D-14/D-15).
    - Both green (Task 1) and red reports are committed.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/jfwas/Projects/LeoCards && grep -qi "perf:recert" AGENTS.md && ls .planning/phases/18-field-validation-guardrails/measurements/recert-*.md >/dev/null 2>&1</automated>
    <human-check>Red run exited non-zero, printed the red FAIL table, and wrote a report marked FAILED.</human-check>
  </verify>
  <done>The gate is demonstrated red (non-zero exit on impossible threshold) and green, both reports committed, and the D-15 cadence is documented in AGENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed report artifacts | Reports written to `.planning/` must not embed secrets (DB URL, tokens) |
| DATABASE_URL at invocation | Real prod credential used for the demo runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02d | Information Disclosure | committed green/red re-cert reports | mitigate | Before committing, confirm the report contains no `DATABASE_URL`, session token, or password (only routes/metrics/exit codes) — the orchestrator's "never log secrets" discipline extends to what it writes to disk |
| T-18-09 | Repudiation | red-path evidence | mitigate | The FAILED run's dated report IS the committed evidence for success criterion 3 (D-07) — provenance preserved, never re-edited |
</threat_model>

<verification>
- Green run exits 0 with an all-PASS committed report; red run exits non-zero with a committed FAILED report.
- AGENTS.md documents the D-15 cadence + D-14 local-only rule.
- No secrets embedded in committed reports.
</verification>

<success_criteria>
The re-cert gate is demonstrated to fail loudly on a real regression signal (impossible threshold → red table + non-zero exit) and pass on healthy code, both reports committed as evidence, and the cadence convention is documented for future sessions.
</success_criteria>

<output>
Create `.planning/phases/18-field-validation-guardrails/18-05-SUMMARY.md` when done.
</output>
