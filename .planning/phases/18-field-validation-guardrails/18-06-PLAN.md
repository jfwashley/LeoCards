---
phase: 18-field-validation-guardrails
plan: 06
type: execute
wave: 5
depends_on: ["18-01", "18-05"]
files_modified:
  - .planning/phases/18-field-validation-guardrails/18-FIELD-COMPARISON.md
autonomous: false
requirements: [PERF-05]

must_haves:
  truths:
    - "After the 14-day window, field p75 (LCP/INP/CLS) per key route is read off the Vercel Speed Insights dashboard"
    - "Each route's field p75 is compared against the CWV Good band, with anything outside Good getting a documented explanation"
    - "Thin or absent data closes the requirement via a documented variance explanation — the phase never blocks on traffic"
  artifacts:
    - path: ".planning/phases/18-field-validation-guardrails/18-FIELD-COMPARISON.md"
      provides: "The PERF-05 field-vs-Good-band comparison doc that closes the phase"
      contains: "INP"
  key_links:
    - from: ".planning/phases/18-field-validation-guardrails/18-FIELD-COMPARISON.md"
      to: "Vercel Speed Insights dashboard p75"
      via: "per-route field data read after the D-03 window"
      pattern: "p75"
---

<objective>
Close PERF-05: after the 14-day D-03 window (started in Plan 01), read the Vercel Speed Insights per-route p75 field data and write the comparison doc — field p75 vs the CWV Good band (LCP≤2500ms, INP≤200ms, CLS≤0.1), with INP as the field stand-in for the lab TBT gate (D-04). This is the phase finisher and the D-03 checkpoint/wait plan: Josh returns after the window to trigger it.

Purpose: Confirm the lab wins hold up with real users, or document the variance honestly. Per D-02 the requirement closes either way — thin/no data becomes the documented variance explanation; the phase never blocks waiting for traffic.
Output: `18-FIELD-COMPARISON.md`. With Plan 05's gate demonstrated, this closes Phase 18 and v3.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-field-validation-guardrails/18-CONTEXT.md
@.planning/phases/18-field-validation-guardrails/18-RESEARCH.md
@.planning/phases/18-field-validation-guardrails/18-BASELINE-SUMMARY.md
@.planning/phases/18-field-validation-guardrails/18-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Wait out the 14-day window, then read per-route field p75</name>
  <files>(none — wait/coordination checkpoint)</files>
  <action>PAUSE for the D-03 14-day window to elapse (checkpoint/wait — Josh returns to trigger this plan). Then Josh reads per-route p75 (LCP/INP/CLS) for the four key routes off the Vercel Speed Insights dashboard, noting the view used (Route vs Path) and event-count caveats. Follow the how-to-verify steps.</action>
  <what-built>Nothing to build — this is the D-03 checkpoint/wait. Speed Insights has been collecting field data since the Plan 01 window start. Josh returns after 14 days to read the dashboard; only Josh has Vercel dashboard access.</what-built>
  <how-to-verify>
    1. Confirm ≥14 days have elapsed since the D-03 window start recorded in 18-01-SUMMARY.md.
    2. Open Vercel Dashboard → LeoCards → Speed Insights.
    3. For each of the four key routes (`/dashboard`, `/study`, `/deck/new-card`, `/deck/browse`), read the p75 for LCP, INP, and CLS. Use the "Route" grouping first; if `/study` traffic looks mis-attributed due to its `?deck=` query-string redirect behavior (Open Question 2), switch to the "Path" view to isolate it and note which view was used.
    4. Note the total event count and any Hobby-tier cap / by-day thinning (Pitfall 4), plus whether harness runs during the window inflated any day's events (so field data is not mis-attributed to real traffic).
    5. Record the per-route p75 numbers (or "insufficient data" per route) for Task 2.
  </how-to-verify>
  <read_first>
    - .planning/phases/18-field-validation-guardrails/18-01-SUMMARY.md (the D-03 window start date)
    - .planning/phases/18-field-validation-guardrails/18-RESEARCH.md (§Open Questions 2, §Pitfall 4, §Common Pitfalls — Route vs Path, Hobby caps)
  </read_first>
  <acceptance_criteria>
    - ≥14 days elapsed since the recorded window start.
    - Per-route p75 for LCP/INP/CLS captured for each of the 4 key routes (or explicitly marked "insufficient data" per route), with the dashboard view used (Route vs Path) noted.
  </acceptance_criteria>
  <resume-signal>Paste the per-route p75 numbers (LCP/INP/CLS × 4 routes) and event-count/caveat notes, or "insufficient data — <details>".</resume-signal>
  <verify><human-check>A human completed the checkpoint per the how-to-verify steps and gave the resume signal.</human-check></verify>
  <done>14 days elapsed; per-route field p75 captured (or explicitly marked insufficient data) for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write the PERF-05 field-vs-Good-band comparison doc</name>
  <files>.planning/phases/18-field-validation-guardrails/18-FIELD-COMPARISON.md</files>
  <read_first>
    - .planning/phases/18-field-validation-guardrails/18-BASELINE-SUMMARY.md (the lab baseline to contextualise field data against)
    - .planning/phases/18-field-validation-guardrails/18-CONTEXT.md (D-02, D-04 — acceptance standard + INP-maps-to-TBT)
    - .planning/phases/18-field-validation-guardrails/18-RESEARCH.md (§Specifics — field INP maps to lab TBT; §Pitfall 4 event-cap caveat)
  </read_first>
  <action>
    Write `18-FIELD-COMPARISON.md`: a per-route table of field p75 (LCP/INP/CLS from Task 1) against the CWV Good band (LCP≤2500ms, INP≤200ms, CLS≤0.1), marking each cell IN-GOOD or OUTSIDE-GOOD. Per D-04, compare against the Good band ONLY — do NOT play p75-vs-lab-median tolerance games. Explicitly note that field INP is the stand-in for the lab TBT gate (there is no field TBT — do not hunt for it). For any route/metric OUTSIDE the Good band, write a documented explanation (e.g. known accepted-miss route, low sample size, device mix). Record the dashboard view used (Route vs Path) and the `/study` query-string caveat. Note the event count and any Hobby-tier thinning / harness-inflation caveat (Pitfall 4). If data was thin or absent for any route, that IS the documented variance explanation per D-02 — the requirement closes either way; state so explicitly. Close by stating whether the lab wins were confirmed in the field or the variance is documented, satisfying PERF-05.
  </action>
  <acceptance_criteria>
    - `18-FIELD-COMPARISON.md` exists with a per-route field-p75-vs-Good-band table for all 4 key routes (values or explicit "insufficient data").
    - The doc states INP is the field stand-in for lab TBT and does NOT invent a p75-vs-lab-median tolerance (D-04).
    - Every OUTSIDE-GOOD or insufficient-data cell has a written explanation (D-02).
    - The doc records the dashboard view used and the event-count/Hobby-cap caveat.
    - A closing statement explicitly satisfies PERF-05 (confirmed-in-field OR variance-documented).
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/jfwas/Projects/LeoCards && grep -qi "INP" .planning/phases/18-field-validation-guardrails/18-FIELD-COMPARISON.md && grep -qi "PERF-05" .planning/phases/18-field-validation-guardrails/18-FIELD-COMPARISON.md</automated>
  </verify>
  <done>The PERF-05 comparison doc is written and closes the requirement (field-confirmed or variance-documented); Phase 18 and v3.0 can close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel dashboard (external SaaS) → comparison doc | Field data is read manually from a third-party dashboard; no API on Hobby tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Information Disclosure | field data (route paths, timing) in the comparison doc | accept | Route paths + aggregate p75 timing only; no PII, no secrets — safe to commit |
| T-18-11 | Repudiation / misattribution | harness-generated events counted as real-user field data (Pitfall 4 / A3) | mitigate | Doc explicitly notes event count + any harness-run inflation + Hobby-cap thinning, so field data is not misattributed to real traffic |
</threat_model>

<verification>
- 14-day window elapsed; per-route field p75 captured.
- Comparison doc written against the Good band with INP↔TBT mapping, explanations for any misses, and the event-cap caveat.
- PERF-05 closed (confirmed or variance-documented).
</verification>

<success_criteria>
Field p75 data confirms the lab wins on the key routes, or the variance is documented with an explanation (D-02/D-04) — PERF-05 satisfied and Phase 18 (the v3.0 finisher) closed.
</success_criteria>

<output>
Create `.planning/phases/18-field-validation-guardrails/18-06-SUMMARY.md` when done.
</output>
