---
phase: 18-field-validation-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/app/layout.tsx
autonomous: false
requirements: [PERF-05]
user_setup:
  - service: vercel-speed-insights
    why: "Real-user field CWV collection (PERF-05, D-01) — Josh owns the dashboard toggle + production deploy"
    dashboard_config:
      - task: "Enable Speed Insights for the LeoCards project"
        location: "Vercel Dashboard → LeoCards → Speed Insights tab → Enable"
      - task: "Confirm production deploy is live and current (main HEAD, includes Phase 26/27 code)"
        location: "Vercel Dashboard → LeoCards → Deployments → Current Production deployment"

must_haves:
  truths:
    - "The root layout renders <SpeedInsights /> on every route (D-01: Vercel Speed Insights is the field-data source)"
    - "@vercel/speed-insights is installed and legitimacy-verified before install"
    - "Production is deployed with current main HEAD (Phase 26/27 code + SpeedInsights) and Speed Insights is enabled in the Vercel dashboard"
    - "The 14-day field-data window (D-02: wired early, time-boxed not traffic-gated; D-03) has an explicit recorded start date"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root-layout Speed Insights mount point"
      contains: "@vercel/speed-insights/next"
    - path: "package.json"
      provides: "@vercel/speed-insights dependency"
      contains: "@vercel/speed-insights"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@vercel/speed-insights/next"
      via: "SpeedInsights component import + render inside <body>"
      pattern: "SpeedInsights"
---

<objective>
Wire Vercel Speed Insights into the app and get it deployed + enabled so the 14-day field-data window (D-03) starts as early as possible. This is the front-loaded PERF-05 setup: field data cannot accrue until the component ships AND Josh flips the dashboard toggle AND a deploy lands after that toggle.

Purpose: Start the real-user (field) CWV collection clock immediately; the PERF-06 re-cert gate (Plans 02–05) is built during the window, and the comparison doc (Plan 06) closes the phase when the window ends.
Output: `@vercel/speed-insights` installed, `<SpeedInsights />` in the root layout, production deployed + Speed Insights enabled, window start date recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-field-validation-guardrails/18-CONTEXT.md
@.planning/phases/18-field-validation-guardrails/18-RESEARCH.md
@.planning/phases/18-field-validation-guardrails/18-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — @vercel/speed-insights (T-18-SC)</name>
  <files>(none — verification checkpoint, no files modified)</files>
  <action>PAUSE for human package-legitimacy verification (T-18-SC). Do NOT run npm install until Josh verifies @vercel/speed-insights on npmjs.com (Vercel first-party, 2.x, source repo present, no postinstall) per the how-to-verify steps. This gate is blocking-human and never auto-approvable.</action>
  <what-built>Nothing yet — this gate runs BEFORE any npm install. `@vercel/speed-insights` was tagged [ASSUMED] in 18-RESEARCH.md because slopcheck could not run in this environment (no Python interpreter). Per the Package Legitimacy Gate, a human must verify the package before it is installed. This checkpoint is NOT auto-approvable regardless of workflow.auto_advance.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@vercel/speed-insights in Chrome.
    2. Confirm: publisher is Vercel (first-party), current version is 2.x (research saw 2.0.0 published 2026-07-03), the linked source repo is github.com/vercel/speed-insights, and there is no suspicious `postinstall` script.
    3. Confirm the weekly download count is consistent with a widely-used Vercel first-party package (not a days-old low-download slopsquat).
  </how-to-verify>
  <read_first>
    - .planning/phases/18-field-validation-guardrails/18-RESEARCH.md (§Package Legitimacy Audit, §Assumptions Log A1)
  </read_first>
  <acceptance_criteria>
    - Human explicitly approves the package as legitimate (Vercel first-party, 2.x, source repo present, no postinstall) before Task 2 installs it.
    - If the human reports anything suspicious (wrong publisher, unexpected postinstall, slopsquat name), STOP — do not install; return to planning.
  </acceptance_criteria>
  <resume-signal>Type "approved — legit" to proceed to install, or describe the concern.</resume-signal>
  <verify><human-check>A human completed the checkpoint per the how-to-verify steps and gave the resume signal.</human-check></verify>
  <done>Human has approved the package as legitimate; Task 2 may install it.</done>
</task>

<task type="auto">
  <name>Task 2: Install package + mount SpeedInsights in the root layout</name>
  <files>package.json, package-lock.json, src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current full contents — the exact integration point)
    - .planning/phases/18-field-validation-guardrails/18-PATTERNS.md (§src/app/layout.tsx — the additive-only diff pattern)
    - .planning/phases/18-field-validation-guardrails/18-RESEARCH.md (§Pattern 3, §Pitfall 2 — the /next entry point specifically)
    - AGENTS.md (Next.js-is-different note; verify against node_modules/next/dist/docs if any App-Router doubt)
  </read_first>
  <action>
    Run `npm install @vercel/speed-insights` (latest 2.x, no version pin per D-01 / RESEARCH State-of-the-Art). Then edit `src/app/layout.tsx`: add the import `import { SpeedInsights } from "@vercel/speed-insights/next";` (the `/next` framework entry point specifically — NOT the bare `@vercel/speed-insights` or `/react` entry, per Pitfall 2), and render `<SpeedInsights />` as a sibling AFTER `{children}` inside the existing `<body>`. This is additive only — do not change the existing `<html>`/`<body>` className, fonts, or `{children}` placement. No props, no client directive (it is a Server Component wrapper).
  </action>
  <acceptance_criteria>
    - `npm ls @vercel/speed-insights` resolves a 2.x version.
    - `grep "@vercel/speed-insights/next" src/app/layout.tsx` matches (correct entry point).
    - `grep "<SpeedInsights />" src/app/layout.tsx` matches and appears inside `<body>` after `{children}`.
    - `npx tsc --noEmit` is clean.
    - No other line of `src/app/layout.tsx` changed (html/body className, font vars, children unchanged).
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/jfwas/Projects/LeoCards && npx tsc --noEmit && grep -q "@vercel/speed-insights/next" src/app/layout.tsx && grep -q "SpeedInsights" src/app/layout.tsx</automated>
  </verify>
  <done>Package installed, `<SpeedInsights />` rendered once in the root layout via the `/next` entry point, tsc clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Enable Speed Insights, deploy current main to prod, start the D-03 window</name>
  <files>(none — coordination checkpoint; deploy is Josh-controlled)</files>
  <action>PAUSE for Josh to enable Speed Insights in the Vercel dashboard, deploy current main HEAD to prod (resolving deployment freshness — prod then runs Phase 26/27 + SpeedInsights), confirm the live SHA == main HEAD and the /_vercel/speed-insights beacon fires, and record the 14-day D-03 window start. Follow the how-to-verify steps exactly.</action>
  <what-built>The SpeedInsights component and dependency are committed. Speed Insights requires a two-step activation (Pitfall 3): the dashboard toggle AND a deploy AFTER the toggle. This step also resolves the deployment-freshness concern (Pitfall 1 / A2): pushing current `main` to prod deploys main HEAD, which includes all Phase 26/27 code — so the same deploy that starts the field window also makes prod current for the Plan 03 baseline.</what-built>
  <how-to-verify>
    1. In the Vercel Dashboard → LeoCards → Speed Insights tab, click Enable. Confirm the indicator is NOT the gray "not enabled" circle (Pitfall 3 warning sign).
    2. Push/merge this plan's commit to `main` (Josh controls deploys per the Phase 17 "Deploy HELD" note). Wait for the Vercel production deployment to finish.
    3. Confirm the current production deployment's commit SHA equals `git rev-parse origin/main` — i.e. prod now runs main HEAD (Phase 26/27 + SpeedInsights), not the stale 2026-07-15 / held Phase 17 build (Pitfall 1).
    4. Load leocards.vercel.app in Chrome, open DevTools → Network, and confirm a `/_vercel/speed-insights/*` request fires (Pitfall 2 warning sign is its absence).
    5. Record today's date as the D-03 14-day window start in the plan SUMMARY.
  </how-to-verify>
  <read_first>
    - .planning/phases/18-field-validation-guardrails/18-RESEARCH.md (§Pitfall 1, §Pitfall 3, §Open Questions 1)
    - .planning/STATE.md (§Blockers/Concerns — the Phase 17 "Deploy HELD" note)
  </read_first>
  <acceptance_criteria>
    - Speed Insights shows an enabled (non-gray) state in the Vercel dashboard.
    - Current production deployment SHA == `git rev-parse origin/main` (deployment-freshness confirmed; prod runs Phase 26/27 code).
    - A `/_vercel/speed-insights/*` network request is observed on a live prod page load.
    - The D-03 window start date is recorded in the SUMMARY (drives when Plan 06 can run).
  </acceptance_criteria>
  <resume-signal>Type "deployed — enabled, SHA matches, beacon firing, window started <YYYY-MM-DD>" or describe what failed.</resume-signal>
  <verify><human-check>A human completed the checkpoint per the how-to-verify steps and gave the resume signal.</human-check></verify>
  <done>Speed Insights enabled, prod deployed with current main HEAD, beacon firing, D-03 window start recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party package pulled into the production bundle |
| Vercel edge → user browser | First-party RUM script runs in every visitor's browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-SC | Tampering (supply chain) | `npm install @vercel/speed-insights` | mitigate | Task 1 blocking-human legitimacy gate (npmjs.com verify) before install; never auto-approvable ([ASSUMED] per 18-RESEARCH slopcheck-unavailable) |
| T-18-01 | Tampering / Information Disclosure | `<SpeedInsights />` third-party RUM script in every visitor's browser | accept | First-party Vercel tooling, no `postinstall` (verified `npm view scripts.postinstall` empty), same platform the app already trusts for prod infra; residual risk = standard npm supply-chain trust, gated by T-18-SC |
| T-18-02 | Information Disclosure | Speed Insights field data (paths, timing) sent to Vercel | accept | No PII in route paths/timing; data is p75 aggregate CWV only; Hobby-tier retention owned by Vercel per D-01 |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; SpeedInsights imported from `/next` and rendered once in `src/app/layout.tsx`.
- Speed Insights enabled + prod deployed with current main HEAD; `/_vercel/speed-insights/*` beacon observed.
- D-03 window start date recorded.
</verification>

<success_criteria>
Field-data collection is live: package legitimacy-verified + installed, `<SpeedInsights />` in the root layout, production deployed with Phase 26/27 code, dashboard toggle on, beacon firing, and the 14-day D-03 window has a recorded start.
</success_criteria>

<output>
Create `.planning/phases/18-field-validation-guardrails/18-01-SUMMARY.md` when done. Record the D-03 window start date prominently (Plan 06 depends on it).
</output>
