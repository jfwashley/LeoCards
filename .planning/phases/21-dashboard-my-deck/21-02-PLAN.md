---
phase: 21-dashboard-my-deck
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/components/app-header.tsx
  - src/components/deck-switcher.tsx
  - src/components/logout-button.tsx
  - src/components/__tests__/deck-switcher.test.tsx
  - e2e/08-deck-switching.spec.ts
  - e2e/10-mobile-responsive.spec.ts
  - e2e/01-auth-signup-login.spec.ts
autonomous: true
requirements: [DSH-01]
nyquist_compliant: true

must_haves:
  truths:
    - "The persistent header shows a LionFace + 'LeoCards' wordmark (no tiger emoji) and a Daybreak logout glyph button — D-01, L-01"
    - "The deck picker is a dropdown popover anchored to a compact LangChip pill (text 'ES'/'FR'/'EN', no flag emoji); tapping it lists decks by full language name with an active marker plus a '+ New deck' row that expands inline to per-language LangChip buttons with per-language creating spinner + error — D-01"
    - "The existing createDeck action and create/error/creating state from deck-switcher are reused unchanged — only the surface (Select → Popover) and emoji → LangChip change (D-01, L-02)"
    - "e2e specs that targeted the old Select (data-slot='select-trigger', role='option') and the 'Sign out' text button are retargeted to data-testid / role+accessible-name, preserving each assertion's intent — L-06"
  artifacts:
    - path: "src/components/deck-switcher.tsx"
      provides: "Popover-based deck picker with inline create flow + LangChip"
      contains: "deck-picker-trigger"
    - path: "src/components/app-header.tsx"
      provides: "Daybreak TopBar — LionFace + wordmark + DeckSwitcher + logout glyph"
      contains: "LionFace"
    - path: "src/components/logout-button.tsx"
      provides: "Icon-only logout button with aria-label='Sign out'"
      contains: "Sign out"
    - path: "src/components/__tests__/deck-switcher.test.tsx"
      provides: "Unit test — popover opens on trigger, lists decks, '+ New deck' reveals language chips"
  key_links:
    - from: "src/components/deck-switcher.tsx"
      to: "src/components/ui/popover.tsx"
      via: "import { Popover, PopoverTrigger, ... }"
      pattern: "ui/popover"
    - from: "src/components/deck-switcher.tsx"
      to: "src/lib/deck-actions.ts"
      via: "createDeck (reused unchanged)"
      pattern: "createDeck"
    - from: "src/components/app-header.tsx"
      to: "src/components/daybreak/lion-face.tsx"
      via: "LionFace brand mark"
      pattern: "LionFace"
---

<objective>
Re-skin the persistent app header to the Daybreak `TopBar` and convert the deck picker from a shadcn `Select` to a dropdown popover with an inline create-deck flow (D-01). Replace the 🐯 with `LionFace` + "LeoCards" wordmark (L-01), and replace the "Sign out" text button with a Daybreak logout glyph (icon-only, `aria-label="Sign out"`).

Purpose: DSH-01 — the persistent header with a working deck picker is the top-of-screen chrome for every dashboard state.
Output: restyled `app-header.tsx`, popover-based `deck-switcher.tsx`, glyph `logout-button.tsx`, a unit test for the popover/create flow, and retargeted e2e specs.

PRESENTATION-ONLY: the `createDeck` server action and ALL of DeckSwitcher's state logic (`creatingLang`, `showPicker`, `error`, `handleCreateDeck`, `handleValueChange`) are reused verbatim — only the render layer and the flag-emoji→LangChip swap change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-my-deck/21-CONTEXT.md
@.planning/phases/21-dashboard-my-deck/21-RESEARCH.md
@.planning/phases/21-dashboard-my-deck/21-PATTERNS.md
@AGENTS.md

# Design contract (this phase runs --skip-ui; the JSX mock IS the spec)
@design/handoff-daybreak/daybreak-dashboard.jsx

# Foundation this plan consumes
@src/components/ui/popover.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + design contract. -->

Popover wrapper (created in Plan 01, src/components/ui/popover.tsx):
  Popover (Root), PopoverTrigger, PopoverPortal, PopoverPositioner, PopoverPopup, PopoverClose
  (PopoverTrigger is generic — attach data-testid + Daybreak pill styles at the call site)

DeckSwitcher state to PRESERVE verbatim (src/components/deck-switcher.tsx lines 47-81):
  creatingLang / showPicker / error useState; learningLanguages useMemo;
  handleCreateDeck(langCode) (calls createDeck, onDeckChange, setShowPicker(false));
  handleValueChange(value) ("__new__" → setShowPicker(true), else onDeckChange)

LionFace (src/components/daybreak/lion-face.tsx):
  <LionFace size={number} mane="#E8973B" face="#FFD9A6" muzzle="#FFF1DC" ink="#4A331C" />  (aria-hidden already set)

LogoutButton logic to PRESERVE (src/components/logout-button.tsx):
  authClient.signOut() → router.push("/login")

Design-contract values (daybreak-dashboard.jsx):
  TopBar brand (lines 130-133): LionFace size 27 + wordmark fontDisplay 21px 700 #4A331C
  Deck-picker pill (lines 135-138): height 36, gap 6, padding "0 9px", radius 10, border "1.5px solid #EDDFC9", bg #FFFFFF; LangChip size 20 + Chevron down
  Logout button (line 139): 36x36, radius 10, border "1.5px solid #EDDFC9", bg #FFFFFF, LogoutGlyph inside
  LangChip (lines 56-60): width size+7, height size, radius 6, bg #FFF1DC, border "1px solid #F0E3CF", text size*0.46 700 #B4762A
  LogoutGlyph SVG (lines 18-26); Chevron (lines 27-30); PlusGlyph (lines 31-38)

Language code → display name (en/fr/es → English/French/Spanish): ALL_LANGUAGES already in deck-switcher.tsx (lines 22-26).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert DeckSwitcher to a Popover deck picker + inline create (D-01)</name>
  <files>src/components/deck-switcher.tsx, src/components/__tests__/deck-switcher.test.tsx</files>
  <behavior>
    - Clicking the trigger pill opens the popover; the deck list renders one row per deck with its full language name and an active marker on the current deck.
    - The popover contains a "+ New deck" row; clicking it reveals an inline set of LangChip buttons for languages the user is not already learning (learningLanguages).
    - Clicking a language chip calls createDeck for that language; while creating, that chip shows a spinner (creatingLang === lang.code); on failure the error message renders.
    - The pill shows the active deck's language CODE as a LangChip (e.g. "ES"), not the flag emoji and not the deck name.
  </behavior>
  <read_first>
    - src/components/deck-switcher.tsx (self — preserve ALL state/handlers lines 47-81; only the render JSX changes)
    - src/components/ui/popover.tsx (the wrapper from Plan 01)
    - src/components/ui/select.tsx (the OLD render being replaced — for the deck-list + separator + "__new__" structure)
    - src/components/__tests__/level-up-overlay.test.tsx (the @testing-library/react + jsdom docblock + afterEach(cleanup) test convention to copy)
    - design/handoff-daybreak/daybreak-dashboard.jsx lines 56-60 (LangChip), 135-138 (pill), 27-30 (Chevron), 31-38 (PlusGlyph)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/components/deck-switcher.tsx" (state-to-keep + new render targets + data-testid requirements)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"Pattern 1" + §"Pitfall 8" (use Popover not Menu) + §"Pitfall 2" (e2e selector breakers)
  </read_first>
  <action>
    Rewrite the RENDER of `DeckSwitcher` to use the Plan 01 Popover wrapper; keep every hook/handler (`creatingLang`, `showPicker`, `error`, `learningLanguages`, `handleCreateDeck`, `handleValueChange`, `activeDeck`) unchanged. Delete `FLAG_MAP` and the `Select`/`SelectContent`/`SelectItem`/`SelectSeparator`/`SelectTrigger`/`SelectValue` imports.
    Add a local `LangChip` component (text chip per the mock: bg #FFF1DC, border 1px #F0E3CF, radius 6, color #B4762A, font-bold) and a local `Chevron` glyph (or reuse a lucide ChevronDown sized ~16px — planner's discretion, but no flag emoji anywhere — L-01).
    Trigger: render `<PopoverTrigger data-testid="deck-picker-trigger">` styled as the compact pill (height 36, radius 10, border 1.5px #EDDFC9, bg white) containing `<LangChip code={activeDeck.language.toUpperCase()} size={20} />` + a down chevron. Give it `aria-label={"Switch deck, current " + (activeDeck?.name ?? "deck")}` for accessibility.
    Popup: render the deck list — for each deck a `<button type="button" data-testid={"deck-option-" + deck.language}>` showing the deck's full language name (map via ALL_LANGUAGES / the existing label lookup) and a check/dot marker when `deck.id === activeDeckId`; clicking calls `handleValueChange(deck.id)` (then closes the popover). Below a separator, a `<button type="button" data-testid="new-deck-row">+ New deck</button>` (PlusGlyph + text) that calls `setShowPicker(true)`.
    When `showPicker` is true, render INSIDE the popup an inline column of `learningLanguages` buttons, each `<LangChip>` + label, disabled while `creatingLang !== null`, showing a `Loader2` spinner when `creatingLang === lang.code`, calling `handleCreateDeck(lang.code)`. Include a "Cancel" button (visible text "Cancel" so its accessible name is preserved) that does `setShowPicker(false); setError(null)`. Render `{error && <span>...{error}</span>}` inside the popup.
    Use the Popover's own open state for the menu; keep `showPicker` strictly for the inline-create sub-section (do NOT conflate them). Closing the popover should not need to reset `showPicker`, but resetting it on close is acceptable.
    Then WRITE `src/components/__tests__/deck-switcher.test.tsx` with the `// @vitest-environment jsdom` docblock and `afterEach(cleanup)`. Mock `@/lib/deck-actions` (`createDeck`) with `vi.fn()`. Tests: (1) renders the trigger pill with the active language code; (2) clicking the trigger opens the popover and shows a `data-testid="deck-option-..."` per deck plus the `new-deck-row`; (3) clicking `new-deck-row` reveals the learning-language chip buttons. (Keep tests deterministic — assert on testids / roles, not pixel styles.)
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/deck-switcher.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FLAG_MAP" src/components/deck-switcher.tsx` returns 0 (flag emoji removed — L-01).
    - `grep -c "ui/select" src/components/deck-switcher.tsx` returns 0 (Select no longer imported); `grep -c "ui/popover" src/components/deck-switcher.tsx` returns ≥1.
    - `deck-switcher.tsx` still imports and calls `createDeck` (preserved): `grep -c "createDeck" src/components/deck-switcher.tsx` returns ≥2 (import + call).
    - `data-testid="deck-picker-trigger"`, `data-testid="new-deck-row"`, and `data-testid="deck-option-` all present in deck-switcher.tsx.
    - `npx vitest run src/components/__tests__/deck-switcher.test.tsx` passes all three tests.
  </acceptance_criteria>
  <done>The deck picker is a Daybreak popover with a LangChip pill trigger, deck list with active marker, and inline create flow; createDeck + state logic preserved; flag emoji gone; unit test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle AppHeader TopBar + logout glyph (DSH-01, L-01)</name>
  <files>src/components/app-header.tsx, src/components/logout-button.tsx</files>
  <behavior>
    - The header renders a LionFace mark + a "LeoCards" wordmark (no 🐯 emoji).
    - The logout control is an icon-only button whose accessible name is "Sign out" (aria-label), still triggering authClient.signOut() → /login.
  </behavior>
  <read_first>
    - src/components/app-header.tsx (self — preserve "use client", AppHeaderProps, the DeckSwitcher + LogoutButton composition + sticky header behavior)
    - src/components/logout-button.tsx (self — preserve handleSignOut: authClient.signOut() → router.push("/login"))
    - src/components/daybreak/lion-face.tsx (LionFace props)
    - design/handoff-daybreak/daybreak-dashboard.jsx lines 18-26 (LogoutGlyph SVG), 127-143 (TopBar layout)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/components/app-header.tsx" (brand + logout glyph targets)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"Pitfall 7" (logout text → glyph; aria-label required for e2e)
  </read_first>
  <action>
    In `app-header.tsx`: replace the `<span className="text-lg">🐯</span>` + text wordmark with `<LionFace size={27} mane="#E8973B" face="#FFD9A6" muzzle="#FFF1DC" ink="#4A331C" />` followed by a `<span>` wordmark styled `font-display text-[21px] font-bold text-foreground` reading "LeoCards" (L-01). Keep the header element's existing layout responsibilities (it currently is `h-14 ... sticky top-0 z-40`); align it to the Daybreak TopBar spacing (the mock uses gap 8 on the brand cluster and gap 9 on the right cluster). Keep `{decks.length > 0 && <DeckSwitcher .../>}` and `<LogoutButton />` composition and all props.
    In `logout-button.tsx`: keep `handleSignOut` verbatim. Replace the visible "Sign out" text with the Daybreak `LogoutGlyph` (reproduce the SVG/CSS glyph from the mock lines 18-26, ink color #4A331C) inside a 36x36 button (radius 10, border 1.5px #EDDFC9, bg white). The button MUST set `aria-label="Sign out"` (so `getByRole("button", { name: "Sign out" })` matches — RESEARCH Pitfall 7). Keep `type="button"`, `onClick={handleSignOut}`, and a ≥44px touch target (the 36px box is fine for the visual but ensure the clickable area / padding meets the existing accessibility intent — keep min-h consistent with the mock's 36 while not regressing tap size; acceptable to keep 36 box per the locked mock).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "app-header|logout-button" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "🐯" src/components/app-header.tsx` returns 0; `grep -c "LionFace" src/components/app-header.tsx` returns ≥1 (L-01).
    - `app-header.tsx` wordmark uses `font-display` and text "LeoCards".
    - `logout-button.tsx` button has `aria-label="Sign out"` (grep `aria-label="Sign out"`) and no longer renders the bare text "Sign out" as the button child (the accessible name comes from aria-label).
    - `logout-button.tsx` still calls `authClient.signOut()` and `router.push("/login")` (preserved).
    - `npx tsc --noEmit` reports no error in app-header.tsx / logout-button.tsx.
  </acceptance_criteria>
  <done>The header shows LionFace + "LeoCards" (no emoji); logout is an icon button with aria-label "Sign out" that preserves the sign-out behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget e2e specs for the popover + logout-glyph changes (L-06)</name>
  <files>e2e/08-deck-switching.spec.ts, e2e/10-mobile-responsive.spec.ts, e2e/01-auth-signup-login.spec.ts</files>
  <read_first>
    - e2e/08-deck-switching.spec.ts (lines 12, 16, 36, 38, 47, 60, 62, 67 — the Select-based selectors)
    - e2e/10-mobile-responsive.spec.ts (line 40 — getByText("Sign out"))
    - e2e/01-auth-signup-login.spec.ts (lines 38, 60, 89 — getByText("Sign out").click())
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"e2e/*.spec.ts" (the retarget map table)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"e2e Impact — Complete Audit" (priorities + intent)
  </read_first>
  <action>
    Retarget every selector this plan's UI changes break, PRESERVING each assertion's behavioral intent (never re-add old copy, never weaken an assertion). DO NOT change the test flow/expectations — only the locators.
    `e2e/08-deck-switching.spec.ts`: replace `page.locator('[data-slot="select-trigger"]')` (lines 12, 36, 60) with `page.getByTestId("deck-picker-trigger")`. Replace `page.locator('[role="option"]').filter({ hasText: "New deck" })` (lines 16, 38, 62) with `page.getByTestId("new-deck-row")`. Replace `page.locator('[role="option"]').filter({ hasText: "French" })` (line 47) with `page.getByTestId("deck-option-fr")`. The "Cancel" assertion (line 67) stays as-is (visible "Cancel" text is preserved by Task 1).
    `e2e/10-mobile-responsive.spec.ts` line 40: replace `page.getByText("Sign out")` with `page.getByRole("button", { name: "Sign out" })`.
    `e2e/01-auth-signup-login.spec.ts` lines 38, 60, 89: replace `page.getByText("Sign out").click()` with `page.getByRole("button", { name: "Sign out" }).click()`.
    After edits, grep the WHOLE e2e/ dir for any remaining `data-slot="select-trigger"`, `role="option"` filtered for deck options, or `getByText("Sign out")` and fix stragglers (strict-mode multi-match: scope with the testids above if a phrase renders twice).
  </action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "data-slot=\"select-trigger\"" e2e/` returns 0 across the dir.
    - `grep -rn "getByText(\"Sign out\")" e2e/` returns no matches (all three call-sites + the mobile spec retargeted to role+name).
    - `e2e/08-deck-switching.spec.ts` uses `getByTestId("deck-picker-trigger")`, `getByTestId("new-deck-row")`, and `getByTestId("deck-option-fr")`.
    - No test assertion intent was changed (same expectations, only locators) — reviewer-checkable diff: only locator expressions differ.
    - NOTE for the wave gate: full Playwright run happens at the Wave 2 boundary (per VALIDATION.md sampling) after a fresh `npm run dev` restart — these retargets are validated then, alongside Plan 04's specs.
  </acceptance_criteria>
  <done>The Select-era and "Sign out"-text locators are retargeted to data-testid / role+name across the affected specs, preserving intent; no stale literals remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → createDeck server action | Deck creation; ownership/auth enforced inside the existing `createDeck` action (unchanged) |
| client → authClient.signOut() | Session teardown via the existing auth client (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-E | Elevation of Privilege | Deck creation via the popover create flow | accept (preserved) | The popover calls the SAME `createDeck` action with the same language-code argument as the old Select; server-side ownership/validation is unchanged. The surface changed, not the authorization. |
| T-21-02-S | Spoofing | Logout glyph button | accept (preserved) | Logout still calls `authClient.signOut()`; only the visual (text → icon + aria-label) changed. No session-handling change. |
| T-21-02-T | Tampering | Popover focus/z-index | accept | `@base-ui/react` Popover (already installed) manages focus trap + Escape + portal; Positioner renders at z-50 above the z-40 sticky header (no overlap/clickjacking regression). |
| T-21-02-SC | Tampering | npm installs | accept | No packages installed (`@base-ui/react` already present). No `[ASSUMED]`/`[SUS]` packages; legitimacy gate N/A. |

No HIGH-severity threats — presentation-only re-skin reusing the existing createDeck/signOut paths.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/deck-switcher.test.tsx` green.
- `npx tsc --noEmit` clean for app-header.tsx / logout-button.tsx.
- e2e retarget greps clean (no stale `select-trigger` / `getByText("Sign out")`).
- Wave-2 boundary: fresh `npm run dev` restart, then `npm run test:e2e` green for 08/10/01 (validated with Plan 04's specs).
- Manual (phase UAT): header matches the mock TopBar; popover create flow shows per-language creating/error.
</verification>

<success_criteria>
- DSH-01: persistent header = LionFace + "LeoCards" wordmark + working popover deck picker (active-language LangChip, switch decks, inline create with per-language creating/error) + logout glyph.
- D-01 implemented exactly (Select → Popover, flag emoji → LangChip, brand 🐯 → LionFace, logout glyph); createDeck + state logic reused unchanged.
- Affected e2e specs retargeted to data-testid / role+name with intent preserved (L-06).
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-my-deck/21-02-SUMMARY.md` when done.
</output>
