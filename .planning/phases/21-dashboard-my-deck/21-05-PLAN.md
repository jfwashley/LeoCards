---
phase: 21-dashboard-my-deck
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/card-list.tsx
  - src/components/card-list.test.tsx
  - src/components/card-edit-dialog.tsx
  - src/components/card-edit-dialog.test.tsx
  - e2e/05-card-management.spec.ts
  - e2e/02-first-visit-deck-creation.spec.ts
  - e2e/helpers.ts
autonomous: true
requirements: [DSH-04, DSH-05, DSH-06, DSH-07]
nyquist_compliant: true

must_haves:
  truths:
    - "'Your words' is a tap-to-expand inline accordion: collapsed by default with a 'N learned' count in the header, expanding via a height/opacity transition (NOT a swipe gesture) to reveal the search field + word rows; the search field lives INSIDE the expanded panel — D-03, DSH-04"
    - "Word rows use Daybreak styling — source tag (Curated / Added by you / Paused), a 3-bar mastery meter (amber bars, green + check at 3/3), and pause+edit icon buttons; paused rows are de-emphasised but present — DSH-05"
    - "Word-row orientation is the DELIBERATE D-04 OVERRIDE of the handoff: native term BOLD ON TOP / target translation muted BENEATH (the OPPOSITE of the mock's target-on-top CardRow). This is intended — the UI auditor must NOT 'correct' it back to target-on-top — D-04"
    - "Source-tag copy maps source==='wordlist' → 'Curated', source==='manual' → 'Added by you', and pausedAt → 'Paused' (replacing the old 'word list'/'manual' strings) — DSH-05, L-06"
    - "The edit-card modal is restyled to Daybreak (TField/TBtn, Daybreak surface) while preserving the Save / Discard / Delete-with-confirm ('Delete this card? This can't be undone.') flow and the save/delete error states — DSH-06"
    - "The empty-deck and no-search-results states (already Daybreak from ONB-06) are reused; the no-search-results state lives inside the accordion; the empty-deck 'Browse words' link is the ONLY place that link remains — L-03, L-05; the QA badge + optimistic pause toggle are preserved — DSH-07"
  artifacts:
    - path: "src/components/card-list.tsx"
      provides: "WordsAccordion-wrapped card list — collapsed default, search inside, Daybreak CardRow native-on-top, source tags, mastery meter"
      contains: "words-accordion-header"
    - path: "src/components/card-list.test.tsx"
      provides: "Unit test — accordion collapsed/expand, native-on-top, source-tag copy, mastery meter"
    - path: "src/components/card-edit-dialog.tsx"
      provides: "Daybreak-restyled edit modal, behavior preserved"
      contains: "Delete this card?"
    - path: "src/components/card-edit-dialog.test.tsx"
      provides: "Unit test — Daybreak surface render + delete-confirm flow preserved"
  key_links:
    - from: "src/components/card-list.tsx"
      to: "motion/react"
      via: "AnimatePresence + motion.div height/opacity accordion"
      pattern: "motion/react"
    - from: "src/components/card-list.tsx"
      to: "/api/cards/[id]/pause|unpause"
      via: "togglePause optimistic fetch (preserved)"
      pattern: "/api/cards/"
    - from: "src/components/card-edit-dialog.tsx"
      to: "src/lib/deck-actions.ts"
      via: "editCard / deleteCard (preserved)"
      pattern: "editCard"
---

<objective>
Re-skin the "Your words" list (DSH-04/05) and the edit-card modal (DSH-06) to Daybreak. Wrap today's always-visible `card-list.tsx` populated list in a tap-to-expand inline accordion (collapsed by default, height/opacity transition, search inside — D-03); restyle the rows to the Daybreak CardRow styling with the DELIBERATE D-04 override (native bold on top / target muted beneath); map the source-tag copy to Curated / Added by you / Paused; and restyle the shadcn `Dialog` edit modal to Daybreak (TField/TBtn) while preserving its Save/Discard/Delete-with-confirm flow.

Purpose: DSH-04 (accordion), DSH-05 (word rows), DSH-06 (edit modal), and DSH-07's search-no-results + empty-deck reuse.
Output: accordion-wrapped `card-list.tsx` + unit test, Daybreak `card-edit-dialog.tsx` + unit test, retargeted `05-card-management` / `02-first-visit-deck-creation` specs, and the `browse-words-empty` testid wired into `helpers.ts`.

PRESENTATION-ONLY: `togglePause` (optimistic `useTransition` + `fetch POST /api/cards/[id]/pause|unpause` + `router.refresh()`), the `filtered` search useMemo, the QA badge (`QaStateBadge`/`qaMode`), the empty-deck + no-search-results Daybreak states (ONB-06), and `editCard`/`deleteCard` are all preserved — only the surface changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-my-deck/21-CONTEXT.md
@.planning/phases/21-dashboard-my-deck/21-RESEARCH.md
@.planning/phases/21-dashboard-my-deck/21-PATTERNS.md
@AGENTS.md

# Design contract (--skip-ui). NOTE: the mock's CardRow is target-on-top; D-04 OVERRIDES it to native-on-top.
@design/handoff-daybreak/daybreak-dashboard.jsx

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + design contract. -->

CardList state to PRESERVE (src/components/card-list.tsx):
  query/editCard/pendingCardIds useState; togglePause (lines 34-59, fetch + router.refresh); filtered useMemo (lines 61-70);
  empty-deck state (lines 72-118 — already Daybreak, KEEP); no-search-results state (lines 145-180 — already Daybreak, KEEP);
  QaStateBadge integration (lines 220-229 desktop, 311-320 mobile); CardEditDialog at bottom (lines 384-391)

Accordion pattern (established): motion/react v12.38.0
  import { AnimatePresence, motion } from "motion/react";
  import { usePrefersReducedMotion } from "@/hooks/use-prefers-reduced-motion";  // project hook, NOT motion's
  <button aria-expanded={open} aria-controls="words-panel" onClick={() => setOpen(o=>!o)}>
  <AnimatePresence initial={false}>{open && <motion.div id="words-panel" role="region"
     initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}}
     transition={{duration: reduced ? 0 : 0.22, ease:"easeInOut"}} style={{overflow:"hidden"}}>...}</motion.div>}
  CRITICAL (Pitfall 1): style={{overflow:"hidden"}} is REQUIRED on the motion.div.
  learnedCount = cards.filter(c => (c.masteryRound ?? 0) >= 3).length

Design-contract values (daybreak-dashboard.jsx):
  Accordion header (lines 222-228): bg #FFFFFF, border dt.cardBorder, radius 16, padding "15px 18px", flex space-between; title font-display 18px 700; "N learned" 13px 600 muted; chevron up/down by open
  CardRow (lines 105-124) — MOCK is target-on-top; D-04 → native(card.front) bold on top / target(card.back) muted beneath:
     row: gap 12, padding "12px 2px", borderBottom "1px solid #F4ECDD", opacity 0.55 when paused
     primary line: 16.5px 700 #4A331C (native = card.front); second line: 13.5px #8C7A63 (target = card.back) + SourceTag
  SourceTag (lines 95-99): paused→"Paused" {#F1E9DD/#8C7A63}; manual→"Added by you" {#EAF3EC/#3E8B5C}; else→"Curated" {#FFF1DC/#B4762A}
  Mastery (lines 82-93): 3 bars 8x16 radius 4, filled amber #F28A1F (#3E9B5F + ✓ at step≥3); paused rows render step 0
  IconBtn (line 101-103): 36x36 radius 10 border 1.5px #EDDFC9; pause/resume glyph + PencilGlyph
  Search (line 231-234): height 42 radius 12 bg dt.fieldBg; placeholder "Search your words"

Source-tag mapping change (card-list.tsx lines 235-239 desktop AND 327-332 mobile):
  OLD: card.pausedAt ? "Paused" : card.source === "wordlist" ? "word list" : "manual"
  NEW: card.pausedAt ? "Paused" : card.source === "manual" ? "Added by you" : "Curated"

Edit modal to PRESERVE (src/components/card-edit-dialog.tsx):
  EditForm state (lines 33-39); useEffect reset (lines 41-47); handleSave/handleDelete (editCard/deleteCard);
  delete-confirm flow (lines 75-105: "Delete this card?" / "This can't be undone." → Delete / Keep card); saveError/deleteError;
  Dialog wrapper (lines 170-179, showCloseButton={false})
  TField (label/value/onChange/disabled) + TBtn (isPending/onClick) are the Daybreak primitives to swap in.

e2e dependencies on this plan:
  - 05-card-management.spec.ts:24 + 02-first-visit-deck-creation.spec.ts:99 use getByPlaceholder("Search your cards...") → add data-testid="words-search-input" + new placeholder "Search your words"; tests must OPEN the accordion first.
  - 05-card-management.spec.ts:29,32 use getByText("No cards match") (PRE-EXISTING stale — card-list already says "No words match") → fix to /No words match/.
  - helpers.ts:164 + 02 + (Plan 04's 09 rewrite) depend on the empty-deck "Browse words" link → add data-testid="browse-words-empty" to it.
  - Plan 04's 09-language-breakdown test asserts the accordion header learned count → expose data-testid="words-accordion-header" with text "N learned".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap the list in the 'Your words' accordion + Daybreak CardRow (native-on-top, D-04) + source tags + mastery (DSH-04/05, D-03)</name>
  <files>src/components/card-list.tsx, src/components/card-list.test.tsx</files>
  <behavior>
    - The populated list is collapsed by default; the accordion header button has aria-expanded=false and shows "N learned" (count of cards with masteryRound >= 3).
    - Clicking the header sets aria-expanded=true and reveals (via motion height/opacity) the search field + word rows; the search input is inside the panel (not visible when collapsed).
    - Each word row shows the native term (card.front) bold on top and the target term (card.back) muted beneath (D-04 override — NOT target-on-top).
    - The source tag reads "Curated" for source==='wordlist', "Added by you" for source==='manual', and "Paused" when pausedAt is set.
    - The mastery meter renders 3 bars; at masteryRound >= 3 the bars are green with a check; paused rows render the meter at step 0 and the row is de-emphasised (opacity ~0.55).
    - Under prefers-reduced-motion the transition duration is 0 (no animation), but expand/collapse still works.
  </behavior>
  <read_first>
    - src/components/card-list.tsx (self — PRESERVE togglePause/filtered/empty-deck/no-search/QA badge; restyle the populated rows; move the search inside the accordion)
    - src/components/study-session.tsx (the established AnimatePresence + motion.div pattern — lines ~3, 342-349, 503-516)
    - src/components/__tests__/level-up-overlay.test.tsx (jsdom docblock + @testing-library/react + afterEach(cleanup) + the usePrefersReducedMotion vi.mock pattern to copy)
    - src/hooks/use-prefers-reduced-motion.ts (the project hook — usePrefersReducedMotion, NOT motion's useReducedMotion)
    - design/handoff-daybreak/daybreak-dashboard.jsx lines 82-124 (Mastery, SourceTag, IconBtn, CardRow), 217-242 (WordsAccordion header + search)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/components/card-list.tsx" (accordion wrapper, AnimatePresence, CardRow restyle with the D-04 OVERRIDE block, SourceTag/Mastery mappings, search testid)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"Pattern 2" + §"Pitfall 1" (overflow hidden) + §"Pitfall 6" (search moves inside accordion)
  </read_first>
  <action>
    Wrap the populated section (everything that renders when `cards.length > 0` and there are rows) in a "Your words" inline accordion. Add `const [open, setOpen] = useState(false)` and `const reduced = usePrefersReducedMotion()`. Compute `const learnedCount = cards.filter(c => (c.masteryRound ?? 0) >= 3).length`.
    Render an accordion header `<button type="button" aria-expanded={open} aria-controls="words-panel" data-testid="words-accordion-header" onClick={() => setOpen(o => !o)}>` styled per the mock (bg white, border 1px #F0E3CF, radius 16, padding "15px 18px", flex space-between): left = font-display 18px bold "Your words"; right = a `<span>` with text exactly `{learnedCount} learned` + a chevron (up when open). The "N learned" text MUST be present for Plan 04's 09-language-breakdown assertion.
    Below the header, `<AnimatePresence initial={false}>{open && (<motion.div id="words-panel" role="region" aria-label="Your words" initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} transition={{duration: reduced ? 0 : 0.22, ease:"easeInOut"}} style={{overflow:"hidden"}}> ... </motion.div>)}</AnimatePresence>`. Inside the panel: the search field (MOVED here from its current top-level position) — keep the `query`/`setQuery` wiring, add `data-testid="words-search-input"`, change the placeholder to "Search your words"; then the word rows; then the existing no-search-results state (which already lives in this region — keep it inside the panel so it shows when `filtered.length === 0 && query`).
    Restyle the word rows to the Daybreak CardRow styling. The current file has a desktop `<table>` and a mobile card layout — converge both to the Daybreak row presentation (a single responsive row list is acceptable and preferred; if you keep two layouts, apply the SAME D-04 ordering + tokens to both). For EACH row apply the **D-04 DELIBERATE OVERRIDE**: render `card.front` (native) as the BOLD top line (16.5px 700 #4A331C) and `card.back` (target) as the muted second line (13.5px #8C7A63) — this is the OPPOSITE of the mock's target-on-top CardRow and is INTENDED; add an inline code comment `// D-04: native-on-top override of the handoff CardRow — intentional, do not "correct"`.
    SourceTag: replace BOTH occurrences (the old desktop lines ~235-239 and mobile ~327-332) with the new mapping — `card.pausedAt ? "Paused" : card.source === "manual" ? "Added by you" : "Curated"` and the mock's tone colors. Mastery: replace the dot meter with the mock's 3-bar meter (8x16 bars radius 4, amber fill, green+✓ at masteryRound>=3); paused rows pass step 0. Keep the pause/resume IconBtn wired to `togglePause(card)` (preserve `disabled={pendingCardIds.has(card.id)}` + the existing aria-labels "Pause this card"/"Resume this card") and the edit IconBtn wired to `setEditCard(card)` (aria-label "Edit card"). De-emphasise paused rows (opacity ~0.55). PRESERVE the `QaStateBadge` rendering when `qaMode` (keep it positioned on the row's native/front cell as today). Keep `<CardEditDialog ... />` at the bottom.
    Do NOT touch `togglePause`, `filtered`, the empty-deck state, or the no-search-results copy ("No words match …" stays). Do NOT re-add "Browse words" anywhere except the existing empty-deck link (that link is handled in Task 3 for the testid).
    Then WRITE `src/components/card-list.test.tsx` (`// @vitest-environment jsdom`, `afterEach(cleanup)`, `vi.mock("@/hooks/use-prefers-reduced-motion")`, `vi.mock("next/navigation")` for useRouter). Tests: (1) collapsed by default — `words-accordion-header` has aria-expanded="false" and `words-search-input` is NOT in the document; (2) clicking the header sets aria-expanded="true" and reveals the search input + rows; (3) header shows "{N} learned" for the learned count; (4) D-04 — within a row, the native term (front) renders before/bolder than the target term (back) (assert DOM order: front appears before back in the row); (5) source tag shows "Curated" for a wordlist card, "Added by you" for a manual card, "Paused" for a paused card; (6) mastery — a card with masteryRound 3 renders the done/check treatment. Use a small fixed `cards` fixture.
  </action>
  <verify>
    <automated>npx vitest run src/components/card-list.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "motion/react" src/components/card-list.tsx` ≥ 1 and `grep -c "overflow" src/components/card-list.tsx` ≥ 1 (Pitfall 1 — overflow hidden on the motion.div).
    - `grep -c "usePrefersReducedMotion" src/components/card-list.tsx` ≥ 1 (project hook, gating the transition).
    - `grep -c "data-testid=\"words-accordion-header\"" src/components/card-list.tsx` ≥ 1 and `grep -c "data-testid=\"words-search-input\"" src/components/card-list.tsx` ≥ 1.
    - `grep -c "Search your words" src/components/card-list.tsx` ≥ 1 and `grep -c "Search your cards" src/components/card-list.tsx` returns 0 (placeholder changed).
    - Source-tag copy updated: `grep -c "word list" src/components/card-list.tsx` returns 0; `grep -c "Added by you" src/components/card-list.tsx` ≥ 1 and `grep -c "Curated" src/components/card-list.tsx` ≥ 1.
    - D-04 override comment present: `grep -c "D-04" src/components/card-list.tsx` ≥ 1 (and the test asserts native-before-target DOM order).
    - `togglePause` + `router.refresh` preserved: `grep -c "router.refresh" src/components/card-list.tsx` ≥ 1; QA badge preserved: `grep -c "QaStateBadge" src/components/card-list.tsx` ≥ 1.
    - All six tests in `card-list.test.tsx` pass via `npx vitest run`.
  </acceptance_criteria>
  <done>The list is a collapsed-by-default "Your words" accordion (search inside, motion height/opacity, reduced-motion gated) with Daybreak rows in the D-04 native-on-top order, Curated/Added-by-you/Paused tags, and the 3-bar mastery meter; pause toggle + QA badge preserved; unit test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle the edit-card modal to Daybreak (DSH-06) — behavior preserved</name>
  <files>src/components/card-edit-dialog.tsx, src/components/card-edit-dialog.test.tsx</files>
  <behavior>
    - The edit form renders Daybreak TField inputs labeled "Native word" and "Target word" bound to front/back.
    - Save is a Daybreak TBtn that calls editCard and shows a pending spinner; on failure the save error renders.
    - The delete flow is preserved: a Delete action opens the confirm ("Delete this card?" / "This can't be undone.") with Delete / Keep card; delete error renders on failure.
    - Discard closes without saving.
  </behavior>
  <read_first>
    - src/components/card-edit-dialog.tsx (self — PRESERVE EditForm state, useEffect reset, handleSave/handleDelete, the delete-confirm block, the Dialog wrapper; restyle inputs/buttons to Daybreak)
    - src/components/daybreak/t-field.tsx (TField: label/value/onChange/disabled props)
    - src/components/daybreak/t-btn.tsx (TBtn: isPending/onClick props)
    - src/components/ui/dialog.tsx (the @base-ui/react Dialog wrapper — keep it; optionally apply Daybreak surface tokens to DialogContent)
    - src/components/daybreak/__tests__/t-field.test.tsx (the Daybreak primitive test convention to copy)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/components/card-edit-dialog.tsx" (Input→TField, Button→TBtn, Dialog surface)
    - .planning/phases/21-dashboard-my-deck/21-CONTEXT.md §"Claude's Discretion" (edit-modal restyle latitude; the mock doesn't draw the modal — follow the Daybreak system)
  </read_first>
  <action>
    Restyle `card-edit-dialog.tsx` to Daybreak, preserving ALL behavior. Replace the two `<Input>` + `<label>` pairs with `<TField label="Native word" value={front} onChange={(e) => setFront(e.target.value)} disabled={saving} />` and `<TField label="Target word" value={back} onChange={(e) => setBack(e.target.value)} disabled={saving} />`. Replace the primary Save `<Button>` with `<TBtn isPending={saving} onClick={handleSave}>Save changes</TBtn>`. For the secondary actions (Discard changes, and Delete / Keep card in the confirm view) you may keep shadcn `Button variant="outline"`/`variant="destructive"` OR style ghost TBtns (Claude's discretion) — but PRESERVE the exact flow and copy: "Save changes", "Discard changes", the delete trigger, the confirm heading "Delete this card?" + "This can't be undone.", "Delete", "Keep card". Keep `handleSave`/`handleDelete` (calling `editCard`/`deleteCard`), the `useEffect` state reset, the `saveError`/`deleteError` rendering, and the `showDeleteConfirm` branch verbatim. Keep the `Dialog`/`DialogContent` wrapper (`showCloseButton={false}`); optionally add Daybreak surface tokens to `DialogContent` className (bg var(--background), rounded-[22px], border #F0E3CF) — keep the DialogTitle "Edit card".
    Then WRITE `src/components/card-edit-dialog.test.tsx` (`// @vitest-environment jsdom`, `afterEach(cleanup)`, `vi.mock("@/lib/deck-actions")` with `editCard`/`deleteCard` as `vi.fn()`). Tests: (1) renders TField inputs for "Native word" and "Target word" with the card's values; (2) clicking "Save changes" calls editCard with the (edited) front/back; (3) the delete-confirm flow — clicking the delete trigger shows "Delete this card?" + "This can't be undone." with Delete/Keep card, and clicking "Keep card" returns to the edit form (does not call deleteCard); (4) clicking "Delete" in the confirm calls deleteCard.
  </action>
  <verify>
    <automated>npx vitest run src/components/card-edit-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TField" src/components/card-edit-dialog.tsx` ≥ 2 (both fields) and `grep -c "TBtn" src/components/card-edit-dialog.tsx` ≥ 1 (Save).
    - Behavior copy preserved: `grep -c "Delete this card?" src/components/card-edit-dialog.tsx` ≥ 1; `grep -c "This can" src/components/card-edit-dialog.tsx` ≥ 1 (the "can't be undone" line); `grep -c "Keep card" src/components/card-edit-dialog.tsx` ≥ 1.
    - `editCard`/`deleteCard` still called: `grep -c "editCard" src/components/card-edit-dialog.tsx` ≥ 2 and `grep -c "deleteCard" src/components/card-edit-dialog.tsx` ≥ 2 (import + call each).
    - All four tests in `card-edit-dialog.test.tsx` pass via `npx vitest run`.
  </acceptance_criteria>
  <done>The edit modal uses Daybreak TField/TBtn on a Daybreak surface while preserving Save/Discard/Delete-with-confirm + error states; unit test green.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget e2e (05-card-management, 02-first-visit) + add browse-words-empty testid + helper (L-06)</name>
  <files>e2e/05-card-management.spec.ts, e2e/02-first-visit-deck-creation.spec.ts, e2e/helpers.ts, src/components/card-list.tsx</files>
  <read_first>
    - e2e/05-card-management.spec.ts (line 24 getByPlaceholder("Search your cards..."); lines 29,32 getByText("No cards match") — PRE-EXISTING stale)
    - e2e/02-first-visit-deck-creation.spec.ts (line 99 getByPlaceholder("Search your cards..."); lines 30,76 the empty-deck "Browse words" link)
    - e2e/helpers.ts (line 164 getByRole("link", { name: "Browse words" }).first())
    - src/components/card-list.tsx (the empty-deck "Browse words" Link — currently lines ~97-103 — to receive data-testid)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"e2e/*.spec.ts" (retarget map) + §"src/components/card-list.tsx" (search testid)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"Pitfall 5" (05:29 pre-existing) + §"Pitfall 6" (search inside accordion) + §"CRITICAL: helpers.ts addWordsFromBrowser"
  </read_first>
  <action>
    First, in `src/components/card-list.tsx`, add `data-testid="browse-words-empty"` to the empty-deck "Browse words" `<Link href="/deck/browse">` (the primary action in the empty-deck state) — this future-proofs the helper (RESEARCH). Do NOT change its accessible name (keep "Browse words" so existing role-name locators still match).
    Then retarget the specs, PRESERVING intent (the search now lives inside the accordion, so tests must OPEN it first):
    `e2e/05-card-management.spec.ts`: before line 24's search interaction, add a step to expand the accordion — `await page.getByTestId("words-accordion-header").click()`. Replace `page.getByPlaceholder("Search your cards...")` with `page.getByTestId("words-search-input")`. Fix the PRE-EXISTING stale assertion at lines 29, 32: replace `getByText("No cards match")` with `getByText(/No words match/)` (the component already renders "No words match" — flag this in the SUMMARY as a pre-existing bug fixed in passing).
    `e2e/02-first-visit-deck-creation.spec.ts` line 99: same treatment — expand the accordion (`getByTestId("words-accordion-header").click()`) then `getByTestId("words-search-input")`. The empty-deck "Browse words" usages (lines 30, 76) keep their role-name locator (still valid) — optionally switch to `getByTestId("browse-words-empty")` for robustness (planner's discretion; keep intent).
    `e2e/helpers.ts` line 164: retarget `page.getByRole("link", { name: "Browse words" }).first().click()` to `page.getByTestId("browse-words-empty").click()` (the helper always runs against the empty-deck state, where this testid now exists). Keep the subsequent `waitForURL(/\/deck\/browse/)` etc.
    After edits, grep the WHOLE e2e/ dir for any remaining `getByPlaceholder("Search your cards...")` and `getByText("No cards match")` and fix stragglers. Watch for Playwright strict-mode multi-match on "Browse words" (the `.first()` / testid scoping handles it).
  </action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data-testid=\"browse-words-empty\"" src/components/card-list.tsx` ≥ 1 (empty-deck link future-proofed).
    - `grep -rc "Search your cards" e2e/` returns 0 (placeholder retargeted everywhere).
    - `grep -rc "No cards match" e2e/` returns 0 (pre-existing stale assertion fixed to /No words match/).
    - `e2e/05-card-management.spec.ts` and `e2e/02-first-visit-deck-creation.spec.ts` expand the accordion (`getByTestId("words-accordion-header")`) before targeting the search input.
    - `e2e/helpers.ts` uses `getByTestId("browse-words-empty")`.
    - NOTE: full Playwright validation runs at the Wave-1/overall e2e gate after `npm run dev` restart (these specs also depend on Plan 02/04 chrome; run the suite once all dashboard plans land).
  </acceptance_criteria>
  <done>The search-input + no-results selectors are retargeted (accordion opened first), the pre-existing "No cards match" assertion is fixed, and the empty-deck "Browse words" link carries a testid the helper now uses; intent preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/cards/[id]/pause\|unpause | Pause toggle; card ownership/authorization enforced in the existing API route (unchanged) |
| client → editCard / deleteCard server actions | Card edit/delete; ownership/validation enforced inside the existing server actions (unchanged) |
| client → search filter | Client-side `filtered` over already-fetched, already-owned cards; no new server query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-E | Elevation of Privilege | Pause / edit / delete actions | accept (preserved) | All three call the SAME server endpoints/actions (`/api/cards/[id]/pause\|unpause`, `editCard`, `deleteCard`) with the same card id; server-side ownership/authorization is unchanged. The re-skin changes only the buttons that invoke them. |
| T-21-05-I | Information Disclosure | Client-side search filter | accept | Filters only the user's own already-loaded cards in memory; no new data fetched or exposed; introduces no new surface (CONTEXT security note). |
| T-21-05-T | Tampering | Edit modal inputs (front/back) | accept (preserved) | Field values are validated/persisted by the existing `editCard` server action; the TField swap is presentational. |
| T-21-05-SC | Tampering | npm installs | accept | No packages installed (`motion` already present). No `[ASSUMED]`/`[SUS]` packages; legitimacy gate N/A. |

No HIGH-severity threats — presentation-only re-skin over preserved pause/edit/delete authorization paths; the client-side search adds no attack surface.
</threat_model>

<verification>
- `npx vitest run src/components/card-list.test.tsx src/components/card-edit-dialog.test.tsx` green.
- `npx tsc --noEmit` clean for the touched components.
- e2e retarget greps clean (no "Search your cards" / "No cards match"); `browse-words-empty` testid present + used by helper.
- Overall e2e gate (after `npm run dev` restart): `npm run test:e2e` green for 05/02 (with the rest of the dashboard plans).
- Manual (phase UAT): "Your words" is collapsed by default and expands inline (no swipe); rows show native-on-top (D-04), Curated/Added-by-you/Paused tags, the 3-bar mastery meter; paused rows de-emphasised; edit modal is Daybreak with Save/Discard/Delete-confirm + error states; search-no-results renders inside the panel.
</verification>

<success_criteria>
- DSH-04: tap-to-expand inline accordion (height/opacity, not swipe), collapsed-default with "N learned", search inside, no-search-results state.
- DSH-05: word rows with source tag (Curated/Added by you/Paused), 3-bar mastery (green+check at 3/3), pause+edit actions, paused de-emphasised — in the D-04 native-on-top order.
- DSH-06: Daybreak edit modal preserving Save/Discard/Delete-with-confirm + error states.
- DSH-07 (this slice): search-active-no-results + empty-deck/brand-new-user states render in Daybreak (reused from ONB-06); QA badge + optimistic pause preserved.
- D-03 + D-04 implemented exactly (D-04 is the intended handoff override — not to be "corrected").
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-my-deck/21-05-SUMMARY.md` when done.
</output>
