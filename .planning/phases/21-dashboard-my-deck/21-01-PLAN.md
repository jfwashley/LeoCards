---
phase: 21-dashboard-my-deck
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/popover.tsx
  - src/app/(protected)/dashboard/page.tsx
  - src/components/deck-view.tsx
autonomous: true
requirements: [DSH-01, DSH-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The Daybreak dashboard has no standalone 'My Deck' heading and no cross-language learned breakdown line — D-02"
    - "getLanguageBreakdown is no longer fetched in dashboard/page.tsx and the languageBreakdown prop is gone from DeckView — D-02"
    - "A reusable @base-ui/react Popover wrapper exists at src/components/ui/popover.tsx for the deck-picker (consumed by the header plan, DSH-01)"
    - "The dashboard still compiles and all existing unit + e2e behavior (deck list, study button, card list, habitat link) is unchanged"
  artifacts:
    - path: "src/components/ui/popover.tsx"
      provides: "Daybreak-styled @base-ui/react Popover wrapper (Root/Trigger/Portal/Positioner/Popup/Close)"
      contains: "PopoverTrigger"
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Dashboard server entry with getLanguageBreakdown removed"
    - path: "src/components/deck-view.tsx"
      provides: "DeckView with languageBreakdown prop + My Deck heading removed"
  key_links:
    - from: "src/components/ui/popover.tsx"
      to: "@base-ui/react/popover"
      via: "named sub-component wrappers"
      pattern: "@base-ui/react/popover"
---

<objective>
Establish the two foundation pieces every other Phase 21 plan builds on:
1. Create the `src/components/ui/popover.tsx` wrapper around `@base-ui/react/popover` (the deck-picker in Plan 02 consumes it).
2. Execute the D-02 removal: drop `getLanguageBreakdown` from the dashboard server entry and remove the `languageBreakdown` prop + the `My Deck` heading + cross-language breakdown render from `DeckView`.

Purpose: Plan 02 (header/deck-picker) cannot build the popover trigger without the wrapper; Plan 04 (deck-view body) cannot cleanly rewrite the action line while the breakdown prop/render still exist. Doing both removals atomically here keeps `deck-view.tsx` compiling and leaves the action-line region untouched for Plan 04.
Output: a new Popover wrapper + a leaner dashboard page + DeckView (heading/breakdown gone, action line/study button/card list untouched).

This is a PRESENTATION-ONLY re-skin. No backend, schema, route, or engine changes. `getLanguageBreakdown` itself stays in `milestone-queries.ts` (it has its own unit tests) — only its dashboard call-site is removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-my-deck/21-CONTEXT.md
@.planning/phases/21-dashboard-my-deck/21-RESEARCH.md
@.planning/phases/21-dashboard-my-deck/21-PATTERNS.md

# Project conventions — Next.js 16 has breaking changes; read node_modules/next/dist/docs/ before asserting any Next.js API
@AGENTS.md

<interfaces>
<!-- Wrapper convention to copy exactly. Extracted from codebase — no exploration needed. -->

The @base-ui/react wrapper convention (src/components/ui/select.tsx, lines 1-201):
- "use client" at top
- import { Popover as PopoverPrimitive } from "@base-ui/react/popover"
- Each export is a NAMED function (not const arrow), typed with PopoverPrimitive.<Part>.Props
- Add data-slot="popover-<part>" to each wrapper
- Use cn() from "@/lib/utils" for className merging
- Portal > Positioner (className="isolate z-50") > Popup pattern

@base-ui/react/popover (v1.3.0) exports (VERIFIED via node require):
  Root, Trigger, Portal, Positioner, Popup, Close, Arrow, Backdrop, Title, Description

Daybreak card-surface tokens for the Popup (src/components/daybreak/card.tsx):
  background: "#FFFFFF"; border: "1px solid #F0E3CF"; borderRadius: 22; boxShadow: "0 12px 30px rgba(160,110,40,0.16)"

DeckView current props with the breakdown still present (src/components/deck-view.tsx):
  interface DeckViewProps { ...; languageBreakdown: Array<{ language: string; count: number }>; ... }  // line 103
  Render block to remove: lines 175-188 (the <div> holding <h1>My Deck</h1> + the languageBreakdown <p>)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Daybreak Popover wrapper (src/components/ui/popover.tsx)</name>
  <files>src/components/ui/popover.tsx</files>
  <read_first>
    - src/components/ui/select.tsx (the exact wrapper convention to copy — imports, named-function sub-components, data-slot naming, Portal/Positioner/Popup nesting, export block)
    - src/components/ui/dialog.tsx (second @base-ui/react wrapper example for the same convention)
    - src/components/daybreak/card.tsx (Daybreak surface tokens for the Popup background/border/radius/shadow)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/components/ui/popover.tsx" (sub-component list + token mapping)
  </read_first>
  <action>
    Create `src/components/ui/popover.tsx` following the `select.tsx` convention exactly. Start with `"use client"`, then `import { Popover as PopoverPrimitive } from "@base-ui/react/popover"` and `import { cn } from "@/lib/utils"`.
    Export named-function wrappers for: `Popover` (= PopoverPrimitive.Root), `PopoverTrigger`, `PopoverPortal`, `PopoverPositioner`, `PopoverPopup`, `PopoverClose`. Type each with `PopoverPrimitive.<Part>.Props` and spread `{...props}`. Add `data-slot="popover-<part>"` to each (e.g. `data-slot="popover-trigger"`).
    `PopoverTrigger` MUST render as a bare button-ish element with `render`/`className` passthrough so the consumer (Plan 02) can attach `data-testid="deck-picker-trigger"` and Daybreak pill styles — do NOT bake pill styling into the wrapper (it stays generic like select.tsx).
    `PopoverPopup`: apply the Daybreak card surface via className tokens — `bg-white border border-[#F0E3CF] rounded-[22px] shadow-[0_12px_30px_rgba(160,110,40,0.16)]` plus padding `p-2` and `outline-none`; allow `className` override via `cn()`. Wrap Popup in `PopoverPortal > PopoverPositioner` and put `className="isolate z-50"` on the Positioner (matches select.tsx so the popover renders above the sticky header z-40).
    Accept `side`/`sideOffset`/`align` props on the content wrapper with sane defaults (`side="bottom"`, `sideOffset={8}`, `align="end"`) so the deck picker drops below-and-right of the pill.
    Do NOT import lucide chevrons here (the trigger glyph is the consumer's concern). Keep this file purely structural.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "popover" || echo "no popover type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/popover.tsx` starts with `"use client"` and imports from `@base-ui/react/popover` (not the root package).
    - File exports (named) `Popover`, `PopoverTrigger`, `PopoverPortal`, `PopoverPositioner`, `PopoverPopup`, `PopoverClose` — verifiable: `grep -c "export {" src/components/ui/popover.tsx` returns 1 and the export block lists all six names.
    - `PopoverPositioner` carries `className="isolate z-50"` (grep `z-50`).
    - `npx tsc --noEmit` reports no errors originating in `popover.tsx`.
    - No Daybreak pill styling baked into `PopoverTrigger` (it stays generic; styling is the consumer's job).
  </acceptance_criteria>
  <done>The Popover wrapper exists, type-checks, exports all six sub-components, and applies Daybreak surface tokens only to the Popup.</done>
</task>

<task type="auto">
  <name>Task 2: Remove getLanguageBreakdown + 'My Deck' heading + breakdown render (D-02)</name>
  <files>src/app/(protected)/dashboard/page.tsx, src/components/deck-view.tsx</files>
  <read_first>
    - src/app/(protected)/dashboard/page.tsx (current import line 15, Promise.all lines 36-42, DeckView prop line 129 — all confirmed)
    - src/components/deck-view.tsx (DeckViewProps line 103, destructure line 116, render block lines 175-188)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/app/(protected)/dashboard/page.tsx" and §"src/components/deck-view.tsx" (exact removal scope)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"Pattern 4: getLanguageBreakdown Removal" (the three consumers; the function itself is KEPT in milestone-queries.ts)
  </read_first>
  <action>
    Per D-02, remove the cross-language learned breakdown and the standalone heading. Make exactly these edits:
    In `src/app/(protected)/dashboard/page.tsx`: remove the `import { getLanguageBreakdown } from "@/lib/milestone-queries"` line; drop `getLanguageBreakdown(...)` (the 4th element) from the `Promise.all` and drop `languageBreakdown` from the destructured tuple (so it reads `const [decks, nativeLang, habitatFacts] = await Promise.all([...])`); remove `languageBreakdown={languageBreakdown}` from the `<DeckView>` JSX. Also remove the now-unused `import { HabitatWidget } from "@/components/habitat-widget"` at the top of page.tsx (it is imported but unused — Plan 03/04 own the hero swap; this import is dead either way and removing it now avoids a lint error). Do NOT touch the `decks.length === 0 → redirect("/welcome")` guard, `buildCardRows`/`cardRows`, `readQaAuth`, `computeHabitatState`, `assembleSession`, `earliestCooldownEnd`, or any other fetch.
    In `src/components/deck-view.tsx`: remove `languageBreakdown` from the `DeckViewProps` interface (line 103); remove it from the destructured params (line 116); remove the render block (lines 175-188) that contains the wrapping `<div>` with `<h1 className="text-xl font-semibold">My Deck</h1>` and the `languageBreakdown.map(...)` `<p>`. Preserve the `LANGUAGE_LABELS` map (still used for `nativeLangLabel`/`targetLangLabel`). IMPORTANT: leave the rest of the JSX flow row intact for now — specifically the surrounding `<div className="flex ... justify-between ...">` that also holds `renderStudyButton()` / "Browse words" / "Add a card" stays as-is (Plan 04 rewrites the action line). Only the `<div>` holding the heading + breakdown `<p>` is removed, then close the layout so `renderStudyButton()`/action links still render. The file MUST compile with no dangling `languageBreakdown` reference.
    Do NOT modify `CountdownTimer`, `renderStudyButton`, the `<AppHeader>` usage, `<HabitatWidget>` usage, or `<CardList>` usage — those are Plan 03/04's territory.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "deck-view|dashboard/page|languageBreakdown" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getLanguageBreakdown" src/app/\(protected\)/dashboard/page.tsx` returns 0.
    - `grep -c "languageBreakdown" src/components/deck-view.tsx` returns 0.
    - `grep -c "My Deck" src/components/deck-view.tsx` returns 0.
    - `npx tsc --noEmit` reports no error referencing `languageBreakdown`, `deck-view.tsx`, or `dashboard/page.tsx`.
    - The `getLanguageBreakdown` function still exists in `src/lib/milestone-queries.ts` (NOT deleted) — verifiable: `grep -c "export.*getLanguageBreakdown" src/lib/milestone-queries.ts` returns ≥1.
    - `renderStudyButton`, `CountdownTimer`, `<AppHeader>`, `<HabitatWidget>`, and `<CardList>` usages are unchanged in deck-view.tsx (this plan does not touch them).
  </acceptance_criteria>
  <done>getLanguageBreakdown is gone from the dashboard page and DeckView; the My Deck heading + breakdown render are removed; the function survives in milestone-queries.ts; deck-view.tsx and page.tsx compile.</done>
</task>

<task type="auto">
  <name>Task 3: Behavior-preservation gate — typecheck + full unit suite + scoped biome</name>
  <files>src/components/ui/popover.tsx, src/app/(protected)/dashboard/page.tsx, src/components/deck-view.tsx</files>
  <read_first>
    - .planning/phases/21-dashboard-my-deck/21-VALIDATION.md §"Test Infrastructure" + §"biome gotcha" (scope biome to touched files; never `biome ci .`)
  </read_first>
  <action>
    Run the behavior-preservation gate for this plan's foundation changes. This is a re-skin: the existing milestone-queries unit tests (which cover `getLanguageBreakdown`) and all other unit tests MUST stay green since the function is untouched. Run `npx tsc --noEmit` (whole project), then `npm test` (full vitest run), then scope biome to ONLY the three touched files. Do NOT run `biome ci .` (the repo has ~429 pre-existing errors; git hooks are disabled). Fix any NEW error introduced by this plan's edits; do not attempt to fix pre-existing repo errors in untouched files.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npx biome ci "src/components/ui/popover.tsx" "src/app/(protected)/dashboard/page.tsx" "src/components/deck-view.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test` (vitest run) exits 0 — all unit tests green, including `milestone-queries` tests for the retained `getLanguageBreakdown`.
    - `npx biome ci` over exactly the three touched files exits 0 (scoped, not repo-wide).
  </acceptance_criteria>
  <done>Typecheck, full unit suite, and scoped biome all pass — the foundation changes preserve all existing behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → dashboard RSC | Existing `(protected)/layout.tsx` + `auth.api.getSession` already gate this route; unchanged this plan |
| RSC → DB queries | `getUserDecks`/`getHabitatFacts`/`getUserNativeLanguage` ownership scoping unchanged; one query (`getLanguageBreakdown`) is merely no longer called |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-I | Information Disclosure | Removing the `getLanguageBreakdown` fetch | accept (preserved) | No data exposure change — a query is removed, not added; the function and its access scoping remain intact in milestone-queries.ts. No new surface. |
| T-21-01-T | Tampering | `popover.tsx` wrapper | accept (preserved) | Pure structural wrapper over the already-trusted `@base-ui/react` (v1.3.0, already installed — see RESEARCH Package Legitimacy Audit; no new dependency). No data flow, no input handling. |
| T-21-01-SC | Tampering | npm installs | accept | No packages installed this plan (`@base-ui/react` already present). No `[ASSUMED]`/`[SUS]` packages; legitimacy gate N/A. |

No HIGH-severity threats. This is a presentation/foundation change with no new attack surface (a DB query is removed, not introduced).
</threat_model>

<verification>
- `npx tsc --noEmit` clean (whole project).
- `npm test` green (full unit suite; milestone-queries tests prove `getLanguageBreakdown` still works).
- `npx biome ci` clean over the three touched files only.
- Manual (deferred to phase UAT): dashboard renders without a "My Deck" heading and without the "French: N · Spanish: M learned" line.
</verification>

<success_criteria>
- `src/components/ui/popover.tsx` exists, type-checks, and exports the six Popover sub-components with Daybreak surface tokens on the Popup.
- `getLanguageBreakdown` is no longer fetched in the dashboard page and the `languageBreakdown` prop is gone from `DeckView`; the `My Deck` heading + breakdown render are removed (D-02).
- The `getLanguageBreakdown` function survives in `milestone-queries.ts` with its tests passing.
- All existing unit tests stay green; deck-view.tsx and page.tsx compile.
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-my-deck/21-01-SUMMARY.md` when done.
</output>
