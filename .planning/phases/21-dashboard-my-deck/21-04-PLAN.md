---
phase: 21-dashboard-my-deck
plan: 04
type: execute
wave: 2
depends_on: ["21-01", "21-03"]
files_modified:
  - src/components/deck-view.tsx
  - src/components/deck-view.test.tsx
  - e2e/12-pause-cards.spec.ts
  - e2e/09-language-breakdown.spec.ts
  - e2e/07-habitat-display.spec.ts
autonomous: true
requirements: [DSH-02, DSH-03, DSH-07]
nyquist_compliant: true

must_haves:
  truths:
    - "DeckView renders the new HabitatHero (replacing the legacy HabitatWidget .webp thumbnail) and passes the cooldown sleeping flag (earliestCooldownEnd && !hasDueCards) so the hero naps over a still-accurate ring — L-04, D-06 sibling"
    - "The Option-D action line renders a full-width 'Start studying' primary (amber + active link when due, dimmed when not) above a status row: status text on the left + 'Add a card' on the right — DSH-03"
    - "The status row adapts across four states: '12 due' (amber dot) / '0 due' (outline dot) / 'Resting · {countdown}' (napping glyph, reusing the existing CountdownTimer value) / 'All paused' — DSH-03"
    - "'Browse words' is removed from the populated action line (it remains ONLY in the empty-deck card-list state); the 'All cards are paused — unpause one to study.' paragraph is folded into the 'All paused' status — L-05"
    - "The 0-deck → /welcome redirect, SRS due/cooldown logic (assembleSession/earliestCooldownEnd), CountdownTimer 60s tick + router.refresh on expiry, and QA badge are all preserved unchanged — L-02, L-03"
    - "Dashboard covers all DSH-07 states (cards-due, none-due, resting, all-paused, plus empty-deck/brand-new-user via the existing card-list empty state) in Daybreak — DSH-07"
  artifacts:
    - path: "src/components/deck-view.tsx"
      provides: "Daybreak DeckView — HabitatHero + Option-D action line + StatusText state machine"
      contains: "HabitatHero"
    - path: "src/components/deck-view.test.tsx"
      provides: "Unit test — StudyButton active/dimmed + StatusText four states + Resting countdown"
  key_links:
    - from: "src/components/deck-view.tsx"
      to: "src/components/habitat-hero.tsx"
      via: "import { HabitatHero } (replaces HabitatWidget)"
      pattern: "habitat-hero"
    - from: "src/components/deck-view.tsx"
      to: "/study"
      via: "Start studying Link when hasDueCards"
      pattern: "/study"
    - from: "src/components/deck-view.tsx"
      to: "/deck/new-card"
      via: "Add a card link"
      pattern: "new-card"
---

<objective>
Rewrite the body of `DeckView` to the Daybreak layout (DSH-03): swap the legacy `HabitatWidget` for the new `HabitatHero` (DSH-02 wiring, L-04), build the locked "Option D" action line (full-width "Start studying" + a four-state status row + "Add a card"), remove "Browse words" from the populated action line (L-05), and fold the "All cards are paused" paragraph into the "All paused" status (D-06 sibling). Then retarget the e2e specs this changes break.

Purpose: DSH-03 (action line + adaptive status) and DSH-07 (all dashboard states render in Daybreak); completes the DSH-02 wiring started in Plan 03.
Output: restyled `deck-view.tsx`, a unit test for the StudyButton/StatusText state machine, and retargeted `12-pause-cards` / `09-language-breakdown` (rewrite) / `07-habitat-display` specs.

PRESENTATION-ONLY: the SRS engine (`assembleSession`, `earliestCooldownEnd`), the `CountdownTimer` 60s-tick + `router.refresh()` logic, the 0-deck → `/welcome` redirect (in page.tsx, untouched), and the QA badge are all preserved — only the surface changes. The `languageBreakdown` prop + "My Deck" heading were already removed in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-my-deck/21-CONTEXT.md
@.planning/phases/21-dashboard-my-deck/21-RESEARCH.md
@.planning/phases/21-dashboard-my-deck/21-PATTERNS.md
@AGENTS.md

# Design contract (--skip-ui) + the hero component this plan wires in
@design/handoff-daybreak/daybreak-dashboard.jsx
@src/components/habitat-hero.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + design contract. -->

DeckView props AFTER Plan 01 (languageBreakdown removed):
  { decks, initialCards: CardRow[], nativeLang, activeDeckId, hasDueCards: boolean,
    earliestCooldownEnd: string | null, habitatState: HabitatState, celebratingLevel?, qaMode? }

CountdownTimer (deck-view.tsx lines 44-92) — PRESERVE the formatCountdown + useEffect(60s tick + router.refresh on expiry) logic. Currently it renders a disabled <button> "Next cards in {countdown}". REWORK: expose the countdown string so the status row renders "Resting · {countdown}". Acceptable approaches (planner's discretion): (a) keep CountdownTimer but change its returned JSX to the Daybreak status text "Resting · {countdown}" with the napping glyph; or (b) lift the countdown string via a small render-prop/child. Either way the 60s interval + router.refresh() on expiry MUST remain.

HabitatHero (Plan 03): <HabitatHero habitatState={habitatState} celebratingLevel={celebratingLevel} sleeping={boolean} />
  Pass sleeping = Boolean(earliestCooldownEnd && !hasDueCards) (the resting/cooldown state).

Design-contract values (daybreak-dashboard.jsx):
  StudyButton (lines 164-170): height 58, radius 14 (dt.btnRadius), fontDisplay 21px 700; active bg #F28A1F color #FFF shadow "0 10px 22px rgba(242,138,31,0.32)"; inactive bg #F4E7D2 color #B49B78 no shadow
  StatusText (lines 172-199): cooldown = napping LionFace(22) + "z" + "Resting · {countdown}"; paused = two pause bars + "All paused"; due/none = dot (amber if count else outline) + "{count} due"
  ActionLine (lines 201-215): column gap 14 — StudyButton, then a row [StatusText (left) | "Add a card" pill (right)]
  "Add a card" pill (lines 208-211): height 40, padding "0 15px", radius 12, border "1.5px solid #EDDFC9", bg #FFFFFF, PlusGlyph(#F28A1F) + text "Add a card"

deck-view.tsx render blocks to REMOVE (post Plan 01 the heading/breakdown are already gone):
  - the populated "Browse words" <Link> (was lines 191-196) — L-05
  - the "All cards are paused — unpause one to study." <p> (was lines 205-209) — folded into StatusText "All paused"
  - the legacy <HabitatWidget> usage (was lines 169-174) → replace with <HabitatHero>; also drop the now-unused HabitatWidget import

State → StatusText mapping (RESEARCH Pattern 5):
  due:      hasDueCards                         → amber dot + "{N} due"  (StudyButton active, <Link href=/study>)
  none:     !hasDueCards && !earliestCooldownEnd && hasCards && (no paused-only case) → outline dot + "0 due" (dimmed)
  cooldown: earliestCooldownEnd && !hasDueCards → napping glyph + "Resting · {countdown}" (dimmed)
  paused:   hasCards && !hasDueCards && !earliestCooldownEnd && all cards paused → pause glyph + "All paused" (dimmed)
  NOTE: the current code distinguishes the all-paused case by the existing `hasCards && !hasDueCards && !earliestCooldownEnd` branch that rendered the "All cards are paused" <p>. Preserve that exact condition for the "All paused" status; the plain "0 due" none-state is when there are due-eligible cards resting without cooldown end — keep parity with today's behavior (do not invent new conditions; mirror the existing branch logic).

"Add a card" accessible-name CONSTRAINT (e2e): e2e/04-manual-card-entry.spec.ts uses getByRole("link", { name: "Add a card" }) from the EMPTY-deck state (card-list.tsx renders "+ Add a card", which substring-matches). This plan's populated action-line "Add a card" pill must ALSO be a link whose accessible name matches "Add a card" (the pill text is exactly "Add a card"). Add data-testid="add-a-card" to the populated pill for safety. Do NOT remove "Add a card" — only "Browse words" is removed (L-05).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire HabitatHero + build the Option-D action line + StatusText state machine (DSH-02/03, L-05)</name>
  <files>src/components/deck-view.tsx, src/components/deck-view.test.tsx</files>
  <behavior>
    - When hasDueCards: "Start studying" is the active amber primary and is a <Link href="/study?deck=..."> ; the status shows an amber dot + "{N} due".
    - When !hasDueCards && !earliestCooldownEnd (cards present, none due, not all-paused): "Start studying" is dimmed/disabled; the status shows an outline dot + "0 due".
    - When earliestCooldownEnd && !hasDueCards: "Start studying" is dimmed; the status shows the napping glyph + "Resting · {countdown}" using the existing CountdownTimer value; the hero receives sleeping=true.
    - When all cards are paused (hasCards && !hasDueCards && !earliestCooldownEnd, the existing all-paused branch): the status shows the pause glyph + "All paused" (the old "All cards are paused — unpause one to study." paragraph no longer renders).
    - "Browse words" never renders in the populated action line; "Add a card" renders as a link named "Add a card".
  </behavior>
  <read_first>
    - src/components/deck-view.tsx (self — current CountdownTimer lines 44-92, renderStudyButton lines 133-157, render lines 159-219; the blocks to remove)
    - src/components/habitat-hero.tsx + src/components/habitat-medallion.tsx (Plan 03 — the hero props)
    - src/lib/habitat-engine.ts (HabitatState — already imported)
    - src/components/__tests__/level-up-overlay.test.tsx (jsdom docblock + @testing-library/react + afterEach(cleanup) + the router-mock convention)
    - design/handoff-daybreak/daybreak-dashboard.jsx lines 164-215 (StudyButton, StatusText, ActionLine, "Add a card" pill)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/components/deck-view.tsx" (the action-line replacement targets + lines to remove)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"Pattern 5" (StatusText 4-state machine) + §"Anti-Patterns" (don't re-add Browse words; don't move Resting to hero)
  </read_first>
  <action>
    Rewrite the `<main>` body of `DeckView`. Replace the `<HabitatWidget .../>` block with `<HabitatHero habitatState={habitatState} celebratingLevel={celebratingLevel} sleeping={Boolean(earliestCooldownEnd && !hasDueCards)} />` and remove the `import { HabitatWidget }` (now unused). 
    Build the Option-D action line as a small set of local components/JSX (StudyButton + StatusText + the "Add a card" pill), matching the mock tokens. StudyButton: full-width, height 58, radius 14, font-display 21px bold; when `hasDueCards` render it as a `<Link href={"/study?deck=" + activeDeckId}>` with the active amber style + shadow; otherwise render a non-interactive dimmed element (bg #F4E7D2, color #B49B78, `aria-disabled` / not a link). Reuse the existing `hasCards` guard (no action line content beyond the empty-deck state when `!hasCards` — the empty-deck CardList state owns that, L-03).
    StatusText: implement the four-state machine from the mapping in <interfaces>. For the cooldown state, render the napping LionFace(22)+"z" glyph + "Resting · {countdown}" where `{countdown}` comes from the PRESERVED CountdownTimer logic — keep `formatCountdown` + the `useEffect` 60s interval + `router.refresh()` on expiry intact; only change what CountdownTimer renders (to the "Resting · …" status text) or lift its string into the status row (planner's discretion). For the paused state render the two-bar pause glyph + "All paused" (this REPLACES the removed `<p>All cards are paused — unpause one to study.</p>` — L-05/D-06 sibling). For due/none render the dot (amber filled if N>0 else outline) + "{N} due".
    "Add a card": a pill-styled `<Link href={"/deck/new-card?deck=" + activeDeckId}>` (height 40, radius 12, border 1.5px #EDDFC9, bg white, PlusGlyph + text exactly "Add a card") with `data-testid="add-a-card"`. REMOVE the "Browse words" `<Link>` entirely from this populated view (L-05) — it must NOT render here (it stays only in card-list.tsx's empty-deck state). 
    Keep `<AppHeader .../>`, `<CardList .../>` (Plan 05 turns it into the accordion — do not change its usage here beyond passing the same props), `handleDeckChange`, `nativeLangLabel`/`targetLangLabel`, and the `celebratingLevel` plumbing.
    Then WRITE `src/components/deck-view.test.tsx` (`// @vitest-environment jsdom`, `afterEach(cleanup)`, mock `next/navigation` useRouter, mock `@/components/habitat-hero` + `@/components/card-list` + `@/components/app-header` as lightweight stubs so the test focuses on the action line). Tests: (1) hasDueCards → "Start studying" is a link to /study and status shows "{N} due" with N>0; (2) none-due (no cooldown, not all-paused) → status shows "0 due" and Start studying is not a link; (3) cooldown (earliestCooldownEnd set, !hasDueCards) → status shows "Resting" + a countdown substring and Start studying is dimmed; (4) all-paused branch → status shows "All paused" and the old paragraph text "All cards are paused — unpause one to study." is ABSENT; (5) "Browse words" is NOT in the document and "Add a card" link IS present.
  </action>
  <verify>
    <automated>npx vitest run src/components/deck-view.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HabitatWidget" src/components/deck-view.tsx` returns 0; `grep -c "HabitatHero" src/components/deck-view.tsx` returns ≥1 (L-04 wiring).
    - `grep -c "Browse words" src/components/deck-view.tsx` returns 0 (L-05); `grep -c "All cards are paused" src/components/deck-view.tsx` returns 0 (folded into "All paused").
    - `grep -c "All paused" src/components/deck-view.tsx` returns ≥1 and `grep -c "Resting" src/components/deck-view.tsx` returns ≥1 (status states).
    - `grep -c "router.refresh" src/components/deck-view.tsx` returns ≥1 (CountdownTimer expiry behavior preserved).
    - `grep -c "data-testid=\"add-a-card\"" src/components/deck-view.tsx` returns ≥1 and the pill text is exactly "Add a card".
    - All five tests in `deck-view.test.tsx` pass via `npx vitest run`.
  </acceptance_criteria>
  <done>DeckView renders the HabitatHero (napping during cooldown), the Option-D action line with the four-state StatusText, "Add a card" (no "Browse words"); CountdownTimer expiry logic preserved; unit test green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite/retarget e2e — 09-language-breakdown (feature removal), 12-pause-cards, 07-habitat-display (L-06)</name>
  <files>e2e/09-language-breakdown.spec.ts, e2e/12-pause-cards.spec.ts, e2e/07-habitat-display.spec.ts</files>
  <read_first>
    - e2e/09-language-breakdown.spec.ts (tests 1 & 3 assert "My Deck" — removed by D-02; test 2 uses the empty-deck "Browse words" — still valid)
    - e2e/12-pause-cards.spec.ts (lines 20 comment, 150, 159 — "All cards are paused — unpause one to study." copy; verify the desktopTable scoping is unaffected)
    - e2e/07-habitat-display.spec.ts (line 10 "Level 1"; line 11 `.bg-secondary.rounded-full` progress bar; line 12 `/\/.*cards/` regex)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"e2e/*.spec.ts" (retarget map)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"Feature Removal — e2e/09-language-breakdown.spec.ts" (the exact rewrite for tests 1 & 3)
  </read_first>
  <action>
    Retarget/rewrite the specs this plan's changes break, PRESERVING behavioral intent (never re-add removed UI, never weaken assertions).
    `e2e/09-language-breakdown.spec.ts` — FEATURE REMOVAL (D-02): rewrite test 1 ("dashboard shows per-language learned count") to keep its setup (signUpWithDeck + addWordsFromBrowser) but assert the NEW surface for the learned-count concept — the "Your words" accordion header showing a learned count, e.g. `page.getByText(/\d+ learned/)` (this header is added in Plan 05; the assertion targets the accordion header copy "N learned"). Remove the `getByText("My Deck")` assertions (lines 8, 35). For test 3 ("habitat widget shows on dashboard alongside deck"), drop the "My Deck" assertion and keep a habitat assertion that survives the re-skin — e.g. `page.getByText(/Level \d+/)` or `page.locator('a[href*="/habitat"]')`. Test 2 (empty-deck "Browse words" link) is unchanged. Rename the describe block to reflect the remaining scope (e.g. "Dashboard — habitat hero and deck integration"). If the "N learned" assertion in test 1 depends on Plan 05's accordion header, scope it to `data-testid="words-accordion-header"` (Plan 05 adds that testid) — coordinate the exact testid with Plan 05; if uncertain, assert the regex `/\d+ learned/` which Plan 05's header text satisfies.
    `e2e/12-pause-cards.spec.ts` lines 150, 159: replace `page.getByText("All cards are paused — unpause one to study.")` with `page.getByText("All paused")`. Update the line-20 doc comment to match. Verify the surrounding `desktopTable`/`table tbody` scoping is untouched (this plan does not change that).
    `e2e/07-habitat-display.spec.ts` test "habitat widget is visible on dashboard": line 11's `page.locator(".bg-secondary.rounded-full")` targets the OLD widget's progress bar (gone — the HabitatHero uses a conic ring, no `.bg-secondary.rounded-full`). Retarget this assertion to the new hero — assert the medallion is visible via `page.getByTestId("habitat-medallion")` (Plan 03 adds that testid) OR the hero link `page.locator('a[href*="/habitat"]')`. Line 12's `getByText(/\/.*cards/)` → retarget to `getByText(/\d+ of \d+ cards/)` (the new "X of Y cards to Level N+1" copy). Line 10's `getByText("Level 1")` still substring-matches "Habitat · Level 1" — leave it, but if Playwright strict-mode complains use `getByText(/Level 1/)`.
    After edits, grep the WHOLE e2e/ dir again for any remaining `"My Deck"`, `"All cards are paused`, `.bg-secondary.rounded-full`, or `/\/.*cards/` and fix stragglers.
  </action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "My Deck" e2e/` returns 0 across the dir (D-02 feature removal complete).
    - `grep -rc "All cards are paused" e2e/` returns 0; `e2e/12-pause-cards.spec.ts` asserts `"All paused"`.
    - `grep -rc "bg-secondary.rounded-full" e2e/` returns 0 (old widget progress-bar selector gone).
    - `e2e/07-habitat-display.spec.ts` uses `/\d+ of \d+ cards/` (not `/\/.*cards/`).
    - `e2e/09-language-breakdown.spec.ts` describe block is renamed and test 1 asserts the new learned-count surface (regex `/\d+ learned/` or the accordion testid) instead of "My Deck"; intent preserved (cards added → learned count visible).
    - NOTE: full Playwright validation runs at the Wave-2 boundary after `npm run dev` restart, together with Plan 02's specs and Plan 05's accordion (the "N learned" header dependency). Sequence Plan 05 before this e2e gate or run the gate once all Wave-2 plans land.
  </acceptance_criteria>
  <done>09-language-breakdown is reworked for the D-02 removal (asserts the new learned-count surface), 12-pause-cards asserts "All paused", and 07-habitat-display targets the new hero (medallion testid + "X of Y cards"); no stale literals remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /study, /deck/new-card navigation | Plain Next.js `<Link>`s; routes are already `(protected)` |
| client → CountdownTimer router.refresh() | Re-fetches the server-computed due/cooldown state on cooldown expiry (existing behavior) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-T | Tampering | StudyButton gating (due vs dimmed) | accept (preserved) | The "due/not-due" decision is computed server-side (`assembleSession`/`earliestCooldownEnd`) and passed as `hasDueCards`/`earliestCooldownEnd`; the client only chooses link-vs-dimmed. Navigating to /study directly is already gated by the study route's own server checks (unchanged). |
| T-21-04-D | Denial of Service | CountdownTimer interval | accept (preserved) | The 60s interval + single `router.refresh()` on expiry is the existing logic, unchanged; no new polling added. |
| T-21-04-I | Information Disclosure | Status row counts | accept | Shows the user's own due-count/cooldown only — same data the prior UI exposed. |
| T-21-04-SC | Tampering | npm installs | accept | No packages installed. No `[ASSUMED]`/`[SUS]` packages; legitimacy gate N/A. |

No HIGH-severity threats — the action line re-renders server-trusted state; all gating decisions remain server-side.
</threat_model>

<verification>
- `npx vitest run src/components/deck-view.test.tsx` green (5 action-line cases).
- `npx tsc --noEmit` clean for deck-view.tsx.
- e2e retarget greps clean (no "My Deck" / "All cards are paused" / `.bg-secondary.rounded-full` / `/\/.*cards/`).
- Wave-2 boundary (after `npm run dev` restart): `npm run test:e2e` green for 09/12/07 (with Plan 02 + Plan 05).
- Manual (phase UAT): cycle due / none / resting / all-paused — Start studying dims correctly, status row matches the mock, no "Browse words" in the populated view, hero naps over a real ring during cooldown.
</verification>

<success_criteria>
- DSH-03: full-width "Start studying" (dims when nothing due) + adaptive status row ("N due"/"0 due"/"Resting · countdown"/"All paused") + "Add a card".
- DSH-02 wiring: HabitatHero replaces the legacy widget; cooldown passes sleeping=true (D-06 sibling).
- L-05: "Browse words" gone from the populated action line; "All cards are paused" folded into "All paused".
- DSH-07: cards-due / none-due / resting / all-paused states render in Daybreak (empty-deck/brand-new-user via the existing card-list empty state).
- SRS/CountdownTimer/redirect/QA-badge behavior preserved.
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-my-deck/21-04-SUMMARY.md` when done.
</output>
