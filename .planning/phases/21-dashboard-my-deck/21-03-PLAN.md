---
phase: 21-dashboard-my-deck
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/habitat-medallion.tsx
  - src/components/habitat-hero.tsx
  - src/components/habitat-medallion.test.tsx
autonomous: true
requirements: [DSH-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The habitat hero shows Leo on a sunrise disc with a conic progress ring + level badge, the 'Habitat · Level N' title, and 'X of Y cards to Level N+1' (computed from the real engine state)"
    - "At max level (L9, where nextLevelThreshold === null) the medallion uses the gold ring + gold badge, the subtitle reads 'Course 1 complete', and the 'X of Y cards' line is hidden — D-05"
    - "The mock's level >= 10 gold branch is dead code here (engine caps at L9); the medallion uses level >= 9 / nextLevelThreshold === null as the canonical max-level signal — D-05"
    - "During cooldown only, the hero shows the napping treatment (dimmed face + 'z' + recharging cue) WHILE the conic ring stays accurate (NOT zeroed) and the 'X of Y cards' line stays visible; the countdown is NOT shown on the hero — D-06"
    - "The hero links to /habitat (preserving the celebrate query param when celebratingLevel is set)"
  artifacts:
    - path: "src/components/habitat-medallion.tsx"
      provides: "Conic-ring medallion (progress ring, level badge, sleeping + max-level variants)"
      contains: "nextLevelThreshold"
    - path: "src/components/habitat-hero.tsx"
      provides: "Daybreak hero card wrapping the medallion + title/subtitle + View habitat link"
      contains: "Course 1 complete"
    - path: "src/components/habitat-medallion.test.tsx"
      provides: "Unit test — ring math, L9 max (gold + Course 1 complete + no next-line), cooldown ring-not-zeroed"
  key_links:
    - from: "src/components/habitat-medallion.tsx"
      to: "src/lib/habitat-engine.ts"
      via: "LEVEL_THRESHOLDS + HabitatState type (read-only)"
      pattern: "LEVEL_THRESHOLDS"
    - from: "src/components/habitat-hero.tsx"
      to: "/habitat"
      via: "next/link href"
      pattern: "/habitat"
---

<objective>
Build the Daybreak habitat hero (DSH-02): a new `HabitatMedallion` (Leo on a sunrise disc with a CSS conic-gradient progress ring + level badge) and a `HabitatHero` card that wraps it with the "Habitat · Level N" title, the "X of Y cards to Level N+1" subtitle, and a "View habitat" link. These replace the legacy 80px .webp thumbnail widget (L-04) — but the actual swap into `deck-view.tsx` happens in Plan 04 (this plan only creates the components + their unit test, with zero deck-view edits, so it can run in Wave 1 with no file conflicts).

Purpose: DSH-02 — the focal habitat hero. Implements D-05 (L9 max → gold + "Course 1 complete", next-line hidden) and D-06 (cooldown = napping Leo over a STILL-ACCURATE ring; countdown lives only in Plan 04's action line).
Output: `habitat-medallion.tsx`, `habitat-hero.tsx`, and `habitat-medallion.test.tsx` (the Nyquist Wave-0 net-new behavior test for DSH-02).

PRESENTATION-ONLY: reads `HabitatState` (from `habitat-engine.ts`) as a prop; the engine and its L9 cap are NOT modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-my-deck/21-CONTEXT.md
@.planning/phases/21-dashboard-my-deck/21-RESEARCH.md
@.planning/phases/21-dashboard-my-deck/21-PATTERNS.md
@AGENTS.md

# Design contract (--skip-ui; the JSX mock IS the spec) + the source-of-truth engine
@design/handoff-daybreak/daybreak-dashboard.jsx
@src/lib/habitat-engine.ts

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + design contract. -->

HabitatState (src/lib/habitat-engine.ts lines 80-100), the prop source:
  { level: number (1-9, engine caps at 9 via Math.min(9, level));
    learnedCardCount: number;
    nextLevelThreshold: number | null;  // null when level === 9 (LEVEL_THRESHOLDS[8] is undefined → ?? null)
    quality, mood, effectiveCardCount, isDecaying, minutesSinceActivity }
  LEVEL_THRESHOLDS = [5, 15, 30, 50, 80, 120, 170, 230]  (8 entries, indices 0-7)

CONFIRMED engine facts (do NOT re-derive against the mock's dead branches):
  - Max level is 9. nextLevelThreshold === null exactly when level === 9.
  - Progress math (mirrors habitat-widget.tsx lines 27-41):
      if (nextLevelThreshold === null) progress = 1;
      else { prev = level >= 2 ? (LEVEL_THRESHOLDS[level-2] ?? 0) : 0;
             range = nextLevelThreshold - prev;
             progress = range > 0 ? clamp01((learnedCardCount - prev) / range) : 1; }

Design-contract values (daybreak-dashboard.jsx):
  HabitatMedallion (lines 63-79): ring conic-gradient(#F28A1F deg, #F3E3C6 deg); inner sunrise disc inset 6, gradient #FFE7BC→#FFFDF8; LionFace size*0.56; sun dot; level badge bottom-right (radius 999, border 3px #FFF6E9)
  Mock max branch is `level >= 10` (lines 65, 76) → DEAD here; retarget to nextLevelThreshold===null / level>=9 (D-05)
  Mock gold ring #F2B33A; gold badge bg #F2B33A; sleeping badge bg #C9B79A content "•"; sleeping ring is greyed #F3E3C6 with progress 0 in the MOCK → OVERRIDE per D-06 (ring stays accurate, only face dims)
  HabitatHero (lines 146-161): card bg linear-gradient(180deg,#FFF3DC 0%,#FFFFFF 70%), border dt.cardBorder, radius 22, shadow "0 10px 26px rgba(160,110,40,0.12)", padding "26px 22px 22px"; title fontDisplay 26px 700; subtitle 14.5px muted; "View habitat" link with right chevron
  Mock hero passes progress={sleeping ? 0 : 0.7} → WRONG per D-06; pass REAL progress always

HabitatWidget current link wrapper to mirror (src/components/habitat-widget.tsx lines 43-49):
  <Link href={celebratingLevel ? "/habitat?celebrate=" + celebratingLevel : "/habitat"} className="block">

LionFace + usePrefersReducedMotion: ring is static CSS — no motion gating needed for the ring or the decorative "z".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HabitatMedallion — conic ring, level badge, max-level (D-05) + cooldown (D-06) variants</name>
  <files>src/components/habitat-medallion.tsx, src/components/habitat-medallion.test.tsx</files>
  <behavior>
    - progressRatio returns 1 when nextLevelThreshold === null (max level), else clamp01((learned - prevThreshold)/range) with prevThreshold = LEVEL_THRESHOLDS[level-2] (or 0 below L2).
    - When nextLevelThreshold === null (L9 max): the ring is the gold solid (#F2B33A) and the level badge background is gold (#F2B33A) — NOT the amber/greyed variants.
    - When sleeping (cooldown): the ring's conic-gradient still reflects the REAL progress degrees (it is NOT forced to 0/greyed); only the inner face/sun dim (reduced opacity) and a "z" mark appears; the badge shows the sleeping treatment.
    - In the normal (non-max, non-sleeping) state: the ring is conic-gradient(amber deg, #F3E3C6 deg) for the real progress and the badge shows the level number on amber.
  </behavior>
  <read_first>
    - src/components/habitat-widget.tsx (the progress math to reuse — lines 27-41 — and the component being replaced)
    - src/lib/habitat-engine.ts (HabitatState type lines 80-100; confirm nextLevelThreshold === null at L9 via lines 259-263; LEVEL_THRESHOLDS line 35; the Math.min(9, level) cap line 164)
    - src/components/daybreak/lion-face.tsx (LionFace props)
    - src/components/__tests__/level-up-overlay.test.tsx (the jsdom docblock + @testing-library/react + afterEach(cleanup) convention to copy for the test)
    - design/handoff-daybreak/daybreak-dashboard.jsx lines 63-79 (HabitatMedallion render — retarget the level>=10 branches)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/components/habitat-medallion.tsx" (props shape + conic-ring render pattern + D-05/D-06 overrides)
    - .planning/phases/21-dashboard-my-deck/21-RESEARCH.md §"Pattern 3" + §"Pitfall 3" (level>=9 not >=10) + §"Pitfall 4" (nextLevelThreshold null at L9)
  </read_first>
  <action>
    Create `src/components/habitat-medallion.tsx` (a client or pure presentational component — no data fetching). Props: `level: number`, `learnedCardCount: number`, `nextLevelThreshold: number | null`, `sleeping?: boolean`, `size?: number` (default 132 for the hero). Import `LEVEL_THRESHOLDS` and the `HabitatState` type from `@/lib/habitat-engine` and `LionFace` from `@/components/daybreak/lion-face`.
    Implement `progressRatio(level, learnedCardCount, nextLevelThreshold)` exactly per the confirmed math above (return 1 when `nextLevelThreshold === null`). Compute `const isMaxLevel = nextLevelThreshold === null;` (canonical max signal per D-05 — equivalent to level >= 9, but use the null check). `const deg = progressRatio(...) * 360;`.
    Ring background: `isMaxLevel ? "#F2B33A" : "conic-gradient(#F28A1F " + deg + "deg, #F3E3C6 " + deg + "deg)"`. D-06 OVERRIDE: when `sleeping`, the ring KEEPS this accurate value (do NOT substitute the greyed `#F3E3C6`/`progress: 0` from the mock). Only dim the inner sunrise disc + sun dot (e.g. opacity 0.45) and render the "z" mark when sleeping.
    Reproduce the medallion structure from the mock (outer ring div, inner sunrise disc inset 6 with the gradient, sun dot, `LionFace size={size*0.56}` with the lion palette, the bottom-right level badge radius 999 border 3px #FFF6E9). Badge background: `sleeping ? "#C9B79A" : (isMaxLevel ? "#F2B33A" : "#F28A1F")`; badge content `sleeping ? "•" : level`. Add `data-testid="habitat-medallion"` on the root and `data-max-level={isMaxLevel}` + `data-sleeping={!!sleeping}` attributes so the test (and the auditor) can assert variant without parsing inline styles. Expose the computed ring `background` so it is assertable (e.g. set it as an inline style on a child with `data-testid="medallion-ring"`).
    Ring is static CSS — do NOT animate it and do NOT import usePrefersReducedMotion (RESEARCH anti-pattern: conic-gradient backgrounds can't transition).
    Then WRITE `src/components/habitat-medallion.test.tsx` with `// @vitest-environment jsdom` + `afterEach(cleanup)`. Tests (assert via testids/data-attributes/computed style, not screenshots): (1) normal mid-level — ring is a conic-gradient (contains "conic-gradient" and the amber #F28A1F), badge shows the level number; (2) progress math — given level/learnedCardCount/nextLevelThreshold, the ring degrees reflect clamp01 math (e.g. learned at prevThreshold → ~0deg amber arc; at/above nextLevelThreshold → full); (3) L9 max (`nextLevelThreshold={null}`) — `data-max-level="true"`, ring background is the gold `#F2B33A` solid (NOT a conic-gradient), badge shows "9"; (4) cooldown (`sleeping`) — `data-sleeping="true"`, ring STILL contains "conic-gradient" with the real degrees (assert it is NOT the flat greyed `#F3E3C6` solid and NOT forced to 0deg) — this guards D-06; the "z" mark is present.
  </action>
  <verify>
    <automated>npx vitest run src/components/habitat-medallion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/habitat-medallion.tsx` exists and imports `LEVEL_THRESHOLDS` from `@/lib/habitat-engine` (`grep -c "LEVEL_THRESHOLDS" src/components/habitat-medallion.tsx` ≥ 1).
    - The max-level branch keys off `nextLevelThreshold === null` / `level >= 9`, NOT `level >= 10`: `grep -c ">= 10\|>=10\|=== 10" src/components/habitat-medallion.tsx` returns 0 (D-05 — the dead mock branch is not copied).
    - All four tests in `habitat-medallion.test.tsx` pass via `npx vitest run`.
    - Test 4 explicitly asserts the cooldown ring is NOT zeroed/greyed (D-06 guard) — verifiable in the test source.
  </acceptance_criteria>
  <done>HabitatMedallion renders the conic ring + badge with correct progress math, the L9 gold max variant (no `>=10`), and the cooldown variant that keeps the ring accurate; the four-case unit test is green.</done>
</task>

<task type="auto">
  <name>Task 2: HabitatHero — Daybreak card + title/subtitle + max-level + napping copy (DSH-02, D-05/D-06)</name>
  <files>src/components/habitat-hero.tsx, src/components/habitat-medallion.test.tsx</files>
  <read_first>
    - src/components/habitat-widget.tsx (the Link wrapper + celebrate query param to mirror — lines 43-49)
    - src/components/habitat-medallion.tsx (the component created in Task 1 — its props)
    - src/lib/habitat-engine.ts (HabitatState fields used: level, learnedCardCount, nextLevelThreshold)
    - design/handoff-daybreak/daybreak-dashboard.jsx lines 146-161 (HabitatHero card + title + subtitle + View habitat link)
    - .planning/phases/21-dashboard-my-deck/21-PATTERNS.md §"src/components/habitat-hero.tsx" (card wrapper + subtitle copy patterns)
  </read_first>
  <action>
    Create `src/components/habitat-hero.tsx` (a client component — it renders a `next/link`). Props: `habitatState: HabitatState` and `celebratingLevel?: number | null`; optionally a `sleeping?: boolean` flag (the cooldown state) — the cooldown signal is owned by `deck-view.tsx` (Plan 04 passes it based on `earliestCooldownEnd && !hasDueCards`); accept it as a prop defaulting to `false`.
    Render a `<Link href={celebratingLevel ? "/habitat?celebrate=" + celebratingLevel : "/habitat"} className="block">` wrapping a Daybreak hero card (bg `linear-gradient(180deg, #FFF3DC 0%, #FFFFFF 70%)`, border `1px solid #F0E3CF`, borderRadius 22, boxShadow `0 10px 26px rgba(160,110,40,0.12)`, padding `26px 22px 22px`, centered column). Inside: `<HabitatMedallion size={132} level={level} learnedCardCount={learnedCardCount} nextLevelThreshold={nextLevelThreshold} sleeping={sleeping} />` (pass the REAL state — never `progress: 0` — D-06).
    Title: `font-display text-[26px] font-bold text-foreground` reading `Habitat · Level {level}` (use a real middot).
    Subtitle (per D-05/D-06): if `nextLevelThreshold === null` → render exactly `Course 1 complete`; else if `sleeping` → render an encouraging recharging line (e.g. "Your lion is napping · cards recharging") with the progress line STILL implied by the accurate ring (do NOT render the countdown here — D-06; countdown is Plan 04's status row); else → render `{learnedCardCount} of {nextLevelThreshold} cards to Level {level + 1}` (with the count emphasized). Add `data-testid="habitat-hero-subtitle"`.
    "View habitat" link affordance: a row reading `View habitat` + a right chevron, colored with the Daybreak link color (#C96F12). The whole card is already a link; this is the visual cue.
    Do NOT render the cooldown countdown anywhere in the hero (D-06). Do NOT duplicate the "Resting · …" text here.
    Then ADD hero-level tests to `habitat-medallion.test.tsx` (same file, new describe block) OR a sibling — keep it in `habitat-medallion.test.tsx` to satisfy the VALIDATION map's single command. Tests: (a) renders a link with href `/habitat`; (b) at L9 (`nextLevelThreshold: null`) the subtitle reads "Course 1 complete" and the "X of Y cards" line is absent; (c) normal level renders "{n} of {m} cards to Level {level+1}"; (d) sleeping=true does NOT render any "Resting" / countdown text in the hero (D-06).
  </action>
  <verify>
    <automated>npx vitest run src/components/habitat-medallion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/habitat-hero.tsx` renders a `<Link>` to `/habitat` (`grep -c "/habitat" src/components/habitat-hero.tsx` ≥ 1) and preserves the `?celebrate=` param when `celebratingLevel` is set.
    - `grep -c "Course 1 complete" src/components/habitat-hero.tsx` returns ≥ 1 (D-05).
    - `grep -c "Resting\|countdown\|2h 15m" src/components/habitat-hero.tsx` returns 0 (D-06 — countdown is NOT on the hero).
    - The hero passes real `nextLevelThreshold`/`learnedCardCount` to `HabitatMedallion` (no `progress={0}` / `progress: 0` literal): `grep -c "progress" src/components/habitat-hero.tsx` returns 0.
    - All hero tests in `habitat-medallion.test.tsx` pass via `npx vitest run`.
  </acceptance_criteria>
  <done>HabitatHero wraps the medallion in a Daybreak card linking to /habitat, shows "Habitat · Level N" + the correct subtitle ("Course 1 complete" at L9, real "X of Y cards" otherwise, napping cue when sleeping with NO countdown); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client prop (HabitatState) | `habitatState` is computed server-side by the trusted `computeHabitatState` and passed read-only; this plan only renders it |
| client → /habitat navigation | A plain Next.js `<Link>`; no privileged action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-T | Tampering | HabitatMedallion ring math | accept (preserved) | Pure read-only render of the server-computed `HabitatState`; no client mutation of level/progress. The `celebrate` query param is display-only (existing behavior, already clamped server-side in dashboard/page.tsx). |
| T-21-03-I | Information Disclosure | Hero subtitle (card counts) | accept | Exposes only the user's own learned-card count + level — same data the legacy widget already displayed; no cross-user data. |
| T-21-03-SC | Tampering | npm installs | accept | No packages installed. No `[ASSUMED]`/`[SUS]` packages; legitimacy gate N/A. |

No HIGH-severity threats — a presentation component rendering already-trusted server state.
</threat_model>

<verification>
- `npx vitest run src/components/habitat-medallion.test.tsx` green (medallion 4 cases + hero cases).
- `npx tsc --noEmit` clean for the two new components.
- Manual (phase UAT): hero matches the mock at a mid level; L9 shows gold + "Course 1 complete" with no next-line; cooldown shows napping Leo over a real progress ring and NO countdown on the hero.
</verification>

<success_criteria>
- DSH-02: a habitat hero medallion with conic progress ring + level badge, "Habitat · Level N", "X of Y cards to Level N+1" (absent at max), linking to /habitat.
- D-05 implemented: L9 → gold ring/badge + "Course 1 complete" + next-line hidden; `>= 10` dead branch not copied.
- D-06 implemented: cooldown = napping Leo over a STILL-ACCURATE ring; countdown not on the hero.
- Wave-0 net-new behavior test for DSH-02 exists and is green.
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-my-deck/21-03-SUMMARY.md` when done.
</output>
