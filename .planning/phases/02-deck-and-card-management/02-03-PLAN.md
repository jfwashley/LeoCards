---
phase: 02-deck-and-card-management
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/(protected)/dashboard/page.tsx
  - src/app/(protected)/layout.tsx
  - src/components/app-header.tsx
  - src/components/deck-switcher.tsx
  - src/components/card-list.tsx
  - src/components/card-edit-dialog.tsx
  - src/components/first-visit-picker.tsx
autonomous: false
requirements:
  - DECK-04
  - DECK-05
  - DECK-06
must_haves:
  truths:
    - "User with no decks sees first-visit language picker and can create their first deck"
    - "User can switch between decks via header dropdown"
    - "User can create a new deck from the header dropdown"
    - "User can see all cards in the selected deck as a searchable list"
    - "User can click a card to open edit dialog, change front/back, and save"
    - "User can delete a card from the edit dialog with confirmation"
    - "Deck switcher shows flag emoji + auto-name for each deck"
  artifacts:
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Deck management view replacing Phase 1 stub"
      min_lines: 30
    - path: "src/components/app-header.tsx"
      provides: "App header with deck switcher + logout"
    - path: "src/components/deck-switcher.tsx"
      provides: "shadcn Select dropdown for switching/creating decks"
    - path: "src/components/card-list.tsx"
      provides: "Card list table with search and edit trigger"
    - path: "src/components/card-edit-dialog.tsx"
      provides: "Edit modal with save/delete functionality"
    - path: "src/components/first-visit-picker.tsx"
      provides: "Language picker for first-time users"
  key_links:
    - from: "src/app/(protected)/dashboard/page.tsx"
      to: "src/lib/deck-queries.ts"
      via: "Server-side data loading"
      pattern: "getUserDecks|getDeckCards"
    - from: "src/components/deck-switcher.tsx"
      to: "src/lib/deck-actions.ts"
      via: "createDeck server action"
      pattern: "createDeck"
    - from: "src/components/card-edit-dialog.tsx"
      to: "src/lib/deck-actions.ts"
      via: "editCard and deleteCard server actions"
      pattern: "editCard|deleteCard"
    - from: "src/components/card-list.tsx"
      to: "src/components/card-edit-dialog.tsx"
      via: "onClick opens edit dialog with selected card"
      pattern: "setEditCard"
---

<objective>
Dashboard UI with deck switcher, card list, card editing, and first-visit experience.

Purpose: Replace the Phase 1 dashboard stub with the full deck management view. Users can switch decks, browse their cards, edit/delete cards, and first-time users see a language picker to create their initial deck.
Output: Working dashboard with header, deck switcher, card list with search, edit/delete dialog, first-visit picker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deck-and-card-management/02-CONTEXT.md
@.planning/phases/02-deck-and-card-management/02-UI-SPEC.md
@.planning/phases/02-deck-and-card-management/02-RESEARCH.md
@.planning/phases/02-deck-and-card-management/02-01-SUMMARY.md
@.planning/phases/02-deck-and-card-management/02-02-SUMMARY.md

<interfaces>
<!-- Server Actions from Plan 02 -->
From src/lib/deck-actions.ts:
```typescript
"use server";
export async function createDeck(language: string): Promise<{ id: DeckId; name: string; language: string }>;
export async function saveCard(deckId: string, front: string, back: string, source: "manual" | "wordlist"): Promise<{ id: string }>;
export async function editCard(cardId: string, front: string, back: string): Promise<void>;
export async function deleteCard(cardId: string): Promise<void>;
```

From src/lib/deck-queries.ts:
```typescript
export async function getUserDecks(userId: string): Promise<typeof decks.$inferSelect[]>;
export async function getDeckCards(deckId: string): Promise<typeof cards.$inferSelect[]>;
export async function getUserNativeLanguage(userId: string): Promise<string>;
```

From src/components/ui/select.tsx (shadcn):
```typescript
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
```

From src/components/ui/dialog.tsx (shadcn):
```typescript
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";
```

From src/components/logout-button.tsx:
```typescript
export function LogoutButton(): JSX.Element;
```

From src/db/schema.ts:
```typescript
export type DeckId = Brand<string, "DeckId">;
export type CardId = Brand<string, "CardId">;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App header, deck switcher, first-visit picker, and dashboard page shell</name>
  <files>src/app/(protected)/dashboard/page.tsx, src/app/(protected)/layout.tsx, src/components/app-header.tsx, src/components/deck-switcher.tsx, src/components/first-visit-picker.tsx</files>
  <read_first>
    - src/app/(protected)/dashboard/page.tsx (current stub — must see what to replace)
    - src/app/(protected)/layout.tsx (protected layout with session check — must see how session is passed)
    - src/components/logout-button.tsx (existing pattern for client component with auth)
    - src/components/ui/select.tsx (shadcn Select component — must see exact export names and API)
    - src/components/ui/button.tsx (shadcn Button — must see variant props)
    - src/lib/deck-queries.ts (getUserDecks, getDeckCards, getUserNativeLanguage — Plan 02 output)
    - src/lib/deck-actions.ts (createDeck — Plan 02 output)
    - .planning/phases/02-deck-and-card-management/02-UI-SPEC.md (header layout, deck switcher, first-visit picker specs)
    - AGENTS.md (Next.js breaking change warnings)
    - node_modules/next/dist/docs/01-app/02-guides/forms.md (Server Actions from client components)
  </read_first>
  <action>
    1. **Update `src/app/(protected)/layout.tsx`:**
       - Keep the existing session check and redirect logic
       - Wrap children with the AppHeader component (passes session and decks data)
       - Load user's decks server-side: `const userDecks = await getUserDecks(session.user.id)`
       - Load user's native language: `const nativeLang = await getUserNativeLanguage(session.user.id)`
       - Pass `session`, `decks`, `nativeLang` as props to AppHeader
       - Structure: `<AppHeader ... />` above `{children}` in a flex column layout

    2. **Create `src/components/app-header.tsx`** — client component (`"use client"`):
       - Fixed top header: `h-14 bg-background border-b border-border flex items-center px-8`
       - Left: TioCards wordmark — `<span className="text-sm font-semibold text-foreground">TioCards</span>` + tiger emoji `<span className="text-lg">🐯</span>`
       - Center-right: DeckSwitcher component (renders only if decks.length > 0)
       - Far right: LogoutButton component
       - Props: `{ decks: Deck[], activeDeckId: string | null, onDeckChange: (id: string) => void, nativeLang: string }`
       - The header uses a flex layout with `justify-between` and `items-center`

    3. **Create `src/components/deck-switcher.tsx`** — client component (`"use client"`):
       - Uses shadcn `<Select>` with controlled value = activeDeckId
       - Trigger: `min-w-[160px]` showing flag emoji + deck name
       - Items: each deck as `<SelectItem value={deck.id}>{flag} {deck.name}</SelectItem>`
       - Flag mapping: `{ en: "🇬🇧", fr: "🇫🇷", es: "🇪🇸" }`
       - Separator before last item
       - Last item: `<SelectItem value="__new__">+ New deck</SelectItem>` with lucide `Plus` icon
       - When value changes to "__new__": show inline language picker (popover or inline options within the dropdown)
       - When a real deck.id is selected: call onDeckChange(id)
       - New deck creation: call `createDeck(language)` server action, then update parent state
       - Languages shown exclude user's native language (passed as prop)

    4. **Create `src/components/first-visit-picker.tsx`** — client component (`"use client"`):
       - Full page centered card: `max-w-sm mx-auto rounded-xl shadow-sm p-6`
       - Tiger emoji 🐯 above heading
       - Heading: "What language do you want to learn?" (text-xl font-semibold)
       - Language buttons stacked vertically: flag emoji + language name
       - Each button: `variant="outline"` `h-11 w-full`
       - Exclude user's native language from options
       - On click: set loading state (spinner + disabled), call `createDeck(language)` server action
       - On success: trigger page refresh (router.refresh()) — dashboard will now see the deck and show the card list view
       - On error: inline error below buttons: "Something went wrong. Try again."
       - Languages: English/French/Spanish with codes "en"/"fr"/"es"

    5. **Rewrite `src/app/(protected)/dashboard/page.tsx`** — server component:
       - Load session via `auth.api.getSession({ headers: await headers() })`
       - Load decks via `getUserDecks(session.user.id)`
       - If no decks: render `<FirstVisitPicker nativeLang={nativeLang} />`
       - If decks exist: render the DeckView client component that manages active deck state
       - DeckView receives: `decks`, `initialCards` (for the first/active deck), `nativeLang`
       - The active deck defaults to the first deck (decks[0])
       - When deck changes in the switcher, the page re-fetches cards (use router.refresh or a client-side fetch pattern)
       - Page heading: "My Deck" (text-xl font-semibold)
       - Action buttons (right-aligned above card list): "Browse words" (Link to /deck/browse) and "Add a card" (Link to /deck/new-card), both `variant="outline"`
       - Below actions: CardList component with search and card data

    Note on deck switching: The simplest approach is to use a client-side state for activeDeckId and fetch cards via a server action or API route when the deck changes. Alternatively, use URL search params (?deck=id) so the server component can load the right cards. Choose whichever approach works cleanly — URL params is preferred for shareability and server-side rendering.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/TioCards" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/dashboard/page.tsx exists and does NOT contain "Your habitat is being built" (old stub removed)
    - grep -q "getUserDecks" src/app/(protected)/dashboard/page.tsx returns 0
    - grep -q "FirstVisitPicker" src/app/(protected)/dashboard/page.tsx returns 0
    - src/components/app-header.tsx exists with "use client" directive
    - grep -q "TioCards" src/components/app-header.tsx returns 0 (wordmark)
    - grep -q "LogoutButton" src/components/app-header.tsx returns 0
    - src/components/deck-switcher.tsx exists with "use client" directive
    - grep -q "SelectTrigger" src/components/deck-switcher.tsx returns 0 (uses shadcn Select)
    - grep -q "__new__" src/components/deck-switcher.tsx returns 0 (new deck option)
    - grep -q "createDeck" src/components/deck-switcher.tsx returns 0
    - src/components/first-visit-picker.tsx exists with "use client" directive
    - grep -q "What language do you want to learn" src/components/first-visit-picker.tsx returns 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Dashboard page renders first-visit picker for new users or deck management view for users with decks. Header shows TioCards wordmark, deck switcher with flag emojis, and logout. Deck switcher allows switching and creating new decks.</done>
</task>

<task type="auto">
  <name>Task 2: Card list with search and card edit/delete dialog</name>
  <files>src/components/card-list.tsx, src/components/card-edit-dialog.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (shadcn Dialog — must see exact export names and API: Dialog, DialogContent, DialogHeader, DialogTitle, etc.)
    - src/components/ui/input.tsx (shadcn Input component)
    - src/components/ui/button.tsx (shadcn Button — variant props)
    - src/lib/deck-actions.ts (editCard, deleteCard — Plan 02 output)
    - .planning/phases/02-deck-and-card-management/02-UI-SPEC.md (card list table spec, card edit dialog spec, delete confirmation spec, copywriting contract)
    - .planning/phases/02-deck-and-card-management/02-CONTEXT.md (card list: search bar, source indicator, review queue sort; card editing: modal, both fields editable, delete with confirmation)
  </read_first>
  <action>
    1. **Create `src/components/card-list.tsx`** — client component (`"use client"`):

       Props: `{ cards: CardRow[], nativeLangLabel: string, targetLangLabel: string }` where CardRow = `{ id: string, front: string, back: string, source: string, createdAt: Date }`

       **Search bar:**
       - Full-width shadcn `<Input>` above the table
       - Left adornment: lucide `Search` icon (use absolute positioning or input wrapper)
       - Right: lucide `X` clear button, visible only when query is non-empty
       - Placeholder: "Search your cards..."
       - Client-side contains-match filtering on both `front` and `back` fields
       - No debounce needed (local filtering)

       **Empty deck state** (when cards array is empty, no search active):
       - Heading: "Your deck is empty" (text-lg font-semibold)
       - Body: "Browse the word list or add a card to get started."
       - CTA: "Browse words" link button to /deck/browse

       **No search results** (when filter returns 0):
       - "No cards match \"{query}\"."
       - Sub: "Try a different word."

       **Card table:**
       - Column headers: nativeLangLabel | targetLangLabel | "Source" | (action column, no header)
       - Header style: `text-sm text-muted-foreground font-normal`
       - Row: `border-b border-border min-h-[48px] hover:bg-secondary transition-colors`
       - Native word column: `text-base` (16px)
       - Target translation column: `text-base`
       - Source pill: `<span className="bg-muted text-muted-foreground text-xs px-2 py-0.5 rounded-full">{source}</span>` — values "word list" or "manual"
       - Action column: lucide `Pencil` icon button, `variant="ghost"`, `h-11 w-11`, `aria-label="Edit card"`, `opacity-60 hover:opacity-100`
       - Clicking the pencil icon: sets `editCard` state to the card, which opens the CardEditDialog

       **Integration with CardEditDialog:**
       - Render `<CardEditDialog card={editCard} open={editCard !== null} onOpenChange={(open) => { if (!open) setEditCard(null) }} />` within this component
       - When dialog saves or deletes: the card data is revalidated via server actions (revalidatePath)

    2. **Create `src/components/card-edit-dialog.tsx`** — client component (`"use client"`):

       Props: `{ card: CardRow | null, open: boolean, onOpenChange: (open: boolean) => void }`

       **Edit view (default):**
       - shadcn `<Dialog open={open} onOpenChange={onOpenChange}>`
       - `<DialogContent className="max-w-md">`
       - `<DialogHeader><DialogTitle>Edit card</DialogTitle></DialogHeader>`
       - Two shadcn `<Input>` fields with `<Label>` above each: native word (front) and target word (back)
       - Pre-populated with card.front and card.back via react-hook-form or useState
       - "Save changes" button: `variant="default"` (accent), full width, calls `editCard(card.id, front, back)` server action
       - "Discard changes" button: `variant="outline"`, full width below Save — closes dialog without saving
       - "Delete card" link at bottom: `text-sm text-destructive` with lucide `Trash2` icon, separated by a visual divider (`border-t pt-4 mt-4`)
       - Clicking delete switches to delete confirmation view

       **Delete confirmation view (replaces edit form content):**
       - Same dialog, body content swaps with opacity transition
       - Heading: "Delete this card?"
       - Body: "This can't be undone."
       - "Delete" button: `variant="destructive"`, full width, calls `deleteCard(card.id)` server action
       - "Keep card" button: `variant="outline"`, full width — returns to edit view
       - Delete loading: button shows lucide `Loader2` with `animate-spin`, disabled
       - Delete success: dialog closes, card removed from list (revalidation handles this)
       - Delete error: error text inside dialog: "Couldn't delete. Try again."

       **Save loading state:**
       - "Save changes" disabled + lucide `Loader2` with `animate-spin`
       - Fields locked (disabled) during save
       - Save error: inline error below button: "Couldn't save. Try again."
       - Save success: dialog closes, card list updates

       **Key implementation detail (per RESEARCH.md Pitfall 4):**
       - Use `{card && <EditForm card={card} />}` pattern inside DialogContent so form resets when card prop changes
       - OR use `useEffect` to reset form state when `card` changes
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/TioCards" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/card-list.tsx exists with "use client" directive
    - grep -q "Search your cards" src/components/card-list.tsx returns 0 (search placeholder)
    - grep -q "Your deck is empty" src/components/card-list.tsx returns 0 (empty state)
    - grep -q "No cards match" src/components/card-list.tsx returns 0 (no results state)
    - grep -q 'aria-label="Edit card"' src/components/card-list.tsx returns 0 (accessible edit button)
    - grep -q "Pencil" src/components/card-list.tsx returns 0 (lucide icon import)
    - src/components/card-edit-dialog.tsx exists with "use client" directive
    - grep -q "editCard" src/components/card-edit-dialog.tsx returns 0 (calls editCard action)
    - grep -q "deleteCard" src/components/card-edit-dialog.tsx returns 0 (calls deleteCard action)
    - grep -q "Delete this card" src/components/card-edit-dialog.tsx returns 0 (confirmation heading)
    - grep -q "can't be undone" src/components/card-edit-dialog.tsx returns 0 (confirmation body)
    - grep -q "Save changes" src/components/card-edit-dialog.tsx returns 0
    - grep -q "Discard changes" src/components/card-edit-dialog.tsx returns 0
    - grep -q "Keep card" src/components/card-edit-dialog.tsx returns 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Card list renders with search, empty state, source indicators, and edit button per row. Edit dialog allows changing front/back text and saving, or deleting with inline confirmation. All states (loading, error, success) handled per UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dashboard, deck switcher, card management</name>
  <files>none</files>
  <action>
    Human verification of the complete dashboard UI. No code changes — this task pauses for manual testing.
    What was built: Complete dashboard with deck switcher, card list with search, card edit/delete dialogs, and first-visit language picker. Replaces Phase 1 dashboard stub.
    How to verify:
    1. Visit http://localhost:3000/dashboard while logged in
    2. If first visit (no decks): verify language picker appears with "What language do you want to learn?" and flag buttons
    3. Click a language button — verify deck is created and you land on the deck view
    4. Verify header shows: TioCards wordmark with tiger emoji, deck switcher showing flag + deck name, logout button
    5. Verify deck view shows "My Deck" heading, "Browse words" and "Add a card" buttons, and empty deck state
    6. Open deck switcher dropdown — verify it shows the created deck and "+ New deck" option
    7. Create a second deck via "+ New deck" — verify language picker appears and deck is created with "#2" in name
    8. Switch between decks — verify the view updates
    9. (If cards exist): verify card list shows native word, target translation, source pill, edit icon
    10. Click edit icon on a card — verify edit dialog opens with pre-populated fields
    11. Edit a field and click "Save changes" — verify card updates
    12. Click "Delete card" in edit dialog — verify confirmation appears with "Delete this card?" / "This can't be undone."
    13. Click "Keep card" — verify you return to edit view
    14. Click "Delete" on confirmation — verify card is removed from list
    15. Test search bar: type a word, verify cards filter in real time
  </action>
  <verify>Human confirms all 15 verification steps pass. Type "approved" or describe issues.</verify>
  <done>User has verified dashboard, deck switcher, card list, card edit/delete, and first-visit picker all work correctly in the browser.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` passes (no regressions)
- Dashboard replaces Phase 1 stub
- First-visit picker creates a deck
- Deck switcher allows switching and creating decks
- Card list displays with search
- Edit dialog saves and deletes with confirmation
</verification>

<success_criteria>
1. First-visit users see language picker and can create their first deck
2. Header has TioCards wordmark, deck switcher with flag emoji + name, logout button
3. Deck switcher allows switching between decks and creating new ones
4. Card list shows native/target/source/edit per row with search
5. Card edit dialog allows editing front/back and saving
6. Card delete has inline confirmation with "Delete this card?" / "This can't be undone."
7. All copywriting matches UI-SPEC contract
</success_criteria>

<output>
After completion, create `.planning/phases/02-deck-and-card-management/02-03-SUMMARY.md`
</output>
