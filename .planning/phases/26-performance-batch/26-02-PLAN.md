---
phase: 26-performance-batch
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/app/api/study/complete/route.ts
  - src/app/api/study/complete/route.test.ts
autonomous: true
requirements: [PERF-07]

must_haves:
  truths:
    - "The study-session commit performs its per-card mastery updates (plus the recall_events insert and habitat upsert) in a SINGLE round trip to Neon via db.batch() — no per-card sequential await db.update loop remains in /api/study/complete"
    - "A test proves the whole step-6 write phase executes as exactly one batch (batchCalls.count === 1) — this is the D-02 proof, not a timing gate"
    - "Replay-safety (WR-04) still holds after batching — a replayed commit is a per-card no-op"
    - "D-01: Keep the WR-04 per-session commitId idempotency machinery this phase, even though the atomic db.batch() write arguably supersedes it. Harmless redundancy, smallest possible diff to the app's most critical save path. Retiring it is a candidate future cleanup — note it in the phase summary, do not do it here"
    - "D-02: Proof = round-trip count, not a timing gate. A test asserts the study-session commit performs its card updates in a single DB round trip (vs ~27 sequential today). No new e2e timing harness for the 'Saving your progress…' moment; an informal before/after stopwatch observation goes in the phase summary"
  artifacts:
    - path: "src/app/api/study/complete/route.ts"
      provides: "Step-6 writes rewritten to one db.batch([insertRecallEvents, ...cardUpdates, upsertHabitat]) with tuple cast"
      contains: "db.batch("
    - path: "src/app/api/study/complete/route.test.ts"
      provides: "db.batch mock + batchCalls counter asserting one round trip; existing happy-path and replay-safety describe blocks still green"
      contains: "batch"
  key_links:
    - from: "src/app/api/study/complete/route.ts"
      to: "Neon (sql.transaction)"
      via: "db.batch single HTTP round trip"
      pattern: "db\\.batch\\("
---

<objective>
Collapse the study-session commit's step-6 write phase — currently 1 recall_events insert + N sequential `await db.update(cards)` calls + 1 habitat upsert (1+N+1 round trips, ~27 for a full session) — into ONE atomic `db.batch([...])` call (a real single-round-trip Neon `sql.transaction()`, verified in RESEARCH). Keep the WR-04 commitId idempotency machinery exactly as-is (D-01).

Purpose: The app's most critical save path ("Saving your progress…") does one HTTP round trip instead of ~27, and gains atomicity the current code explicitly documents NOT having.
Output: One `db.batch()` write phase, a rewritten test mock proving `batchCalls === 1` (D-02), an updated stale "no transactions" comment, and WR-04 untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-performance-batch/26-RESEARCH.md
@.planning/phases/26-performance-batch/26-PATTERNS.md

<interfaces>
From 26-PATTERNS.md + 26-RESEARCH.md Pattern 1 (verified against installed drizzle-orm 0.45.1 + @neondatabase/serverless 1.0.2):
- db.batch signature requires a NON-EMPTY TUPLE `Readonly<[U, ...U[]]>` — a .map()-built array must be cast `as [Batchable, ...Batchable[]]` (drizzle #1301). `type Batchable = ReturnType<typeof db.insert> | ReturnType<typeof db.update>`.
- Current step-6 (route.ts): recall_events multi-row insert :222-234 (already correct — mirror it), per-card update loop :236-254 (REPLACE), habitat upsert :256-267 (FOLD into batch). All three live inside ONE try/catch that returns 500 on error — reuse it.
- CommitSchema.grades is `.min(1)` (and `.max(500)`), so cardUpdateQueries.length is always >=1 and <=500 at runtime — the non-empty tuple cast is sound and the batch size is transitively bounded.
- Stale comment "Neon HTTP driver does not support transactions" at :91 (and :208-221 region) becomes factually wrong once db.batch lands — update it in this same diff.
- Existing test mock (route.test.ts:89-142) already models insert/update/select as EAGER chains that resolve at construction time — so `batch: (queries) => { h.batchCalls.count++; return Promise.all(queries); }` is the correct minimal mock (Pitfall 1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): db.batch mock + batchCalls round-trip assertion</name>
  <files>src/app/api/study/complete/route.test.ts</files>
  <read_first>
    - src/app/api/study/complete/route.test.ts (full file — the vi.mock("@/db") factory :89-142, the vi.hoisted() `h` block, existing h.onConflictDoNothingCalls counter :196/:223, "happy path" :180-202, "replay safety" describe :205-251)
    - 26-RESEARCH.md Pitfall 1 (why Promise.all over already-eager query builders is the correct mock) + Validation Architecture PERF-07 rows
    - 26-PATTERNS.md section "src/app/api/study/complete/route.test.ts"
  </read_first>
  <behavior>
    - After a normal 2-card commit, the whole step-6 write phase runs as exactly one batch: h.batchCalls.count === 1.
    - Happy-path assertions on h.recallEventStore / h.cardStore state remain unchanged and green (writes still land, just via Promise.all inside the batch mock).
    - Replay safety (WR-04): a replayed commit remains a per-card no-op; the existing replay describe block stays green with the new mock.
  </behavior>
  <action>
    Add `batchCalls: { count: 0 }` to the vi.hoisted() `h` block. Add a `batch` key to the vi.mock("@/db") db object: `batch: (queries) => { h.batchCalls.count++; return Promise.all(queries); }` — this works because the existing insert()/update() chain mocks already apply their side effects eagerly at construction, so each array element is already a resolved Promise by the time batch receives it. Do NOT alter the existing select/insert/update mock chains. Add `expect(h.batchCalls.count).toBe(1)` to the happy-path test (mirroring the existing h.onConflictDoNothingCalls.toBe(1) precedent). Leave every other assertion in the file unchanged — this is RED until Task 2 adds db.batch() to production code (the "db.batch is not a function" failure is the expected RED signal).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/study/complete/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - route.test.ts vi.mock("@/db") object contains a `batch:` key incrementing `h.batchCalls.count` and returning `Promise.all`.
    - route.test.ts contains `expect(h.batchCalls.count).toBe(1)`.
    - The existing "replay safety" describe block is present and unmodified in its assertions.
    - Running the file now fails ONLY on the batch-count expectation / "db.batch is not a function" (expected RED before Task 2), not on unrelated mock-shape errors.
  </acceptance_criteria>
  <done>db.batch mock + h.batchCalls counter added; batch-count assertion in place; RED pending Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite step-6 writes to one db.batch([...])</name>
  <files>src/app/api/study/complete/route.ts</files>
  <read_first>
    - src/app/api/study/complete/route.ts (step-6 region :222-267 in full, the surrounding try/catch, the WR-04 commitId guard in the card update .where clause, and the stale comment at :91)
    - 26-PATTERNS.md section "src/app/api/study/complete/route.ts" (the three excerpts: recall_events insert to mirror, per-card loop to replace, habitat upsert to fold in)
    - 26-RESEARCH.md Pattern 1 (tuple-cast composition) + State of the Art (stale-comment update guidance)
  </read_first>
  <action>
    Inside the existing step-6 try/catch, replace the three separate awaited statements (recall_events insert, per-card update loop, habitat upsert) with a single `await db.batch([...])`. Build the array as: the recall_events insert (unchanged shape, `.onConflictDoNothing()`), spread `...cardUpdateQueries` where cardUpdateQueries = `cardUpdates.map((u) => db.update(cards).set({...}).where(and(eq(cards.id, u.cardId), or(isNull(cards.lastCommitId), ne(cards.lastCommitId, commitId)))))`, then the habitat upsert (`.onConflictDoUpdate`). The card `.set()` must keep every existing field including `lastCommitId: commitId` and `recallCount: sql\`"recallCount" + ${delta}\`` — the WR-04 commitId guard in the WHERE clause MUST NOT change (D-01). Cast the array `as [Batchable, ...Batchable[]]` where `type Batchable = ReturnType<typeof db.insert> | ReturnType<typeof db.update>` (declare the type locally; the .min(1) grades schema guarantees non-emptiness at runtime). Do NOT cast to `any` (biome noExplicitAny). Reuse the existing single try/catch → 500 error shape verbatim. Update the stale "Neon HTTP driver does not support transactions" comment (~:91 and the step-6 region) to reflect that db.batch() IS a single atomic Neon transaction. Add no new imports — db.batch is a method on the already-imported db.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/study/complete/route.test.ts && npx biome ci src/app/api/study/complete/route.ts src/app/api/study/complete/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - route.ts contains `db.batch(` exactly once and contains `as [Batchable, ...Batchable[]]` (or an equivalently-named non-empty tuple cast) with a locally-declared Batchable type; grep confirms no `as any` on the batch array.
    - The per-card `for (const update of cardUpdates)` await loop is gone (grep: no `await db.update(cards)` inside a loop in step-6).
    - The WR-04 guard `or(isNull(cards.lastCommitId), ne(cards.lastCommitId, commitId))` is still present in the card update WHERE clause.
    - The stale "does not support transactions" comment is updated (grep no longer finds the old claim as a live statement).
    - `npx vitest run src/app/api/study/complete/route.test.ts` exits 0 (happy path incl. batchCalls===1 + replay-safety GREEN); scoped biome exits 0.
  </acceptance_criteria>
  <done>Step-6 write phase is one atomic db.batch(); WR-04 guard intact; stale comment updated; all study-complete tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Study UI (browser) → /api/study/complete | Untrusted grades[] payload crosses into the authenticated write path |
| Route handler → Neon (db.batch) | Server issues one atomic multi-statement transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering / Repudiation | Replayed study commit | mitigate | WR-04 commitId idempotency machinery is kept untouched (D-01) — the card-update WHERE clause still no-ops a replayed commit; test-covered by the retained replay-safety describe block |
| T-26-06 | Denial of Service | db.batch array size | mitigate | Transitively bounded — CommitSchema.grades is `.min(1).max(500)` (unchanged), so the batch array can never exceed ~502 statements regardless of the refactor |
| T-26-07 | Spoofing | /api/study/complete | accept-existing | Existing auth.api.getSession() 401 gate + rate limiter unchanged; no new surface added |
| T-26-08 | Tampering | atomicity gain | accept (positive) | db.batch() adds all-or-nothing atomicity the current per-await loop lacks — a mid-write failure now rolls back instead of leaving a partial commit |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (26-RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Scoped: `npx vitest run src/app/api/study/complete/route.test.ts` exits 0.
- Scoped biome on the two touched files exits 0.
- Wave gate (orchestrator): full `npx tsc --noEmit` + full `npx vitest run`.
- QA correctness gate (MANDATORY — this plan touches the study/SRS write path, per Phase 17 D-10): the orchestrator runs `npm run qa:run` against a freshly-restarted dev server after the wave. This is the criterion-6 core-journey proof for PERF-07.
- No perf measurement in this phase (17 D-14: prod-build-only, out of scope here). D-02 proof is the batchCalls===1 unit assertion plus an informal before/after stopwatch note in the SUMMARY.
</verification>

<success_criteria>
Roadmap criterion 1 met: the study-session commit performs its per-card mastery updates in a single Neon round trip (db.batch), no per-card sequential await loop remains, WR-04 idempotency preserved, qa:run green.
</success_criteria>

<output>
Create `.planning/phases/26-performance-batch/26-02-SUMMARY.md` when done. Record: the informal before/after "Saving your progress…" stopwatch observation (D-02), and the D-01 note that WR-04 commitId retirement is a candidate future cleanup deferred out of this phase.
</output>
