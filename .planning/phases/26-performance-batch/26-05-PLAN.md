---
phase: 26-performance-batch
plan: 05
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - next.config.ts
  - scripts/render-habitat-clips.mjs
autonomous: true
requirements: [PERF-11]

must_haves:
  truths:
    - "Habitat clips under /habitat/clips/ ship with a long-lived immutable Cache-Control header (public, max-age=31536000, immutable) via next.config.ts headers(), verified in the actual response headers"
    - "Repeat visits no longer re-fetch the 240 KB-1 MB clips — the header is applied on the existing clip filenames with no versioning churn"
    - "The companion naming rule is documented in the render-pipeline docs so future clip re-renders don't silently serve stale cached content"
    - "D-08: Immutable long-lived Cache-Control on the EXISTING clip filenames via a next.config.ts headers() block for /habitat/clips/:path*. No filename versioning churn now. Companion naming rule (must land in this phase): document in the render-pipeline docs that any future clip re-render MUST ship under a NEW filename — the forever-cache makes same-name replacement invisible to returning users"
  artifacts:
    - path: "next.config.ts"
      provides: "headers() block mapping /habitat/clips/:path* to Cache-Control public, max-age=31536000, immutable"
      contains: "immutable"
    - path: "scripts/render-habitat-clips.mjs"
      provides: "D-08 naming-rule doc comment: future re-renders MUST ship under a new filename"
      contains: "PERF-11"
  key_links:
    - from: "next.config.ts headers()"
      to: "/habitat/clips/:path*"
      via: "Cache-Control source match"
      pattern: "/habitat/clips"
---

<objective>
Add a long-lived immutable `Cache-Control` header for `/habitat/clips/*` via a `next.config.ts` `headers()` block, so returning visitors stop re-downloading the 240 KB-1 MB habitat clips on every visit (today `public/` assets default to `Cache-Control: public, max-age=0`). Apply it to the EXISTING filenames — no versioning churn — and land the D-08 companion naming rule in the render-pipeline docs (part of definition-of-done, not optional).

Purpose: A config-only bytes-transferred win for repeat visits (no habitat re-measuring, per the REQUIREMENTS.md out-of-scope exception).
Output: A `headers()` block in the currently-stub `next.config.ts`, verified via response headers, plus the forever-cache naming discipline documented where clips are rendered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-performance-batch/26-RESEARCH.md
@.planning/phases/26-performance-batch/26-PATTERNS.md
@AGENTS.md

<interfaces>
From 26-PATTERNS.md + 26-RESEARCH.md Pattern 4 (verified against installed Next 16.2.1 docs per the AGENTS.md mandatory caveat):
- next.config.ts is currently a 7-line stub (`const nextConfig: NextConfig = { /* ... */ }`). Target shape: `async headers() { return [{ source: "/habitat/clips/:path*", headers: [{ key: "Cache-Control", value: "public, max-age=31536000, immutable" }] }]; }`.
- headers() is a STABLE (non-experimental) API — no D-07-style checkpoint needed (unlike Phase 17's experimental flags). Headers are "checked before the filesystem which includes pages and /public files" per node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md — READ THAT DOC before writing (this Next differs from training data).
- Clips live in public/habitat/clips/ as l{N}-{mood}.{mp4,webm} (per level/mood).
- scripts/render-habitat-clips.mjs has a header-comment block at :1-30 — add the D-08 naming-rule paragraph there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: headers() block for /habitat/clips/:path*</name>
  <files>next.config.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md (MANDATORY per AGENTS.md — this Next version differs from training data; confirm the async headers() syntax, the source-pattern matching, and the filesystem-ordering note before writing)
    - next.config.ts (current 7-line stub)
    - 26-PATTERNS.md section "next.config.ts (config, greenfield)" (target shape with the D-08 comment)
    - 26-RESEARCH.md Pattern 4 + Security Domain (clip URLs are level/mood-keyed, not user-keyed — safe to cache publicly)
  </read_first>
  <action>
    Add an `async headers()` method to nextConfig returning a single rule: `source: "/habitat/clips/:path*"`, `headers: [{ key: "Cache-Control", value: "public, max-age=31536000, immutable" }]`. Include an inline comment referencing the D-08 naming rule (any future re-render MUST use a new filename because this caches the existing filenames effectively forever). Keep the existing NextConfig import and default export. Do not add any other config option. Verify the exact method signature against the installed headers.md doc (not training-data recall).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx biome ci next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contains `async headers()`, `source: "/habitat/clips/:path*"`, and `"public, max-age=31536000, immutable"`.
    - An inline comment references the D-08 new-filename-on-re-render rule.
    - `npx tsc --noEmit` exits 0; scoped biome exits 0.
    - Response-header proof (run against a running server, dev acceptable for the initial check): `curl -sI http://localhost:3000/habitat/clips/<an-existing-clip-file> | grep -i cache-control` shows `max-age=31536000, immutable` (pick a real file from public/habitat/clips/). Final prod verification is the orchestrator's ROADMAP-criterion-5 check.
  </acceptance_criteria>
  <done>next.config.ts serves immutable Cache-Control on /habitat/clips/*; header confirmed present in a live response.</done>
</task>

<task type="auto">
  <name>Task 2: D-08 naming-rule doc in the render pipeline</name>
  <files>scripts/render-habitat-clips.mjs</files>
  <read_first>
    - scripts/render-habitat-clips.mjs:1-30 (existing header-comment block — the discoverable render-pipeline doc location)
    - 26-PATTERNS.md section "scripts/render-habitat-clips.mjs (doc-comment addition, D-08 companion)"
    - 26-CONTEXT.md D-08 (the companion naming rule is part of definition-of-done)
  </read_first>
  <action>
    Add a clearly-labeled comment block (e.g. a `// ── Cache-Control (Phase 26 PERF-11) ──` header) to the existing top-of-file comment block in scripts/render-habitat-clips.mjs stating: any future re-render of an existing l{N}-{mood} clip MUST ship under a NEW filename, because next.config.ts sets `Cache-Control: immutable` on /habitat/clips/*, making a same-name replacement invisible to returning users until their cache clears (effectively never for immutable assets). This is the discoverable render-pipeline doc location (the script that produces the clips); no separate docs file is created. Do not change any rendering logic — comment addition only.
  </action>
  <verify>
    <automated>node --check scripts/render-habitat-clips.mjs && npx biome ci scripts/render-habitat-clips.mjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/render-habitat-clips.mjs header comment contains "PERF-11" and a statement that future re-renders MUST use a new filename due to the immutable cache.
    - No executable/rendering logic changed (git diff shows only comment-line additions).
    - `node --check scripts/render-habitat-clips.mjs` exits 0 (syntax valid); scoped biome exits 0.
  </acceptance_criteria>
  <done>The forever-cache new-filename discipline (D-08) is documented in the render-pipeline script — definition-of-done met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /habitat/clips/* static assets | Public static video files served with a caching directive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-15 | Information Disclosure | Publicly-cacheable-forever clip URLs | accept | Clip URLs are level/mood-keyed (l{N}-{mood}.{mp4,webm}), NOT user-keyed — identical content for every user, no session/PII in URL or body (26-RESEARCH.md Security Domain). Safe to cache publicly and immutably |
| T-26-16 | Tampering (stale content) | Same-name clip re-render served from forever-cache | mitigate | D-08 naming rule documented in the render pipeline (Task 2): any future re-render MUST use a new filename, so an updated clip is never masked by a stale immutable cache entry |
| T-26-17 | Denial of Service | headers() misconfiguration breaking other routes | mitigate | The source pattern is scoped narrowly to `/habitat/clips/:path*` only; tsc + a live curl header check confirm no broader route is affected |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (26-RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `node --check scripts/render-habitat-clips.mjs` exits 0; scoped biome on both files exits 0.
- Response-header proof: `curl -sI http://localhost:3000/habitat/clips/<existing-file> | grep -i cache-control` shows `max-age=31536000, immutable` (ROADMAP criterion 5 — the empirical check that also resolves RESEARCH Assumption A2 on dev-vs-prod header parity). Orchestrator confirms on prod after deploy.
- Wave gate (orchestrator): full `npx tsc --noEmit` + full `npx vitest run`. No study/SRS path touched → no qa:run required by this plan.
</verification>

<success_criteria>
Roadmap criterion 5 met: /habitat/clips/ ships immutable Cache-Control via next.config.ts headers(), verified in response headers; the D-08 companion naming rule is documented in the render pipeline.
</success_criteria>

<output>
Create `.planning/phases/26-performance-batch/26-05-SUMMARY.md` when done. Record the curl response-header verification result (dev) and note that prod verification is the orchestrator's post-deploy criterion-5 check.
</output>
