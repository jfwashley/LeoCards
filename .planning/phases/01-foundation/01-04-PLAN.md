---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Env vars are validated at startup via Zod — missing vars cause a loud failure before the app serves any requests"
    - "CI build has all required env vars and does not fail due to missing RESEND_API_KEY"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Side-effect import of env.ts that triggers Zod validation at module load"
      contains: 'import "@/env"'
    - path: ".github/workflows/ci.yml"
      provides: "Complete set of env vars for next build step"
      contains: "RESEND_API_KEY"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/env.ts"
      via: 'import "@/env"'
      pattern: 'import\s+"@/env"'
---

<objective>
Wire the orphaned env.ts Zod validation module into the app module graph and fix the CI env block.

Purpose: The phase promised "environment variables are validated at startup — missing vars cause a loud failure." Currently env.ts exists but is never imported, so validation never runs. This gap closure wires it in and ensures CI has the complete env var set.

Output: Two file edits — layout.tsx gets a side-effect import, ci.yml gets the missing RESEND_API_KEY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire env.ts into app module graph and add RESEND_API_KEY to CI</name>
  <files>src/app/layout.tsx, .github/workflows/ci.yml</files>
  <read_first>src/env.ts, src/app/layout.tsx, .github/workflows/ci.yml</read_first>
  <action>
1. In `src/app/layout.tsx`, add `import "@/env";` as the FIRST import line (before all other imports). This is a side-effect-only import — it does not use the exported `env` object, it just ensures `createEnv()` runs when the module graph loads, which triggers Zod validation of all required env vars.

   The resulting import block should start:
   ```
   import "@/env";
   import type { Metadata } from "next";
   ```

   Do NOT change any other lines in layout.tsx. Do NOT import the named `env` export — use the bare string import for side-effect only.

2. In `.github/workflows/ci.yml`, find the `Build` step's `env:` block (around line 36-39). Add `RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}` to the env block, after the existing three vars. The final env block should contain exactly four vars:
   - DATABASE_URL
   - BETTER_AUTH_SECRET
   - NEXT_PUBLIC_APP_URL
   - RESEND_API_KEY

   Do NOT change any other part of ci.yml.

Note on db/index.ts: Per the Phase 01 decision log, `src/db/index.ts` intentionally uses `process.env.DATABASE_URL` directly (not env.ts) to avoid a circular import when auth.ts imports db at module scope. Do NOT change db/index.ts.
  </action>
  <verify>
    <automated>cd C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards && grep -q 'import "@/env"' src/app/layout.tsx && grep -q 'RESEND_API_KEY' .github/workflows/ci.yml && npx tsc --noEmit && npx biome ci . && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'import "@/env"' src/app/layout.tsx` returns a match on the first import line
    - `grep 'RESEND_API_KEY' .github/workflows/ci.yml` returns a match inside the Build step env block
    - `npx tsc --noEmit` exits 0 (no type errors introduced)
    - `npx biome ci .` exits 0 (no lint errors introduced)
    - `npx vitest run` still passes (existing env tests unaffected)
    - No other files modified besides layout.tsx and ci.yml
  </acceptance_criteria>
  <done>
    - env.ts is imported at app startup via layout.tsx side-effect import
    - CI build step has all 4 required env vars (DATABASE_URL, BETTER_AUTH_SECRET, NEXT_PUBLIC_APP_URL, RESEND_API_KEY)
    - TypeScript and Biome checks pass with no regressions
  </done>
</task>

</tasks>

<verification>
1. `grep -n 'import "@/env"' src/app/layout.tsx` shows the import on line 1
2. `grep -c 'RESEND_API_KEY' .github/workflows/ci.yml` returns 1
3. `npx tsc --noEmit` exits 0
4. `npx biome ci .` exits 0
5. `npx vitest run` passes (existing tests still green)
</verification>

<success_criteria>
- The orphaned env.ts module is wired into the app module graph via a side-effect import in layout.tsx
- CI build env block contains all 4 required secrets
- No regressions in type checking, linting, or existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
