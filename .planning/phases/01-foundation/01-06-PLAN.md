---
phase: 01-foundation
plan: 06
type: execute
wave: 2
depends_on: ["01-05"]
files_modified:
  - .github/workflows/ci.yml
  - .env.example
autonomous: true
gap_closure: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "CI pipeline would pass on a PR (tsc, biome, build all green)"
    - ".env.example documents all required env vars including DEEPL_API_KEY"
    - "npm install completes with fewer vulnerabilities"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Working CI pipeline with correct step ordering"
      contains: "DEEPL_API_KEY"
    - path: ".env.example"
      provides: "Complete env var documentation"
      contains: "DEEPL_API_KEY"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub Actions"
      via: "PR trigger"
      pattern: "pull_request"
---

<objective>
Fix the CI pipeline step ordering and missing secrets, update .env.example with DEEPL_API_KEY, and run npm audit fix.

Purpose: Ensure CI would pass on a real PR and new developers can onboard without Zod crashes.
Output: A green CI config and complete .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-UAT.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
</context>

<interfaces>
<!-- Current ci.yml structure (to be modified): -->
<!-- Steps: Checkout -> Node -> Install -> Type check -> Biome -> Build -->
<!-- Must become: Checkout -> Node -> Install -> Build -> Type check -> Biome -->

<!-- Current .env.example (to be extended): -->
<!-- DATABASE_URL, BETTER_AUTH_SECRET, RESEND_API_KEY, NEXT_PUBLIC_APP_URL -->
<!-- Must add: DEEPL_API_KEY -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix CI pipeline and .env.example</name>
  <files>.github/workflows/ci.yml, .env.example</files>
  <action>
1. In `.github/workflows/ci.yml`, make these changes to the `check` job:
   a. **Reorder steps**: Move the "Build" step (with `npm run build` and env vars) BEFORE the "Type check" step. Next.js build generates `next-env.d.ts` and `.next/types/` which tsc needs. New order: Checkout -> Node -> Install -> Build -> Type check -> Biome.
   b. **Add DEEPL_API_KEY** to the Build step's env block: `DEEPL_API_KEY: ${{ secrets.DEEPL_API_KEY }}`
   c. **Pin Biome version**: Change `uses: biomejs/setup-biome@v2` to include `with: version: 2.4.8` (matching the project's biome.json $schema version).
   d. **Scope biome ci**: Change `biome ci .` to `biome ci src/` to avoid scanning node_modules or .claude/worktrees paths.

2. In `.env.example`, add a DEEPL_API_KEY entry:
   ```
   # Translation (DeepL)
   DEEPL_API_KEY="your_deepl_api_key"
   ```
   Place it after the NEXT_PUBLIC_APP_URL line.

3. Run `npm audit fix` to address non-breaking vulnerabilities. Do NOT run `npm audit fix --force` (that can break dependencies).
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/TioCards" && grep -n "DEEPL_API_KEY" .github/workflows/ci.yml .env.example && grep -n "version:" .github/workflows/ci.yml | head -5</automated>
  </verify>
  <done>CI yml has Build before Type check, DEEPL_API_KEY in env block, Biome pinned to 2.4.8, biome ci scoped to src/. .env.example lists all 5 required env vars. npm audit fix ran without errors.</done>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` has Build step before Type check step
- `DEEPL_API_KEY` appears in both ci.yml and .env.example
- Biome version pinned to 2.4.8
- `biome ci src/` (not `biome ci .`) in CI
- `.env.example` has 5 env vars matching what `src/env.ts` validates
</verification>

<success_criteria>
CI pipeline config is correct (would pass if secrets are configured). .env.example is complete. npm vulnerabilities reduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
