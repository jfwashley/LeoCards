---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/lib/study-engine.test.ts
  - .gitattributes
autonomous: true
gap_closure: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "npm run build completes with exit code 0"
    - "npx tsc --noEmit passes with zero errors"
    - "npx biome ci src/ passes with zero errors"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page with Suspense-wrapped useSearchParams"
      contains: "Suspense"
    - path: "src/lib/study-engine.test.ts"
      provides: "Type-safe test helpers"
    - path: ".gitattributes"
      provides: "LF line ending enforcement"
      contains: "eol=lf"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "next build"
      via: "Suspense boundary enabling prerender"
      pattern: "Suspense"
---

<objective>
Fix the build blocker (login page Suspense boundary) and all code quality failures (TypeScript errors, Biome lint/format violations, CRLF line endings).

Purpose: Unblock the 9 UAT tests that are blocked by the build failure, and bring tsc + biome to green.
Output: A buildable app with zero tsc errors and zero biome errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix login Suspense boundary and TypeScript errors</name>
  <files>src/app/(auth)/login/page.tsx, src/lib/study-engine.test.ts</files>
  <action>
1. In src/app/(auth)/login/page.tsx, apply the same Suspense pattern used in reset-password/page.tsx:
   - Rename the current `LoginPage` default export to `LoginForm` (inner component)
   - Create a new default export `LoginPage` that wraps `<LoginForm />` in `<Suspense fallback={<div className="text-sm text-muted-foreground">Loading...</div>}>`
   - Add `Suspense` to the React import
   - The `useSearchParams()` call stays inside `LoginForm`
   - Everything else (Card wrapper, form, links) stays inside `LoginForm`

2. In src/lib/study-engine.test.ts, fix the 12 type errors:
   - In the `makeCard` function (line 22), change the overrides parameter type from `Partial<CardForSession> & { id?: string }` to just `Partial<CardForSession>`. The `id` field is already in `CardForSession` (as `CardId`), so the `& { id?: string }` intersection widens it back to plain string. After removing it, the `(overrides.id ?? "card-1") as CardId` cast on line 25 handles the narrowing.
   - In the `makeCards` function (line 35), similarly remove `{ id?: string }` if present — use `Partial<CardForSession>` only.
   - Find all array index accesses on test results (like `result[3]`, `result[7]`, `result[11]`) that fail with `noUncheckedIndexedAccess`. Add non-null assertions (`!`) after each: e.g., `result[3]!`. These are test assertions where the index is known valid.

3. Verify: `npx tsc --noEmit` passes with zero errors. `npm run build` completes with exit code 0.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>npm run build exits 0. npx tsc --noEmit reports zero errors. Login page renders correctly with Suspense boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Biome lint/format violations and enforce LF line endings</name>
  <files>.gitattributes, src/ (43+ files auto-fixed by biome)</files>
  <action>
1. Create `.gitattributes` at project root with:
   ```
   * text=auto eol=lf
   ```
   This prevents future CRLF issues on Windows.

2. Run `npx biome check --write src/` to auto-fix the ~93 of 102 errors (CRLF -> LF, import sorting, formatting).

3. For the ~9 remaining manual-fix errors, inspect `npx biome ci src/` output and fix each:
   - **useExhaustiveDependencies**: Add missing dependencies to the dependency array, or if intentionally omitted, add a `// biome-ignore lint/correctness/useExhaustiveDependencies: <reason>` comment with a genuine reason.
   - **noArrayIndexKey**: Replace array index keys with stable unique identifiers where possible, or add `// biome-ignore lint/suspicious/noArrayIndexKey: <reason>` if the list is static/non-reorderable.
   - **a11y issues** (e.g., missing alt text, missing labels): Add the required accessibility attributes.

4. Also check for any nested biome.json in `.claude/worktrees/` — if found, delete it to prevent config conflicts.

5. Verify: `npx biome ci src/` passes with zero errors.
  </action>
  <verify>
    <automated>cd "C:/Users/jfwas/.claude/projects/C--Users-jfwas/LeoCards" && npx biome ci src/</automated>
  </verify>
  <done>npx biome ci src/ reports zero errors and zero warnings. .gitattributes exists with eol=lf. No nested biome.json in .claude/worktrees/.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (unblocks 9 UAT tests)
- `npx tsc --noEmit` exits 0
- `npx biome ci src/` exits 0
</verification>

<success_criteria>
Build succeeds. TypeScript strict mode passes. Biome lint and format pass. All three gates green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
