---
phase: 10-vision-extraction-endpoint
plan: 04
type: execute
wave: 2
depends_on: ["10-02", "10-03"]
files_modified:
  - src/app/api/extract/__tests__/reference-labels.json
  - src/app/api/extract/__tests__/extract-eval.test.ts
autonomous: false
requirements: [EXT-01, EXT-03]
must_haves:
  truths:
    - "The ASSUMED-latest model id (claude-sonnet-4-6) is confirmed against current Anthropic provider docs before any production deploy"
    - "A 20-image reference dataset + ground-truth labels exist so the code-based eval dimensions (D3 orthographic, D4 verbatim, D5a no-words) can run"
    - "The gated eval suite executes against the live endpoint and the code-based assertions pass; subjective dimensions get a tutor rubric pass"
  artifacts:
    - path: "src/app/api/extract/__tests__/fixtures/"
      provides: "20 reference images (manual curation — Joshua)"
    - path: "src/app/api/extract/__tests__/reference-labels.json"
      provides: "Ground-truth word lists keyed by fixture filename (D3/D4/D5a)"
    - path: "src/app/api/extract/__tests__/extract-eval.test.ts"
      provides: "Fixture-driven code-based eval assertions (filled in from the 10-01 skeleton)"
  key_links:
    - from: "src/app/api/extract/__tests__/extract-eval.test.ts"
      to: "src/app/api/extract/__tests__/reference-labels.json"
      via: "fixture filename → expected word array"
      pattern: "reference-labels"
---

<objective>
Close the two manual Wave 0 gaps from 10-VALIDATION.md and validate real extraction
quality: (1) verify the ASSUMED-latest model id against current provider docs, (2) curate
the 20-image reference dataset + ground-truth labels (MANUAL — Joshua, with FR/ES tutor
for subjective dimensions), and (3) fill in and run the gated eval suite for the
code-based dimensions plus a manual rubric pass for the subjective ones.

Purpose: EXT-01 (real vision accuracy) and EXT-03 (no-words correctness) cannot be proven
by mocked unit tests alone — they require the AI-SPEC reference dataset and live eval.
This is the production-readiness gate.
Output: fixtures dir, reference-labels.json, completed eval test, rubric results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
- .planning/phases/10-vision-extraction-endpoint/10-AI-SPEC.md — Section 4 (model id ASSUMED-latest note + verification URL), Section 5 (Reference Dataset composition table — 20 images, labeling approach, CI integration), Section 1b (rubric dimensions for the manual pass)
- .planning/phases/10-vision-extraction-endpoint/10-VALIDATION.md — "Manual-Only Verifications" + Wave 0 manual gaps
- .planning/phases/10-vision-extraction-endpoint/10-PATTERNS.md — `extract-eval.test.ts` section + "No Analog Found" (manual fixtures)
- src/app/api/extract/__tests__/extract-eval.test.ts (the gated skeleton from 10-01 — fill it in)
- src/app/api/extract/route.ts (from 10-02 — the live endpoint under eval)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the ASSUMED-latest model id against current provider docs</name>
  <files>src/app/api/extract/route.ts</files>
  <read_first>
  - .planning/phases/10-vision-extraction-endpoint/10-AI-SPEC.md Section 4 (Model Configuration ASSUMED note + https://ai-sdk.dev/providers/ai-sdk-providers/anthropic)
  - src/app/api/extract/route.ts (the `anthropic("claude-sonnet-4-6", ...)` line + adjacent verify comment)
  </read_first>
  <action>
  Fetch https://ai-sdk.dev/providers/ai-sdk-providers/anthropic and confirm whether `claude-sonnet-4-6` is still the current Sonnet-tier vision-capable model id.
  - If still current: leave the route id unchanged; record "verified <date>: claude-sonnet-4-6 current" in the SUMMARY and update the adjacent code comment to "verified <date>".
  - If a newer Sonnet-tier vision id has superseded it: update the single string literal in `anthropic("…", { apiKey: env.ANTHROPIC_API_KEY })` to the current id, run `npm run typecheck` + `npx vitest run src/app/api/extract/__tests__/extract.unit.test.ts` (mocked — must stay green), and record the change + rationale in the SUMMARY.
  Only the model-id string literal may change; no other route logic.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/extract/__tests__/extract.unit.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - The model-id line in `src/app/api/extract/route.ts` carries a comment containing the literal token "verified" followed by a 2026 date
  - `npx vitest run src/app/api/extract/__tests__/extract.unit.test.ts` stays fully green after any id change
  - SUMMARY records the docs-check outcome (unchanged vs. updated-to-X)
  </acceptance_criteria>
  <done>Model id confirmed (or corrected) against live provider docs; mocked unit suite green; outcome recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Curate the 20-image reference dataset + ground-truth labels</name>
  <action>BLOCKING human-action checkpoint — no autonomous work. Pause and present the <what-built> context and the numbered <how-to-verify> steps to the user; wait for the <resume-signal> before proceeding to Task 2. Joshua (with FR/ES tutor for subjective dimensions) curates the fixtures and authors reference-labels.json — this cannot be auto-generated (no images are persisted; ground truth needs human judgment).</action>
  <what-built>
  The extraction endpoint (10-02) and client flow (10-03) are complete and unit-tested
  with mocked SDK. The code-based eval dimensions (D3 orthographic faithfulness, D4
  verbatim surface form, D5a no-words correctness) and the subjective dimensions (D1
  target-language purity, D2 word identity/noise, D5b yield density) require a real
  reference image set that cannot be auto-generated (no images are persisted by design;
  ground truth needs human + FR/ES tutor judgment — 10-AI-SPEC Section 5 / 10-VALIDATION
  Manual-Only Verifications).
  </what-built>
  <how-to-verify>
  1. Curate 20 images into `src/app/api/extract/__tests__/fixtures/` following the
     10-AI-SPEC Section 5 composition table EXACTLY (1–3 FR menu w/ EN headings; 4–5 ES
     signage; 6–7 FR textbook; 8–9 bilingual FR/EN packaging; 10–11 handwritten FR
     specials; 12–13 dense FR food label; 14–15 sparse 2–4-word sign; 16–17 EN-only
     tested with fr target; 18–19 blank/no-text; 20 multilingual FR/EN/ES). Store as
     small JPEG/PNG/WebP or base64 `.txt` (prefer small files for git).
  2. Author `src/app/api/extract/__tests__/reference-labels.json` mapping each fixture
     filename → its expected verbatim word array (exact printed forms incl. diacritics),
     plus the per-image `targetLanguage`. For images 18–19 the expected array is `[]`.
     Code-based dimensions (D3/D4/D5a) are Joshua-authored exact strings; subjective
     dimensions (D1/D2/D5b) need the FR/ES tutor rubric pass per 10-AI-SPEC Section 5.
  3. Confirm `ANTHROPIC_API_KEY` is set in the local/CI environment for the live eval run.
  4. Reply "fixtures ready" when the directory + labels + key are in place, or describe
     what is blocking.
  </how-to-verify>
  <resume-signal>Type "fixtures ready" or describe the blocker</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fill in + run the code-based eval; record subjective rubric outcome</name>
  <files>src/app/api/extract/__tests__/extract-eval.test.ts, src/app/api/extract/__tests__/reference-labels.json</files>
  <read_first>
  - .planning/phases/10-vision-extraction-endpoint/10-AI-SPEC.md Section 5 (dimensions table — which are code-based vs manual; CI command) + Section 1b (rubric Good/Bad per dimension)
  - .planning/phases/10-vision-extraction-endpoint/10-RESEARCH.md Section 5 ("Automatable vs. manual-only" table)
  - src/app/api/extract/__tests__/extract-eval.test.ts (the gated skeleton from 10-01)
  </read_first>
  <action>
  1. Inside the existing `describe.skipIf(!RUN_EVALS)(...)` block, iterate every fixture in `reference-labels.json`. For each: read the fixture, build the `{ image, mimeType, deckId, targetLanguage }` body, invoke the live extraction (call `POST` from `@/app/api/extract/route` directly with a constructed `Request`, real `ANTHROPIC_API_KEY` in env), parse `{ words }`, and assert the CODE-BASED dimensions only:
     - D5a (no-words): images 18–19 ⇒ `expect(words).toEqual([])`.
     - D3/D4 (orthographic + verbatim): for labeled images, assert every expected ground-truth word (exact string incl. diacritics) is present in `words` (membership; do not fail on extra words — those are the subjective D2 dimension, scored by the tutor, not asserted here).
     Keep the suite skipped when `RUN_EXTRACTION_EVALS` is unset (must not break `npm test`).
  2. Run the gated suite: `RUN_EXTRACTION_EVALS=true npx vitest run src/app/api/extract/__tests__/extract-eval.test.ts`. If code-based assertions fail, record per-image failures in the SUMMARY (these inform prompt tuning — do NOT silently weaken assertions to pass).
  3. Subjective dimensions (D1 purity, D2 noise, D5b density): run the FR/ES tutor rubric pass per 10-AI-SPEC Section 5 against the same extraction outputs; record pass/flag per image in the SUMMARY (manual rubric — not a code assertion).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/extract/__tests__/extract-eval.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - `npx vitest run src/app/api/extract/__tests__/extract-eval.test.ts` (no env) exits 0 with the suite SKIPPED (npm test stays safe)
  - `reference-labels.json` parses as valid JSON and has ≥ 20 keyed entries; images 18–19 map to `[]`
  - `grep "describe.skipIf(!RUN_EVALS)" src/app/api/extract/__tests__/extract-eval.test.ts` still matches; `grep "toEqual(\[\])\|reference-labels" src/app/api/extract/__tests__/extract-eval.test.ts` matches (assertions wired)
  - SUMMARY records: live eval pass/fail per code-based dimension + the tutor rubric outcome for D1/D2/D5b
  </acceptance_criteria>
  <done>Eval test asserts code-based dimensions against the 20-image set; gated suite runs live and remains skip-safe; subjective rubric outcome recorded for production-readiness decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → Anthropic | Live eval sends real fixture images to the paid vision API (cost-incurring, gated) |
| fixtures → git | Reference images committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Denial of Service | accidental cost from eval running on every CI push | mitigate | `describe.skipIf(!RUN_EXTRACTION_EVALS)` gate — live eval only runs on explicit opt-in; skip-safe assertion in acceptance criteria (10-AI-SPEC Section 5) |
| T-10-14 | Information Disclosure | committing fixture images containing incidental personal data (faces/plates) | mitigate | Curator (Joshua) selects text-focused images; transient-processing posture documented in 10-AI-SPEC Section 1b applies to runtime, and fixtures are deliberately chosen text/menu/sign images — flag any image with identifiable people during curation |
| T-10-15 | Tampering | weakening eval assertions to force a pass | mitigate | Task 2 action explicitly prohibits silently weakening assertions; failures are recorded for prompt tuning, not hidden |
</threat_model>

<verification>
- `npx vitest run src/app/api/extract/__tests__/extract-eval.test.ts` (no env) → skipped, exit 0
- `npm test` full suite green (eval skip-safe)
- `reference-labels.json` valid JSON, ≥20 entries
- SUMMARY documents model-id verification + eval + tutor rubric outcomes
</verification>

<success_criteria>
- Model id confirmed/corrected against live provider docs (production-deploy safety)
- 20-image reference dataset + ground-truth labels exist (manual gaps closed)
- Code-based eval dimensions asserted and run live; subjective dimensions get a tutor rubric pass; results recorded for the production-readiness decision (EXT-01/EXT-03)
</success_criteria>

<output>
After completion, create `.planning/phases/10-vision-extraction-endpoint/10-04-SUMMARY.md`
</output>
