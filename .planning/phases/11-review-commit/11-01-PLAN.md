---
phase: 11-review-commit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/deck-actions.ts
  - src/components/review-list.test.ts
  - src/lib/deck-actions.test.ts
autonomous: true
requirements: [RVW-01, RVW-02, RVW-03, RVW-04, RVW-05]
user_setup: []

must_haves:
  truths:
    - "saveCard accepts \"image\" as a source value with no TypeScript error"
    - "review-list.test.ts exists and runs (RED — covers reducer, isDuplicate, fan-out, batch commit, cancel)"
    - "deck-actions.test.ts has a getSameLanguageDeckBackWords describe block (RED until Wave 2)"
    - "Prior suite (Phases 1–10) stays green"
  artifacts:
    - path: "src/components/review-list.test.ts"
      provides: "Wave 0 test scaffold for reviewListReducer, isDuplicate, translation fan-out, batch commit, cancel"
      contains: "describe(\"reviewListReducer\""
    - path: "src/lib/deck-actions.test.ts"
      provides: "Extended with getSameLanguageDeckBackWords describe block"
      contains: "describe(\"getSameLanguageDeckBackWords\""
    - path: "src/lib/deck-actions.ts"
      provides: "saveCard source union widened to include \"image\""
      contains: "\"manual\" | \"wordlist\" | \"image\""
  key_links:
    - from: "src/components/review-list.test.ts"
      to: "src/components/review-list (subject — created Wave 2)"
      via: "import reviewListReducer, isDuplicate"
      pattern: "from \"@/components/review-list\""
    - from: "src/lib/deck-actions.test.ts"
      to: "getSameLanguageDeckBackWords"
      via: "import from @/lib/deck-actions"
      pattern: "getSameLanguageDeckBackWords"
---

<objective>
Wave 0: lay the Nyquist test scaffolds and the one-line type gate BEFORE any
component or server-action implementation lands.

Purpose: Per 11-VALIDATION.md, the failing test files and the source-union type
change must exist first so that Waves 1–3 turn them green (RED→GREEN). This plan
also widens the `saveCard` source union (D-11) — a one-line, no-migration change.

Output: `src/components/review-list.test.ts` (new, intentionally RED awaiting
Wave 2's component), `getSameLanguageDeckBackWords` describe block appended to the
existing `src/lib/deck-actions.test.ts` (RED awaiting Wave 2's action), and the
widened `saveCard` source union (GREEN immediately via tsc).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-review-commit/11-VALIDATION.md
@.planning/phases/11-review-commit/11-RESEARCH.md
@.planning/phases/11-review-commit/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen saveCard source union to include "image" (D-11)</name>
  <read_first>
    - src/lib/deck-actions.ts lines 60–90 (saveCard signature + body — the ONLY edit point)
    - 11-PATTERNS.md § "widen saveCard source union (one-line edit)" (lines 477–490)
    - 11-RESEARCH.md § "Focus Area 1: Source Union Widening (D-11)" — confirms 1 file, 1 line; NO migration; schema.ts NOT edited
  </read_first>
  <files>src/lib/deck-actions.ts</files>
  <action>
    In `src/lib/deck-actions.ts`, change the `saveCard` signature parameter
    (currently at line ~69) from:
      `source: "manual" | "wordlist",`
    to:
      `source: "manual" | "wordlist" | "image",`
    This is the ONLY change in this task. Do NOT edit `src/db/schema.ts` (the
    `cards.source` column is free `text` — no DB constraint, no migration per
    D-11 / 11-RESEARCH.md Focus Area 1). Do NOT change `translation-form.tsx`
    (still passes `"manual"`, still valid). Do NOT change `addWordToCard`
    (hardcodes `"wordlist"` in its values object, not via the saveCard
    signature) or `removeWordFromDeck` (filters `eq(cards.source, "wordlist")`,
    unaffected by adding a union member).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`npx tsc --noEmit` passes; `grep -n '"manual" | "wordlist" | "image"' src/lib/deck-actions.ts` returns the saveCard signature line; no other file modified.</done>
</task>

<task type="auto">
  <name>Task 2: Create review-list.test.ts Wave 0 scaffold (RED)</name>
  <read_first>
    - src/app/api/extract/__tests__/extract.unit.test.ts (vi.hoisted → vi.mock → import-subject order; beforeEach clearAllMocks)
    - src/lib/deck-actions.test.ts lines 1–115 (db chain mock + auth mock + next/cache + next/headers mock setup; beforeEach re-wire)
    - 11-PATTERNS.md § "src/components/review-list.test.ts (test, unit)" (lines 543–684 — vi.hoisted, auth/headers/cache/db mocks, reducer test structure)
    - 11-RESEARCH.md § "Validation Architecture" → "Phase Requirements → Test Map" + "Unit-Testable Pure Functions / Logic"
    - 11-VALIDATION.md § "Wave 0 Requirements" (the exact behaviors each test must cover)
    - 11-CONTEXT.md D-02/D-03/D-04/D-06/D-07/D-12/D-14 (semantics under test)
  </read_first>
  <files>src/components/review-list.test.ts</files>
  <action>
    Create `src/components/review-list.test.ts`. It imports the not-yet-existing
    subject — this file is intentionally RED until Wave 2 (record this in the
    SUMMARY, mirroring Phase 10's intentionally-RED scaffold convention in
    STATE.md). Use the mandatory Vitest order: `vi.hoisted()` declaring mocks →
    `vi.mock()` factories referencing the hoisted vars → `import` of subject →
    `beforeEach(() => vi.clearAllMocks())` + chain re-wire.

    Import (will resolve once Wave 2 exports them):
      `import { reviewListReducer, isDuplicate } from "@/components/review-list";`
    Also import the type(s) needed to build initial state objects (e.g.
    `type ReviewState` / action union) from the same module.

    Mock `global.fetch` for translation fan-out tests (vi.hoisted mockFetch),
    and mock `@/lib/deck-actions` so `saveImageCards` / `saveCard` are spies for
    the batch-commit and cancel tests:
      `vi.mock("@/lib/deck-actions", () => ({ saveImageCards: vi.fn(), getSameLanguageDeckBackWords: vi.fn() }));`

    Author these describe blocks (assertions concrete — no TODO-only stubs):

    `describe("isDuplicate")` — RVW-05a:
      - returns true for exact match against the known Set
      - returns true for case-insensitive match ("Chien" vs Set(["chien"]))
      - returns true for trimmed match ("  chat  " vs Set(["chat"]))
      - returns false for a genuinely new word

    `describe("reviewListReducer")` — RVW-01/02 (Step A) + Step B + commit:
      - DEDUPE_DONE: rows initialized from words; words present in knownWords
        moved to `duplicates`; non-dupes become rows with `kept: true`; step → "step-a"
      - DEDUPE_ERROR: sets dedupeError; step stays renderable
      - TOGGLE_WORD: flips `kept` on the targeted row id only (immutable spread)
      - EDIT_WORD: updates `word` on targeted row only
      - REMOVE_WORD: removes the targeted row entirely
      - SELECT_ALL / SELECT_NONE: every row `kept` true / false respectively
      - TRANSLATE_START: step → "translating"
      - TRANSLATION_ROW_DONE: sets `nativeText` on the matching translationRow,
        clears its translationError
      - TRANSLATION_ROW_ERROR: sets `translationError` on the matching row,
        leaves nativeText empty; does NOT affect other rows
      - EDIT_NATIVE / EDIT_TARGET: update only the targeted translationRow field
      - COMMIT_START: step → "committing"
      - COMMIT_DONE: step → "success"; addedCount/failedCount set from payload
      - BACK_TO_STEP_A: step → "step-a"
      - default/unknown action: returns state unchanged (referential or deep-equal)

    `describe("translation fan-out")` — RVW-03:
      - mockFetch resolves `{ translation: "..." }` for some words and rejects /
        non-ok for others; assert the orchestration helper (the function Wave 2
        exports that wraps `Promise.allSettled` over `/api/translate`) maps
        fulfilled→nativeText and rejected→translationError copy
        "Translation unavailable — enter manually." per row, and that a single
        failure never throws / never blocks the others
      - assert request body shape: `{ text: word, sourceLang: targetLang, targetLang: nativeLang }` (D-08 direction)

    `describe("batch commit")` — RVW-04:
      - with `saveImageCards` mocked to return mixed `[{ok:true},{ok:false},...]`,
        assert the commit orchestration computes accurate addedCount / failedCount
        and that skippedCount equals the duplicates length
      - continue-on-failure: a failing entry does not abort the batch

    `describe("cancel")` — RVW-05b:
      - Step A cancel path invokes the `onCancel` callback and makes ZERO
        `saveImageCards`/`saveCard` calls
      - Step B cancel path: same — onCancel called, zero commit calls

    Where Wave 2's exact exported helper names are not yet fixed, choose stable
    names and document them in the SUMMARY as the contract Wave 2 MUST satisfy
    (e.g. `reviewListReducer`, `isDuplicate`, `runTranslationFanOut`,
    `commitReviewRows`). Wave 2 implements against these names.
  </action>
  <verify>
    <automated>npx vitest run src/components/review-list.test.ts 2>&1 | grep -E "review-list.test|Cannot find module|FAIL|No test" ; echo "scaffold-present"</automated>
  </verify>
  <done>File exists; `grep -c 'describe(' src/components/review-list.test.ts` ≥ 5; the five describe names (isDuplicate, reviewListReducer, translation fan-out, batch commit, cancel) all present; the run is RED because `@/components/review-list` does not yet exist (expected — documented in SUMMARY); no syntax errors (the failure is module-not-found, not a parse error).</done>
</task>

<task type="auto">
  <name>Task 3: Append getSameLanguageDeckBackWords describe block to deck-actions.test.ts (RED)</name>
  <read_first>
    - src/lib/deck-actions.test.ts (entire file — extend it; reuse its existing vi.hoisted chain, auth/headers/cache mocks, beforeEach re-wire; follow its saveCard describe block as the structural analog)
    - 11-PATTERNS.md § "Test for getSameLanguageDeckBackWords" (lines 656–684 — exact describe/it shape, mockResolvedValueOnce sequencing for the two queries)
    - 11-RESEARCH.md § "Focus Area 2" (the two-query design: deck language+ownership lookup, then same-language back values; trimmed+lowercased Set)
    - 11-CONTEXT.md D-04/D-05 (match key semantics)
  </read_first>
  <files>src/lib/deck-actions.test.ts</files>
  <action>
    Extend the EXISTING `src/lib/deck-actions.test.ts` — do NOT create a new
    file, do NOT duplicate its mock setup. Add an import of
    `getSameLanguageDeckBackWords` from `@/lib/deck-actions` (alongside the
    existing imports) and append a new `describe("getSameLanguageDeckBackWords")`
    block following the existing `saveCard` describe block's structure and the
    existing session/db mock helpers (mockSession / mockNoSession / selectChain).

    Tests (concrete assertions; this block is RED until Wave 2 implements the
    action):
      - throws "Unauthorized" when `auth.api.getSession` resolves null
      - throws "Forbidden" when the target-deck ownership/language lookup
        returns no row (`selectChain.where.mockResolvedValueOnce([])`)
      - returns `new Set(["chien", "chat"])` when:
          first query (deck language) → `[{ language: "fr" }]`
          second query (card back values) → `[{ back: "Chien" }, { back: "  chat  " }]`
        proving trim + lowercase transform and same-language filtering
      - (sequencing note in a comment) the two `selectChain.where`
        `mockResolvedValueOnce` calls map to: (1) target deck language+ownership
        lookup, (2) innerJoin same-language back values

    Use the existing FAKE_DECK_ID / fixture constants in that file (or add one
    consistent with the existing style if absent).
  </action>
  <verify>
    <automated>npx vitest run src/lib/deck-actions.test.ts 2>&1 | grep -E "getSameLanguageDeckBackWords|deck-actions.test" ; grep -c 'getSameLanguageDeckBackWords' src/lib/deck-actions.test.ts</automated>
  </verify>
  <done>`grep -c 'describe("getSameLanguageDeckBackWords"' src/lib/deck-actions.test.ts` == 1; the three `it(...)` cases (Unauthorized, Forbidden, trimmed-lowercase Set) present; the new block is RED (action not yet implemented) while every PRE-EXISTING describe block in deck-actions.test.ts still passes (regression: `npx vitest run src/lib/deck-actions.test.ts -t "saveCard"` green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action (`saveCard`) | This plan only widens a TypeScript union on the existing, already-authz'd `saveCard`. No new runtime trust boundary is introduced in Wave 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `saveCard` source union widening | accept | The `source` column is free `text` already; widening the TS union adds no new privilege and changes no auth path. Existing `saveCard` auth + deck-ownership check is untouched. No mitigation required. |
| T-11-02 | (n/a) | test scaffolds | accept | Test files contain no secrets, no image bytes, no extracted-word logging. Pure mocked-unit scaffolds. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (Task 1 type gate green).
- `npx vitest run src/components/review-list.test.ts` is RED with a
  module-not-found for `@/components/review-list` (expected — Wave 2 turns it green).
- `npx vitest run src/lib/deck-actions.test.ts` — the new
  `getSameLanguageDeckBackWords` block RED; ALL pre-existing blocks GREEN.
- `npm test` — entire prior suite (Phases 1–10, incl. image-validation 8 and
  extract 12+6) stays green except the two intentionally-RED new scaffolds.
</verification>

<success_criteria>
- saveCard signature reads `source: "manual" | "wordlist" | "image",` and tsc is clean.
- review-list.test.ts exists with the 5 required describe blocks, RED by design.
- deck-actions.test.ts extended (not replaced) with the getSameLanguageDeckBackWords block, RED by design, prior blocks still green.
- No regression in the Phases 1–10 suite.
</success_criteria>

<output>
After completion, create `.planning/phases/11-review-commit/11-01-SUMMARY.md`.
Record explicitly: the exported-symbol contract Wave 2 must satisfy
(`reviewListReducer`, `isDuplicate`, plus the chosen fan-out / commit
orchestration helper names), and that review-list.test.ts +
the new deck-actions.test.ts block are intentionally RED awaiting Waves 1–2.
</output>
