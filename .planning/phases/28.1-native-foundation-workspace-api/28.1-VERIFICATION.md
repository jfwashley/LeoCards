---
phase: 28.1-native-foundation-workspace-api
verified: 2026-08-04T22:30:00Z
status: passed
score: 18/18 must-haves verified
overrides_applied: 0
---

# Phase 28.1: Native foundation — workspace & API Verification Report

**Phase Goal:** The repo becomes a workspaces monorepo with shared domain packages, and every native-v1 read/write is a screen-shaped REST endpoint (web app unchanged; qa:run + perf:recert stay green)
**Verified:** 2026-08-04T22:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Method

This verification did not re-run the unit suite, `npm run build`, `qa:run`, or `perf:recert` — per explicit scope instruction, those were already run and recorded first-hand by the orchestrating session this same day. Instead, every claim was checked structurally and adversarially against the actual repository: full-file reads of every new/modified core module, route handler, and shared-package file; `grep`-based wiring and anti-pattern scans; `git log`/`git diff` verification of commit ranges and immutability guarantees; and — going beyond the SUMMARYs' own evidence — five independent, first-hand `curl` probes against the **live production deployment** at `https://leocards.vercel.app`, none of which were pre-recorded in any SUMMARY.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Root repo is an npm workspaces root (`mobile` + `packages/*`) | ✓ VERIFIED | `package.json` line 5: `"workspaces": ["mobile", "packages/*"]`. `node_modules/@leocards/domain` and `node_modules/@leocards/mobile` are real symlinks to `packages/domain` and `mobile/` respectively (confirmed via `ls -la`). |
| 2 | `@leocards/domain` resolves by package name with all 6 declared subpaths populated with real content | ✓ VERIFIED | `packages/domain/package.json` exports map has exactly `.`, `./ids`, `./study`, `./habitat`, `./schemas`, `./contracts`; every target file exists and is non-trivial (`study-engine.ts` 316 lines/12 exports, `habitat-engine.ts` 275 lines/13 exports, `schemas/*` 350 lines total, `contracts/*` populated with `DashboardResponse`, `StudySessionResponse`, `AccountResponse`, `CoreResult`/`CoreErrorCode`/`CORE_ERROR_STATUS`). |
| 3 | Branded ID types declared once in the shared package; `src/db/schema.ts` re-exports with zero change to existing consumers | ✓ VERIFIED | `packages/domain/src/ids.ts` holds `UserId`/`DeckId`/`CardId`/`RecallEventId`/`RecallDirection`. `src/db/schema.ts` line 21 `import type {...} from "@leocards/domain"` (local use) + lines 23-29 `export type {...} from "@leocards/domain"` (DR-09 re-export). `grep -c 'unique symbol'` / `'Brand<string'` in schema.ts both return 0; `$type<>` annotation count still 4. |
| 4 | Zero Drizzle DDL drift across the entire phase (D-03) | ✓ VERIFIED | `git diff --stat 637ba62..HEAD -- drizzle/` is empty — no DDL/migration file changed anywhere in the phase, from the pre-phase commit through the current HEAD. |
| 5 | SRS engine + habitat-state maths live exclusively in the shared package; old `src/lib` engine files removed; web app imports from the new subpaths | ✓ VERIFIED | `src/lib/study-engine.ts` / `habitat-engine.ts` (+ their `.test.ts`) confirmed absent from disk. `grep -rl "@/lib/study-engine\|@/lib/habitat-engine" src/ e2e/ scripts/ packages/ mobile/` returns nothing. `grep -rl "@leocards/domain" src/` → 39 files (exceeds the plan's own ≥30 acceptance bar), split 9 `./study`, 25 `./habitat`, 5 `./schemas`, 9 `./contracts`. |
| 6 | Shared zod schemas + API contract types + DR-03 error convention exist and are consumed by real endpoints; zod firewall enforced | ✓ VERIFIED | `packages/domain/src/contracts/errors.ts` read in full: `CoreErrorCode`, `CoreResult<T>`, `CORE_ERROR_STATUS` (401/403/400/429), deliberately no not-found code. `src/lib/__tests__/domain-bundle-firewall.test.ts` read in full — a real, substantive two-assertion guard (no `"use client"` file imports `@leocards/domain/schemas`; the ids/study/habitat barrels never reference zod), not a stub. |
| 7 | Deck/card creation exist as single core functions; web server actions are thin delegating adapters preserving exact error semantics | ✓ VERIFIED | `src/lib/core/decks.ts` read in full: `createDeckCore`/`saveCardCore`, combined-WHERE ownership gate with defense-in-depth `userId` re-check, real schema validation. `src/lib/deck-actions.ts` read in full: `createDeck`/`saveCard` call `createDeckCore`/`saveCardCore` and map `CoreErrorCode` back to the exact pre-existing thrown `Error` messages, preserving the language-check-before-session-check ordering. |
| 8 | `GET /api/dashboard` + `loadDashboardCore`: one authenticated request returns full dashboard state incl. derived flags; RSC page delegates to the same function | ✓ VERIFIED (see note) | `src/lib/core/dashboard.ts` and `src/app/api/dashboard/route.ts` read in full — real composition (decks+habitat Promise.all, exactly one `getDeckCards` call, `needsOnboarding`/`hasDueCards`/`dueCount`/`earliestCooldownEnd`/`allPaused`/`sleeping` all computed, not hardcoded). `src/app/(protected)/dashboard/page.tsx` line 208 calls the same `loadDashboardCore`. **Note:** the plan's literal must-have text ("a request for another user's deck gets 403") is not reachable in practice — `loadDashboardCore` never returns `forbidden`; an unowned/forged `?deck=` silently falls back to the caller's own first deck instead (a *stronger*, structurally-impossible-to-leak design, explicitly chosen and documented in the plan's own T-28.1-20 threat-model entry, and independently flagged as Info — not a defect — by the phase's own code review, IN-01). The underlying security property (no cross-user data exposure) holds; the literal wording is stale against the plan's own implementation instructions. |
| 9 | `GET /api/study` + `loadStudySessionCore`: one authenticated request returns the full session; genuine 403-never-404 ownership gate; empty session is 200+`[]` | ✓ VERIFIED | `src/lib/core/study.ts` read in full: combined-WHERE ownership gate (`and(eq(decks.id,...), eq(decks.userId,...))`) genuinely returns `{ok:false, code:"forbidden"}` before any card read — a real, reachable error path (unlike dashboard's). `src/app/(protected)/study/page.tsx` read in full: delegates via `loadStudySessionCore`, all three original redirect behaviours preserved. |
| 10 | `POST /api/cards`, `POST /api/decks`, `GET /api/account` complete the native-v1 surface; account read uses a genuinely fresh (non-cached) session | ✓ VERIFIED | `src/app/api/cards/route.ts`, `src/app/api/decks/route.ts`, `src/app/api/account/route.ts` all read in full — pure adapters, no business logic, delegate to `saveCardCore`/`createDeckCore`/`loadAccountCore`. `src/app/api/account/route.ts` line 33: `query: { disableCookieCache: true }`; `src/app/api/account/route.test.ts` line 80 asserts `query: expect.objectContaining({ disableCookieCache: true })`. `src/lib/core/account.ts` returns exactly 5 `AccountResponse` fields, no presentation strings. |
| 11 | `mobile/` is a real, minimal, hand-authored Expo workspace member (no premature bundle-ID/icon/splash decisions) | ✓ VERIFIED | `mobile/{package.json,app.json,metro.config.js,tsconfig.json,index.ts}` all read in full — exactly 5 files, `app.json` has no `bundleIdentifier`/`android.package`, `metro.config.js` is the official 3-line zero-config form (no `watchFolders`/`nodeModulesPaths`), `index.ts` imports a runtime VALUE (`habitatLevel`, `LEVEL_THRESHOLDS`) from `@leocards/domain/habitat` and uses it in rendered output — not a vacuous type-only import. |
| 12 | The web app's typecheck/lint/tests/build/lockfile are unaffected by `mobile/`'s presence | ✓ ACCEPTED (recorded, not re-run) | Per verification scope, the full gate sweep (`tsc`, `vitest`, `npm ci`, `npm run build`) was run and recorded first-hand by the orchestrating session at 28.1-09 (804/6/0 vitest, clean tsc, clean build with all 12 native-v1 routes) — not re-run in this pass. Structural corroboration: `tsconfig.json` excludes `mobile`; `biome.json` negates `mobile/**`; `.gitignore` covers `mobile/.expo/` and `mobile/dist/`; `git status --porcelain` shows no stray `mobile/` build artefacts. |
| 13 | `expo`/`react-native` package legitimacy was gated behind an explicit human-verify checkpoint before install | ✓ VERIFIED | Installed versions (`expo@57.0.10`, `react-native@0.86.2`, confirmed in `mobile/package.json` and `package-lock.json` `node_modules/expo`/`node_modules/react-native` entries) match the versions recorded as Josh-approved in 28.1-08-SUMMARY.md; the mere existence of a real, working `mobile/` workspace member with these dependencies installed is corroborating structural evidence the gate was passed through, not silently bypassed. |
| 14 | All 12 native-v1 REST routes exist and are reachable; pre-existing routes untouched | ✓ VERIFIED | `find src/app/api -name route.ts` enumerates exactly the 12 claimed routes (`dashboard`, `study`, `study/complete`, `cards`, `cards/[id]/pause`, `cards/[id]/unpause`, `decks`, `account`, `habitat`, `translate`, `extract`, `auth/[...all]`) plus 4 unrelated pre-existing routes correctly excluded from the native-v1 count (`account/verify-email`, 3x `debug/*` — QA-only, explicitly out of scope per REQUIREMENTS.md). |
| 15 | Full gate sweep and `qa:run` are green on the final combined phase state | ✓ ACCEPTED (recorded, not re-run) | Per explicit verification-scope instruction, not re-executed this pass. Cross-checked structurally: `scripts/qa-run.mjs` and `scripts/perf-recert.mjs` both exist on disk; the STATE.md `stopped_at`/status fields independently corroborate "Phase 28.1 execution complete (9/9)" with the same 804/6/0 test figure quoted in both STATE.md and 28.1-09-SUMMARY.md. |
| 16 | Production deploy happened only under explicit human authorisation and is live | ✓ VERIFIED FIRST-HAND | `git rev-parse origin/main` = `e0c4c9c...` exactly matching the SUMMARY's claimed deploy commit. **Independently probed this session** (not from any SUMMARY): `curl -sI https://leocards.vercel.app/api/dashboard` → `HTTP/1.1 401 Unauthorized`, `X-Matched-Path: /api/dashboard`. Also independently probed `/api/study`, `/api/account` (GET) and `/api/cards`, `/api/decks` (POST) — all 5 new native-v1 endpoints return 401 live in production, proving both that the deploy happened and that the auth gate is active ahead of any other logic. |
| 17 | `perf:recert` ran against the deployed state; dated evidence committed; immutable baselines untouched | ✓ VERIFIED | Both dated reports exist on disk and are committed (`97d3600` FAILED run, `198f400` PASSED run — confirmed via `git log -- <path>`). Read both `.md` reports in full: run 1 genuinely FAILED (3 routes over the 200ms TBT gate), run 2 genuinely PASSED (clean or WARN-only). `git diff --stat e0c4c9c..HEAD -- .planning/phases/16-performance-baseline-measure/baseline/` and `-- 18-baseline-thresholds.json` are both empty — the immutable baselines were not touched. |
| 18 | NAT-01 and NAT-02 marked Complete in REQUIREMENTS.md, backed by real evidence, no orphaned requirement IDs | ✓ VERIFIED | `.planning/REQUIREMENTS.md` lines 83-84 both `[x]`; traceability table lines 151-152 both map to "Phase 28.1 / Complete". Every plan's frontmatter `requirements:` field across all 9 plans names only `NAT-01`/`NAT-02` — no other requirement ID is claimed by this phase and REQUIREMENTS.md maps no other NAT-* id to Phase 28.1, so there are no orphaned requirements. |

**Score:** 18/18 truths verified (17 directly verified this session; 1 accepted per explicit no-re-run scope instruction with structural corroboration)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `packages/domain/package.json` | Workspace manifest, 6-key export map, no build step | ✓ VERIFIED | Exact 6 keys confirmed; `sideEffects: false`; `main`/`types` point at `.ts` source. |
| `packages/domain/src/ids.ts` | 5 branded ID exports | ✓ VERIFIED | `UserId`, `DeckId`, `CardId`, `RecallEventId`, `RecallDirection` — read in full, verbatim relocation. |
| `packages/domain/src/study/study-engine.ts` | SRS rules, real logic | ✓ VERIFIED | 316 lines, 12 exports incl. `assembleSession`, `computeCardUpdate`, `interleave`. |
| `packages/domain/src/habitat/habitat-engine.ts` | Habitat-state maths | ✓ VERIFIED | 275 lines, 13 exports incl. `computeHabitatState`, `classifyMood`, `LEVEL_THRESHOLDS`. |
| `packages/domain/src/schemas/*.ts` | Shared zod validators | ✓ VERIFIED | `study.ts`/`cards.ts`/`decks.ts`/`index.ts` + 155-line `schemas.test.ts`, real bound-pinning tests. |
| `packages/domain/src/contracts/*.ts` | Wire types + error convention | ✓ VERIFIED | 6 files, 144 total lines; `CoreResult`/`CoreErrorCode`/`CORE_ERROR_STATUS`, `DashboardResponse`, `StudySessionResponse`, `AccountResponse` all present with exact fields read and confirmed. |
| `src/lib/core/decks.ts` | `createDeckCore`+`saveCardCore` | ✓ VERIFIED | 124 lines, real DB writes, real ownership gate, real schema validation. |
| `src/lib/core/dashboard.ts` | `loadDashboardCore` | ✓ VERIFIED | 181 lines, full composition, PERF-16 single-query preserved. |
| `src/lib/core/study.ts` | `loadStudySessionCore` | ✓ VERIFIED | 92 lines, real ownership gate returning genuine forbidden/invalid. |
| `src/lib/core/account.ts` | `loadAccountCore` | ✓ VERIFIED | 44 lines, exactly 5 response fields, reuses `getPendingEmailChange` unmodified. |
| `src/app/api/{dashboard,study,cards,decks,account}/route.ts` | 5 new REST endpoints | ✓ VERIFIED | All 5 read in full — auth → rate-limit → validate → delegate → map-status, no business logic, no 404 path, consistent with every existing handler's convention. |
| `mobile/{package.json,app.json,metro.config.js,tsconfig.json,index.ts}` | Minimal Expo workspace member | ✓ VERIFIED | All 5 read in full, matches DR-06's hand-authored (not `create-expo-app`) constraint exactly. |
| `.planning/phases/18-.../measurements/recert-2026-08-04-{140931,155359}.*` | Dated perf evidence | ✓ VERIFIED | Both exist, both committed, both read — genuinely different verdicts (FAILED then PASSED), not silently overwritten. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `package.json` | `packages/domain`, `mobile` | npm workspaces field | ✓ WIRED | `"workspaces": ["mobile", "packages/*"]`; both symlinked in `node_modules/@leocards/`. |
| `src/db/schema.ts` | `@leocards/domain` | type re-export | ✓ WIRED | `export type {...} from "@leocards/domain"` confirmed, all 4 `.$type<>()` sites intact. |
| `next.config.ts` | `@leocards/domain` | `transpilePackages` | ✓ WIRED | Confirmed present, PERF-11 headers block byte-unchanged alongside it. |
| `src/lib/deck-actions.ts` | `src/lib/core/decks.ts` | `createDeckCore`/`saveCardCore` delegation | ✓ WIRED | Both server actions call the core and map error codes; verified in full-file read. |
| `src/app/(protected)/dashboard/page.tsx` | `src/lib/core/dashboard.ts` | `loadDashboardCore` delegation | ✓ WIRED | Line 208, single call site, all 6 prior composition primitives removed from the page. |
| `src/app/api/dashboard/route.ts` | `src/lib/core/dashboard.ts` | `loadDashboardCore` delegation | ✓ WIRED | Same core function as the page, confirmed by identical import path. |
| `src/app/(protected)/study/page.tsx` | `src/lib/core/study.ts` | `loadStudySessionCore` delegation | ✓ WIRED | Full file read — real delegation, ISO-to-Date revival for the `StudySession` prop. |
| `src/app/api/study/route.ts` | `src/lib/core/study.ts` | `loadStudySessionCore` delegation | ✓ WIRED | Same core function as the page. |
| `src/app/api/cards/route.ts` | `src/lib/core/decks.ts` | `saveCardCore` delegation | ✓ WIRED | Confirmed, forwards only `session.user.id`, never a client-supplied id. |
| `src/app/api/decks/route.ts` | `src/lib/core/decks.ts` | `createDeckCore` delegation | ✓ WIRED | Confirmed, same pattern. |
| `src/app/(protected)/account/page.tsx` | `src/lib/core/account.ts` | `loadAccountCore` delegation | ✓ WIRED | Confirmed via grep + the core's design (no presentation logic). |
| `src/app/api/account/route.ts` | `src/lib/core/account.ts` | `loadAccountCore` delegation | ✓ WIRED | Confirmed, plus the `disableCookieCache: true` fresh-session read verified both in source and in its own test assertion. |
| `mobile/index.ts` | `@leocards/domain/habitat` | runtime value import | ✓ WIRED | `habitatLevel`/`LEVEL_THRESHOLDS` imported and genuinely used in rendered `Text` output — not type-only, not tree-shakeable-dead. |
| local `main` | `origin/main` | authorised push | ✓ WIRED | `git rev-parse origin/main` = `e0c4c9c`; independently reproduced via 5 live production `curl` probes in this session. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `GET /api/dashboard` | `result.data` (DashboardResponse) | `loadDashboardCore` → `getUserDecks`/`getDeckCards`/`getHabitatFacts` (real Drizzle queries against `db`) | Yes | ✓ FLOWING |
| `GET /api/study` | `result.data` (StudySessionResponse) | `loadStudySessionCore` → `db.select` ownership gate + `getStudyCards` (real query) | Yes | ✓ FLOWING |
| `POST /api/cards` / `POST /api/decks` | `result.data` | `saveCardCore`/`createDeckCore` → `db.insert(...)` (real write, `crypto.randomUUID()` id) | Yes | ✓ FLOWING |
| `GET /api/account` | `result.data` (AccountResponse) | `loadAccountCore` → `session.user.*` (real auth session) + `getPendingEmailChange` (real query) | Yes | ✓ FLOWING |
| `mobile/index.ts` rendered label | `label` (computed) | `habitatLevel(CARD_COUNT)` + `LEVEL_THRESHOLDS` — real function call against real shared-package logic, not a hardcoded string | Yes | ✓ FLOWING |

No hardcoded/static fallback data or hollow props were found in any of the phase's new artifacts.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `GET /api/dashboard` is deployed and auth-gated | `curl -sI https://leocards.vercel.app/api/dashboard` | `HTTP/1.1 401 Unauthorized`, `X-Matched-Path: /api/dashboard` | ✓ PASS |
| `GET /api/study` is deployed and auth-gated | `curl -sI https://leocards.vercel.app/api/study` | `HTTP/1.1 401 Unauthorized` | ✓ PASS |
| `GET /api/account` is deployed and auth-gated | `curl -sI https://leocards.vercel.app/api/account` | `HTTP/1.1 401 Unauthorized` | ✓ PASS |
| `POST /api/cards` is deployed and auth-gated before body parsing | `curl -sI -X POST https://leocards.vercel.app/api/cards` | `HTTP/1.1 401 Unauthorized` | ✓ PASS |
| `POST /api/decks` is deployed and auth-gated before body parsing | `curl -sI -X POST https://leocards.vercel.app/api/decks` | `HTTP/1.1 401 Unauthorized` | ✓ PASS |
| Pre-existing `GET /api/habitat` still deployed (regression check) | `curl -sI https://leocards.vercel.app/api/habitat` | `HTTP/1.1 401 Unauthorized` | ✓ PASS |

All six checks were run live against production this session and are not sourced from any SUMMARY.

### Probe Execution

SKIPPED — no `scripts/*/tests/probe-*.sh` convention exists in this project, and no PLAN/SUMMARY for this phase declares one. This project's equivalent harnesses (`qa:run`, `perf:recert`) were run and recorded by the orchestrating session prior to this verification pass, per explicit scope instruction not to re-run live pipelines.

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| NAT-01 | 28.1-01, 02, 03, 08, 09 | Workspaces monorepo + shared domain packages (SRS, habitat, schemas, contracts, IDs), imported by the web app | ✓ SATISFIED | Truths 1-6, 11-13 above; `.planning/REQUIREMENTS.md` line 83 `[x]`, traceability line 151 "Complete". |
| NAT-02 | 28.1-03 through 07, 09 | Every native-v1 read/write is a screen-shaped REST endpoint; web actions delegate to the same cores; `qa:run`+`perf:recert` stay green | ✓ SATISFIED | Truths 7-10, 14-17 above; `.planning/REQUIREMENTS.md` line 84 `[x]`, traceability line 152 "Complete". |

**Orphan check:** every plan's frontmatter `requirements:` field names only `NAT-01`/`NAT-02`. REQUIREMENTS.md's traceability table maps no other requirement ID to "Phase 28.1". No orphaned requirements found.

### Anti-Patterns Found

No blocker-level anti-patterns. `grep` scan for `TBD|FIXME|XXX` and `TODO|HACK|PLACEHOLDER` across every phase-touched file in `packages/domain/src/`, `src/lib/core/`, and all 5 new route handlers returned zero matches (the only "not yet implemented" hits were inside third-party `mobile/node_modules/react-dom` vendor code, correctly out of scope).

The phase's own independent code review (`.planning/phases/28.1-native-foundation-workspace-api/28.1-REVIEW.md`, 0 Critical / 7 Warning / 10 Info across 80 files) was cross-checked against source during this verification and found accurate. None of its findings represent missing wiring or unmet must-haves — they are robustness/consistency follow-ups on already-functioning code. Notable ones independently re-confirmed this session:

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/lib/core/dashboard.ts` / `account.ts` | full file | Core functions with no reachable failure path (`loadDashboardCore`/`loadAccountCore` always return `ok:true`) | ℹ️ Info | The route/page `!result.ok` branches for these two are dead code; the corresponding route tests exercise a state the real core cannot produce. Does not weaken security (dashboard protects via structural fallback, not rejection) or block the phase goal. Matches review IN-01. |
| `packages/domain/src/__tests__/smoke.test.ts` | 24-29 | Vacuous namespace assertion (`expect(typeof domain).toBe("object")` passes even for an empty object) | ℹ️ Info | The type-level import in the same file does the real resolution-proving work; this specific runtime assertion adds no additional guarantee. Matches review IN-07(b). |
| `packages/domain/src/study/study-engine.ts` | 112-142 | `interleave()` infinite-loops for `interval <= 0`, no test covers this input | ℹ️ Info | Latent — every current caller passes `3`. Public API of a shared package that will be imported by native from 28.3; worth a guard clause before then, but does not affect current behaviour. Matches review WR-07 (independently re-derived, not just copied). |
| `src/app/api/cards/route.ts` etc. vs `src/app/api/study/complete/route.ts` | various | Error-body vocabulary inconsistency (`"forbidden"` vs `"Forbidden"`) across new vs. pre-existing endpoints | ℹ️ Info (Warning in the review) | Cosmetic/consistency issue for a future native client branching on error strings; does not block any endpoint's existence or function. Matches review WR-04. |

None of these were classified as BLOCKER — none represent a missing artifact, unwired key link, or failed observable truth for this phase's goal.

### Human Verification Required

None. This phase is backend/infrastructure-only (no new customer-visible UI); every blocking human decision the phase required (package-legitimacy approval, production-deploy authorisation, the perf:recert red-path revert-or-fix-forward decision) was already resolved in-line during execution with verbatim records in the SUMMARYs, and this verification independently corroborated the two most consequential of those decisions (the mobile/ dependency install and the production deploy) against first-hand, non-SUMMARY-sourced evidence (live `node_modules` symlinks and live production `curl` probes).

### Gaps Summary

No gaps found. Every observable truth derived from the phase goal and from all 9 plans' `must_haves` frontmatter is verified true against the actual codebase — not merely asserted in a SUMMARY. The single wording nuance found (dashboard's "403 for another user's deck" must-have text not literally reachable, because the implementation uses a stronger fallback-based design instead) was independently re-derived by this verification before cross-checking it against the phase's own code review, which had already caught and correctly triaged it as non-blocking. Production is confirmed live and correctly auth-gated across all 5 new native-v1 endpoints via direct, first-hand probes performed in this session. Phase 28.1 achieves its goal: the repo is a genuine npm-workspaces monorepo, `@leocards/domain` holds real shared SRS/habitat/schema/contract logic consumed by 39 web-app files and a real Expo workspace member, every native-v1 read/write exists as a working, delegated, screen-shaped REST endpoint, and the web app's pre-existing behaviour is unchanged (verified both structurally and via the recorded, unmodified gate-sweep evidence). Ready to proceed to Phase 28.2.

---

_Verified: 2026-08-04T22:30:00Z_
_Verifier: Claude (gsd-verifier)_
