---
phase: 28.1-native-foundation-workspace-api
plan: 09
subsystem: infra
tags: [phase-gate, qa-run, deploy, vercel, perf-recert, npm-audit]

# Dependency graph
requires:
  - phase: 28.1-01..28.1-08
    provides: "the complete workspaces + @leocards/domain + native-v1 API + mobile/ Expo state this gate certifies"
provides:
  - "Phase 28.1 certified: full gate sweep green on the final combined state (tsc root+domain, vitest 804/6/0, npm ci, build with all 12 native-v1 routes, qa:run 5/5 + cleanup zero-residue)"
  - "Production deploy of the entire phase: origin/main advanced 637ba62 → e0c4c9c under Josh's explicit authorisation, Vercel deploy verified successful, prod /api/dashboard auth gate probed live (401 + X-Matched-Path)"
  - "perf:recert certification of the deployed code: two-run record (FAILED-then-PASSED) resolving a measurement-contention false alarm via Josh's quiet-re-run decision; both reports committed immutably per D-07"
  - "NAT-01 and NAT-02 flipped to Complete in REQUIREMENTS.md (all contributing plans landed)"
  - "npm-audit risk register for the follow-up dependency-security work (1 critical better-auth, 18 high)"
affects: [28.2, 28.3, 28.4, 28.5, "18-06 (field window continues against the new prod deploy)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-run D-07 re-cert record: a FAILED lab run is committed as permanent evidence, analysed with explicit for/against reasoning, escalated to Josh, and resolved by a deliberately quiet re-run — never silently re-run until green"
    - "Cross-context deploy execution: when a subagent's git network ops are permission-blocked, the orchestrator executes the authorised push in the main session and the subagent verifies via local refs + public APIs + first-hand prod probes"

key-files:
  created:
    - .planning/phases/18-field-validation-guardrails/measurements/recert-2026-08-04-140931.md (+ .json + artifacts dir — run 1, FAILED, committed 97d3600)
    - .planning/phases/18-field-validation-guardrails/measurements/recert-2026-08-04-155359.md (+ .json + artifacts dir — run 2, PASSED, committed 198f400)
    - .planning/phases/28.1-native-foundation-workspace-api/28.1-09-SUMMARY.md
  modified:
    - .planning/REQUIREMENTS.md (NAT-01/NAT-02 → [x] + traceability Complete)
    - .planning/STATE.md
    - .planning/ROADMAP.md

# Decisions (checkpoint outcomes, verbatim)
decisions:
  - "Task 2 deploy gate — Josh: \"authorised — push\""
  - "Task 3 red-path gate — Josh: \"Quiet re-run (Recommended)\""
---

# 28.1-09 — Phase gate: full sweep, deploy, post-deploy re-cert

**One-liner:** Every gate proven green on the final combined state; the whole phase deployed to production under Josh's authorisation and certified by a two-run perf:recert whose first-run failure was diagnosed (with committed evidence) as measurement contention and disproven on an idle machine.

## Task 1 — full pre-push gate sweep

| Gate | Result | Detail |
|---|---|---|
| `npx tsc --noEmit` (root + packages/domain) | PASS | exit 0 both |
| `npx vitest run` | PASS | 804 passed / 6 skipped / 0 failed — clean, no flakes triggered |
| `npm ci` | PASS | lockfile installs exactly as committed (2m29s) |
| `npm run build` | PASS | all 29 routes incl. the full native-v1 surface |
| `qa:run` | **PASS 5/5 + cleanup** | QAJ-01..05 all green incl. real L1→L2→L3 habitat crossings; zero residue; 25 stale `*test.local` users swept. **Resolves the blocker carried since 28.1-03.** |
| `npx biome ci src/` | FAIL (pre-existing) | byte-identical to the 16-file debt documented in deferred-items.md at 28.1-08; zero deploy impact (`npm run build` never runs biome) |

NAT-01 evidence: workspaces `["mobile","packages/*"]`; 39 `@leocards/domain` import sites in src/ (≥30 criterion); all six package subpaths populated; Metro cross-workspace proof carried from 28.1-08. NAT-02 evidence: endpoint-to-capability mapping confirmed via source grep — dashboard/study/study-complete/cards/decks/account all validated by shared schemas and delegating to the same `src/lib/core/*` functions the web's server actions call.

**New risk register (first-ever `npm audit` of this lockfile):** 1 critical — `better-auth` ≤1.6.21 (account-takeover-class advisories incl. magic-link/email-OTP pre-account hijacking, OAuth auto-link takeover, stale sessions after deletion, stored XSS; non-major fix available). 18 high — `next` (22 advisories, fix 16.3.0), `drizzle-orm` (identifier-escaping SQLi), `undici`, `sharp`/libvips, others. ~39 moderate/low. **All pre-existing:** no 28.1 plan changed any flagged package's version, and prod ran these exact versions before this phase's deploy — the push changed nothing about this exposure. Recommendation: a dedicated dependency-security phase next (better-auth first, with auth-focused regression testing), not an inline fix minutes before a deploy.

## Task 2 — production deploy authorisation and execution

- Checkpoint presented with the five decision inputs (install footprint +59s/+4866 lockfile lines; bundle parity with the 18-03 baseline to the KB; qa:run green; Metro proof; first-deploy-of-phase confirmation via live fetch) plus the npm-audit register and the two ride-along commits (`86982c2` reduced-motion opt-in from the concurrent session; `0eeed10` the orchestrator's VS1 test repair).
- **Pre-authorisation side-task:** habitat e2e (`e2e/13-habitat-states.spec.ts`) run in full — **8 passed / 0 failed / 4 project-gated skips**, including the first-ever green run of `86982c2`'s reduced-motion opt-in spec on both Playwright projects. This closed the concurrent session's stated precondition ("run habitat e2e, then push on Josh's word") for its ride-along commit.
- **Josh's authorisation, verbatim: "authorised — push".**
- Execution wrinkle: the executor subagent's `git push` (and even read-only `git fetch`) were denied by Claude Code's auto-mode permission classifier. Correctly not worked around. The orchestrator executed the push from the main session: `637ba62..e0c4c9c  main -> main`, one ref, no force.
- Verification, three independent ways: local reflog shows `update by push`; GitHub's public commit-status API reported the Vercel context `success` ("Deployment has completed"); a first-hand probe of `https://leocards.vercel.app/api/dashboard` returned `401` with `X-Matched-Path: /api/dashboard` — the native-v1 surface is live behind its auth gate.

## Task 3 — post-deploy perf:recert (two-run record, D-07)

**Run 1 — `recert-2026-08-04-140931`, committed `97d3600`, Overall FAILED.** /dashboard TBT 388ms, /deck/new-card 387ms, /deck/browse 303ms (all > the 200ms absolute mobile gate); /study WARN-only; nav-gate PASS; harness exit clean. Analysis committed alongside: two of the three failing routes have zero 28.1 code changes anywhere in the phase; /dashboard + /study first-load JS byte-matched the Phase 18-03 baseline (12/13 chunk hashes identical); /deck/browse's 5,222ms Lighthouse bootupTime was a 5-10x outlier against its own same-run siblings (968/411/811ms); Lighthouse threw internal trace-engine errors during the run; the differently-methodologied nav-gate passed. Honest counters recorded: the baseline was measured on this same machine, and the drift spread (195-646%) is not uniform. Machine-load snapshot: 23 node/chrome-family processes alive incl. two concurrent claude sessions (678s/613s cumulative CPU).

**Josh's gate decision, verbatim: "Quiet re-run (Recommended)"** — a transparent diagnostic re-run on an idle machine, committed either way, per AGENTS.md's revert-or-fix-forward gate (explicitly not the prohibited silent-rerun-until-green).

**Run 2 — `recert-2026-08-04-155359`, committed `198f400`, Overall PASSED.** /study and /deck/browse clean PASS; /dashboard and /deck/new-card WARN-only drift (+108%/+83% — D-09 drift warnings are advisory by design and never failures); nav-gate PASS. Same deployed code, same machine, idle conditions: the contention hypothesis is empirically confirmed and the deployed phase is certified.

## Commits

| Commit | What |
|---|---|
| `97d3600` | docs(28.1-09): run-1 FAILED perf:recert report as permanent evidence (D-07) |
| `198f400` | docs(28.1-09): run-2 PASSED quiet-machine perf:recert re-run (D-07) |
| (close-out) | docs(28.1-09): REQUIREMENTS NAT-01/02 flip + STATE + ROADMAP + this SUMMARY |

## Deviations

- No functional deviations. The gate itself worked as designed — including the red path: the first FAILED report was committed untouched and escalated rather than silently re-run.
- Process deviations, recorded honestly: (1) the executor subagent's git network operations were permission-blocked, so the Josh-authorised push was executed by the orchestrator in the main session (verified three ways above); (2) the long-lived executor agent stranded twice awaiting background-task completion notifications and finally stalled (stream watchdog) mid-close-out — the orchestrator completed the close-out inline. The REQUIREMENTS.md flip was the executor's own edit (verified); SUMMARY/STATE/ROADMAP are the orchestrator's.
- During the stall-recovery, the executor also reported (and correctly ignored) unattributed "system-reminder" content in its context claiming tools it didn't have and an auto-mode framing contradicting the project config — treated as inert, deferred to the platform permission system. Recorded factually.

## Issues Encountered

- `.env.local` values are double-quoted; the first `grep | cut` extraction passed literal quotes into `neon()` (rejected as invalid URL) — fixed by stripping quotes. The file must never be `source`d (documented trap).
- Reproducible Windows/libuv assertion (`!(handle->flags & UV_HANDLE_CLOSING)`) when spawning a `node -e fetch()` readiness probe from the same shell as a backgrounded dev server — worked around by reading the server's log for the Ready banner.
- Expo/Hermes and biome items carried from 28.1-08 in deferred-items.md, unchanged.

## User Setup Required

None. **Pending item:** local `main` is ahead of `origin/main` by the two D-07 report commits plus the close-out commit(s). These are .planning docs only; they ship whenever Josh next authorises a push.

## Next Phase Readiness

- NAT-01 and NAT-02 are Complete; the native programme's foundation (workspaces, shared domain package, screen-shaped API, proven Expo member) is deployed and certified. 28.2 (Expo app shell & auth) can plan against a live API surface.
- Follow-ups on record: dependency-security phase (better-auth critical first); vitest count investigation (separate task); biome 16-file debt; keyboard-wake gap on designed habitat freezes (concurrent session's noted minor gap).
- Phase 18-06 remains blocked until on/after 2026-08-08 (D-03 field window) — the Speed Insights field data now accumulates against the 28.1 deploy.

## Self-Check: PASSED

Both recert report sets confirmed on disk and committed (`97d3600`, `198f400`); REQUIREMENTS.md NAT-01/NAT-02 lines confirmed `[x]`/Complete; prod probe, deploy status, and push reflog all verified first-hand this session; vitest/tsc/build results cross-checked against the executor's recorded outputs (identical). Close-out completed by the orchestrator after the executor stall; all facts in this SUMMARY are drawn from tool outputs in the orchestrating session's transcript, not reconstruction.

---
*Phase: 28.1-native-foundation-workspace-api*
*Completed: 2026-08-04*
