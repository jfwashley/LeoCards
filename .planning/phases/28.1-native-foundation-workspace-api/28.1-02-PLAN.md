---
phase: 28.1-native-foundation-workspace-api
plan: 02
type: execute
wave: 2
depends_on: ["28.1-01"]
files_modified:
  - packages/domain/src/study/study-engine.ts
  - packages/domain/src/study/study-engine.test.ts
  - packages/domain/src/study/index.ts
  - packages/domain/src/habitat/habitat-engine.ts
  - packages/domain/src/habitat/habitat-engine.test.ts
  - packages/domain/src/habitat/index.ts
  - src/lib/study-engine.ts
  - src/lib/study-engine.test.ts
  - src/lib/habitat-engine.ts
  - src/lib/habitat-engine.test.ts
  - src/app/(protected)/dashboard/page.tsx
  - src/app/(protected)/study/page.tsx
  - src/app/(protected)/debug/page.tsx
  - src/app/(protected)/habitat/page.tsx
  - src/app/api/cards/[id]/unpause/route.ts
  - src/app/api/study/complete/route.ts
  - src/app/api/study/complete/route.test.ts
  - src/app/api/study/__tests__/cooldown-config.test.ts
  - src/app/api/habitat/route.ts
  - src/app/api/debug/state/route.ts
  - src/app/api/debug/__tests__/state.test.ts
  - src/components/study-card.tsx
  - src/components/study-session.tsx
  - src/components/daybreak/h-mood-chip.tsx
  - src/components/daybreak/h-top.tsx
  - src/components/deck-view.tsx
  - src/components/deck-view.test.tsx
  - src/components/habitat-3d-canvas.tsx
  - src/components/habitat-hero.tsx
  - src/components/habitat-medallion.tsx
  - src/components/habitat-medallion.test.tsx
  - src/components/habitat-scene.tsx
  - src/components/habitat-video.tsx
  - src/lib/debug-cheat.ts
  - src/lib/habitat-3d/mood-decay.ts
  - src/lib/habitat-queries.ts
  - src/lib/habitat-tint.ts
  - src/lib/habitat-ui-utils.ts
autonomous: false
requirements: [NAT-01]

must_haves:
  truths:
    - "The SRS rules and habitat-state maths live in the shared package and are imported by the web app from `@leocards/domain/study` and `@leocards/domain/habitat`"
    - "No file under `src/` imports `@/lib/study-engine` or `@/lib/habitat-engine` any more, and those files no longer exist"
    - "Every relocated unit test still passes with identical assertions"
    - "The web app's production build still succeeds and its route bundles have not grown"
  artifacts:
    - path: "packages/domain/src/study/study-engine.ts"
      provides: "assembleSession, interleave, getCardStage, computeCardUpdate, earliestCooldownEnd, computeUnpauseUpdate, DEFAULT_COOLDOWN_MS, shuffleTake + session types"
      exports: ["assembleSession", "computeCardUpdate", "earliestCooldownEnd", "computeUnpauseUpdate", "DEFAULT_COOLDOWN_MS"]
    - path: "packages/domain/src/habitat/habitat-engine.ts"
      provides: "computeHabitatState, computeQuality, habitatLevel, classifyMood + habitat types and constants"
      exports: ["computeHabitatState", "classifyMood", "habitatLevel", "computeQuality", "LEVEL_THRESHOLDS"]
    - path: "packages/domain/src/study/index.ts"
      provides: "`@leocards/domain/study` subpath entry"
    - path: "packages/domain/src/habitat/index.ts"
      provides: "`@leocards/domain/habitat` subpath entry"
  key_links:
    - from: "src/app/api/study/complete/route.ts"
      to: "@leocards/domain/study"
      via: "computeCardUpdate import"
      pattern: "from \"@leocards/domain/study\""
    - from: "src/app/api/habitat/route.ts"
      to: "@leocards/domain/habitat"
      via: "computeHabitatState import"
      pattern: "from \"@leocards/domain/habitat\""
    - from: "packages/domain/src/study/study-engine.ts"
      to: "packages/domain/src/ids.ts"
      via: "relative type import"
      pattern: "from \"\\.\\./ids\""
---

<objective>
Relocate the two pure-domain engines — SRS rules (`study-engine.ts`) and habitat-state maths
(`habitat-engine.ts`) — plus their unit tests into `packages/domain`, and repoint every import
site in the web app at the new `@leocards/domain/study` and `@leocards/domain/habitat` subpaths.

Purpose: this IS NAT-01's payload. D-07 requires study behaviour and habitat maths to be
identical on web and native "by construction" — that guarantee only exists once both apps read
the same file. Relocating the tests alongside the source keeps the behavioural contract proven
from inside the package.

Output: two populated package subpaths, two deleted `src/lib` modules, ~28 rewritten import
statements, and an unchanged web app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1-native-foundation-workspace-api/28.1-01-SUMMARY.md
@.planning/phases/28.1-native-foundation-workspace-api/28.1-PATTERNS.md
@.planning/phases/28.1-native-foundation-workspace-api/28.1-RESEARCH.md
@AGENTS.md

<interfaces>
<!-- Exact current import lines that must change. Verified by direct read this session. -->

The ONLY code change either engine needs is its first line, which currently reaches into the
web app through the `@/` alias (RESEARCH Pitfall 5) — that alias does not resolve from inside
an independent workspace package:

- `src/lib/study-engine.ts` line 1: `import type { CardId, RecallDirection } from "@/db/schema";`
- `src/lib/study-engine.test.ts` line 2: `import type { CardId } from "@/db/schema";`
- `src/lib/habitat-engine.ts` line 1: `import type { UserId } from "@/db/schema";`
- `src/lib/habitat-engine.test.ts` line 2: `import type { UserId } from "@/db/schema";`

All four become relative imports of the co-located ids module — e.g. `from "../ids"` from
`packages/domain/src/study/`. Everything else in both files and both tests is copy-verbatim.

Every exported function in both engines takes an explicit `now: Date` parameter and makes no
internal `Date.now()` / `new Date()` call. That determinism is exactly what makes the relocation
safe and what the relocated tests must keep asserting — do not "simplify" any signature.

**The three test files that `vi.mock` the engines (why DR-02's subpaths exist):**
- `src/app/api/study/__tests__/cooldown-config.test.ts` line 30 `vi.mock("@/lib/habitat-engine", () => ({...}))` AND line 38-39 `vi.mock("@/lib/study-engine", async (importOriginal) => { const original = await importOriginal<typeof import("@/lib/study-engine")>(); ... })`
- `src/app/api/study/complete/route.test.ts` line 65 `vi.mock("@/lib/habitat-engine", () => ({...}))`
- `src/app/api/debug/__tests__/state.test.ts` line 130 `vi.mock("@/lib/habitat-engine", () => ({...}))`

Each mock path is a pure string swap to the matching subpath. The `importOriginal<typeof import("...")>()`
generic in `cooldown-config.test.ts` must be swapped too, or its partial mock silently loses its
real implementations.

**Complete import-site inventory (verified by grep this session):**

`@/lib/study-engine` — 7 files:
`src/app/(protected)/dashboard/page.tsx`, `src/app/(protected)/study/page.tsx`,
`src/app/api/cards/[id]/unpause/route.ts`, `src/app/api/study/__tests__/cooldown-config.test.ts`,
`src/app/api/study/complete/route.ts`, `src/components/study-card.tsx`,
`src/components/study-session.tsx`

`@/lib/habitat-engine` — 24 files:
`src/app/(protected)/dashboard/page.tsx`, `src/app/(protected)/debug/page.tsx`,
`src/app/(protected)/habitat/page.tsx`, `src/app/api/debug/__tests__/state.test.ts`,
`src/app/api/debug/state/route.ts`, `src/app/api/habitat/route.ts`,
`src/app/api/study/__tests__/cooldown-config.test.ts`, `src/app/api/study/complete/route.test.ts`,
`src/app/api/study/complete/route.ts`, `src/components/daybreak/h-mood-chip.tsx`,
`src/components/daybreak/h-top.tsx`, `src/components/deck-view.test.tsx`,
`src/components/deck-view.tsx`, `src/components/habitat-3d-canvas.tsx`,
`src/components/habitat-hero.tsx`, `src/components/habitat-medallion.test.tsx`,
`src/components/habitat-medallion.tsx`, `src/components/habitat-scene.tsx`,
`src/components/habitat-video.tsx`, `src/lib/debug-cheat.ts`,
`src/lib/habitat-3d/mood-decay.ts`, `src/lib/habitat-queries.ts`, `src/lib/habitat-tint.ts`,
`src/lib/habitat-ui-utils.ts`

Note `src/app/(protected)/dashboard/page.tsx` imports `earliestCooldownEnd` under the local
alias `getEarliestCooldownEnd` (`import { assembleSession, earliestCooldownEnd as getEarliestCooldownEnd } from "@/lib/study-engine";`) — preserve the alias exactly when rewriting the module specifier.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Resolve the working-tree collision on habitat-video.tsx</name>
  <files>(decision only — no file is written; this task decides how Task 3 may commit src/components/habitat-video.tsx)</files>
  <read_first>
    - src/components/habitat-video.tsx (line 37 — the `@/lib/habitat-engine` type import this plan must rewrite)
    - .planning/STATE.md (the Phase 25-04 "Non-blocking flag" entry — the concurrent-session precedent that established pathspec-scoped commits on this repo)
  </read_first>
  <action>
Run `git status --porcelain` and `git diff --stat -- src/components/habitat-video.tsx AGENTS.md e2e/13-habitat-states.spec.ts`, and present the ACTUAL current diff stat to Josh alongside the two options below. Do not describe the working tree from this plan's text — the separate investigation may have advanced or been committed since planning. Then stop and wait for his selection, and record it verbatim in the summary before Task 2 begins.
  </action>
  <verify>
    <automated>git status --porcelain -- src/components/habitat-video.tsx AGENTS.md e2e/13-habitat-states.spec.ts</automated>
  </verify>
  <done>Josh has selected clear-tree or authorise-sweep, and the selection is recorded verbatim in the plan summary.</done>
  <decision>How should this plan handle `src/components/habitat-video.tsx`, which carries unrelated uncommitted changes from a separate investigation and must also have its import rewritten by this plan?</decision>
  <context>
`git status` shows four unrelated modified/untracked paths from a different piece of work:
`AGENTS.md`, `e2e/13-habitat-states.spec.ts`, `src/components/habitat-video.tsx`, `uat-shots/`.

`src/components/habitat-video.tsx` line 37 is `import type { HabitatState } from "@/lib/habitat-engine";`.
This plan deletes `src/lib/habitat-engine.ts`, so that import MUST be rewritten — there is no
version of this plan that leaves the file alone and still leaves `main` compiling.

That creates a conflict with the standing rule for this repo ("use pathspec-scoped commits and
NEVER sweep the unrelated files"): committing the import fix would also commit the unrelated
habitat-video investigation diff. Committing everything EXCEPT habitat-video.tsx would leave
HEAD broken — a file importing a deleted module — which violates "the web app is never broken at
any commit boundary" (every push to main auto-deploys prod).

Claude cannot resolve this alone: it must not commit someone else's half-finished work, and it
must not leave HEAD broken.
  </context>
  <options>
    <option id="clear-tree">
      <name>Josh commits or stashes the in-flight habitat-video work first</name>
      <pros>Cleanest history; this plan's commit contains only the import rewrite; the unrelated investigation stays intact and separately attributable</pros>
      <cons>Requires Josh to interrupt and decide what to do with the in-flight edits</cons>
    </option>
    <option id="authorise-sweep">
      <name>Josh authorises including the unrelated habitat-video.tsx diff in this plan's commit</name>
      <pros>Zero interruption to Josh's other work; unblocks immediately</pros>
      <cons>One commit mixes this phase's mechanical rewrite with an unrelated investigation diff; muddies the audit trail that Phase 25-04 established pathspec discipline to protect</cons>
    </option>
  </options>
  <resume-signal>Select: clear-tree (and confirm when the working tree is clean), or authorise-sweep</resume-signal>
  <acceptance_criteria>
    - Josh's selection is recorded verbatim in the plan summary
    - If `clear-tree`: `git status --porcelain -- src/components/habitat-video.tsx` returns empty before Task 3 begins
    - If `authorise-sweep`: the summary names `src/components/habitat-video.tsx` as a deliberately-mixed commit with Josh's authorisation quoted
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Relocate the SRS engine to @leocards/domain/study</name>
  <files>packages/domain/src/study/study-engine.ts, packages/domain/src/study/study-engine.test.ts, packages/domain/src/study/index.ts, src/lib/study-engine.ts, src/lib/study-engine.test.ts, src/app/(protected)/dashboard/page.tsx, src/app/(protected)/study/page.tsx, src/app/api/cards/[id]/unpause/route.ts, src/app/api/study/complete/route.ts, src/app/api/study/__tests__/cooldown-config.test.ts, src/components/study-card.tsx, src/components/study-session.tsx</files>
  <read_first>
    - src/lib/study-engine.ts (whole file, 316 lines — you are moving it verbatim; you must see every export)
    - src/lib/study-engine.test.ts (whole file)
    - packages/domain/src/ids.ts
    - packages/domain/package.json (the `exports` map created in 28.1-01 — `./study` already points at `./src/study/index.ts`)
    - src/app/api/study/__tests__/cooldown-config.test.ts (the double-`vi.mock` file — lines 1-60)
    - src/app/(protected)/dashboard/page.tsx (lines 1-30 — note the `earliestCooldownEnd as getEarliestCooldownEnd` alias)
  </read_first>
  <action>
1. Move `src/lib/study-engine.ts` to `packages/domain/src/study/study-engine.ts` with `git mv`
   (preserves history). Change ONLY line 1: `import type { CardId, RecallDirection } from "@/db/schema";`
   becomes `import type { CardId, RecallDirection } from "../ids";`. Every other line —
   `CardForSession`, `SessionCard`, `GradeEntry`, `SessionStats`, `shuffleTake`,
   `assembleSession`, `interleave`, `getCardStage`, `DEFAULT_COOLDOWN_MS`, `computeCardUpdate`,
   `earliestCooldownEnd`, `computeUnpauseUpdate` — stays byte-identical. Do not reorder, do not
   reformat, do not "improve" any signature.

2. Move `src/lib/study-engine.test.ts` to `packages/domain/src/study/study-engine.test.ts` with
   `git mv`. Change ONLY its `@/db/schema` type import (line 2) to `from "../ids"`, and its
   import of the module under test to `./study-engine`. Assertions stay byte-identical — this
   file is the behavioural contract that proves the move changed nothing.

3. Create `packages/domain/src/study/index.ts` re-exporting everything from `./study-engine`.
   This is the file `packages/domain/package.json`'s `./study` export already points at.

4. Rewrite the module specifier in all 7 `@/lib/study-engine` import sites to
   `@leocards/domain/study`. In `src/app/(protected)/dashboard/page.tsx` preserve the
   `earliestCooldownEnd as getEarliestCooldownEnd` alias exactly. In
   `src/app/api/study/__tests__/cooldown-config.test.ts` rewrite BOTH the top-level
   `import { DEFAULT_COOLDOWN_MS } from "@/lib/study-engine"` AND the
   `vi.mock("@/lib/study-engine", async (importOriginal) => ...)` call including its
   `importOriginal<typeof import("@/lib/study-engine")>()` type argument — missing the type
   argument silently strips the partial mock's real implementations.

5. Confirm `src/lib/study-engine.ts` and `src/lib/study-engine.test.ts` no longer exist and no
   file anywhere in the repo (including `e2e/` and `scripts/`) still references
   `@/lib/study-engine`.

6. Run a scoped Biome pass on the touched files only (`npx biome check --write <files>`) —
   never repo-wide (AGENTS.md) — then a full `npx tsc --noEmit`, because Biome's import
   reordering has previously produced shapes tsc rejects under `noUncheckedIndexedAccess`.

Commit with an explicit pathspec.
  </action>
  <verify>
    <automated>npx vitest run packages/domain/src/study/study-engine.test.ts src/app/api/study/__tests__/cooldown-config.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npx tsc -p packages/domain --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/lib/study-engine.ts` and `test ! -f src/lib/study-engine.test.ts` both succeed
    - `grep -rl "@/lib/study-engine" src/ e2e/ scripts/ packages/` returns nothing (exit 1)
    - `grep -c 'from "\.\./ids"' packages/domain/src/study/study-engine.ts` returns 1
    - `grep -c '@/db/schema' packages/domain/src/study/study-engine.ts packages/domain/src/study/study-engine.test.ts` returns 0 for both files
    - `git diff HEAD~1 -- packages/domain/src/study/study-engine.ts` shows exactly one changed line (the import) when compared against the pre-move file
    - `npx vitest run packages/domain/src/study/study-engine.test.ts` exits 0 with the same test count as the pre-move `src/lib/study-engine.test.ts`
    - `npx vitest run src/app/api/study/__tests__/cooldown-config.test.ts` exits 0 (proves the double-`vi.mock` swap worked)
    - `grep -c "@leocards/domain/study" src/app/\(protected\)/dashboard/page.tsx` returns 1 and that file still contains `earliestCooldownEnd as getEarliestCooldownEnd`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The SRS engine and its tests live in `packages/domain/src/study/`, all 7 web import sites read `@leocards/domain/study`, and the engine's behavioural contract is proven unchanged by its own relocated test suite.</done>
</task>

<task type="auto">
  <name>Task 3: Relocate the habitat engine and re-verify the whole web app</name>
  <files>packages/domain/src/habitat/habitat-engine.ts, packages/domain/src/habitat/habitat-engine.test.ts, packages/domain/src/habitat/index.ts, src/lib/habitat-engine.ts, src/lib/habitat-engine.test.ts, src/app/(protected)/dashboard/page.tsx, src/app/(protected)/debug/page.tsx, src/app/(protected)/habitat/page.tsx, src/app/api/debug/state/route.ts, src/app/api/debug/__tests__/state.test.ts, src/app/api/habitat/route.ts, src/app/api/study/complete/route.ts, src/app/api/study/complete/route.test.ts, src/app/api/study/__tests__/cooldown-config.test.ts, src/components/daybreak/h-mood-chip.tsx, src/components/daybreak/h-top.tsx, src/components/deck-view.tsx, src/components/deck-view.test.tsx, src/components/habitat-3d-canvas.tsx, src/components/habitat-hero.tsx, src/components/habitat-medallion.tsx, src/components/habitat-medallion.test.tsx, src/components/habitat-scene.tsx, src/components/habitat-video.tsx, src/lib/debug-cheat.ts, src/lib/habitat-3d/mood-decay.ts, src/lib/habitat-queries.ts, src/lib/habitat-tint.ts, src/lib/habitat-ui-utils.ts</files>
  <read_first>
    - src/lib/habitat-engine.ts (whole file, 275 lines — moving verbatim)
    - src/lib/habitat-engine.test.ts (whole file)
    - src/app/api/study/complete/route.test.ts (lines 55-80 — the `vi.mock("@/lib/habitat-engine")` block)
    - src/app/api/debug/__tests__/state.test.ts (lines 120-145 — the `vi.mock("@/lib/habitat-engine")` block)
    - packages/domain/src/study/index.ts (created in Task 2 — mirror its shape)
    - .planning/phases/17-performance-optimization/measurements/ (the shared-chunk floor reference: 514.72 KB / 9 chunks post-Wave-2, recorded in STATE.md Phase 17-02)
  </read_first>
  <action>
1. Move `src/lib/habitat-engine.ts` to `packages/domain/src/habitat/habitat-engine.ts` with
   `git mv`. Change ONLY line 1: `import type { UserId } from "@/db/schema";` becomes
   `import type { UserId } from "../ids";`. Every other line — `GRACE_PERIOD_MS`,
   `DECAY_RATE_PER_DAY`, `DECAY_FLOOR`, `LEVEL_THRESHOLDS`, `EXCITED_WINDOW_MINUTES`,
   `TigerMood`, `HabitatOverride`, `HabitatFacts`, `HabitatState`, `computeQuality`,
   `habitatLevel`, `classifyMood`, `computeHabitatState` — stays byte-identical.

2. Move `src/lib/habitat-engine.test.ts` to `packages/domain/src/habitat/habitat-engine.test.ts`
   with `git mv`; change its `@/db/schema` type import to `../ids` and its module-under-test
   import to `./habitat-engine`. Assertions unchanged.

3. Create `packages/domain/src/habitat/index.ts` re-exporting everything from `./habitat-engine`,
   mirroring `packages/domain/src/study/index.ts`.

4. Rewrite the module specifier in all 24 `@/lib/habitat-engine` import sites to
   `@leocards/domain/habitat`. This includes the three `vi.mock("@/lib/habitat-engine", ...)`
   calls in `src/app/api/study/complete/route.test.ts`, `src/app/api/debug/__tests__/state.test.ts`
   and `src/app/api/study/__tests__/cooldown-config.test.ts` — the mock path must match the
   specifier the module under test actually imports, or the mock silently no-ops and the test
   starts exercising real habitat maths.

5. Confirm `src/lib/habitat-engine.ts` and `src/lib/habitat-engine.test.ts` no longer exist and
   nothing in `src/`, `e2e/`, `scripts/` or `packages/` still references `@/lib/habitat-engine`.

6. Scoped Biome pass on touched files only, then full `npx tsc --noEmit`.

7. Full regression sweep:
   - `npx vitest run` — the four documented parallel-execution flakes (`deck-switcher`,
     `image-upload-flow-extract-errors`, `review-list-commit-guard`, `cooldown-config`) are known;
     re-run any that fail in isolation. Any OTHER failure is a real regression and blocks.
   - `npm run build` — must succeed.
   - Bundle-neutrality check: this plan moved code that 24 files import, several of them
     `"use client"` components. Capture the shared-chunk first-load JS figure reported by
     `npm run build` and compare it against the 514.72 KB / 9-chunk shared floor recorded in
     STATE.md (Phase 17-02). A material increase means the DR-02 firewall leaked (something
     pulled `./schemas` or a runtime dependency into the client graph) — investigate before
     committing rather than accepting it. Record the measured figure in the summary either way.
   - `npm ci` — proves the lockfile is still exactly what Vercel will install.

Commit with an explicit pathspec, honouring Task 1's recorded decision about
`src/components/habitat-video.tsx`.
  </action>
  <verify>
    <automated>npx vitest run packages/domain/src/habitat/habitat-engine.test.ts src/app/api/study/complete/route.test.ts src/app/api/debug/__tests__/state.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npx tsc -p packages/domain --noEmit &amp;&amp; npx vitest run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/lib/habitat-engine.ts` and `test ! -f src/lib/habitat-engine.test.ts` both succeed
    - `grep -rl "@/lib/habitat-engine" src/ e2e/ scripts/ packages/` returns nothing (exit 1)
    - `grep -c 'from "\.\./ids"' packages/domain/src/habitat/habitat-engine.ts` returns 1
    - `grep -rc 'vi.mock("@leocards/domain/habitat"' src/app/api/study/complete/route.test.ts src/app/api/debug/__tests__/state.test.ts src/app/api/study/__tests__/cooldown-config.test.ts` returns 1 for each of the three files
    - `grep -rl "@leocards/domain/habitat" src/ | wc -l` returns 24
    - `grep -c 'zod' packages/domain/src/habitat/index.ts packages/domain/src/study/index.ts` returns 0 for both (DR-02 firewall intact)
    - `npx vitest run packages/domain/src/habitat/habitat-engine.test.ts` exits 0 with the same test count as the pre-move `src/lib/habitat-engine.test.ts`
    - `npx vitest run` full suite: no failures beyond the four documented flakes, each re-verified in isolation
    - `npm run build` exits 0 and the reported shared first-load JS is not materially above the 514.72 KB / 9-chunk Phase 17-02 floor — the measured figure is recorded in the summary
    - `npm ci` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both engines live in `packages/domain`, no `src/lib` engine module remains, all 31 import sites (including the three `vi.mock` paths) point at the new subpaths, the full test suite and production build are green, and the client bundle has not regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared package → web app runtime | SRS grading and habitat level/decay maths now cross a package boundary; a silent behaviour change here corrupts real user learning state |
| shared package → web client bundle | 24 importers, several `"use client"` — the package's dependency graph is now part of the browser payload |
| repo working tree → git history | An unrelated concurrent investigation has uncommitted edits in a file this plan must touch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28.1-06 | Tampering | SRS grading + habitat decay maths | mitigate | Both engines move byte-verbatim except one import line each, asserted by a `git diff` single-line check; their existing unit tests move with them and must report the SAME test count and pass; `qa:run` re-verifies the real SRS pipeline at plan 28.1-03 and again at the 28.1-09 phase gate. |
| T-28.1-07 | Spoofing | `vi.mock` interception in three route tests | mitigate | DR-02's per-engine subpaths keep each mock path distinct; acceptance criteria assert `vi.mock("@leocards/domain/habitat"` appears in each of the three files and that `cooldown-config.test.ts`'s `importOriginal<typeof import(...)>()` type argument was swapped too. A mismatched mock path fails open (test exercises real code) rather than loudly, so the assertion is the control. |
| T-28.1-08 | Denial of Service | Web client bundle size | mitigate | Explicit shared-chunk first-load JS comparison against the Phase 17-02 514.72 KB / 9-chunk floor, plus `grep` assertions that neither subpath barrel references zod. |
| T-28.1-09 | Tampering | Unrelated in-flight work in `src/components/habitat-video.tsx` | mitigate | Task 1 is a blocking decision checkpoint; no commit touching that file happens until Josh selects `clear-tree` or `authorise-sweep`, and the selection is recorded in the summary. |
| T-28.1-SC | Tampering | npm installs | mitigate | No packages installed in this plan; `npm ci` re-run only to prove the lockfile is unchanged and CI-safe. |
</threat_model>

<verification>
1. `npx vitest run packages/domain/src/study/study-engine.test.ts packages/domain/src/habitat/habitat-engine.test.ts` — relocated contracts hold
2. `npx vitest run src/app/api/study/complete/route.test.ts src/app/api/debug/__tests__/state.test.ts src/app/api/study/__tests__/cooldown-config.test.ts` — all three `vi.mock` swaps work
3. `grep -rl "@/lib/study-engine\|@/lib/habitat-engine" src/ e2e/ scripts/ packages/` returns nothing
4. `npx tsc --noEmit` and `npx tsc -p packages/domain --noEmit` both exit 0
5. `npx vitest run` full suite green (four documented flakes re-verified in isolation)
6. `npm run build` green; shared first-load JS compared against the Phase 17-02 floor and recorded
7. `npm ci` green
8. `git show --stat` on each commit matches the intended pathspec
</verification>

<success_criteria>
- `packages/domain/src/study/` and `packages/domain/src/habitat/` hold the engines and their tests
- `src/lib/study-engine.ts`, `src/lib/study-engine.test.ts`, `src/lib/habitat-engine.ts`, `src/lib/habitat-engine.test.ts` no longer exist
- 31 import sites (7 study + 24 habitat) read the new subpaths; the three `vi.mock` paths are swapped
- Relocated tests report identical test counts and all pass
- Full suite, production build, `tsc` (both projects) and `npm ci` all green
- Shared client-bundle floor not materially regressed vs Phase 17-02's 514.72 KB / 9 chunks
- Task 1's checkpoint decision is recorded verbatim in the summary
</success_criteria>

<output>
Create `.planning/phases/28.1-native-foundation-workspace-api/28.1-02-SUMMARY.md` when done.
Record: Josh's Task 1 decision, the measured shared first-load JS figure, the pre/post test
counts for both relocated test files, and any Biome-induced import reordering that required a
follow-up `tsc` fix.
</output>
