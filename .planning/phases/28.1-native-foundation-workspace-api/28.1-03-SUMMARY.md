---
phase: 28.1-native-foundation-workspace-api
plan: 03
subsystem: api
tags: [zod, typescript, npm-workspaces, api-contracts, validation, monorepo]

# Dependency graph
requires:
  - phase: 28.1-02
    provides: "npm workspaces root, @leocards/domain package with DR-02's six-key subpath export map declared (./schemas and ./contracts targets existed but were unpopulated), study/habitat engines already relocated"
provides:
  - "@leocards/domain/schemas: GradeSchema + CommitSchema (relocated verbatim, full zod, from /api/study/complete), plus new CreateCardSchema (T-28.1-12 over-posting guard) and CreateDeckSchema (T-28.1-11 SEC-06 allow-list) for plans 28.1-05..07 — every boundary pinned by schemas.test.ts"
  - "@leocards/domain/contracts: the DR-03 error convention (CoreErrorCode/CoreResult<T>/CORE_ERROR_STATUS) plus all five native-v1 endpoint response shapes (dashboard/study/cards/decks/account) — zero runtime dependencies, verified by grep"
  - "/api/study/complete now imports CommitSchema from the shared package instead of declaring it inline — first proof the ./schemas subpath is genuinely shared, not just declared"
  - "Automated DR-02/PERF-14 bundle-firewall regression test (src/lib/__tests__/domain-bundle-firewall.test.ts) with a verified negative control, replacing 28.1-01's now-retired zero-dependencies check"
  - "packages/domain gained its first dependency (zod ^4.3.6), deduped against the root's existing install at the identical range — zero new packages entered the tree"
affects: [28.1-05, 28.1-06, 28.1-07, 28.1-09, 28.2, 28.3]

# Tech tracking
tech-stack:
  added: ["zod ^4.3.6 as packages/domain's first dependency (already a root dependency at this exact range — npm dedupes to one install, confirmed via package-lock.json diff)"]
  patterns:
    - "DR-03 CoreResult<T>/CoreErrorCode/CORE_ERROR_STATUS discriminated-union error convention for extracted core functions — a route handler maps code->status via the constant map, a server action throws a named Error; no code ever signals resource existence distinctly from ownership, so T-12-08's 403-never-404 rule can't be bypassed by a new endpoint"
    - "contracts/ subpath is pure TypeScript types + one constant map, zero runtime dependencies, importable by any client (web or, from 28.3, native) at zero bundle cost"
    - "schemas/ subpath deliberately carries full zod (DR-08) for verbatim-relocated, already-in-production validators — protected from client-bundle leakage not by a zero-deps rule (retired this plan) but by an automated bundle-firewall test with a proven negative control"

key-files:
  created:
    - packages/domain/src/schemas/study.ts
    - packages/domain/src/schemas/cards.ts
    - packages/domain/src/schemas/decks.ts
    - packages/domain/src/schemas/index.ts
    - packages/domain/src/schemas/schemas.test.ts
    - packages/domain/src/contracts/errors.ts
    - packages/domain/src/contracts/dashboard.ts
    - packages/domain/src/contracts/study.ts
    - packages/domain/src/contracts/cards.ts
    - packages/domain/src/contracts/decks.ts
    - packages/domain/src/contracts/account.ts
    - packages/domain/src/contracts/index.ts
    - src/lib/__tests__/domain-bundle-firewall.test.ts
  modified:
    - packages/domain/package.json
    - package-lock.json
    - src/app/api/study/complete/route.ts

key-decisions:
  - "route.ts imports only CommitSchema (not GradeSchema) from @leocards/domain/schemas — GradeSchema is only referenced internally by CommitSchema's grades array field, and route.ts never names it directly, so importing it too would be an unused import"
  - "DashboardResponse/StudySessionResponse field types use plain `string` for ids, never the branded CardId/DeckId/UserId types — wire contracts are JSON, which carries no brand; native and web both cast to the branded type only at the point of DB use, exactly the existing branded-ID-casting convention"
  - "qa:run NOT executed — DATABASE_URL and DEBUG_CHEAT_SECRET are both unavailable in this execution environment; per the plan's own explicit instruction this is recorded as a blocker, not faked or silently skipped, and carried to the 28.1-09 phase gate which runs the same harness"
  - "NAT-01/NAT-02 traceability NOT marked complete despite this plan's own frontmatter listing both — both requirements span multiple contributing plans (NAT-01: 01/02/03/08/09; NAT-02: 03-07/09) and 28.1-01's STATE.md flag is explicit that only the LAST contributing plan (28.1-09, for both) should flip the checkbox"

patterns-established:
  - "Pattern: when a plan's <action> text instructs documenting that some field/pattern is deliberately absent, check whether <acceptance_criteria> then greps for that same literal substring's absence — write the in-file comment without the literal trigger substring from the first draft, not as a reactive fix (see Deviations)"

requirements-completed: []  # NAT-01 and NAT-02 both span multiple plans; only the LAST contributing plan (28.1-09 for both) should mark them complete — see key-decisions and STATE.md's 28.1-01 flag.

# Metrics
duration: ~30min
completed: 2026-08-03
---

# Phase 28.1 Plan 03: Shared zod schemas + native-v1 API contracts Summary

**Full-zod GradeSchema/CommitSchema relocated verbatim into `@leocards/domain/schemas` (plus new CreateCardSchema/CreateDeckSchema) alongside a runtime-free `@leocards/domain/contracts` (DR-03 `CoreResult` error convention + all five native-v1 endpoint response shapes), with `/api/study/complete` now importing its schema from the package and an automated DR-02/PERF-14 bundle-firewall test with a proven negative control.**

## Performance

- **Duration:** ~30 min (estimated — session start not independently timestamped; task commits span 19:01:55 to 19:12:18 BST, with file-reading/context-gathering and the full regression sweep, including a ~2min `npm ci` and a full `vitest run` + `npm run build`, on either side)
- **Completed:** 2026-08-03T19:12:18+01:00 (final task commit)
- **Tasks:** 3 (all `type="auto"`, no checkpoints)
- **Files modified:** 16 (13 created, 3 modified)

## Accomplishments

- `packages/domain/package.json` gained its first dependency (`zod ^4.3.6`, identical to the root's existing range) — `npm install` deduped to a single copy (confirmed via a 4-line `package-lock.json` diff), and `npm ci` re-verified the lockfile is CI/Vercel-installable
- `GradeSchema`/`CommitSchema` relocated byte-verbatim from `/api/study/complete/route.ts` into `packages/domain/src/schemas/study.ts`, including the `commitId` bound's `recall_events` primary-key comment; the route now imports `CommitSchema` from `@leocards/domain/schemas` and declares no schema inline
- New `CreateCardSchema` (T-28.1-12 over-posting guard, no `.passthrough()`/`.catchall()`, matching `saveCard`'s and `saveImageCards`'s existing bounds) and `CreateDeckSchema` (T-28.1-11, mirrors `deck-actions.ts`'s `ALLOWED_LANGUAGES` exactly) for the endpoints plans 28.1-05..07 will build
- `schemas.test.ts` pins every boundary named in the plan (empty/501/500-grade arrays, 101/100-char `commitId`, invalid `direction`, 501-char/empty `front`, unknown `source`, `"de"` rejection) — 14 tests, well past the 9-assertion minimum
- `packages/domain/src/contracts/` created from scratch: `errors.ts` (`CoreErrorCode`/`CoreResult<T>`/`CORE_ERROR_STATUS` — the DR-03 convention, locked before any endpoint exists), `dashboard.ts` (`DashboardResponse`, every derived flag — `needsOnboarding`/`hasDueCards`/`dueCount`/`earliestCooldownEnd`/`allPaused`/`sleeping` — as explicit fields, `habitat: HabitatState` reused not re-declared), `study.ts` (`StudySessionResponse`/`StudySessionCardWire`), `cards.ts`/`decks.ts`/`account.ts` (the remaining three response shapes), and an `index.ts` barrel — confirmed zero `zod` references anywhere in the directory
- `src/lib/__tests__/domain-bundle-firewall.test.ts` added: asserts no `"use client"` file under `src/` imports `@leocards/domain/schemas`, and that the root/study/habitat barrels plus both engine modules never reference `zod`. Negative control performed and reverted: a temporary probe import into `change-password-card.tsx` made the first assertion fail with a diagnostic naming the exact offending file; reverted to a byte-identical, zero-diff state before committing
- Full regression sweep green: `npx tsc --noEmit` (root, twice re-run) and `npx tsc -p packages/domain --noEmit` both clean; full `npx vitest run` — 729/730 non-flake tests pass, the one failure (`cooldown-config.test.ts`) is one of the four project-documented pre-existing parallel-execution flakes, re-verified passing 4/4 in isolation; `npm run build` succeeds (Turbopack production build, all 24 routes)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create @leocards/domain/schemas and rewire /api/study/complete** - `9ec6192` (feat)
2. **Task 2: Create @leocards/domain/contracts — the native-v1 API type surface** - `4811d2a` (feat)
3. **Task 3: Add the zod bundle-firewall guard and run the SRS regression harness** - `d6e0b56` (test)

**Plan metadata:** committed alongside this SUMMARY (see final docs commit)

## Files Created/Modified

- `packages/domain/package.json` - Gained `"dependencies": { "zod": "^4.3.6" }` — the package's first-ever dependency
- `package-lock.json` - Regenerated via `npm install`; workspace-linkage delta only (dedupes to the existing root zod install)
- `packages/domain/src/schemas/study.ts` - `GradeSchema` + `CommitSchema`, relocated verbatim
- `packages/domain/src/schemas/cards.ts` - `CreateCardSchema` (T-28.1-12)
- `packages/domain/src/schemas/decks.ts` - `CreateDeckSchema` (T-28.1-11)
- `packages/domain/src/schemas/index.ts` - Barrel re-exporting all three schema modules; documents `/api/translate`'s `RequestSchema` is deliberately NOT relocated
- `packages/domain/src/schemas/schemas.test.ts` - Pins every schema's accept/reject boundary (14 tests)
- `packages/domain/src/contracts/errors.ts` - `CoreErrorCode`/`CoreResult<T>`/`CORE_ERROR_STATUS` (DR-03)
- `packages/domain/src/contracts/dashboard.ts` - `DashboardResponse`, reuses `HabitatState` from `../habitat`
- `packages/domain/src/contracts/study.ts` - `StudySessionResponse`/`StudySessionCardWire`
- `packages/domain/src/contracts/cards.ts` - `CreateCardResponse`
- `packages/domain/src/contracts/decks.ts` - `CreateDeckResponse`
- `packages/domain/src/contracts/account.ts` - `AccountResponse` (raw values only, no presentation strings)
- `packages/domain/src/contracts/index.ts` - Barrel re-exporting all six contract modules
- `src/app/api/study/complete/route.ts` - Deleted the two inline schema declarations and the now-unused `import { z } from "zod"`; imports `CommitSchema` from `@leocards/domain/schemas` instead
- `src/lib/__tests__/domain-bundle-firewall.test.ts` - Automated DR-02/PERF-14 firewall guard with a verified negative control

## Decisions Made

- `route.ts` imports only `CommitSchema` (not `GradeSchema`) from the shared package — `GradeSchema` is only ever referenced internally through `CommitSchema`'s `grades` array field; the route handler never names it directly, so a separate import would be unused.
- Wire contract `id` fields use plain `string`, never the branded `CardId`/`DeckId`/`UserId` types — JSON carries no brand, and this mirrors the existing branded-ID-casting convention (cast only at the point of DB use, never earlier).
- `qa:run` was not executed this session — `DATABASE_URL` and `DEBUG_CHEAT_SECRET` are both confirmed absent from this execution environment (verified via direct env inspection before attempting anything). Per the plan's own explicit instruction, this is recorded here as a blocker rather than faked or silently skipped, and is carried forward to the 28.1-09 phase gate, which runs the identical harness.
- `NAT-01`/`NAT-02` traceability intentionally NOT marked complete in REQUIREMENTS.md this plan, despite this plan's own frontmatter listing both — both requirements span multiple contributing plans within Phase 28.1 (NAT-01: 01/02/03/08/09; NAT-02: 03/04/05/06/07/09), and 28.1-01's STATE.md flag is explicit that only the LAST contributing plan for each (28.1-09, for both) should flip the checkbox.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug, self-caught pre-commit] Explanatory comments tripped this plan's own acceptance-criteria greps — recurrence of a pattern already seen twice in this phase**
- **Found during:** Task 1 (`packages/domain/src/schemas/cards.ts`) and Task 2 (`packages/domain/src/contracts/index.ts`), both caught by running the plan's own acceptance-criteria grep commands before committing — the same pre-commit self-check technique 28.1-01 and 28.1-02 both used to catch their own instances of this exact issue class.
- **Issue:** The plan's `<action>` text instructs documenting certain absent concepts in-file (e.g. "no `.passthrough()`/`.catchall()`", "no `zod`", "no `not-found` code", "`memberSince` and `nativeLanguageLabel` are deliberately absent"), while the corresponding `<acceptance_criteria>` then greps the whole file — comments included — for the literal absence of those same substrings. Writing the documentation exactly as the action text phrases it necessarily trips the criterion checking for its absence. Two files failed only after being drafted and grep-checked (`cards.ts`'s comment named `.passthrough()`/`.catchall()` literally; `contracts/index.ts`'s comment named `zod` literally); two more (`packages/domain/src/contracts/errors.ts`'s "not-found"/"404" concept, `packages/domain/src/contracts/account.ts`'s `memberSince`/`nativeLanguageLabel` concept) were drafted correctly from the start, having anticipated the same trap from 28.1-01/02's documented precedent.
- **Fix:** Reworded each comment to preserve the documented meaning without using the literal trigger substring: `cards.ts` now describes "unknown keys ... dropped, never accepted" instead of naming the two zod methods; `contracts/index.ts` now says "no schema-validation library" instead of writing "zod"; `errors.ts` describes "no missing-resource code" / "never a distinct existence-signalling code" instead of writing "not-found" or "404"; `account.ts` describes "two presentation strings ... deliberately not part of this contract" instead of naming the two fields literally.
- **Files modified:** `packages/domain/src/schemas/cards.ts`, `packages/domain/src/contracts/index.ts` (reactive fixes, both folded into their task commits pre-commit — no separate fix-up commit needed); `packages/domain/src/contracts/errors.ts`, `packages/domain/src/contracts/account.ts` (drafted correctly from the start, listed here for completeness since they hit the identical trap class).
- **Verification:** Re-ran every relevant acceptance-criteria grep after each edit — all return the expected zero/absent count (`passthrough\|catchall`: 0, `zod` across `contracts/`: 0, `not-found`: 0, `404`: 0, `memberSince`: 0, `nativeLanguageLabel`: 0).
- **Committed in:** `9ec6192` (Task 1, `cards.ts`), `4811d2a` (Task 2, `contracts/index.ts`, `errors.ts`, `account.ts`)

---

**Total deviations:** 1 auto-fixed issue class, 2 reactive instances + 2 proactively-avoided instances of the same root cause.
**Impact on plan:** Purely a documentation-phrasing correction to satisfy the plan's own acceptance criteria; zero effect on runtime behaviour, no scope creep, no architectural change. Flagging strongly for plans 28.1-04 through 28.1-09: this is now the **third** occurrence of this exact class across Phase 28.1 (28.1-01 hit it twice, 28.1-02 once, 28.1-03 twice reactively plus twice avoided proactively) — any future task whose `<action>` text says "document that X is absent" should immediately check whether `<acceptance_criteria>` greps for X's absence, and phrase the in-file comment without the literal trigger substring on the first draft.

## Issues Encountered

- **`qa:run` blocked on missing environment credentials (non-blocking for this plan, carried forward).** Task 3's action text requires running `npm run qa:run` against a fresh dev server started with `STUDY_COOLDOWN_MINUTES=1`, `DATABASE_URL`, and `DEBUG_CHEAT_SECRET` in the environment, because this plan touched the SRS commit endpoint's validation path (Task 1's schema relocation). Direct inspection confirmed both `DATABASE_URL` and `DEBUG_CHEAT_SECRET` are absent from this execution environment. Per the plan's own explicit instruction ("do NOT fake it and do NOT silently skip: record the exact blocking reason ... and flag it explicitly for the plan 28.1-09 phase gate, which runs the same harness"), this was not attempted, faked, or silently bypassed — it is recorded here and must be re-attempted (with the harness rerun in full) whenever 28.1-09's phase gate runs, in an environment where those two variables are actually available. All of this plan's OTHER static verification (schema-boundary tests, the unchanged-behaviour `route.test.ts`, `tsc`, full `vitest run`, `npm run build`) is unaffected by this gap and is fully green.

## User Setup Required

None — no external service configuration required. `DATABASE_URL`/`DEBUG_CHEAT_SECRET` are pre-existing project secrets (not new to this plan) needed only for the deferred `qa:run` step above, not for anything this plan otherwise built.

## Next Phase Readiness

- `@leocards/domain/schemas` and `@leocards/domain/contracts` are both fully populated — all six of the package's declared subpaths (`.`, `./ids`, `./study`, `./habitat`, `./schemas`, `./contracts`) now have real content, closing out the six-key export map 28.1-01 first declared.
- Plans 28.1-05/06/07 (the new dashboard/study/cards/decks/account route handlers) can now implement directly against fixed request schemas and response contracts rather than inventing shapes ad hoc — this was this plan's whole purpose per RESEARCH Open Question 4.
- The DR-03 error convention is locked and ready: a route handler adapts `CoreResult` via `Response.json({ error: code }, { status: CORE_ERROR_STATUS[code] })`; a server action adapts it via `throw new Error(...)`, preserving `deck-actions.ts`'s and `account-actions.ts`'s existing throw-based caller contracts byte-for-byte.
- **One item carries forward to 28.1-09's phase gate:** `qa:run` must be executed there (or in any environment with `DATABASE_URL`/`DEBUG_CHEAT_SECRET` available) to close out Task 3's SRS-regression verification for real — see Issues Encountered.
- No blockers to 28.1-04 or any other remaining plan in this phase. `NAT-01`/`NAT-02` traceability remains correctly unmarked — 3 of 5 contributing plans for NAT-01 now landed (01/02/03), 1 of 6 for NAT-02 (03); both still need 08 and/or 04-07, with 09 last for both.

---
*Phase: 28.1-native-foundation-workspace-api*
*Completed: 2026-08-03*

## Self-Check: PASSED

All 13 created files (5 schemas, 7 contracts, 1 firewall test) plus this SUMMARY.md confirmed present via `[ -f "$f" ]`. All 3 task commit hashes (`9ec6192`, `4811d2a`, `d6e0b56`) confirmed present via `git log --oneline --all`. No missing items.
