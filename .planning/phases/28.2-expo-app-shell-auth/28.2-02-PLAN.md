---
phase: 28.2-expo-app-shell-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/app.json
  - mobile/eas.json
autonomous: false
requirements: [NAT-03]

user_setup:
  - service: expo
    why: "D-35 — EAS project linkage so Phase 28.5 starts from a ready build pipeline"
    env_vars: []
    dashboard_config:
      - task: "Create a free Expo account"
        location: "https://expo.dev/signup"
      - task: "Authenticate the local CLI (npx eas-cli@latest login)"
        location: "terminal on the dev machine"

must_haves:
  truths:
    - "The mobile app is linked to a real Expo project — the project ID is committed, not held in someone's shell"
    - "Build profiles exist and are committed, so Phase 28.5 can build without configuring anything first"
    - "Zero builds were triggered — no EAS build minutes were consumed this phase"
  artifacts:
    - path: "mobile/eas.json"
      provides: "EAS build profiles (development / preview / production), committed, no builds run"
      contains: "build"
    - path: "mobile/app.json"
      provides: "Expo app config carrying the linked EAS project ID alongside the existing scheme"
      contains: "projectId"
  key_links:
    - from: "mobile/app.json"
      to: "expo.dev project"
      via: "extra.eas.projectId written by `eas init`"
      pattern: "projectId"
---

<objective>
Satisfy D-35's definition of "EAS-configured": an Expo account exists, the `mobile/` project is
linked to it by ID, and `eas.json` build profiles are committed — with **zero builds triggered**.

Purpose: Phase 28.5 ships real TestFlight / Play-internal builds. Doing the account creation and
project linkage now, off the critical path, means 28.5 starts from a ready pipeline instead of
stalling on a human action. D-34 keeps this whole phase on Expo Go, so nothing here is exercised by a
build until 28.5.

Output: `mobile/eas.json` (new) and a project ID in `mobile/app.json`. Nothing else changes; no
dependency is installed; no build runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.2-expo-app-shell-auth/28.2-CONTEXT.md
@.planning/phases/28.2-expo-app-shell-auth/28.2-RESEARCH.md
@AGENTS.md

<planner_decisions>
- **This plan is deliberately isolated and depends on nothing.** It is the phase's one unavoidable
  human-account action, mirroring the D-11 enrolment pattern and 18-01's Vercel-dashboard checkpoint.
  Nothing downstream imports or reads either file it touches, so a stall here never blocks native work.
- **`eas-cli` is run via `npx eas-cli@latest`, never installed as a project dependency.** It is a dev
  tool, not part of the app's dependency tree, so it does not enter the lockfile and needs no
  supply-chain gate (RESEARCH Standard Stack).
- **Bundle identifiers are NOT curated here.** `com.joshashley.leocards` plus the display-name / icon /
  splash identity work is D-17..D-20, owned by Phase 28.5 (NAT-08). Do not front-run those decisions.
- **RESEARCH Assumption A4 (`eas init` writes `extra.eas.projectId` and needs `eas login` first) is
  WebSearch-sourced.** The command is interactive and self-documenting — follow what it actually
  prints rather than this document.
</planner_decisions>

<interfaces>
`mobile/app.json` current full contents (9 lines, read this planning session): a single `expo` object
with `name: "LeoCards"`, `slug: "leocards"`, `version: "0.0.0"`, `scheme: "leocards"`.

`scheme: "leocards"` is already correct and load-bearing for the whole phase — it is what
`expoClient({ scheme: "leocards" })` and the server's `trustedOrigins: ["leocards://"]` agree on.
**Do not change `name`, `slug`, `version` or `scheme` in this plan.** The only addition is the EAS
project ID.

`mobile/eas.json` does not exist, and there is no EAS configuration anywhere in this repo yet — no
in-repo analog (28.2-PATTERNS.md Group I). The minimal schema lives at `docs.expo.dev/eas/json`; the
conventional profile set is `development` / `preview` / `production`, plus a `cli` block pinning the
CLI version range. Validate against whatever the installed CLI accepts rather than inventing keys.

RESEARCH verified `eas-cli` at 21.5.0 on the live registry (published 2026-08-03). Running
`npx eas-cli@latest` resolves it fresh at execution time.
</interfaces>

<execution_notes>
- Commit with an explicit trailing pathspec: `git commit -m "..." -- mobile/app.json mobile/eas.json`.
- `gsd-sdk query state.*` verbs are known-broken on this project — hand-edit STATE.md, verify via `git diff`.
- `mobile/app.json` is also edited by plan 28.2-05 (expo-router config) in a later wave. Keep this
  plan's diff to the EAS field only so the two changes stay independently reviewable.
</execution_notes>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Josh creates the Expo account and authenticates the CLI</name>
  <files>(no file written — human action only)</files>
  <read_first>
    - .planning/phases/28.2-expo-app-shell-auth/28.2-CONTEXT.md (D-35 — the exact scope: account + init + eas.json, zero builds)
  </read_first>
  <action>
Ask Josh to create a free Expo account and authenticate the local CLI, then STOP until he confirms.
There is no CLI or API path to creating an account, so this is a genuine human-only action — the rare
case where `checkpoint:human-action` is warranted.

Before presenting, run `npx eas-cli@latest whoami` and report the result. If it already prints a
username, the account and login already exist and this checkpoint resolves immediately with no work
for Josh — do not ask him to do something he has already done.

State explicitly that this creates no builds, consumes no build minutes and requires no payment:
D-35 scopes this to account plus linkage only, and D-34 keeps the entire phase on Expo Go.
  </action>
  <verify>
    <automated>npx eas-cli@latest whoami</automated>
  </verify>
  <acceptance_criteria>
    - `npx eas-cli@latest whoami` prints an Expo username and exits 0
    - No `eas build` command was run at any point during this task
  </acceptance_criteria>
  <what-built>
Nothing yet — this is the account prerequisite for `eas init`. `npx eas-cli@latest whoami` has
already been run; report whether it shows a logged-in user.
  </what-built>
  <how-to-verify>
1. If `whoami` already printed a username, there is nothing to do — just confirm.
2. Otherwise create a free account at https://expo.dev/signup (in Chrome).
3. In a terminal run `npx eas-cli@latest login` and sign in with that account.
4. Confirm `npx eas-cli@latest whoami` prints your username.
  </how-to-verify>
  <resume-signal>Type "logged in" once `whoami` shows your username, or describe any blocker.</resume-signal>
  <done>`npx eas-cli@latest whoami` exits 0 with a username, and no build was triggered.</done>
</task>

<task type="auto">
  <name>Task 2: Link the project with `eas init` and commit build profiles</name>
  <files>mobile/app.json, mobile/eas.json</files>
  <read_first>
    - mobile/app.json (current 9-line contents — confirm the scheme value before and after)
    - .planning/phases/28.2-expo-app-shell-auth/28.2-CONTEXT.md (D-35 — zero builds)
    - .planning/phases/28.2-expo-app-shell-auth/28.2-PATTERNS.md (Group I — mobile/eas.json has no in-repo analog)
  </read_first>
  <action>
From inside `mobile/`, run `npx eas-cli@latest init` and let it create and link the Expo project for
slug `leocards`, writing the project ID into `app.json` itself. Follow the CLI's own prompts. Do not
hand-write a project ID.

Then author `mobile/eas.json` with three build profiles named `development`, `preview` and
`production`, plus a `cli` block pinning the CLI version range. Keep it minimal: development uses a
development client with internal distribution; preview uses internal distribution; production uses
the default store distribution. Add nothing speculative — no credentials block, no env block, no
submit profiles. Phase 28.5 owns whatever else the pipeline turns out to need.

Never run `eas build` in any form. If any command offers to start a build, decline.

After the edits, confirm `mobile/app.json` still carries its original name, slug, version and scheme
values and that the only structural addition is the EAS project ID. Confirm the placeholder app still
typechecks and bundles, then delete the export output so no build artefact is committed
(`mobile/dist/` is gitignored, but leave the tree clean anyway).
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; node -e "const a=require('./app.json'); if(!a.expo.extra||!a.expo.extra.eas||!a.expo.extra.eas.projectId) throw new Error('no projectId'); if(a.expo.scheme!=='leocards') throw new Error('scheme changed'); if(a.expo.slug!=='leocards') throw new Error('slug changed'); const e=require('./eas.json'); for (const p of ['development','preview','production']) if(!e.build||!e.build[p]) throw new Error('missing profile '+p); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/eas.json` exists, is valid JSON, and its `build` object has `development`, `preview` and `production` keys (asserted by the automated command above)
    - `mobile/app.json` contains a non-empty `expo.extra.eas.projectId`
    - `mobile/app.json` still has `"scheme": "leocards"`, `"slug": "leocards"`, `"name": "LeoCards"` — `git diff mobile/app.json` shows additions only, no modified lines among those four original fields
    - `npx eas-cli@latest config --non-interactive` (run from `mobile/`) exits 0
    - `npx tsc --noEmit --project mobile/tsconfig.json` exits 0
    - `git status --porcelain mobile/dist` is empty (no export artefact left behind)
    - No `eas build` invocation appears anywhere in the session transcript
  </acceptance_criteria>
  <done>`mobile/eas.json` and the linked project ID are committed, the CLI validates the config, the existing scheme/slug/name are untouched, and zero builds were triggered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → expo.dev account | `eas init` associates this codebase with a third-party account; the project ID becomes a committed, public-in-repo identifier |
| npx → transient CLI download | `eas-cli` is fetched and executed at run time without entering the lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28.2-04 | Information Disclosure | `mobile/eas.json`, `mobile/app.json` | mitigate | Commit only the project ID and build profiles. No credentials, no API keys, no `env` block in `eas.json` — the acceptance criteria forbid speculative keys, and EAS secrets belong in the EAS dashboard, never in a committed file |
| T-28.2-05 | Denial of Service (cost) | EAS build minutes | mitigate | D-35 caps this plan at account + init + eas.json. The acceptance criteria assert no `eas build` ran; the CLI is only invoked with `whoami`, `init` and `config` |
| T-28.2-SC | Tampering | `npx eas-cli@latest` | accept | Official first-party Expo CLI (`github.com/expo/eas-cli`), invoked per-command via npx and never added to the dependency tree, so it cannot affect the app's runtime or the production deploy footprint |
</threat_model>

<verification>
- `npx eas-cli@latest whoami` exits 0 with a username
- The Task 2 automated node assertion exits 0 (project ID present, scheme/slug intact, three build profiles present)
- `npx eas-cli@latest config --non-interactive` exits 0 from `mobile/`
- `npx tsc --noEmit --project mobile/tsconfig.json` exits 0
- `git log -1 --name-only` lists exactly `mobile/app.json` and `mobile/eas.json`
</verification>

<success_criteria>
- Josh has an Expo account and the local CLI is authenticated
- `mobile/` is linked to a real Expo project by a committed project ID
- `mobile/eas.json` exists with three committed build profiles and no secrets
- Zero EAS builds were triggered (D-35), and the app's scheme/slug/name are byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/28.2-expo-app-shell-auth/28.2-02-SUMMARY.md` when done. Record the Expo
username, the linked project ID, and explicit confirmation that no build was run.
</output>
