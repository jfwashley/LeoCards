---
phase: 12-pause-cards-in-active-deck-review-let-users-temporarily-excl
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/lib/study-engine.ts
  - src/lib/study-engine.test.ts
  - src/lib/study-queries.ts
autonomous: true
requirements: [P12-03, P12-04, P12-05]
user_setup: []

must_haves:
  truths:
    - "computeUnpauseUpdate(pausedAt, cooldownUntil, now) is exported from study-engine.ts and is pure (no I/O, no Date.now() in the body)"
    - "NULL cooldown stays NULL; non-NULL cooldown shifts forward by exactly (now − pausedAt); pausedAt always returns null"
    - "getStudyCards() filters out rows where pausedAt IS NOT NULL via Drizzle isNull(cards.pausedAt) in the WHERE clause"
    - "assembleSession and earliestCooldownEnd remain UNCHANGED — pause filtering happens at the query layer, not in the engine"
    - "Existing study-engine.test.ts suite stays green; the new computeUnpauseUpdate describe block is green"
  artifacts:
    - path: "src/lib/study-engine.ts"
      provides: "computeUnpauseUpdate pure function appended after earliestCooldownEnd"
      exports: ["computeUnpauseUpdate"]
      contains: "export function computeUnpauseUpdate"
    - path: "src/lib/study-engine.test.ts"
      provides: "computeUnpauseUpdate describe block covering NULL / future / past cooldown + zero-duration pause"
      contains: "describe(\"computeUnpauseUpdate\""
    - path: "src/lib/study-queries.ts"
      provides: "getStudyCards filtered by isNull(cards.pausedAt)"
      contains: "isNull(cards.pausedAt)"
  key_links:
    - from: "src/lib/study-queries.ts getStudyCards()"
      to: "src/db/schema.ts cards.pausedAt"
      via: "drizzle-orm isNull predicate inside and(...)"
      pattern: "and\\(eq\\(cards\\.deckId.*isNull\\(cards\\.pausedAt\\)"
    - from: "src/lib/study-engine.ts computeUnpauseUpdate"
      to: "src/lib/study-engine.test.ts (4 cases)"
      via: "import + Vitest describe block"
      pattern: "describe\\(\"computeUnpauseUpdate\""
---

<objective>
Wave 2: pure additions to the study-engine and the query layer that together
make pause invisible to the rest of the app. Two surgical edits:

1. Add `computeUnpauseUpdate(pausedAt, cooldownUntil, now)` — the pure
   cadence-shift function (D-01 from CONTEXT.md). Co-located with the existing
   pure functions in `study-engine.ts`; unit-tested in the same style as
   `computeCardUpdate`. Plan 12-03's unpause endpoint will call it.

2. Add `isNull(cards.pausedAt)` to the WHERE clause of `getStudyCards()`. This
   single line makes the dashboard's due-count, `earliestCooldownEnd`, and
   `assembleSession` all skip paused cards automatically — `assembleSession`
   itself is NEVER touched (12-RESEARCH.md "Pitfall 3"). No CardForSession field
   added, no engine code changed.

Purpose: Establish the in-memory + DB-query foundation for pause. Subsequent
plans (12-03 API, 12-04 UI) consume these primitives.

Output: extended study-engine.ts (+ tests) + a 1-line addition to study-queries.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pause-cards-in-active-deck-review-let-users-temporarily-excl/12-CONTEXT.md
@.planning/phases/12-pause-cards-in-active-deck-review-let-users-temporarily-excl/12-RESEARCH.md
@.planning/phases/12-pause-cards-in-active-deck-review-let-users-temporarily-excl/12-01-SUMMARY.md
@src/lib/study-engine.ts
@src/lib/study-engine.test.ts
@src/lib/study-queries.ts

<interfaces>
<!-- The pure function to add — exact signature per 12-RESEARCH.md "Where to put computeUnpauseUpdate" -->

export function computeUnpauseUpdate(
  pausedAt: Date,
  cooldownUntil: Date | null,
  now: Date,
): { cooldownUntil: Date | null; pausedAt: null };

<!-- Existing exports in study-engine.ts that this function lives ALONGSIDE
     (not modify): assembleSession, computeCardUpdate, earliestCooldownEnd.
     CardForSession interface is also NOT changed (Pitfall 3). -->

<!-- The query-layer change — getStudyCards in src/lib/study-queries.ts.
     Existing predicate: `.where(eq(cards.deckId, deckId))`
     New predicate:     `.where(and(eq(cards.deckId, deckId), isNull(cards.pausedAt)))`
     `isNull` already exported by `drizzle-orm` (sibling of `eq`).
     `and` is NOT yet imported in study-queries.ts (the existing file imports
     only `eq` from drizzle-orm) — add `and, isNull` to the import. -->

<!-- The dashboard data path inherits the filter for free:
     dashboard/page.tsx line 70-73 calls getStudyCards → maps to CardForSession
     → assembleSession / earliestCooldownEnd. Paused cards never enter that
     array, so hasDueCards and the cooldown countdown both auto-exclude them. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append computeUnpauseUpdate + its Vitest describe block</name>
  <read_first>
    - src/lib/study-engine.ts (full file — confirm `earliestCooldownEnd` is the last function so the new one appends cleanly; mirror the JSDoc style used by `computeCardUpdate`)
    - src/lib/study-engine.test.ts (mirror the existing `makeCard` / fixed-NOW/PAST/FUTURE constants style; same import style; same describe/it phrasing)
    - 12-RESEARCH.md § "Where to put computeUnpauseUpdate" (verbatim function body to use) + § "Code Examples" → "Vitest test scaffold" (verbatim 4 test cases)
    - 12-CONTEXT.md "Cadence-preservation semantics" (D-01: edge cases — NULL cooldown stays NULL; overdue card stays overdue by same amount; lastStudiedAt UNCHANGED — confirm function does NOT touch lastStudiedAt)
  </read_first>
  <behavior>
    - computeUnpauseUpdate is pure: no Date.now(), no I/O, no closure over module state; all inputs are arguments
    - cooldownUntil === null → returns `{ cooldownUntil: null, pausedAt: null }` (new card stays new)
    - cooldownUntil non-null → returns `{ cooldownUntil: new Date(cooldownUntil.getTime() + (now.getTime() - pausedAt.getTime())), pausedAt: null }`
    - return shape always has `pausedAt: null` (typed as literal `null`, not `Date | null`) — encodes "this writes will-clear pausedAt"
    - return type explicitly does NOT include lastStudiedAt — pause does NOT mutate lastStudiedAt (D-01 explicit, Pitfall 4)
    - zero-duration pause (`now === pausedAt`) leaves cooldownUntil unchanged
    - past cooldown shifts forward by the same delta (overdue card stays overdue by same amount, never "extra overdue")
  </behavior>
  <files>src/lib/study-engine.ts, src/lib/study-engine.test.ts</files>
  <action>
    Step 1 — Append the pure function to `src/lib/study-engine.ts`, AFTER
    `earliestCooldownEnd` (the last existing export). Use the verbatim body
    from 12-RESEARCH.md § "Where to put computeUnpauseUpdate":

      /**
       * Computes the cooldown shift when unpausing a card.
       *
       * Rule: cooldownUntil shifts forward by exactly (now − pausedAt). The SRS clock
       * was frozen during pause; on unpause the card resurfaces with the same cadence
       * it would have had absent the pause. NULL cooldown stays NULL.
       *
       * lastStudiedAt is NOT mutated — pausedAt is the source of truth for "card
       * was unavailable" intervals.
       */
      export function computeUnpauseUpdate(
        pausedAt: Date,
        cooldownUntil: Date | null,
        now: Date,
      ): { cooldownUntil: Date | null; pausedAt: null } {
        if (cooldownUntil === null) {
          return { cooldownUntil: null, pausedAt: null };
        }
        const pauseDurationMs = now.getTime() - pausedAt.getTime();
        return {
          cooldownUntil: new Date(cooldownUntil.getTime() + pauseDurationMs),
          pausedAt: null,
        };
      }

    Do NOT touch any other export. Do NOT change CardForSession. Do NOT add a
    field to assembleSession's input. (Pitfall 3 — filter belongs at the
    query layer, not here.)

    Step 2 — In `src/lib/study-engine.test.ts`, add an import of
    `computeUnpauseUpdate` (alongside the existing imports) and APPEND (not
    replace) a new `describe("computeUnpauseUpdate", () => { ... })` block at
    the end of the file using EXACTLY the 4 cases from 12-RESEARCH.md § "Code
    Examples" → "Vitest test scaffold":

      1. "leaves NULL cooldown NULL"
      2. "shifts future cooldown forward by exact pause duration"
      3. "shifts past cooldown forward too (overdue card stays overdue by same amount)"
      4. "zero-duration pause leaves cooldown unchanged"

    Use fixed Date constants inside each `it()` (no Date.now() — keep the
    existing file's deterministic-time convention). Assert via `.toISOString()`
    string equality on the result's `cooldownUntil` and direct `toBeNull()` on
    `pausedAt`.
  </behavior>
  <verify>
    <automated>npx vitest run src/lib/study-engine.test.ts -t computeUnpauseUpdate && npx vitest run src/lib/study-engine.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    `grep -c 'export function computeUnpauseUpdate' src/lib/study-engine.ts` == 1;
    `grep -c 'describe(\"computeUnpauseUpdate\"' src/lib/study-engine.test.ts` == 1;
    4 `it(...)` cases present in that describe block;
    `npx vitest run src/lib/study-engine.test.ts` GREEN (the new block + all
    pre-existing blocks); `npx tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add isNull(cards.pausedAt) to getStudyCards WHERE clause</name>
  <read_first>
    - src/lib/study-queries.ts (entire file — 43 lines; the ONLY function is getStudyCards on lines 19-42; existing import line: `import { eq } from "drizzle-orm";`)
    - 12-RESEARCH.md § "src/lib/study-queries.ts" (verbatim filter code)
    - 12-CONTEXT.md "Visibility & filtering of paused cards" (D-04: assembleSession excludes paused; dashboard counts exclude paused — both inherited from this single filter)
    - src/app/(protected)/dashboard/page.tsx lines 70-100 (confirms getStudyCards output → assembleSession + earliestCooldownEnd; no other change needed downstream)
  </read_first>
  <files>src/lib/study-queries.ts</files>
  <action>
    In `src/lib/study-queries.ts`:

      1. Change the import:
           BEFORE: `import { eq } from "drizzle-orm";`
           AFTER:  `import { and, eq, isNull } from "drizzle-orm";`

      2. Change the WHERE clause of `getStudyCards`:
           BEFORE: `.where(eq(cards.deckId, deckId));`
           AFTER:  `.where(and(eq(cards.deckId, deckId), isNull(cards.pausedAt)));`

    Do NOT change the SELECT projection (the engine does not consume pausedAt
    — the filter does all the work). Do NOT modify `getDeckCards`,
    `getDeckCardWords`, or `getUserNativeLanguage` in deck-queries.ts in this
    plan — those propagate pausedAt to the UI via `cards.$inferSelect` and are
    handled at the call-site level in Plan 12-04.

    Do NOT add a server-side test for this query: the file is too small to mock
    db meaningfully and Vitest coverage for the engine side already validates
    the downstream effect — but write a one-line comment above the where()
    referencing 12-CONTEXT.md D-04 so future readers see why pause is enforced
    at this layer rather than in study-engine.
  </action>
  <verify>
    <automated>grep -c 'isNull(cards.pausedAt)' src/lib/study-queries.ts && grep -c 'and, eq, isNull' src/lib/study-queries.ts && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>
    `grep -c 'isNull(cards.pausedAt)' src/lib/study-queries.ts` == 1;
    drizzle-orm import line contains `and`, `eq`, and `isNull`;
    `npx tsc --noEmit` clean; `npm test` (full Phases 1–11 suite + new Plan
    12-02 tests) GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure function (computeUnpauseUpdate) | No trust boundary — pure compute, no I/O, no inputs from network. |
| Query layer (getStudyCards) | DB-side WHERE filter; downstream of an already-authenticated server component caller (dashboard/page.tsx ensures session). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | computeUnpauseUpdate math | accept | Pure function over caller-supplied Dates. Unit tests cover NULL / past / future / zero-duration. No external input. |
| T-12-04 | Information Disclosure | getStudyCards exposing paused-card existence | accept | Paused cards are filtered OUT of the result set. The caller never sees them, so no leakage. (Ownership is enforced by dashboard/page.tsx's session lookup, not this query — unchanged.) |
</threat_model>

<verification>
- `npx vitest run src/lib/study-engine.test.ts` — every existing describe block
  + the new `computeUnpauseUpdate` block GREEN.
- `npx tsc --noEmit` clean.
- `npm test` — full Phases 1–11 suite + Plan 12-02 additions GREEN.
- The grep checks in each task's <done> pass.
</verification>

<success_criteria>
- `computeUnpauseUpdate` exists, exported, pure, and covered by 4 unit tests.
- `getStudyCards` filters out paused cards at the SQL layer with one
  `isNull(cards.pausedAt)` predicate combined via `and(...)`.
- `assembleSession`, `earliestCooldownEnd`, and `CardForSession` are unchanged.
- No regression in any prior suite.
</success_criteria>

<output>
After completion, create
`.planning/phases/12-pause-cards-in-active-deck-review-let-users-temporarily-excl/12-02-SUMMARY.md`.
Record explicitly: the exact line number where `computeUnpauseUpdate` lives in
study-engine.ts (Plan 12-03's unpause endpoint imports from it) and confirm
that assembleSession / CardForSession were untouched per Pitfall 3.
</output>
</content>
</invoke>