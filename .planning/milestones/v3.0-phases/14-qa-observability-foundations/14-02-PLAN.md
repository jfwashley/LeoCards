---
phase: 14-qa-observability-foundations
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/qa-state-badge.tsx
  - src/components/__tests__/qa-state-badge.test.ts
  - src/components/study-card.tsx
  - src/components/study-session.tsx
  - src/components/card-stack.tsx
  - src/components/card-list.tsx
  - src/components/card-edit-dialog.tsx
  - src/components/deck-view.tsx
  - src/app/(protected)/study/page.tsx
  - src/app/(protected)/dashboard/page.tsx
autonomous: true
requirements: [QAOB-01]
must_haves:
  truths:
    - "When QA-authed (leo-qa-mode cookie valid), every study session card shows a top-right monospace state badge like R2·t2n·cd:14m"
    - "When QA-authed, every dashboard card-list row shows the same state badge"
    - "The cooldown segment ticks down live (cd:14m -> cd:13m) without a page refresh"
    - "Customers (no QA cookie) receive no QaStateBadge in the DOM — no [data-qa-badge] element, no countdown interval"
    - "The badge causes no layout shift (absolute-positioned overlay)"
    - "D-01: the badge renders only when QA-authed (RSC reads readQaAuth and gates the prop); customers never receive it"
    - "D-04: placement is a small monospace corner badge pinned top-right, semi-transparent, with no layout shift"
    - "D-05: state codes appear on study session cards AND dashboard card-list rows"
    - "D-06: the cooldown segment ticks live and the client timer is itself QA-gated (no interval for customers)"
  artifacts:
    - path: "src/components/qa-state-badge.tsx"
      provides: "QaStateBadge client component, QaCardData type, formatCd() exported"
      contains: "data-qa-badge"
      min_lines: 40
    - path: "src/app/(protected)/study/page.tsx"
      provides: "readQaAuth() gate passing qaMode to StudySession"
      contains: "readQaAuth"
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "readQaAuth() gate + cooldownUntil on cardRows when qaMode"
      contains: "readQaAuth"
  key_links:
    - from: "src/app/(protected)/study/page.tsx"
      to: "src/components/qa-state-badge.tsx"
      via: "qaMode prop -> StudySession -> CardStack -> StudyCard -> QaStateBadge"
      pattern: "qaMode"
    - from: "src/components/study-card.tsx"
      to: "src/components/qa-state-badge.tsx"
      via: "renders <QaStateBadge data={qaCardData} /> only when qaCardData != null"
      pattern: "QaStateBadge"
---

<objective>
Surface per-card SRS state codes on the study and dashboard surfaces (QAOB-01): build the `QaStateBadge` client component (corner monospace badge + live cooldown countdown), then wire it through RSC gating so it renders ONLY when the browser is QA-authed (valid `leo-qa-mode` cookie) and is provably absent from the customer DOM.

Purpose: This is the visible half of QA observability. It must satisfy the hard QAOB-04 constraint that customers' DOM literally does not contain the badge — gating is at the RSC layer (omit the prop), never CSS hiding. Depends on `readQaAuth()` from Plan 01.

Output: New `QaStateBadge` component + unit tests; prop-threaded wiring through study-card / study-session / card-stack and card-list / card-edit-dialog / deck-view; `readQaAuth()` gates in study and dashboard RSC pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-qa-observability-foundations/14-CONTEXT.md
@.planning/phases/14-qa-observability-foundations/14-RESEARCH.md
@.planning/phases/14-qa-observability-foundations/14-PATTERNS.md
@.planning/phases/14-qa-observability-foundations/14-01-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Verified from source. -->

From src/lib/debug-cheat.ts (added in Plan 01 — Plan 02 consumes):
```typescript
export async function readQaAuth(): Promise<boolean>;  // true iff valid leo-qa-mode cookie
```

From src/lib/study-engine.ts (SessionCard already carries badge data — no extra query):
```typescript
interface SessionCard { id; front; back; masteryRound: number; cooldownUntil: Date | null; stage: "n2t" | "t2n"; isResurface: boolean; /* ... */ }
```

From src/components/card-edit-dialog.tsx (CardRow — extend with cooldownUntil for badge):
```typescript
export interface CardRow {
  id: string; front: string; back: string; source: string;
  createdAt: Date; masteryRound?: number; pausedAt: Date | null;
}
```

From src/components/study-card.tsx: receives `card: SessionCard`; outermost element is `<motion.div ... className="relative w-full">` (line 84) — already `relative`, so an absolute badge needs no extra positioning wrapper.

From src/app/(protected)/dashboard/page.tsx (lines 111-119): `cardRows` is built from `cards.map(...)`; `studyCards` (already fetched) has `cooldownUntil` and is keyed via `masteryByCardId`. Pass `qaMode` + `cooldownUntil` here. DeckView receives `initialCards={cardRows}`.

From src/app/(protected)/study/page.tsx (line 61): `return <StudySession initialCards={sessionCards} deckId={deckId} />;` — add `qaMode`.

State-code format (D-07, finalized): tokens joined with `·`:
- `R{masteryRound}` always (R0..R3)
- `n2t`/`t2n` (stage) when not learned; `L` when masteryRound >= 3 (replaces direction)
- `cd:{Nm | Hh Mm-as-1h30m}` only when cooldownUntil is in the future
- `P` when pausedAt != null
Examples: `R0·n2t`, `R1·t2n·cd:22m`, `R2·t2n·cd:1h15m`, `R3·L`, `R1·t2n·P`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QaStateBadge client component + unit tests</name>
  <files>src/components/qa-state-badge.tsx, src/components/__tests__/qa-state-badge.test.ts</files>
  <read_first>
    - src/components/deck-view.tsx (CountdownTimer + formatCountdown: the hydration-safe useState lazy-initializer + useEffect interval pattern to mirror)
    - src/components/study-card.tsx (parent it overlays — `relative w-full` motion.div, so badge uses `absolute`)
    - src/lib/debug-cheat.test.ts (vitest test structure to mirror for the new test file)
  </read_first>
  <behavior>
    - formatCd(0) === "" ; formatCd(14*60000) === "14m" ; formatCd(90*60000) === "1h30m"
    - Token assembly R0·n2t when masteryRound=0, stage="n2t", no cooldown, not paused
    - Token assembly R1·t2n·cd:22m when masteryRound=1, stage="t2n", cooldownUntil 22 min in future
    - Token assembly R3·L when masteryRound=3 (learned token replaces direction)
    - Token assembly R1·t2n·P when pausedAt is set
    - Rendered element has the data-qa-badge attribute (DOM presence checkable by QAOB-04)
  </behavior>
  <action>
    Create src/components/qa-state-badge.tsx as a `"use client"` component. Define and EXPORT `interface QaCardData { masteryRound: number; stage: "n2t" | "t2n"; cooldownUntil: Date | null; pausedAt: Date | null; }`. EXPORT a pure `function formatCd(ms: number): string` (so the unit test imports it directly): returns "" when ms <= 0; otherwise `totalMin = Math.ceil(ms / 60_000)`, `h = floor(totalMin/60)`, `m = totalMin%60`, return `h>0 ? `${h}h${m}m` : `${m}m``. EXPORT `function QaStateBadge({ data }: { data: QaCardData })`. Compute `learned = data.masteryRound >= 3`. Use a hydration-safe lazy `useState(() => data.cooldownUntil ? formatCd(data.cooldownUntil.getTime() - Date.now()) : "")` for the cooldown label (lazy initializer — NEVER `useState(formatCd(...))` — RESEARCH pitfall 2). In `useEffect` keyed on `[data.cooldownUntil]`: if no cooldownUntil, return; define a `tick()` that recomputes the label, sets it to "" when expired; call tick() once; pick interval `(remaining < 5*60_000) ? 10_000 : 60_000` (fine-grained for short QA cooldowns per D-06); `setInterval(tick, intervalMs)`; clean up with clearInterval. Build the token array `[`R${data.masteryRound}`, learned ? "L" : data.stage, cdLabel ? `cd:${cdLabel}` : "", data.pausedAt ? "P" : ""].filter(Boolean)`. Render a single `<span data-qa-badge aria-hidden="true" className="absolute top-1 right-1 z-20 font-mono text-[10px] bg-black/40 text-white/90 rounded px-1 py-0.5 pointer-events-none select-none">{tokens.join("·")}</span>`.

    Create src/components/__tests__/qa-state-badge.test.ts. Import `formatCd` and test the three formatCd cases. For token assembly, either import a small extracted `buildTokens(data, cdLabel)` helper (cleanest — extract and export it) OR render `QaStateBadge` via a testing-library render and assert textContent. Prefer extracting+exporting `buildTokens` so tokens are unit-tested without DOM. No env mock needed (pure logic). Cover R0·n2t, R1·t2n·cd:22m, R3·L, R1·t2n·P.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/qa-state-badge.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/qa-state-badge.tsx begins with `"use client"` and exports `QaStateBadge`, `QaCardData`, and `formatCd`
    - The rendered span has literal attribute `data-qa-badge` and className contains `absolute top-1 right-1`
    - Cooldown label state uses a lazy `useState(() => ...)` initializer (grep: `useState(() =>` present)
    - `npx vitest run src/components/__tests__/qa-state-badge.test.ts` exits 0 asserting formatCd(0)==="", formatCd(90*60000)==="1h30m", and token cases R0·n2t / R1·t2n·cd:22m / R3·L / R1·t2n·P
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>QaStateBadge renders a data-qa-badge overlay with a live, hydration-safe cooldown countdown; token + formatCd logic unit-tested green; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire badge into study surface (study/page.tsx -> StudySession -> CardStack -> StudyCard)</name>
  <files>src/app/(protected)/study/page.tsx, src/components/study-session.tsx, src/components/card-stack.tsx, src/components/study-card.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/cookies.md (Next.js 16 cookies() is async — readQaAuth awaits it in this RSC page; AGENTS.md requires reading before touching Next.js APIs)
    - src/app/(protected)/study/page.tsx (the file being modified — line 61 StudySession return; sessionCards already carry masteryRound/cooldownUntil/stage)
    - src/components/study-session.tsx (the file being modified — StudySession props, how queue/currentIndex flows to CardStack)
    - src/components/card-stack.tsx (the file being modified — how it renders StudyCard for the top card)
    - src/components/study-card.tsx (the file being modified — StudyCardProps, the `relative w-full` motion.div outermost element)
    - src/components/qa-state-badge.tsx (QaStateBadge, QaCardData from Task 1)
    - src/lib/debug-cheat.ts (readQaAuth from Plan 01)
  </read_first>
  <action>
    In src/app/(protected)/study/page.tsx, import `readQaAuth` from `@/lib/debug-cheat`. After the session/deck-ownership checks, add `const qaMode = await readQaAuth();`. Change the return to `return <StudySession initialCards={sessionCards} deckId={deckId} qaMode={qaMode} />;`. No extra query — sessionCards already include masteryRound, cooldownUntil, stage.

    In src/components/study-session.tsx, add `qaMode?: boolean` to the StudySession props (default false). Thread it down to `CardStack` (and/or directly to the rendered `StudyCard`) — pass `qaMode` through whatever component renders the current `StudyCard`. Do not alter session/reducer logic.

    In src/components/card-stack.tsx, accept and forward `qaMode?: boolean` to the `StudyCard` it renders for the active card.

    In src/components/study-card.tsx, add `qaMode?: boolean` to `StudyCardProps`. Build `qaCardData: QaCardData | null` from `card`: when `qaMode` is true, `{ masteryRound: card.masteryRound, stage: card.stage, cooldownUntil: card.cooldownUntil, pausedAt: card.pausedAt ?? null }`; otherwise `null`. (If `SessionCard` lacks `pausedAt`, use `null` — paused cards are filtered out of the active session anyway; do not add a query.) Render `{qaCardData && <QaStateBadge data={qaCardData} />}` as a child of the outermost `relative w-full` motion.div, after the swipe-overlay children. CRITICAL (RESEARCH anti-pattern + QAOB-04): render the badge ONLY when `qaCardData` is non-null — never render it for customers and hide with CSS. Import QaStateBadge + QaCardData from `@/components/qa-state-badge`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
    <human-check>With DEBUG_CHEAT_SECRET set, enter the secret on /debug, then start a study session: the top-right badge shows e.g. R0·n2t (or with cd: when STUDY_COOLDOWN_MINUTES is set and a prior cooldown is active). In a separate browser with no QA cookie, the same study session shows no badge.</human-check>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/study/page.tsx contains `readQaAuth` import and `const qaMode = await readQaAuth()` and passes `qaMode={qaMode}` to StudySession
    - study-card.tsx renders `<QaStateBadge` guarded by `qaCardData &&` (badge omitted, not CSS-hidden, when qaMode false)
    - `qaMode` prop is threaded study/page -> StudySession -> CardStack -> StudyCard (grep `qaMode` present in all four files)
    - `npx tsc --noEmit` exits 0 (primary gate — this is a 4-file prop-threading task with no isolated unit; the QaStateBadge unit test is owned by sibling task 02-T1 in this same wave and cannot be referenced before it lands, so the full `npx vitest run` is the intentional regression guard) and `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Study session cards show the QA state badge when QA-authed and omit it entirely otherwise; prop chain typed; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire badge into dashboard card-list rows (dashboard/page.tsx -> DeckView -> CardList)</name>
  <files>src/app/(protected)/dashboard/page.tsx, src/components/deck-view.tsx, src/components/card-list.tsx, src/components/card-edit-dialog.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/cookies.md (Next.js 16 cookies() is async — readQaAuth awaits it in this RSC page; AGENTS.md requires reading before touching Next.js APIs)
    - src/app/(protected)/dashboard/page.tsx (the file being modified — lines 44 readHabitatOverride, 100-119 cardRows construction, studyCards with cooldownUntil, 121-133 DeckView return)
    - src/components/deck-view.tsx (the file being modified — DeckView props, how initialCards flow to CardList)
    - src/components/card-list.tsx (the file being modified — CardListProps, the desktop row render where the badge attaches; rows are CardRow)
    - src/components/card-edit-dialog.tsx (the file being modified — CardRow interface, extend with cooldownUntil)
    - src/components/qa-state-badge.tsx (QaStateBadge, QaCardData from Task 1)
    - src/lib/debug-cheat.ts (readQaAuth from Plan 01)
  </read_first>
  <action>
    In src/components/card-edit-dialog.tsx, extend the exported `CardRow` interface with `cooldownUntil?: Date | null;` (optional — keeps backward compat with other CardRow producers).

    In src/app/(protected)/dashboard/page.tsx, import `readQaAuth` from `@/lib/debug-cheat`. After the existing `readHabitatOverride()` call (line 44 area), add `const qaMode = await readQaAuth();`. In the `cardRows` map (lines 111-119), add `cooldownUntil: qaMode ? (studyCards.find((s) => s.id === c.id)?.cooldownUntil ?? null) : null` (studyCards is already fetched and includes cooldownUntil — no new query). Pass `qaMode={qaMode}` to `<DeckView ... />`.

    In src/components/deck-view.tsx, add `qaMode?: boolean` to DeckView props (default false) and forward it to the `CardList` it renders.

    In src/components/card-list.tsx, add `qaMode?: boolean` to `CardListProps`. For each rendered card row, when `qaMode` is true and the row's data is available, render `<QaStateBadge data={{ masteryRound: card.masteryRound ?? 0, stage: (card.masteryRound ?? 0) === 1 ? "t2n" : "n2t", cooldownUntil: card.cooldownUntil ?? null, pausedAt: card.pausedAt }} />` positioned in the row. The row container needs `relative` positioning for the `absolute` badge — add `relative` to the row's className if not present. Guard with `{qaMode && <QaStateBadge .../>}` so customers get no badge element. Note on stage for browse rows: CardList has no per-session stage; derive the NEXT direction from masteryRound (0/2/3 -> "n2t" display intent, 1 -> "t2n"); since learned cards (round 3) render `L` and replace direction, the derived stage for round 3 is cosmetic. Import QaStateBadge from `@/components/qa-state-badge`.

    Scope note (RESEARCH pitfall 5 / D-05): the QA browse surface is the dashboard `CardList` (which has real per-card SRS data), NOT the `/deck/browse` WordListBrowser (a dictionary catalog with no per-user card state). Do not touch word-list-browser.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run lint</automated>
    <human-check>With QA mode active (secret entered on /debug), open /dashboard: each card-list row shows its state badge (e.g. R1·t2n·cd:12m, R3·L, R0·n2t·P). With no QA cookie, no badges appear and no [data-qa-badge] exists in the rendered HTML.</human-check>
  </verify>
  <acceptance_criteria>
    - src/components/card-edit-dialog.tsx CardRow includes `cooldownUntil?: Date | null`
    - dashboard/page.tsx contains `readQaAuth` import, `const qaMode = await readQaAuth()`, `cooldownUntil:` in the cardRows map gated on `qaMode ?`, and `qaMode={qaMode}` on DeckView
    - card-list.tsx renders `<QaStateBadge` guarded by `qaMode &&` and the row container has `relative`
    - `qaMode` threaded dashboard/page -> DeckView -> CardList (grep present in all)
    - `npx tsc --noEmit`, `npx vitest run`, and `npm run lint` all exit 0
  </acceptance_criteria>
  <done>Dashboard card-list rows show the QA state badge when QA-authed and omit it for customers; CardRow carries cooldownUntil; typecheck + unit + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC render -> client component props | The decision to include QA SRS data must happen server-side; props omitted for customers |
| browser DOM | Customer DOM must literally not contain QA badge elements (QAOB-04 asserts absence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Information Disclosure | QA SRS state codes appearing in customer DOM | mitigate | Badge rendered ONLY when `qaCardData`/`qaMode` truthy; the gate is RSC `readQaAuth()` (httpOnly cookie), props omitted for customers — never CSS-hidden (RESEARCH anti-pattern). QAOB-04 (Plan 03) proves `[data-qa-badge]` count 0 |
| T-14-08 | Information Disclosure | Client-side gate using document.cookie (would expose intent / fail on httpOnly) | mitigate | No client cookie read; `readQaAuth()` runs server-side and passes a boolean prop down |
| T-14-09 | Denial of Service (self) | Countdown setInterval running for all users | mitigate | QaStateBadge only mounts when prop present; no interval for customers (component not in tree) |
| T-14-10 | Tampering | Hydration mismatch flicker from Date.now() in render | mitigate | Lazy `useState(() => ...)` initializer + useEffect interval (deck-view.tsx CountdownTimer pattern) — RESEARCH pitfall 2 |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this phase — no install task, no legitimacy gate needed |
</threat_model>

<verification>
- `npx tsc --noEmit` — type-clean prop threading
- `npx vitest run` — full unit suite green (qa-state-badge tests pass)
- `npm run lint` — 0 biome errors
- Manual QA-on / QA-off DOM check (Task 2 + Task 3 human-checks) — badge present when authed, absent otherwise (formalized in Plan 03 QAOB-04 e2e)
</verification>

<success_criteria>
- QaStateBadge renders a top-right monospace state code with a live cooldown countdown on study session cards AND dashboard card-list rows when QA-authed (QAOB-01)
- The badge is omitted from the DOM entirely (no [data-qa-badge], no interval) for customers without the QA cookie
- No layout shift (absolute overlay over an already-`relative` parent)
- Type-clean, unit-tested, lint-clean
</success_criteria>

<output>
Create `.planning/phases/14-qa-observability-foundations/14-02-SUMMARY.md` when done
</output>
