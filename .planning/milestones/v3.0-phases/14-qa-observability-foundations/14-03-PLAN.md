---
phase: 14-qa-observability-foundations
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - e2e/14-qa-parity.spec.ts
autonomous: true
requirements: [QAOB-04]
must_haves:
  truths:
    - "With DEBUG_CHEAT_SECRET unset, a signed-in customer's /dashboard and /study DOM contain zero [data-qa-badge] elements"
    - "With DEBUG_CHEAT_SECRET unset, GET /api/debug/state returns 404"
    - "With DEBUG_CHEAT_SECRET unset, POST /api/debug/cheat returns 404"
    - "The test is self-cleaning: the user it creates uses the *test.local domain so cleanup-test-users.mjs removes it"
  artifacts:
    - path: "e2e/14-qa-parity.spec.ts"
      provides: "Playwright prod-parity gating test for QAOB-04"
      contains: "data-qa-badge"
  key_links:
    - from: "e2e/14-qa-parity.spec.ts"
      to: "[data-qa-badge] selector + /api/debug/* endpoints"
      via: "page.locator count assertions + page.request status assertions"
      pattern: "data-qa-badge"
---

<objective>
Prove the QAOB-04 prod-parity invariant: when `DEBUG_CHEAT_SECRET` is unset, no QA affordances reach the customer — the DOM contains no state-code badges and every QA endpoint 404s. This is the automated guarantee that all of Phase 14's QA surface is provably absent from the customer experience.

Purpose: This test is the safety net the whole milestone leans on (v2.1's study-loop bug survived behind green tests; QA observability must not become a leak). It closes the loop on Plans 01-02 by asserting their gates from the browser's perspective.

Output: `e2e/14-qa-parity.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-qa-observability-foundations/14-RESEARCH.md
@.planning/phases/14-qa-observability-foundations/14-PATTERNS.md
@.planning/phases/14-qa-observability-foundations/14-01-SUMMARY.md
@.planning/phases/14-qa-observability-foundations/14-02-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Verified from e2e/helpers.ts and playwright.config.ts. -->

From e2e/helpers.ts:
```typescript
export function testEmail(): string;  // returns qa+...@test.local  (cleanup-safe domain)
export async function signUpWithDeck(page: Page, language?: "French" | "Spanish"): Promise<{ email: string }>;
export async function addWordsFromBrowser(page: Page, count: number): Promise<void>;
```

From playwright.config.ts: `webServer: undefined` — tests run against an already-running dev server at http://localhost:3000. The harness cannot strip env vars from a running server, so the spec must self-detect whether the feature is enabled (RESEARCH pitfall 6).

Selectors/endpoints under test (from Plan 01/02):
- Badge selector: `[data-qa-badge]` (added to QaStateBadge in Plan 02)
- `GET /api/debug/state?secret=...` -> 404 when DEBUG_CHEAT_SECRET unset (Plan 01 preserves guard)
- `POST /api/debug/cheat` body `{ secret, level }` -> 404 when DEBUG_CHEAT_SECRET unset

Study navigation pattern (from study-progression.spec.ts): after addWordsFromBrowser, click "Start studying" link, waitForURL(/\/study/), waitForSelector for the reveal prompt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prod-parity gating e2e spec (QAOB-04)</name>
  <files>e2e/14-qa-parity.spec.ts</files>
  <read_first>
    - e2e/study-progression.spec.ts (the closest analog — header-comment style, test.describe/test structure, page.request usage, study navigation: "Start studying" + waitForURL(/\/study/))
    - e2e/helpers.ts (signUpWithDeck, addWordsFromBrowser, testEmail — exact signatures and the *test.local domain guarantee)
    - playwright.config.ts (webServer: undefined — tests hit a pre-running server; baseURL)
    - .planning/phases/14-qa-observability-foundations/14-02-SUMMARY.md (confirm the final [data-qa-badge] selector and which surfaces render it)
  </read_first>
  <action>
    Create e2e/14-qa-parity.spec.ts. Begin with a header comment block documenting (a) what it proves (QAOB-04: QA affordances absent when DEBUG_CHEAT_SECRET unset), (b) the PREREQUISITE that it must run against a dev server started WITHOUT the secret — `DEBUG_CHEAT_SECRET="" npx playwright test e2e/14-qa-parity.spec.ts` — and (c) that if the secret IS set the 404 assertions are skipped with a console warning (RESEARCH pitfall 6), so the spec never false-passes nor false-fails based on local env.

    Import `{ expect, test } from "playwright/test"` and `{ addWordsFromBrowser, signUpWithDeck } from "./helpers"`.

    Write `test.describe("QA prod-parity gating (QAOB-04)", () => { ... })` with a single `test(...)` that calls `test.setTimeout(120_000)`.

    Step 1 — feature-state detection: probe `const probe = await page.request.get("/api/debug/state?secret=___parity_probe___");`. Treat `probe.status() === 404` as "feature disabled" (the assertable case). If status is NOT 404 (403/401/200 — feature enabled locally), log `console.warn("DEBUG_CHEAT_SECRET appears set locally; skipping endpoint-404 assertions (see RESEARCH pitfall 6)")` and set a `featureDisabled = false` flag. Do this probe before sign-up so the request has no session (still 404 when feature off).

    Step 2 — set up a customer: `await signUpWithDeck(page, "French");` then `await addWordsFromBrowser(page, 1);` (user email is *test.local via the helper — cleanup-safe, QAOB-04 self-clean alignment).

    Step 3 — DOM assertions (ALWAYS run, regardless of featureDisabled — when the secret is unset the customer has no QA cookie, and even when set, an un-authed customer has no cookie so the badge must still be absent):
    - `await page.goto("/dashboard");` then `expect(await page.locator("[data-qa-badge]").count()).toBe(0);`
    - Navigate to study: click the "Start studying" link (mirror study-progression.spec.ts), `await page.waitForURL(/\/study/);`, wait for the reveal prompt selector, then `expect(await page.locator("[data-qa-badge]").count()).toBe(0);`

    Step 4 — endpoint assertions (run only when `featureDisabled`):
    - `const stateRes = await page.request.get("/api/debug/state?secret=anything"); expect(stateRes.status()).toBe(404);`
    - `const cheatRes = await page.request.post("/api/debug/cheat", { data: { secret: "anything", level: 1 } }); expect(cheatRes.status()).toBe(404);`
    - Wrap these in `if (featureDisabled) { ... }`.

    Do NOT add a webServer to playwright.config.ts (out of scope; tests run against the existing dev server). Do NOT use watch mode. Keep the spec a single file.
  </action>
  <verify>
    <automated>DEBUG_CHEAT_SECRET="" npx playwright test e2e/14-qa-parity.spec.ts</automated>
    <human-check>Run once with the secret SET (normal .env.local): the spec still passes — DOM badge-absence assertions pass for the un-authed customer, and the endpoint-404 block is skipped with the console warning. Run once with `DEBUG_CHEAT_SECRET=""`: all assertions including the 404 checks run and pass.</human-check>
  </verify>
  <acceptance_criteria>
    - e2e/14-qa-parity.spec.ts exists and contains `[data-qa-badge]`, `/api/debug/state`, and `/api/debug/cheat`
    - The spec asserts `page.locator("[data-qa-badge]").count()` toBe(0) on both /dashboard and /study
    - Endpoint 404 assertions are guarded by a feature-disabled detection probe (grep: `404` and a `featureDisabled`/skip-warning branch present)
    - The spec uses `signUpWithDeck`/`addWordsFromBrowser` (so the test user is on *test.local and cleanup-removable)
    - `DEBUG_CHEAT_SECRET="" npx playwright test e2e/14-qa-parity.spec.ts` exits 0
  </acceptance_criteria>
  <done>QAOB-04 gating spec passes: no [data-qa-badge] in customer DOM on dashboard + study, and QA endpoints 404 when the secret is unset; self-cleaning test user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer browser -> app | The test impersonates a customer to prove no QA surface leaks |
| test data -> prod cleanup | The test user must be removable by the standard cleanup script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Information Disclosure | QA badge/endpoint reachable by customers (the threat this test guards) | mitigate | This spec IS the mitigation verification — asserts `[data-qa-badge]` count 0 + /api/debug/* 404 when secret unset (closes T-14-03 and T-14-07 from Plans 01/02) |
| T-14-12 | Tampering | False-pass when local env has the secret set | mitigate | Feature-state probe skips endpoint-404 assertions with a console warning instead of silently passing/failing (RESEARCH pitfall 6); DOM badge-absence still asserted for the un-authed customer |
| T-14-13 | Information Disclosure | Test residue in prod data | mitigate | Test user created via signUpWithDeck uses the `*test.local` domain (testEmail) — removable by scripts/cleanup-test-users.mjs (QAOB-04 self-clean) |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this phase — no install task, no legitimacy gate needed |
</threat_model>

<verification>
- `DEBUG_CHEAT_SECRET="" npx playwright test e2e/14-qa-parity.spec.ts` — passes (full assertions including 404s)
- Same spec with secret set — passes (DOM assertions only; endpoint block skipped with warning)
- After run: `node scripts/cleanup-test-users.mjs` removes the created *test.local user (no residue)
</verification>

<success_criteria>
- Automated prod-parity gating test passes: no state codes in DOM (dashboard + study) and no QA endpoints reachable when secrets/env are unset (QAOB-04)
- Test self-cleans via *test.local user domain
- No false-pass when the local secret is set (feature-detection skip with warning)
</success_criteria>

<output>
Create `.planning/phases/14-qa-observability-foundations/14-03-SUMMARY.md` when done
</output>
