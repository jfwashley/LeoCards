---
phase: 11-review-commit
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - src/components/review-list.tsx
autonomous: true
requirements: [RVW-01, RVW-02, RVW-03, RVW-04, RVW-05]
user_setup: []

must_haves:
  truths:
    - "ReviewList renders Step A with all extracted words kept (checked) by default; duplicates segregated to a non-interactive Already learned list"
    - "User can edit any word, toggle/remove rows, select-all/select-none; Confirm disabled when none kept"
    - "On Next: kept words are DeepL-translated target→native via /api/translate fan-out; per-row failure shows inline error + manual entry, never blocks the batch"
    - "Step B has two editable fields per row mirroring TranslationForm exactly; Add N cards commits via saveImageCards then shows the success summary"
    - "Cancel at any pre-commit step calls onCancel with zero DB writes; review-list.test.ts goes GREEN; full suite green"
  artifacts:
    - path: "src/components/review-list.tsx"
      provides: "ReviewList client component + reviewListReducer + isDuplicate + fan-out/commit orchestration"
      exports: ["ReviewList", "reviewListReducer", "isDuplicate"]
      min_lines: 250
      contains: "export function ReviewList"
  key_links:
    - from: "src/components/review-list.tsx"
      to: "/api/translate"
      via: "Promise.allSettled fetch fan-out (sourceLang=targetLang, targetLang=nativeLang)"
      pattern: "fetch\\(\"/api/translate\""
    - from: "src/components/review-list.tsx"
      to: "saveImageCards / getSameLanguageDeckBackWords"
      via: "import from @/lib/deck-actions"
      pattern: "from \"@/lib/deck-actions\""
    - from: "src/components/review-list.tsx"
      to: "/dashboard?deck={deckId}"
      via: "useRouter().push on success CTA"
      pattern: "router.push\\(`/dashboard\\?deck="
---

<objective>
Wave 3: build the self-contained `ReviewList` client component — the entire
Phase 11 UI surface. Owns the 6-state `useReducer` machine (loading-dedupe →
step-a → translating → step-b → committing → success), the dedupe call, the
DeepL fan-out, the batched commit, and the success summary.

Purpose: This is the RVW-01..05 deliverable. It turns the Wave 0
`review-list.test.ts` GREEN and is the component Wave 4 wires into
`image-upload-flow.tsx`.

Output: `src/components/review-list.tsx` exporting `ReviewList`,
`reviewListReducer`, `isDuplicate`, and the fan-out/commit orchestration helpers
named in the 11-01 SUMMARY contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-review-commit/11-UI-SPEC.md
@.planning/phases/11-review-commit/11-PATTERNS.md
@.planning/phases/11-review-commit/11-RESEARCH.md
@.planning/phases/11-review-commit/11-CONTEXT.md
@.planning/phases/11-review-commit/11-01-SUMMARY.md
@.planning/phases/11-review-commit/11-02-SUMMARY.md
@src/components/translation-form.tsx
@src/components/image-upload-flow.tsx

<interfaces>
<!-- Server actions available (from Wave 2 — verified signatures): -->
import { getSameLanguageDeckBackWords, saveImageCards } from "@/lib/deck-actions";
// getSameLanguageDeckBackWords(deckId: string): Promise<Set<string>>
// saveImageCards(deckId, cardInputs: {front,back}[]): Promise<{ok:boolean;error?:string}[]>

<!-- Props this component receives from image-upload-flow.tsx (Wave 4): -->
interface ReviewListProps {
  words: string[];        // state.extractWords (non-empty)
  deckId: string;         // state.selectedDeckId
  nativeLang: string;     // threaded from new-card/page.tsx
  targetLang: string;     // deck.language
  onCancel: () => void;   // () => dispatch({ type: "BACK_TO_PICK" }) in outer flow
  nativeLangLabel: string;  // human label for the native column (Step B)
  targetLangLabel: string;  // human label for the target column (Step B)
}
<!-- nativeLangLabel/targetLangLabel: pass through from image-upload-flow in Wave 4;
     if not yet available there, derive from a lang→label map consistent with
     translation-form.tsx's existing label handling. Confirm exact prop names
     against translation-form.tsx during read_first. -->

<!-- Reducer/helper export names MUST match the 11-01-SUMMARY contract so the
     Wave 0 tests resolve. Re-read 11-01-SUMMARY.md for the exact names. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reviewListReducer + isDuplicate + dedupe load + Step A UI (RVW-01/02/05)</name>
  <read_first>
    - 11-01-SUMMARY.md (the EXACT exported symbol names the Wave 0 tests import: reviewListReducer, isDuplicate, and the orchestration helper names)
    - src/components/review-list.test.ts (the contract under test — implement to satisfy it)
    - src/components/image-upload-flow.tsx lines 44–105 (imageFlowReducer spread-and-override convention) + lines 187–249 (fetch-then-dispatch on async resolve idiom for the dedupe-on-mount)
    - src/components/translation-form.tsx lines 1–11 (imports), 109/135–143 (activeField ref → cancelled ref pattern)
    - 11-PATTERNS.md § "src/components/review-list.tsx" (lines 27–319 — imports, schema re-declare, reducer shape, props, cancel ref, in-flight button, inline error, back button, success nav)
    - 11-RESEARCH.md § "Focus Area 5" (reducer shape + action union + render branch) + Pitfall 1 (cancel-race ref), Pitfall 3 (guard empty deckId), Pitfall 5 (trim+lowercase)
    - 11-UI-SPEC.md § "Step A — Prune & Edit" + "Layout → Step A full layout" + "Copywriting Contract → Step A" + "Already Learned Section" + "Accessibility Contract" (VERBATIM copy + exact classNames + ARIA strings)
    - 11-CONTEXT.md D-01/D-02/D-03/D-04/D-05/D-14
    - node_modules/next/dist/docs/ — "use client" component + useRouter (next/navigation) guide (Next.js 16)
  </read_first>
  <behavior>
    - reviewListReducer is a pure exported function; every case returns a new
      object via spread (never mutates state); unknown action → state unchanged
    - isDuplicate(word, set): `set.has(word.trim().toLowerCase())`
    - DEDUPE_DONE partitions `words` into rows (non-dupe, kept:true) vs
      `duplicates` (isDuplicate true) and sets step "step-a"
    - DEDUPE_ERROR sets dedupeError, still renders Step A treating all as new
    - TOGGLE_WORD / EDIT_WORD / REMOVE_WORD / SELECT_ALL / SELECT_NONE mutate
      only the intended row(s) immutably
    - none-kept (no row with kept:true) → "Next: translate" disabled + hint
    - cancel at Step A → onCancel(); zero saveImageCards calls
  </behavior>
  <files>src/components/review-list.tsx</files>
  <action>
    Create `src/components/review-list.tsx` with `"use client"`. Implement and
    EXPORT (names per 11-01-SUMMARY contract — re-read it):
      - `reviewListReducer(state, action)` — pure; the action union + state
        shape from 11-PATTERNS.md lines 76–105 / 11-RESEARCH.md Focus Area 5
        (ReviewStep, ReviewRow, TranslationRow, ReviewState, ReviewAction).
        Use stable row ids (index-derived, e.g. `row-${i}`).
      - `isDuplicate(word: string, knownBackWords: Set<string>): boolean` →
        `knownBackWords.has(word.trim().toLowerCase())`.
      - `ReviewList(props: ReviewListProps)` component.

    Re-declare locally (do NOT import from translation-form.tsx — Pitfall 4):
      `const TranslationResponseSchema = z.object({ translation: z.string().min(1) });`

    On mount: `if (!deckId) return;` (Pitfall 3) then call
    `getSameLanguageDeckBackWords(deckId)` in a `useEffect`; on resolve dispatch
    `DEDUPE_DONE` with the Set; on reject dispatch `DEDUPE_ERROR`. Initial step
    is `"loading-dedupe"` — render a brief standalone Loader2 spinner
    (`<Loader2 className="size-4 animate-spin text-primary" aria-hidden="true" />`)
    while loading (per 11-RESEARCH Open Question 2 recommendation).

    Add a `cancelled` ref (`useRef(false)`); the Cancel handler sets
    `cancelled.current = true` then calls `props.onCancel()` (Pitfall 1).

    Render Step A EXACTLY per 11-UI-SPEC § "ReviewList — Step A full layout"
    and "Step A — Prune & Edit": heading "Review extracted words", sub-copy
    "Uncheck or remove words you don't want to add.", "Select all" / "Select
    none" ghost buttons, the word list (ReviewWordRow local sub-component:
    `<input type="checkbox">` + `<Input className="flex-1 h-8">` + ghost icon
    `<Button>` with `<X className="size-4" />`), the conditional "Already
    learned" section (label "Already learned", AlreadyLearnedRow:
    `line-through text-muted-foreground` + "already learned" tag — NON
    interactive, no checkbox/input/X), and the action row: primary
    `variant="default" className="w-full h-11"` "Next: translate"
    `disabled={noWordsKept}`, the hint "Keep at least one word to continue."
    when none kept, and a ghost "Cancel". Use the VERBATIM copy + ARIA strings
    from the Copywriting/Accessibility contract (e.g. checkbox
    `aria-label={`Keep "${word}"`}`, X `aria-label={`Remove "${word}"`}`).
    Decompose ReviewWordRow / AlreadyLearnedRow as local sub-components in this
    file (Claude's Discretion per 11-CONTEXT.md / 11-UI-SPEC decomposition note).
  </action>
  <verify>
    <automated>npx vitest run src/components/review-list.test.ts -t "isDuplicate" && npx vitest run src/components/review-list.test.ts -t "reviewListReducer" && npx vitest run src/components/review-list.test.ts -t "cancel" && npx tsc --noEmit</automated>
  </verify>
  <done>isDuplicate, reviewListReducer, and cancel describe blocks GREEN; tsc clean; `grep -c 'Review extracted words' src/components/review-list.tsx` == 1 and `grep -c 'Already learned' src/components/review-list.tsx` ≥ 1 (verbatim copy present); component exports ReviewList, reviewListReducer, isDuplicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translation fan-out, Step B, commit, success summary (RVW-03/04/05)</name>
  <read_first>
    - src/components/review-list.test.ts (translation fan-out + batch commit describe blocks — the contract)
    - src/components/translation-form.tsx lines 122–133 (/api/translate call shape), 177–182 (saveCard call → here replaced by saveImageCards), 214–248 (two-field grid + skeleton shimmer)
    - 11-PATTERNS.md § fan-out (145–174), saveImageCards client usage (412–473), in-flight button (193–224), skeleton shimmer (228–239), two-field grid (243–270), inline error (274–289), back button (293–307), success nav (311–317)
    - 11-RESEARCH.md § "Focus Area 3" (D-08 direction sourceLang=targetLang, targetLang=nativeLang; 30/min rate limit → 429 = per-row error not batch block) + "Focus Area 4" (batched saveImageCards)
    - 11-UI-SPEC.md § "Step A → Step B: Translating in-flight", "Step B — Translate & Edit", "Commit in-flight", "Success Summary", "Cancel (any step)", "Copywriting Contract" (Translating / Step B / Commit / Success — VERBATIM incl. pluralisation + " · " interpunct + zero-added failure copy), "Validation Error Patterns", "Accessibility Contract"
    - 11-CONTEXT.md D-06/D-07/D-08/D-09/D-12/D-13/D-14
    - node_modules/next/dist/docs/ — Promise/async client patterns + useRouter push (Next.js 16)
  </read_first>
  <behavior>
    - "Next: translate" → step "translating": Promise.allSettled over kept words
      calling fetch("/api/translate", body `{ text, sourceLang: targetLang,
      targetLang: nativeLang }`), validate with TranslationResponseSchema;
      fulfilled → TRANSLATION_ROW_DONE(nativeText); rejected/non-ok →
      TRANSLATION_ROW_ERROR with copy "Translation unavailable — enter
      manually."; one failure never rejects the batch; cancelled.current guards
      all dispatches
    - Step B: two editable Inputs per row (native + target) mirroring
      TranslationForm grid exactly; per-row inline error `role="alert"
      text-destructive`; error clears on edit of either field; native Input
      replaced by skeleton while that row is in-flight
    - "Add N cards" → step "committing": all inputs disabled, Back+Cancel
      hidden; call saveImageCards(deckId, rows.map(r => ({ front:
      r.nativeText.trim(), back: r.targetText.trim() }))); compute addedCount =
      outcomes.filter(ok).length, failedCount = !ok, skippedCount =
      duplicates.length; dispatch COMMIT_DONE → step "success"
    - Success summary VERBATIM: "{N} card(s) added to your deck.", secondary
      "{M} already learned (skipped) · {K} failed" (only present parts joined by
      " · "), CTA "Go to my deck" → router.push(`/dashboard?deck=${deckId}`);
      all-failed (N=0,K>0) → "Couldn't add cards — please try again." + "Back to
      deck" variant="outline"
    - Cancel during translating / Step B → onCancel(); zero saveImageCards calls
  </behavior>
  <files>src/components/review-list.tsx</files>
  <action>
    Extend `review-list.tsx` (same file as Task 1) with the translating, step-b,
    committing, and success render branches and the orchestration helpers named
    in the 11-01-SUMMARY contract (e.g. `runTranslationFanOut`,
    `commitReviewRows`) so the Wave 0 fan-out + batch-commit tests resolve.

    Translation fan-out — copy 11-PATTERNS.md lines 162–174 verbatim
    (target→native direction). On settle, dispatch per-row DONE/ERROR; guard
    every dispatch with `if (cancelled.current) return;` (Pitfall 1).

    Step B — copy translation-form.tsx's `grid grid-cols-1 md:grid-cols-2`
    Label+Input pattern (11-PATTERNS lines 243–270) with per-row unique ids
    `native-${i}` / `target-${i}`, `className="h-8"`, `gap-1` column wrappers,
    skeleton shimmer `<div className="bg-muted animate-pulse rounded-md h-8
    w-full" />` while a row is loading. Inline error `<p className="text-sm
    text-destructive mt-1" role="alert">Translation unavailable — enter
    manually.</p>`; clear that row's error in EDIT_NATIVE/EDIT_TARGET.

    Commit — call `saveImageCards` (the batched Wave 2 action; D-12 RECOMMENDED
    over an N-loop). Commit in-flight button per 11-PATTERNS lines 213–224
    (`disabled aria-busy="true" aria-label="Adding cards, please wait"` +
    Loader2 + "Adding cards…") and the below-button hint; hide Back & Cancel
    during commit.

    Success — exact 11-UI-SPEC § "Success Summary" / "ReviewList — Success
    layout" markup incl. optional `<CheckCircle2 className="size-8
    text-green-600" aria-hidden="true" />`, pluralised primary line, " · "-
    joined secondary, "Go to my deck" → `router.push(\`/dashboard?deck=\${deckId}\`)`
    (`useRouter` from `next/navigation`), and the all-failed variant.

    Use ALL copy strings VERBATIM from the Copywriting Contract (pluralisation
    rules included). No DB write occurs until "Add N cards" — Cancel anywhere
    before that is a pure onCancel() with zero saveImageCards calls (D-14, no
    confirm dialog).
  </action>
  <verify>
    <automated>npx vitest run src/components/review-list.test.ts && npx tsc --noEmit && npx biome check src/components/review-list.tsx && npm test</automated>
  </verify>
  <done>Entire `src/components/review-list.test.ts` GREEN (all 5 describe blocks); `npx tsc --noEmit` clean; `npx biome check src/components/review-list.tsx` clean; `npm test` (full Phases 1–10 + Phase 11 unit suites) GREEN — no remaining RED files; verbatim grep checks pass: `grep -c 'Translation unavailable — enter manually.' src/components/review-list.tsx` ≥ 1, `grep -c 'Go to my deck' src/components/review-list.tsx` == 1, `grep -c 'Adding cards…' src/components/review-list.tsx` == 1, `grep -c '/api/translate' src/components/review-list.tsx` ≥ 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReviewList (client) → /api/translate | Reuses the EXISTING rate-limited (30/min/user) DeepL route. No new endpoint. |
| ReviewList (client) → saveImageCards / getSameLanguageDeckBackWords | Server-side authz enforced in Wave 2; client passes deckId only — no client-trusted ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Denial of Service | Fan-out of up to ~50 /api/translate calls | mitigate | Existing per-user 30/min rate limiter (unchanged, no new endpoint); a 429 is handled as a per-row `translationError` (D-07) — never retried in a loop, never blocks the batch. Word cap ~50 (Phase 10 D-08). |
| T-11-08 | Information Disclosure | Logging extracted words / translations | mitigate | ReviewList performs no console/remote logging of words, translations, or image data. Errors are rendered inline only. |
| T-11-09 | Tampering | Client forging deckId in saveImageCards/getSameLanguageDeckBackWords | mitigate | Authz is server-side (Wave 2 ownership checks). This client-only component holds no authority; threat is fully mitigated at the server boundary, not here. |
| T-11-10 | (n/a) | Client review-list state machine | accept | Pure UI state; no trust boundary beyond the two server actions above. No additional threats invented per security_context scope. |
</threat_model>

<verification>
- `npx vitest run src/components/review-list.test.ts` fully GREEN (RVW-01..05 unit coverage).
- `npx tsc --noEmit` + `npx biome check src/components/review-list.tsx` clean.
- `npm test` — full prior suite (Phases 1–10) + Phase 11 units GREEN; zero RED files.
- Verbatim-copy grep checks (see Task 2 done) pass.
</verification>

<success_criteria>
- ReviewList owns the 6-state reducer; dedupe-on-mount; Step A prune/edit with Already-learned segregation; fan-out target→native translation with per-row failure resilience; Step B two-editable-field rows; batched saveImageCards commit; verbatim success summary; zero-write cancel.
- All Phase 11 unit tests green; full suite green; copy used verbatim from 11-UI-SPEC.md.
</success_criteria>

<output>
After completion, create `.planning/phases/11-review-commit/11-03-SUMMARY.md`.
Record the final `ReviewListProps` shape (the exact Wave 4 prop contract,
including the resolved nativeLangLabel/targetLangLabel prop names) and confirm
the entire unit suite is green.
</output>
