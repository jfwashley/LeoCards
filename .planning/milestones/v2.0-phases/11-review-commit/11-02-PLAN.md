---
phase: 11-review-commit
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/lib/deck-actions.ts
autonomous: true
requirements: [RVW-04, RVW-05]
user_setup: []

must_haves:
  truths:
    - "getSameLanguageDeckBackWords returns a trimmed+lowercased Set of back values from the user's same-language decks only"
    - "An attacker passing another user's deckId is rejected (Forbidden) before any data is returned"
    - "saveImageCards inserts kept cards sequentially, continues on per-card failure, revalidates once, returns per-card outcomes"
    - "deck-actions.test.ts getSameLanguageDeckBackWords block goes GREEN; prior suite stays green"
  artifacts:
    - path: "src/lib/deck-actions.ts"
      provides: "getSameLanguageDeckBackWords + saveImageCards server actions"
      exports: ["getSameLanguageDeckBackWords", "saveImageCards"]
      contains: "export async function getSameLanguageDeckBackWords"
  key_links:
    - from: "src/lib/deck-actions.ts (getSameLanguageDeckBackWords)"
      to: "decks/cards tables (ownership + same-language scope)"
      via: "auth.api.getSession + db innerJoin on decks.userId & decks.language"
      pattern: "eq\\(decks.userId, userId\\)"
    - from: "src/lib/deck-actions.ts (saveImageCards)"
      to: "cards table"
      via: "ownership check then sequential db.insert + single revalidatePath"
      pattern: "revalidatePath\\(\"/dashboard\"\\)"
---

<objective>
Wave 2: implement the two NEW server actions in `src/lib/deck-actions.ts` —
`getSameLanguageDeckBackWords` (D-04/D-05 dedupe lookup) and the batched
`saveImageCards` (D-12 commit, the RECOMMENDED approach over an N-loop client
`saveCard`).

Purpose: These are the only new server-side surface in Phase 11. Both MUST be
auth + deck-ownership scoped exactly like the existing `saveCard`. They make the
Wave 0 `deck-actions.test.ts` block GREEN and provide the client contract Wave 3
(ReviewList) consumes.

Output: two new top-level `"use server"` exports in `deck-actions.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-review-commit/11-RESEARCH.md
@.planning/phases/11-review-commit/11-PATTERNS.md
@.planning/phases/11-review-commit/11-CONTEXT.md

<interfaces>
<!-- Exact contracts Wave 3 (ReviewList) consumes. -->
export async function getSameLanguageDeckBackWords(deckId: string): Promise<Set<string>>;
export async function saveImageCards(
  deckId: string,
  cardInputs: Array<{ front: string; back: string }>,
): Promise<Array<{ ok: boolean; error?: string }>>;

<!-- Already imported at top of deck-actions.ts (verified): and, eq from drizzle-orm;
     revalidatePath from next/cache; headers from next/headers; db from @/db;
     CardId, DeckId, UserId from @/db/schema; cards, decks from @/db/schema;
     auth from @/lib/auth; crypto.randomUUID available. NO new imports needed
     except none — `and` and `eq` already present (line 3). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement getSameLanguageDeckBackWords server action (D-04/D-05)</name>
  <read_first>
    - src/lib/deck-actions.ts lines 1–9 (imports — and/eq/db/decks/cards/auth/headers/UserId/DeckId all already present) and lines 65–120 (saveCard auth+ownership pattern; editCard innerJoin pattern)
    - 11-PATTERNS.md § "getSameLanguageDeckBackWords (server action, CRUD read)" (lines 321–408 — full verified implementation, auth+ownership, innerJoin)
    - 11-RESEARCH.md § "Focus Area 2" + Pitfall 2 (MUST be a server action in deck-actions.ts, NOT deck-queries.ts) + Pitfall 5 (trim+lowercase BOTH sides) + Pitfall 7 (top-level named export)
    - 11-CONTEXT.md D-04 (dedupe before translation), D-05 (match key: back field, same-language decks only, case-insensitive+trimmed)
    - node_modules/next/dist/docs/ — the "use server" / server actions guide (this is Next.js 16; confirm server-action export semantics — file already has "use server" at top per existing saveCard; do NOT add a duplicate directive)
  </read_first>
  <behavior>
    - no session → throws Error("Unauthorized")
    - deck not owned by caller (language+ownership lookup returns no row) → throws Error("Forbidden")
    - happy path: returns `new Set` of every same-language deck's card `back`
      values, each `.trim().toLowerCase()` (e.g. DB `"Chien"`, `"  chat  "`
      under a `fr` deck → `Set(["chien","chat"])`)
    - only decks where `decks.userId == caller` AND `decks.language ==` target
      deck language are scanned (cross-language and cross-user excluded)
  </behavior>
  <files>src/lib/deck-actions.ts</files>
  <action>
    Add a new top-level exported async function `getSameLanguageDeckBackWords`
    to `src/lib/deck-actions.ts` (this file already begins with the `"use
    server"` directive — do NOT add another; place the function near the other
    deck/card actions). Implement EXACTLY per 11-PATTERNS.md lines 377–405:

      export async function getSameLanguageDeckBackWords(
        deckId: string,
      ): Promise<Set<string>> {
        const session = await auth.api.getSession({ headers: await headers() });
        if (!session) throw new Error("Unauthorized");
        const userId = session.user.id as UserId;

        const [targetDeck] = await db
          .select({ language: decks.language })
          .from(decks)
          .where(and(eq(decks.id, deckId as DeckId), eq(decks.userId, userId)));
        if (!targetDeck) throw new Error("Forbidden");

        const rows = await db
          .select({ back: cards.back })
          .from(cards)
          .innerJoin(decks, eq(cards.deckId, decks.id))
          .where(
            and(
              eq(decks.userId, userId),
              eq(decks.language, targetDeck.language),
            ),
          );

        return new Set(rows.map((r) => r.back.trim().toLowerCase()));
      }

    `and` and `eq` are already imported (deck-actions.ts line 3) — add no new
    imports. The combined ownership+language `where` in query 1 is the authz
    gate (T-11-03): a foreign deckId yields no row → "Forbidden" before any
    card data is read.
  </action>
  <verify>
    <automated>npx vitest run src/lib/deck-actions.test.ts -t "getSameLanguageDeckBackWords" && npx tsc --noEmit</automated>
  </verify>
  <done>`npx vitest run src/lib/deck-actions.test.ts -t "getSameLanguageDeckBackWords"` is GREEN (all 3 cases); tsc clean; function is a top-level named export; no import added; no edit to deck-queries.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement saveImageCards batched commit server action (D-12)</name>
  <read_first>
    - src/lib/deck-actions.ts lines 65–90 (saveCard: auth → ownership → insert → revalidatePath — the structural analog)
    - 11-PATTERNS.md § "saveImageCards (server action, batch insert)" (lines 412–456 — full verified implementation; revalidatePath ONCE after the loop)
    - 11-RESEARCH.md § "Focus Area 4: Batch Commit (D-12)" (RECOMMENDED batched action; Neon HTTP = no transactions = continue-on-failure, no rollback)
    - 11-CONTEXT.md D-12 (sequential continue-on-failure, collect per-word outcomes), D-13 (counts feed the success summary)
    - node_modules/next/dist/docs/ — server-action + revalidatePath guide (Next.js 16; revalidatePath called once, server-side only, inside this "use server" function)
  </read_first>
  <behavior>
    - no session → throws Error("Unauthorized")
    - deck not owned → throws Error("Forbidden") (single ownership check, before any insert)
    - inserts each `{front, back}` as a card row with `source: "image"`,
      sequentially; a thrown insert does NOT abort the loop — that index gets
      `{ ok: false, error }`, the rest continue
    - returns an outcomes array aligned by index with `cardInputs`
    - `revalidatePath("/dashboard")` called exactly ONCE, after the loop
  </behavior>
  <files>src/lib/deck-actions.ts</files>
  <action>
    Add a second new top-level exported async function `saveImageCards` to
    `src/lib/deck-actions.ts`, implemented EXACTLY per 11-PATTERNS.md lines
    423–455:

      export async function saveImageCards(
        deckId: string,
        cardInputs: Array<{ front: string; back: string }>,
      ): Promise<Array<{ ok: boolean; error?: string }>> {
        const session = await auth.api.getSession({ headers: await headers() });
        if (!session) throw new Error("Unauthorized");
        const userId = session.user.id as UserId;

        const deckRows = await db
          .select()
          .from(decks)
          .where(eq(decks.id, deckId as DeckId));
        const deck = deckRows[0];
        if (!deck || deck.userId !== userId) throw new Error("Forbidden");

        const outcomes: Array<{ ok: boolean; error?: string }> = [];
        for (const input of cardInputs) {
          try {
            const id = crypto.randomUUID() as CardId;
            await db
              .insert(cards)
              .values({ id, deckId: deckId as DeckId, front: input.front, back: input.back, source: "image" });
            outcomes.push({ ok: true });
          } catch (err) {
            outcomes.push({ ok: false, error: err instanceof Error ? err.message : "Unknown error" });
          }
        }

        revalidatePath("/dashboard");
        return outcomes;
      }

    Continue-on-failure is mandatory (D-12 / Neon HTTP has no transactions — no
    rollback). `revalidatePath` is OUTSIDE and AFTER the loop (one call total).
    Source is the literal `"image"` (valid since Wave 1 widened the union).
  </action>
  <verify>
    <automated>npx vitest run src/lib/deck-actions.test.ts && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>`saveImageCards` is a top-level named export; tsc clean; `npx vitest run src/lib/deck-actions.test.ts` fully GREEN; `npm test` (full Phases 1–10 suite) GREEN; `review-list.test.ts` remains the only RED file (still awaiting Wave 3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component (ReviewList) → server action | Untrusted `deckId` and `cardInputs` cross from the browser into `getSameLanguageDeckBackWords` / `saveImageCards`. This is the real Phase 11 server surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | `getSameLanguageDeckBackWords` reading another user's vocabulary | mitigate | First query combines `eq(decks.id, deckId)` AND `eq(decks.userId, userId)`; a foreign/forged deckId returns no row → throws "Forbidden" before any `cards.back` is read. Second query is itself scoped by `eq(decks.userId, userId)`. |
| T-11-04 | Tampering / Elevation | `saveImageCards` writing cards into a deck the caller doesn't own | mitigate | Single ownership check (`deck.userId !== userId → "Forbidden"`) before the insert loop — identical to existing `saveCard`. No deckId is trusted from the client without this check. |
| T-11-05 | Denial of Service | Large `cardInputs` array driving many sequential inserts | accept | Extraction is capped at ~50 words upstream (Phase 10 D-08); inserts are sequential and bounded. No new endpoint introduced; no rate-limit change. Accepted as low-value, bounded. |
| T-11-06 | Information Disclosure | Logging extracted words / image bytes | mitigate | These actions perform NO logging of `cardInputs`, `back` values, or any image data. Errors return generic `err.message` only; no PII/word content emitted to logs. |
</threat_model>

<verification>
- `npx vitest run src/lib/deck-actions.test.ts` fully GREEN (saveCard + new getSameLanguageDeckBackWords block + any saveImageCards coverage).
- `npx tsc --noEmit` clean.
- `npm test` — full Phases 1–10 regression GREEN; only `review-list.test.ts` RED (Wave 3 pending).
- Manual grep: both functions are top-level `export async function` (server-action import requirement, Pitfall 7); `revalidatePath` appears exactly once inside `saveImageCards`.
</verification>

<success_criteria>
- getSameLanguageDeckBackWords: auth + combined ownership/language gate, trimmed+lowercased Set, same-language+same-user scope only.
- saveImageCards: one ownership check, sequential continue-on-failure inserts with source "image", single revalidatePath, per-card outcomes returned.
- deck-actions.test.ts fully green; prior suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-review-commit/11-02-SUMMARY.md`.
Record the final exact signatures of both actions (the Wave 3 import contract)
and confirm review-list.test.ts is now the only RED file.
</output>
