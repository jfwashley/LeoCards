---
phase: 11-review-commit
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - src/components/image-upload-flow.tsx
autonomous: false
requirements: [RVW-01, RVW-02, RVW-03, RVW-04, RVW-05]
user_setup: []

must_haves:
  truths:
    - "image-upload-flow.tsx renders <ReviewList> on EXTRACT_SUCCESS in place of the Phase 10 disabled stub"
    - "onCancel from ReviewList dispatches BACK_TO_PICK in the outer reducer (zero DB writes)"
    - "Full prior suite (Phases 1–10) + Phase 11 unit suite stays green; tsc + biome clean"
    - "Human UAT confirms the end-to-end Step A → translate → Step B → commit → summary flow, the Already-learned section, and zero-write cancel"
  artifacts:
    - path: "src/components/image-upload-flow.tsx"
      provides: "EXTRACT_SUCCESS branch renders ReviewList"
      contains: "<ReviewList"
  key_links:
    - from: "src/components/image-upload-flow.tsx (EXTRACT_SUCCESS branch)"
      to: "src/components/review-list.tsx"
      via: "import + render with words/deckId/nativeLang/targetLang/onCancel props"
      pattern: "import \\{ ReviewList \\} from \"@/components/review-list\""
---

<objective>
Wave 4: replace the Phase 10 EXTRACT_SUCCESS stub in `image-upload-flow.tsx`
with the real `<ReviewList>` render, completing the v2.0 image-to-flashcards
pipeline; then a human UAT checkpoint covers the browser-only / live-DB / live-
DeepL behaviors that unit tests cannot.

Purpose: This is the final wiring + the explicit manual verification gate (per
11-VALIDATION.md Manual-Only Verifications). The full-flow, Already-learned-
segregation, and zero-write-cancel behaviors require a real extraction + Neon +
DeepL and MUST NOT be auto-claimed.

Output: a one-branch edit to `image-upload-flow.tsx` + a completed human UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-review-commit/11-UI-SPEC.md
@.planning/phases/11-review-commit/11-PATTERNS.md
@.planning/phases/11-review-commit/11-VALIDATION.md
@.planning/phases/11-review-commit/11-CONTEXT.md
@.planning/phases/11-review-commit/11-03-SUMMARY.md
@src/components/image-upload-flow.tsx

<interfaces>
<!-- From Wave 3 (11-03-SUMMARY.md has the final exact ReviewListProps): -->
import { ReviewList } from "@/components/review-list";
// ReviewList props: words, deckId, nativeLang, targetLang, onCancel,
//                    nativeLangLabel, targetLangLabel (confirm exact set in 11-03-SUMMARY)

<!-- Props available in image-upload-flow.tsx scope (verified):
     decks (ImageUploadFlowProps.decks, line ~15),
     state.selectedDeckId (reducer state, line ~25),
     nativeLang (ImageUploadFlowProps.nativeLang, line ~18),
     dispatch (useReducer). The current stub to replace is at lines ~380-400. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace EXTRACT_SUCCESS stub with ReviewList render</name>
  <read_first>
    - 11-03-SUMMARY.md (the EXACT final ReviewListProps shape — use it; resolve nativeLangLabel/targetLangLabel source)
    - src/components/image-upload-flow.tsx lines 1–30 (imports + ImageUploadFlowProps: decks, nativeLang) and lines ~380–400 (the current Phase 10 disabled stub to replace)
    - 11-PATTERNS.md § "image-upload-flow.tsx — replace EXTRACT_SUCCESS stub" (lines 493–539 — exact replacement, props in scope, import to add)
    - 11-RESEARCH.md § "Focus Area 5" render branch + Pitfall 3 (selectedDeckId guaranteed non-empty at EXTRACT_SUCCESS but guard anyway)
    - 11-UI-SPEC.md § "Entry Point: EXTRACT_SUCCESS hand-off" (recap thumbnail / DeckSwitcher NOT shown in review; onCancel → BACK_TO_PICK)
    - 11-CONTEXT.md D-01 (separate component, same page, props piped in), D-14 (cancel = BACK_TO_PICK, zero writes)
    - node_modules/next/dist/docs/ — only if any Next.js 16 client-component import nuance arises (this is a client-component edit, no server action added here)
  </read_first>
  <files>src/components/image-upload-flow.tsx</files>
  <action>
    Add `import { ReviewList } from "@/components/review-list";` to the import
    block. Replace the current EXTRACT_SUCCESS stub (the
    `if (Array.isArray(state.extractWords) && state.extractWords.length > 0)`
    block rendering the disabled "Review words →" button, lines ~380–400) with
    the ReviewList render per 11-PATTERNS.md lines 519–530:

      if (Array.isArray(state.extractWords) && state.extractWords.length > 0) {
        const deck = decks.find((d) => d.id === state.selectedDeckId);
        return (
          <ReviewList
            words={state.extractWords}
            deckId={state.selectedDeckId}
            nativeLang={nativeLang}
            targetLang={deck?.language ?? "fr"}
            onCancel={() => dispatch({ type: "BACK_TO_PICK" })}
            /* + nativeLangLabel / targetLangLabel per 11-03-SUMMARY contract */
          />
        );
      }

    Pass `nativeLangLabel` / `targetLangLabel` exactly as the 11-03-SUMMARY
    contract specifies (thread through from props or derive via the same
    lang→label mapping translation-form.tsx uses — match Wave 3's expectation
    precisely so types align). Do NOT render the recap thumbnail / DeckSwitcher
    in this branch (the deck is already confirmed — 11-UI-SPEC entry point).
    Remove any now-unused imports left by deleting the stub (e.g. if the stub's
    `<img>`/`<Button>` usage becomes dead) — but only if genuinely unused
    elsewhere in the file; verify with biome.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx biome check src/components/image-upload-flow.tsx && npm test</automated>
  </verify>
  <done>`grep -c '<ReviewList' src/components/image-upload-flow.tsx` == 1; `grep -c 'Review words →' src/components/image-upload-flow.tsx` == 0 (stub removed); `import { ReviewList }` present; `npx tsc --noEmit` + `npx biome check src/components/image-upload-flow.tsx` clean; `npm test` (full Phases 1–10 + Phase 11 unit suite) GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete Phase 11 review/commit experience is now wired: a real
    extraction on /deck/new-card renders ReviewList (Step A prune/edit → Next →
    DeepL translate fan-out → Step B two-editable-field rows → Add N cards →
    success summary), with same-language duplicate segregation into the
    "Already learned" section and a zero-write Cancel at any step.
  </what-built>
  <how-to-verify>
    Pre-req: app running locally with live Neon + DeepL + ANTHROPIC_API_KEY
    configured (same env as Phase 10 manual UAT).

    1. Seed a duplicate: on /deck/new-card pick (or create) a French deck and
       manually add a card whose target word you can reproduce in an image
       (e.g. back = "chien").
    2. Go to /deck/new-card → image mode → upload an image containing several
       French words including "chien". Trigger extraction.
    3. EXPECT: ReviewList Step A — heading "Review extracted words", all
       extracted words listed and CHECKED by default; "chien" appears in the
       bottom "Already learned" section (struck-through, "already learned"
       tag, NOT checkable/editable/removable).
    4. Edit one word's text; uncheck one; click the X to remove one; use
       "Select none" then "Select all". With none kept, confirm "Next:
       translate" is disabled and the hint "Keep at least one word to
       continue." shows.
    5. Keep ≥1 word → "Next: translate". EXPECT a standalone spinner
       "Translating N word(s)…", then Step B "Check translations" with two
       editable fields per row (native + target), DeepL-filled native side.
       If any row failed translation, EXPECT "Translation unavailable — enter
       manually." inline and an empty native field you can type into.
    6. Edit a translation. Click "Add N cards". EXPECT in-flight "Adding
       cards…" (Back/Cancel hidden), then success summary "N cards added to
       your deck." with the "M already learned (skipped) · K failed" secondary
       line if applicable, and "Go to my deck".
    7. Click "Go to my deck" → EXPECT /dashboard?deck=… showing the new cards
       (and the previously-seeded "chien" NOT duplicated — it was skipped).
    8. Cancel test: restart the flow, reach Step A and then Step B, click
       "Cancel" each time → EXPECT immediate return to the add-card start with
       NO new cards created (verify the deck card count is unchanged).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any deviation from the expected behavior above.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| image-upload-flow.tsx → ReviewList | Pure client prop hand-off (words/deckId/nativeLang/targetLang/onCancel). No new server boundary introduced in this wave. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | Client passing selectedDeckId into ReviewList → server actions | accept | All authz remains server-side (Wave 2 ownership checks on getSameLanguageDeckBackWords / saveImageCards). This wave only forwards the deckId already used by the existing flow; no new privilege path. |
| T-11-12 | (n/a) | EXTRACT_SUCCESS render branch | accept | No logging of extracted words/image bytes added; this is a render-branch swap only. No new threats per security_context scope. |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npx biome check` clean.
- `npm test` — full Phases 1–10 + Phase 11 unit suite GREEN.
- Human UAT checkpoint completed and "approved" (Step A/B flow, Already-learned
  segregation, zero-write cancel — the manual-only verifications from
  11-VALIDATION.md).
</verification>

<success_criteria>
- EXTRACT_SUCCESS branch renders <ReviewList>; the Phase 10 stub is gone.
- onCancel wired to BACK_TO_PICK (zero DB writes).
- Full suite + typecheck + lint clean.
- Human UAT confirms the end-to-end RVW-01..05 behavior on a live environment.
</success_criteria>

<output>
After completion, create `.planning/phases/11-review-commit/11-04-SUMMARY.md`
recording the wiring, the UAT outcome (verbatim approval / any noted
deviations), and that Phase 11 (v2.0 final phase) is functionally complete.
</output>
