---
phase: 10-vision-extraction-endpoint
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/env.ts
  - src/lib/image-constants.ts
  - src/lib/image-constants.test.ts
  - src/lib/image-validation.ts
  - src/app/api/extract/__tests__/extract.unit.test.ts
  - src/app/api/extract/__tests__/extract-reducer.test.ts
  - src/app/api/extract/__tests__/extract-eval.test.ts
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03, EXT-04, EXT-05]
must_haves:
  truths:
    - "Phase 9's 8 image-validation tests still pass after the D-12 constants refactor (regression gate)"
    - "ALLOWED_IMAGE_TYPES and MAX_IMAGE_BYTES are exported from a single shared module consumed by both client validation and the server route"
    - "ai@6.0.185 and @ai-sdk/anthropic@3.0.78 are installed; ANTHROPIC_API_KEY is in the typed env module as optional"
    - "Test scaffolds for the route guard sequence, the client reducer, and the gated eval harness exist and run (red until their subjects ship)"
  artifacts:
    - path: "src/lib/image-constants.ts"
      provides: "ALLOWED_IMAGE_TYPES + MAX_IMAGE_BYTES single source of truth (D-12)"
      contains: "export const ALLOWED_IMAGE_TYPES"
    - path: "src/lib/image-constants.test.ts"
      provides: "Constant value/membership assertions"
    - path: "src/app/api/extract/__tests__/extract.unit.test.ts"
      provides: "Route guard-sequence unit test (mocked SDK/auth/limiter), red until 10-02 ships"
    - path: "src/app/api/extract/__tests__/extract-reducer.test.ts"
      provides: "Reducer action unit test, red until 10-03 ships"
    - path: "src/app/api/extract/__tests__/extract-eval.test.ts"
      provides: "RUN_EXTRACTION_EVALS-gated eval skeleton"
    - path: "src/env.ts"
      provides: "ANTHROPIC_API_KEY optional env (D-03)"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "src/lib/image-validation.ts"
      to: "src/lib/image-constants.ts"
      via: "import { ALLOWED_IMAGE_TYPES, MAX_IMAGE_BYTES }"
      pattern: "from \"@/lib/image-constants\""
---

<objective>
Wave 0 foundations for the vision extraction endpoint. This plan installs the AI SDK,
adds the `ANTHROPIC_API_KEY` env binding, performs the D-12 shared-constants refactor
(WITHOUT changing Phase 9 behavior — the existing 8 `image-validation.test.ts` tests are
a regression gate), and creates the four test scaffolds (constants test, route unit test,
reducer unit test, gated eval skeleton) that downstream plans 10-02 and 10-03 turn green.

Purpose: Per 10-VALIDATION.md, the test files MUST exist before the route/client consume
them (Nyquist). The shared constants module is the structural keystone consumed by both
the client validator and the server route.
Output: Installed deps, env binding, shared constants module + test, refactored validator,
3 test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
- ./AGENTS.md — "this is NOT the Next.js you know"
- .planning/phases/10-vision-extraction-endpoint/10-RESEARCH.md — Sections 1 (constants refactor), 6 (env wiring), "Validation Architecture" / "Wave 0 Gaps"
- .planning/phases/10-vision-extraction-endpoint/10-PATTERNS.md — sections: `image-constants.ts`, `image-constants.test.ts`, `image-validation.ts (edit)`, `src/env.ts (edit)`, `extract.unit.test.ts`, `extract-reducer.test.ts`, `extract-eval.test.ts`
- .planning/phases/10-vision-extraction-endpoint/10-AI-SPEC.md — Section 3 (route handler shape the unit test will assert against), Section 5 (eval gating + reference dataset)
- .planning/phases/10-vision-extraction-endpoint/10-VALIDATION.md — Wave 0 Requirements + regression gate
- src/lib/image-validation.ts (the file being refactored)
- src/lib/image-validation.test.ts (the 8-test regression gate — read to confirm no test imports the constants)
- src/lib/deck-actions.test.ts (the vi.hoisted + vi.mock analog for the route unit test)
- src/env.ts (the DEEPL_API_KEY pattern to mirror)
</read_first>

<interfaces>
From src/lib/image-validation.ts (current — to be refactored):
```typescript
const ALLOWED_TYPES = new Set(["image/jpeg", "image/png", "image/webp"]);
const MAX_BYTES = 5 * 1024 * 1024; // 5,242,880 bytes
export type ValidationResult = { ok: true } | { ok: false; message: string };
export function validateImageFile(file: File): ValidationResult
```

From src/env.ts (DEEPL_API_KEY pattern to mirror, server block + runtimeEnv block):
```typescript
DEEPL_API_KEY: z.string().min(1).optional(),        // server block
DEEPL_API_KEY: process.env.DEEPL_API_KEY,           // runtimeEnv block
```

Route contract the unit test asserts against (from 10-AI-SPEC.md Section 3):
`POST(request: Request)` → 401 unauth · 429 rate-limited (+Retry-After) · 400 bad JSON/schema ·
413 oversized · 415 bad MIME/magic-bytes · 503 no key · 200 `{ words, detectedLanguage? }` ·
200 `{ words: [] }` no-words · 504 AbortError · 502 generic vision error.

Reducer contract the reducer test asserts against (from 10-PATTERNS.md / 10-RESEARCH.md Section 4):
new state fields `extracting: boolean`, `extractError: { status, message } | null`,
`extractWords: string[] | null`; actions `EXTRACT_START`, `EXTRACT_SUCCESS`,
`EXTRACT_NO_WORDS`, `EXTRACT_ERROR`, `EXTRACT_RETRY`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AI SDK deps + add ANTHROPIC_API_KEY env binding</name>
  <files>package.json, package-lock.json, src/env.ts</files>
  <read_first>
  - .planning/phases/10-vision-extraction-endpoint/10-RESEARCH.md Section 6 (env wiring) + "Standard Stack"
  - .planning/phases/10-vision-extraction-endpoint/10-PATTERNS.md section `src/env.ts (edit)`
  - src/env.ts (full file — mirror DEEPL_API_KEY exactly)
  </read_first>
  <action>
  1. Install the exact pinned versions (D-04): run `npm install ai@6.0.185 @ai-sdk/anthropic@3.0.78`. Do NOT use caret/latest — the AI-SPEC pinned these versions verified against the npm registry 2026-05-19.
  2. Edit `src/env.ts`. In the `server:` block, add immediately AFTER the `DEEPL_API_KEY: z.string().min(1).optional(),` line:
     `ANTHROPIC_API_KEY: z.string().min(1).optional(),`
  3. In the `runtimeEnv:` block, add immediately AFTER the `DEEPL_API_KEY: process.env.DEEPL_API_KEY,` line:
     `ANTHROPIC_API_KEY: process.env.ANTHROPIC_API_KEY,`
  4. CRITICAL (D-03 / 10-RESEARCH.md Section 6): the field MUST be `.optional()`. A required field breaks `npm run dev` when no key is set (env.ts is side-effect-imported in layout.tsx and Zod-validates at startup). The 503 guard in the route is the runtime gate, not the schema.
  Do NOT add ANTHROPIC_API_KEY to the `client:` block (server-only secret).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(p.dependencies.ai!=='6.0.185')throw new Error('ai version: '+p.dependencies.ai);if(p.dependencies['@ai-sdk/anthropic']!=='3.0.78')throw new Error('anthropic version: '+p.dependencies['@ai-sdk/anthropic']);console.log('deps OK')"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "ANTHROPIC_API_KEY" src/env.ts` returns `2` (server block + runtimeEnv block)
  - `grep "ANTHROPIC_API_KEY: z.string().min(1).optional()" src/env.ts` matches exactly one line
  - `package.json` dependencies contains `"ai": "6.0.185"` and `"@ai-sdk/anthropic": "3.0.78"` (exact, no `^`)
  - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Both deps installed at pinned versions; ANTHROPIC_API_KEY present in both env.ts blocks as optional; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared image-constants module + test, refactor image-validation (D-12)</name>
  <files>src/lib/image-constants.ts, src/lib/image-constants.test.ts, src/lib/image-validation.ts</files>
  <read_first>
  - .planning/phases/10-vision-extraction-endpoint/10-PATTERNS.md sections `image-constants.ts`, `image-constants.test.ts`, `image-validation.ts (edit)`
  - .planning/phases/10-vision-extraction-endpoint/10-RESEARCH.md Section 1 (safe refactor approach + "Why this is safe for existing tests")
  - src/lib/image-validation.ts (the file being refactored — read fully)
  - src/lib/image-validation.test.ts (regression gate — confirm no test imports the constants, only validateImageFile)
  </read_first>
  <behavior>
  - image-constants.test.ts: ALLOWED_IMAGE_TYPES.has("image/jpeg"|"image/png"|"image/webp") === true
  - image-constants.test.ts: ALLOWED_IMAGE_TYPES.has("image/gif"|"image/heic") === false
  - image-constants.test.ts: MAX_IMAGE_BYTES === 5 * 1024 * 1024 (5242880)
  - REGRESSION: all 8 existing src/lib/image-validation.test.ts tests stay green unchanged
  </behavior>
  <action>
  1. Create `src/lib/image-constants.ts` with EXACTLY (no imports, no logic):
     ```typescript
     export const ALLOWED_IMAGE_TYPES = new Set(["image/jpeg", "image/png", "image/webp"]);
     export const MAX_IMAGE_BYTES = 5 * 1024 * 1024; // 5,242,880 bytes
     ```
  2. Create `src/lib/image-constants.test.ts` mirroring the 10-PATTERNS.md `image-constants.test.ts` section verbatim: import `{ describe, expect, it } from "vitest"` and `{ ALLOWED_IMAGE_TYPES, MAX_IMAGE_BYTES } from "@/lib/image-constants"`; three `it` blocks per the <behavior> above. No vi.mock / vi.hoisted.
  3. Refactor `src/lib/image-validation.ts` as a PURE re-export swap (no behavior change):
     - Replace lines 1–2 (`const ALLOWED_TYPES = ...` and `const MAX_BYTES = ...`) with a single import:
       `import { ALLOWED_IMAGE_TYPES, MAX_IMAGE_BYTES } from "@/lib/image-constants";`
     - Inside `validateImageFile`: `ALLOWED_TYPES.has(file.type)` → `ALLOWED_IMAGE_TYPES.has(file.type)`; `file.size > MAX_BYTES` → `file.size > MAX_IMAGE_BYTES`.
     - Do NOT touch the error message strings, the `ValidationResult` type, or the function signature. The 8 existing tests assert on `{ ok }` and message substrings only.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-constants.test.ts src/lib/image-validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - `npx vitest run src/lib/image-validation.test.ts` reports 8 passed, 0 failed (regression gate — D-12)
  - `npx vitest run src/lib/image-constants.test.ts` reports 3 passed
  - `grep "from \"@/lib/image-constants\"" src/lib/image-validation.ts` matches one line
  - `grep -c "const ALLOWED_TYPES\|const MAX_BYTES" src/lib/image-validation.ts` returns `0` (old privates removed)
  - `npm run typecheck` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>Shared constants module + test exist; image-validation.ts imports from it with zero behavior change; Phase 9's 8 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Create route/reducer/eval test scaffolds (Nyquist Wave 0)</name>
  <files>src/app/api/extract/__tests__/extract.unit.test.ts, src/app/api/extract/__tests__/extract-reducer.test.ts, src/app/api/extract/__tests__/extract-eval.test.ts</files>
  <read_first>
  - .planning/phases/10-vision-extraction-endpoint/10-PATTERNS.md sections `extract.unit.test.ts`, `extract-reducer.test.ts`, `extract-eval.test.ts` (vi.hoisted/vi.mock structure, request factory, test name list)
  - src/lib/deck-actions.test.ts (the authoritative vi.hoisted + vi.mock + beforeEach(clearAllMocks) analog)
  - .planning/phases/10-vision-extraction-endpoint/10-AI-SPEC.md Section 3 (exact status codes + body shapes the unit test asserts) and Section 5 (RUN_EXTRACTION_EVALS gating)
  - .planning/phases/10-vision-extraction-endpoint/10-RESEARCH.md Section 4 (reducer action/state contract) and Section 5 (eval gating pattern)
  </read_first>
  <action>
  Create three Vitest files. These import subjects that do not exist yet (route in 10-02, reducer export in 10-03); they will be RED until those plans ship — that is the intended Nyquist scaffold. Use `vi.hoisted()` BEFORE `vi.mock` factories and `beforeEach(() => vi.clearAllMocks())` per the deck-actions.test.ts analog.

  1. `extract.unit.test.ts` — mirror 10-PATTERNS.md `extract.unit.test.ts` section verbatim:
     - `vi.hoisted` → `mockGetSession`, `mockLimiterCheck`, `mockGenerateText`.
     - `vi.mock` (after hoisted, before subject import): `next/headers` (headers → resolved `new Headers()`), `@/lib/auth` (`auth.api.getSession = mockGetSession`), `@/lib/rate-limit` (`createRateLimiter` → `{ check: mockLimiterCheck }`), `ai` (`generateText: mockGenerateText`, `Output: { object: vi.fn().mockReturnValue({}) }`, `NoObjectGeneratedError: { isInstance: vi.fn().mockReturnValue(false) }`), `@ai-sdk/anthropic` (`anthropic: vi.fn().mockReturnValue({})`), `@/env` (`env: { ANTHROPIC_API_KEY: "test-key" }`).
     - `import { POST } from "@/app/api/extract/route";` after all mocks.
     - `makeRequest(body)` factory returning `new Request("http://localhost/api/extract", { method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify(body) })`. Add a `makeRequestRaw(text)` variant that sends invalid JSON for the 400 case.
     - Helpers `mockSession`/`mockNoSession`/`mockAllowed`/`mockRateLimited` per the analog.
     - `describe("POST /api/extract")` with these `it` cases (one per status — assert `res.status` and parsed body): 401 unauthenticated, 429 rate-limited (also assert `res.headers.get("Retry-After")` is set), 400 invalid JSON, 400 missing required fields, 413 oversized payload (oversized base64 string), 415 disallowed MIME, 415 bad magic bytes, 503 when env key absent (use `vi.doMock`/re-mock of `@/env` with `ANTHROPIC_API_KEY: undefined` in a dedicated test, or assert via a per-test override — follow deck-actions.test.ts re-wiring approach), 200 `{ words }` valid (mockGenerateText resolves `{ output: { words: ["chien"] } }`), 200 `{ words: [] }` no-words, 504 on AbortError (mockGenerateText rejects an Error with `name: "AbortError"`), 502 on generic vision error.
     Build valid-image fixtures inline: a minimal valid JPEG data-URL is `"data:image/jpeg;base64," + btoa(String.fromCharCode(0xff,0xd8,0xff,0xe0,0,0,0,0))`. Bad-magic-byte case: declared `image/png` but bytes start `0xff,0xd8`.
  2. `extract-reducer.test.ts` — mirror 10-PATTERNS.md `extract-reducer.test.ts` section. No vi.mock. `import { imageFlowReducer } from "@/components/image-upload-flow";` (10-03 will export it). `baseState` with `step:"deck"`, a mock `File`, `previewUrl:"blob:x"`, `pickError:null`, `selectedDeckId:"deck-1"`, `extracting:false`, `extractError:null`, `extractWords:null`. Cases: EXTRACT_START sets `extracting:true`/clears error+words; EXTRACT_SUCCESS sets `extractWords` + `extracting:false`; EXTRACT_NO_WORDS sets `extractWords:[]` (assert `Array.isArray` AND length 0, NOT null); EXTRACT_ERROR sets `extractError:{status,message}`; EXTRACT_ERROR preserves `file`/`previewUrl`/`selectedDeckId` (explicit EXT-04/D-16 assertion — `toBe` identity on file); EXTRACT_RETRY identical to EXTRACT_START.
  3. `extract-eval.test.ts` — mirror 10-PATTERNS.md `extract-eval.test.ts` section: `const RUN_EVALS = process.env.RUN_EXTRACTION_EVALS === "true";` then `describe.skipIf(!RUN_EVALS)("Extraction eval — reference dataset", () => { ... })`. Inside, add a single placeholder `it` that, when fixtures are absent, asserts the fixtures directory contract is documented (e.g. reads `reference-labels.json` if present, otherwise `it.skip`). No vi.mock — eval hits the real API. It MUST be a no-op skip when `RUN_EXTRACTION_EVALS` is unset so `npm test` stays green.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/extract/__tests__/extract-eval.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - `npx vitest run src/app/api/extract/__tests__/extract-eval.test.ts` exits 0 with the suite skipped (RUN_EXTRACTION_EVALS unset) — does NOT fail the run
  - `grep -c "vi.hoisted" src/app/api/extract/__tests__/extract.unit.test.ts` returns `1`
  - `grep "describe.skipIf(!RUN_EVALS)" src/app/api/extract/__tests__/extract-eval.test.ts` matches
  - `grep -c "it(" src/app/api/extract/__tests__/extract.unit.test.ts` returns ≥ 12 (one per guard/status)
  - `grep "file.*previewUrl.*selectedDeckId\|preserves" src/app/api/extract/__tests__/extract-reducer.test.ts` matches (EXT-04 preservation assertion present)
  - extract.unit.test.ts and extract-reducer.test.ts are RED (cannot resolve their not-yet-existing subjects) — expected; do not "fix" by stubbing the subjects here
  </acceptance_criteria>
  <done>Three scaffolds exist; eval skeleton is green-when-skipped; route+reducer scaffolds are intentionally red awaiting 10-02/10-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/deps | New third-party packages (`ai`, `@ai-sdk/anthropic`) enter the supply chain |
| env config | `ANTHROPIC_API_KEY` secret introduced into the typed env surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | dependency install | mitigate | Exact pinned versions (`ai@6.0.185`, `@ai-sdk/anthropic@3.0.78`) verified against npm registry in 10-AI-SPEC.md; lockfile committed |
| T-10-02 | Information Disclosure | ANTHROPIC_API_KEY in env.ts | mitigate | Key added to `server:` block only (never `client:`); `.optional()` so absence is a controlled 503 path, not a startup leak/crash |
| T-10-07 | Tampering | D-12 refactor regressing Phase 9 validation | mitigate | Phase 9's 8 `image-validation.test.ts` tests are an asserted regression gate in Task 2 acceptance criteria |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npx vitest run src/lib/image-validation.test.ts` → 8 passed (Phase 9 regression gate intact)
- `npx vitest run src/lib/image-constants.test.ts` → 3 passed
- `npx vitest run src/app/api/extract/__tests__/extract-eval.test.ts` → skipped, exit 0
</verification>

<success_criteria>
- ai@6.0.185 + @ai-sdk/anthropic@3.0.78 installed (exact pins)
- ANTHROPIC_API_KEY present in both env.ts blocks as optional
- image-constants.ts is the single source of ALLOWED_IMAGE_TYPES + MAX_IMAGE_BYTES; image-validation.ts imports from it; Phase 9 tests unchanged-green
- Route unit, reducer unit, and gated eval scaffolds exist; eval skeleton green-when-skipped
</success_criteria>

<output>
After completion, create `.planning/phases/10-vision-extraction-endpoint/10-01-SUMMARY.md`
</output>
</invoke>
