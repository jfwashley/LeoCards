---
phase: 10-vision-extraction-endpoint
plan: 03
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - src/components/image-upload-flow.tsx
autonomous: true
requirements: [EXT-02, EXT-03, EXT-04]
must_haves:
  truths:
    - "Clicking Extract shows an in-flight loading state and the request cannot be double-submitted (EXT-02)"
    - "A no-words result shows the 'No words found' message with a 'Choose another image' affordance (EXT-03)"
    - "Any error shows the correct per-HTTP-status friendly copy inline, with a 'Try again' affordance, and never loses file/previewUrl/selectedDeckId (EXT-04)"
    - "A successful extraction shows the transitional 'Found {N} word(s)' hand-off stub (EXT-01 hand-off — Phase 11 owns the review list)"
  artifacts:
    - path: "src/components/image-upload-flow.tsx"
      provides: "Extended reducer (5 new actions/3 new fields), exported imageFlowReducer, real handleExtract, friendlyErrorCopy, 5 client states"
      contains: "export function imageFlowReducer"
  key_links:
    - from: "src/components/image-upload-flow.tsx"
      to: "/api/extract"
      via: "fetch POST in handleExtract"
      pattern: "fetch\\(\"/api/extract\""
    - from: "handleExtract"
      to: "imageFlowReducer"
      via: "dispatch EXTRACT_START/SUCCESS/NO_WORDS/ERROR"
      pattern: "EXTRACT_START"
---

<objective>
Wire Phase 9's `handleExtract` no-op to the new `/api/extract` endpoint and extend the
existing `useReducer` state machine in `image-upload-flow.tsx` with the five extraction
states (idle/in-flight/success/no-words/error) per the LOCKED 10-UI-SPEC. This delivers
EXT-02 (loading + double-submit guard), EXT-03 (no-words UX), and EXT-04 (recoverable
error preserving selection). The success state is a transitional hand-off stub only —
Phase 11 owns the editable review list.

Purpose: Turns `extract-reducer.test.ts` (from 10-01) green and delivers the client UX.
Output: One modified component file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
- ./AGENTS.md
- src/components/image-upload-flow.tsx — the file being extended (Phase 9 reducer + handleExtract no-op at line ~127 + Step 2 render branch ~140–174)
- src/components/deck-switcher.tsx — `DeckOption` (has `id`, `name`, `language`; NO `targetLanguage`)
- .planning/phases/10-vision-extraction-endpoint/10-UI-SPEC.md — Interaction States (0–4), full Copywriting Contract (verbatim strings), Layout per state, Accessibility Contract — ALL COPY IS LOCKED, copy verbatim
- .planning/phases/10-vision-extraction-endpoint/10-PATTERNS.md — `src/components/image-upload-flow.tsx (edit)` section + `DeckOption Open Question — Resolution` (use `deck.language` directly) + `extract-reducer.test.ts` section (reducer export requirement)
- .planning/phases/10-vision-extraction-endpoint/10-RESEARCH.md — Section 4 (full handleExtract implementation, friendlyErrorCopy helper verbatim, AbortController 35s rationale, Pitfall 6 & 7)
- src/app/api/extract/__tests__/extract-reducer.test.ts (from 10-01 — this plan turns it green; read for exact asserted reducer behavior)
</read_first>

<interfaces>
Current reducer (src/components/image-upload-flow.tsx — extend, don't rewrite):
```typescript
interface ImageFlowState { step:"pick"|"deck"; file:File|null; previewUrl:string|null; pickError:string|null; selectedDeckId:string }
type ImageFlowAction = | {type:"FILE_PICKED";...} | {type:"FILE_ERROR";...} | {type:"CLEAR_FILE"} | {type:"ADVANCE_STEP"} | {type:"BACK_TO_PICK"} | {type:"SET_DECK";deckId:string}
function imageFlowReducer(state, action): ImageFlowState  // currently NOT exported
function handleExtract() {}  // line ~127 — Phase 9 no-op to replace
```
DeckOption (src/components/deck-switcher.tsx) — `language` IS already BCP-47 ("en"/"fr"/"es"):
```typescript
export interface DeckOption { id: string; name: string; language: string }
```
Server contract (from 10-02): `POST /api/extract { image, mimeType, deckId, targetLanguage }`
→ 200 `{ words: string[]; detectedLanguage? }` (words.length 0 = no-words) | non-2xx `{ error: string }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend reducer (export it) + add 3 state fields + 5 actions</name>
  <files>src/components/image-upload-flow.tsx</files>
  <read_first>
  - src/components/image-upload-flow.tsx lines 20–85 (state, action union, reducer, useReducer init)
  - .planning/phases/10-vision-extraction-endpoint/10-PATTERNS.md `image-upload-flow.tsx (edit)` section (exact new fields/actions/cases) + `extract-reducer.test.ts` section
  - .planning/phases/10-vision-extraction-endpoint/10-RESEARCH.md Section 4 reducer logic block
  </read_first>
  <behavior>
  - EXTRACT_START → extracting:true, extractError:null, extractWords:null
  - EXTRACT_SUCCESS{words} → extracting:false, extractWords:words
  - EXTRACT_NO_WORDS → extracting:false, extractWords:[] (array, NOT null — Pitfall 7)
  - EXTRACT_ERROR{status,message} → extracting:false, extractError:{status,message}; file/previewUrl/selectedDeckId UNTOUCHED (EXT-04/D-16)
  - EXTRACT_RETRY → identical to EXTRACT_START
  </behavior>
  <action>
  1. Add to `ImageFlowState`: `extracting: boolean; extractError: { status: number; message: string } | null; extractWords: string[] | null;`
  2. Add to `ImageFlowAction` union: `| { type:"EXTRACT_START" } | { type:"EXTRACT_SUCCESS"; words:string[] } | { type:"EXTRACT_NO_WORDS" } | { type:"EXTRACT_ERROR"; status:number; message:string } | { type:"EXTRACT_RETRY" }`
  3. Add the 5 `case` blocks before `default:` exactly per 10-PATTERNS.md / 10-RESEARCH.md Section 4 (spread-and-override style matching the existing cases; EXTRACT_ERROR must NOT spread-overwrite file/previewUrl/selectedDeckId).
  4. Change `function imageFlowReducer` → `export function imageFlowReducer` (required so `extract-reducer.test.ts` can import it — 10-PATTERNS.md note).
  5. Extend the `useReducer` initial-state object (lines ~79–85) with `extracting: false, extractError: null, extractWords: null`.
  Do NOT alter existing cases or Phase 9 fields.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/extract/__tests__/extract-reducer.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep "export function imageFlowReducer" src/components/image-upload-flow.tsx` matches
  - `grep -c "EXTRACT_START\|EXTRACT_SUCCESS\|EXTRACT_NO_WORDS\|EXTRACT_ERROR\|EXTRACT_RETRY" src/components/image-upload-flow.tsx` returns ≥ 10 (union + cases)
  - `npx vitest run src/app/api/extract/__tests__/extract-reducer.test.ts` — all reducer cases pass incl. the EXT-04 file/previewUrl/selectedDeckId preservation assertion
  - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Reducer exported and extended with 3 fields + 5 actions; reducer unit test fully green; Phase 9 cases untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Implement handleExtract (fetch + abort) + friendlyErrorCopy + render the 5 states</name>
  <files>src/components/image-upload-flow.tsx</files>
  <read_first>
  - src/components/image-upload-flow.tsx lines 1–12 (imports), 126–174 (handleExtract no-op + Step 2 render)
  - .planning/phases/10-vision-extraction-endpoint/10-UI-SPEC.md — Interaction States 1–4, Layout per state, Copywriting Contract (every string verbatim), Accessibility Contract
  - .planning/phases/10-vision-extraction-endpoint/10-RESEARCH.md Section 4 — handleExtract full implementation + friendlyErrorCopy verbatim + Pitfall 6/7
  - .planning/phases/10-vision-extraction-endpoint/10-PATTERNS.md `image-upload-flow.tsx (edit)` (handleExtract bullet spec) + `DeckOption Open Question — Resolution`
  </read_first>
  <action>
  1. Extend the lucide import (line 3) to: `import { ArrowLeft, Loader2, ImageOff, AlertCircle, X } from "lucide-react";`
  2. Replace `function handleExtract() {}` with the async implementation per 10-RESEARCH.md Section 4 verbatim:
     - Guard: `if (!state.file || !state.selectedDeckId || state.extracting) return;` then `dispatch({ type:"EXTRACT_START" })`.
     - Read file as data-URL via `FileReader.readAsDataURL` wrapped in a Promise.
     - `const deck = decks.find(d => d.id === state.selectedDeckId); const targetLanguage = deck?.language ?? "fr";` — USE `deck.language` (it is already BCP-47; DeckOption has NO `targetLanguage` — per 10-PATTERNS resolution; do NOT modify deck-switcher.tsx or DeckOption).
     - `AbortController` with `setTimeout(() => controller.abort(), 35_000)` (35s client > 30s server so the server's clean 504 wins — 10-RESEARCH A4).
     - `fetch("/api/extract", { method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify({ image: dataUrl, mimeType: state.file.type, deckId: state.selectedDeckId, targetLanguage }), signal: controller.signal })`.
     - `res.ok`: parse `{ words, detectedLanguage? }`; `words.length === 0` ⇒ `EXTRACT_NO_WORDS` else `EXTRACT_SUCCESS{words}`.
     - non-2xx: `const data = await res.json().catch(() => ({error:"Unknown error"}))`; `EXTRACT_ERROR{ status: res.status, message: data.error }`.
     - catch: `AbortError` ⇒ `EXTRACT_ERROR{ status:504, ... }`; else `{ status:0, ... }`.
     - `finally { clearTimeout(timeoutId); }`.
  3. Add a `friendlyErrorCopy(status: number): string` module-level function with the EXACT switch from 10-RESEARCH.md Section 4 / 10-UI-SPEC Copywriting Contract — all 9 strings VERBATIM (429/413/415/504/503/502+500/400/401/default). Do not paraphrase.
  4. Rewrite the `state.step === "deck"` render branch to render the 5 states per 10-UI-SPEC Interaction States + Layout (recap thumbnail unchanged across all):
     - In-flight (`state.extracting`): DeckSwitcher `disabled`; Back button hidden; button disabled, content `<Loader2 className="size-4 animate-spin mr-2" aria-hidden="true" /> Extracting words…`, `aria-busy="true"` + `aria-label="Extracting words, please wait"`; optional below-button `<p className="text-sm text-muted-foreground text-center">This can take up to 30 seconds…</p>`.
     - No-words (`state.extractWords` is `[]` — Array.isArray && length 0; NOT null — Pitfall 7): hide the Extract button; render the centered block with `ImageOff` (size-8 text-muted-foreground), `<p className="text-sm font-medium text-foreground">No words found in this image.</p>`, `<p className="text-sm text-muted-foreground">Try a photo with clearer text, or choose a different image.</p>`, and `<Button variant="outline">Choose another image</Button>` whose onClick dispatches `BACK_TO_PICK` then `CLEAR_FILE` (clears file; deck preserved).
     - Error (`state.extractError`): keep recap thumbnail + Label + DeckSwitcher (interactive) + Back button restored; render `<p role="alert" className="text-sm text-destructive mt-1">{friendlyErrorCopy(state.extractError.status)}</p>` then `<Button variant="outline">Try again</Button>` (onClick dispatches `EXTRACT_RETRY` then re-invokes handleExtract); hide the Extract button while error shown.
     - Success (`state.extractWords` non-empty): hide Label + DeckSwitcher; show recap thumbnail, `<p className="text-sm text-muted-foreground">Found {N} word{N !== 1 ? "s" : ""} — ready to review.</p>`, and a disabled `variant="default" w-full h-11` button labelled `Review words →` (Phase 11 wires the handler — leave disabled, do NOT navigate).
     - Idle (none of the above): the existing Phase 9 markup unchanged (Label, DeckSwitcher, Back, `Extract words` button `disabled={!state.file || !state.selectedDeckId}` onClick handleExtract).
  Render precedence: extracting → error → no-words(`[]`) → success(non-empty) → idle. SCOPE GUARD: the success state is a stub only — do NOT build the editable review list, do NOT add words to a deck, do NOT call DeepL (all Phase 11).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run src/app/api/extract/__tests__/extract-reducer.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep "fetch(\"/api/extract\"" src/components/image-upload-flow.tsx` matches
  - `grep "deck?.language ?? \"fr\"\|deck?.language" src/components/image-upload-flow.tsx` matches; `grep -c "targetLanguage:" src/components/deck-switcher.tsx` returns `0` (DeckOption NOT modified)
  - All 9 verbatim error strings present: `grep -c "please wait a moment and try again\|too large for the server\|isn't supported\|took too long and timed out\|isn't available right now\|went wrong with the word extraction\|problem with the request\|session has expired\|Something went wrong. Please try again" src/components/image-upload-flow.tsx` returns ≥ 9
  - `grep "No words found in this image.\|Try a photo with clearer text, or choose a different image.\|Choose another image\|This can take up to 30 seconds…\|Extracting words…\|Review words →" src/components/image-upload-flow.tsx` — all present
  - `grep "role=\"alert\"" src/components/image-upload-flow.tsx` and `grep "aria-busy" src/components/image-upload-flow.tsx` match (Accessibility Contract)
  - `grep "35_000\|35000" src/components/image-upload-flow.tsx` and `grep "clearTimeout" src/components/image-upload-flow.tsx` match
  - `npm run typecheck` and `npm run lint` exit 0; reducer test still green
  </acceptance_criteria>
  <done>handleExtract POSTs to /api/extract with abort handling; friendlyErrorCopy has all 9 locked strings verbatim; all 5 UI-SPEC states render with locked copy + a11y attributes; success is a non-navigating stub; DeckOption untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/extract | Client sends base64 image + deckId; all enforcement is server-side (10-02) — client guards are UX only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Denial of Service | rapid repeated Extract clicks (cost amplification) | mitigate | Client double-submit guard: `disabled={state.extracting}` (+ `pointer-events-none` via buttonVariants) AND early `if (state.extracting) return` in handleExtract (D-15). Server rate limiter (10-02 T-10-06) is the authoritative backstop — client guard is defense-in-depth, not the security control. |
| T-10-12 | Information Disclosure | rendering raw server error body to the user | mitigate | UI shows only `friendlyErrorCopy(status)` keyed off HTTP status (D-14) — the server `{ error }` string is never displayed; no status codes/jargon surfaced |
</threat_model>

<verification>
- `npx vitest run src/app/api/extract/__tests__/extract-reducer.test.ts` — green
- `npm run typecheck` and `npm run lint` exit 0
- `npm test` full suite green (no cross-regression with Phase 9 / 10-01 / 10-02)
</verification>

<success_criteria>
- Extract click → in-flight loading state, button disabled, no double-submit (EXT-02)
- No-words → locked "No words found" block + "Choose another image" (EXT-03)
- Error → correct per-status locked copy inline + "Try again", file/previewUrl/selectedDeckId preserved (EXT-04)
- Success → transitional "Found {N} word(s)" stub, no Phase 11 behavior leaked in
</success_criteria>

<output>
After completion, create `.planning/phases/10-vision-extraction-endpoint/10-03-SUMMARY.md`
</output>
