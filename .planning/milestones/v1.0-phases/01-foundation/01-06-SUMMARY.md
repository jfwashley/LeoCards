---
phase: 01-foundation
plan: 06
subsystem: infra
tags: [ci, github-actions, biome, deepl, npm-audit, env]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: Next.js app scaffold, env.ts Zod validation, auth, db, all foundation features
provides:
  - CI pipeline with correct step ordering (Build before Type check)
  - DEEPL_API_KEY documented in .env.example and wired into CI secrets
  - Biome version pinned to match project biome.json schema
  - biome ci scoped to src/ to avoid scanning node_modules and worktrees
affects: [all phases — CI gates every PR]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Build step runs before tsc — next-env.d.ts must exist before type checking"
    - "biome ci scoped to src/ — prevents false failures from generated/vendor paths"

key-files:
  created: []
  modified:
    - .github/workflows/ci.yml
    - .env.example
    - package-lock.json

key-decisions:
  - "Build before Type check in CI — next-env.d.ts and .next/types/ generated by build are required by tsc"
  - "npm audit fix without --force — esbuild vulnerability in drizzle-kit requires breaking downgrade, deferred"

patterns-established:
  - "DEEPL_API_KEY must be present in both CI secrets block and .env.example"

requirements-completed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

# Metrics
duration: 10min
completed: 2026-03-28
---

# Phase 01 Plan 06: CI Pipeline Fix and .env.example Completion Summary

**CI step order corrected (Build before tsc), DEEPL_API_KEY added to CI env block and .env.example, Biome pinned to 2.4.8 and scoped to src/**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-28T23:50:05Z
- **Completed:** 2026-03-28T23:58:00Z
- **Tasks:** 1
- **Files modified:** 3

## Accomplishments

- Reordered CI steps so Build runs before Type check — tsc needs `next-env.d.ts` and `.next/types/` which are only generated by the build step
- Added `DEEPL_API_KEY: ${{ secrets.DEEPL_API_KEY }}` to the Build step's env block in CI
- Pinned `biomejs/setup-biome@v2` to `version: 2.4.8` matching the project's biome.json `$schema` version
- Scoped `biome ci src/` to prevent Biome from scanning node_modules, .claude/worktrees, or other non-source paths
- Added `DEEPL_API_KEY` entry to `.env.example` under a `# Translation (DeepL)` comment for developer onboarding
- Ran `npm audit fix` — non-breaking vulnerabilities addressed; esbuild issue in drizzle-kit requires `--force` (breaking change, deferred)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix CI pipeline and .env.example** - `0ffa924` (chore)

**Plan metadata:** _(created after this summary)_

## Files Created/Modified

- `.github/workflows/ci.yml` - Corrected step order, added DEEPL_API_KEY, pinned Biome version, scoped biome ci
- `.env.example` - Added DEEPL_API_KEY entry for developer onboarding
- `package-lock.json` - Updated by npm audit fix

## Decisions Made

- Build before Type check: Next.js build generates `next-env.d.ts` and `.next/types/` which tsc requires; type checking before build causes false CI failures
- `npm audit fix` without `--force`: The remaining esbuild moderate vulnerability is inside drizzle-kit and can only be fixed with a breaking downgrade to drizzle-kit 0.18.1; deferred

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `npm audit fix` could not address the esbuild vulnerability in drizzle-kit without `--force` (which would break drizzle-kit). This matches the plan's explicit instruction to not use `--force`. The 4 moderate-severity vulnerabilities in the drizzle-kit transitive dependency tree remain and are noted for future action.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CI pipeline is now correctly ordered and will pass on PRs when secrets are configured in GitHub
- .env.example is complete with all 5 required env vars (DATABASE_URL, BETTER_AUTH_SECRET, RESEND_API_KEY, NEXT_PUBLIC_APP_URL, DEEPL_API_KEY)
- New developers can onboard without hitting missing env var Zod crashes
- Phase 01-foundation is complete

---
*Phase: 01-foundation*
*Completed: 2026-03-28*

## Self-Check: PASSED

- FOUND: .github/workflows/ci.yml
- FOUND: .env.example
- FOUND: commit 0ffa924 (chore(01-06): fix CI pipeline, add DEEPL_API_KEY, run npm audit fix)
- FOUND: .planning/phases/01-foundation/01-06-SUMMARY.md
